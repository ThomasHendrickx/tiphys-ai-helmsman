{
  "id": "kernel-0-2-1-validate-invalid-still-exits-1",
  "behavior": "validate-invalid-still-exits-1",
  "tests": [
    "a verdict with a real INVALID line still exits 1 without a context, whether the schema or a derived check found it"
  ],
  "class": "classification",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/commands/validate.ts",
      "find": "  return checks.violated ? 1 : 0;",
      "replace": "  return 0;"
    },
    {
      "kind": "mutation",
      "file": "src/commands/validate.ts",
      "find": "     work history records. A real violation still exits 1. */\n  return checks.violated ? 1 : 0;",
      "replace": "     work history records. A real violation still exits 1. */\n  return checks.violated && !checks.lines.some((line) => line.startsWith(\"SKIPPED\")) ? 1 : 0;"
    }
  ],
  "deterministic": true,
  "repeats": 2
}
