{
  "id": "m5-p3-review-pair-arms",
  "behavior": "m5-p3-review-pair-arms",
  "tests": [
    "a refusing pair, a shared family, a shared framing and a shared contract are each red through the real runner",
    "an approving pair that names an ancestor whose gap adds shipped bytes is red, and names the exclusion",
    "an approving decorrelated pair over the reviewed shipped tree is green through the real runner"
  ],
  "class": "classification",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "  return shipped.length === 0\n    ? { kind: \"evidence-only-ancestor\", changed }",
      "replace": "  return true\n    ? { kind: \"evidence-only-ancestor\", changed }"
    },
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "        } else if (raw !== APPROVING_VERDICT) {",
      "replace": "        } else if (false) {"
    }
  ],
  "deterministic": true,
  "repeats": 2
}
