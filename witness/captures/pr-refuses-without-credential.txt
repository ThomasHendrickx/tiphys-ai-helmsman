# Real output of the plugin pull-request commands with NO credential in the
# environment, taken 2026-09-18 on node v26.6.0 from the repository
# root. Nothing below is edited: each block is the child process argv, its stderr
# verbatim, its stdout, and its exit code. test/next.test.ts parses this file and
# requires a live run to reproduce it, so the refusal is anchored to a recorded
# run of the program rather than to a string chosen to match the implementation.

$ node plugin/src/pr-main.ts open --repo owner/name --number 1
stderr: "pr open: no pull-request credential is present: none of GH_TOKEN, GITHUB_TOKEN, GH_ENTERPRISE_TOKEN, GITHUB_ENTERPRISE_TOKEN carries a value, so this command has no authority to act and refuses rather than proceeding\n"
stdout: ""
exit: 65

$ node plugin/src/pr-main.ts merge --repo owner/name --number 1
stderr: "pr merge: no pull-request credential is present: none of GH_TOKEN, GITHUB_TOKEN, GH_ENTERPRISE_TOKEN, GITHUB_ENTERPRISE_TOKEN carries a value, so this command has no authority to act and refuses rather than proceeding\n"
stdout: ""
exit: 65

