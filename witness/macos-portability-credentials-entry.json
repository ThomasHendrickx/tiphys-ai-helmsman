{
  "id": "macos-portability-credentials-entry",
  "behavior": "macos-credentials-direct-entry-alias",
  "tests": [
    "credentials direct entry runs through an aliased path and writes its result"
  ],
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/gates/credentials.ts",
      "find": "  const isMain = pathsIdentifySameObject(fileURLToPath(import.meta.url), entry);",
      "replace": "  const isMain = fileURLToPath(import.meta.url) === entry;"
    }
  ],
  "class": "additive",
  "deterministic": true,
  "repeats": 2,
  "consumesExternalOutput": {
    "program": "/usr/bin/git config --get-all credential.helper",
    "captures": ["witness/captures/macos-apple-git-prefix-helper.txt"],
    "provenance": "The changed credentials gate spawns Git and gh probes, so rule (f) binds this witness. The credential test source reads the real Apple Git prefix-helper capture and requires the selected Apple Git to reproduce it before exercising the scrubbed child. The alias mutation must independently make the named direct-entry test red."
  }
}
