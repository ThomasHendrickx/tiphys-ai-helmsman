{
  "id": "checks-code-block-content-not-quotable",
  "behavior": "mode-conditions-code-block-content-not-quotable",
  "tests": [
    "code block content in the cited record is not a quotable unit, in the fenced form and in the indented form"
  ],
  "class": "additive",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "    if (child.type === \"paragraph\") {\n      const unit = carriesProse(child)",
      "replace": "    if (child.type === \"paragraph\" || child.type === \"code_block\") {\n      const unit = child.type === \"code_block\" || carriesProse(child)"
    },
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "    } else if (!NOT_QUOTABLE.has(child.type)) {\n      collectUnits(child, lines, units, quoteDepth);\n    }",
      "replace": "    } else {\n      const leaked = normalizeProse(sourceSlice(lines, child.sourcepos, quoteDepth));\n      if (leaked !== \"\") {\n        units.add(leaked);\n      }\n    }"
    }
  ],
  "deterministic": true,
  "repeats": 2
}
