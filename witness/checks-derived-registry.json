{
  "id": "checks-derived-registry",
  "behavior": "check-plan-verification-first-present",
  "tests": [
    "an unverified claim whose owning phase has no verification-first step is rejected naming the check, and is accepted with the check deregistered",
    "an addressed-by naming a criterion that does not exist, and one deferring to a phase that does not exist, are each rejected naming the check"
  ],
  "class": "additive",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "      if (!hasVerificationFirst) {",
      "replace": "      if (false) {"
    },
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "          if (!criterionIds.has(criterionId)) {",
      "replace": "          if (false) {"
    }
  ],
  "deterministic": true,
  "repeats": 2
}
