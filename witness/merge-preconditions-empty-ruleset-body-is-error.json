{
  "id": "merge-preconditions-empty-ruleset-body-is-error",
  "behavior": "merge-preconditions-empty-ruleset-body-is-error",
  "tests": [
    "an empty body from the ruleset API makes the gate report error rather than a default, where a mutant that defaults to protected reports green",
    "a zero-length response body is a failure with a reason naming the emptiness, never a parsed default"
  ],
  "class": "classification",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/gates/merge-preconditions.ts",
      "find": "  if (response.body.trim() === \"\") {\n    return {\n      ok: false,",
      "replace": "  if (response.body.trim() === \"\") {\n    return { ok: true, value: [{ id: 0, name: \"assumed\", target: \"branch\", enforcement: \"active\", rules: [{ type: \"required_status_checks\", parameters: { required_status_checks: [{ context: \"gates\" }] } }] }] };\n    return {\n      ok: false,"
    },
    {
      "kind": "mutation",
      "file": "src/gates/merge-preconditions.ts",
      "find": "  if (response.status < 200 || response.status > 299) {",
      "replace": "  if (false) {"
    }
  ],
  "deterministic": true,
  "repeats": 2,
  "consumesExternalOutput": {
    "program": "GitHub REST API (api.github.com)",
    "captures": [
      "witness/captures/m4-p12-github-check-runs.json",
      "witness/captures/m4-p12-github-rulesets-list.json",
      "witness/captures/m4-p12-github-ruleset-detail.json"
    ],
    "provenance": "Captured 2026-09-17 from the live GitHub REST API with curl and $GH_TOKEN, against ThomasHendrickx/tiphys-ai-helmsman: GET /repos/{slug}/commits/713731ff771afa2555b7850b53648923f36cde0c/check-runs (HTTP 200), GET /repos/{slug}/rulesets?includes_parents=true (HTTP 200), and GET /repos/{slug}/rulesets/20446760 (HTTP 200). The reachability probe and its deliberately-invalid-token control are recorded in delivery/work-history/m4-p12.md."
  }
}
