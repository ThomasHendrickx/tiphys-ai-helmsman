{
  "id": "project-write-block-git-carve-out-is-segment-wise",
  "behavior": "project-write-block-git-carve-out-is-segment-wise",
  "tests": [
    "the .git carve-out is segment-wise and does not cover gitignore or a github directory"
  ],
  "class": "classification",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "plugin/src/hooks/project-write-block.ts",
      "find": "  if (!isGitInternal(root, path)) {\n    return false;\n  }\n",
      "replace": "  if (!isGitInternal(root, path)) {\n    return relative(root, path).includes(\".git\");\n  }\n"
    },
    {
      "kind": "mutation",
      "file": "plugin/src/hooks/project-write-block.ts",
      "find": "  if (isGitRefSurface(root, target)) {\n",
      "replace": "  if (isGitRefSurface(root, target) || target.includes(\".git\")) {\n"
    }
  ],
  "deterministic": true,
  "repeats": 2
}
