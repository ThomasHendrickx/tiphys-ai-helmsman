{
  "id": "kernel-0-2-1-admission-requires-no-stamp",
  "behavior": "admission-ignores-stamp-admits-0-2-0-verdict",
  "tests": [
    "a real-shaped 0.2.0 verdict pair, with a head and no stamp, is admitted by check-dual-review and merge-preconditions, and so is the same pair stamped old or current"
  ],
  "class": "classification",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "    if (declaresNoHead(candidate.record)) {",
      "replace": "    if (declaresNoHead(candidate.record) || !(\"tiphys-version\" in (candidate.record ?? {}))) {"
    },
    {
      "kind": "mutation",
      "file": "src/gates/merge-preconditions.ts",
      "find": "    if (declaresNoHead(entry.record)) {",
      "replace": "    if (declaresNoHead(entry.record) || !(\"tiphys-version\" in entry.record)) {"
    }
  ],
  "deterministic": true,
  "repeats": 2,
  "consumesExternalOutput": {
    "program": "git",
    "captures": [
      "witness/captures/kernel-0-2-1-history-git.json"
    ],
    "provenance": "Real output of git diff -z --no-renames --name-only base...head (the name list classifyReviewBudget reads), captured 2026-09-23 with git 2.43.0 against the scratch repositories test/history-compat.test.ts stages; the named tests stage them again and compare git's live output with the recorded stdout, commit shas spelled by staging name, before trusting either gate."
  }
}
