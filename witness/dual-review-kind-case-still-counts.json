{
  "id": "dual-review-kind-case-still-counts",
  "behavior": "dual-review-kind-case-still-counts",
  "class": "classification",
  "deterministic": true,
  "repeats": 2,
  "tests": [
    "a verdict whose kind is written in another case still counts toward the group it correlates with"
  ],
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "  if (reading.value !== \"verdict\") {\n    return { kind: \"other\" };\n  }\n  return { kind: \"verdict\", record };",
      "replace": "  if (record[\"kind\"] !== \"verdict\") {\n    return { kind: \"other\" };\n  }\n  return { kind: \"verdict\", record };"
    },
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "  return { ok: true, value: collapsed.toLowerCase() };",
      "replace": "  return { ok: true, value: collapsed };"
    }
  ]
}
