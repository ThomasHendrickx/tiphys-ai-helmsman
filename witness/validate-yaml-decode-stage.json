{
  "id": "validate-yaml-decode-stage",
  "behavior": "validate-yaml-decode-diagnostic",
  "tests": [
    "malformed YAML produces one concise diagnostic and a nonzero exit"
  ],
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/validate.ts",
      "find": "      return {\n        ok: false,\n        reason: `${label} is not valid YAML${at}: ${singleLineReason(error.message)}`,\n      };",
      "replace": "      throw error;"
    },
    {
      "kind": "mutation",
      "file": "src/validate.ts",
      "find": "  return { ok: true, value };",
      "replace": "  return { ok: false, reason: `${label} could not be decoded` };"
    }
  ],
  "class": "additive",
  "deterministic": true,
  "repeats": 2
}
