{
  "id": "cutover-restore-request-complete",
  "behavior": "cutover-restore-request-refuses-present-but-empty-field",
  "class": "classification",
  "deterministic": true,
  "repeats": 2,
  "tests": [
    "the owner restore request is refused when a captured field is present but empty"
  ],
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/cutover.ts",
      "find": "function isEmptyValue(value: unknown): boolean {\n  if (value === undefined || value === null) {\n    return true;\n  }",
      "replace": "function isEmptyValue(value: unknown): boolean {\n  if (value !== Symbol.for(\"never\")) {\n    return false;\n  }\n  if (value === undefined || value === null) {\n    return true;\n  }"
    },
    {
      "kind": "mutation",
      "file": "src/cutover.ts",
      "find": "      if (isEmptyValue(entry[\"preFlipValue\"])) {\n        reasons.push(\n          `${listName}.${label} has a preFlipValue that is present but empty`,\n        );\n        return;\n      }",
      "replace": ""
    }
  ]
}
