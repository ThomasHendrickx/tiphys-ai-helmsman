{
  "id": "kernel-0-2-1-stamp-old-excluded-at-merge-preconditions",
  "behavior": "stamp-old-excluded-at-merge-preconditions",
  "tests": [
    "an old-stamped verdict is excluded by name at merge-preconditions and never counts toward the two reviews"
  ],
  "class": "classification",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/gates/merge-preconditions.ts",
      "find": "    const stampProblem = admissionStampProblem(entry.record, running);",
      "replace": "    const stampProblem = undefined;"
    },
    {
      "kind": "mutation",
      "file": "src/stamp.ts",
      "find": "  if (version.major < running.major || (version.major === running.major && version.minor < running.minor)) {",
      "replace": "  if (false) {"
    }
  ],
  "deterministic": true,
  "repeats": 2,
  "consumesExternalOutput": {
    "program": "git",
    "captures": [
      "witness/captures/kernel-0-2-1-history-git.json"
    ],
    "provenance": "Real output of git diff -z --no-renames --name-only base...head (the name list classifyReviewBudget reads) and git log --reverse --topo-order --full-history --format=%H %P ref -- ./charter.yaml (the log firstDeclarationCommit reads), captured 2026-09-23 with git 2.43.0 against the scratch repositories test/history-compat.test.ts stages; the named tests stage them again and compare git's live output with the recorded stdout, commit shas spelled by staging name, before trusting the gate."
  }
}
