{
  "id": "macos-portability-suite-entry",
  "behavior": "macos-suite-direct-entry-alias",
  "tests": [
    "suite direct entry runs through an aliased path and writes its result"
  ],
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/gates/suite.ts",
      "find": "  pathsIdentifySameObject(fileURLToPath(import.meta.url), process.argv[1]);",
      "replace": "  fileURLToPath(import.meta.url) === process.argv[1];"
    },
    {
      "kind": "mutation",
      "file": "src/gates/suite.ts",
      "find": "import { fileURLToPath } from \"node:url\";",
      "replace": "import { pathToFileURL as fileURLToPath } from \"node:url\";"
    }
  ],
  "class": "additive",
  "deterministic": true,
  "repeats": 2,
  "consumesExternalOutput": {
    "program": "node --test --test-reporter tap",
    "captures": ["witness/captures/node-test-tap-real.txt"],
    "provenance": "The changed suite gate is a spawning and parsing module, so rule (f) binds this witness. The red-witness harness executes the named direct-entry test with Node's pinned TAP reporter and parses that stream. The named test source references the repository's real pass-and-fail TAP capture; the mutation must still produce a live red result."
  }
}
