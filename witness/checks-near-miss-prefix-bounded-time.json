{
  "id": "checks-near-miss-prefix-bounded-time",
  "behavior": "mode-conditions-near-miss-prefix-bounded-time",
  "tests": [
    "a long near-miss block prefix is rejected in bounded time, for a bullet run and for an ordered run"
  ],
  "class": "additive",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "function isSkippablePrefix(span: string): boolean {\n  let at = 0;\n  for (;;) {\n    const quote = quoteMarkerLength(span, at);\n    if (quote > 0) {\n      at += quote;\n      continue;\n    }\n    const list = listMarkerLength(span, at);\n    if (list > 0) {\n      at += list;\n      continue;\n    }\n    while (isIndent(span.charCodeAt(at))) {\n      at += 1;\n    }\n    return at === span.length;\n  }\n}\n",
      "replace": "function isSkippablePrefix(span: string): boolean {\n  return /^(?:[ \\t]*(?:>[ \\t]?|(?:[0-9]{1,9}[.)]|[-*+])[ \\t]*))*[ \\t]*$/.test(span);\n}\n"
    },
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "function isSkippablePrefix(span: string): boolean {\n  let at = 0;\n  for (;;) {\n    const quote = quoteMarkerLength(span, at);\n    if (quote > 0) {\n      at += quote;\n      continue;\n    }\n    const list = listMarkerLength(span, at);\n    if (list > 0) {\n      at += list;\n      continue;\n    }\n    while (isIndent(span.charCodeAt(at))) {\n      at += 1;\n    }\n    return at === span.length;\n  }\n}\n",
      "replace": "function isSkippablePrefix(span: string): boolean {\n  return /^(?:[ \\t]*(?:>|[-*+]|[0-9]{1,9}[.)])[ \\t]*)*[ \\t]*$/.test(span);\n}\n"
    }
  ],
  "deterministic": true,
  "repeats": 2
}
