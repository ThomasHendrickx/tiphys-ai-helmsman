{
  "id": "cutover-command-refuses-freeze-point",
  "behavior": "cutover-rollback-refuses-freeze-point-restore-as-one-command",
  "class": "classification",
  "deterministic": true,
  "repeats": 2,
  "tests": [
    "cutover rollback refuses freeze-point-restore as a single command"
  ],
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/commands/cutover.ts",
      "find": "  if (triggerValue === \"freeze-point-restore\") {\n    return fail(",
      "replace": "  if (triggerValue === \"freeze-point-restore\" && false) {\n    return fail("
    },
    {
      "kind": "mutation",
      "file": "src/commands/cutover.ts",
      "find": "        `restore-request for the authority half, which is an OWNER action because ${UNREHEARSABLE_REASON}`,\n      1,\n    );",
      "replace": "        `restore-request for the authority half, which is an OWNER action because ${UNREHEARSABLE_REASON}`,\n      EX_USAGE,\n    );"
    }
  ]
}
