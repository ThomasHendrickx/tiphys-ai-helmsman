{
  "id": "kernel-0-2-1-headless-verdict-never-admitted",
  "behavior": "verdict-head-required",
  "tests": [
    "a dual-tier change whose only reviews declare no head is red at check-dual-review, and each is excluded by name",
    "merge-preconditions excludes a verdict that declares no head by name and never counts it toward the two reviews"
  ],
  "class": "classification",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "      offHead.push({ path: candidate.path, declared: \"\", relation: { kind: \"no-head\" } });",
      "replace": "      onHead.push(candidate);\n      admitted.push({ path: candidate.path, declared: \"\", relation: { kind: \"same\" } });"
    },
    {
      "kind": "mutation",
      "file": "src/gates/merge-preconditions.ts",
      "find": "      excluded.push({ path: entry.path, declared: \"\", relation: { kind: \"no-head\" } });",
      "replace": "      admitted.push({ path: entry.path, declared: \"\", relation: { kind: \"same\" } });\n      forHead.push({ path: entry.path, record: entry.record });"
    },
    {
      "kind": "mutation",
      "file": "src/checks.ts",
      "find": "  return record === undefined || !(\"head\" in record);",
      "replace": "  return false;"
    }
  ],
  "deterministic": true,
  "repeats": 2,
  "consumesExternalOutput": {
    "program": "git",
    "captures": [
      "witness/captures/kernel-0-2-1-history-git.json"
    ],
    "provenance": "Real output of git diff -z --no-renames --name-only base...head (the name list classifyReviewBudget reads) and git log --reverse --topo-order --full-history --format=%H %P ref -- ./charter.yaml (the log firstDeclarationCommit reads), captured 2026-09-23 with git 2.43.0 against the scratch repositories test/history-compat.test.ts stages; the named tests stage them again and compare git's live output with the recorded stdout, commit shas spelled by staging name, before trusting the gate."
  }
}
