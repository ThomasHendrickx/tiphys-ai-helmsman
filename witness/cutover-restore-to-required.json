{
  "id": "cutover-restore-to-required",
  "behavior": "cutover-rollback-refuses-switch-without-restore-to",
  "class": "classification",
  "deterministic": true,
  "repeats": 2,
  "tests": [
    "a switch record with no restore-to is refused and nothing is written"
  ],
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/cutover.ts",
      "find": "    if (!isSwitchState(row[\"restoreTo\"])) {\n      reasons.push(`switch ${name} has no restoreTo of current or kernel`);\n    }",
      "replace": "    if (!isSwitchState(row[\"restoreTo\"])) {\n      row[\"restoreTo\"] = \"current\";\n    }"
    },
    {
      "kind": "mutation",
      "file": "src/cutover.ts",
      "find": "  const reasons = validateCutoverDocument(parsed);\n  if (reasons.length > 0) {",
      "replace": "  const reasons = validateCutoverDocument(parsed);\n  if (reasons.length > 99) {"
    }
  ]
}
