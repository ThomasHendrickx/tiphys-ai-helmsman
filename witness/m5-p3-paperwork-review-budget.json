{
  "id": "m5-p3-paperwork-review-budget",
  "behavior": "m5-p3-paperwork-review-budget",
  "tests": [
    "a delivery-only change is not forced through the two-verdict rule and names its tier",
    "merge-preconditions through the real runner is not-applicable for a delivery-only change, and an approving pair reaches the network conditions"
  ],
  "class": "classification",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/gates/merge-preconditions.ts",
      "find": "      return { tier: row.tier, source: row.source };",
      "replace": "      return { tier: \"dual\", source: row.source };"
    },
    {
      "kind": "mutation",
      "file": "src/gates/merge-preconditions.ts",
      "find": "    if (budget.tier !== \"dual\") {",
      "replace": "    if (false) {"
    }
  ],
  "deterministic": true,
  "repeats": 2
}
