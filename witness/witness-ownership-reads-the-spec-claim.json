{
  "id": "witness-ownership-reads-the-spec-claim",
  "behavior": "witness-ownership-reads-the-spec-claim",
  "tests": [
    "rewriting a witness spec's claim imposes rule (d) on every member, and changing a field that is not the claim imposes nothing"
  ],
  "class": "additive",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "src/witness/spec.ts",
      "find": "  if (claimRePointed(headSpec, baselineSpec)) {",
      "replace": "  if (false) {"
    },
    {
      "kind": "mutation",
      "file": "src/witness/spec.ts",
      "find": "  const headTests = new Set(headSpec.tests);\n  return baselineSpec.tests.some((name) => !headTests.has(name));",
      "replace": "  return false;"
    }
  ],
  "deterministic": true,
  "repeats": 2
}
