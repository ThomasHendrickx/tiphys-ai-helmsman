{
  "id": "license-gate-covers-runtime-dependency-tree",
  "behavior": "license-gate-covers-runtime-dependency-tree",
  "tests": [
    "the inventory is the transitive production set, compared by name against what M3-P1 recorded",
    "skipping ONE package out of several IS a vacuous green, and the recorded-set comparison is what catches it"
  ],
  "class": "classification",
  "dangerousStates": [
    {
      "kind": "mutation",
      "file": "scripts/license-gate.mjs",
      "find": "    for (const next of Object.keys(meta.dependencies ?? {})) {\n      visit(next, name);\n    }",
      "replace": "  "
    },
    {
      "kind": "mutation",
      "file": "scripts/license-gate.mjs",
      "find": "  const unexpected = [...found.keys()].filter((name) => !expected.has(name)).sort();",
      "replace": "  const unexpected = [];"
    }
  ],
  "deterministic": true,
  "repeats": 2
}
