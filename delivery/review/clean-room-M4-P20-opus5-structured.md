# M4-P20 clean-room review by claude-opus-5 (Opus 5)

**VERDICT: APPROVE**

## framing

Evidence integrity and data loss combined: I treated every claim as unproven until I re-ran it, and I assumed the change could destroy work until I found the place where it can.

## derivation_honesty

The not-covered statements are HONEST and unusually specific: seven items in the work history, six in the evidence document, each naming what was excluded and why rather than "further work is possible". Two judgements a reader should not have to infer. (1) One item is BETTER than it sounds: item 7 admits the gated-witness decision rests on a READING of src/gates/suite.ts rather than a run, but the repository already executes that exact property in test/suite-gate.test.ts ("a failing test makes the suite gate red naming it"), which I watched PASS in my own full-suite run at 6aea774. The reading was correct and confirmable at zero cost. (2) One item is WORSE than it sounds, and it is my finding CR-M4P20-003. The `grep -rn "skip:" test/*.ts` derivation admits its SCOPE gap (top level only, literal `skip:`), and I checked that admitted gap: it is nominal, because `find test -mindepth 2 -name '*.ts'` returns ZERO files, so test/*.ts and test/** are the same set here. What the item does NOT admit is that the published OUTPUT is incomplete. It is labelled "Full output, sixteen lines"; the real output is 24 lines at head and 21 at the merge base, and test/watcher.test.ts:1401 is dropped with no elision marker, no compression label and no mention, while its sibling at :1330 IS published. That is a larger problem than the scope gap it does admit, and no gate can see it because the citations gate does not lint work histories.

## what_i_tried_to_break

I attacked six things and five held. (1) THE HEADLINE RED WITNESS: I wrote a two-line stub at src/exclusion.ts, ran the file and got `tests 8 pass 5 fail 3 skipped 0`, exit 1, with the failure text `exactly one environment may hold the fleet lease; A ok=true B ok=true` verbatim as reported; removing the stub gave `8 / 6 / 0 fail / 2 skipped`, exit 0. Reproduced exactly. HELD. (2) THE SKIP, which is the classic guard that cannot go red: I verified BOTH bounds the implementer claims. The expiry guard (test 8) does go red under the stub, and the anti-vacuity control (test 7) is green today and reddens under my own defang. HELD. (3) MY OWN DEFANG of the shipped source rather than their test helper: deleting `"state/"` from src/fleet.ts:28 turned three tests red (`8 / 3 / 3 fail / 2 skipped`). The pins are live and anchored to a shipped claim rather than to a fixture. HELD, and stronger than they showed. (4) THE PROBE GUARD, contention removal: making B push the correct expectation gave `5 / 3 / 2 fail`, matching their row. HELD. (5) THE PROBE GUARD, FABRICATION: I replaced refusalStderrLine with a body returning a hand-written constant carrying a `deadbeef` object name no git produced. The probe printed the fabricated line at exit 0 and ALL FIVE guard tests stayed GREEN. BROKE. That is criterion 2's own property ("never hand-written", T-003) and it is finding CR-M4P20-002; I also witnessed my proposed one-line fix red under the defang and green pristine. (6) DATA LOSS: I built a directory holding .git/HEAD, charter/plan.md and an uncommitted WORK.md, pointed the documented reproduction command at it, and it was gone at exit 0 with no warning. scripts/probe-cas-ref.mjs:189 rm -rf's its raw argv operand with no guard of any kind. BROKE. Finding CR-M4P20-001. I also probed the shipped surface directly rather than accepting the dispatch's word: `git diff --name-only 3b40118 6aea774 -- src/ bin/ package.json package-lock.json` is EMPTY, and package.json `files` contains neither `scripts` nor `test`, so nothing here reaches a shipped artifact and both MEDIUMs are recorded rather than blocking.

## suite_reproduced

Yes, with all four qualifiers. MINE: git CHECKOUT (a `git clone` of the repository, detached at 6aea774, NOT a `git archive` copy), interpreter node v26.6.0 from /tmp/node26, `dist/` BUILT (npm ci exit 0, npm run build exit 0, `git status --porcelain` zero lines after the build), invocation `npm test`, load average 0.92 at start: 862 tests, 859 pass, 1 fail, 2 skipped, 0 todo, exit 1. THEIRS: npm test, node v26.6.0, dist/ built, 862 tests, 860 pass, 0 fail, 2 SKIPPED, 0 todo, exit 0, measured at ea0318c with a captured argument that ea0318c..HEAD changes markdown only. The TEST COUNT and SKIP COUNT reproduce exactly (862 and 2), and the two skips are confirmed by name to be the two gated group-3 witnesses. THE ONE FAILURE IS NOT ATTRIBUTABLE TO THIS CHANGE, AND I ESTABLISHED THE BASE BEFORE SAYING SO: `test/gates.test.ts:3571` ("a precondition command exiting nonzero is error, not a skip, whenever a path-shaped argv element cannot be opened") also FAILS at the branch point 6961186 in a separate clone, same interpreter, same build state (`tests 1 pass 0 fail 1`). It is an artifact of WHERE my clone sits (under /tmp/claude-0, mode drwx------), which is standing warning 1's family with the repository rather than the interpreter as the variable; the phase touches no file involved. The work history's suite sentence is complete on three of the four qualifiers and names its head, but does NOT explicitly state the checkout-versus-archive qualifier, though "a separate clone at /tmp/m4p20-base" implies a checkout.

## criteria_walked

7 of 7 declared in delivery/plan/kernel-plan-m4.md:2911. All seven reached and all seven met. None was unreachable. Criterion 7 is met as WRITTEN (the behavior is registered by name, the test runs the probe against a scratch bare repository and asserts the two lines and exit 0) but only HALF guards the anti-no-op PURPOSE it states, which is finding CR-M4P20-002 and is recorded in that criterion's evidence rather than hidden. Criteria 1, 3 carry declared deviations, all four of which I judged separately.

## citation_hit_rate

38 of 38 (100 percent). I collected every path.ext:LINE token from delivery/work-history/m4-p20.md, delivery/verification/cross-environment-exclusion-probe.md, delivery/plan/m4-conflict-pre-pass.md and the three new source files, resolved all 38 with `sed -n '<N>p'`, and read each resolved line against its claim. All 38 resolve and all 38 say what is claimed. Weight-bearing spot checks: src/lock.ts:63 is the exclusion-domain comment; src/fleet.ts:28 is `export const FLEET_IGNORED = ["state/", "worktrees/", "projects/"] as const;` and I proved it load-bearing by defanging it; src/gates/red-witness.ts:38 really declares the precondition as diff-touches on src/ and bin/; delivery/plan/kernel-plan-m4.md:2989 really lists test/cross-environment-lock.test.ts, confirming the M4-P21 scope gap the implementer raises. `src/gates/suite.ts:1074` and `:1056` appear only inside backticks inside the sentence recording that they were WRONG and were corrected, which is CLAUDE.md rule 3b used deliberately; both corrected forms (:1063, :1058) resolve.

## not_read

I did not read the 3719-line delivery/plan/kernel-plan-m4.md in full; I read section 4.2, the whole M4-P20 section and M4-P21's files-to-touch list. I did not read the ten decision records (DR-0035 to DR-0043, DR-0010), the eleven documents under delivery/evidence/m4-probes/, delivery/plan/m4-intake.md, delivery/plan/pstack-borrow-review.md, delivery/STATE.md, or the 95 added lines of CLAUDE.md, all of which arrive on this branch as inherited orchestrator paperwork rather than as this phase's work. I did NOT run the full gate bundle (I ran the suite, the authored-bytes check, both new test files, and schema validation of my own verdict); I did not run the citations, scope, coverage or clause-map gates myself and I am quoting the implementer's bundle output for those, which is a bundle-level green and not a gate-level one. I observed NO CI run: every number I report is local, and by T-009 there is no push-event or pull_request arm witnessed here at all. I did not run the suite on the default toolchain (node v22.22.2), and I did not run it from a `git archive` copy, so I can speak to only one point on that fourth axis. I did not read delivery/verification/citations-gate-does-not-see-reviews-or-work-histories.md or red-witness-does-not-run-on-scripts.md beyond confirming their first lines resolve.

## findings

### 1. [MEDIUM] scripts/probe-cas-ref.mjs recursively force-deletes its raw argv path operand with no guard of any kind, and the deletion is documented nowhere.

- **citation**: scripts/probe-cas-ref.mjs:189

- **evidence**: scripts/probe-cas-ref.mjs:188 `const absRoot = resolve(root);` and :189 `rmSync(absRoot, { recursive: true, force: true });`. `root` is the raw operand of --remote, --vacuity or --namespaces; parseArgs at scripts/probe-cas-ref.mjs:566 checks only that a value exists and does not start with `--`. No emptiness check, no must-not-exist check, no tmpdir constraint, no --force flag, no prompt. MEASURED: I built /tmp/claude-0/dataloss/precious holding .git/HEAD, charter/plan.md and an uncommitted WORK.md, ran `node scripts/probe-cas-ref.mjs --remote /tmp/claude-0/dataloss/precious`, and it printed `A accept` and `B refuse ! [rejected] 25ef89d7... -> refs/tiphys/lease (stale info)` at exit 0; afterwards the directory held only A, B, remote.git and seed. WORK.md destroyed, no warning. The USAGE string and the file header both say only 'Builds a bare repository at <path>'.

- **reaches_shipped**: NOT a shipped artifact. I measured package.json `files` = [dist, !dist/node_modules, LICENSE, AGENTS.md, gate-registry.yaml, gates.manifest.json, assurance-modes.yaml, checklists, role-model-config.yaml, roles, schemas, templates, tuition], which contains neither `scripts` nor `test`, and `git diff --name-only 3b40118 6aea774 -- src/ bin/` is empty. It DOES reach a real user path: delivery/verification/cross-environment-exclusion-probe.md lines 86, 148 and 200 publish these three commands as the reproduction instructions with <path> as a free operand. Under DR-0027 and this one-round dispatch it is TRACKED rather than blocking, because it makes no shipped artifact wrong.

- **recommendation**: Add a guard as the first statement of buildScratchFleet, before scripts/probe-cas-ref.mjs:189: `if (existsSync(absRoot) && readdirSync(absRoot).length > 0) throw new Error(...refusing to build a scratch fleet in a non-empty directory...)`, importing readdirSync alongside the existing node:fs imports at scripts/probe-cas-ref.mjs:57, and add one sentence to USAGE saying the path operand is created and deleted recursively. Graded medium rather than high because triggering it needs the operator to name a path they care about, and every in-repository invocation passes a fresh mkdtempSync directory.

### 2. [MEDIUM] The probe guard is green against a HAND-WRITTEN refusal line, which is precisely the property criterion 2 names and the only reason the guard exists.

- **citation**: test/cas-probe.test.ts:138

- **evidence**: Criterion 2 (delivery/plan/kernel-plan-m4.md:2939) requires the refusal signature to be CAPTURED from a real forced contention, 'never hand-written (T-003)'. test/cas-probe.test.ts:12 says the guard exists because 'an evidence producer that quietly becomes a no-op produces a document that reads exactly the same as a real measurement.' DEFANG I APPLIED: scripts/probe-cas-ref.mjs:167 to :174 replaced by a body ignoring stderr and returning the constant '! [rejected]        deadbeefdeadbeefdeadbeefdeadbeefdeadbeef -> refs/tiphys/lease (stale info)'. The probe then PRINTED that fabricated line at exit 0, and `node --test test/cas-probe.test.ts` reported `tests 5 pass 5 fail 0 skipped 0`. All five green against fabricated evidence. The work history's two probe-guard defang rows are BOTH contention-removal members (B pushes the correct expectation; the discriminator arm moved to a tracked branch), so for this claim they are one defect twice rather than two structurally different members. Their group-1 and group-2 pairs ARE genuinely different; this criticism is scoped to the probe-guard pair.

- **reaches_shipped**: NOT a shipped artifact. The reach is one hop: this probe is the evidence behind M4-D-11, and M4-D-11 governs src/exclusion.ts which M4-P21 ships. I cannot show a direct path from this gap to a wrong byte in a shipped file, so I mark it TRACKED rather than blocking and say so plainly instead of inflating it.

- **recommendation**: One assertion in test/cas-probe.test.ts immediately after line 138, where payload.b.stderr (git's real stderr) and payload.b.refusalLine (the printed line, emitted at scripts/probe-cas-ref.mjs:337) are both already in scope: `assert.equal(payload.b.stderr.includes(payload.b.refusalLine), true, ...)`. I WITNESSED THIS FIX BOTH WAYS: pristine run gives true, the fabrication defang gives false.

### 3. [MEDIUM] A derivation published as 'Full output' is not the full output, and one row is dropped with no marker at all.

- **citation**: delivery/work-history/m4-p20.md, section 'The derivation I did run, and its exact scope'

- **evidence**: delivery/work-history/m4-p20.md publishes `grep -rn "skip:" test/*.ts` labelled 'Full output, sixteen lines'. The fix-round contract item 2 requires 'its full output. Not a summary of it.' MEASURED: at head 6aea774 the command returns 24 lines; at the merge base 3b40118, reconstructed with git show over each tracked test/*.ts, it returns 21. The 24-to-16 gap decomposes into three things and only two are declared: five test/suite-gate.test.ts rows (209, 361, 362, 392, 548) visibly collapsed into one; two license-gate ternaries declared truncated with (...); and test/watcher.test.ts:1401 ('  { skip: fifoSkip },') simply ABSENT, with no compression label, no elision marker and no mention, while its sibling test/watcher.test.ts:1330 IS published. `git show 3b40118:test/watcher.test.ts | sed -n '1401p'` prints the line, and `git diff --name-only 3b40118...6aea774 -- test/watcher.test.ts` is empty, so it pre-exists this phase and the phase does not touch that file. Three rows from the phase's own test/cross-environment.test.ts (516, 547, 623) are also absent, and they are exactly the counterexample to the conclusion the block supports.

- **reaches_shipped**: NO shipped artifact and no user-visible command. TRACKED. I raise it despite DR-0027's warning about prose findings because it is not prose: it is a falsified evidence label on the one artifact the fix-round contract makes a later reviewer trust, and the citations gate does not lint work histories, so no gate can see it.

- **recommendation**: Replace the block with the real 24-line output at HEAD, change the label to 'Full output, twenty-four lines', and if the five suite-gate rows stay collapsed, label the collapse explicitly with its own count and line numbers. The CONCLUSION survives untouched: watcher.test.ts:1401 is `{ skip: fifoSkip }`, an environment gate, so 'every existing gate in test/ is an ENVIRONMENT gate' remains true.

### 4. [LOW] The disjointness argument is file-level only, and these tests depend on a shipped file another wave-1 unit owns.

- **citation**: test/cross-environment.test.ts:320

- **evidence**: The work history asserts 'no file I touch appears on any of their lists', a FILE-level check. But test/cross-environment.test.ts:320 asserts `git check-ignore --quiet state/orchestrator.lock` exits 0, true only because src/fleet.ts:28 lists 'state/', and test/cross-environment.test.ts:402 writes charter/divergence.md, which needs `tiphys init` to create charter/. delivery/plan/m4-conflict-pre-pass.md:16 grants src/fleet.ts and src/commands/init.ts to M4-P16, running concurrently. I PROVED the dependency is load-bearing: deleting "state/" from src/fleet.ts:28 in a scratch clone gave `tests 8 pass 3 fail 3 skipped 2`. I also PROVED the collision is not live today: `git diff <merge-base>..claude/m4-p16-fleet-rehydration -- src/fleet.ts src/commands/init.ts` is purely additive and derives EPHEMERAL_DIRS FROM FLEET_IGNORED, leaving FLEET_IGNORED, FLEET_FILES and FLEET_DIRS unchanged.

- **reaches_shipped**: src/fleet.ts IS shipped, so a FUTURE instance could turn main red after both merge, and neither branch's own CI could see it. No instance exists today, which is why this is LOW and TRACKED rather than blocking. It is a gap in the METHOD of the disjointness argument, not an error in this branch's bytes.

- **recommendation**: Add one line to delivery/plan/m4-conflict-pre-pass.md beneath the M4-P20 row naming the semantic dependency: M4-P20's tests READ src/fleet.ts (FLEET_IGNORED must contain `state/`) and the layout `tiphys init` creates; M4-P16 owns both files.

### 5. [LOW] The usage test hands two real absolute paths to a script that rm -rf's its path operand.

- **citation**: test/cas-probe.test.ts:255

- **evidence**: test/cas-probe.test.ts:255 is `runProbe(["--remote", "/tmp/a", "--vacuity", "/tmp/b"])`. It is safe TODAY only because scripts/probe-cas-ref.mjs:580 returns the usage error 'exactly one mode may be given' and main returns 64 at scripts/probe-cas-ref.mjs:599 before any runner is dispatched. The other four tests in the file already build their paths with scratch(t, name) at test/cas-probe.test.ts:54. Combined with CR-M4P20-001, the day argument parsing is reordered this test deletes /tmp/a and /tmp/b on a developer machine.

- **reaches_shipped**: No shipped artifact; test/ only. Latent, fires only if parsing order changes. TRACKED, recorded because it compounds the unguarded rm -rf.

- **recommendation**: Replace the two literals at test/cas-probe.test.ts:255 with scratch(t, "two-modes-a") and scratch(t, "two-modes-b"), taking the test context parameter the way the other four tests do.
