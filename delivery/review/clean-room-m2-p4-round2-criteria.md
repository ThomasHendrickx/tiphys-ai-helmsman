# CLEAN-ROOM DELTA re-review: M2-P4 fix round one (criteria/regression)

Status: COMPLETE, verdict FIX-ROUND-NEEDED
Subject branch: claude/m2-p4-scope-auditor @ c9b00c9
Source findings: CR-1395

## Log

- Started review, created workdir marker.
- Fetched origin/claude/m2-p4-scope-auditor = c9b00c96c923426ed1da3a2077e02047b17a4095 (matches expected head exactly). Fetched origin/claude/m2-phase-reviews for arbitration-m2-p4.md and clean-room-m2-p4-criteria.md (copied to scratchpad).
- Checked out c9b00c9 in detached HEAD in this session's worktree (/home/user/tiphys-ai-helmsman/.claude/worktrees/agent-a0e19c2a4e755da35). git status clean before and after checkout.
- Floor toolchain confirmed present: /tmp/.../scratchpad/toolchain/node-v26.6.0-linux-x64/bin/node --version = v26.6.0. Default toolchain (ambient) = v22.22.2 via /opt/node22/bin/node.
- Read arbitration-m2-p4.md: round-1 verdict FIX-ROUND-NEEDED, HIGH CR-1030/CR-1045 (yardstick swap mechanism: --phase/--base/--head forwarded unchecked against declaration.id/declaration.branch/trunk-ancestry/real HEAD), mediums CR-1047 (crash-to-verdict), CR-1046 (overclaim correction), CR-1048 (already fixed on main, absorbed at merge), low CR-1031 (plan-text regex typo).
- Read clean-room-m2-p4-criteria.md (pre-fix round, head 2118f68): independent scope audit PASS, item-7 aggregate-policy recommendation (decided by orchestrator: keep scope required, not reclassified -- not this phase's concern).
- Read plan section M2-P4 (delivery/plan/kernel-plan-m2.md) in full: 7 acceptance criteria, hazard class, step 6 branch pattern text.
- Read delivery/work-history/m2-p4.md fix-round-1 section (lines 622-973) in full: mechanism naming, derivation (grep enumerations), red witnesses (W1/W2/W3/CR-1047), CR-1046 correction, CR-1031 deviation record, claim grep re-run, gates on both toolchains (219 tests: 215/2fail/2skip default, 216/3fail/0skip floor; scope-gate.test.ts 16/16 both).
- Read src/gates/scope.ts in full (851 lines) at c9b00c9. Confirmed the four cross-checks described in the work history are present in code: phase.toUpperCase()===declaration.id (line 702), branch!==declaration.branch (line 714), isAncestorOf(mergeBase, trunk) (lines 629-660), resolveRef(head)===resolveRef(HEAD) (lines 589-620). CR-1047 try/catch wraps main() body (line 568) and the invokedDirectly entry point (line 830).
- Read test/scope-gate.test.ts (1159 lines, 16 tests). Confirmed 4 new tests (lines 861, 903, 948, 1006) each stage a real dangerous state: W1 commits TWO real declarations at merge base and swaps --phase; W2 commits a real widening commit ON the audited branch and forges --base to it; W3 makes two real commits and sets --head to the earlier one while working tree stands at the later; CR-1047 uses copyInstallation to remove/corrupt/FIFO the real shipped schema file. Each has an honest CONTROL assertion plus the ATTACK assertion.
- Verified CR-1031 plan-text fix landed: delivery/plan/kernel-plan-m2.md line 345 now reads pattern `claude/m[0-9]+-p[0-9]+-.*` (was `-p[0-9]+-*`), matching the delivered gates.manifest.json pattern exactly (gates.manifest.json line 32).
- Computed merge-base(origin/main, HEAD) = e1390f3195be72c31d73c5707f9957ed9fd85a7e (confirmed: this is main's tip at the time of the work history's "merge commit 5f4d91f", e1390f3 is-ancestor-of 5f4d91f = true, reverse false; consistent with "head behind latest origin/main, P6 merged after").
- git diff --name-status e1390f3..HEAD = exactly: M delivery/plan/kernel-plan-m2.md, A delivery/work-history/m2-p4.md, M gates.manifest.json, A src/gates/schemas/phase-declaration.schema.json, A src/gates/scope.ts, M test/behaviors.json, A test/scope-gate.test.ts. Matches declared files-to-touch + work-history standing extra + the one CR-1031 plan-text line. PASS, exact match, no undeclared files.
- git diff e1390f3..HEAD -- delivery/plan/kernel-plan-m2.md is exactly the one line (step 6 regex), confirmed by reading the diff.
- **FINDING CANDIDATE (registry gap, MEDIUM):** test/behaviors.json's scope- keys number exactly 12 in HEAD (`git show HEAD:test/behaviors.json | grep -c "scope-"` = 12), identical to the count already present before the fix round (the original 12 tests). Grepped for the four new fix-round test titles' concepts (yardstick, swap, fork, hidden, W1/W2/W3, CR-1047, pipe+schema) across the whole file: zero matches beyond the pre-existing 12 keys. Extracted all 16 test() titles from test/scope-gate.test.ts via grep -oP; the first 12 exactly match the 12 registered values; the last 4 (yardstick swap W1 / merge-base fork W2 / hidden commit W3 / CR-1047) match NO key anywhere in the 223-line file. Confirmed against kernel-plan-v1.md's EXT-F-05 (section 3, cited by section 1.4): "each phase's acceptance criteria name its newly required behaviors; the phase adds at least one identified test per named behavior and registers the mapping ... in test/behaviors.json". M2-P4 criterion 7 explicitly requires "the behavior registry criterion of section 1.4 holds". The fix round named four new behaviors (the mechanism: cross-checking every caller-supplied selector) and wrote four new tests, but registered ZERO of the four mappings. This is a genuine, confirmed violation of a plan acceptance criterion (criterion 7) introduced fresh by this round, not a pre-existing gap: MEDIUM finding, blocking under DR-0012 ("no unresolved high or medium finding").
- Independent gate execution, default toolchain (ambient, /opt/node22, v22.22.2): `npm ci` exit 0 (EBADENGINE warning expected, below floor). `npm run build` exit 0, `git status --porcelain` clean after. `npm test`: 1..219, tests 219, pass 217, fail 0, skipped 2 (better than work history's 215/2fail/2skip -- the 2 failures reported there were real-clock flakes not attributable to this round, and did not reproduce in my run). `node --test test/scope-gate.test.ts` alone: 16/16 pass, 0 fail.
- Independent gate execution, floor toolchain (/tmp/.../node-v26.6.0-linux-x64, v26.6.0, npm 11.18.0): `npm ci` exit 0, no EBADENGINE. `npm run build` exit 0, `git status --porcelain` clean after (both captured via script files under scratchpad, run-floor-ci.sh / run-floor-build.sh, since inline PATH-prefixed multi-part commands were refused by the worktree-isolation guard). Full `npm test` dispatched in background (task b3fk3vq6k), awaiting completion.
- **Independent red-witness re-execution (>=2, structurally different, sha256-restored):**
  1. Captured pre-fix scope.ts via `git show 5f4d91f2bae9adce3d4b47a3f9ed8bca658e79c0:src/gates/scope.ts` (561 lines, matches the criteria review's own line count for the pre-fix file). Copied over the working `src/gates/scope.ts` (git status showed ` M src/gates/scope.ts` only).
  2. Ran `node --test --test-name-pattern "yardstick swap|hidden commit" test/scope-gate.test.ts` against this pre-fix code: BOTH reddened -- W1 reported `scope: green (1 changed paths audited) ... audited against declaration .../m2-p9.json` (the attack's swapped, more permissive declaration silently accepted) and W3 reported `scope: green (1 changed paths audited) ... audited against declaration .../m2-p4.json at merge base <base>` (the hidden commit never measured). `tests 2, pass 0, fail 2`. This independently reproduces the exact dangerous-state text the work history captured.
  3. Restored: `git checkout HEAD -- src/gates/scope.ts`; `git status --porcelain` empty (clean) immediately after.
  4. Re-ran the SAME two tests against the restored (fixed) code: both green (`tests 2, pass 2, fail 0`).
  5. Also ran the other two new tests (merge-base fork W2, CR-1047) against the fixed code for completeness: both green (`tests 2, pass 2, fail 0`). All four new red witnesses confirmed genuine.
- Confirmed no MECHANICAL checker of the behavior registry exists yet in this tree (src/gates/ contains only manifest.ts, pin.ts, result.ts, run.ts, scope.ts, validate.ts, schemas/; M2-P3's suite.ts, which would eventually enforce "every behavior in test/behaviors.json resolves by name", has not merged as of this branch's merge-base). So the registry gap is currently caught only by review, not by CI.
- Floor-toolchain full suite (background task, ~244s wall): 219 tests, 218 pass, 1 fail, 0 skipped. The 1 failure is test/liveness.test.ts:633 ("doctor and the guard return one verdict about one beacon"), a real-clock beacon-age flake (observed age 14s vs expected pattern 13s) in a module this round does not touch, matching the SAME flake class the work history itself recorded (CLAUDE.md warning 11). Not in src/gates/scope.ts, phase-declaration.schema.json, or test/scope-gate.test.ts.
- Floor-toolchain test/scope-gate.test.ts alone: 16 tests, 16 pass, 0 fail.
- Default-toolchain (re-confirmed): npm test 219 tests, 217 pass, 0 fail, 2 skipped; test/scope-gate.test.ts alone 16/16 pass.

## FINAL VERDICT: FIX-ROUND-NEEDED

One new MEDIUM finding (registry gap, CR-1046/CR-1030/CR-1045 fix round did not register its own four new behaviors in test/behaviors.json, violating M2-P4 criterion 7 / EXT-F-05 / CLAUDE.md's binding registration convention). Everything else in the round is genuine and verified: the mechanism fix, all four red witnesses, the CR-1047 crash wrap, the CR-1046 prose correction, the CR-1031 plan-text fix, the scope audit, and both toolchains' gates.

STATUS: COMPLETE. Tree left clean at reviewed sha c9b00c96c923426ed1da3a2077e02047b17a4095 (all scratch defangs reverted, git status --porcelain empty).
