# M4-P1 clean-room review by Claude Opus 5 (claude-opus-5)

**VERDICT: FIX-ROUND-NEEDED**

## framing

Evidence integrity and data loss combined: every claim treated as unproven until I ran it, and the change assumed capable of destroying work until I found why it cannot.

## derivation_honesty

The work history HAS the section, twice over: the fix-round contract's item 3 at delivery/work-history/m4-p1.md:520 and a fuller seven-item "What is NOT covered" at :553. I judged it FIRST, before any row, and it is honest on its own terms. It admits the bypass arms ran as root, that the non-root repeat was stopped by a classifier refusal it did not work around, that the Q3a race was not bisected and has no rate (two empty reads in twelve resolutions), that no usage-limit or OOM arm ran, that `claude --restricted` was never run, and that everything is one container, one account, one CLI build, one day. It even discloses that the grep-based derivation cannot see a site inside a python3 heredoc and that the one such heredoc was read by hand instead. ONE ADMISSION IS WORSE THAN IT SOUNDS. Item 1 at :520 says "this unit touches no shipped code and made no claim about it", and the command published for that clause three times (:754, :910, and the criterion-6 attribution) is `git diff --name-only plan/pstack-borrow-review...HEAD -- src/ bin/ scripts/ package.json tsconfig.json`. `plan/pstack-borrow-review` is an UNMERGED branch and is not this branch's merge base with main (3b40118 is), so the command hides everything those 22 commits carry and omits schemas/, roles/, tuition/, .github/, package-lock.json and test/ entirely. That is exactly the failure item 3 exists against: a search whose scope is wrong returns an empty result indistinguishable from an absence of defects. I re-derived it against the real base and the CONCLUSION survives, so nothing in the document changes; what fails is the derivation a later reader will re-run. Recorded as CR-003 (low).

## what_i_tried_to_break

THE RED WITNESSES. Q1's witness is red against the DANGEROUS state, not the absent feature: same flag, same prompt, same plugin, hook exit 0 moves app.txt md5 ab0b8843 to 737481f9 with stdout WROTE, hook exit 2 leaves the md5 unchanged with stdout BLOCKED. I checked the md5s across five fixtures for internal consistency and they hold. The class has TWO genuinely different members, not one defect twice: Write carries tool_input.file_path, Bash carries tool_input.command, and I read both marker lines to confirm the payload shapes differ. A GUARD THAT CANNOT GO RED is where I looked hardest, and the implementer got there first: the settings-route arm IS that shape (hook reports permission_mode "default", so the row is evidence about default and nothing else) and the bad-plugin-dir postscript is the same class again, upstream of the hook. I could not find a third. A COUNT PINNED OVER AN APPEND-ONLY REGISTRY: none introduced; the branch adds no test, does not touch test/behaviors.json, gates.manifest.json or clause-map.json, and coverage stays green at 115 ids with M4 13 while clause-map stays green at 74 rows. C-1: the Q3 resolver reads the WHOLE transcript and collects a DISTINCT set, which is C-1 respected. C-2: grep for getuid, /proc/, process.pid, kill -0, pgrep, ps- over the fixture tree returns nothing; SIGKILL and SIGTERM appear only as a stimulus to force abandonment. C-3: both committed runners wait in the foreground. DATA LOSS: both runners confine every write to $LAB in the scratchpad, the Q1 runner rewrites only $LAB/project/app.txt, the Q2 runner's rm -rf targets $LAB/log/$ARM, and no committed script names a repository path; I found no route by which this branch destroys work. WHERE IT DID NOT HOLD: I ran the gate bundle myself and scope is RED, then removed the first obstacle (put the declaration on a simulated trunk) to see what was behind it and found a second red on 27 undeclared paths; and I re-ran criterion 2's own command and got 44 where the document says 38, which led to six fixtures carrying 73 undeclared glyph substitutions.

## suite_reproduced

Yes, independently, in a fresh GIT CLONE (not a git archive copy, the fourth qualifier). Interpreter node v26.6.0 from /tmp/node26/bin with npm 11.18.0; npm ci exit 0; npm run build exit 0 with git status --porcelain zero lines afterwards; invocation npm test; head 0025b78; load average 8.90 at start. Result: tests 849, pass 848, fail 1, cancelled 0, SKIPPED 0, todo 0, duration_ms 584937, exit 1. My one failure is test/gates.test.ts:3571 with spawnSync node EACCES, which is CLAUDE.md standing warning 1's clone-location trap for a repository under /tmp/claude-0, and it is NOT either failure the work history reports. THE BASE WAS ESTABLISHED, by byte identity rather than by a second run: git diff --stat 3b40118 0025b78 -- src/ test/ excluding test/fixtures/harness-probe/ is EMPTY, so every source and test file is identical at base and head and no suite result here can be attributable to this branch. The work history's own sentence is complete (849 tests, 847 pass, 2 fail, 0 skipped, node v26.6.0, dist built, npm test, head 80b67d0) and it establishes its base with a four-run single-variable experiment at 6961186, then states honestly that criterion 6 is NOT met in the form "exit 0" and gives the full six-row table including the run that did not finish and its 27 cancelled files. It also correctly quotes the CANCELLED count, which standing warning 12 does not yet require.

## criteria_walked

6 of 6 declared in delivery/plan/kernel-plan-m4.md:274, all reached. Met: 1, 3, 4. Not met: 2 (fixture count stale and six fixtures carry undeclared transliteration), 5 (the M4-P5 manifest shape has no disposition in the verification document; grep for "manifest" in it returns zero hits), 6 (node --test does not exit 0; the work history states this plainly at :915 rather than softening it, and I confirmed the failure is not attributable to the branch).

## citation_hit_rate

42 of 42, 100 percent. I extracted every path:line token outside backticks from delivery/work-history/m4-p1.md, 18 non-fixture and 24 fixture. All 42 resolve to an existing file at an in-range line, and I read back each of the 18 non-fixture ones to confirm it points at the sentence it is cited for (for example CLAUDE.md:116 is the guard-whose-condition-does-not-test-the-property paragraph, CLAUDE.md:416 is "One witness is not a class", kernel-plan-m4.md:274 is the acceptance-criteria list head, src/gates/coverage.ts:260 is the RegexBoundExceededError throw). Independently, the citations gate at this head reports green: 16 changed documents linted, 505 citations resolved, 0 self-citation, 0 unverifiable-external; note it does NOT lint work histories, which the work history itself states at :833, so the 42 above are otherwise ungated.

## not_read

I did not read all 3719 lines of delivery/plan/kernel-plan-m4.md; I read the M4-P1 entry (:199 to :330) in full plus the sections reached by grep. I did not read the ten files under delivery/evidence/m4-probes/ at all, nor delivery/plan/m4-intake.md, nor delivery/plan/pstack-borrow-review.md. Of the nine new decision records DR-0035 to DR-0043 I read none in full; I read only the DR-0010 diff. I read delivery/verification/m4-prototype-probes.md only at its structure plus the amendment (:290 to :447) and the sections grep pointed at, not its first 289 lines in full. Those omissions matter because the 27-path scope red in CR-001 is about files whose CONTENT I largely did not audit; my finding is about whether they belong in this pull request, not about whether they are correct. I did not re-run the work history's own two coverage-gate failures under induced load; my container was at load 8.90 and did not reproduce them, and I accepted the implementer's four-run single-variable experiment plus my own proof that src/ and test/ are byte-identical between base and head. I could not run the scope gate against a real trunk carrying the declaration, only against a simulated one built in my own clone.

## findings

### 1. [HIGH] The `scope` gate is RED at this head, and it is red twice over. First: no phase declaration exists at delivery/plan/phase-declarations/M4-P1.json in the merge base, because the declaration is ADDED on this branch at commit af6993f. Second, hidden behind the first: with the declaration placed on a simulated trunk, the gate reddens on TWENTY-SEVEN undeclared paths, because the branch carries 22 commits of the unmerged plan/pstack-borrow-review planning corpus (the whole M4 plan, DR-0035 to DR-0043, two CLAUDE.md rule sections, delivery/STATE.md and the DR-0010 decision) under a pull request titled as the harness probe. The work history walks criteria 1 to 6 and never mentions the scope gate at all.

- **citation**: delivery/work-history/m4-p1.md:5, delivery/plan/phase-declarations/m4-p1.json:1, src/gates/scope.ts:110, delivery/plan/m4-conflict-pre-pass.md:12

- **evidence**: Run by me at head 0025b78: `node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full --only scope --phase M4-P1 --base origin/main --head HEAD` returns `scope: red: branch claude/m4-p1-harness-probe (phase M4-P1) matches the phase pattern but no phase declaration exists at delivery/plan/phase-declarations/M4-P1.json in the merge base 3b401182301361700ffa0fd4b2ae099993b3d733`. Second run, declaration simulated onto trunk and this head merged onto it: `scope: red: touched path(s) outside the declared scope: CLAUDE.md, delivery/STATE.md, delivery/decisions/DR-0010-...md, DR-0035 ... DR-0043, ten files under delivery/evidence/m4-probes/, delivery/plan/kernel-plan-m4.md, delivery/plan/m4-conflict-pre-pass.md, delivery/plan/m4-intake.md, delivery/plan/pstack-borrow-review.md (2 declared path(s) not touched)`. `git rev-list --count 3b40118..HEAD` is 43, of which a985a96 through 6961186 (22 commits) are the planning corpus; the branch's composition is disclosed in one line at delivery/work-history/m4-p1.md:5 (`cut from: plan/pstack-borrow-review`) and never analysed. delivery/plan/phase-declarations/m4-p1.json:1 declares 2 filesToTouch plus 3 declaredExtras against 73 changed files. src/gates/scope.ts:110 relaxes the merge-base read only for an entry ADDED to an existing declaration, not for a wholly new one. FLEET-WIDE: all twelve claude/m4-* branches carry the same five undeclared paths (`git merge-base claude/m4-p1-harness-probe claude/m4-p2-async-launch` is 6961186), and it is NOT a collision risk because the blobs are identical (`git rev-parse <branch>:CLAUDE.md` is 6fb17cf on m4-p1, m4-p2 and m4-p13 alike).

- **reaches_shipped**: It reaches NO shipped file. It reaches the MERGE itself: a red required gate cannot pass branch protection, and CLAUDE.md makes the scope audit a condition of a phase being done. DR-0027's reachability test is about whether a latent defect blocks; this is not latent, it is an observed red on the reviewed head, so it blocks regardless. Stated plainly: I cannot name a shipped artifact this makes wrong.

- **recommendation**: Land the already-prepared branch claude/m4-declarations-to-main (it carries a byte-identical delivery/plan/phase-declarations/m4-p1.json, verified with diff over git show of both), then `git merge origin/main` into claude/m4-p1-harness-probe so the merge base advances past it; that clears the first red. For the second red, which landing the declaration does NOT clear, open plan/pstack-borrow-review as its own pull request (it is a self-contained unit of value) and merge main into the phase branch again, so the phase diff reduces to delivery/verification/m4-prototype-probes.md, test/fixtures/harness-probe/ and delivery/work-history/m4-p1.md. One action fixes all twelve wave-1 branches. Do NOT widen filesToTouch to 27 entries: that turns the gate green while leaving the pull-request-contents mismatch DR-0031 names.

### 2. [MEDIUM] Criterion 2's audit is stale at the reviewed head and six fixtures carry UNDECLARED transliteration. The work history claims THIRTY-EIGHT fixture files, counted with a command that returns 44 at this head; the six added after the walk are all of test/fixtures/harness-probe/suite-and-controls/, and every one of them has had the Node test reporter's glyphs substituted without appearing in the declared transliteration table. Those six also carry hand-written header and annotation lines, against the criterion's `No file in that tree is hand-written`, and two are explicitly elided.

- **citation**: delivery/work-history/m4-p1.md:671, delivery/work-history/m4-p1.md:676, test/fixtures/harness-probe/suite-and-controls/head-npm-test-run1.txt:1

- **evidence**: delivery/work-history/m4-p1.md:671 says `THIRTY-EIGHT fixture files under test/fixtures/harness-probe/, counted with git ls-files test/fixtures/harness-probe | wc -l`; I ran that exact command at head 0025b78 and got 44. Per commit with git ls-tree -r --name-only: 2447541 gives 38, 86736c2 gives 42, 0f58643 gives 44, 5bfdb20 gives 44, 0025b78 gives 44. Glyph substitutions counted across the six: 47 lines beginning 'i ' (U+2139), 12 beginning 'x ' (U+2716), 14 beginning 'ok ' (U+2714). My own npm test at this head emits the real codepoints, so the committed captures are altered. The declared table at delivery/work-history/m4-p1.md:676 names only U+2014 in two q3 files (counts 1 and 4) and covers none of it, while test/fixtures/harness-probe/suite-and-controls/head-npm-test-run1.txt's own header says 'verbatim except the declared transliteration'. Fourteen hand-written '#' lines across the six, including '# aborted test FILES in this run: count: 27' and '# a sample of the aborted files:'. CLAUDE.md rule 3: 'Silent transliteration is the failure mode this entry exists to prevent, because after the fact it is indistinguishable from fabricated evidence.'

- **reaches_shipped**: I cannot name a shipped artifact this reaches. test/fixtures/** ships nothing (package.json `files` is dist, LICENSE, AGENTS.md, gate-registry.yaml, gates.manifest.json, assurance-modes.yaml, checklists, role-model-config.yaml, roles, schemas, templates, tuition) and no code reads these files. TRACKED, not blocking, under DR-0027. Graded medium because it is the evidence-integrity property this repository has paid for repeatedly, not because it endangers a build.

- **recommendation**: Re-run criterion 2's walk at the final head: change THIRTY-EIGHT to FORTY-FOUR at delivery/work-history/m4-p1.md:671, and add three rows to the table at :676 naming U+2139 rendered as `i` (47), U+2716 rendered as `x` (12) and U+2714 rendered as `ok` (14) across test/fixtures/harness-probe/suite-and-controls/*.txt with per-file counts. Add one sentence stating those six files are ANNOTATED with a header and that two are ELIDED, so `verbatim capture` is not claimed for them.

### 3. [LOW] The derivation published three times for 'this branch changes no code' is scoped to the wrong base and to an incomplete path list, which is the fix-round contract's item 3 failing in the document that answers item 3. The claim itself survives re-derivation, so no conclusion changes; what fails is the command a later reader will re-run.

- **citation**: delivery/work-history/m4-p1.md:754, delivery/work-history/m4-p1.md:910

- **evidence**: delivery/work-history/m4-p1.md:754 and :910 both publish `git diff --name-only plan/pstack-borrow-review...HEAD -- src/ bin/ scripts/ package.json tsconfig.json` with '(no output)'. plan/pstack-borrow-review is unmerged and is not the merge base with main; `git merge-base origin/main claude/m4-p1-harness-probe` is 3b40118. The path list omits schemas/, roles/, tuition/, .github/, package-lock.json and test/. My re-derivation against the real base also produces no output, so the conclusion is TRUE.

- **reaches_shipped**: I cannot name a shipped artifact this reaches. TRACKED under DR-0027.

- **recommendation**: Replace both occurrences with `git diff --name-only 3b40118...HEAD -- src/ bin/ schemas/ roles/ tuition/ scripts/ package.json package-lock.json tsconfig.json .github/` and paste the empty result, and add beside it the complete form `git diff --name-only 3b40118 0025b78 -- . ':!delivery/' ':!test/fixtures/harness-probe/'`, which prints CLAUDE.md and nothing else and is a statement about the whole tree rather than a filtered one.

### 4. [LOW] Criterion 5's fourth item, the M4-P5 manifest shape, has no disposition in the document the criterion names. The criterion requires the verification document to state CLOSES, NARROWS or leaves open for four questions; it disposes three.

- **citation**: delivery/verification/m4-prototype-probes.md:290, delivery/work-history/m4-p1.md:711

- **evidence**: `grep -n 'manifest' delivery/verification/m4-prototype-probes.md` returns zero hits. M4-D-01 is disposed at :93, M4-D-07 at :17, M4-D-06 at :212 and :250. The fourth disposition exists only in the work history, at delivery/work-history/m4-p1.md:711, as 'untouched'.

- **reaches_shipped**: I cannot name a shipped artifact this reaches. TRACKED under DR-0027.

- **recommendation**: Add a four-row disposition table to the amendment section of delivery/verification/m4-prototype-probes.md near its head at :290, copying the work history's own rows for M4-D-01, M4-D-06(b), M4-D-07 and the M4-P5 manifest shape, so the verification document is self-contained for a reader who never opens the work history.

### 5. [LOW] The Q1 answer was obtained only with the harness's own root safety gate defeated, and the conclusion it feeds ('M4-P9 may stay in a PreToolUse hook') is a design input for a phase that will ship a hook. This is disclosed in both documents and the non-root arm is called NOT ESTABLISHED in those words, which is correct practice; the residual risk is that the condition lives in a not-covered list rather than travelling with the conclusion.

- **citation**: delivery/work-history/m4-p1.md:155, delivery/verification/m4-prototype-probes.md:428

- **evidence**: delivery/work-history/m4-p1.md:155 records the two refusals ('--dangerously-skip-permissions cannot be used with root/sudo privileges for security reasons') and that the probe then exported IS_SANDBOX=1 for the child to pass the gate, with the gate's condition extracted from the shipped bundle by a quoted grep at :167. test/fixtures/harness-probe/q1-bypass-permission-mode/bypass-mode-exit0.summary.txt:1 captures the refusal with marker NO and zero hook invocations. delivery/verification/m4-prototype-probes.md:428 declares the non-root arm NOT ESTABLISHED.

- **reaches_shipped**: Nothing today. It could reach M4-P9's shipped hook LATER, through a design decision taken on this evidence; I cannot name a shipped file it reaches now, so it is TRACKED.

- **recommendation**: Add one sentence to item 13 of the amendment in delivery/verification/m4-prototype-probes.md stating that the 'hooks may stay' conclusion is conditional on the pilot running at the same uid and sandbox declaration as the probe, and that a non-root confirmation is owed before M4-P9's acceptance criteria are frozen.
