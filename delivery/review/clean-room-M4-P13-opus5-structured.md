# M4-P13 clean-room review by Claude Opus 5 (claude-opus-5)

**VERDICT AS RETURNED: APPROVE**

## framing

Evidence integrity and data loss: I treated every count, capture and citation as unproven until I re-ran it, and I assumed the re-disposition could silently drop or falsify a requirement until I found why it cannot.

## derivation_honesty

Section 9 is unusually honest and it is also where the phase's real defect is hiding, so I read it as PARTLY honest in a way the orchestrator must act on. Six of its seven exclusions are accurate and I verified two of them by running the thing they describe: item 6 (the scope gate cannot pass because the declaration is absent from the merge base) is EXACTLY right, and I confirmed it by running `node src/gates/scope.ts --declarations delivery/plan/phase-declarations --base origin/main --head HEAD --phase m4-p13`, which returned red with the predicted detail. I specifically suspected item 6 was stale, because M3-P11 change B ships "the declaration is read from BOTH sides" (src/gates/scope.ts:596); it is not stale, because change B permits AMENDING an existing declaration and an absent merge-base declaration is still a hard red. Item 5 (no CI observed) and item 1 (untracked files) are plainly true. Item 7 (whether `decision` has a consumer the suite does not reach) is honest and turned out to be the thread worth pulling, see F-3. WHAT IS WORSE THAN IT SOUNDS IS ITEM 4. It says "the M4 paragraph at delivery/plan/kernel-plan-v1.md:368 ... is now inconsistent with their buckets. That is KNOWN and is another unit's work ... I did not check whether any OTHER prose paragraph enumerates the same rows." That reads like a remote possibility in some other document. I ran the check, and there are TWO more live contradictions, both inside the ONE plan file the phase edited, one of them three lines below a line the phase DID edit, and one of them the very line the work history cites as its authority for the move. See F-1. The structural cause is also stated plainly rather than hidden: all three derivation greps in section 2.1 search for restatements of the COUNTS. None searches for the thirteen ROW IDS. The mechanism the round named ("a literal restatement of the per-milestone or per-kind bucket distribution") is narrower than the mechanism it needed ("any live artifact asserting a bucket for a re-dispositioned row"), which is the fix-round contract's own instance-versus-mechanism shape. Everything else in the work history I checked held up, including two claims I expected to break: site 10's reasoning about test/fixtures/gate-runner-capture.summary.json (I read test/gate-registry.test.ts:450-478 myself; the consumer's type annotation has no `detail` field and it is the only reader of that fixture), and the claim that section 6's red witnesses leave the worktree clean.

## what_i_tried_to_break

TEN attacks. (1) The new cross-document test's ability to go red: I re-ran the control green in a scratch clone, then seeded their member (i) (R-072 migration cell M5->M4) and member (ii) (R-026a appendix bucket M5->M4) independently and got the named mismatches both times. The two members are genuinely structurally different: one mutates the inventory document and leaves the distribution correct, the other mutates the coverage document and moves it (M4 6, M5 2), so one is invisible to counts and one is not. (2) I added a THIRD member the work history does not claim: a CONSISTENT revert of R-042 to M4 in BOTH documents, which the declared-exemption assertion catches ("R-042 is declared as decision-bucketed but Appendix A buckets it to a milestone: M4"). That assertion earns its place. (3) THE ATTACK THAT SUCCEEDED: I set R-042's bucket to DR-9999 in both documents, a decision record that does not exist, and the coverage gate, the counts deepEqual and the new cross-document test were ALL GREEN, detail string unchanged. See F-3. (4) A CONSISTENT SWAP (R-041 M4->M2 and R-048 M2->M4 in both documents) is invisible to everything because the counts balance; that is outside the invariant the test claims, so it is a residual (F-2), not a broken claim. (5) The count pin over an append-only registry: I checked whether the inventory is append-only and it is not. `git log --all` shows exactly TWO commits have ever touched delivery/requirements/migration-table.md and both carry 115 rows, and shipped src/gates/coverage.ts:194 already pins expectedUnits 115, so `compared === 115` is legitimate; the exemption list is correctly asserted by name and one-directionally so a seventh decision row will not redden it. (6) Whether collapsing six assert.equal into one deepEqual lost coverage: it did not. The file imports `strict as assert`, so deepEqual is deepStrictEqual, and the new form pins the WHOLE perKind and perMilestone objects where the old one pinned seven keys, so an unexpected extra key now fails where it used to be ignored. Strictly stronger. (7) Shipped reach, four ways, all negative: no shipped file names any moved row or D-15 (one false positive, "M2-D-15" at src/witness/run.ts:1191); both edited files are line-count stable at 207 and 557 base and head, so schemas/report.schema.json:115's citation into migration-table.md:39 did not move; the coverage gate reproduces units 115 and a detail byte-identical to the plan's prediction; and the `decision` bucket kind was already declared and already executed 115 times per run in unchanged shipped code (src/gates/coverage.ts:489-495 tests EVERY kind for EVERY row, no short-circuit), so the phase does not even increase exposure to the wall-clock regex bound. (8) Data loss: `git merge-tree --write-tree 12ba558 ca20e79` exits 0 and the stack base's advance shares zero files with the phase's six. (9) The suite: I re-ran it and got 851 tests, 848 pass, 3 FAIL, 0 skipped, exit 1. I diagnosed all three rather than attributing them to the branch. Two are boundedExec wall-clock timeouts on patterns that cannot backtrack (`^(?:DR-[0-9]{4}|D-[0-9]+|M2-D-[0-9]+)$` against a 2-character value, `^(?:parked)$` against a 5-character value) at load 55-67 on nproc 4, which is precisely the mechanism the work history records in section 8.2.1; both tests pass IN ISOLATION on the same tree, interpreter and build state at load 67. The third is `Cannot find module .../bin/tiphys.ts` from runCliUnprivileged, and `namei -m` prints `drwx------ claude-0`, which is standing warning 1's trap caused by where I put my clone. The TEST COUNT 851 matches the implementer exactly. (10) The claim greps, both binding forms plus measurement 10's seven passive forms: 48 line-visible occurrences, 48 wrap-insensitive, 0 missed by wrap, which matches the work history's own Appendix B self-report exactly. The passive grep returns one real authored hit, delivery/work-history/m4-p13.md:299 ("the property holds NOW and is checked NOW"), which F-2 shows is stronger than what was built.

## suite_reproduced

Yes, and it did not reproduce green. My run: scratch git CLONE (not a git archive copy) at ca20e79, node v26.6.0 from /tmp/m4p13-node26, `npm run build` exit 0 with a clean `git status` afterwards, invocation `npm test`. Result: 851 tests, 848 pass, 3 fail, 0 skipped, 0 todo, TEST_EXIT=1, duration 1186s. The implementer reported 851/851/0 fail/0 skipped/exit 0 at head a6e43ff (and a6e43ff..ca20e79 touches only the work history, so their number carries to the final head), with the base established FIRST at bd92a01 as 849/849/0/0/exit 0. The test count 851 is identical in both runs and the 849+2 arithmetic checks out independently: test/coverage-gate.test.ts goes 17 -> 19 tests and test/behaviors.json goes 775 -> 777 rows, which also matches the suite gate's reported "777 behavior(s) resolve". All three of my failures are environment: two are `boundedExec` 250ms WALL-CLOCK timeouts at src/gates/coverage.ts:260 on non-backtracking patterns under load 55-67 on 4 CPUs (both pass in isolation on the identical tree), the third is the /tmp/claude-0 traversal trap confirmed with `namei -m`. The work history's suite sentence names interpreter, build state and invocation but NOT measurement 9's fourth qualifier (checkout versus archive); that measurement was committed at 2026-09-16 01:56, twenty-six minutes AFTER this head at 01:30, so the implementer could not have known it, and section 8.2.1's working `git diff --name-only` and `git status --short` captures settle that it was a git checkout anyway. The honest sentence for this head today is that the suite is LOAD-DEPENDENT on this machine and 851 is the count both of us measured.

## citation_hit_rate

26 of 26 = 100%. I extracted every path:LINE token from delivery/work-history/m4-p13.md and read the cited line on the branch for a 26-token sample chosen to favour the ones most likely to have drifted: test/coverage-gate.test.ts:494 (the verbatim-row pin the implementer says they corrected from :483), :988, :156, :213, :51; src/gates/coverage.ts:55, :235, :253, :356, :797; src/gates/citations.ts:233; test/gates.test.ts:3530; test/gate-registry.test.ts:459; test/behaviors.json:209; delivery/plan/kernel-plan-m4.md:2064, :2087, :2162, :2199, :2207; delivery/plan/kernel-plan-v1.md:368, :370, :390, :435; DR-0029:38 and :50; delivery/plan/m4-conflict-pre-pass.md:16; src/checks.ts:1802. Every one says what is claimed. I also independently reproduced the citations GATE at the final head ca20e79: green, 18 changed documents, 504 citations resolved, 0 self-citation, 0 unverifiable-external, exit 0. One base-branch (not this phase) mis-pointed citation is recorded as an observation: delivery/plan/kernel-plan-m4.md:2174 cites delivery/plan/m4-intake.md:766 for the R-064 ruleset measurement, but line 766 is the heading "## 7. Owner actions" and the measurement is at line 780.

## not_read

I did not read the twenty-two prerequisite commits' content in depth: delivery/plan/kernel-plan-m4.md (3719 lines) outside sections 5 and 3.2, delivery/plan/m4-intake.md outside sections 6 and 7, delivery/plan/pstack-borrow-review.md, delivery/STATE.md, the nine new DR records (I read DR-0028 in full and DR-0029's Part 1 to check the disposition's authority, and skimmed the rest), and the ten delivery/evidence/m4-probes/ documents. These are on the branch only because it is cut from plan/pstack-borrow-review at 6961186; they are NOT this phase's work and my APPROVE does not cover them. I read all twelve headings and the relevant bodies of delivery/verification/m4-prototype-probes.md and confirmed none of its twelve measurements bears on the migration table. I did not observe CI on either event arm (T-009's both-arms rule is unsatisfied for this head by me as well as by the implementer). I did not re-run the `suite` gate or the full gate bundle, so the work history's "777 behavior(s) resolve" is corroborated only by my independent count of test/behaviors.json rows, not by re-running the gate. I did not run the full suite a second time at low load, so I cannot say whether 851/851 reproduces when the machine is quiet. I did not attempt to construct a synthetic merge base carrying the declaration to see what the scope gate says about the 27 inherited paths; I established analytically that they are undeclared and that they disappear once the stack base lands.

## findings

### 1. [MEDIUM] The plan now contradicts itself on R-072 and R-097, and the contradicting line is the one the work history cites as its own authority for the move.

- **evidence**: On the branch at ca20e79: delivery/plan/kernel-plan-v1.md:390 still reads "- D-15: The kernel repo's own CI gets a per-ref concurrency group in M1-P1 ...; the productized form of R-072/R-097 (bootstrap checks for onboarded projects) remains M4." while delivery/plan/kernel-plan-v1.md:523 and :552 now bucket R-072 and R-097 to M5. A second site, delivery/plan/kernel-plan-v1.md:136, reads "R-072 and R-097 (kernel repo's own concurrency group, early instance per plan decision D-15; the productized form stays M4)". The phase edited D-12 at delivery/plan/kernel-plan-v1.md:387 in exactly the right way, adding a supersession sentence; D-15 is THREE LINES BELOW IT in the same bulleted list and was left asserting M4. delivery/work-history/m4-p13.md:256 cites delivery/plan/kernel-plan-v1.md:390 as the authority for moving R-072, quoting the first half of D-15 and not the second. delivery/plan/phase-declarations/m4-p13.json's citations array contains "D-15", so the phase declared it, read it, quoted half of it and left it wrong. The reason the derivation missed it: all three greps in work-history section 2.1 search for restatements of the COUNTS and none searches for the row IDS; `git grep -nE 'R-072|R-097' -- delivery/plan/kernel-plan-v1.md` surfaces both sites immediately. Work-history section 9 item 4 admits "I did not check whether any OTHER prose paragraph enumerates the same rows", which is precisely this.

- **reaches_shipped**: NO. I checked directly: `git grep -nE 'R-041|R-042|R-045|R-046|R-047|R-050a|R-051|R-064|R-065a|R-071|R-072|R-097|D-12|D-15' -- src/ bin/ schemas/ roles/ tuition/` returns one line and it is the unrelated token "M2-D-15" at src/witness/run.ts:1191. No shipped file or user-visible command names a moved row or D-15. TRACKED, not blocking, per DR-0027.

- **recommendation**: Two edits, foldable into the merge rather than a fix round. Append a supersession clause to D-15 at delivery/plan/kernel-plan-v1.md:390 in the same shape as the one added to D-12, and correct the parenthetical at delivery/plan/kernel-plan-v1.md:136. Both are inside the phase's declared filesToTouch. And record the widened mechanism for the phases that follow: after any re-disposition, grep the ROW IDS across every live plan document, not only the counts.

### 2. [MEDIUM] A requirement can be discharged to an owner decision record that does not exist, and the coverage gate, the counts deepEqual and the new cross-document test are all green. The work history over-states what the new test enforces.

- **evidence**: This phase makes `decision` a USED bucket kind for the first time in this repository (per-kind decision goes from absent to 6), so I attacked that new surface. In a scratch clone at ca20e79 I changed R-042's bucket from DR-0029 to DR-9999 in BOTH documents. `ls delivery/decisions/ | grep -c 9999` is 0. Results: `node src/gates/coverage.ts` -> green, 115 units, detail UNCHANGED ("decision 6, milestone 98, phase 11; per-milestone: M1 11, M2 16, M3 74, M4 5, M5 3, decision 6"); `node --test --test-name-pattern 'Appendix A bucket agrees'` -> pass 1, fail 0; `node --test --test-name-pattern 'units 115 with per-kind'` -> pass 1, fail 0. All green against a fabricated decision record. The over-claim is at work-history section 6.3: "a decision or parked bucket must match the migration table's cell VERBATIM, so the six DR-0029 rows are required to say DR-0029 in BOTH documents". The first half is true; the "so" does not follow. The declared-list loop at test/coverage-gate.test.ts:1004-1012 only asserts `exempt.includes(id)`, i.e. that the bucket is not a milestone. It never pins the value to DR-0029 and never checks that the record resolves. This is the same shape CLAUDE.md's identifier-schemes section already records being paid for once, when DR-0019 was created and then deleted as a FABRICATED owner decision and was caught by a delta verifier rather than by any check; now it has six live users.

- **reaches_shipped**: NO. The permissive `decision` pattern `DR-[0-9]{4}|D-[0-9]+|M2-D-[0-9]+` is pre-existing and unchanged at src/gates/coverage.ts:189, and under DR-0029 an adopting project supplies its own bucketKinds, so the kernel's generic gate is not wrong. The gap is in the new test (test/) and in delivery/ data. TRACKED, not blocking, per DR-0027.

- **recommendation**: One line inside the loop the phase already wrote: for each declared id, assert the bucket VALUE equals the expected decision id, and assert that `delivery/decisions/<id>-*.md` resolves. That turns the declared exemption list from "is not a milestone" into the check the work history says it already is, and it closes the fabricated-decision path for every future re-disposition.

### 3. [LOW] The new cross-document test is blind to a consistent SWAP of two rows' milestones, and the work history's "the property holds NOW and is checked NOW" is stronger than what was built.

- **evidence**: Seeded in a scratch clone at ca20e79: R-041 M4->M2 and R-048 M2->M4, changed consistently in BOTH delivery/requirements/migration-table.md and delivery/plan/kernel-plan-v1.md. `node --test --test-name-pattern 'Appendix A bucket agrees'` -> pass 1, fail 0. `node --test --test-name-pattern 'units 115 with per-kind'` -> pass 1, fail 0. The coverage gate is green with an unchanged detail. The distribution balances, the two documents agree, and the disposition is wrong. This sits outside the invariant the test claims ("buckets follow the migration table's milestone column"), so it is a residual rather than a broken claim, but delivery/work-history/m4-p13.md:299 asserts "the property holds NOW and is checked NOW", and that sentence is the one real authored hit from measurement 10's passive-form grep.

- **reaches_shipped**: NO. test/ and delivery/ only; no shipped file is affected. TRACKED.

- **recommendation**: Either narrow the sentence at delivery/work-history/m4-p13.md:299 to the invariant that is actually mechanised (the two documents agree row by row), or record the swap as a known residual so the next phase that extends this guard knows what it does not cover. No code change needed for this phase.

### 4. [LOW] A new live claim about repository configuration was written into a long-lived requirements artifact with no pointer to its measurement, and the one pointer that does exist elsewhere resolves to a heading.

- **evidence**: The R-064 row this phase wrote into delivery/requirements/migration-table.md asserts "already discharged on the kernel repository, whose ruleset main-protection is enforcement active with required_status_checks naming gates", with no citation and no capture anywhere in delivery/work-history/m4-p13.md (grepping the work history for main-protection/required_status_checks/ruleset returns only the disposition table restating the claim). The underlying measurement does exist, at delivery/plan/m4-intake.md:780. The base-branch document that carries it forward, delivery/plan/kernel-plan-m4.md:2174, cites delivery/plan/m4-intake.md:766, and line 766 is the heading "## 7. Owner actions", not the measurement. The citations gate passed because in-range resolution is all it checks, which is the silent-resolution hazard CLAUDE.md names under rule 3b.

- **reaches_shipped**: NO. delivery/ paperwork only. TRACKED.

- **recommendation**: Add `delivery/plan/m4-intake.md:780` to the R-064 note, and correct the :766 pointer at delivery/plan/kernel-plan-m4.md:2174 when that file is next edited (it is not on this phase's list).

### 5. [LOW] The new test repeats the literal 115 twice where the value is already available from the config object it imports, and the red-witness test reintroduces two bare single-number pins of the kind this round exists to remove.

- **evidence**: test/coverage-gate.test.ts asserts `compared === 115` and `totalInventoryIds: 115` while `coverageModule.KERNEL_COVERAGE_CONFIG.expectedUnits` is 115 (src/gates/coverage.ts:194) and is already in scope in both tests. Separately, the round's stated mechanism is that "six separate assert.equal calls over one report abort at the first mismatch, so the failure names one number"; the new red-witness test then adds `assert.equal(gateOne.perMilestone["M4"], 5)` and `assert.equal(gateTwo.perMilestone["M4"], 6)`, and `assert.equal(compared, 115)` runs BEFORE `assert.deepEqual(mismatches, [])` so a count drift aborts before the mismatch list is printed. I checked the append-only question before raising this and it is NOT a convention-5 violation: `git log --all` shows exactly two commits have ever touched delivery/requirements/migration-table.md and both carry 115 rows, so the inventory is a closed extraction, and shipped code already pins the same number.

- **reaches_shipped**: NO. test/ only, and the shipped pin it duplicates (expectedUnits 115) is pre-existing and correct. TRACKED.

- **recommendation**: Derive both literals from `KERNEL_COVERAGE_CONFIG.expectedUnits`, which keeps the anti-skip property and removes a third site to move. The two perMilestone pins are load-bearing for the structural-difference argument, so leave them but be aware the round did not eliminate the shape it named.
