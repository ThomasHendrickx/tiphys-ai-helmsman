# M4-P27 clean-room review by Claude Opus 5 (claude-opus-5)

**VERDICT AS RETURNED: APPROVE**

## framing

Evidence integrity and data loss combined: assume every claim is unproven until I have run it, and assume the change can destroy work until I have found why it cannot.

## derivation_honesty

HONEST, and unusually good. Section 6.3 of delivery/work-history/m4-p27.md names eight gaps, names the MECHANISM ("a predicate whose false case and unknown case are collapsed into one value") rather than the instances, and publishes a 48-site derivation with full output that I reproduced exactly (grep -n 'return arm(\|verdict: "' over the two scripts returns 48 lines in a clean clone at 4e95204). TWO THINGS ARE WORSE THAN THEY SOUND. (1) Item 2 admits arms a and c were never run against a real `tiphys cutover status` and says "If the delivered shapes differ, both arms fail closed to `unreachable`, which is the safe direction". I MEASURED THAT SENTENCE FALSE TWICE: a stub exiting 1 while printing a stale DRAIN line gives ARM a satisfied, exit 0; a stub printing only a summary line gives ARM c satisfied, exit 0. Both fail OPEN. This is the sentence the fix-round contract tells the reviewer to weigh first, and it is the one claim in the document that does not survive execution. (2) ONE GAP IS MISSING ENTIRELY: arm d measures filesystem mtime, which git does not preserve, so it is decided by checkout order rather than by content. The derivation could not reach it because it enumerated where a VERDICT IS DECIDED, never what each verdict's INPUT actually measures.

## what_i_tried_to_break

I re-ran EIGHT of the twelve red witnesses in a mutation lab I rebuilt myself from the section 5.3 descriptions (the implementer's lab is uncommitted). All eight reddened with green unmutated controls: D2, D4, D5, D6, D8, D9, D9b, D12. D8 is red against the HARM, not the absent guard: its first failing assertion is "the probe created a commit in the scratch repository" with the two head shas printed. D9's failure line reproduced the work history's capture byte for byte. I THEN BUILT A NINTH DEFANG THE IMPLEMENTER DID NOT RUN (D8b, let ONLY `checkout` through, so the head assertion cannot mask member B) and confirmed member B reddens alone and against the harm: "the probe altered the uncommitted edit in the scratch repository". The git chokepoint class is genuinely two structurally different members. I attacked the registry discipline: 31 rows appended by name, no count, no length assertion anywhere; the only counted assertions count call sites in the probe's own source, which is not append-only, and are backed by an import-binding assertion. I attacked the verb grep: it carries a control string and asserts every pattern fires on it, so a zero is the source being clean. I verified 20 citations by reading the cited line, 20 of 20 correct. I reproduced the citations gate EXACTLY in a clean clone (18 documents, 522 citations, 0 self, 0 unverifiable, exit 0 at 4e95204). I verified the elided-sha claim: `git diff --name-only 62007b9 4e95204` prints exactly one path and it is the work history. I re-ran both binding claim greps plus the seven-passive-form variant, then wrote my own fence/section classifier and got the same 39/9/6 split and the same six prose lines; the passive forms add no new over-claim. I reproduced the trigger document's own measured state (all four arms not-yet, exit 1). C-1, C-2 and C-3 are clean: a grep for pid, /proc, kill, signals, liveness across both scripts, the test file and the document returns ZERO hits; nothing reads state from a log tail; nothing is auto-backgrounded. What broke: arm d's mtime proxy in both directions, arms a and c ignoring the CLI exit status, arm c's word-grep row counter, an untested `--json` path with no HALT, `--out` destroying prior evidence, an unescaped target injecting a fabricated `satisfied` row into the evidence table, and a red scope gate the work history never ran.

## suite_reproduced

PARTIALLY, and I state what I could not do rather than quoting the work history. Clean clone at 4e95204, `npm run build` exit 0 with `git status --short` clean. `test/cutover-entry.test.ts` alone: node v22.22.2, dist built, git CHECKOUT tree, invocation `node --test --test-reporter tap test/cutover-entry.test.ts` gives 32 tests, 32 pass, 0 fail, 0 SKIPPED, exit 0. That matches the work history exactly. `test/coverage-gate.test.ts` alone on the same head and interpreter gives 17 tests, 17 pass, 0 fail, 0 skipped, exit 0, which corroborates section 8.4's load attribution. I could NOT reproduce the full-suite numbers (881 tests, 876 pass, 3 fail, 2 skipped on v22.22.2): `npm test` did not finish inside 580 seconds with the container at load 41 to 61 on nproc 4 and nine other agents running, and no node v26.6.0 toolchain exists here now. I did not re-run the file-removed control. The work history's suite sentence names interpreter, build state, invocation, pass count, skipped count and the measured head, but NOT the fourth qualifier (git checkout versus `git archive` copy).

## citation_hit_rate

20 of 20 sampled citations resolve and say what is claimed (100 percent), across delivery/plan/cutover/entry-trigger.md, delivery/work-history/m4-p27.md and the two scripts. Separately, the citations gate reproduced exactly at the submitted head: 18 changed documents, 522 citations resolved, 0 self-citations, 0 unverifiable-external, exit 0. FIVE `:1` placeholder citations are reported as finding 8 and excluded from the 20: three of them point at delivery/verification/m4-prototype-probes.md:1, which is the document title, while the claim they attach to lives at :153 (correctly cited elsewhere in the same phase).

## not_read

I did not read: the nine unrelated M4 decision records, delivery/plan/m4-intake.md beyond line 150, delivery/plan/pstack-borrow-review.md, the eleven delivery/evidence/m4-probes/ documents, or the 25 orchestrator commits the branch carries beneath the phase commit (I read their names and diffstat only). I read delivery/verification/m4-prototype-probes.md only at the lines cited (1, 46, 153, 286) rather than in full, and delivery/plan/kernel-plan-m4.md only at 3289 and 3538-3620. I did not run `node --test` over any test file other than test/cutover-entry.test.ts and test/coverage-gate.test.ts, did not run the full gate bundle (only scope and citations), did not run `scripts/check-authored-bytes.mjs`, and did not probe the real pilot repositories. I did not re-run witnesses D1, D3, D7, D10 or D11.

## findings

### 1. [HIGH] Arm d measures filesystem mtime, so its verdict is decided by checkout order rather than by content, in both directions

- **evidence**: Plan text at delivery/plan/kernel-plan-m4.md:3570 asks for the ruleset to be "present and NEWER than the most recent inventory change". Implementation at scripts/check-cutover-entry.mjs:455 is `rulesetMtime < newest.mtime` over `statSync(...).mtimeMs`. FALSE GREEN, measured: a fixture with the ruleset genuinely older reports `ARM d ... not-yet`, EXIT=1; `touch` alone, with sha1sum 6d42a50f74030c1ed54a5a2c6bbbb704a2abe058 identical before and after, flips it to `ARM d ... satisfied`, EXIT=0. FALSE RED, measured, and it is the state every fresh clone is in: git does not preserve mtimes, so in a scratch repo where the ruleset was committed FIRST and the inventory a second later, `git clone` gave mtimes 1789525058762.029 and 1789525058762.5947 and armRuleset returned `not-yet` on a 0.57 MILLISECOND gap that is only the order git wrote two files whose names sort p before r. On a coarser-timestamp filesystem the two are EQUAL and the implementation's `<` (the plan said "newer than", the code and its output string say "not older than", an undeclared relaxation) returns SATISFIED. Witness D4 reddens only the NO-INVENTORY case; the one test that exercises staleness, test/cutover-entry.test.ts:261, creates it with utimesSync back-dating, a state git cannot produce. Neither the work history's eight-item not-covered list, nor delivery/plan/cutover/entry-trigger.md:74, nor the source comment mentions mtime.

- **reaches_shipped**: I cannot name a shipped file or user-visible shipped command this reaches. scripts/check-cutover-entry.mjs is under scripts/, not src/, bin/, schemas/, roles/ or tuition/. TRACKED, not blocking, under DR-0027. It should nevertheless be fixed before the trigger is ever ACTED on, because it is the one arm that does not depend on M4-P25 and it is wrong today.

- **recommendation**: Stop using mtime. Compare the ruleset's own recorded capture timestamp (it already carries a `capturedAt` field in the fixture) against the inventory's, or use `git log -1 --format=%ct -- <path>` on both so the comparison is over commit order rather than checkout order. Add a witness that reddens on a state git can actually produce, and make the equal case NOT satisfied so the code matches the plan's "newer than".

### 2. [MEDIUM] Arms a and c never look at the CLI's exit status, so a FAILING cutover status that still prints a DRAIN line is read as satisfied

- **evidence**: classifyCliFailure at scripts/check-cutover-entry.mjs:176 turns a run into a failure only for exit 64, a /unknown subcommand|usage:/ match, or a permission-refusal regex. Every other nonzero exit falls through to `return null` and armDrain parses the text as if the command had succeeded. Measured with a stub CLI printing `SWITCH planning-and-scope kernel\nDRAIN clean\nERROR: could not read the drain register, the numbers above are stale` and exiting 1: `ARM a drain satisfied -- cutover status reports DRAIN clean`, `STEP 1 preconditions: preconditions-satisfied-owner-action-pending`, EXIT=0. This FALSIFIES work history section 6.3 item 2, the sentence the fix-round contract tells the reviewer to check first: "If the delivered shapes differ, both arms fail closed to `unreachable`, which is the safe direction." They fail OPEN.

- **reaches_shipped**: I cannot name a shipped file this reaches; scripts/check-cutover-entry.mjs is not under src/, bin/, schemas/, roles/ or tuition/. TRACKED, not blocking, under DR-0027.

- **recommendation**: Require `run.status === 0` before parsing in both armDrain and armRetirement; a nonzero exit that is not a usage error and not a refusal is `unreachable`, not an invitation to parse. Fold this into the M4-P25 reconciliation the work history already owes, and correct the section 6.3 item 2 sentence, which is currently a false safety claim in the document a later reviewer trusts.

### 3. [MEDIUM] Arm c counts WORDS, so a summary line reads as a ported row and defeats the zero-rows guard

- **evidence**: countRetirementRows at scripts/check-cutover-entry.mjs:369 counts any line containing `unported` as an unported row and any other line containing `ported` as a ported row. Measured with a stub printing ONLY `RETIREMENT SUMMARY: 12 rows, all ported` and no rows at all: `ARM c retirement satisfied -- all 1 retirement row(s) are ported`, EXIT=0. The zero-rows guard is one of the phase's headline properties ("zero rows is not zero unported rows", witness D3, behavior cutover-entry-zero-retirement-rows-is-unreachable) and it is defeated by any single line that happens to contain the word. This is the same class as the exit-status finding, with a structurally different member: that one is the exit code, this one is the row grammar.

- **reaches_shipped**: I cannot name a shipped file this reaches; scripts/check-cutover-entry.mjs is not under src/, bin/, schemas/, roles/ or tuition/. TRACKED, not blocking, under DR-0027.

- **recommendation**: Anchor the row regex to the row shape M4-P25 criterion 6 actually specifies (the fixture uses `PORT <name> ported`), for example `/^\s*PORT\s+\S+\s+(un)?ported\s*$/`, and treat any line that matches neither the row shape nor a known header as an unrecognised shape rather than ignoring it.

### 4. [MEDIUM] The scope gate is RED on this branch and the work history never ran it

- **evidence**: Run with the repository's own runner in a clean clone at 4e95204, on a correctly named local branch, base origin/main (3b40118, the same merge base the pull request will have): `gates: declared 1 applicable 1 verdict 1 green 0 red 1`, `gates: scope: red: branch claude/m4-p27-cutover-entry-trigger (phase m4-p27) matches the phase pattern but no phase declaration exists at delivery/plan/phase-declarations/m4-p27.json in the merge base 3b401182301361700ffa0fd4b2ae099993b3d733 of --base origin/main and --head HEAD; the declaration must be committed to main before the phase branch is created`, EXIT=1. M3-P11's both-declarations relaxation at src/gates/scope.ts:110 covers an ENTRY added on the head, not a declaration file absent from the merge base entirely. Work history section 8.1 lists npm ci, npm run build, check-authored-bytes and citations only; `scope` appears nowhere in delivery/work-history/m4-p27.md (grep for 'scope' returns one hit, and it is about the derivation's scope). Section 9 nevertheless says "Nothing is marked CI-deferred. Every criterion was walked locally."

- **reaches_shipped**: Not a shipped artifact; this is a delivery-state fact about the pull request. TRACKED as an ORCHESTRATOR ACTION rather than a fix round: CI will be red until the declaration reaches main.

- **recommendation**: Land delivery/plan/phase-declarations/m4-p27.json on main before opening or re-basing the phase pull request, as CLAUDE.md's gates note already warns ("Scope declaration grants still need their own pull request"). Add `--only scope --phase <id>` to the local gate list in the work history template so the next phase does not learn this from CI, which DR-0031 calls a defect in the local procedure.

### 5. [MEDIUM] probe-pilot-readonly.mjs --out truncates any existing file before the first read, destroying prior evidence

- **evidence**: scripts/probe-pilot-readonly.mjs:601 calls `writeFileSync(options.out, beaconHeader(...))` unconditionally, before any target is probed, with no existence check and no refusal. Measured: a file containing `IMPORTANT PRIOR EVIDENCE` (37 bytes) was passed as --out to a run against an unreachable base; the run reported OVERALL unreachable, EXIT=3, and `grep -c 'IMPORTANT PRIOR EVIDENCE'` on the file afterwards returned 0. At the DEFAULT path this is the case that matters: DEFAULT_OUT is delivery/verification/pulse-re-probe.md, the evidence document the trigger's step 2 produces, so a second run that is REFUSED destroys a first run that SUCCEEDED, and destroys it before it knows it will be refused. The T-008 beacon rule requires writing the header early; it does not require clobbering the previous run.

- **reaches_shipped**: I cannot name a shipped file this reaches; it destroys content under delivery/verification/, which is process paperwork, not a kernel deliverable. TRACKED, not blocking, under DR-0027. It is the single largest data-loss surface in the change.

- **recommendation**: Refuse to truncate an existing file unless --force is given, or write to a timestamped path and update a stable pointer at the end. Either keeps the beacon property (a partial file exists from the first minutes) without trading away the previous run's record.

### 6. [LOW] A target string is interpolated into the evidence table unescaped, so a --repo value can inject a fabricated row reading satisfied

- **evidence**: targetRow at scripts/probe-pilot-readonly.mjs:498 passes `detail` through `cell()` (which replaces pipes and newlines) but interpolates `result.target` raw. Measured with one --repo argument carrying a pipe and a newline, the evidence document gained: `| `x/y | satisfied | all good |` and `| `z/w` | satisfied | fabricated row` | unreachable | ... |`. A rendered markdown table now shows a row whose verdict column reads `satisfied`. The OVERALL line still says unreachable and the reason column names the malformation, so a careful reader is not fooled.

- **reaches_shipped**: I cannot name a shipped file this reaches; the injection lands in delivery/verification/, and the injecting value comes from the operator's own command line. TRACKED, not blocking, under DR-0027.

- **recommendation**: Use `cell(result.target)` in the same expression that already calls `cell(detail)`. One function call.

### 7. [LOW] Five :1 placeholder citations, three of them pointing at a document title rather than the claim

- **evidence**: delivery/plan/cutover/entry-trigger.md:67 cites delivery/work-history/m4-p27.md:1; entry-trigger.md:117 cites test/cutover-entry.test.ts:1; scripts/check-cutover-entry.mjs:22, scripts/probe-pilot-readonly.mjs:34 and test/cutover-entry.test.ts:290 all cite delivery/verification/m4-prototype-probes.md:1. Line 1 of that file is `# M4 prototype probes: what was measured, and what it overturns`. The claim all three attach to ("a nonzero exit does NOT mean the condition is false") is at :153, which the work history at line 43 and entry-trigger.md at line 34 both cite CORRECTLY. The three in-code ones pass only because .mjs and .ts are outside the citation gate's document set (src/gates/citations.ts:232, six globs, none of them source). This is CLAUDE.md rule 3b one scope smaller: the citation that reddens is not the dangerous one, and a citation that resolves silently against the wrong line is.

- **reaches_shipped**: I cannot name a shipped file this reaches; two are in a delivery document and three are in comments under scripts/ and test/. TRACKED, not blocking, under DR-0027.

- **recommendation**: Point the three m4-prototype-probes citations at :153. For the two file-pointer citations, either give the real line or use backticks deliberately, which CLAUDE.md 3b says is a legitimate way to name a file you are not asserting exists at a particular line.

### 8. [LOW] The D9b capture in the work history cannot have come from the shipped test

- **evidence**: Work history section 5.4 quotes D9b as `requests reached the server: PATCH /anything, GET /anything`, two entries. The shipped test at test/cutover-entry.test.ts:502 issues THREE shaping attempts: {method:"PATCH"}, {body:"x"}, {redirect:"follow"}. I rebuilt the described defang (remove the option-key allowlist AND let options.method steer the request) and got `requests reached the server: PATCH /anything, GET /anything, GET /anything`, three entries. Section 5.2 explicitly DROPPED two earlier lab passes for exactly this reason ("a capture that does not correspond to the code it is offered as evidence for is worse than no capture"); this one was retained. The lab script is deliberately uncommitted, so the capture cannot be audited any other way. The PROPERTY D9b claims does hold: I reproduced a real PATCH reaching a real server.

- **reaches_shipped**: No shipped artifact; this is a work-history evidence-integrity point. TRACKED, not blocking, under DR-0027.

- **recommendation**: Re-capture D9b against the shipped test, or say in one sentence that the capture predates the third shaping option. Given section 5.2's own standard, the first is cheaper than defending the second.

### 9. [LOW] A crash in the probe exits 1, which is that script's own EXIT_REAL_NEGATIVE

- **evidence**: `node scripts/probe-pilot-readonly.mjs --repo owner/name --out` (trailing flag with no value) resolves out to the current working directory, then throws EISDIR on `open`, and node exits 1. scripts/probe-pilot-readonly.mjs:84 defines EXIT_REAL_NEGATIVE = 1. So a caller reading exit 1 as "the remote answered and the answer is a real negative" is wrong. This is exactly the collapse of the unknown case into the false case that the whole phase exists against, arriving through the process exit code instead of through a verdict. The checker's parseArgs validates its own missing values (--require-behavior and --exclusion-test both return usage); the probe's does not, for any of --api-base value-position, --out or --repo.

- **reaches_shipped**: I cannot name a shipped file this reaches; scripts/probe-pilot-readonly.mjs is not under src/, bin/, schemas/, roles/ or tuition/. TRACKED, not blocking, under DR-0027.

- **recommendation**: Validate --out and --repo the way --api-base and --timeout-ms are already validated, and wrap run() so an unexpected throw exits with the indeterminate code (3) rather than the real-negative code (1).

### 10. [LOW] --json prints no step-3 HALT and no test exercises it, contradicting the commit message and the work history

- **evidence**: Measured on the all-arms-satisfied fixture: `node scripts/check-cutover-entry.mjs --root <all satisfied> --json` prints `"overall": "preconditions-satisfied-owner-action-pending"`, the four arms, and `"ownerAction": {"step": 3, "status": "blocked", ...}`, EXIT=0. There is no `HALT, OWNER ACTION`, no `never reports it done`, and no STEP 2 or STEP 4 line, because render() and STEP_LINES are only reached on the non-json branch (scripts/check-cutover-entry.mjs:580). The commit message says "Step 3 prints as a HALT on every run, including the all-satisfied one" and work history section 2 says "prints the step-3 HALT on EVERY run"; both are false for this mode. `grep -n json test/cutover-entry.test.ts` finds no test that passes --json, so the forbidden-ready-token assertion at test/cutover-entry.test.ts:231 never runs against this path. The substantive safety property survives: ownerAction.status is `blocked` and no vocabulary member means ready.

- **reaches_shipped**: I cannot name a shipped file this reaches; scripts/check-cutover-entry.mjs is not under src/, bin/, schemas/, roles/ or tuition/. TRACKED, not blocking, under DR-0027.

- **recommendation**: Add one test that runs --json on the all-satisfied fixture and asserts the ownerAction block plus the same forbidden-token list, and correct the two "on EVERY run" sentences to say "in the human-readable output".

### 11. [LOW] The suite sentence is missing the fourth qualifier and the branch carries 25 foreign commits

- **evidence**: Two separate small things recorded together. (1) Work history section 8.2 gives interpreter, build state, invocation, pass and skipped counts and the measured head 62007b9, but does not say whether the tree was a git checkout or a `git archive` copy, which the 2026-09-16 measurement showed changes the numbers (863 pass exit 0 against 850 pass, 13 fail, exit 1, every failure a git exit-128 artifact). (2) The work history records "cut from: `plan/pstack-borrow-review` at `c86c07f`". Measured: `git log --oneline 3b40118..claude/m4-p27-cutover-entry-trigger` is 26 commits, of which ONE is the phase. Merging this pull request also lands CLAUDE.md, delivery/STATE.md, the 3719-line M4 plan, nine decision records, the conflict pre-pass and T-026. DR-0031's mirror: the pull request's contents do not match the unit of value it claims to deliver. The phase COMMIT itself is clean: `git show --name-only 4e95204` is exactly the four declared files plus the three declared extras, matching delivery/plan/m4-conflict-pre-pass.md:132.

- **reaches_shipped**: Neither reaches a shipped artifact. The second is an orchestrator merge-ordering concern, not a code defect. TRACKED, not blocking, under DR-0027.

- **recommendation**: State the tree kind alongside the interpreter, build state and invocation. For the branch shape, decide the merge order deliberately: either land the M4 plan branch first so the phase pull request carries one commit, or say explicitly in the pull request body that it carries the plan branch beneath it.
