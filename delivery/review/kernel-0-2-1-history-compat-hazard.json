{
  "kind": "verdict",
  "phase": "claude/kernel-0-2-1-history-compat",
  "head": "fc44f28e0cf473fecf6de6bf443fa04d0853d37b",
  "tiphys-version": "0.2.0",
  "verdict": "APPROVE",
  "produced-by": "claude-sonnet-5",
  "framing": "hazard-attack",
  "review-contract": "hazard",
  "hazard-classes-addressed": [
    {
      "class-id": "delta-scope-confirmed-test-only",
      "probed": "Ran `git diff --stat 281d8925f06dbb81cca16fd32d7199e600908c23 fc44f28e0cf473fecf6de6bf443fa04d0853d37b` directly.",
      "cleared-because": "21 files changed, all under delivery/review, delivery/work-history, test/behaviors.json, test/, and witness/. No src/, schemas/, roles/, bin/ or tuition/ path anywhere in the list. Confirmed exactly as the coordinator's brief claimed."
    },
    {
      "class-id": "env-mutation-confined-to-test-process",
      "probed": "Read the GIT_CEILING_DIRECTORIES / GIT_DIR-family strip block directly at test/dual-review.test.ts:55-76 (representative of all six victim files, matching the work history's own citations exactly).",
      "cleared-because": "The block sets process.env only inside a TEST file's own module scope, never in src/, bin/, or a shipped script. Node's test runner gives each test file its own process, so the mutation cannot leak between sibling test files, and confirmed by the diff-stat there is zero src/ change for it to leak into. Cannot mask or change kernel production behavior."
    },
    {
      "class-id": "ceiling-does-not-hide-a-consumer-facing-discovery-bug",
      "probed": "Confirmed via the diff-stat that src/checks.ts's resolveCorpusSource and gitIn (the kernel's own repository-discovery code) are untouched by this round, and reasoned from that fact plus the ceiling's scope (test-file-only).",
      "cleared-because": "A real consumer whose project sits nested under a directory that is itself a git repository gets the SAME upward git-discovery behavior after this PR as before it: the shipped kernel process is never given a GIT_CEILING_DIRECTORIES value by this change. That upward-walk behavior is a pre-existing design property, not something this round introduced, changed, or concealed. The ceiling exists solely so the TEST SUITE stops being fooled by CI's own scratch-directory ancestry; it has no reach into, and cannot mask, what a real invocation of the kernel does."
    },
    {
      "class-id": "rename-out-removal-safe-no-leftover-no-cross-test-race",
      "probed": "Read test/support/remove-git-directory.ts directly (renameSync to a fresh mkdtemp under the caller's graveyard, an existsSync assertion, then rmSync of the moved copy), and its three call sites (test/single-family-exception.test.ts:1318, test/resume.test.ts:367 and :442), each passing its own per-test scratch root as the graveyard.",
      "cleared-because": "Rename is one atomic directory-entry move, so the directory has a complete .git or none, closing the exact race the work history diagnoses (git 2.55's detached maintenance child unlinking by the OLD path, which no longer resolves after the rename). No leftover scratch: the graveyard's mkdtemp'd subdirectory is itself removed by the same function's own final rmSync before it returns. No cross-test race: each call site's graveyard is a fresh, disjoint per-test scratch directory (scratch() or makeTempDir(t)), so concurrent calls never share a target. One honestly stated limit: this review did not independently force the underlying git-2.55 race a second time (git 2.43.0 was used here, same as the implementer; CI's 2.55.0 was not reproduced by either of us), matching the implementer's own stated boundary that the unforced race was not reproduced (0/2900 trials) and that whether ad33779 fixes CI's intermittent failure is unknown until CI itself is observed."
    },
    {
      "class-id": "named-tests-run-and-counted",
      "probed": "node --test test/git-ceiling.test.ts test/dual-review.test.ts test/exit-test-local.test.ts test/kernel-charter.test.ts test/merge-preconditions.test.ts test/single-family-exception.test.ts test/verdict-head.test.ts test/remove-git-directory.test.ts test/resume.test.ts, node v26.6.0, dist/ built.",
      "cleared-because": "164 tests, 164 pass, 0 fail, 0 cancelled, 0 skipped, 0 todo. Full suite (node --test \"test/**/*.test.ts\") run in parallel with this review's write-up; see the report's Delta section for the final line once it completed."
    }
  ],
  "criteria": [
    {
      "id": "delta-item-1-scope",
      "quote": "Check `git diff --stat 281d892 fc44f28` confirms no src/, schemas/, roles/, bin/ or tuition/ change.",
      "evidence": [
        "git diff --stat run directly: 21 files, all delivery/, test/, witness/; zero src/, schemas/, roles/, bin/, tuition/ lines"
      ],
      "met": true
    },
    {
      "id": "delta-item-2-hazard-questions",
      "quote": "1. leak/mask production. 2. does the ceiling hide a real kernel bug for a nested consumer. 3. is the rename safe. 4. run the named tests and quote counts.",
      "evidence": [
        "q1: env mutation is test-file-module-scoped only, no src/ change, per-file process isolation confirmed by fix round 3's own derivation and consistent with this round's read",
        "q2: resolveCorpusSource/gitIn untouched; a nested consumer's discovery behavior is unchanged by this PR; the ceiling cannot mask what it never touches",
        "q3: rename-then-assert-then-remove read directly; race closed by path invalidation after rename; graveyard scoped per test, removed before return; one stated limit (git-2.55 race not independently re-forced)",
        "q4: 164 tests, 164 pass, 0 fail, 0 skipped, node v26.6.0, dist/ built"
      ],
      "met": true
    }
  ],
  "findings": [],
  "deviations-judged": []
}
