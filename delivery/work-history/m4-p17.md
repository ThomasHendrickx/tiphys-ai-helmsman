# M4-P17: doctor reports a post-reclaim fleet

Branch `claude/m4-p17-doctor-post-reclaim`, cut from `origin/main` at
`d044a52`. The phase section is delivery/plan/kernel-plan-m4.md:2683.

The command this phase changes is `tiphys doctor`. It gains two checks, has a
third verified rather than rebuilt, and has two existing verdicts corrected. The
clause it is delivering against is AGENTS.md:311, which says doctor reports
"which of the durable areas is present, which leases are expired and who last
held them, and which phase branches exist and are unmerged".

## TWO THINGS FOUND BEFORE ANY CODE, BOTH OF WHICH CHANGE THE READING

### 1. The phase declaration is not on `main`

The dispatch brief said the declaration was already on `main` and told me not to
create it. It is not there. Measured at `d044a52`:

```
$ ls delivery/plan/phase-declarations/m4-p17.json
ls: cannot access 'delivery/plan/phase-declarations/m4-p17.json': No such file or directory

$ for r in $(git for-each-ref --format='%(refname)' refs/remotes/origin); do \
    git cat-file -e "$r:delivery/plan/phase-declarations/m4-p17.json" 2>/dev/null \
      && echo "FOUND in $r"; done
FOUND in refs/remotes/origin/claude/prepass-wave-5
```

It is on an unmerged branch. That matters because the scope gate reads the
declaration from the MERGE BASE, and src/gates/scope.ts:874 is the arm that
fires when it is not there: status `red`, "the declaration must be committed to
main before the phase branch is created". So the scope gate cannot go green on
this branch until `claude/prepass-wave-5` lands, whatever this phase does. That
is recorded here and escalated rather than worked around.

I did create the file, for two reasons. The plan lists it in this phase's
files-to-touch (delivery/plan/kernel-plan-m4.md:2689), and M3-P11 change B made
the head declaration readable alongside the merge-base one precisely so an
amendment can land with the phase that needs it (src/gates/scope.ts:922). The
copy I wrote is the `claude/prepass-wave-5` version with ONE entry added,
`witness/`, for the reason in the deviation section below. If that branch lands
first, this is an add/add conflict on one file whose resolution is to keep its
version plus that entry.

### 2. Criterion 4 was already delivered, by M4-P19

The plan orders M4-P19 after M4-P17 and says M4-P19 depends on it
(delivery/plan/kernel-plan-m4.md:2830). In the repository the order is the other
way round: M4-P19 merged as `e7342f0` and shipped `CHECK worktrees`, which is
this phase's criterion 4.

```
$ git log --oneline -3 -- src/commands/doctor.ts
e7342f0 M4-P19: reconstruct a pool record from the fleet rather than trusting what a clone happened to carry (#156)
1945d69 M3 exit test: the subject change, the evidence bundle, and DR-0034 (#148)
e019688 M3-P8: the tuition schema, the cross-project feed, and the generated mechanism index (#125)
```

`checkWorktrees` is at src/commands/doctor.ts:1358 and its two behaviours,
`doctor-check-worktrees-names-reconstructed` and
`doctor-check-worktrees-no-process-probe`, resolve by name in
test/behaviors.json. So criterion 4 is walked below as a VERIFICATION, not as
work this phase did. Rebuilding it would have been a second implementation of a
shipped check.

## WHAT WAS BUILT

### The mechanism behind criteria 1 and 2: a second copy of one comparison

The finding is "an expired lease prints PASS". The MECHANISM is that the kernel
had TWO implementations of "has this lease expired", one in the lock module and
one inside doctor, and the second could differ from the first without any test
noticing. Correcting only the verdict would have left that.

`isExpired` (src/lock.ts:173) needs a whole `Lease`. Doctor's lock check does
not have one: it reads the lease file defensively and holds only `holderId` and
`expiresAt`, because a lease file that fails `parseLease` must still produce a
diagnosis. That is why the copy existed. The comparison now lives once, at
src/lock.ts:170, behind `expiryHasPassed`, and `isExpired` delegates to it.

THE DERIVATION, published rather than summarised. Every site in `src/` and
`bin/` that could hold an expiry comparison:

```
$ grep -rn "Date.parse" --include=*.ts src bin
src/gates/release.ts:744:    const parsed = Date.parse(created);
src/gates/release.ts:752:  const mergedMs = Date.parse(subject.mergedAt);
src/liveness.ts:239:    Number.isNaN(Date.parse(candidate.writtenAt)) ||
src/liveness.ts:481:  const ageMs = nowMs - Date.parse(beacon.writtenAt);
src/lock.ts:128:    Number.isNaN(Date.parse(candidate.expiresAt))
src/lock.ts:160: * carry its own `Date.parse(...) <= Date.now()`, which is a SECOND comparison
src/lock.ts:170:  return Date.parse(expiresAt) <= nowMs;
src/lock.ts:179:  return Date.parse(lease.expiresAt) - (lease.durationSeconds * 1000) / 2;
src/lock.ts:550:    Date.parse(observed.lease.expiresAt) + 1,
src/watcher.ts:330:    Number.isNaN(Date.parse(candidate.lastHeartbeatAt)) ||
src/watcher.ts:353:  return Date.parse(state.lastHeartbeatAt) + intervalMsFor(state.backoffStreak, cadence);
src/watcher.ts:422:  const previousMs = previous === undefined ? undefined : Date.parse(previous.writtenAt);
src/watcher.ts:472:  const ms = Date.parse(deadline);
src/commands/doctor.ts:511:  if (Number.isNaN(Date.parse(expiresAt))) {
```

Read row by row: `release.ts` is about release timestamps and `liveness.ts` and
`watcher.ts` are about the beacon and the heartbeat, which are a different
subject with their own module. Of the rows over a LEASE expiry, `lock.ts:128` is
a parseability test rather than a comparison, `lock.ts:179` and `lock.ts:550`
are arithmetic on the parse (the renew-by half-life and the renew monotonicity
bump), `doctor.ts:511` is the same parseability test doctor already had, and
`lock.ts:170` is the comparison. One.

And the consumers of it:

```
$ grep -rn "isExpired" --include=*.ts src bin
src/lock.ts:156: * `isExpired` below needs a whole `Lease`, and doctor's lock check does
src/lock.ts:173:export function isExpired(lease: Lease, nowMs: number): boolean {
src/lock.ts:254:            ? isExpired(observed.lease, Date.now())
src/lock.ts:464:    if (!isExpired(observed.lease, nowMs)) {
src/lock.ts:538:  if (isExpired(observed.lease, nowMs)) {
src/lock.ts:639:    state: isExpired(observed.lease, nowMs) ? "expired" : "held",
```

Four call sites, all inside `src/lock.ts`, plus doctor through
`expiryHasPassed`. `checkHoldership` (src/task.ts:345) reads
`status.state`, which `leaseStatus` sets at src/lock.ts:639 from that same
`isExpired`, so the guard that refuses task mutation under an expired lease is
on the one comparison too.

WHAT THE DERIVATION DID NOT COVER, and the reviewer's first check is this
paragraph. The grep was over `src/` and `bin/` with a `*.ts` include, so it
looked at neither `test/` nor `scripts/`, and neither at a comparison written
some other way. A comparison spelled `new Date(x).getTime()` or `+new Date(x)`
would not appear in it. I ran the wider probe to close that:

```
$ grep -rnE "new Date\(|getTime\(\)" --include=*.ts src bin | grep -i expir
(no output, exit 1)
```

Exit 1 with no output is what "no other spelling of an expiry comparison in
`src/` or `bin/`" looks like here. `test/` is deliberately outside the scope: a
test that computes its own expected instant is doing its job. `scripts/` was
checked separately and holds no `.ts`.

### Criterion 1 and 2: the verdict

`lockCheckFor` (src/commands/doctor.ts:506) takes the clock as a parameter.
That is not decoration. I did not find a way to drive `Date.now()` in a child
process to a chosen millisecond from outside it, so I could not reach criterion
2's boundary member through the CLI: by the time the child runs, "exactly now"
has become "a moment ago" and the case under test is criterion 1's again. The
parameter is what made it reachable. The caller passes `Date.now()`; the witness
passes an instant. If a reviewer knows a way to drive the child's clock, the
boundary member could be an end-to-end test instead and this paragraph is the
open question.

The detail names the holder id and the expiry, because the remedy needs both.

### Criterion 3: CHECK tasks

Derived from `tasks/` alone. Two file existences per candidate. A task is OPEN
when `tasks/<id>/meta.json` is a regular file and `tasks/<id>/turn-end` is
absent. Implementation at src/commands/doctor.ts:1128.

THE PART THAT MATTERS MORE THAN THE COUNT is that ESTABLISHED, ABSENT and
UNUSABLE do not print the same word. The kernel carries a live instance of the
opposite shape: the retention check selects charter documents by a raw `kind`
read, so a document whose `kind` cannot be read is skipped, and skipping is
indistinguishable from absence, so the command reports PASS over a fleet it
could not examine. That is tracked at
delivery/verification/tracked-doctor-charter-selection.md:1 and is not this
phase's to fix. A fourth check with that shape would have been worse than no
check, so every candidate under `tasks/` lands in exactly one of three buckets
and the third is reported by id.

No path is opened. `classifyEntry` lstats, stats and answers a kind, so a named
pipe at `tasks/<id>/meta.json` is a named WARN in bounded time rather than a
doctor that hangs with no output, which is the defect CR-520 recorded at the
lease path.

C-1 and C-2 (CLAUDE.md:1261 and CLAUDE.md:1263) are asserted over the
function's own source text, because a violation of either is invisible in the
output: a check that shelled out to `ps` would print the same line. The test
also asserts the scan still SEES something, so a rename cannot leave it green
and empty.

### Criterion 5: CHECK branches

Implementation at src/commands/doctor.ts:1255. A branch is PUSHED when a
remote-tracking ref exists for it, which is the only local evidence that it is
somewhere other than this disk, and UNMERGED when `git merge-base
--is-ancestor <ref> <trunk>` exits 1.

IT REPORTS EVERY PUSHED REF RATHER THAN FILTERING TO A NAMING PATTERN, and that
is a decision. At least two spellings are in use across the repositories this
kernel runs over: the pool names task branches `task/<id>` (src/pool.ts:54) and
this project's own delivery names phase branches with a harness prefix and a
milestone-and-phase segment. A filter written for either is blind to the other.
A superset prints rows a reader can skip; a filter that misses a branch prints
nothing at all, and nothing is what a healthy fleet prints too.

THE FIRST FULL SUITE RUN FOUND A REAL DEFECT IN THAT COMMENT, and it is
recorded rather than quietly fixed. The first draft spelled the delivery
prefix out inside the header of `checkBranches`. test/schemas.test.ts:800 walks
`src/`, `bin/`, `schemas/` and `roles/` and asserts BY NAME which shipped files
carry that harness-derived prefix, existing to stop the set growing; the comment
added `src/commands/doctor.ts` to it:

```
+   'src/commands/doctor.ts',
    'src/gates/run.ts',
```

1161 tests, 1158 pass, 1 fail, 2 skipped, exit 1. The fix was to take the
spelling out rather than to widen the allowed set: the spelling belongs in a
delivery process, not in a kernel that reports over any of them. The corrected
comment says so, so the next person to reach for the literal gets the reason
without reading the test.

WARN, and no profile promotes it. The reason is the measured one the plan gives:
remote ref deletion is refused in this container and `git push --dry-run
--delete` exits 0 whether deletion is allowed or not (CLAUDE.md:1090), so a
promotable branch check would make `tiphys doctor --for full` unpassable on the
kernel's own fleet with no action its operator could take, and an unpassable
check is one that gets switched off. The test walks every profile in `PROFILES`
rather than naming `full`, so a profile added later cannot promote it by
accident, and that test is demonstrated red below under two different promotions.

### Criterion 6: CHECK remote fetches

Implementation at src/commands/doctor.ts:318. The old check listed the
configured remotes and printed PASS whenever the list was non-empty. That
condition is true of the dangerous state in two ordinary ways: a fleet whose
history has never been pushed, and a fleet whose remote URL points at nothing.
Both printed `CHECK remote PASS remote configured (origin)`. Nothing the check
did could have told either from a healthy fleet, because `git remote` reads a
config file.

THE FETCH IS BOUNDED, AND THAT IS A DEPARTURE FROM THE CLASSIFICATION M4-P19
WROTE. test/pool.test.ts:1564 executes a table that leaves object transfers
(`clone`, `fetch`, `pull`, `push`) unbounded, on the reasoning that a legitimate
transfer's duration is set by how much data there is, so a wall-clock bound
would kill real work. That reasoning is right where it was written and it does
not reach here: this fetch moves no work an operator asked for, it is a probe
inside a report. Killing it costs one diagnostic line; not killing it costs the
whole diagnosis, and M4-P19 measured `doctor` still running at 25 seconds
against a remote that accepts and says nothing. The bound reads the same
`TIPHYS_GIT_NETWORK_TIMEOUT_MS` override the pool's ref probe reads, so a test
can shorten it.

That table scans `src/pool.ts` and `src/teardown.ts` only, so this new call is
outside it and produces no red there. Stated as an omission rather than left to
be found: extending the scan to `src/commands/doctor.ts` would be an edit to
test/pool.test.ts, which is outside this phase's declaration.

A FETCH THAT DID NOT RUN IS FAIL. The plan asks for "a third status naming the
fetch failure and never PASS", and the status vocabulary here has exactly three
words, so the third one is FAIL. It is also what this command already does
everywhere else an input could not be established: an unreadable lease and an
undecodable charter are both FAIL today. The alternative I weighed and rejected
was a WARN with a non-promoted condition, on the grounds that an operator
offline would then see doctor exit 1; I rejected it because an unreachable push
target is exactly the SC-002 state this check exists to report, and because the
plan's words allow only three readings and two of them are PASS-adjacent.

The check also reports, without printing PASS, three states it cannot compare
in: outside a fleet home, on a detached HEAD, and where the remote carries no
ref for the current branch.

## ACCEPTANCE CRITERIA, WALKED

| criterion | verdict | evidence |
|---|---|---|
| 1, expired lease is FAIL with holder and expiry, exit 1 | met | test `doctor reports CHECK lock FAIL and exits 1 for a lease that expired one second ago`; asserts the STATUS TOKEN and the EXIT CODE, and compares a second arm byte for byte against a recorded CLI run |
| 2, boundary member, agrees with the lock module | met | test `the lock check and the lock module agree at the inclusive expiry boundary`; compares doctor's verdict against `isExpired` at five offsets including exactly 0 |
| 3, CHECK tasks, 1 open of 3, no log tail, no process | met | `CHECK tasks WARN 1 open of 3 (t-open)` verbatim, in witness/captures/m4-p17-doctor-cli.txt and reproduced live; the source grep test requires zero hits for the four tokens |
| 4, CHECK worktrees | VERIFIED, delivered by M4-P19 | src/commands/doctor.ts:1358; test/pool.test.ts:1197 |
| 5, CHECK branches, WARN, no profile promotes | met | test `doctor CHECK branches names a pushed branch that is not merged` and test `no profile promotes this phase's new conditions to FAIL` |
| 6, remote fetches, member A and member B | met | tests `doctor CHECK remote reports unpushed commits where it used to name the configured remote` and `a fetch that fails is its own verdict and never PASS`, plus a third member for the hang |
| 7, one line per check, exit 0 only when no FAIL, advisory last | met | test `a FAILing check still leaves the whole diagnosis printed and the advisory last`, and the pre-existing per-check-line test, whose expected set is derived from `runChecks` |

Criterion 7's second half, "the existing doctor behaviors still resolve by
name", is settled by the diff rather than by a new test: no existing row was
removed and no existing test was renamed.

```
$ git diff origin/main -- test/behaviors.json | grep -c '^-[^-]'
0
```

## RED WITNESSES

### A, the whole set against the base source

The strongest single reading. `src/commands/doctor.ts` and `src/lock.ts` were
replaced with their `origin/main` versions in a scratch copy, leaving the new
tests in place, and the eleven new tests were run:

```
$ git show origin/main:src/commands/doctor.ts > src/commands/doctor.ts
$ git show origin/main:src/lock.ts > src/lock.ts
$ node --test --test-name-pattern "<the eleven names>" test/doctor.test.ts
x doctor reports CHECK lock FAIL and exits 1 for a lease that expired one second ago
x the lock check and the lock module agree at the inclusive expiry boundary
x doctor CHECK tasks names the open task and counts it against the total
x a task whose state cannot be established is named, never silently skipped
x the tasks check reads meta.json and turn-end only, never a log tail or a process
x doctor CHECK branches names a pushed branch that is not merged
i no profile promotes this phase's new conditions to FAIL
x doctor CHECK remote reports unpushed commits where it used to name the configured remote
x a fetch that fails is its own verdict and never PASS
x doctor returns against a remote that accepts and never answers
x a FAILing check still leaves the whole diagnosis printed and the advisory last
i tests 11
i pass 1
i fail 10
i skipped 0
```

TRANSLITERATION DECLARED. Node's test reporter prints U+2714 for a passing
point, U+2716 for a failing one and U+2139 at the head of its summary lines.
This capture renders U+2716 as `x` (10 occurrences) and U+2139 as `i` (5
occurrences); U+2714 did not occur in the filtered output. Nothing else in any
captured output in this document was changed. The same declaration covers the
per-member table below, where the counts are re-stated as plain text.

THE ONE THAT IS GREEN AGAINST THE BASE IS THE INTERESTING ROW. `no profile
promotes this phase's new conditions to FAIL` passes against `origin/main`,
correctly and uselessly: at the base none of those conditions exists, so no
profile promotes any of them. That is the exact shape the red-witness rule's
stronger form names, a test that is green because the hazard cannot occur. It
therefore has its own dangerous-state witness in table B, reddened under two
different promotions.

### B, per-member mutation of the head source

Each row mutates the head source, runs the one named test, restores the head
source and runs it again. Every red run is a real dangerous state, not the
absence of the feature.

| member | test | dangerous state | restored |
|---|---|---|---|
| D1-m0 | no profile promotes this phase's new conditions to FAIL | `full` gains `branches-unmerged`: exit 1, tests 1, pass 0, fail 1 | exit 0, tests 1, pass 1, fail 0 |
| D1-m1 | no profile promotes this phase's new conditions to FAIL | `watch` gains `tasks-open`: exit 1, tests 1, pass 0, fail 1 | exit 0, tests 1, pass 1, fail 0 |
| D2-m0 | doctor CHECK tasks names the open task and counts it against the total | the turn-end guard is defanged so nothing is open: exit 1, tests 1, pass 0, fail 1 | exit 0, tests 1, pass 1, fail 0 |
| D2-m1 | doctor CHECK tasks names the open task and counts it against the total | the count is kept and the open ids are dropped: exit 1, tests 1, pass 0, fail 1 | exit 0, tests 1, pass 1, fail 0 |
| D3-m0 | the tasks check reads meta.json and turn-end only, never a log tail or a process | a comment carrying `pid` is inserted in the body: exit 1, tests 1, pass 0, fail 1 | exit 0, tests 1, pass 1, fail 0 |
| D3-m1 | the tasks check reads meta.json and turn-end only, never a log tail or a process | a comment carrying `/proc` is inserted in the body: exit 1, tests 1, pass 0, fail 1 | exit 0, tests 1, pass 1, fail 0 |
| D4-m0 | doctor CHECK branches names a pushed branch that is not merged | the ancestor exit code is misread so nothing is unmerged: exit 1, tests 1, pass 0, fail 1 | exit 0, tests 1, pass 1, fail 0 |
| D4-m1 | doctor CHECK branches names a pushed branch that is not merged | the count is kept and the branch names are dropped: exit 1, tests 1, pass 0, fail 1 | exit 0, tests 1, pass 1, fail 0 |
| D5-m0 | a FAILing check still leaves the whole diagnosis printed and the advisory last | a FAIL stops moving the exit code: exit 1, tests 1, pass 0, fail 1 | exit 0, tests 1, pass 1, fail 0 |
| D5-m1 | a FAILing check still leaves the whole diagnosis printed and the advisory last | the tasks check joins the failing set: exit 1, tests 1, pass 0, fail 1 | exit 0, tests 1, pass 1, fail 0 |

Each of the five tests has TWO structurally different members, which is what
"one witness is not a class" asks for: in D2 and D4 one member breaks the
classification and the other breaks the naming; in D1 the two members promote a
different condition in a different profile; in D3 one token is a word and the
other is a path; in D5 one member stops the exit code moving and the other adds
a second failing check.

### C, the durable witness specs, run by the gate

Six specs were added under `witness/`, each with two structurally different
members. The gate applied every member, ran every named test under it, and
required red under each and green at head:

```
$ node src/gates/red-witness.ts --result <dir>/red-witness.json \
    --evidence <dir> --base origin/main --head HEAD
red-witness: green (16 witness(es) evaluated (6 own, 10 stored re-evaluated in 139533ms); every witness red against every declared dangerous state and green at head)
[exited with code 0]
```

The six, with their two members each:

| spec | member 0 | member 1 |
|---|---|---|
| doctor-lock-expired-is-fail | the comparison in `src/lock.ts` is defanged | the comparison stands and doctor's arm that acts on it is removed |
| doctor-lock-expiry-boundary-agrees-with-the-module | the shared comparison moves off the inclusive boundary | doctor is given a SECOND comparison that disagrees by one millisecond |
| doctor-tasks-unestablished-is-not-a-pass | the unusable report is dropped | the unusable candidates are dropped from the total |
| doctor-remote-reports-unpushed | the divergence arm is never reached | git's two counts are read in the wrong order |
| doctor-remote-fetch-failure-is-not-a-pass | the fetch failure is swallowed and the stale ref compared | the failure is noticed and reported green |
| doctor-remote-returns-against-a-silent-remote | the bound is removed | the bound is kept but made one no test can shorten |

The ten stored witnesses re-evaluated are the pre-existing specs whose members
touch `src/commands/doctor.ts`; the gate re-runs them because this phase changed
that file, and all ten stayed red against their own members.

### D, the assertions are anchored to real captured output

Every check added or corrected here reports over another program's output, so
the expected strings are read out of recorded runs rather than written to match
the implementation.

`witness/captures/m4-p17-doctor-cli.txt` records real `tiphys doctor` runs with
their exit codes: the in-sync remote, the unpushed remote, the failed fetch, the
merged and unmerged branch lines, the three task lines, and the expired lease at
a FIXED past instant so the line is reproducible byte for byte.

`witness/captures/m4-p17-git-remote-comparison.txt` records the git commands the
checks parse. The rev-list counts are TAB separated, which is the byte a
hand-written expectation loses:

```
== git rev-list --left-right --count origin/main...HEAD, two commits ahead ==
0<TAB>2
exit: 0
```

(that one line is shown with the tab rendered `<TAB>` for this document only;
the capture holds the real byte, and the test asserts the live output contains
a tab before comparing.)

It also records that `git fetch` exits 0 against a remote that is there and 128
against one that is not, and that `git merge-base --is-ancestor` exits 0 for
merged and 1 for unmerged. The named tests reproduce each of those live before
believing the verdict built on them, so "the fetch failed" is a measured fact
about git rather than an assumption about what nonzero means.

## SUITE

Interpreter `/tmp/claude-0/n26/bin/node`, v26.6.0. Build state: `dist/` PRESENT
(`npm run build` run immediately before, `dist/bin/tiphys.js` on disk).
Invocation: `npm test`, which is `node --test "test/**/*.test.ts"`.

```
$ node --version
v26.6.0
$ command -v node
/tmp/claude-0/n26/bin/node
$ ls dist/bin/tiphys.js
dist/bin/tiphys.js
$ git rev-parse HEAD
bd0fda2df7bd296f3667e0d4c1c92c7e6f5c4f9f
$ git status --short
(no output)
$ npm test
i tests 1161
i suites 0
i pass 1159
i fail 0
i cancelled 0
i skipped 2
i todo 0
i duration_ms 324274.223328
npm-test-exit=0
```

Two SKIPPED, and a skipped test is not a passing test, so they are named. Both
are M4-P20's cross-environment witnesses, skipped by their own reason line
because `src/exclusion.ts` does not exist yet and M4-P21 is the phase that adds
it. The same two are the ones M4-P19's merge commit named at its head. Neither
is in this phase's changed set.

The first run of this suite, at the previous commit, reported 1161 tests, 1158
pass, 1 fail, 2 skipped and exit 1. That failure was real, was caused by this
branch, and is written up in the CHECK branches section above rather than
dropped.

The invocation is quoted because this repository has paid three times for an
unexplained suite-count difference and bare `node --test` from the repository
root reports two more than `npm test` does, from the tracked sandbox fixture
`sandbox/test/greet.test.js` that the package script's glob excludes
(CLAUDE.md:993).

## GATES

All gates at head `d3438f0`, run with the command the brief gives:

```
$ node bin/tiphys.ts gates run --registry gate-registry.yaml --mode direct-pr \
    --evidence <dir> --base origin/main --head HEAD --phase m4-p17
gates: run 619c0d9fc7fefb9b6365ec6b
gates: 2 registry gate(s) declared verified-by clean-room-checklist and NOT executed by this runner: unit-tests-for-changed-service-methods (probe unit-tests-for-changed-service-methods), fixtures-for-changed-component-states (probe fixtures-for-changed-component-states)
gates: registry gate-registry.yaml mode direct-pr
gates: declared 16 applicable 13 verdict 13 green 12 red 1 not-applicable 3 error 0 vacuous 0
```

The three lines the brief asks for verbatim:

```
gates: scope: red: branch claude/m4-p17-doctor-post-reclaim (phase m4-p17) matches the phase pattern but no phase declaration exists at delivery/plan/phase-declarations/m4-p17.json in the merge base d044a52bca7892376fff44631c4ffd0875ad1ebf of --base origin/main and --head HEAD; the declaration must be committed to main before the phase branch is created
gates: suite: green: suite green via tiphys-suite-events-v1 (child node v26.6.0): reported 1161 test(s) from 54 file(s) (pass 1159, fail 0, skipped 2, todo 0, did-not-run 0); discovered 54 file(s) walking test for .test.ts; 1050 behavior(s) resolve; merge base d044a52bca78
gates: red-witness: green: 16 witness(es) evaluated (6 own, 10 stored re-evaluated in 93216ms); every witness red against every declared dangerous state and green at head
```

The other ten verdicts, all green: `manifest-self-check` (8 schema documents),
`coverage` (115 inventory ids), `credential-scrub` (7 sources probed),
`clause-map` (74 rows), `agent-rules-drift`, `brief-drift` (20 rows each),
`check-agents-references` (21 references), `license` (10 production packages),
`typecheck` (414 files), `gate-classes` (all 3 required classes declared). Three
not applicable: `credential-token` and `check-dual-review` on unmet
preconditions, and `citations` because no changed path is under its configured
document globs, which is the same not-applicable M4-P19's merge recorded.

`scope` IS THE ONE RED AND IT IS THE FIRST FINDING OF THIS DOCUMENT, not a
defect in the branch: the declaration is not at the merge base because it is
sitting on `claude/prepass-wave-5`. To show that the branch is otherwise inside
its declaration, and that the `witness/` amendment lands the way M3-P11 intends,
I simulated the post-wave-5 world in a throwaway clone: `origin/main` was moved
to a commit carrying the wave-5 declarations exactly as that branch has them,
this branch was merged with it, and the gate was run again.

```
$ git update-ref refs/remotes/origin/main <origin/main + the wave-5 declarations>
$ node src/gates/scope.ts --declarations delivery/plan/phase-declarations \
    --result <dir>/scope.json --evidence <dir> --base origin/main --head HEAD --phase m4-p17
scope: green (14 changed paths audited)
14 changed path(s) audited against declaration delivery/plan/phase-declarations/m4-p17.json at merge base 7b0f487c3cd58b403ead2d8a9154e54487651a43 (sha256 f9a291056107403126407decfe0a00ff9ab24e187af5b17496aa87469a69a165) (1 declared path(s) not touched: src/pool.ts) DECLARATION AMENDED AT HEAD: 1 entry/entries ADDED at head a640e5745c73e826e1c616cbcdfafe1d2f436b00 that are absent from the merge-base declaration, allowed and NAMED here for a reviewer to sign off (this gate does not sign them off), 1 of them a DIRECTORY PREFIX rather than a single file: filesToTouch witness/ (DIRECTORY PREFIX: grants every current and future path under it, not one file).
```

The gate bundle above and this simulation are both at `d3438f0`; the simulated
merge commit `a640e574` is that head merged with the simulated trunk.

RE-RUN AT `e6d2662`, the commit that corrected this section to name the head it
was measured on, with the same registry, mode and flags:

```
gates: declared 16 applicable 13 verdict 13 green 12 red 1 not-applicable 3 error 0 vacuous 0
gates: suite: green: suite green via tiphys-suite-events-v1 (child node v26.6.0): reported 1161 test(s) from 54 file(s) (pass 1159, fail 0, skipped 2, todo 0, did-not-run 0); discovered 54 file(s) walking test for .test.ts; 1050 behavior(s) resolve; merge base d044a52bca78
gates: scope: red: branch claude/m4-p17-doctor-post-reclaim (phase m4-p17) matches the phase pattern but no phase declaration exists at delivery/plan/phase-declarations/m4-p17.json in the merge base d044a52bca7892376fff44631c4ffd0875ad1ebf of --base origin/main and --head HEAD; the declaration must be committed to main before the phase branch is created
gates: red-witness: green: 16 witness(es) evaluated (6 own, 10 stored re-evaluated in 87817ms); every witness red against every declared dangerous state and green at head
gates: 1 gate(s) reported red: scope
```

Identical to the run above, and the only delta between those two heads is this
document. THE RESIDUAL, stated rather than left to be noticed: the commit that
adds THIS paragraph is itself one more commit, and a bundle run on it is
reported to the orchestrator rather than pasted back in here, because pasting it
in would need another commit and so on. The delta it carries is again this file
alone, which no gate in the bundle reads: `citations` does not have
`delivery/work-history/**` among its document globs (src/gates/citations.ts:233),
and `scope` already audits this path as a declared extra.

That is a SIMULATION and it is labelled one. It says the branch's 14 paths are
inside the declaration and that the one amendment is `witness/`, printed for a
reviewer. It does not say the real gate will be green, because the real merge
base does not exist yet.

THE DEVIATION, DECLARED. `witness/` is not in the plan's files-to-touch for this
phase (delivery/plan/kernel-plan-m4.md:2687) and is not in the
`claude/prepass-wave-5` declaration. It is there because the red-witness gate
requires every changed file under `src/` to be covered by a witness spec, and
`src/lock.ts` was covered by none:

```
$ node -e 'const fs=require("fs");const hits={};for(const f of fs.readdirSync("witness").filter(f=>f.endsWith(".json"))){for(const m of (JSON.parse(fs.readFileSync("witness/"+f,"utf8")).dangerousStates||[])){const k=m.file||"(patch)";(hits[k]=hits[k]||[]).push(f);}}console.log("src/lock.ts covered?", !!hits["src/lock.ts"]);'
src/lock.ts covered? false
```

src/gates/red-witness.ts:364 is the arm that reddens for it. So the choice was
between adding a witness spec (and with it the `witness/` grant) and leaving a
required gate red. A DIRECTORY PREFIX grants every path under it, which is why
the gate prints that in capitals; the eight files actually added under it are the
six specs and the two captures, all named in the diff.

## WHAT THIS PHASE DID NOT COVER

The reviewer's first check.

1. **The scope gate cannot go green on this branch yet.** The declaration is not
   at the merge base. That is the first finding above and it is an orchestrator
   action, not something this phase can close.

2. **`src/pool.ts` is in the plan's files-to-touch and was not touched.** It was
   listed for criterion 4, which M4-P19 had already delivered, and for the
   network-timeout helper, which is already exported (`resolveNetworkTimeoutMs`)
   and needed no change to import. Touching it would have been change for the
   sake of the list.

3. **The network-call classification in test/pool.test.ts:1564 does not scan
   `src/commands/doctor.ts`.** This phase adds a socket-opening git call to that
   file, and the table that exists to force every such call to be classified
   does not look at it. Extending the scan is an edit to `test/pool.test.ts`,
   which is outside this declaration. The consequence is concrete: a later phase
   adding an unbounded `fetch` to doctor gets no red from that table.

4. **Three of the eleven new behaviours have no durable witness spec.** They are
   `doctor-tasks-open-count`, `doctor-tasks-no-log-tail-or-process-probe`,
   `doctor-new-conditions-are-never-promoted`, plus
   `doctor-branches-unmerged-named` and
   `doctor-diagnosis-survives-a-failing-check`, which is five rather than three;
   the count is corrected here rather than left wrong. Each is demonstrated red
   and green in table B above under two members, so the property is measured,
   but it is measured ONCE rather than re-measured by the gate on every future
   change to the file. The reason is red-witness rule (f): any member touching
   `src/commands/doctor.ts` takes a capture obligation, and for the source-text
   and profile-table assertions there is no program output to cite that would
   not be decoration. This is a real reduction in durable coverage and is stated
   rather than hidden.

5. **The tasks check does not read `meta.json`.** It tests that the file is a
   regular file and does not parse it, so a task whose record is corrupt JSON
   counts as a task. That is deliberate (the check's subject is open versus
   closed, and the record's contents are another command's concern) and it means
   `CHECK tasks` says nothing about whether a task record is valid.

6. **`CHECK branches` compares against one trunk.** It takes the remote's
   advertised default, then the current branch's upstream, then HEAD, and
   reports every other pushed ref against that one. A fleet with two long-lived
   integration branches gets a report against one of them.

7. **The remote check fetches ONE remote.** Where several are configured it
   picks the current branch's configured remote, then `origin`, then the first
   listed. A fleet with two push targets has only one of them probed.

8. **No macOS witness.** Everything here was measured on Linux, node v26.6.0,
   git 2.43.0. The `git for-each-ref`, `merge-base --is-ancestor` and
   `rev-list --left-right --count` contracts are portable as far as I know, and
   I did not run them on another platform, so that is an untested reading rather
   than a measured one.

9. **The silent-remote witness uses the `git://` protocol.** M4-P19 chose it
   because that is the shape that hangs on a listener which accepts and says
   nothing. An `https://` remote behind a proxy that stalls may fail by a
   different route; I did not build that fixture.

10. **I did not run the M1 exit test.** Its step A2 provisions a fleet with a
    `file://` remote, pushes it with `-u`, and asserts `tiphys doctor` prints no
    FAIL line (scripts/m1-exit-test.sh:542). The new remote check fetches that
    remote and the new branches and tasks checks report over that fleet, so the
    step's inputs changed. I reproduced the same fixture by hand and doctor
    exits 0 over it with the remote line reading `in sync`, which is recorded in
    witness/captures/m4-p17-doctor-cli.txt, but the script itself was not
    executed here.

## CLAIM GREPS

The binding line-based grep, run on this document at the commit that carries it:

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m4-p17.md
15
$ grep -oEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' body.md | wc -l
16
```

And the wrap-insensitive form, because this prose is hard-wrapped by hand and a
phrase straddling a wrap is invisible to the first:

```
$ tr '\n' ' ' < delivery/work-history/m4-p17.md | grep -oEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' | sort | uniq -c
      2 cannot be
      2 needs a
     12 never
$ tr '\n' ' ' < body.md | grep -oEi '...same alternation...' | wc -l
16
```

Both greps are run over the document MINUS this section, written to `body.md`
by `sed '/^## CLAIM GREPS/,$d'`, and that is stated rather than hidden: the
command lines pasted into this section contain every phrase in the alternation,
so a whole-file run counts the transcript as well as the prose. The whole-file
numbers are given at the end of this section.

The two agree: 16 occurrences on 15 lines line-visibly, 16 occurrences
wrap-insensitively. Zero missed by a wrap, which is the same result the three
work histories in CLAUDE.md's table got for two of the four documents measured
there.

How each hit is settled, by class rather than one row per occurrence, since the
hits fall into five kinds:

1. **A quoted test name.** Most of the `never` hits are the titles of tests
   (`... never silently skipped`, `... never a log tail or a process`, `... never
   PASS`, `... never answers`). Each appears in table A or table B above with its
   measured red and its measured green, so the adjacent evidence is the table the
   name sits in.
2. **A quotation of the plan.** "a third status naming the fetch failure and
   never PASS" is the plan's sentence, cited to
   delivery/plan/kernel-plan-m4.md:2731, and the reading taken from it is argued
   rather than asserted.
3. **A description of a mutation.** "the divergence arm is never reached"
   describes what a witness member does, and the gate output showing that member
   red is three lines above it.
4. **A statement about the tracked retention defect.** "a document whose `kind`
   cannot be read is skipped" is not my finding; it is cited to
   delivery/verification/tracked-doctor-charter-selection.md:1.
5. **A grep artefact.** `whenever` contains `never`. The line about the old
   remote check ("printed PASS whenever the list was non-empty") is a hit for
   that reason alone.

The one claim that was an OVER-CLAIM in the first draft is now an open question:
whether a child process's `Date.now()` can be driven to a chosen millisecond from
outside it. I did not find a way; if a reviewer knows one, criterion 2's boundary
member can be an end-to-end test rather than a unit one. The other rewritten
sentence replaced "a gap cannot be seen" with what is actually observable, which
is that a filter missing a branch prints nothing and a healthy fleet prints
nothing too.

Whole-file, after this section was pasted in, the line-based grep reports 30
lines and the wrap-insensitive one reports 64 occurrences. The 48 beyond the 16
measured above all sit inside this section, in its two command lines (which
spell out the alternation itself) and in the five explanations, which quote the
phrases they explain. That is the transcript counting itself, which is why the
measurement that matters is the one over the body.
