# Work history: M1-P3 (session lock and worktree pool)

## Prompt context

Implementer for phase M1-P3 of kernel plan v1 (delivery/plan/kernel-plan-v1.md, revision 7), branch claude/m1-p3-lock-and-pool, worktree based on origin/main at 2431813 (merged M1-P1 and M1-P2). Mandated reading completed in order before any code: plan header and binding rule, full section 3 preamble (shared phase fields, invocation form, constraints C-1/C-2/C-3, test accounting rule), the full M1-P3 phase section (all steps, files-to-touch, all 17 acceptance criteria, citations, including the EXT-F-01 lease mutation contract and the EXT-F-03 base resolution verbatim), DR-0007 (dual substrate, lease exclusion domain one filesystem and one clock), DR-0005 and DR-0002, the firstmate scout report sections the P3 citations name (FM-017 to FM-022 lock protocol, FM-023 to FM-026 pool provenance, FM-036/FM-051 stale-lock proof, traps FM-053 and FM-058), the merged P1/P2 code (bin/tiphys.ts, src/cli.ts, src/fleet.ts, src/commands/init.ts and doctor.ts, test/behaviors.json), and delivery/work-history/m1-p1.md and m1-p2.md. Scope: exactly the M1-P3 files-to-touch list plus test/behaviors.json (the standing files-to-touch clerical gap, mandated by the section 3 test accounting rule and criterion 17) plus this file.

## Verify-before-editing

Confirmed absent at branch point 2431813 before writing: src/lock.ts, src/commands/lock.ts, src/pool.ts, src/commands/pool.ts, test/lock.test.ts, test/pool.test.ts, delivery/work-history/m1-p3.md. Confirmed present: src/cli.ts with the P1/P2 dispatch-table shape, src/fleet.ts exporting LOCK_FILE = state/orchestrator.lock and loadFleet (consumed, not re-invented).

## Files touched

Created: src/lock.ts, src/commands/lock.ts, src/pool.ts, src/commands/pool.ts, test/lock.test.ts, test/pool.test.ts. Edited: src/cli.ts (registered lock and pool in the one dispatch table), test/behaviors.json (pure append of this phase's mappings). Plus this file.

## Per-step commits

- 421a003 M1-P3 step 1: lease-based session lock with one shared atomic mutation primitive
- 06eb31a M1-P3 step 2: lock CLI subcommands acquire, renew, release, status
- 39e306e M1-P3 step 3: worktree pool with EXT-F-03 base resolution and fail-safe destroy
- 257456a M1-P3 step 4: pool CLI subcommands and dispatch-table registration
- 2d0865b M1-P3 step 5: lock and pool tests with deterministic race witnesses, behavior registry entries
- (this file's commit follows)

## Key decisions and the why that is invisible in the diff

1. The one shared mutation primitive (applyLeaseMutation in src/lock.ts) implements EXT-F-01 as: serialize through a claim file beside the lock (<lock>.mutex, O_EXCL create; FM-022's serialize-through-a-claim pattern), then inside the claim re-read the lock file, byte-compare against the observed state the mutation was decided on, apply (O_EXCL create for the absent-lock acquire per PR-006; write-temp-then-rename otherwise; unlink for release), and confirm by re-reading the mutation's unique token (FM-022 write-then-verify). Any mismatch at compare or confirm is a loss without retry, exactly the plan's wording. No steal protocol exists on purpose (FM-058): a claim file left by a crashed mutation makes later mutations fail loudly after a bounded 5s wait, naming the file for manual removal; the critical section is a few file operations, so that window is tiny and honesty beats cleverness.
2. The lease JSON carries two fields beyond the plan's named four (holderId, hostname, acquiredAt, expiresAt): token, which is the plan's own per-mutation confirmation token made visible (criterion 6 requires asserting on it), and durationSeconds, so renew defaults to the acquired duration and half-life (renewByMs, PR-203) is computable. Declared as deviation 2. No pid-like field anywhere (C-2); doctor's P2 lock check requires only holderId and expiresAt and is unaffected.
3. Holder identity transport across CLI invocations (FM-022: an identity that outlives one tool call): acquire prints "acquired <holderId> expires <ts>" and the holder passes --holder <id> to renew and release. M1-P4's spawn/teardown holdership checks will consume the same flag or the lease file.
4. Renew computes the new expiry as max(now + duration, old expiry + 1ms) so criterion 4's strict increase holds even for a sub-millisecond renew; a renew that does not extend the lease would be useless.
5. Release semantics derived from criterion 8: a matching holder may release even after expiry (cleanup of one's own expired lease is legitimate and is exactly the release-versus-takeover race the criterion stages); a non-matching holder is always refused (criterion 9), so a losing holder can never remove the winner's lease.
6. Takeover of an absent lock behaves as a plain O_EXCL acquire; a takeover whose observed lease disappeared before apply loses with "the observed lease is gone" (criterion 8's release-first outcome). Takeover of an unexpired lease is refused at decision, before any mutation (plan step 1).
7. Deterministic race witnesses: TIPHYS_LOCK_TEST_HOLD in src/commands/lock.ts is a documented hold point (the environment guidance for this phase explicitly prefers an injected hold point or the primitive's own staging over sleep-timing). When set, a mutating lock subcommand observes the lease and freezes its decision clock, writes <barrier>.observed, and waits (bounded 30s) for the barrier file before deciding and applying. This models the paused holder of DR-0007 (a reclaimed session resuming mid-operation) and lets tests interleave two real CLI invocations at the exact compare-and-swap point. Inert unless the variable is set. The lock library additionally exposes the primitive's own staging seam (an observed option on acquire/renew/release), used for the applied-order-renew-first half of criterion 6.
8. A corrupt (unparseable) lease file: status reports "corrupt" and still exits 0 (status never blocks a human); every mutation refuses with a reason naming the file. Doctor already FAILs on it (P2).
9. Pool records live beside the worktree at worktrees/<task-id>.pool.json (plain JSON per D-3), never inside it, so the record can never dirty the destroy-time cleanliness check and no exemption list is ever needed (FM-059). The record carries taskId, project, remote, branch, baseSha, branchName, offline, createdAt; M1-P4 spawn reads baseSha and offline from it for meta.json (baseSha, baseOffline).
10. Task branch name is task/<task-id>, created by git worktree add -b at the exact fetched base SHA (EXT-F-03 step 4). Task ids are validated as safe path segments; anything else is refused.
11. The fetch uses the explicit force refspec +refs/heads/<branch>:refs/remotes/<remote>/<branch> so the remote-tracking ref mirrors the remote exactly even after a remote rewind; the base SHA is then rev-parse of that tracking ref, never a local branch (criterion 11's behind and ahead cases).
12. Remote resolution: origin if configured, else the single configured remote, else refuse. Default branch: the local symbolic ref refs/remotes/<remote>/HEAD when set (no network), else the remote's advertised default via ls-remote --symref (criterion 12). With --offline and origin/HEAD unset and the remote unreachable, create fails honestly (the default branch cannot be known); observed during the criteria walk.
13. --offline authorizes the fallback, it does not skip the fetch: the fetch is always attempted; on success the fresh SHA is used and offline: false is recorded even when the flag was passed; only on fetch failure with the flag does create use the last fetched remote-tracking SHA and record offline: true. Without the flag, fetch failure creates nothing (criterion 13, both directions).
14. Parallel-safety for criterion 15: the record file is created with O_EXCL as the atomic duplicate gate, and fetch and worktree add retry up to 5 times with linear backoff when stderr matches a transient git lock-contention signature (index.lock, cannot lock ref, Unable to create .lock). Concurrent fetches of the same tracking ref can transiently contend; the retry absorbs it. Failures that do not match the signature (for example an unreachable remote) fail on the first attempt.
15. Destroy's index.lock rule (FM-036, FM-051): after retries exhaust on a lock signature, provablyStaleLock removes the lock only when it exists, its mtime age exceeds 300s, and lsof is available and provably shows no holder (exit 1, empty stdout); lsof missing, erroring, or listing anything means not stale, leave it, exit nonzero. The predicate takes an injectable lsof runner so the refusal ladder is unit-tested deterministically (fresh lock, lsof unavailable, lsof holder, lsof error, and the one provable case).
16. --duration accepts fractional seconds (parsed as a positive finite number); the default is 900 (DEFAULT_LEASE_DURATION_SECONDS). Tests use 1s leases and wait past the recorded expiresAt, so expiry is real, never mocked.

## Deviations from the plan's letter

1. test/behaviors.json edited although not in the M1-P3 files-to-touch list: the standing clerical gap; the section 3 test accounting rule and criterion 17 mandate the append. Pure append, nothing modified or removed.
2. Lease fields durationSeconds and token beyond the plan's named four (key decision 2): token is required by criterion 6's assertion surface; durationSeconds makes renewal-at-half-life computable.
3. TIPHYS_LOCK_TEST_HOLD hold point in src/commands/lock.ts (key decision 7): test-determinism seam, inert in normal operation, sanctioned by the phase's determinism rule (inject a hold point or use the primitive's own staging).
4. Registered tests beyond the criteria-named behaviors: lock-takeover-unexpired-refused, lock-dead-holder-still-excludes (both mandated by step text rather than a numbered criterion), lock-release-by-holder, lock-status-held-format, lock-usage-errors, pool-list-lines (step 3's pool list), pool-index-lock-failsafe (step 3's fail-safe rule), pool-usage-errors. All appended to test/behaviors.json.
5. Criterion 13's final clause ("a spawn over that offline-created worktree writes meta.json with baseOffline true") is not witnessable in this phase: spawn is M1-P4 (the criterion's own text cross-references it as "M1-P4"). This phase delivers and witnesses the pool half (offline: true in the pool record); the meta.json half is the named P4 obligation (P4 criteria cover baseSha/baseOffline). Declared, not improvised.

No other deviations: no tsconfig changes, no dependency changes, CI workflow untouched, no delivery/ writes beyond this file, src/fleet.ts consumed unmodified.

## Gate evidence (local, Node v22.22.2, npm 10.9.7; CI on Node 26 is the authority)

1. npm ci from clean state (node_modules and dist removed first): exit 0 (EBADENGINE warnings expected below the floor, ignored).
2. npm run build: exit 0; dist/bin/tiphys.js, dist/src/lock.js, dist/src/pool.js, dist/src/commands/lock.js, dist/src/commands/pool.js emitted; git status --porcelain empty afterward.
3. npm test with dist/ removed first (no prior build): exit 0; tests 56, pass 54, fail 0, cancelled 0, skipped 2 (the unchanged P2 floor-gated doctor exit-0 witnesses with their recorded reason), zero unaccounted.
4. Behavior registry check by name (script over the same run's TAP output): all 57 mappings resolve to test titles present in the run, including every P1 and P2 mapping; nothing previously registered removed; exit 0. (Two behaviors, pool-destroy-dirty-refused and pool-destroy-discard-removes, deliberately map to the one test that witnesses both halves of criterion 16's dirty/discard pair.)
5. Non-ASCII scan of all touched files: clean. Lock and pool CLI exit codes are floor-independent (neither command checks the node version), so no new floor-gated skips were needed.

## Acceptance criteria walk (command evidence, exit codes captured)

1. lock acquire in a fresh fleet: exit 0; state/orchestrator.lock parses; holderId a fresh UUID; expiresAt in the future; grep -ci pid over the file matches 0 lines. PASS (walk + test lock-acquire-creates-lease).
2. Second acquire while unexpired: exit 1, stderr "lock held by <holder>", cmp of the file before/after identical. PASS (walk + test).
3. Five concurrent acquires (shell background jobs, and the registered spawned-process test): exactly 1 exit 0, 4 exit 1, every loser's stderr contains "lock held", file holds the winner's holderId (equal to the winner's stdout). PASS; 0 failures in 20 repeated test runs.
4. renew by holder on unexpired lease: exit 0, expiresAt strictly increased (walk: ...33.224Z to ...33.621Z); renew on an expired lease with the matching holder: exit 1, stderr "expired", file byte-identical (the paused-holder EXT-F-01 witness); renew with non-matching holder: exit 1, file byte-identical. PASS (walk + tests).
5. With a real expired lease (1s duration, waited out): lock status exit 0, stdout "expired holder <id> ... expires <ts>"; acquire without --take-over exit 1 reporting expired; acquire --take-over exit 0 with a new holderId and future expiry. PASS (walk + tests).
6. Renew versus takeover at the expiry boundary, scripted interleave: renew (real CLI process) observes and freezes its clock while unexpired, holds at the barrier; takeover wins after expiry (exit 0); resumed renew exits 1 with "lost" and the file is byte-identical to the winner's lease including its token. Applied-order-renew-first also witnessed through the primitive's staging seam (staged takeover loses, renewed lease untouched). PASS (test lock-renew-vs-takeover-serializes); 0 failures in 20 runs; deliberate-failure witness below shows the test detects a broken compare-and-swap.
7. Two concurrent takeovers on an expired lease (real processes): exactly one exit 0, one nonzero with a diagnostic, file holds the winner's holderId. PASS (test); 0 failures in 20 runs.
8. Release versus takeover, both auditable outcomes, scripted interleaves: takeover-first (held release loses with "lost", winner lease byte-identical) and release-first (held takeover loses with "the observed lease is gone", no lease left behind). PASS (two tests); 0 failures in 20 runs each.
9. After a completed takeover, renew and release with the losing holderId: both exit 1, winner lease byte-identical (cmp). PASS (walk + test).
10. grep -Ei 'process\.kill|/proc|pid|signal-0|kill' over src/lock.ts: no matches, exit 1. PASS (walk + registered structural test, which also enforces it on every future run).
11. pool create with the clone's local main behind the remote, and separately ahead of it: exit 0 both; emitted base SHA equals the remote default branch head, never the local branch; worktree HEAD equals that SHA; worktree porcelain empty. PASS (walk + tests).
12. Clone at detached HEAD: create exit 0 with the correct base. origin/HEAD unset (remote set-head --delete): create exit 0, default branch resolved from the remote's advertised symref, correct base. PASS (walk + tests).
13. Remote unreachable: create exit 1 and nothing created (no worktree, no record, worktree list unchanged). Same invocation with --offline: exit 0, base is the last fetched remote-tracking SHA (upstream had advanced unseen, proving staleness is recorded, not hidden), record carries that SHA and offline: true. PASS for the pool half (walk + tests); the spawn/meta.json clause is the plan's named M1-P4 obligation (deviation 5).
14. Duplicate task id: exit 1, stderr "task id already used: t-off2". PASS (walk + test).
15. Two concurrent creates for distinct ids: both exit 0, git worktree list shows both. PASS (walk + test); 0 failures in 20 runs.
16. Destroy of a dirty worktree: exit 1 with a reason line, directory intact; --discard: exit 0, directory gone, registration pruned; clean worktree destroyed without flags: exit 0. PASS (walk + tests).
17. npm test exit 0, 56 tests, 0 failing, zero unaccounted (2 skips with recorded reasons); registry check resolves all 57 mappings by name, nothing removed. PASS.

No CI-DEFERRED entries: every criterion in this phase is locally witnessable and was witnessed (the two standing P2 skips belong to P2's doctor criteria, re-witnessed on CI as before).

## Deliberate-failure witnesses (captured, reverted)

1. Broken compare-and-swap: replaced the primitive's byte-compare branch with an always-false condition; the criterion 6 test failed (not ok, exit 1: both operations won). Reverted via git checkout; porcelain clean; re-run passed. This witnesses that the race tests detect a lost-update primitive, not merely exercise a working one.
2. Registry name check: renamed the registered test "lock release by the holder removes the lease and frees the lock"; the registry check exited 1 reporting MISSING lock-release-by-holder. Reverted; porcelain clean; full check exits 0 with all 57 mappings.
3. Criteria 2, 4, 5, 8, 9, 13, 14, 16 are themselves negative witnesses (refusals and losses with required exit codes and diagnostics) and were each executed as failing commands with captured nonzero exits, listed in the walk above.

## Race-test determinism notes

Mechanisms, per the phase's determinism rule (no sleep-based interleaving anywhere):

- Scripted interleaves: the TIPHYS_LOCK_TEST_HOLD barrier (real CLI processes held at the observe/apply boundary with a ready marker, so the test controls the exact application order) for criterion 6's takeover-first order and both criterion 8 outcomes; the primitive's staging seam (observed and nowMs options) for criterion 6's renew-first order. Expiry is always a real 1-5s lease waited past its recorded expiresAt (bounded polling, no fixed sleeps).
- True parallelism with interleave-independent postconditions: criteria 3, 7, 15 spawn concurrent node processes and assert the single-winner (or both-succeed) postcondition plus loser diagnostics.

Local repeat counts, all with corrected --test-name-pattern invocation: five-concurrent-acquires 20/20 pass, two-concurrent-takeovers 20/20, two-concurrent-pool-creates 20/20, renew-vs-takeover staged 20/20, release-vs-takeover (takeover-first) 20/20, release-vs-takeover (release-first) 20/20. 120 targeted runs, 0 flakes.

## Environment warnings (inherited plus new, for future phases)

1. Inherited and confirmed: local Node v22.22.2 against the ">=26" floor; EBADENGINE warnings ignored; type stripping runs all local gates; CI Node 26 is the authority.
2. New: node --test flag ordering matters on this Node line: --test-name-pattern must precede the test-file positional; placed after it, the flag is silently ignored and every test in the file runs (cost me a timed-out 20x loop before diagnosis).
3. New: lock and pool CLI exit codes are floor-independent (no node-version check in either command), so P3 tests need no floor gating; the P2 warning about doctor exit-0 assertions still stands for P4/P5/P6.
4. Confirmed (P2 warning 4): pool tests create commits in scratch repos and needed command-scoped GIT_AUTHOR_*/GIT_COMMITTER_* on every commit-producing git call; the git() helper in test/pool.test.ts is the pattern to reuse.
5. New: concurrent git fetches updating the same remote-tracking ref can transiently contend on ref locks; pool create retries on the lock-contention stderr signature (5 attempts, linear backoff). Anything running parallel fetches in P4+ should reuse runGitRetrying in src/pool.ts rather than re-deriving the signature.
6. New: the suite now takes about 28s locally, dominated by real-clock lease expiries and scratch-repo git work; node --test runs files in parallel so the wall time holds, but budget for it in harness timeouts.
7. New: git remote set-url with a relative path is resolved relative to the repository, not the caller's cwd; test staging should use absolute paths (a mis-counted relative URL produced a confusing walk failure that looked like a code bug).
