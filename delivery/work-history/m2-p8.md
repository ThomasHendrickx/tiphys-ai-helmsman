# Work history: M2-P8 credential scoping

- Branch: claude/m2-p8-credential-scoping, off origin/main at 4c9bfbc
  (M2-P1 merged at 8718852).
- Implementer session started 2026-08-06. This file is written incrementally
  (T-008); each section is appended when the step it records completes.

## Step 1: M2-C-1 verification of the delivered seams (recorded BEFORE any code)

### src/spawn.ts, both spawnSync call sites

Verified at 4c9bfbc:

1. PAYLOAD call site: src/spawn.ts lines 172-175. `spawnSync(program, args,
   { cwd: request.worktree, stdio: "inherit" })`. NO `env` option is passed,
   so the payload child inherits the parent's entire environment, exactly as
   the plan's grounding states.
2. TURN-END HOOK call site: src/spawn.ts lines 187-190.
   `spawnSync(process.execPath, [request.hookPath, String(exitCode)],
   { stdio: "inherit" })`. NO `env` option and NO `cwd` option; the hook
   child also inherits everything.

Both call sites are inside `subprocessAdapter.launch` (src/spawn.ts lines
144-215). The adapter receives an `ExecutorRequest` (lines 61-73) carrying
taskId, worktree, command, hookPath, recordPath, deadlineSeconds; it carries
no environment field today.

### src/hooks.ts

Verified at 4c9bfbc: the generated turn-end hook script (renderTurnEndHook,
lines 38-58) bakes the turn-end path in as a literal and reads ONLY
process.argv[2]. The module comment at line 36 says "the hook needs no fleet
resolution and no environment at all", and the script body confirms it: no
process.env access, no relative path, no HOME use. Conclusion: the hook
SCRIPT needs no redirection of its own; what M2R-004 edit 4 requires is that
the CHILD PROCESS that runs it receives the same scrubbed environment, which
is the second spawnSync call site above. src/hooks.ts therefore needs no
edit; the file stays untouched and this record is the step 1 evidence for
that decision.

### scripts/stub-payload.sh: does the payload open the PR itself?

YES. Verified at 4c9bfbc, lines 114-120: in full mode the payload runs
`gh pr create --head <branch> --title ... --body ...` and reports the URL as
`payload pr <url>`. Step 5's conditional therefore FIRES: pull-request
creation must move out of the payload and into the harness so the exercised
shape matches R-008 (implementers never create PRs) instead of contradicting
it.

How each child environment is constructed today: the harness
(scripts/m1-exit-test.sh) exports TIPHYS_EXIT_TEST_MODE,
TIPHYS_EXIT_TEST_TASK, TIPHYS_EXIT_TEST_REPORT (lines 671-678) in its own
process, invokes `node dist/bin/tiphys.js spawn ... --exec
scripts/stub-payload.sh`, and the payload receives them purely by
inheritance through the two env-less spawnSync calls above. The payload's
documented environment contract is exactly those three variables
(stub-payload.sh lines 19-24). Git identity is command-scoped inside the
payload (git_identified, lines 73-79), NOT taken from the environment.

### scripts/m1-exit-test.sh, full-mode PR consumption

Verified at 4c9bfbc: the harness reads the PR URL back out of the payload
report (`payload pr` line, m1-exit-test.sh line 706) and then drives
`gh pr view` itself (lines 710-713, 963-967). So the only PR-creating
command in the delivered exit-test machinery lives in the payload, and the
harness already owns every other gh interaction. Moving `gh pr create` into
the harness immediately after A6's spawn keeps the evidence shape intact
(the harness can emit the same PR fact from its own record).

### Delivered gate contract (M2-P1, consumed rather than assumed)

Verified at 4c9bfbc: gates are subprocesses invoked as
`<command...> --result <abs path> --evidence <abs dir>` plus declared
parameters (src/gates/run.ts lines 40-48, 703-710); exit codes 0/1/20/21/64
map to green/red/not-applicable/error/usage (src/gates/result.ts lines
65-97); the only record constructor is makeGateResult (result.ts line 158)
and a green record with 0 units is rewritten to error (M2-C-2). The
manifest schema has NO `env` precondition kind (gate-manifest.schema.json
lines 90-96), confirming STATE.md's carried-forward note: credential-token's
presence check wires through `command-exit-zero`, not an environment probe
kind.
