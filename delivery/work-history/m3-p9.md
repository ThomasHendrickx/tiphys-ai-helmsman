# M3-P9 work history: orchestrator policy (AGENTS.md)

- phase: M3-P9
- branch: `claude/m3-p9-agents-policy`
- branch point: `e019688` (main tip after M3-P8 merged)
- declaration: delivery/plan/phase-declarations/m3-p9.json:1
- plan section: delivery/plan/kernel-plan-m3.md:4395
- dispatch read (design constraint honoured here):
  delivery/plan/m3-p9-dispatch-read.md:43

This file is written INCREMENTALLY and committed as work proceeds (T-008
dispatch contract, `CLAUDE.md:440`). Its mtime is this agent's beacon. Entries
are appended in the order the work happened, not reordered afterwards.

## Status

- [x] worktree created, beacon committed
- [ ] orientation reads
- [ ] AGENTS.md
- [ ] scripts/check-agents-references.mjs
- [ ] scripts/check-dual-review.mjs
- [ ] tests
- [ ] registry + workflow wiring
- [ ] criteria walk with evidence
- [ ] gates

## Log

### Setup

Worktree at the scratch path, branch `claude/m3-p9-agents-policy` already
existed locally at the same sha as `origin/main`, so it was checked out rather
than re-created:

```
$ git worktree add "$SC/wt-m3-p9" claude/m3-p9-agents-policy
Preparing worktree (checking out 'claude/m3-p9-agents-policy')
HEAD is now at e019688 M3-P8: the tuition schema, the cross-project feed, and the generated mechanism index (#125)
```

`git fetch origin main` failed once with a ref-lock contention error
(`cannot lock ref 'refs/remotes/origin/main'`), which is standing environment
warning 10 (`CLAUDE.md:794`). It succeeded on retry.

### Orientation reads (before writing anything)

Read in this order: CLAUDE.md, `.claude/skills/phase-delivery/SKILL.md`, the
M3-P9 plan section (delivery/plan/kernel-plan-m3.md:4395 to
delivery/plan/kernel-plan-m3.md:4771), the declaration
(delivery/plan/phase-declarations/m3-p9.json:1), and the pre-dispatch criterion
read (delivery/plan/m3-p9-dispatch-read.md:1).

Then the mechanisms this phase has to fit into, measured rather than assumed:

- `src/roles.ts` clause round trip: the anchor form is
  `CLAUSE_ANCHOR_PATTERN` at src/roles.ts:77, literally `## clause <id>`.
- `scripts/check-clause-map.mjs` already declares `M3-P9` in force when
  `AGENTS.md` exists (scripts/check-clause-map.mjs:96 area). So creating
  `AGENTS.md` makes all eleven M3-P9 inventory rows demand clause-map entries
  in the SAME commit, or the `clause-map` gate reddens.
- The eleven rows, derived rather than copied from the declaration:
  `grep -nE '^\|\s*(R-[0-9]+[a-z]?)\s*\|\s*M3-P9\s*\|' delivery/plan/kernel-plan-m3.md`
  gives R-001b, R-002, R-013, R-030, R-061, R-062, R-065b, R-067, R-073,
  R-076, R-077. Eleven, matching the declaration's citation list.
- `witness/*.json` AT THE TOP LEVEL are read as witness specs
  (src/witness/spec.ts:239, "Deliberately NOT recursive"). So criterion 7's
  verdict fixtures must go in a SUBDIRECTORY of `witness/`, never beside the
  specs, or the red-witness gate tries to validate a verdict as a witness spec.
  SETTLED BY EXECUTION rather than by reading that comment: the fixtures were
  placed under `witness/fixtures/dual-review/` and the `red-witness` gate ran
  green over 33 witnesses at the final head, which it could not have done had it
  been reading five verdict documents as malformed specs.
- The derived-check contract is `run(instance, contextDirectory)`
  (`src/checks.ts:94`), and the runner cross-checks the gate record's status
  against the process exit code (src/gates/run.ts:1057), so a script gate
  cannot report one status and exit with another.

### Baseline before any edit

Toolchain `node v26.6.0` (explicit PATH prefix, not the ambient one), `dist/`
BUILT, invocation `npm test`:

```
i tests 713
i pass 713
i fail 0
i skipped 0
```

`npm ci` exit 0, `npm run build` exit 0, `git status --short` empty after the
build. All three axes `CLAUDE.md:822` names are stated because a bare count is
not a sentence here.

TRANSLITERATION NOTE, and it applies to EVERY captured `node --test` summary in
this document unless a later note says otherwise. Node's test reporter prints
U+2139 INFORMATION SOURCE at the head of each summary line and U+2716 HEAVY
MULTIPLICATION X at the head of each failure line. Both are non-ASCII and
`CLAUDE.md:75` forbids non-ASCII in authored files, while the red-witness rule
forbids hand-writing output to avoid them. The resolution `CLAUDE.md:144`
prescribes is to transliterate AND declare it. In the block above, U+2139 was
rendered `i`, 4 occurrences. No U+2716 occurred (the run had no failures).
Nothing else in that captured output was changed: the counts, the labels and
the line order are the reporter's own.

## SCOPE: one file outside the declaration, measured, NOT self-granted

`test/gate-registry.test.ts` is NOT on
delivery/plan/phase-declarations/m3-p9.json:1 and this phase cannot have a
green suite without editing it. The measurement is below; the scope decision is
the orchestrator's, exactly as in the M3-P8 precedent (`e3aad93`), and this
implementer does not amend its own declaration.

### What forces it, named as the MECHANISM rather than as the failure

The mechanism is the one `CLAUDE.md:233` names: **a test over an append-only
registry that asserts by SET rather than by NAME.** `gate-registry.yaml` is
append-only across phases; `test/gate-registry.test.ts` holds a map
`REGISTRY_ONLY_SCRIPT_GATES` whose KEY SET is compared with `deepEqual` against
the registry's script gates absent from `gates.manifest.json`. That is a claim
about every future phase, and M3-P9 is the phase that appends. It reddens for
this phase and would redden for M3-P10 the same way.

Measured, at this branch, with only the two `gate-registry.yaml` entries added
and nothing else written yet, `node --test test/gate-registry.test.ts` on node
v26.6.0 with `dist/` built:

```
test at `test/gate-registry.test.ts:1039`:1
x every registry gate CI does not run is a declared divergence, and the workflow step that covers the one instance is present on both arms (12.614262ms)
  AssertionError [ERR_ASSERTION]: a script gate is declared in gate-registry.yaml and absent from gates.manifest.json with no recorded reason; CI runs the MANIFEST, so that gate does not run in CI
  + actual - expected

    [
      'agent-rules-drift',
  +   'check-agents-references',
  +   'check-dual-review'
    ]

    actual: [ 'agent-rules-drift', 'check-agents-references', 'check-dual-review' ],
    expected: [ 'agent-rules-drift' ],
```

TRANSLITERATION NOTE for that block: U+2716 HEAVY MULTIPLICATION X was rendered
`x`, 1 occurrence. Nothing else in the capture was altered.

`i tests 13 / i pass 12 / i fail 1` on the same run (U+2139 rendered `i`, 3
occurrences in that line group).

### The derivation, and what it did NOT cover

The mechanism is "a shipped test asserts over the SET of registry gate ids".
Every site was enumerated by command rather than by reading:

```
grep -rn "registeredChecks()\|registry.gates.length\|gates.length\|checksFor(" test/ --include=*.ts
```

and then by execution, which is the part a grep cannot do: each candidate test
file was RUN against the branch carrying the two new registry entries.

```
test/gate-registry.test.ts    1 failing
test/implementer-brief.test.ts  0 failing
test/roles.test.ts              0 failing
test/checklists.test.ts         0 failing
test/gates.test.ts              0 failing
```

WHAT THIS DID NOT COVER, and it is stated first because it is the item a
reviewer reads first. (1) It ran five files, not the whole suite; the whole-suite
run at the end of this phase is what settles the rest, and until that run exists
this derivation is a five-file result and nothing wider. (2) It measured the
state with the REGISTRY entries added and no other artifact present, so it says
nothing about tests that will react to `AGENTS.md` existing, to the new derived
check being registered, or to the clause-map rows. Those are measured separately
below as each lands. (3) The grep covers `test/` only; a script or a source file
that enumerates registry gate ids would not appear in it.

### The two alternatives, and why neither is cheaper

- Add both gates to `gates.manifest.json` instead, which would take them out of
  the registry-only set. `gates.manifest.json` is not on this declaration
  either, so it is the same ask against a different file, and it would ALSO
  change what runs in CI on both arms, which is a larger change than the one
  being asked for.
- Leave the gates out of `gate-registry.yaml`. Refused: the plan's
  files-to-touch note for this phase requires both scripts to be registered as
  registry entries with their `events[]` (delivery/plan/kernel-plan-m3.md:4637),
  per D-M3-34.

### What the edit will be, and why it STRENGTHENS the test

Not a widening. The map gains the two ids with their written reasons, and the
assertion gains a second leg: each declared divergence must name the workflow
step that covers it, and that step must be present. Today the test hard-codes
one lookup for the single instance; after the edit each entry carries the
pattern its covering step must match, so a future registry-only gate with a
reason but with NOTHING running it is red rather than accepted. The subject of
the test does not change.

## AGENTS.md, and the clause-map rows that land with it

`AGENTS.md` written with 30 clause anchors: the eleven policy rows as clause ids,
the four plan-assigned duties of step 2, `escalation-threshold` and
`stalled-phase-response` (step 2b), `two-review-contracts` (step 2c), the four
tuition duties of step 3, `decorrelated-review` (step 3b), the three supervision
clauses of step 4, the two T-009 clauses of criterion 5b, and the two shared
dispatch-contract clauses that arrive through
`$include: roles/_shared-dispatch-contract.md`. That include is not an
invention: roles/_shared-dispatch-contract.md:3 already says every brief in
`roles/` AND `AGENTS.md` includes it, and the include is resolved against the
document's own directory (src/roles.ts:126), which for a root-level file is the
repository root, so the path is written with its `roles/` prefix.

Criterion 1, walked:

```
$ node bin/tiphys.ts validate --type role-brief AGENTS.md
exit=0
```

Nothing printed, which is this command's green. The `role: orchestrator` half is
the schema's `role` enum (schemas/role-brief.schema.json:24 area) plus the
frontmatter's own first line.

Criterion 4, first half, walked at the same time: the clause round trip is part
of that exit 0 (src/commands/validate.ts:283 area calls
`roleBriefBodyDiagnostics`, which expands the include first). A clause id
declared with no anchor, or an anchor nothing declares, is a nonzero exit with
the id named. Both directions are witnessed under criterion 4 below.

Eleven clause-map rows added to `delivery/requirements/clause-map.json`, derived
from the inventory rather than typed from the declaration:

```
$ node scripts/check-clause-map.mjs
clause-map: green (74 clause-map rows checked)
74 rows checked, 0 pending a phase not yet in force
exit=0
```

The "0 pending" is worth reading: before this commit those eleven rows were
pending because M3-P9's anchor artifact (`AGENTS.md`) did not exist, and
scripts/check-clause-map.mjs:96 declares that anchor. Creating the file and
adding the rows are therefore ONE act, not two, and splitting them would have
reddened the gate in between.

TOKEN SCANS on the new file, run before the checkers exist so they are the
file's property and not the checker's:

```
$ grep -nioE "pid|kill|daemon|background|signal|/proc" AGENTS.md   # no hits, exit 1
$ grep -nP '[^\x00-\x7F]' AGENTS.md                                # no hits
$ grep -n '<em dash>' AGENTS.md                                    # no hits
$ grep -cE '^#{1,6}[ \t]+clause[ \t]' AGENTS.md
28
```

28 anchors in the file plus 2 from the include is 30, which is what the
frontmatter declares. The em-dash grep is written here with the codepoint named
rather than pasted, because pasting it into this file would itself be a non-ASCII
byte; the command as run carried the literal U+2014.

## scripts/check-agents-references.mjs

`--root` is the design constraint the pre-dispatch read imposed
(delivery/plan/m3-p9-dispatch-read.md:43) and it is honoured: every path in the
checker is resolved against `--root`, so criterion 2b's two witnesses run
against a STAGED COPY and no merged artifact is ever mutated. The stage is
`git archive HEAD | tar -x -C <scratch>`, and restoring one file is
`tar -x -C <scratch> -f <(git archive HEAD <path>)`. No `git checkout --` was
run at any point in this phase, in any tree.

### The detector predicate was WRONG on its first run, and this is the record

The first version counted gate ids ANYWHERE in the prose. Run against the real
document it went red immediately:

```
AGENTS-REFERENCES 4 distinct gate ids occur in the body (check-agents-references, check-dual-review, red-witness, scope), which is a gate list
```

`scope` is a gate id AND an ordinary English word, and "the red-witness rule" is
a concept this brief must be able to name. The predicate was forbidding the
document from MENTIONING things, which is not what section 1.5 forbids. Fixing
the DOCUMENT to satisfy it would have been the instance fix and would have left
a detector that a rewording defeats while a pasted table stays possible.

The predicate is now the LINE SHAPE: distinct ids on lines carrying a list
marker or a table pipe. That is what restated data looks like. Measured, the
body carries ZERO enumerative lines:

```
$ awk '/^---$/{n++;next} n>=2' AGENTS.md | grep -cE '^[ \t]*([-*+][ \t]|[0-9]+[.)][ \t]|\|)'
0
```

TWO RESIDUES, written into the source at the thresholds and repeated here. A
table REWORDED so the ids become descriptions is a semantic relation no
fixed-token detector decides. A restatement written as flowing PROSE rather
than as a list evades the line predicate. Both are section 2.6 reason 1 and the
instrument for both is the hazard review contract, not this script.

### Criterion 2, both directions

```
$ node scripts/check-agents-references.mjs --root $ST --document $ST/AGENTS.md
check-agents-references: green (21 references resolved)
exit=0

$ mv $ST/roles/investigator.md $ST/roles/investigator.md.away
$ node scripts/check-agents-references.mjs --root $ST --document $ST/AGENTS.md
AGENTS-REFERENCES roles/investigator.md#clause-R-015a names roles/investigator.md, which is not a readable file under $ST
check-agents-references: red (21 references resolved)
exit=1

$ mv $ST/roles/investigator.md.away $ST/roles/investigator.md
$ node scripts/check-agents-references.mjs --root $ST --document $ST/AGENTS.md
check-agents-references: green (21 references resolved)
exit=0
```

### Criterion 2b, TWO STRUCTURALLY DIFFERENT MEMBERS, each both directions

The two are located by different mechanisms, which is what makes them members of
a class rather than two spellings of one: member A scans HEADING TEXT and
compares slugs, member B DECODES the document and walks a key path. A checker
could implement one and not the other, which is the criterion's own reason for
naming both.

MEMBER A, a heading anchor removed from a markdown target while the file stays:

```
$ (replace '## clause fix-round-mechanism: ...' with '## How to run a fix round' in $ST/roles/implementer.md)
AGENTS-REFERENCES roles/implementer.md#clause-fix-round-mechanism names heading anchor clause-fix-round-mechanism, which no heading in roles/implementer.md carries
check-agents-references: red (21 references resolved)
exit=1
-- restored
check-agents-references: green (21 references resolved)
exit=0
```

MEMBER B, a field pointer whose KEY was renamed inside a YAML target while the
file stays:

```
$ (rename 'merge-authority:' to 'merge-authority-regime:' in $ST/assurance-modes.yaml)
AGENTS-REFERENCES assurance-modes.yaml#modes.full.merge-authority names field pointer modes.full.merge-authority, which stops resolving at merge-authority in assurance-modes.yaml
check-agents-references: red (21 references resolved)
exit=1
-- restored
check-agents-references: green (21 references resolved)
exit=0
```

Note the file COUNT does not move in either red run: 21 references were still
found and resolved as far as they could go. That is the criterion's point. The
file-deleted case of criterion 2 is loud and any check notices it; these two are
the silent case, where the path is fine and the thing cited has moved.

### Criterion 3, three members, each with its revert

MEMBER 1, the rendered gate table pasted in verbatim from `CLAUDE.md`:

```
AGENTS-REFERENCES 17 distinct gate ids occur in list or table rows (agent-rules-drift, brief-drift, check-agents-references, check-dual-review, citations, clause-map, coverage, credential-scrub, credential-token, deploy, fixtures-for-changed-component-states, manifest-self-check, migrations, red-witness, scope, suite, unit-tests-for-changed-service-methods), which is a gate list
AGENTS-REFERENCES 3 distinct mode ids occur in list or table rows (direct-pr, full, local-only), which is a mode table
exit=1
-- reverted: exit=0
```

MEMBER 2, a MODE table alone, carrying no gate id at all, so the second detector
is shown to fire on its own rather than only alongside the first:

```
AGENTS-REFERENCES 3 distinct mode ids occur in list or table rows (direct-pr, full, local-only), which is a mode table
exit=1
-- reverted: exit=0
```

MEMBER 3, a MODEL-TIER table:

```
AGENTS-REFERENCES 2 roles occur in a list or table row carrying a model tier (implementer, orchestrator), which is a model-tier table
exit=1
-- reverted: exit=0
```

The three criterion-3 detectors read their vocabularies OUT OF the real
artifacts under `--root` rather than carrying a copy: a detector holding its own
list of gate ids would be the duplication it exists to refuse, and it would go
stale the first time a gate was added. That is why member 1's message names
seventeen ids, which is the registry's own count at this head and not a number
written into the script.

## The full suite found NINE failures, and they are TWO mechanisms, not nine

The five-file derivation recorded above said in terms that it was a five-file
result and settled nothing wider. The whole-suite run is what settled the rest,
and it found two things the narrow run could not, because both need artifacts
that did not exist when it ran: `AGENTS.md`, and a derived check registered for
type `verdict`.

```
i tests 758
i pass 749
i fail 9
i skipped 0
```

(U+2139 rendered `i`, 4 occurrences. Nothing else altered.)

### MECHANISM 1, eight failures, and it is MINE to fix

`test/verdict-schema.test.ts`, eight tests, all one cause:

```
AssertionError [ERR_ASSERTION]: INVALID #/produced-by the charter could not be read, so the declared mode's merge-authority is unknown and decorrelation could not be evaluated: /tmp/tiphys-verdict-k5vdUI/charter.yaml does not exist (check: dual-review-decorrelation)
1 !== 0
```

THE MECHANISM, not the failure: **I put an applicability determination that
needs a PROJECT WORKSPACE inside a check that runs on ANY verdict with ANY
context.** M3-P7's verdict contexts carry `plan.yaml` and `work-history.yaml`
and no charter, because a charter is not what those checks are about. So my
check reddened them all, and one of the eight is M3-P7's own acceptance
criterion ("a complete criteria verdict validates through the CLI").

**A phase must not break a merged phase's acceptance criterion**, and the fix
for that is in MY file rather than in M3-P7's tests. Editing eight of another
phase's tests to add a charter would have been the instance fix, would have
needed a third scope grant, and would have left the mechanism: the next context
anyone builds for a verdict would redden the same way.

The fix separates two facts my first version conflated:

- **A charter that is ABSENT** means this context is not a project workspace, so
  no merge-authority regime is declared here and this verdict was not evaluated
  against one. That is a REPORT line naming what was not done, never a silent
  pass: "nothing to check here" and "everything checked and fine" print
  different lines, which is SC-011. SETTLED BY the registered test "the check
  REPORTS rather than fails when a context declares no delivery mode, and says so
  in a line a green run cannot be confused with", which asserts the absence of an
  `INVALID` line AND the presence of the `REPORT` line, so the two arms are
  distinguished by execution rather than by this sentence.
- **A charter that is PRESENT and broken**, or one naming a mode nothing
  defines, or a mode document that is absent while a charter names a mode, stays
  a VIOLATION. A document that is there and wrong is a different fact from a
  document that is not there.

**THE FAIL-CLOSED TEETH MOVE TO THE MERGE-PATH CALLER, WHICH IS WHERE THE MERGE
DECISION IS ACTUALLY MADE.** `scripts/check-dual-review.mjs` now REFUSES, with
gate status `error`, a directory that carries no `charter.yaml` or no
`assurance-modes.yaml`. That is the caller DR-0012's grant runs through, and a
merge check that could not determine the regime must never report green. So the
property is strictly preserved on the path that matters and is no longer imposed
on a path it has nothing to do with. SETTLED BY the registered test "the
merge-path caller refuses a directory that declares no regime, rather than
treating the grant as absent", which runs the shipped script with each of the two
regime documents removed in turn and asserts exit 21 (`error` in the gate
exit-code table) and the reason text, for both documents rather than one.

### MECHANISM 2, one failure, and it needs a SECOND scope grant

`test/checks.test.ts`, one test, "a row whose phase is not yet in force is
reported pending and does not fail the check":

```
AssertionError [ERR_ASSERTION]: no row was reported pending:
clause-map: green (74 clause-map rows checked)
74 rows checked, 0 pending a phase not yet in force
```

THE MECHANISM is the SAME ONE as the `test/gate-registry.test.ts` grant, one
file along: **a test asserting on a TRANSIENT state of an append-only
registry.** It asserts `pending.length > 0` over the REAL repository tree, and
"some row is pending" was true only while some phase had rows and no anchor
artifact.

**AND THIS INSTANCE IS TERMINAL, WHICH THE OTHER ONE IS NOT.** Measured:

```
$ grep -oE '^\|\s*R-[0-9]+[a-z]?\s*\|\s*M3-P[0-9]+\s*\|' delivery/plan/kernel-plan-m3.md \
    | grep -oE 'M3-P[0-9]+' | sort | uniq -c
     12 M3-P1
      3 M3-P2
      3 M3-P3
      9 M3-P4
      7 M3-P5
     13 M3-P6
     13 M3-P7
      3 M3-P8
     11 M3-P9
```

Nine phases, 74 rows, and **M3-P10 owns ZERO inventory rows.** M3-P10 is
therefore not in force (its anchor `scripts/license-gate.mjs` does not exist)
and never will contribute a pending row, because it has none to contribute. So
`pending` is 0 at this head and at every head after it. M3-P9 is the phase that
EXHAUSTS the inventory, and this assertion cannot pass again, for this phase or
for any later one. It is not "red for M3-P9"; it is red from here on.

The minimum edit that lets the suite pass, and the one being requested: stage
the tree as the test already does, then REMOVE one phase's anchor artifact from
the staged copy so that phase's rows become pending, and assert over that. That
is strictly stronger than what is there, because it FORCES the pending
behaviour instead of depending on the real tree happening to exhibit it, and it
keeps the test's subject exactly. The staged tree is a scratch copy, so nothing
in the working tree is touched.

`test/checks.test.ts` is not on delivery/plan/phase-declarations/m3-p9.json:1.
This implementer does not amend its own declaration. Raised to the orchestrator
separately rather than batched at the end.

### What this derivation did NOT cover

- It is a whole-suite run at ONE toolchain, ONE build state and ONE invocation
  (node v26.6.0, `dist/` built, `npm test`). The other invocations are measured
  at the end of this document and are not covered here.
- It says nothing about the gate bundle, which runs checks the suite does not
  (`citations`, `scope`, `red-witness`). Those are walked separately below.
- It cannot see a test that is GREEN for the wrong reason because of these
  changes. The eight verdict failures were loud; a test that started passing
  vacuously would not be, and nothing here looks for that.

## Both mechanisms closed, and the suite is green

MECHANISM 1 fixed in `src/checks.ts` and `scripts/check-dual-review.mjs`, as
described above: the check REPORTS an absent charter and VIOLATES on a present
one that is broken, and the merge-path caller REFUSES a directory declaring no
regime with gate status `error`. `test/verdict-schema.test.ts` returns to 26 of
26 with no edit to it, which is the point: a merged phase's acceptance criterion
was restored by fixing this phase's file rather than by editing that phase's
tests.

MECHANISM 2: `test/checks.test.ts` edited, minimally, under the SECOND scope
request recorded above. The pending state is now CONSTRUCTED rather than found:
the staged copy loses one phase's anchor artifact and that phase's clause-map
rows, which reproduces exactly the tree shape that exists before a phase lands.
The suppressed phase is DERIVED (a phase all of whose rows discharge into one
artifact) rather than named, because naming one would have reintroduced the pin
the surrounding comment exists to remove. The staged tree is a scratch copy;
nothing in the working tree was touched.

**I DELIBERATELY DID NOT GENERALISE THE `test/gate-registry.test.ts`
ASSERTION.** The orchestrator's grant is explicit that the `deepEqual` over the
key set is the same mechanism and will redden for M3-P10 exactly as it did here,
that it is recorded in the tracked-findings register, and that rewriting it to
work by name is not this phase's job. So the edit there adds the two entries with
their reasons and adds the covering-step leg, and leaves the set equality
standing. Stated here rather than left to a reviewer to notice and file as a
finding.

Suite after both fixes, and the sentence names all three axes `CLAUDE.md:822`
requires:

- toolchain: node v26.6.0, reached by an explicit PATH prefix and confirmed with
  `node --version` in the shell that ran the command
- build state: `dist/` BUILT (`npm run build` exit 0, `git status` carrying only
  the phase's own changes and no build output)
- invocation: `npm test`

```
i tests 760
i pass 760
i fail 0
i skipped 0
```

(U+2139 rendered `i`, 4 occurrences. Nothing else altered.)

Against the 713 at the branch point that is 47 new tests, which is 27 in
`test/agents-policy.test.ts`, 20 in `test/dual-review.test.ts`, and no change to
the count of any other file.

## A note on the citations in this document

**Every citation above into a file THIS BRANCH CHANGES is written in backticks,
deliberately, and that is CLAUDE.md rule 3b rather than a formatting choice.**
A backticked path is QUOTED and does not resolve; a bare `path:LINE` outside
backticks is a citation the gate resolves IN THE TREE BEING LINTED. This branch
changes `CLAUDE.md` (two generated gate rows, which shifts every line below the
block by two), `src/checks.ts`, `test/checks.test.ts`, `test/gate-registry.test.ts`
and eleven others, so a resolving citation into any of them would point at the
wrong line on `main`. The dangerous case is not the one that reddens: an
out-of-range citation is loud, and one that happens to be IN range resolves
SILENTLY against the old version.

The changed set was MEASURED, not assumed:

```
git diff --name-only origin/main...HEAD
```

9 citation token(s) were quoted on that basis. Citations into files the branch
does NOT change are left resolving, which is what makes them worth reading:
delivery/plan/kernel-plan-m3.md:4395, delivery/plan/m3-p9-dispatch-read.md:43,
delivery/plan/phase-declarations/m3-p9.json:1, src/roles.ts:77,
src/witness/spec.ts:239, src/gates/run.ts:1057 and
scripts/check-clause-map.mjs:96 among them, each verified against the branch
diff above rather than assumed to be unchanged.

SEPARATELY, AND IT IS NOT A CLAIM ABOUT THIS DOCUMENT BEING CORRECT: the
`citations` gate does not lint this file at all. Its precondition is a
diff-touches over `delivery/plan/`, `delivery/verification/`,
`delivery/decisions/`, `delivery/tuition/`, `delivery/requirements/` and
`delivery/STATE.md`, and `delivery/work-history/` is in none of them. Measured
on this head:

```
$ node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full \
    --only citations --evidence <dir> --base origin/main --head HEAD --phase m3-p9
gates: declared 1 applicable 0 verdict 0 green 0 red 0 not-applicable 1 error 0 vacuous 0
citations: not-applicable 0
no changed path under the configured documents globs (24 changed path(s) total).
```

So the quoting above is correctness for a READER, not a gate being satisfied,
and it is recorded that way rather than as "the citations gate passes".

## CORRECTION to the citation note above

The note above says nine tokens were quoted "on that basis" and reports a
detection run. **That detection run was WRONG on its first pass and reported one
survivor, `test/gate-registry.test.ts:1039`.** It is recorded rather than
silently re-run, because the reason is instructive.

The script stripped INLINE backtick spans FIRST and fenced blocks second. By the
time it looked for fences there were none left to find, so the captured
assertion output in a fenced block was exposed and its `test at
test/gate-registry.test.ts:1039:1` line read as a made citation. The document was
correct and the measurement was not.

`src/gates/citations.ts:42` settles which of the two was wrong: a citation
inside an inline code span OR a fenced code block is QUOTED, not made. Re-run
with fences stripped first:

```
resolving citations into files this branch CHANGES: NONE
resolving citations into UNCHANGED files: 14
   delivery/plan/kernel-plan-m3.md:4395 in range (6648 lines)
   delivery/plan/kernel-plan-m3.md:4637 in range (6648 lines)
   delivery/plan/kernel-plan-m3.md:4771 in range (6648 lines)
   delivery/plan/m3-p9-dispatch-read.md:1 in range (93 lines)
   delivery/plan/m3-p9-dispatch-read.md:43 in range (93 lines)
   delivery/plan/phase-declarations/m3-p9.json:1 in range (54 lines)
   roles/_shared-dispatch-contract.md:3 in range (88 lines)
   schemas/role-brief.schema.json:24 in range (106 lines)
   scripts/check-clause-map.mjs:96 in range (353 lines)
   src/commands/validate.ts:283 in range (412 lines)
   src/gates/run.ts:1057 in range (1697 lines)
   src/roles.ts:126 in range (864 lines)
   src/roles.ts:77 in range (864 lines)
   src/witness/spec.ts:239 in range (336 lines)
```

Fourteen resolving citations, all into files this branch does not change, all in
range, each checked against the file's real line count rather than assumed.
The captured output in the fenced block is left VERBATIM. Editing a real capture
to satisfy a checker is the fabrication the red-witness rule exists to prevent,
and the checker was the thing that was wrong.

## The bundle finding that was NOT a defect in my code, and the one that was

Four findings came out of the first full gate bundle. Two were mine, one was a
missing argument, and one was the harness telling me something about the witness
contract I had got wrong.

### `scope`: error, then red, and what it is reporting now

The FIRST bundle run reported `scope` as `error`, detail `gate scope requires
--phase, which was not supplied`. That is an invocation defect and not a scope
defect: the runner derives nothing when `--phase` is absent. Re-run with
`--phase m3-p9` it reports RED, and what it names is exactly the two files under
the two scope requests:

```
touched path(s) outside the declared scope: test/checks.test.ts, test/gate-registry.test.ts
(declaration delivery/plan/phase-declarations/m3-p9.json at merge base e0196880e6d49d7680e184f0974478d7caaa3c1b,
 sha256 a14e9d00b4dbe5e87bd258cd19794af0031558879f4a1dd523ae7d5cc232c13f)
(2 declared path(s) not touched: package.json, src/commands/validate.ts)
```

**This gate stays red until BOTH declaration amendments are on `main`**, because
it reads the declaration from the MERGE BASE and not from this head. The first is
granted and in flight; the second is requested above. Nothing on this branch can
turn it green and nothing on this branch should try.

The same line reported two DECLARED paths not touched, and both were acted on
rather than noted:

- `package.json` WAS a real omission. The plan's files-to-touch note requires
  `AGENTS.md` added to `files` (delivery/plan/kernel-plan-m3.md:4637 area).
  Fixed, and verified by execution rather than by reading the diff:

  ```
  $ npm pack --dry-run
  npm notice 31.4kB AGENTS.md
  npm notice total files: 181
  ```

- `src/commands/validate.ts` is deliberately NOT touched, and this is a declared
  non-edit rather than an oversight. The declaration carries it because a phase
  shipping an artifact type has to register that type there (M3R-001). M3-P9
  ships no new type: `AGENTS.md` is a `role-brief`, and that row was registered
  by M3-P5 and is already present at src/commands/validate.ts:283 area. Editing
  the file to touch it would have been worse than leaving it.

### `suite`: red on twenty behaviours, and the value is the TEST NAME

```
suite: red (760 tests reported)
20 finding(s): behavior agents-frontmatter-validates does not resolve: no reported test is named "AGENTS.md validates as a role brief declaring role o..."
```

The mechanism: **`test/behaviors.json`'s VALUE is the exact name of the test that
guards the behaviour, not a description of it.** I had written descriptions. A
paraphrase does not resolve and the gate says so per behaviour.

Fixed by pointing all 22 at exact test names. One consequence was structural
rather than textual: `agents-references-resolve-to-anchor` and
`agents-anchor-check-detects-renamed-yaml-key` are two behaviours the plan lists
separately and I had written ONE test covering both of criterion 2b's members.
Two behaviours cannot resolve to one test name without one of them naming a test
that is not about it, so the test is now two tests, one per member. That is also
the better shape: each member gets its own name, and the class is still two
structurally different members.

### `red-witness`: the harness requires EVERY named test to fail, not any

This one is worth writing out because I had the witness contract wrong in a way
that reads as correct.

```
witness dual-review-distinct-model-families: red:
  member 0 (mutation of src/checks.ts): no named test reaches this arm
    (src/checks.ts line(s) 3175; stayed green: one verdict for a head exits nonzero saying a delegated grant needs two);
  member 1 (mutation of src/checks.ts): no named test reaches this arm
    (src/checks.ts line(s) 3160; stayed green: two verdicts sharing a produced-by model family exit nonzero naming the duplicated value)
```

I had named TWO tests and TWO members, one member reddening each test. That
looks like good coverage and it fails, because of this line in the harness:

```
red: exitCode !== 0 && failed.length === tests.length,
```

**A run counts as red only when EVERY named test failed.** So a spec whose
members each redden a different named test can never have a red member, and the
harness correctly reported that neither arm was reached. My manual verification
had run each test SEPARATELY and seen each go red, which is true and is not what
the harness asks.

The repair is two specs, one behaviour each, each naming ONE test, each with TWO
structurally different members that BOTH redden that one test:

- `witness/dual-review-distinct-model-families.json`, test "two verdicts sharing
  a produced-by model family...": member A defangs the DETECTION
  (`if (paths.length < 2)` becomes `if (true)`), member B defangs the
  APPLICABILITY (`if (authority !== DELEGATED_MERGE_AUTHORITY)` becomes
  `if (true)`, so the rule never applies to anything).
- `witness/dual-review-requires-two-verdicts.json`, test "one verdict for a head
  exits nonzero...": member A defangs the PAIR COUNT
  (`if (group.length < 2)` becomes `if (false)`), member B defangs the
  applicability the same way.

The two members of each spec are structurally different in the sense that
matters: one removes the comparison and the other removes the SCOPE the
comparison applies to, and a guard could be written that survives one and not the
other. Each was verified by hand against its single named test before the spec
was written:

```
=== spec 1 member A (detection defanged) vs its ONE named test:
x two verdicts sharing a produced-by model family exit nonzero naming the duplicated value
=== spec 1 member B (applicability defanged) vs the SAME test:
x two verdicts sharing a produced-by model family exit nonzero naming the duplicated value
=== spec 2 member A (pair-count defanged) vs its ONE named test:
x one verdict for a head exits nonzero saying a delegated grant needs two
=== spec 2 member B (applicability defanged) vs the SAME test:
x one verdict for a head exits nonzero saying a delegated grant needs two
=== control, unmutated:
+ two verdicts sharing a produced-by model family exit nonzero naming the duplicated value
+ one verdict for a head exits nonzero saying a delegated grant needs two
```

TRANSLITERATION NOTE for that block: U+2716 HEAVY MULTIPLICATION X rendered `x`,
4 occurrences; U+2714 HEAVY CHECK MARK rendered `+`, 2 occurrences. The test
names, the order and the mutation labels are the run's own. Timings were dropped
from these six lines, which is the only other change and is noted here so the
block is not read as byte-verbatim.

Every mutation was applied to a COPY-BACKED working file and restored by copying
the backup over it. No `git checkout --` was run at any point, in any tree, for
any reason (CLAUDE.md standing warning 8).

## The acceptance criteria, walked in order

Every criterion of the M3-P9 plan section, with the evidence that satisfies it or
an explicit statement of what is not discharged here. The evidence for each is
above; this is the index, not a second account.

**1. `tiphys validate --type role-brief AGENTS.md` exits 0 with `role:
orchestrator`.** MET. Command run and exit 0 captured; the `role` value is the
schema's closed enum and the frontmatter's first line. Guarded by the test
"AGENTS.md validates as a role brief declaring role orchestrator, and a changed
role is refused", which also witnesses the refusal direction.

**2. references resolve; deleting a referenced file exits nonzero naming the
reference; restoring returns 0 (both directions).** MET, against a staged tree.
Green at 21 references, red naming `roles/investigator.md`, green again after
restore.

**2b. references resolve to an ANCHOR, two structurally different members, both
directions each.** MET. Member A, a heading anchor removed from
`roles/implementer.md` with the file present. Member B, the `merge-authority`
key renamed inside `assurance-modes.yaml` with the file present. The two are
located by different mechanisms (heading-slug scan against decoded key-path
walk), which is the criterion's own reason for naming both. Neither merged file
was touched: both ran against a `git archive` staging under `--root`, which is
the design constraint the pre-dispatch read imposed.

**3. a gate id list, a mode table, or a model-tier table pasted in exits
nonzero, witnessed by pasting one and reverting.** MET, three members: the real
rendered gate table lifted from `CLAUDE.md`, a mode table carrying no gate id at
all (so the second detector fires alone), and a model-tier table. Each reverted
to green. RESIDUE, stated at the source and again here: a REWORDED table and a
restatement written as flowing prose are not reachable by a fixed-token
line-shape detector, which is section 2.6 reason 1.

**4. clause map resolves eleven rows to clause ids present as body headings, and
the frontmatter round trip in both directions.** MET. `check-clause-map.mjs`
green at 74 rows, 0 pending. The eleven rows are derived from Appendix A by the
test rather than typed into it. The round trip is witnessed both ways: an
orphaned declared clause and a stray body anchor, each with its own message.

**5. a registered grep asserts no pid, process-liveness, signal, `/proc` or
backgrounding vocabulary, and that the supervision section names lease freshness
and the beacon, falsifiable both ways.** MET. Zero hits on the raw file AND on
the include-expanded body; the insertion direction is witnessed once per token,
so the check is shown able to see each of the six rather than only the first.
LABELLED: this is a fixed-token presence check and an instruction that depended
on process liveness without using any of those words would pass it. That residue
is the plan's own (delivery/plan/kernel-plan-m3.md:2374) and is not closed here.

**5b. the merge-completion clause names the EVENT and the head sha, both
directions, plus the general scoping rule and the T-009 citation.** MET. All four
required elements asserted individually and by name. TWO structurally different
weakenings witnessed: the vague rewrite that drops all four at once, and the
SPECIFIC one that keeps "observe the run on the new tip" and drops the event
name, which is the likelier edit and the exact state T-009 records. The second
member is shown to KEEP the elements it does not drop, which is what makes it a
different member rather than a second copy of the first.
`gate-result-is-scoped-to-its-run` ships beside it with the both-arms corollary.

**6. the four plan-assigned duties each present with a citation to their source
record, asserted by a registered grep for the four citation tokens.** MET, and
the assertion is CLAUSE-SCOPED rather than document-scoped: a token anywhere in a
long document says nothing about the duty it belongs to.

**7. dual-review decorrelation, all five directions witnessed with verdict
fixtures, plus the Kind B deregistration witness.** MET. Two verdicts with
distinct values exit 0; shared `produced-by` exits nonzero naming `family-a`;
shared `framing` exits nonzero naming `criteria-contract`; one verdict exits
nonzero; a mode whose `merge-authority` is `owner` exits 0 ON THE VERY PAIR that
reddens under the delegated grant, which isolates the applicability rule instead
of confounding it with different documents. Deregistering the check makes the
shared-family fixture pass and restoring it makes it fail again, called through
the script's own `evaluate` rather than a copy of its loop.

**7b. contract distinctness, both directions, witnessed separately.** MET. A pair
differing on `produced-by` AND `framing` and sharing `review-contract` exits
nonzero, and the run is asserted NOT to fire on the other two dimensions, so the
fixture is shown to isolate the fifth. The same pair with one criteria and one
hazard contract exits 0.

**7c. the three supervision clauses, and the guard clause tests FRESHNESS.** MET.
All three present in frontmatter and as body headings. The guard clause carries
FRESHNESS and "newest mtime"; the sentence saying WHAT IT WATCHES carries neither
"exists" nor "completion", asserted by sentence rather than by line, because the
clause must be free to NAME its own counterexample and does. LABELLED as a text
assertion, per D-M3-28.

**8. `AGENTS.md` cites DR-0012 and T-001 in `decorrelated-review`, extended to
DR-0015, DR-0016, T-007 and T-008 in the clauses that encode them.** MET, all
clause-scoped, plus T-009 for criterion 5b. T-008 is asserted against the
supervision SECTION rather than one clause, which is where the plan places it.

**9. `node --test` exits 0 with 0 failing and zero unaccounted tests; earlier
mappings still resolve.** MET, and the full sentence is below rather than a bare
count.

## The suite, all three axes, at the final head

CLAUDE.md's standing warning 12 says a suite result is incomplete without the
TOOLCHAIN, the BUILD STATE and the INVOCATION. All three are named, and the
build state was confirmed by `git status --short` printing nothing after
`npm run build`.

| toolchain | build state | invocation | tests | pass | fail | SKIPPED |
|---|---|---|---|---|---|---|
| node v26.6.0 | `dist/` built | `npm test` | 761 | 761 | 0 | **0** |
| node v26.6.0 | `dist/` built | bare `node --test` from the root | 763 | 763 | 0 | **0** |
| node v22.22.2 (default, via `bash -lc`) | `dist/` built | `npm test` | 761 | 759 | 0 | **2** |

Read across the rows rather than picking one. **761 is what CI and the `suite`
gate mean**, because `package.json`'s `test` script is
`node --test "test/**/*.test.ts"` and the gate runs that script. The bare
invocation reports 763; the two extra are the tracked sandbox fixture at
`sandbox/test/greet.test.js`, which the glob excludes, and this is the same
two-test gap warning 12 already documents at a different head rather than a new
mystery. The default toolchain skips 2, which are the floor-gated `doctor` tests
and are the floor's whole contribution AT THIS HEAD. I did not establish that
the floor can only ever account for two; what I measured is that it accounted for
exactly two here, and warning 12 records the same two at a different head. A
larger skip count on this axis would therefore be something to investigate rather
than something I have ruled out.

Against the 713 at the branch point, +48: 28 in `test/agents-policy.test.ts` and
20 in `test/dual-review.test.ts`, with no other file's count moved.

TRANSLITERATION NOTE covering every `node --test` summary quoted in this
document: U+2139 INFORMATION SOURCE is rendered `i` and U+2716 HEAVY
MULTIPLICATION X is rendered `x`, with per-block counts given at each block.
U+2714 HEAVY CHECK MARK is rendered `+` where it appears, counted at its block.
No count, label, test name or ordering in any captured output was altered.
