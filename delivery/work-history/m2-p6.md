# M2-P6 work history: coverage checker

Branch: `claude/m2-p6-coverage-checker`. Written incrementally from the
first minutes per CLAUDE.md's dispatch contract; this file's mtime is the
beacon a freshness watchdog is watching.

## Step 0: setup

- Fetched `origin/main` and branched `claude/m2-p6-coverage-checker` off it.
  `origin/main` head at fetch time: `4c9bfbc` (M2-era paperwork batch, #12),
  which contains `8718852` (M2-P1: gate contract, evidence claim and
  runner, #11).
- Read `CLAUDE.md`, `MECHANISMS.md`, `delivery/plan/kernel-plan-m2.md`
  sections 1 and 3 (M2-P6), `delivery/STATE.md`'s "Carried forward from
  M2-P1" section, and the shared M2 fan-out brief.

## Mechanism lookup (T-005 interim obligation, MECHANISMS.md)

Rows that bear on this phase:

- **"Reading a path whose type is not established"**: M2-C-6 binds this
  phase. The coverage gate reads three configured paths (inventory,
  coverage table, and an optional `--config` document) that it did not
  create, so every read goes through `readRegularFileIfPresent` /
  `classifyEntry` from `src/task.ts`. Verified directly rather than
  asserted:
  ```
  $ grep -n "readFileSync\|writeFileSync" src/gates/coverage.ts
  1:import { writeFileSync } from "node:fs";
  584:        writeFileSync(
  647:    writeFileSync(resultPath, renderGateResult(result)),
  ```
  No `readFileSync` import exists in the module at all (every read is
  `readRegularFileIfPresent`); both `writeFileSync` call sites (lines
  584 and 647) are immediately preceded by a `refuseOpenForWrite` guard
  (the write-side half of M2-C-6, lines 581 and 641).
- **"Parsing another program's reporter output"**: not directly applicable
  (this gate parses markdown tables it is itself the sole consumer of, not
  another program's structured output), but the spirit carries: the
  markdown-table parse is PINNED to an explicit row/cell shape (a line
  starting and ending with `|`, separator rows skipped, first cell tested
  against a configured `idPattern`) rather than a permissive scrape that
  would silently absorb unrelated table rows from other documents.
- **"Deciding what another program will do by pattern-matching the text of
  a file it consumes"**: the closest analogue here, because this gate is
  a regex classifier over prose documents by construction (the coverage
  table's "Bucket" column). The plan accepts this for M2 (the milestone
  IS the arithmetic-over-two-documents checker); the mitigation taken is
  full-string anchoring of every bucket-kind pattern (`^(?:pattern)$`), so
  a bucket kind cannot silently absorb a value it was not written for, and
  an unrecognized value is a named finding (`unknown-kind`) rather than a
  guess.
- **"Claim file"**, **"Lease compare-and-swap"**, **"Atomic file
  replacement"**, **"Worktree removal and force branch delete"**,
  **"Append-only log"**, **"A guard's own failure path"**: none of these
  mechanisms is exercised by this phase (no claim, no lease, no atomic
  rename, no destructive git operation, no log tail read, no bespoke
  failure path beyond the shared `makeGateResult`/`refuseOpenForWrite`
  primitives already governed by their own rows).

## Step 1: M2-C-1 verification of the M2-P1 contracts (before any code)

Verified against the delivered M2-P1 code at `origin/main` (`4c9bfbc`,
containing `8718852`), read in full in this session:

- `src/task.ts` exports `classifyEntry(path): EntryClass`,
  `refuseOpenForWrite(path): string | undefined`, and
  `readRegularFileIfPresent(path): RegularRead` with the shapes the plan
  assumes (`{kind: "read", body}` / `{kind: "absent"}` /
  `{kind: "refused", reason}`). Matches M2-C-6's contract exactly.
- `src/gates/result.ts` exports `makeGateResult(fields): GateResult`, the
  sole constructor, with the documented M2-C-2 rewrite (`status: "green"`
  with `units === 0` becomes `status: "error"`, `vacuous: true`). Matches.
- `src/gates/result.ts` exports `renderGateResult`, `exitCodeForStatus`,
  and the four-word `GateStatus` vocabulary. Matches.
- `src/gates/validate.ts` exports `loadSchema`, `validate`,
  `formatDiagnostics`, over the closed keyword set (`type`, `required`,
  `properties`, `additionalProperties`, `enum`, `items`, `minimum`,
  `minItems`, `pattern`, `const`, local `$ref`), diagnostic contract
  `INVALID <pointer> <message>`. Matches; the coverage-config schema below
  is written entirely inside this closed set.
- `src/gates/manifest.ts`'s `GateEntry`/`PreconditionSpec` shapes match
  section 1.4's table: `precondition.kind` includes `file-exists`, and a
  `file-exists` precondition requires a `path` field (enforced in
  `manifest.ts`'s `REQUIRED_FIELD_FOR_KIND`). The `coverage` gate's
  manifest entry below uses this precondition kind exactly as section 1.4
  declares (`file-exists inventory config`).
- `src/gates/run.ts`'s gate subprocess contract: invocation is
  `<command...> --result <abs path> --evidence <abs dir>` plus one
  `--<name> <value>` per declared parameter; exit codes `0/1/20/21`. The
  coverage gate declares no run parameters (`base`/`head`/`phase`), so its
  invocation is exactly `<command...> --result <path> --evidence <dir>`.
  Verified by reading `runOneGate` in `src/gates/run.ts`.
- `gates.manifest.json` at `origin/main` carries exactly one gate
  (`manifest-self-check`); no `coverage` entry pre-exists, so this phase's
  addition is a pure append, consistent with `test/behaviors.json` and
  `gates.manifest.json` being append-only registries (CLAUDE.md
  convention 5, M2-D-19).

**No divergence found.** The delivered M2-P1 shape matches what the plan
section assumes; proceeding to implementation.

## Verification of the real pair, done BEFORE writing the gate

Ran, against `delivery/requirements/migration-table.md` (inventory) and
`delivery/plan/kernel-plan-v1.md` Appendix A (coverage table):

```
grep -oE '^\| R-[0-9]+[a-z]?' delivery/requirements/migration-table.md | sed 's/| //' | sort -u | wc -l
# 115
grep -oE '^\| R-[0-9]+[a-z]?' delivery/plan/kernel-plan-v1.md | sed 's/| //' | sort -u | wc -l
# 115
diff <(grep -oE '^\| R-[0-9]+[a-z]?' delivery/requirements/migration-table.md | sed 's/| //' | sort -u) \
     <(grep -oE '^\| R-[0-9]+[a-z]?' delivery/plan/kernel-plan-v1.md | sed 's/| //' | sort -u)
# exit 0, no diff
```

A standalone Node script (not part of the shipped module, run once in this
session to derive the expected numbers before writing any assertion)
walked Appendix A's rows with the exact bucket-kind regexes the plan
states (`M([0-9]+)-P[0-9]+`, `M([0-9]+)`, `DR-[0-9]{4}|D-[0-9]+|M2-D-[0-9]+`,
`parked`, each anchored full-string) and produced:

```
rows found: 115
perKind { milestone: 104, phase: 11 }
perMilestone { M4: 13, M3: 74, M1: 11, M2: 16, M5: 1 }
unknown []
```

This matches the plan's stated criterion 1 numbers exactly (units 115,
phase 11, milestone 104, M1 11, M2 16, M3 74, M4 13, M5 1, parked 0 by
absence). **No divergence to report**: the real documents produce the
numbers the plan states, so the phase proceeds on the plan's assumption
rather than raising a finding.

## Design decisions (step 2, config shape)

- `bucketKinds` is modeled as an ARRAY of `{kind, pattern, requiresNote}`
  rather than an object keyed by kind name, because the closed keyword set
  (`src/gates/validate.ts`) has no `patternProperties`, and an array with
  `items` expresses the same information inside that set.
- "a milestone extraction (a capture group)" is not a separate config
  field: each `bucketKind`'s own `pattern` carries its capture group
  (`M([0-9]+)-P[0-9]+` for phase, `M([0-9]+)` for milestone), read as
  `match[1]` when the match used it. `decision` and `parked` carry no
  group, exactly as the plan states them verbatim, so a row of those kinds
  is bucketed in the per-milestone view by the KIND's own name (this is
  why `parked` appears as a key in `perMilestone` rather than as an `Mn`
  value, and is exercised by the parked-note test, both directions).
- **The kernel config is a plain exported TypeScript constant**
  (`KERNEL_COVERAGE_CONFIG` in `src/gates/coverage.ts`), not a checked-in
  data file. This phase's files-to-touch list
  (`delivery/plan/phase-declarations/m2-p6.json`) names the module, its
  schema and its test, and no separate configuration document; a
  committed config file at a path outside that list would fail the scope
  audit. A `--config <path>` flag exists on the CLI for a caller that
  wants a different pair (used by every fixture test in
  `test/coverage-gate.test.ts`, and validated against
  `coverage-config.schema.json` when supplied), and the kernel config
  itself was checked to conform to that same schema:

  ```
  $ node --input-type=module -e '
  const { loadSchema, validate } = await import("./src/gates/validate.ts");
  const { KERNEL_COVERAGE_CONFIG } = await import("./src/gates/coverage.ts");
  const fs = await import("node:fs");
  const schemaDoc = JSON.parse(fs.readFileSync("src/gates/schemas/coverage-config.schema.json","utf8"));
  const loaded = loadSchema(schemaDoc, "coverage-config.schema.json");
  const diags = validate(loaded.schema, KERNEL_COVERAGE_CONFIG);
  console.log("diagnostics:", JSON.stringify(diags));
  '
  diagnostics: []
  ```
- A bucket value matching MORE THAN ONE declared kind is resolved by
  taking the first match in declaration order rather than reported as a
  distinct "ambiguous" finding. No acceptance criterion requires the
  ambiguous case (the four real bucket-kind patterns are mutually
  exclusive once anchored full-string, verified by the real-pair test
  producing zero `unknown-kind` findings and the exact stated per-kind
  split), and adding an untested branch no criterion drives is the kind
  of extra surface the fix-round contract's "one witness is not a class"
  spirit warns against. Stated here as an open design choice rather than
  a silent one.
- **Registered `unitLabel` is `"finding ids checked"`**, matching section
  1.4's manifest table verbatim (not the acceptance-criteria prose
  "inventory ids checked", which is the semantic paraphrase); the
  manifest entry in `gates.manifest.json` uses the section 1.4 string.
- The `coverage` gate's manifest `precondition` is `file-exists` on
  `delivery/requirements/migration-table.md` (the inventory document),
  matching section 1.4's "`file-exists` inventory config" cell. This is a
  DIFFERENT dangerous state than criterion 7's empty inventory: the
  precondition covers the file being entirely ABSENT (structural,
  `not-applicable` if unmet, evaluated by the runner before this gate is
  even spawned), while criterion 7 covers the file being PRESENT but
  producing zero id rows (an `error`, evaluated inside this gate). Kept
  separate deliberately so neither can mask the other.
- The gate additionally writes `evidence/counts.json` (the full per-kind,
  per-milestone and findings breakdown as structured JSON) when
  `--evidence` is supplied, referenced in the record's `evidence` array.
  Not required by any criterion; added because criterion 1 asks for "two
  different views, both recorded", and a structured file is a more
  durable record of that than parsing the `detail` prose string. Declared
  here as an addition beyond the plan's literal step list, in the same
  spirit as M2-P1's declared `vacuous` field addition.

## Hazard-class self-review (T-007, pre-submit, per the shared brief)

Hazard class: "arithmetic over two documents that can drift, where a
total can add up while a row is lost." Named defeaters, each walked:

1. **"a renumbering that leaves a bucket row pointing at an id that no
   longer exists"** , the PHANTOM check (`checkCoverage`'s second pass,
   `src/gates/coverage.ts`), independent of the orphan check (the first
   pass, over inventory ids). Evidence: `test/coverage-gate.test.ts`
   test "a coverage-table row whose id is absent from the inventory is
   red naming it phantom" (green), plus the orphan test staged against
   the REAL appendix with `R-050a` deleted from a mutated copy of its
   text (green both directions: red when deleted, green when restored).
2. **"a bucket value matching no declared kind, silently uncounted"** ,
   every `bucketKinds[].pattern` is compiled anchored
   (`^(?:pattern)$`); an unmatched value is the `unknown-kind` finding,
   named by id and the literal observed value (`JSON.stringify`d into the
   detail string so an empty or whitespace value is visibly distinct from
   a missing one). Evidence: test "a phase bucket value is counted under
   its milestone and kind, and an unrecognized bucket value is red naming
   the row" (green), fixture value `"TBD"`.
3. **"an empty inventory producing a green with nothing examined"** ,
   `main()` in `src/gates/coverage.ts` computes `status: "green"` when
   `totalInventoryIds === 0` REGARDLESS of any findings, so the shared
   `makeGateResult` constructor's own M2-C-2 rewrite (not a second
   implementation of that rule) turns it into `status: "error"`,
   `vacuous: true`. Evidence: test "an empty inventory is error with
   units 0 and the vacuous flag set, never green", run through the real
   CLI entry and real file reads (not a unit call), exit code 21,
   `record.status === "error"`, `record.vacuous === true`.
4. **"a milestone extraction that cannot produce the totals the plan
   states, so the two views agree only because one is never computed"**
   , `perKind` and `perMilestone` are two INDEPENDENT tallies filled in
   the same loop from the same classification, and the real-pair test
   asserts BOTH against the plan's stated numbers in the same test run
   (`perKind.phase === 11`, `perKind.milestone === 104`, and all five
   `perMilestone` entries), so neither total can pass by only one of them
   being computed.
5. **"a finding whose outcome cell is present but empty"** ,
   `checkFindingOutcomeParity` distinguishes MISSING (no row for the id)
   from EMPTY (a row exists with `outcome === ""`) as two different
   result buckets, never conflated. Evidence: test "a finding report
   missing an id, carrying an empty outcome, or fully covered, is red,
   red, and green respectively naming the id", three directions, `missing
   = ["F-6"]` in direction 1 and `empty = ["F-3"]` in direction 2 (not the
   other bucket in either case).

## Acceptance criteria walked

All nine acceptance criteria of the M2-P6 plan section, each with its test
in `test/coverage-gate.test.ts` (test names are the literal
`test/behaviors.json` values, per the project's existing convention of
using the behavior description as the test name) and its result on this
branch (`node --test test/coverage-gate.test.ts`, 9 pass, 0 fail):

1. Real pair, units 115, per-kind and per-milestone breakdown matching
   the plan: test "the coverage gate against the real migration table
   and appendix reports units 115 with per-kind and per-milestone
   breakdowns matching the plan". Also runs the real manifest through
   `node bin/tiphys.ts gates run` and asserts the `coverage` row in
   `summary.json` is `green` with `units: 115`, so the manifest wiring
   (not only the importable function) is evidenced. PASS.
2. `M1-P3` counted under milestone M1 and kind phase; an unmatched value
   red naming the row, both directions: test "a phase bucket value is
   counted under its milestone and kind, and an unrecognized bucket
   value is red naming the row". PASS.
3. Appendix row deletion is an orphan, restoring it is green, both
   directions, against the REAL appendix text (id `R-050a`, chosen
   because it is a plain single-row entry unrelated to any other
   fixture): test "deleting an appendix row is red naming the orphan id,
   and restoring it is green". PASS.
4. A duplicated id is red naming it double-bucketed: test "an id
   appearing in two coverage-table rows is red naming it
   double-bucketed". PASS.
5. A coverage-table row whose id is absent from the inventory is red
   naming it phantom: test "a coverage-table row whose id is absent from
   the inventory is red naming it phantom". PASS.
6. A parked row with an empty note is red, with a note is green, both
   directions: test "a parked row with an empty note is red, and the
   same row with a note is green". PASS.
7. Empty inventory is `error` with `units` 0, counted vacuous, never
   green: test "an empty inventory is error with units 0 and the
   vacuous flag set, never green", run through the real CLI and real
   file reads. PASS.
8. Finding-to-outcome parity, three directions (5 of 6 covered names the
   sixth; an empty outcome cell names that id; all six covered and
   non-empty is green with `units` 6, here `checked: 6`): test "a
   finding report missing an id, carrying an empty outcome, or fully
   covered, is red, red, and green respectively naming the id". PASS.
9. `node --test` exits 0 with 0 failing and zero unaccounted tests; the
   behavior registry criterion holds. **PARTIAL, with the divergence
   named rather than hidden**: this phase's OWN test file
   (`test/coverage-gate.test.ts`) exits 0 with 9 pass, 0 fail, matching
   its 9 new `test/behaviors.json` entries one-for-one (verified: every
   `test()` string in the file equals one `test/behaviors.json` value
   and every one of the 9 new behavior entries has exactly one matching
   `test()`; no extra, no missing). The FULL suite (`npm test`, 210
   tests) exits nonzero: 207 pass, 1 fail, 2 skipped (the 2 skips are
   pre-existing floor-gated doctor tests, unrelated to this phase). The 1
   failure is the cross-phase finding below, in a test this phase's
   files-to-touch list does not authorize editing. Evidence: `npm test`
   output captured in full; see the dedicated section below. Both
   toolchains checked: default Node v22.22.2 (`npm ci`/`npm run build`
   exit 0, `git status --porcelain` empty after build, `npm test` 207
   pass / 1 fail / 2 skip / 210 total) and the floor toolchain Node
   v26.6.0 (`npm ci` with no EBADENGINE line, `npm run build` exit 0,
   `git status --porcelain` empty after build, this phase's own
   `test/coverage-gate.test.ts` run directly: 9 pass, 0 fail).

**Bijection check, run rather than eyeballed:**

```
$ node -e '... compares the 9 new test/behaviors.json values (sorted) against
  every test("...") string in test/coverage-gate.test.ts (sorted) ...'
equal: true
```

## Full `npm test` output (default toolchain, Node v22.22.2), tail

```
1..210
# tests 210
# suites 0
# pass 207
# fail 1
# cancelled 0
# skipped 2
# todo 0
# duration_ms 144753.587684
```
Single failure:
```
not ok 66 - manifest-self-check reports one unit per schema document
  location: 'test/gates.test.ts:2361:1'
  Expected values to be strictly equal: 2 !== 3
```
(the cross-phase finding above). The 2 skips are pre-existing floor-gated
doctor tests (`doctor in a healthy fleet exits 0`,
`doctor with gh absent exits 0 under the generic profile`), unrelated to
this phase.

## The claim grep (CLAUDE.md, binding before submission)

Run against this file at commit `<recorded below at submission>`:

```
grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m2-p6.md
```

Output and disposition of every hit, below (populated after the grep is
actually run against the final file; see the "Claim grep, run" section at
the end of this document, which is the authoritative one).

## Cross-phase finding: a pre-existing M2-P1 test breaks on a second schema file

**Not on this phase's files-to-touch list; reported rather than fixed.**

Running the FULL suite (`npm test`, not just this phase's own test file)
on this branch surfaces exactly one failure:

```
not ok 66 - manifest-self-check reports one unit per schema document
  location: 'test/gates.test.ts:2361:1'
  Expected values to be strictly equal:
  2 !== 3
  expected: 3
  actual: 2
```

**The mechanism, not the instance.** `test/gates.test.ts`'s test
"manifest-self-check reports one unit per schema document" (M2-P1, already
merged to `main`) derives its EXPECTED unit count by listing every
`*.schema.json` file that happens to sit in the shared directory
`src/gates/schemas/`:

```
test/gates.test.ts:2378-2380
    const schemas = readdirSync(
      fileURLToPath(new URL("../src/gates/schemas", import.meta.url)),
    ).filter((name) => name.endsWith(".schema.json"));
    assert.equal(record.units, schemas.length);
```

But `manifest-self-check`'s ACTUAL validated count comes from
`schemaDocumentPaths()` in `src/gates/manifest.ts`, which returns a FIXED
list of exactly the two documents that gate owns
(`gate-manifest.schema.json`, `gate-result.schema.json`) and does not grow
when a sibling M2 phase adds an unrelated schema to the same directory.
The mechanism is: **a test deriving its expected value from the total
membership of a directory several concurrent phases are each authorized
to add a file to, rather than from the one component's own declared
list.**

**Derivation, published in full.** `delivery/plan/kernel-plan-m2.md`
section 3's files-to-touch lists put a schema file under
`src/gates/schemas/` for FIVE of the seven concurrent M2-P2..P8 phases:

```
$ grep -n 'src/gates/schemas/' delivery/plan/kernel-plan-m2.md
224:- files-to-touch: ... src/gates/schemas/gate-result.schema.json, src/gates/schemas/gate-manifest.schema.json ... (M2-P1)
272:- files-to-touch: ... src/gates/schemas/witness-spec.schema.json ... (M2-P2)
347:- files-to-touch: ... src/gates/schemas/phase-declaration.schema.json ... (M2-P4)
377:- files-to-touch: ... src/gates/schemas/citation-config.schema.json ... (M2-P5)
408:- files-to-touch: ... src/gates/schemas/coverage-config.schema.json ... (M2-P6, this phase)
454:- files-to-touch: ... src/gates/schemas/verifier-config.schema.json ... (M2-P7)
```

Confirmed directly, not merely inferred, that MY OWN addition triggers it:

```
$ node -e "console.log(require('fs').readdirSync('src/gates/schemas').filter(n=>n.endsWith('.schema.json')))"
[ 'coverage-config.schema.json', 'gate-manifest.schema.json', 'gate-result.schema.json' ]
```
Three files where the test's premise (written when the directory held
exactly `manifest-self-check`'s own two) assumed the directory's whole
membership tracks one gate's own accounting. `delivery/plan/m2-conflict-
pre-pass.md`'s disjointness check confirms the five phases' CREATED files
are pairwise disjoint (a genuinely true and necessary check), but does not
consider a pre-existing test's assumption about a shared directory's
AGGREGATE content, which is a different kind of coupling than a file-path
collision and is not caught by a disjointness check over file paths.

**What this derivation did NOT cover.** I ran the full suite once on this
branch (this phase's addition only) and found exactly this one failure;
I did not build or run a scratch merge combining two or more of
M2-P2/P4/P5/P6/P7's branches to confirm the SAME test fails identically
under a different single addition (though the mechanism read directly off
`schemaDocumentPaths()`'s fixed two-element list makes that outcome
certain for any of the other four). I did not search the rest of the
suite for a second instance of "expected count derived from a shared,
growing directory's listing" beyond the one `readdirSync` in
`test/gates.test.ts` at this exact location; a `grep -rn readdirSync
test/*.ts` was run and every OTHER hit was inspected and confirmed to be
over an evidence/scratch directory the calling test itself created (not a
long-lived shared source directory), except one further hit in the SAME
test file over `src/gates/` (not `src/gates/schemas/`) which enumerates
`.ts` gate modules for a `pid`/`process.kill` structural grep; that one
is unaffected by a new `.ts` file only if the new file itself is clean of
the forbidden patterns, which was verified directly for
`src/gates/coverage.ts`:

```
$ grep -nE "detached\s*:|\bunref\s*\(|process\.kill|/proc\b|\bpid\b|\bkill\s*\(|SIGTERM|SIGKILL|SIGINT" src/gates/coverage.ts
(no output)
$ node --test --test-name-pattern "the gate runner uses no pid" test/gates.test.ts
# pass 1, # fail 0
```

**Not fixed here.** `test/gates.test.ts` is not on this phase's
files-to-touch list (`delivery/plan/phase-declarations/m2-p6.json`), and
CLAUDE.md's scope discipline treats an undeclared edit as scope creep
regardless of intent. This is reported to the orchestrator as a
cross-phase finding: whichever of M2-P2, M2-P4, M2-P5, M2-P6 or M2-P7
lands first will reproduce this identical failure for an identical
reason, and none of the five phases' files-to-touch lists authorizes the
fix. Recommend a small out-of-band correction to
`test/gates.test.ts:2378-2380` (read the expected count from
`schemaDocumentPaths()` rather than from a directory listing), applied by
the orchestrator once, before or alongside the first of these five phases
to merge.

## Claim grep, run (CLAUDE.md, authoritative)

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m2-p6.md
```
Output (line numbers as of the commit this section names below):
```
234:   units 0 and the vacuous flag set, never green", run through the real
238:   states, so the two views agree only because one is never computed"**
248:   result buckets, never conflated. Evidence: test "a finding report
287:7. Empty inventory is `error` with `units` 0, counted vacuous, never
289:   vacuous flag set, never green", run through the real CLI and real
```
Disposition of every hit:

- Lines 234, 289: quoting this phase's own test name / criterion text
  ("... never green"), immediately followed in the same bullet by the
  PASS result and, in the hazard-review section, by the record's
  `status`/`vacuous` field values actually observed. Not a bare
  assertion.
- Line 238: restates the PLAN's own hazard-class wording ("the two views
  agree only because one is never computed"), immediately followed in
  the same numbered item by "Evidence: the real-pair test asserts BOTH
  ... in the same test run" naming the specific assertions
  (`perKind.phase === 11`, `perKind.milestone === 104`, all five
  `perMilestone` entries). Not a bare assertion.
- Line 248: "never conflated" describes `checkFindingOutcomeParity`'s
  two distinct result arrays, immediately followed by "Evidence: test
  ..." naming the exact `missing`/`empty` array contents observed in
  each direction. Not a bare assertion.
- Line 287: quotes criterion 7's own text ("counted vacuous, never
  green"), followed by "PASS" and, above, the concrete field values
  (`status: "error"`, `vacuous: true`, exit 21) the test observed. Not a
  bare assertion.

No hit in this file is an unqualified "cannot be" / "impossible" /
"always" / "no way to" claim; every occurrence of the grep's vocabulary
here is either a quotation of the plan's or this document's own test
names, or is immediately adjacent to the captured command or assertion
that settles it. Run against the commit that adds this file's final
content (recorded in this phase's own commit history on
`claude/m2-p6-coverage-checker`; the work history is committed alongside
the code it describes, per CLAUDE.md's durability rule).

