# M4-P20 work history: the double-acquire witnesses and the compare-and-swap probe

- phase: M4-P20
- branch: `claude/m4-p20-exclusion-pre-pass`
- cut from: `plan/pstack-borrow-review` at 6961186
- started: 2026-09-15
- status: IN PROGRESS (this file is written incrementally, per the dispatch
  contract's beacon rule; a partial file means the round died, not that the
  round finished)

## What this phase is

Tests and a probe only. No mechanism, no new command, no change to
`src/lock.ts`. The phase section is at delivery/plan/kernel-plan-m4.md:2911.

## Log

- read CLAUDE.md, the phase section, the conflict pre-pass and the prototype
  probes document.
