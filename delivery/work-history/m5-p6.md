# M5-P6 work history, KERNEL HALF (plan step 2, criterion p6-prepass)

This file covers ONLY the kernel half of M5-P6: shipping the conflict
pre-pass command. The hemma half (plan steps 1, 3 and 4; criteria
p6-charter-only, p6-parallel-value, p6-attribution) runs in a later wave and
will append below.

Branch claude/m5-p6-scale-out-proof, cut from origin/main at e81c5e8.
Declaration: delivery/plan/phase-declarations/m5-p6.json:1.

## Log

- Branch created from origin/main e81c5e8; this beacon committed first.
- c86439f: this beacon.
- b04df95: src/commands/conflicts.ts, the `conflicts` row in src/cli.ts,
  test/conflicts.test.ts (12 tests, all through the real CLI).
- 567b790: 12 behaviors appended to test/behaviors.json, 9 witness specs under
  witness/, and those 9 witness paths ADDED to this phase's declaration.

## Design decisions (kernel half)

1. **Input.** Two or more phase declarations, positional, in the format the
   scope auditor reads (src/gates/schemas/phase-declaration.schema.json:1). A
   phase's edit set is filesToTouch plus declaredExtras. This is the input the
   two hand pre-passes used (delivery/plan/m2-conflict-pre-pass.md:1 and the
   m5 one on the plan-readiness branch, quoted because it is not on main:
   `delivery/plan/m5-conflict-pre-pass.md`).
2. **Overlap grammar** is the scope auditor's own entry grammar: equal
   strings, or a trailing-slash directory containing the other entry
   (src/gates/scope.ts:517). A directory-contains overlap is named with both
   spellings, `witness/ and witness/x.json (directory contains path)`.
3. **Append-only registries.** Default list is CLAUDE.md convention 5's three
   (test/behaviors.json, gates.manifest.json,
   delivery/requirements/clause-map.json). A shared FILE on the list is an
   `APPEND-ONLY` line, not an `OVERLAP`, and the effective list is printed on
   every verdict run. A shared DIRECTORY is never exempt (src/commands/conflicts.ts:223). `--append-only
   <path>` (repeatable) REPLACES the default, so another project states its
   own registries rather than inheriting this repository's names.
4. **Semantic coupling** is printed as the LAST stdout line of every run:
   overlap, disjoint, input error and usage error alike.
5. **Exit codes:** 0 disjoint, 1 overlap, 2 a declaration could not be used
   (missing, not a regular file, not JSON, wrong shape, duplicated phase id),
   64 usage. Exit 2 prints `NO VERDICT` and no DISJOINT/OVERLAP line.
6. **Duplicate phase id** is exit 2: the same phase given twice would
   otherwise report a total overlap with itself, which is a wrong answer
   rather than a conservative one.

## Deviations

- **Declaration amended (additive only).** The red-witness gate reddens a
  changed src/ file with no covering witness spec (src/gates/red-witness.ts:363),
  and the plan's files-to-touch did not list any witness spec. Nine literal
  witness/ paths were ADDED to delivery/plan/phase-declarations/m5-p6.json:1.
  Literal paths rather than `witness/` so that this phase's edit set does not
  acquire a directory entry that would overlap every other phase adding a
  witness spec. Nothing was removed.
- The m5 pre-pass this command mechanises is not on main (it is on
  `claude/m5-plan-readiness-y2jjo5`, commit ffa5f56). Read from there with
  git show; nothing depends on it landing.

## Evidence (kernel half)

All commands below ran on node v26.6.0 (the scratch toolchain first on PATH,
`node --version` checked in the same shell), in this phase's worktree.

Transliteration note: node's test reporter prints U+2139 at the head of its
summary lines. In the captures below each U+2139 is rendered `i`: 4 in the
`npm test` summary quoted under gates 1 to 3, and 12 in the three-run
lock-test capture (4 per run), 16 in total. Nothing else in any capture was
changed.

### Gates 1 to 3

- `npm ci`: exit 0.
- `npm run build`: exit 0; `git status --short` afterwards printed nothing.
- `npm test` (invocation `node --test "test/**/*.test.ts"`, dist/ built,
  node v26.6.0): exit 0, `i tests 1406`, `i pass 1406`, `i fail 0`,
  `i skipped 0`.

### Criterion p6-prepass, walked

Real CLI against this repository's six m5 declarations (before the witness
paths were added to m5-p6.json; the added entries are all under witness/ and
no other m5 declaration names witness/):

```
$ node bin/tiphys.ts conflicts <m5-p1.json ... m5-p6.json, six paths listed explicitly>
OVERLAP M5-P1 M5-P5 delivery/STATE.md
OVERLAP M5-P1 M5-P6 delivery/STATE.md
OVERLAP M5-P2 M5-P3 roles/clean-room-reviewer.md
OVERLAP M5-P3 M5-P4 test/gate-registry.test.ts
OVERLAP M5-P3 M5-P5 test/clean-room-brief.test.ts
OVERLAP M5-P5 M5-P6 delivery/STATE.md
conflicts: 6 overlapping pair(s), 9 disjoint pair(s), 6 overlapping path(s)
semantic coupling: NOT CHECKED. Literal file overlap is the only thing ...
exit=1
```

(OVERLAP lines and the last two lines shown; DISJOINT and APPEND-ONLY lines
omitted here, 9 and 10 of them.) The six OVERLAP lines are exactly the six
pairs the hand pre-pass on the plan-readiness branch derived.

- Overlapping fixture pair: exit 1, names three paths including a
  directory-contains-path overlap: test "conflicts exits 1 and names every
  overlapping path for an overlapping pair", test/conflicts.test.ts:92.
- Disjoint fixture pair: exit 0, zero OVERLAP lines, `0 overlapping path(s)`,
  obligation as the last line: test/conflicts.test.ts:122.
- Obligation on every run: asserted as the LAST stdout line in 11 of the 12
  tests, covering exit 0, 1, 2 and 64 (the registration test is the one
  without it; it asserts the help line and an unknown-option 64).
- Real m5-p2 against m5-p3 names roles/clean-room-reviewer.md:
  test/conflicts.test.ts:255.

### Red witnesses

The PR bundle's red-witness gate evaluated all 9 own specs (plus 10 stored
specs whose members touch src/cli.ts). Per own spec, red repetitions per
member against the dangerous state, from the gate's witness-records.json:

| spec | members red |
|---|---|
| conflicts-overlap-named-and-nonzero | 2/2 2/2 2/2 |
| conflicts-disjoint-still-prints-obligation | 2/2 2/2 |
| conflicts-unusable-declaration-is-no-verdict (4 tests) | 2/2 2/2 2/2 |
| conflicts-usage-error-prints-obligation | 2/2 2/2 |
| conflicts-append-only-rule-is-explicit | 2/2 2/2 |
| conflicts-append-only-override | 2/2 |
| conflicts-duplicate-phase-id-refused | 2/2 |
| conflicts-command-registered | 2/2 2/2 |
| conflicts-real-m5-declarations-overlap | 2/2 2/2 |

Gate line: `red-witness: green: 19 witness(es) evaluated (9 own, 10 stored
re-evaluated in 149785ms); every witness red against every declared dangerous
state and green at head`. `uncoveredSources` was `[]`.

The members are the dangerous states, not the absent feature: an overlap
exit forced to 0; exact-match or directory-prefix detection disabled; the
obligation dropped from every path, or bypassed on the disjoint exit only;
an unusable declaration skipped instead of counted, or the input-error exit
turned into 0; the append-only exemption disabled or its line not printed.

### Behaviors

12 names appended; each resolves to a literal test title in
test/conflicts.test.ts (node check: `checked 12 missing 0`).

### PR gate bundle

`bash scripts/m2-exit-test.sh ...` was REFUSED by this agent's worktree
sandbox ("runs bash inside a construct too complex to verify"), so the
bundle's core step was run directly, the same command the script runs at
scripts/m2-exit-test.sh:1349:

```
node dist/bin/tiphys.js gates run --manifest gates.manifest.json \
  --evidence <scratch>/pr-bundle --base origin/main --head HEAD --phase m5-p6
gates: declared 15 applicable 10 verdict 10 green 9 red 1 not-applicable 5 error 0 vacuous 0
gates: suite: red: 1 finding(s): failing test: "an initial acquire never publishes a half-written lease" (test/lock.test.ts)
bundle exit=1
```

(Output abbreviated to the three lines shown.) The one red is test/lock.test.ts:744, a cross-process timing test over
src/lock code this phase does not touch. Load average at the time was 20.70
on 4 CPUs (`uptime`), from sibling agents. The same test passed in the full
`npm test` run above and passed 3 of 3 when rerun alone immediately after:

```
run 1 exit=0   i tests 1 i pass 1 i fail 0 i skipped 0
run 2 exit=0   i tests 1 i pass 1 i fail 0 i skipped 0
run 3 exit=0   i tests 1 i pass 1 i fail 0 i skipped 0
```

This is recorded as a load-sensitive flake, not as a green bundle. The suite
gate was rerun alone; its result is appended below.

Merge preview: `git merge-tree --write-tree origin/main HEAD` exit 0, tree
b02affb, origin/main still e81c5e8.

## Hazard self-review

- **false-disjointness** (zero overlap read as zero coupling). Attacked:
  could a disjoint result omit the obligation? The obligation is printed by
  the single `finish` exit helper (src/commands/conflicts.ts:245) and every
  return in `cmdConflicts` goes through it; two witness members remove it or
  bypass it on the disjoint exit and both redden. Could an input error read
  as disjoint? Witnessed by four structurally different bad inputs (absent,
  a directory, YAML-not-JSON, JSON missing filesToTouch) plus a duplicate id,
  all exit 2 with NO VERDICT and no DISJOINT line. Could a directory entry
  hide an overlap? Directory-prefix detection is its own witnessed member,
  and a shared directory is not exempt even when it contains an append-only
  file: `appendOnly` is computed on the concrete path and is false whenever
  that path is a directory (src/commands/conflicts.ts:223).
- Found nothing further. Not attacked: paths that are equal only after
  normalisation (`./a` versus `a`, case folding). The command compares
  literal strings, as the scope auditor does; a declaration writing the same
  file two ways would be reported disjoint. Left as an open question for the
  reviewer rather than claimed safe.
- hidden-bootstrap-handwork and self-hosted-success belong to the hemma half
  and are not exercised here.

### Claim grep

Both forms run over this file. Hits: `never exempt` (line 36, settled by
src/commands/conflicts.ts:223), `never publishes` (inside the captured gate
output, a test title), `whenever` (the hazard paragraph, carrying the same
citation). Wrap-insensitive form: 3 occurrences of `never`, the same three.

### Suite gate rerun, alone

Both the bundle run and this rerun were at head 64b6d1f (the scope line
above names it); the only later commit, 726310e, changes this file alone.

```
node dist/bin/tiphys.js gates run --manifest gates.manifest.json \
  --evidence <scratch>/suite-rerun --base origin/main --head HEAD --phase m5-p6 --only suite
gates: declared 1 applicable 1 verdict 1 green 1 red 0 not-applicable 0 error 0 vacuous 0
gates: suite: green: suite green via tiphys-suite-events-v1 (child node v26.6.0): reported 1406 test(s) from 69 file(s) (pass 1406, fail 0, skipped 0, todo 0, did-not-run 0); ...
suite-rerun exit=0
```

Load average during it was 23.87 (`uptime`). So on this head every gate the
PR bundle declares is green or structurally not-applicable, with the one
suite red attributed to a lock-test flake by the rerun above, not by
argument. CI's pull_request run remains the authority.

# M5-P6 work history, HEMMA HALF, plan step 1 (verification-first intake)

Branch claude/m5-p6-scale-out-proof, continued from 4b44c5a. Evidence
document: delivery/verification/m5-hemma-intake.md:1, created as the beacon in
the first commit of this step and appended per section.

## Log

- e7ee271 and earlier: beacon, method and stack.
- 2331756: gates, CI runs and protection.
- d34964e: bootstrap-input inventory.
- 9ed8971: suitability, candidate pair, conflicts run, blockers, questions.
- Final commit: claim-grep restatements and this entry.

## What was done, and the no-mutation rule

Hemma at `/home/user/hemma` (HEAD a6141d7) was copied once with `cp -a` into
scratch. Every command ran against the copy. No write, branch, push or GitHub
write call was made against hemma. Post-run check: `ls /home/user/hemma/node_modules`
reports "No such file or directory", and `find /home/user/hemma -newer
<npm-ci.log> -not -path '*/.git/*'` printed nothing.

Local runs on node v22.22.2 (CI pins 24, not installed here): `npm ci`, prisma
generate, `next typegen`, `tsc --noEmit`, `npm run lint` and
`vitest run --project unit`, all exit 0. Unit counts: 8540 passed, 14 skipped,
7 todo, 3 test files skipped. GitHub MCP reads: CI push run 35845441652 on
a6141d7 green; `main` `"protected": false`; one collaborator. REST through the
proxy returned 404 on all nine repository probes, including the repository
record, so rulesets are UNKNOWN rather than absent.

## Findings for the orchestrator

1. **Verdict: hemma FITS the DR-0029 envelope with named conditions K-1 to K-5.**
2. **p6-charter-only is not met by the kernel as it stands.** `tiphys init`
   reads no charter (src/commands/init.ts:95). Four kernel-config inputs
   (`assurance-modes.yaml`, `schemas/`, the kernel `gate-registry.yaml`, a
   `.ctx-*/` ignore line) are placed by hand in the only onboarded precedent,
   pulse-fleet. Closing that needs src/commands/init.ts, which is not on this
   phase's files-to-touch. Recorded as a scope decision (intake Q-8), not
   bent around.
3. **Kernel vocabulary is imposed on the project.** `tiphys conflicts` refused
   declarations with ids `H-P1`/`H-P2`, exit 2, because the schema pattern
   is `^M[0-9]+-P[0-9]+$` (src/gates/schemas/phase-declaration.schema.json:13).
4. **The conflicts command's default append-only list is this repository's**
   (src/commands/conflicts.ts:66). It is harmless for hemma and misleading;
   step 3 should pass `--append-only`.
5. **Candidate pair**: M1-P1 site-shed frozen clock and M1-P2 calendar-oauth
   frozen clock, test-only. `conflicts` reports DISJOINT, exit 0. A control
   pair sharing `tests/setup.ts` reports OVERLAP, exit 1.

## Deviations

- Node 22 used for hemma's gates instead of CI's Node 24 (not installed).
- `npm run build`, Playwright and `test:full` not run (no Postgres).
- A read-only clone of `ThomasHendrickx/pulse` was refused by the session's
  permission layer. The clone of `pulse-fleet` succeeded and was only listed.
  So the pulse precedent is half observed.

## Claim grep

The binding line-based grep and the wrap-insensitive form were run over the
intake document and over this entry. Every non-captured hit in the intake was
restated or given its settling command in the final commit: the no-mutation
sentence now points at the post-run check; golden-path's "never block" became
the `grep -n pull_request` result; init's "never run in hemma" became "refuses
a non-empty directory" with src/commands/init.ts:113; "never invokes tiphys"
became `grep -c tiphys` = 0 on both workflows; "never executed" became "read
with ls and cat only"; K-1's "cannot be met" became "is not met ... on the
evidence of section 4d". The remaining hits are the literal `never an overlap`
inside two captured `conflicts` outputs, and "needs a plan amendment", which
sits next to the files-to-touch citation that settles it.

# M5-P6 work history, KERNEL HALF 2: init closes the charter-only gap (DR-0058)

Branch head at start: 6e8ad24 (orchestrator's merge of origin/main plus the
DR-0058 amendment). Decision: delivery/decisions/DR-0058-m5-p6-closes-the-charter-only-gap-in-init.md:1.
This section is the beacon for this round and is appended as work proceeds.

## Log

- Beacon committed first.
- Derivation and two measurements recorded (this block), before any code.

## Derivation: every kernel file a fleet home or project consumes

The mechanism is "a command reads a kernel-owned file from a directory the
operator made, rather than from the kernel's own install". The derivation has
two halves, a static search and a measurement.

### Half 1, static. The command

```
node <scratch>/init-round/derive.mjs "$PWD"
```

The script (kept in scratch, reproduced here in outline) reads package.json
`files`, drops `dist` and `LICENSE`, adds the context documents `charter`,
`charter.yaml`, `plan.yaml`, `work-history.yaml` and `mechanism-index.yaml`,
and prints every non-comment line of every `src/**/*.ts` file carrying one of
those names as a string literal. Output summary: `files scanned: 69; hits: 68`.
Names searched: `AGENTS.md, gate-registry.yaml, gates.manifest.json,
assurance-modes.yaml, checklists, role-model-config.yaml, roles, schemas,
templates, tuition, charter.yaml, plan.yaml, work-history.yaml,
mechanism-index.yaml, charter`.

The 68 hits sort into two classes by HOW the path is resolved:

1. **Resolved from the running kernel's own install** (walk up from the module
   to the package root). Not consumed from any operator directory:
   src/modes.ts:26 `assurance-modes.yaml` via `packageRoot()`;
   src/commands/validate.ts:223 `schemas/` via `schemasDirectory()`;
   src/cutover.ts:1396 `schemas/`; src/roles.ts:88 to src/roles.ts:93 and
   src/roles.ts:378 `roles/`, `AGENTS.md`; src/checklists.ts:44 `checklists/`;
   src/commands/doctor.ts:970 to src/commands/doctor.ts:974 (the
   `kernel-artifacts` check, which inspects the INSTALL, not the fleet);
   src/commands/plan.ts:36 `templates/plan.example.yaml`.
2. **Resolved against a CONTEXT directory the operator supplies** (the reads a
   hand-placed file could be feeding). A second, narrower command enumerates
   them:

   ```
   grep -nE "readContextDocument(At)?\(|contextDocumentPresentAt\(|join\(contextDirectory|classifyEntry\(join\(context" src/checks.ts
   ```

   Relevant hits, by the file they read and who owns it:

   | read | line | owner |
   |---|---|---|
   | `gate-registry.yaml` beside an `assurance-modes` document (`mode-gate-sets-resolve`) | src/checks.ts:668 | KERNEL pairing: assurance-modes names the kernel's gate ids |
   | `schemas/charter.schema.json` beside an `assurance-modes` document (`charter-mode-enum-matches-modes`) | src/checks.ts:746 | KERNEL |
   | `gate-registry.yaml` for `gate-probes-resolve` | src/checks.ts:2110 | whoever owns that registry |
   | `plan.yaml`, `work-history.yaml`, review dir, tuition targets, decision dirs | src/checks.ts:2238, src/checks.ts:2383, src/checks.ts:3605, src/checks.ts:2652, src/checks.ts:1573 | PROJECT |
   | `charter.yaml` and `assurance-modes.yaml`, the merge-authority regime, read FROM THE COMMIT | src/checks.ts:4750, src/checks.ts:4799, src/checks.ts:4907 | charter PROJECT, assurance-modes KERNEL |

   The regime's context directory is the merge gate's `--context`, default
   `process.cwd()` (src/gates/merge-preconditions.ts:1369), which is the
   PROJECT repository the gates run in, not the fleet home.

### Half 2, measured. What a fleet home made by init actually needs

`<scratch>/init-round/probe-fleet.sh` runs this branch's `tiphys init` on
node v26.6.0, writes `charter/probe.yaml` from templates/charter.example.yaml,
and runs the fleet-scoped commands in that home with NO `assurance-modes.yaml`,
NO `schemas/`, NO `gate-registry.yaml` and NO `.ctx-*/` line:

| command | exit | relevant line |
|---|---|---|
| `validate --type charter charter/probe.yaml` | 0 | |
| `validate --type charter --context . charter/probe.yaml` | 0 | |
| `doctor` | 1 | only FAIL: `retention` (the template charter's `delivery/work-history/` does not exist, a PROJECT fact); `kernel-artifacts PASS the kernel install at ... carries roles/, schemas/, checklists/ and AGENTS.md` |
| `mode show --mode full` | 0 | read from the install |
| `next` | 0 | |

So **the fleet home consumes none of I-7, I-8, I-9 or I-10.** The four
hand-placed files in pulse-fleet were not needed by any fleet-scoped command
measured here. The file that IS consumed, and that nothing produces, is
`assurance-modes.yaml` in the PROJECT repository, read from the commit by the
merge-authority regime (src/checks.ts:4907).

### What the derivation did NOT cover

- `scripts/` and `.claude/` were not searched: they are this repository's own
  harness, not shipped (package.json `files` does not list them). The dual
  review gate script is shipped behaviour only through src/checks.ts, which
  was searched.
- Paths built from variables rather than literals (for example a
  `join(root, name)` where `name` comes from an array) are found only where
  the array itself holds a literal; the doctor and roles arrays do. A path
  assembled from configuration at run time would be missed.
- The measurement ran five fleet commands. `spawn`, `pool`, `watch`,
  `teardown`, `sync`, `cutover` and `brief compose` were not run against the
  probe fleet; they were covered only by the static half, where their reads
  resolve through the install or through task state.
- `.ctx-*`: `grep -rn '\.ctx-' src scripts roles AGENTS.md .claude/skills`
  printed nothing, so no kernel code creates such a directory. The pulse-fleet
  line serves a process outside the kernel.

## Measurement: link versus copy under a commit-sourced read

`<scratch>/init-round/symlink-probe.sh` commits a `charter.yaml` and an
`assurance-modes.yaml` into two scratch repositories, one as a symlink to the
kernel's file, one as a copy:

```
== arm link: git cat-file -t HEAD:./assurance-modes.yaml -> blob
== arm link: first line of git show HEAD:./assurance-modes.yaml -> /home/user/tiphys-ai-helmsman/.claude/worktrees/agent-a74ea3935b8e96ac1/assurance-modes.yaml
== arm link: git ls-files -s -> 120000
== arm copy: git cat-file -t HEAD:./assurance-modes.yaml -> blob
== arm copy: first line of git show HEAD:./assurance-modes.yaml -> # THE ASSURANCE MODE DEFINITIONS (kernel plan M3, M3-P3; R-024, R-096).
== arm copy: git ls-files -s -> 100644
```

A committed link PASSES the presence probe (`blob`, the test at
src/checks.ts:3500) and then yields its TARGET PATH as the document body. The
regime would decode a path string where it expects the modes document. That is
the silent shape: present, and wrong.

## Design decision: copy, into the PROJECT repository, via `init --project`

- **Copy, not link.** The regime is read from the commit (src/checks.ts:3467).
  A link commits its target text, measured above. A link into
  `node_modules/@tiphys/kernel/` also dangles until an install, and hemma does
  not depend on the kernel package at all.
- **Copy, not resolution through the installed package.** The merge decision is
  deliberately made from the commit being merged (the DV-001 fix recorded at
  src/checks.ts:4870). Resolving `assurance-modes.yaml` from whatever kernel the
  operator has installed would make that decision depend on the operator's
  machine rather than on the reviewed commit. It would also need a change to
  src/checks.ts, which this phase's declaration does not grant.
- **Where.** The consumer is the project repository, so the producer is a new
  arm, `tiphys init --project <repo>`, which writes into an EXISTING git work
  tree and refuses to overwrite. The fleet-home arm is unchanged in what it
  writes, because the measurement shows the fleet home needs none of the four
  items.
- **The copy is pinned.** It is byte-identical to the initializing kernel's
  file. A differing copy is refused, not overwritten, so an upgrade is a
  deliberate act. A symlink at that path is refused by name.
- **I-8 `schemas/` is not copied.** Its only context read is
  src/checks.ts:746, which runs only when an `assurance-modes` DOCUMENT is
  validated with `--context`; the regime read does not use it.
- **I-9 is a project-owned template,** templates/gate-registry.example.yaml.
  Init does not write it: a registry init wrote would be predicates the project
  did not choose, which is the pulse-fleet substitution again. Init REPORTS
  whether the project has one and names the template path in the installed
  kernel.
- **I-10 is not produced.** No kernel code creates `.ctx-*` (grep above).
- **Q-4, the charter.** Two locations, two readers, both from src/:
  `<fleet>/charter/*.yaml` for doctor and brief compose (src/charter.ts:38,
  src/commands/doctor.ts:735), and `<project>/charter.yaml` for the merge
  regime, read from the commit (src/charter.ts:41, src/checks.ts:4750).
  `locateCharters` reports both (src/charter.ts:162). Init writes neither
  (the charter is the project's) and reports both paths.

## Implementation log (kernel half 2)

Commits on `claude/m5-p6-scale-out-proof`, in order: `d9094ad` (init
`--project`, the template, the tests), `45a7ea2` (witnesses, behaviors,
declaration grants), `5183a0c` (the captured git contract and its live
reproduction test, and `consumesExternalOutput` on every witness over
src/commands/init.ts).

What `tiphys init` now produces, by arm:

- **`tiphys init <dir>` (fleet home).** Unchanged in what it WRITES, because
  the measurement above shows the fleet home consumes none of I-7 to I-10. It
  now PRINTS the two remaining steps (src/commands/init.ts:238): where the
  fleet charter goes, `<dir>/charter/<project>.yaml`, and that the project
  repository needs `tiphys init --project <repo>`.
- **`tiphys init --project <repo>` (new, src/commands/init.ts:277).** Accepts
  only the top level of a git work tree, decided from
  `git -C <repo> rev-parse --show-toplevel` (src/commands/init.ts:299): exit 0
  AND the trimmed stdout equal to the real path of `<repo>`. It then writes
  `assurance-modes.yaml` as a byte copy of the running kernel's file
  (src/commands/init.ts:333) when absent; reports it present when identical;
  refuses a symbolic link (src/commands/init.ts:337), a non-regular file, and a
  differing copy (src/commands/init.ts:347), all without writing. It reports,
  and does not write, the project's `charter.yaml` and `gate-registry.yaml`,
  naming the shipped template path in the installed kernel when the registry
  is absent (the constant is at src/commands/init.ts:101).
- **templates/gate-registry.example.yaml (new, I-9).** Project-owned: three
  placeholder gates (`unit-tests`, `typecheck`, `lint`) whose commands name
  adapter scripts under `scripts/tiphys-gates/` in the PROJECT, the gate
  subprocess contract in the header, `destructiveCommands: []`. It validates
  with `tiphys validate --type gate-registry` (exit 0, asserted by the test
  below).

One mid-round correction, recorded because it changed a test: the first draft
of the template test also required that no template gate ID equal a kernel gate
ID. It went red on `typecheck`, which is a correct project gate name, so that
assertion tested the wrong property. The test now reads the kernel's gate
SCRIPTS from the kernel registry at run time and requires that no template
command names one (test/init.test.ts:403), which is the pulse-fleet substitution
itself.

## Tests, behaviors and witnesses

Nine tests added to test/init.test.ts, each registered in test/behaviors.json
under the id shown and witnessed by witness/<id>.json (class additive, two or
three mutation members each, every member inside a changed hunk):

| id | test (line) | dangerous states (mutations) |
|---|---|---|
| init-project-copies-modes | test/init.test.ts:300 | copy truncated by one byte; the wrong kernel file copied |
| init-project-satisfies-regime | test/init.test.ts:320 | the write removed (charter-only project); the file written under `.git/`, present on disk and absent from the commit |
| init-project-refuses-symlink | test/init.test.ts:347 | the link followed through `realpathSync`; the symlink branch disabled |
| init-project-refuses-differing-copy | test/init.test.ts:357 | the comparison disabled; the refusal overwriting before it returns |
| init-project-reports-project-documents | test/init.test.ts:368 | init writing the registry from the template; the template path wrong; the path not printed |
| init-project-refuses-non-repo | test/init.test.ts:383 | the top-level check disabled; the stdout comparison dropped, so a subdirectory passes |
| gate-registry-template-project-owned | test/init.test.ts:403 | a gate running the kernel's `src/gates/suite.ts`; one running the kernel's `scripts/license-gate.mjs`; an invalid `kind` |
| init-prints-next-steps | test/init.test.ts:434 | the charter path printed at the fleet root; the project-arm line replaced |
| init-project-rev-parse-contract-reproduced | test/init.test.ts:445 | the capture's outside-repository exit changed to 0; its subdirectory stdout changed to the subdirectory |

The test at test/init.test.ts:383 gained its SUBDIRECTORY case in this round,
because the first draft's only case (a plain directory) stayed green with the
stdout comparison removed, so the second member of its witness had nothing to
redden. That is the one-witness-is-not-a-class rule applied to the test rather
than the witness.

The test at test/init.test.ts:368 carries "never writes them" in its NAME; the
settling evidence is its own two `existsSync(...) === false` assertions, and its
witness's first member (init writing the registry) is red against it.

Rule (f) of the red-witness gate bound every witness over src/commands/init.ts,
because that file now spawns git and parses the result. The first gate run was
red for exactly that reason on seven witnesses. The capture is
witness/captures/init-project-rev-parse-toplevel.txt, a real run against git
2.43.0 with the scratch path replaced by `<scratch>` (declared in its header,
along with one trailing space dropped from an empty `stdout:` line). Its one
load-bearing fact: a subdirectory exits 0 and prints the ROOT, so the exit
status alone accepts it.

Red-witness gate, Node v26.6.0, `--phase m5-p6 --base origin/main --head HEAD`
at `5183a0c`:

```
gates: declared 1 applicable 1 verdict 1 green 1 red 0 not-applicable 0 error 0 vacuous 0
gates: red-witness: green: 31 witness(es) evaluated (18 own, 13 stored re-evaluated in 103638ms); every witness red against every declared dangerous state and green at head
```

The 18 own are the nine conflicts witnesses from kernel half 1 and the nine
above.

Declaration grants added to delivery/plan/phase-declarations/m5-p6.json
`filesToTouch` as each file was written: the nine witness files and the capture.

## Verification before push

All on Node v26.6.0 (npm 11.18.0) at the scratch prefix, in this worktree
under `/home/user`, at head `5183a0c`:

- `npm run build`: exit 0; `git status --short` afterwards printed nothing.
- **Suite: bare `node --test` from the repository root, `dist/` built: exit 0,
  1510 tests, 1510 pass, 0 fail, 0 cancelled, 0 skipped.** These are the
  reporter's summary counts restated in prose, not a pasted capture.
- `node --test test/init.test.ts`: 18 tests, 18 pass, 0 fail, 0 skipped.
- `node scripts/check-authored-bytes.mjs`: exit 0.
- scope gate, `--phase m5-p6`: green, 31 changed paths audited; 25 entries
  added at head and NAMED by the gate for a reviewer to sign off (the DR-0058
  amendment's grants plus this round's witness and capture files); the two
  declared paths not yet touched are delivery/STATE.md and
  delivery/verification/m5-scale-out-exit.md.
- citations gate and the claim grep: recorded in the next block, after this
  block is committed, because the citations gate lints HEAD.

## Open questions from kernel half 2

1. **Upgrade drift of the project copy.** A differing `assurance-modes.yaml`
   is refused and there is no refresh command. A kernel upgrade that changes the
   modes document leaves every project holding the old copy until someone
   replaces it by hand. Whether a `--refresh` arm is wanted, and who decides
   when, is open.
2. **A project registry beside the kernel's modes document.**
   `mode-gate-sets-resolve` (src/checks.ts:668) pairs `assurance-modes.yaml`
   with a `gate-registry.yaml` in the same context, and the modes document's
   gate-sets name KERNEL gate ids (DR-0020). A project registry started from the
   template does not carry those ids. MEASURED with
   `<scratch>/init-round/q2.sh` (Node v26.6.0): in a fresh repository after
   `init --project`, `tiphys validate --type assurance-modes --context .
   assurance-modes.yaml` exits 1 in both arms. With no project registry it
   reports three failures: `schemas/charter.schema.json` absent
   (`charter-mode-enum-matches-modes`), `gate-registry.yaml` absent
   (`mode-gate-sets-resolve`), and DR-0012 not found
   (`mode-conditions-quote-granted-by`). With the template copied in as
   `gate-registry.yaml` the second becomes one failure per kernel gate id per
   mode ("gate set suite is not declared in gate-registry.yaml", and so on).
   The template itself validates (exit 0) in the same directory.
   The MERGE REGIME does not run those checks: it reads the copy only to look up
   the charter's mode and its `merge-authority` (src/checks.ts:4799 to
   src/checks.ts:4840). So the project copy is fit for the reader that needs
   it, and validating it WITH a project context is red by construction. Whether
   the modes document's context checks should be scoped to the kernel's own
   tree, or the gate-sets should become project-overridable, is a decision
   outside this declaration.
3. **No kernel adapter from a plain command to a GateResult.** Each project
   writes its own `scripts/tiphys-gates/*.mjs`. A shipped adapter would remove
   that step; it is outside this declaration.
4. **Kernel contract gates from a project.** How a project registry names the
   kernel's own contract gates (scope, red-witness) by path through the
   installed package is unresolved.
5. The fleet measurement ran five commands (listed above); the rest were covered
   by the static half only. Pulse's project side is unobserved. Intake Q-5 (which
   kernel version hemma pins; the kernel is now 0.2.1) is unchanged.

## Citations gate and claim grep (kernel half 2)

Citations gate, Node v26.6.0, `--phase m5-p6 --base origin/main --head HEAD`
at `27d5517` (the commit carrying the block above):

```
gates: citations: green: linted 2 changed document(s) at 27d5517e25f649a958f6caf85745eb4e7fe83665: 33 citation(s) resolved, 0 self-citation(s), 0 unverifiable-external
```

Claim grep over this round's additions (lines 298 to 607 of this file, copied
to `<scratch>/init-round/half2.md` so the line numbers below are relative to
line 298):

```
grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' half2.md
227:The test at test/init.test.ts:368 carries "never writes them" in its NAME; the
```

The wrap-insensitive form (`tr '\n' ' ' < half2.md | grep -oEi ...`) printed one
occurrence, `never`, the same one, so the line-based run missed nothing. That
hit is a quoted test name, and the sentence it sits in names its settling
evidence (the test's two `existsSync` assertions and the witness member that
reddens it).

# M5-P6 work history, KERNEL HALF FIX ROUND 1

Reviews read in full before any change: the hazard review (CR-KH-001 to
CR-KH-005, FIX-ROUND-NEEDED) and the criteria review (CR-001 to CR-005,
FIX-ROUND-NEEDED, documentation only), both at head `520ba00`. They are held in
the orchestrator's scratch and are committed by the orchestrator, not here.

- Round opened (this block), before any change.

## CR-KH-001 (high): a path compared before it is canonical

**Mechanism.** `tiphys conflicts` decides overlap by comparing declared path
STRINGS (equality, a directory prefix, a set key). Two spellings of one file
are different strings, so any comparison made before the path is canonical can
read one file as two, and the verdict is DISJOINT. The finding named two
spellings (`./src/a.ts`, `src//foo.ts`); the mechanism covers every input string
that reaches a comparison.

**Derivation: every place in src/commands/conflicts.ts where a value is
compared or keyed.** Command, run at `e7422bc` (after the fix, so the new guard
lines appear too):

```
grep -nE '===|!==|startsWith|endsWith|new Set|\.has\(|\.find\(|includes\(|indexOf|\.sort\(|< y\.|Map' src/commands/conflicts.ts
100:    if (token === "--append-only") {
102:      if (value === undefined || value.startsWith("--") || value === "") {
109:      if (why !== undefined) {
118:    if (token.startsWith("--")) {
140:  if (cachedSchema !== undefined) {
145:  if (read.kind !== "read") {
147:      read.kind === "absent"
182:  if (entry === "") {
185:  if (entry.startsWith("/")) {
188:  if (entry.includes("\\")) {
191:  const body = entry.endsWith("/") ? entry.slice(0, -1) : entry;
193:  if (segments.some((segment) => segment === "")) {
196:  if (segments.some((segment) => segment === "." || segment === "..")) {
233:      if (why !== undefined) {
244:  const entries = [...new Set([...document.filesToTouch, ...document.declaredExtras])];
259:  return entry.endsWith("/");
273:  const exempt = new Set(appendOnly);
278:      if (a === b) {
280:      } else if (isDirectory(a) && b.startsWith(a)) {
282:      } else if (isDirectory(b) && a.startsWith(b)) {
285:      if (concrete === undefined) {
291:        appendOnly: !isDirectory(concrete) && exempt.has(concrete),
295:  found.sort((x, y) =>
296:    x.left === y.left ? (x.right < y.right ? -1 : 1) : x.left < y.left ? -1 : 1,
302:  if (shared.left === shared.right) {
318:  if (parsed.args === undefined) {
334:    const clash = declarations.find((known) => known.id === read.declaration.id);
335:    if (clash !== undefined) {
```

Classified:

- PATH comparisons or keys (the mechanism): 244 (dedupe key over entries), 259
  (directory marker), 273 and 291 (the append-only exemption set, keyed by the
  `--append-only` values), 278, 280, 282 (overlap), 296 (sort order only), 302
  (display).
- Not paths: 100, 102, 118 (option tokens), 140, 145, 147 (schema cache and read
  kind), 285 (undefined test), 318, 334, 335 (phase ids, which the schema
  pattern already constrains), 109, 233 (the new guard's own test), 182 to 196
  (the new canonical check itself).

Every path site is fed by exactly two INPUTS: the declared entries
(`filesToTouch`, `declaredExtras`, read at src/commands/conflicts.ts:232) and
the `--append-only` values (src/commands/conflicts.ts:108). The fix guards both
inputs, so every site downstream compares canonical strings only.

**Decision: REFUSE, do not normalise.** A non-canonical declared entry gives
exit 2 and NO VERDICT, naming the declaration path, the phase id, the field and
the entry (src/commands/conflicts.ts:181 defines canonical). A non-canonical
`--append-only` value is a usage error, exit 64. Reason: the scope gate matches
declared entries against git's own paths, which are canonical, so an entry like
`./src/a.ts` is one the scope gate would not match. The declaration is
defective, and normalising it here would produce a verdict about a declaration
the phase does not actually have. Refusal surfaces the defect where it was
written.

Canonical, as implemented: non-empty; no leading `/`; no backslash; no empty
segment except the single trailing `/` of a directory entry; no `.` or `..`
segment. Real-input check, all 57 declarations in
delivery/plan/phase-declarations at this branch
(`<scratch>/init-round/canon-scan.ts`):

```
declarations: 57; entries: 769; non-canonical: 0
```

So the refusal changes no verdict on any real declaration.

**Tests and witnesses.** test/conflicts.test.ts:280 runs seven spellings
(`./src/a.ts`, `src//a.ts`, `src/./a.ts`, `src/x/../a.ts`, `/src/a.ts`,
`src\a.ts`, `src//`) in BOTH fields against a canonical `src/a.ts` plus `src/`.
test/conflicts.test.ts:305 runs two `--append-only` spellings. Witnesses:
`witness/conflicts-non-canonical-path-no-verdict.json` (three members: the
empty-segment check disabled; the dot-segment check disabled; the guard call
removed, which is the pre-round behaviour) and
`witness/conflicts-append-only-non-canonical-usage.json` (two members). The
reviewer's two reproductions, re-run after the fix
(`<scratch>/init-round/repro1.sh`, lines cut at 200 characters):

```
$ tiphys conflicts m9-p1.json m9-p2.json
tiphys conflicts: m9-p1.json (phase M9-P1): filesToTouch entry "./src/a.ts" is not a canonical path: it has a . or .. segment. ...
conflicts: NO VERDICT: 1 of 2 declaration(s) could not be used, so no pair has been shown disjoint
semantic coupling: NOT CHECKED. ...
exit=2
$ tiphys conflicts m9-p3.json m9-p4.json
tiphys conflicts: m9-p4.json (phase M9-P4): filesToTouch entry "src//foo.ts" is not a canonical path: it has an empty segment (a doubled /, or a / with nothing before it). ...
conflicts: NO VERDICT: ...
exit=2
```

**What the derivation did NOT cover.**

- Case. `src/A.ts` and `src/a.ts` are compared as different, deliberately: git
  paths are case-sensitive. On a case-insensitive filesystem they are one file,
  and this command would call them disjoint. Not addressed.
- A directory entry written WITHOUT its trailing slash (`witness` rather than
  `witness/`) is treated as a file entry, by the grammar the scope gate shares.
  `witness` against `witness/x.json` therefore reads disjoint. That is the
  declared grammar, not a spelling variant, and it is unchanged.
- A trailing slash on what is really a file, and a symlinked path in the
  repository, both need the tree to detect; this command reads only the
  declarations. Not addressed.
- Only src/commands/conflicts.ts was searched for this round's fix.

**src/gates/scope.ts has the same shape, and every consequence I found by
reading fails closed.** Not changed (outside the declaration), recorded as a
follow-up. Derivation:

```
grep -nE 'new Set\(|\.has\(|includes\(|isAllowed\(' src/gates/scope.ts
168:    if (flag === undefined || !(VALUE_FLAGS as readonly string[]).includes(flag)) {
518:function isAllowed(path: string, allowed: readonly string[]): boolean {
578:  if (basename === "" || basename.includes("/")) {
630:    const baseSet = new Set(base[field]);
631:    const headSet = new Set(head[field]);
633:      if (!baseSet.has(entry)) {
638:      if (!headSet.has(entry)) {
985:      ...new Set([
1015:      ...new Set(
1023:      ...new Set(
1027:              !isAllowed(entry.path, allowed) && !isPhaseOwnEvidence(entry.path, phase),
1034:      ...new Set([
1041:    const touchedSet = new Set(touched.map((entry) => entry.path));
1042:    const underTouched = declaredLiterals.filter((entry) => !touchedSet.has(entry)).sort();
```

- src/gates/scope.ts:518, used at src/gates/scope.ts:1027: a declared entry is
  compared with git's diff paths, which are canonical. A non-canonical entry
  matches nothing, so the file it meant reads OUT OF SCOPE: red, fails closed.
- src/gates/scope.ts:630 to src/gates/scope.ts:638: base and head declarations
  compared by string. Re-spelling an entry reads as one removal and one
  addition, and removals are refused: red, fails closed.
- src/gates/scope.ts:1041 to src/gates/scope.ts:1042: a non-canonical entry is
  listed as declared-and-untouched, which is an informational line.

So the scope gate turns a non-canonical entry into an indirect red, while
conflicts turned it into a false green. The follow-up is for scope to refuse a
non-canonical entry with that reason, rather than an out-of-scope red that
points somewhere else. I read these sites; I did not run the scope gate against
a non-canonical fixture.

## CR-KH-002, CR-KH-003, CR-KH-004: acting on a path's type, or its content, without holding the result

**Mechanism.** In the `--project` arm, one filesystem call establishes a fact
about a path (its type, or that it is absent) and a later call acts on the path
by name. Three findings are instances: the root's type was taken WITHOUT
following a link (CR-KH-002, a false refusal); the target's type was taken by
`lstat` and then written or read by name, following links (CR-KH-003, a
window); and the kernel copy was found by listing and then read with no failure
arm (CR-KH-004, an unattributed crash).

**Derivation: every filesystem or process call in the project arm and its two
helpers.** First run at `e7422bc`, after the first fix commit:

```
awk 'NR>=286' src/commands/init.ts | grep -nE 'lstatSync|statSync|realpathSync|readFileSync|writeFileSync|openSync|fstatSync|classifyEntry|packageRoot\(|spawnSync'
```

(line numbers re-based to the file)

```
293:     fd = openSync(target, fsConstants.O_RDONLY | (fsConstants.O_NOFOLLOW ?? 0));
294:     if (!fstatSync(fd).isFile()) {
297:     return readFileSync(fd);
316:     writeFileSync(target, bytes, { flag: "wx" });
330:   const rootType = classifyEntry(root);
341:     realRoot = realpathSync(root);
348:   if (!statSync(realRoot).isDirectory()) {
352:   const top = spawnSync("git", ["-C", realRoot, "rev-parse", "--show-toplevel"], {
364:     kernelRoot = packageRoot();
375:     kernelBytes = readFileSync(source);
387:     existing = lstatSync(target);
434:     classifyEntry(charter).kind === "absent"
440:     classifyEntry(registry).kind === "absent"
```

That run showed a sixth instance the reviews did not name: line 348, a
`statSync` outside any try, after the `realpathSync`, would throw uncaught if the
root vanished between the two. It is now inside the same try
(`498f404`), so a root that disappears is attributed like one that never
resolved. The same command at `498f404`, whose lines the table below uses:

```
293:     fd = openSync(target, fsConstants.O_RDONLY | (fsConstants.O_NOFOLLOW ?? 0));
294:     if (!fstatSync(fd).isFile()) {
297:     return readFileSync(fd);
316:     writeFileSync(target, bytes, { flag: "wx" });
330:   const rootType = classifyEntry(root);
342:     realRoot = realpathSync(root);
343:     rootIsDirectory = statSync(realRoot).isDirectory();
354:   const top = spawnSync("git", ["-C", realRoot, "rev-parse", "--show-toplevel"], {
366:     kernelRoot = packageRoot();
377:     kernelBytes = readFileSync(source);
389:     existing = lstatSync(target);
436:     classifyEntry(charter).kind === "absent"
442:     classifyEntry(registry).kind === "absent"
```

Disposition of each row:

| line | call | before | after |
|---|---|---|---|
| 330 | `classifyEntry(root)` | absent check | unchanged; a dangling link is not "absent" to it and falls through to 342 |
| 342, 343 | root resolution | `lstatSync(root).isDirectory()`: a link to a repository read as "not a directory" (CR-KH-002) | `realpathSync` then `statSync` of the real path, in one try; every later use is `realRoot`; a dangling link reports `could not be resolved (ENOENT)` |
| 354 | `git rev-parse` | compared with `realpathSync(root)` | compared with `realRoot` (the same value, computed once) |
| 366 | `packageRoot()` | already caught | unchanged |
| 377 | `readFileSync(source)` | uncaught (CR-KH-004) | caught; `tiphys init --project: the kernel's own <path> could not be read: ...`, exit 1 |
| 389 | `lstatSync(target)` | establishes absent, link, file | unchanged |
| 316 | the write | `writeFileSync(target)`, follows a link that appeared after 389 (CR-KH-003 window A) | `flag: "wx"`, O_CREAT and O_EXCL, fails EEXIST on any entry, a dangling link included, without following it |
| 293 to 297 | the comparison read | `readFileSync(target)` by name, follows a link (CR-KH-003 window B) | `openSync` with O_NOFOLLOW, then `fstatSync` of the opened descriptor must be a regular file, then the read from that descriptor; any failure yields an empty buffer, which differs and is REFUSED |
| 436, 442 | report only | unchanged; nothing is written from them |

**CR-KH-003 residue, stated exactly.** O_NOFOLLOW and O_EXCL act on the FINAL
path component only. A directory component of the target (the repository root
or anything above it) replaced by a link between the resolution at line 342 and
the write at line 316 is still followed. `fsConstants.O_NOFOLLOW` is undefined
on Windows, where the read falls back to a plain open. The race itself was not
forced: the tests exercise each guard against the state it guards (a dangling
link present at write time, a link present at read time), not an interleaving.

**Tests and witnesses.** test/init.test.ts:491 (symlinked root accepted and
written at the real path; dangling link named), test/init.test.ts:510 (a staged
kernel whose `assurance-modes.yaml` is a directory: exit 1, the attributed line,
no stack trace, nothing written), test/init.test.ts:530 (exclusive create
refuses a dangling link and does not create its target; the no-follow read
returns nothing for a link to an identical copy). Witnesses:
`witness/init-project-accepts-symlinked-root.json` (the original `lstat` of the
root; the dangling-link message replaced by "is not a directory"),
`witness/init-project-kernel-read-attributed.json` (the catch rethrows; the
attribution removed), `witness/init-project-copy-exclusive-nofollow.json` (`wx`
dropped; O_NOFOLLOW dropped). Five existing init witnesses were re-pointed
because their find text moved (the write is now `createCopyExclusively`, the
root is `realRoot`, the read is `readProjectCopy`); each still mutates the same
behaviour.

**What this derivation did NOT cover.** The fleet arm of init
(src/commands/init.ts:114 onward) was not re-derived: it was not changed and no
review named it. `readOwnVersion` and `spawnSync` of git are out of the
filesystem class. Paths reached inside `packageRoot()` (src/modes.ts) were not
re-derived.

## CR-KH-005: DR-0058's three items versus one produced

No code change and DR-0058 is not edited, per the instruction. The evidence
that I-8 and I-10 are not needed, and that I-9 is the template, is recorded in
delivery/verification/m5-hemma-intake.md section 4e with its commands. In short:

- I-8: the only context read of `schemas/` in src is src/checks.ts:748, inside a
  check of type `assurance-modes` that requires a context (src/checks.ts:733);
  the regime read (src/checks.ts:4799) does not reach it.
- I-10: `grep -rn '\.ctx-' src roles AGENTS.md .claude/skills scripts` exits 1
  with no output.
- I-9: `templates/gate-registry.example.yaml`, guarded by
  test/init.test.ts:403.

The orchestrator adds the DR addendum.

## CR-001, CR-002: the intake

delivery/verification/m5-hemma-intake.md now carries section 4e (commit
`4165a66`): the post-DR-0058 dispositions of I-7 to I-10 with evidence, Q-4
answered (two charter locations), the five post-init steps (commit the copy,
charter in both places, registry and adapter scripts, declarations), and the
table of project-side files kernel gates read at fixed paths, with a
recommendation per gate for step 3 and how hemma supplies each input. The I-5 to
I-10 rows name their new class; 4d is marked superseded and kept as written; Q-8
is marked answered; Q-9 (how hemma's registry invokes a kernel gate) is added.

## CR-003, CR-004, CR-005: residue

Recorded one line each in intake section 4e. None was changed: CR-003 and
CR-004 need a new conformance check, and CR-005 needs a decision about where the
modes document's context checks apply. Neither is inside this declaration.

## Fix round 1: verification before push

All on Node v26.6.0 (npm 11.18.0), this worktree under `/home/user`:

- `npm run build` at `81b17a3`: exit 0; `git status --short` afterwards printed
  nothing.
- **Suite: bare `node --test` from the repository root, `dist/` built, at
  `81b17a3`: exit 0, 1515 tests, 1515 pass, 0 fail, 0 cancelled, 0 skipped**
  (the reporter's summary counts restated in prose). The five new tests account
  for 1515 against 1510 at `520ba00`.
- `node scripts/check-authored-bytes.mjs`: exit 0.
- citations gate, `--phase m5-p6 --base origin/main --head HEAD` at `81b17a3`:
  green, `linted 2 changed document(s) ... 56 citation(s) resolved, 0
  self-citation(s), 0 unverifiable-external`.
- scope gate, same arguments: green, 36 changed paths audited; 30 entries added
  at head and named for sign-off (the 25 the criteria reviewer signed off, plus
  this round's five witness files); the two declared paths not yet touched are
  delivery/STATE.md and delivery/verification/m5-scale-out-exit.md.
- red-witness gate, same arguments: `green: 36 witness(es) evaluated (23 own, 13
  stored re-evaluated in 108349ms); every witness red against every declared
  dangerous state and green at head`.

Claim grep over this round's section (lines 633 to 930 of this file before this
block, copied to `<scratch>/init-round/fr1.md`, line numbers relative to 633):

```
205:(`498f404`), so a root that disappears is attributed like one that never
297:CR-004 need a new conformance check, and CR-005 needs a decision about where the
```

The wrap-insensitive form printed `never` and `needs a`, the same two. Line 205
describes a case (a root path that does not resolve), which
test/init.test.ts:491 exercises with a dangling link. Line 297 is a statement of
what remains open, restated here as the open question it is: where should the
modes document's context checks apply, the kernel's tree only or also a project
holding a copy? The same grep over intake section 4e printed nothing.
