# M5-P2 work history: intent to outcome

Branch claude/m5-p2-intent-to-outcome, cut from origin/main at e81c5e8.
Declaration: delivery/plan/phase-declarations/m5-p2.json:1. Plan entry:
delivery/plan/value-delivery-plan.yaml:97.

## What was done, by step

| step | commit | files |
|---|---|---|
| beacon | 6d8bc1e | this file |
| 1, brief composition reads the charter | 6c13e71 | src/commands/brief.ts |
| 2, one delivered-outcome object | 9f63dd8 | schemas/final-report.schema.json, templates/final-report.example.yaml |
| 3, tests | e6fcee8, acf40b1 | test/brief-compose.test.ts, test/report-contract.test.ts |
| 3, role instructions and registry | 162a832 | roles/implementer.md, roles/clean-room-reviewer.md, test/behaviors.json |

### Step 1: the charter's product intent reaches the brief

(The line numbers in this round-0 section were updated in fix round 1 to the
current head, because the fix moved them. The text is otherwise unchanged,
and the charter-location rule it describes is superseded by the fix round 1
section below.)

src/commands/brief.ts:163 adds `resolveProductIntent`, and src/commands/brief.ts:238
adds `renderIntent`, called at src/commands/brief.ts:445 just before the
rendered phase. The composed brief gains a `# Intent` section with
`## Product intent` (the charter path, then the product intent verbatim) and
`## Phase intent` (the plan phase's intent verbatim).

Design decision, taken here and stated because the plan says "a declared
charter" without saying what declares one:

- A charter is DECLARED when `--charter <file>` names one (a new optional
  flag), or when any entry exists at `<cwd>/charter.yaml`. The second is the
  same file name the dual-review gate reads at a project root (CHARTER_DOCUMENT
  in src/checks.ts:4616), and cwd is where the command already looks for the
  fleet warnings file.
- Presence is judged by lstat (`classifyEntry`), so a dangling link or a named
  pipe at `charter.yaml` counts as declared and is an error, not an absence.
- A declared charter that does not read, does not decode, is not
  `kind: charter`, or has no non-blank `product-intent` makes composition exit 1
  with the charter named, and nothing is written to stdout.
- With NO charter declared, composition still succeeds and the section says so
  in one explicit sentence ("no charter declared: --charter was not given and
  <path> does not exist ..."). I chose this over failing because a fleet before
  its charter is written is a normal state (src/commands/doctor.ts:829 treats
  it as WARN, not FAIL) and every existing staged-kernel test composes with no
  charter. The sentence is visible, so it is not silent omission. If the
  orchestrator wants "no charter" to be an error, it is a one-line change at
  src/commands/brief.ts:196, and I record it as an open question below.
- The full charter schema is NOT applied by brief composition. Only the field
  it carries and `kind` are checked. `tiphys validate --type charter` owns the
  rest.

### Step 2: the delivered-outcome object

schemas/final-report.schema.json:51 adds a required `delivered-outcome`
(required list at schemas/final-report.schema.json:14): `phase-intent`
(non-blank string), `delivered` (boolean), `evidence` (array of non-blank
strings, key required), `explanation` (non-blank string),
`additionalProperties: false`. The if/then at schemas/final-report.schema.json:91
couples `delivered: true` to `evidence` minItems 1. `delivered: false` may carry
an empty list, so an undelivered phase stays reportable. No numeric field exists
anywhere in the schema.

One measured correction during the step: the first form of the `then` branch
carried `minItems` without `type`, and the validator refused the whole schema:

```
$ node bin/tiphys.ts validate --type final-report templates/final-report.example.yaml
INVALID # schema uses keyword minItems without declaring type array, which this validator's strict policy requires
exit 1
```

Adding `"type": "array"` inside the `then` fixed it (exit 0, below).

templates/final-report.example.yaml gains the object. Its phase-intent is
M3-P3's intent verbatim from delivery/plan/kernel-plan-m3.md:2321, because the
example's header says every row is derived from a real record.

### Step 3: role instructions

roles/implementer.md: one paragraph added inside the reporting-contract section,
telling the implementer to answer the phase intent with the four-field object
and no score. roles/clean-room-reviewer.md: one separate paragraph, before the
first clause heading, headed in prose "The delivered outcome.", so M5-P3 can
extend it without touching the clauses. Both still validate:

```
$ node bin/tiphys.ts validate --type role-brief roles/implementer.md          -> exit 0
$ node bin/tiphys.ts validate --type role-brief roles/clean-room-reviewer.md  -> exit 0
```

## Migration derivation (the plan's migrations field)

Mechanism: "a final-report document that must satisfy the schema". Command and
full output, at head 162a832:

```
$ git grep -lE 'kind: *"?final-report'
delivery/evidence/m3-exit-test/e1/e1-9/final-report.yaml
templates/final-report.example.yaml
exit 0
$ git grep -lE '"kind" *: *"final-report"'
exit 1
$ git ls-files | grep -i final-report
delivery/evidence/m3-exit-test/e1/e1-9/final-report.yaml
delivery/evidence/m3-exit-test/e1/records/E1.9-final-report-valid.json
schemas/final-report.schema.json
templates/final-report.example.yaml
$ git grep -n 'final-report.schema\|--type final-report\|"final-report"' -- src scripts bin test .github
src/checks.ts:1809:  type: "final-report",
src/commands/validate.ts:75:  ["final-report", "final-report.schema.json"],
test/assurance-modes.test.ts:224:    "final-report",
test/assurance-modes.test.ts:261:    directPr["pipeline"] = [... "final-report"];
test/assurance-modes.test.ts:282:    partialMode["pipeline"] = [... "final-report"];
test/assurance-modes.test.ts:718:      "final-report",
test/assurance-modes.test.ts:1635:      "final-report",
test/report-contract.test.ts:... (this phase's own and the pre-existing tests, all over templates/final-report.example.yaml)
```

(Two of the test/assurance-modes.test.ts lines are shortened with `...` here;
they are pipeline stage lists, not documents.)

Result: exactly one example (templates/final-report.example.yaml), migrated.
src/checks.ts:1809 is the finding-parity derived check, which reads
`inputs` and `input-findings` only and is unaffected. test/assurance-modes.test.ts
names `final-report` as a PIPELINE STAGE id, not a document. Every pre-existing
test in test/report-contract.test.ts reads the shipped example, so they now
exercise the migrated example and pass.

NOT MIGRATED, deliberately:
delivery/evidence/m3-exit-test/e1/e1-9/final-report.yaml is M3 exit-test
EVIDENCE, and delivery/evidence/m3-exit-test/e1/records/E1.9-final-report-valid.json
records that it validated at that time. Rewriting it would falsify the record,
the plan's migration says historical reports are records and are not rewritten,
and the file is not on the declaration. Measured: it no longer validates against
the new schema:

```
$ node bin/tiphys.ts validate --type final-report delivery/evidence/m3-exit-test/e1/e1-9/final-report.yaml
INVALID #/delivered-outcome required property delivered-outcome is missing
exit 1
```

No reader of that file was found outside the evidence tree itself:

```
$ git grep -n "e1-9\|E1.9" -- ':!delivery/evidence' | cut -c1-80
delivery/plan/kernel-plan-m3.md:5171:irreversibly. E1.9's and E1.10's validation are the same Kind A mechanism C1
delivery/plan/kernel-plan-m3.md:5268:E1.9. Reporting: the phase work history validates against
delivery/plan/kernel-plan-m3.md:5408:with E1.9 and E1.10 covered by KIND rather than by instance. Revision 2's
delivery/plan/kernel-plan-m3.md:5472:   E4.3, which run once and irreversibly. E1.9 and E1.10 are the same Kind A
delivery/work-history/m3-plan-revision-3.md:463:| B-005 / M3R2B-005: ...
delivery/work-history/m3-plan-revision-3.md:580:  restated in the plan and here as the weaker statement it is. E1.9 and E1.10
delivery/work-history/m3-plan-revision-3.md:582:  runs against E1.9 or E1.10 specifically. That is written as "weaker than
```

All hits are prose in plan documents; none is code, a script or a test. The
plan's migration sentence says "historical markdown reports"; this record is
YAML, and I applied the same reasoning to it rather than reading the word
"markdown" as permission to rewrite evidence. Open question 2 below.

What the derivation did not cover: untracked files; documents that are final
reports but carry no `kind: final-report` line and are not named final-report
(a `--type final-report` caller would name them, and no tracked caller outside
the tests exists); the plugin/ tree is tracked and was covered by `git grep`.

## Red witnesses

All run in a scratch copy of the tree (a mutation lab under the session scratchpad,
`p2/lab`); the driver copies the mutated file there and restores it there.
Driver: one mutation, then `node --test --test-name-pattern=<p> <file>`, then
the copy restored. Node v26.6.0.

| mutation (dangerous state) | test | result |
|---|---|---|
| none (baseline, composer tests) | brief-compose "charter or intent" | exit 0, 4 pass |
| stale charter: read templates/charter.example.yaml from the kernel instead of the declared one | p2-charter-reaches-brief | exit 1: "implementer: the inverted charter left the brief unchanged" |
| unreadable declared charter treated as undeclared | p2-charter-fails-closed | exit 1: "a named pipe at charter.yaml (implementer) composed" |
| charter missing product-intent treated as undeclared (unreadable arm intact) | p2-charter-fails-closed | exit 1: "a charter with no product-intent field (implementer) composed" |
| none (baseline, schema tests) | report-contract "delivered-outcome" | exit 0, 2 pass |
| if/then coupling removed from the shipped schema | p2-final-report-contract | exit 1: "delivered true, evidence empty" |
| delivered-outcome removed from `required` | p2-final-report-contract | exit 1: "delivered-outcome deleted" |
| evidence removed from the object's `required` | p2-final-report-contract | exit 1: "evidence deleted" |
| a `value-score` number property added | p2-no-scoring | exit 1 |
| `additionalProperties: false` removed | p2-no-scoring | exit 1 |

The fail-closed class reddens under two structurally different members, each
alone: an entry the reader refuses to OPEN (named pipe) and a document that decodes
and LACKS the field. The schema mutations above are against the SHIPPED schema
file, so they are red through the real `tiphys validate` command; each test
also carries its own in-process Kind A witness on a fresh schema copy.

An unreadable-by-permission file (EACCES) was NOT used: this container runs as
root (`whoami` prints `root`), and root reads a mode-000 file, so that fixture
would be green for the wrong reason. The named pipe and the absent `--charter`
path are the unreadable members used instead.

## Acceptance criteria

Toolchain for every row: node v26.6.0 from the scratch prefix, checked with
`node --version` in the same shell.

| id | result | evidence |
|---|---|---|
| p2-charter-reaches-brief | PASS | test "a composed implementer brief and a composed reviewer brief each carry the charter's exact product intent ..." in test/brief-compose.test.ts; both roles composed through the real CLI; the inverted fixture changes both outputs, and every non-blank line of the old intent is asserted absent from each green output. Red under the stale-charter mutation. Fail-closed arm: "brief compose fails closed ..." with five members, red under two single-member mutations. |
| p2-final-report-contract | PASS | `node bin/tiphys.ts validate --type final-report templates/final-report.example.yaml` exit 0. Test "a final report without delivered-outcome, without its evidence, or claiming delivery on empty evidence is refused by tiphys validate" drives the same command on the three perturbed documents, each exit 1 with its pointer. Red under three schema mutations, one per member. |
| p2-no-scoring | PASS | Test "delivered-outcome is a closed four-key object with no numeric field anywhere in the final report" asserts the closed key set, `additionalProperties: false`, boolean `delivered`, and a walk of the whole schema finding no number or integer type. Test "the composed intent section has a closed heading and field set" asserts the composer's closed set. `grep -niE 'score|confidence|percent|ratio|risk-matrix'`: src/commands/brief.ts exit 1, no hits; schemas/final-report.schema.json two hits, both `$comment` prose, line 52 (this phase's text forbidding them) and line 115 ("ENUMERATION", a pre-existing substring match on "ratio"). |
| p2-suite | PASS | `npm run build` exit 0, `git status --short` empty after it; `npm test` exit 0: tests 1400, pass 1400, fail 0, skipped 0, todo 0, cancelled 0 (node v26.6.0, dist built, invocation `npm test`). |

## Gates

| gate | exit | counts |
|---|---|---|
| `npm ci` | 0 | |
| `npm run build` | 0 | git status clean |
| `npm test` | 0 | 1400 tests, 1400 pass, 0 fail, 0 skipped |
| `node scripts/check-authored-bytes.mjs` | 0 | |
| `git merge-tree --write-tree origin/main HEAD` (origin/main e81c5e8) | 0 | clean |
| scripts/m2-exit-test.sh, pr bundle, phase m5-p2, --no-build | 0 | `declared 15 applicable 10 verdict 10 green 10 red 0 not-applicable 5 error 0 vacuous 0`; required not applicable: citations only; `zero red; zero error; zero vacuous`; suite gate 1400 pass, 1275 behaviors resolve; scope green, 9 paths |
| citations, run alone | 21 | not-applicable: this diff touches no path under the citation-required trees (the work history is not one of them) |

## Hazard self-review

- ceremonial-intent (intent printed, nothing answers it). Attack: can a final
  report pass with no answer? Deleting the object, deleting evidence, and
  delivered true on an empty list are each exit 1 through the real command.
  Residue, not closed: a report can claim `delivered: true` with evidence that
  is plausible and false; the schema's $comment and the reviewer brief both say
  that part is the reviewer's.
- goodhart-score (a number becomes a target). Attack: can a numeric field be
  added beside the object, not in it? The schema walk covers the whole
  final-report schema, so a number anywhere in it is red. Residue: a string
  field could still carry a grade such as "8/10" in `explanation`; nothing
  reads it as a number, and no test forbids the text.
- stale-charter (fallback when unreadable). Attack: can a brief compose green
  with an old or default intent? The stale-charter mutation is red, the two
  fail-open mutations are red, and stdout is asserted empty on refusal.
  Residue: with NO charter declared, composition is green and says so in the
  section (open question 1).

## Fix round 1

Inputs: delivery/review/clean-room-M5-P2-opus-criteria.md:46 (CR-001, medium),
delivery/review/clean-room-M5-P2-sonnet-hazard.md:182 (CR-M5P2-01, medium),
and the two lows CR-003 and CR-002. Both review files were copied unchanged
into delivery/review/ and committed first (0fc4638). CR-004 is the
orchestrator's paperwork and was left alone, as instructed.

### CR-001: the charter is not read in a tiphys init fleet

Finding: in the layout `tiphys init` creates, the charter lives in `charter/`
(src/fleet.ts:93), and `brief compose` looked only at `<cwd>/charter.yaml`. It
exited 0 and said "no charter declared".

Mechanism: two commands located the charter by two different rules. Doctor
walked `charter/` and took documents whose `kind` is `charter`; the composer
joined one fixed file name. Any rule written twice drifts, and here it had
drifted from the first commit.

Fix: one rule, in a new module. src/charter.ts:72 (`readCharterDirectory`)
walks `charter/` in sorted order and classifies each `.yaml` or `.yml` entry
(absent, refused, undecodable, not a charter, charter). This is the logic
doctor had inline, moved rather than rewritten. src/charter.ts:146
(`locateCharters`) adds the project-root `charter.yaml` in front of it.
Doctor's retention check now calls `readCharterDirectory`
(src/commands/doctor.ts:742); its early returns consume the entries in the
same order as before, so the refused and undecodable FAILs, the
no-charter WARN and the non-charter WARN are reached exactly as they were.
The composer calls `locateCharters` (src/commands/brief.ts:163):

- `--charter` given: that file is read, as in round 0.
- exactly one charter found: it is read.
- more than one: refused, naming every path, until `--charter` picks one.
- none found and some YAML in `charter/` that is not a charter: refused.
- a refused or undecodable entry in `charter/`: refused with its reason.
- nothing at all: the undeclared sentence, which now names both places it
  looked.

roles/README.md states the same rule in its Composition section.

Derivation, command 1 (every place that names a charter file or directory):

```
git grep -nE 'CHARTER_DOCUMENT|charterDir|"charter\.yaml"|charter\.yaml`|join\([^)]*"charter"' -- src scripts bin plugin .github
```

Full output, exit 0:

```
scripts/check-dual-review.mjs:84:  CHARTER_DOCUMENT,
scripts/check-dual-review.mjs:307: * `charter.yaml` written into a working tree and committed nowhere passed this
scripts/check-dual-review.mjs:450:          `${CHARTER_DOCUMENT} declares a single review family and only ${String(found.paths.length)} verdict ` +
scripts/check-dual-review.mjs:466:          `${CHARTER_DOCUMENT} declares a single review family and ${String(selected.length)} registered check(s) ` +
scripts/check-dual-review.mjs:776:        `compared on framing and review-contract, and ${CHARTER_DOCUMENT} declares that exactly one model family ` +
scripts/check-dual-review.mjs:783:          `${CHARTER_DOCUMENT} declares ${REVIEW_FAMILIES_FIELD}.available with exactly one entry (${family}), so ` +
scripts/check-dual-review.mjs:788:          `declaration: ${CHARTER_DOCUMENT} at ${run.singleFamily.provenance.refSha}`,
scripts/check-dual-review.mjs:800:        `declaration: ${CHARTER_DOCUMENT} at ${run.singleFamily.provenance.refSha}`,
scripts/rehearse-cutover-rollback.mjs:214:    charterDir: join(fleetRoot, "charter"),
src/charter.ts:9: * only at `<cwd>/charter.yaml`. In the layout `tiphys init` creates, the
src/charter.ts:21: * THE ROOT FILE. A project repository keeps its charter at `<root>/charter.yaml`
src/charter.ts:22: * (CHARTER_DOCUMENT in src/checks.ts, read by the dual-review gate from a
src/charter.ts:40:/** The project-root charter file (the same name as CHARTER_DOCUMENT in src/checks.ts). */
src/charter.ts:41:export const ROOT_CHARTER_FILE = "charter.yaml";
src/checks.ts:3121: * about a directory with a `charter.yaml` sitting in it, because the probe had
src/checks.ts:3413: * force at all, and it read `charter.yaml` and `assurance-modes.yaml` off
src/checks.ts:3414: * DISK, while `readReviewFamilies` read THE SAME `charter.yaml` out of the
src/checks.ts:4498:  const charterPresent = contextDocumentPresentAt(contextDirectory, CHARTER_DOCUMENT, source);
src/checks.ts:4503:       disk, so a `charter.yaml` written into a working tree and committed
src/checks.ts:4514:          `(no ${CHARTER_DOCUMENT} ${describeContextDocumentSource(source)}), so the verdicts for phase ` +
src/checks.ts:4521:  const charter = readContextDocumentAt(contextDirectory, CHARTER_DOCUMENT, source);
src/checks.ts:4616:export const CHARTER_DOCUMENT = "charter.yaml";
src/checks.ts:4626: * so nothing ever reported a disagreement, and an uncommitted `charter.yaml`
src/checks.ts:4629:export const REGIME_DOCUMENTS = [CHARTER_DOCUMENT, MODES_DOCUMENT];
src/checks.ts:4745:  const tree = readContextDocument(contextDirectory, CHARTER_DOCUMENT);
src/checks.ts:4765: * `HEAD:./charter.yaml` AND NOT `HEAD:charter.yaml`, and the difference is not
src/checks.ts:4777:  const relativePath = `./${CHARTER_DOCUMENT}`;
src/checks.ts:4783:          `${join(contextDirectory, CHARTER_DOCUMENT)} declares ${REVIEW_FAMILIES_FIELD} in the working tree and ` +
src/checks.ts:4797:          `${join(contextDirectory, CHARTER_DOCUMENT)} declares ${REVIEW_FAMILIES_FIELD} in the working tree and ` +
src/checks.ts:4807:    path: CHARTER_DOCUMENT,
src/checks.ts:4812:  const decoded = decodeDocument(body, join(contextDirectory, CHARTER_DOCUMENT));
src/checks.ts:4992:            `${CHARTER_DOCUMENT} declares ${REVIEW_FAMILIES_FIELD} at ${declarationRef} and ${contextDirectory} ` +
src/checks.ts:5020:        `${CHARTER_DOCUMENT} declares the single review family ${reading.declaredAs.join(", ")} and the ` +
src/checks.ts:5039:        `${CHARTER_DOCUMENT} declares the single review family ${reading.declaredAs.join(", ")} ` +
src/checks.ts:5053:      `REPORT single-family-declared ${CHARTER_DOCUMENT} declares exactly one review family ` +
src/checks.ts:5082: * declared mode, not assumed: `charter.yaml` names the delivery mode and
src/commands/brief.ts:92:   * root `charter.yaml`, or a fleet's `charter/` directory, the same directory
src/commands/brief.ts:138: *   - any entry at `<cwd>/charter.yaml` is a candidate (judged by lstat, so a
src/commands/brief.ts:152: * looking only at `charter.yaml`, exited 0 there saying no charter was
src/commands/doctor.ts:735:  const charterDir = join(root, CHARTER_DIRECTORY);
src/commands/doctor.ts:742:  const reading = readCharterDirectory(charterDir);
src/commands/doctor.ts:829:        detail: `no charter document in ${charterDir}, so no project is realized here yet and retention is not applicable`,
src/commands/doctor.ts:839:      detail: `${String(candidates)} YAML document(s) in ${charterDir}, none with kind: charter, so no retention paths are declared`,
src/fleet.ts:44:  charterDir: string;
src/fleet.ts:93:    charterDir: join(root, "charter"),
```

Derivation, command 2 (every place that tests a document's kind against
`charter` or names the directory by its bare string):

```
git grep -nE '\.charterDir|"charter"|kind"\] *(!==|===) *"charter"' -- src scripts bin plugin
```

Full output, exit 0:

```
plugin/src/hooks/project-write-block.ts:493:  "charter",
scripts/rehearse-cutover-rollback.mjs:41:const FLEET_DIRS = ["charter", "decisions", "state", "tasks", "worktrees", "projects"];
scripts/rehearse-cutover-rollback.mjs:214:    charterDir: join(fleetRoot, "charter"),
src/charter.ts:38:export const CHARTER_DIRECTORY = "charter";
src/charter.ts:58:  | { path: string; kind: "charter"; document: Record<string, unknown> };
src/charter.ts:113:    if (document["kind"] !== "charter") {
src/charter.ts:117:    entries.push({ path, kind: "charter", document });
src/charter.ts:160:      if (entry.kind === "charter") {
src/commands/brief.ts:212:  if (charter === undefined || charter["kind"] !== "charter") {
src/commands/brief.ts:490:    ["--charter", "charter"],
src/commands/brief.ts:505:  const optional = new Set<keyof Options>(["out", "reviewContract", "charter"]);
src/commands/doctor.ts:759:    if (entry.kind !== "charter") {
src/commands/init.ts:62:const DURABLE_KEEP_DIRS = ["charter", "decisions", "tasks", DURABLE_STATUS_DIR] as const;
src/commands/validate.ts:55:  ["charter", "charter.schema.json"],
src/fleet.ts:13:  "charter",
src/fleet.ts:93:    charterDir: join(root, "charter"),
```

Every site, classified:

| site | what it does | disposition |
|---|---|---|
| src/commands/doctor.ts:742 | reads `charter/` for retention | now the shared rule |
| src/commands/brief.ts:163 | locates the brief's charter | now the shared rule |
| src/checks.ts:4616 and its users, scripts/check-dual-review.mjs:84 | read a project repository's `charter.yaml` FROM A COMMIT for the review regime | left alone: a different subject (a project repository, read at a ref, not a fleet directory on disk) with its own provenance rules; src/charter.ts:40 names the shared file name so the two cannot silently diverge on it |
| src/fleet.ts:93, scripts/rehearse-cutover-rollback.mjs:214 | build the `charterDir` field | a path, not a reader |
| plugin/src/hooks/project-write-block.ts:493, src/fleet.ts:13, scripts/rehearse-cutover-rollback.mjs:41, src/commands/init.ts:62 | directory-name lists (write blocking, layout, bootstrap) | not readers |
| src/commands/validate.ts:55 | schema type map | not a locator |
| src/commands/brief.ts:212, 490, 505 | kind check after location, option parsing | part of the composer, downstream of the rule |

What the derivation did NOT cover: untracked files; `test/` (tests stage
charters, they do not locate them); `templates/` and `schemas/` (data, not
code); and any path built dynamically from pieces that match none of these
tokens (for example a directory name read from configuration). I did not
search for such a construction beyond these two commands.

Witnesses, new tests in test/brief-compose.test.ts, both driving the real CLI:

- "brief compose in a tiphys init fleet reads the charter from charter/, the
  same document doctor reads": runs `tiphys init`, writes a charter into
  `charter/`, composes from the fleet root, and asserts the exact product
  intent and that doctor's `CHECK retention` line names the same path.
- "brief compose in a fleet refuses several charters until --charter picks
  one, and refuses YAML in charter/ that is not a charter": two charters
  (refused), `--charter` picking one (green), a stray `notes.yaml` (refused),
  a fresh fleet (the undeclared sentence).

### CR-M5P2-01: delivered: true passes on duplicate or near-blank evidence

Mechanism: `minItems: 1` counts entries and says nothing about them, so the
coupling "delivered needs evidence" was satisfied by any string, repeated.

Fix, schemas/final-report.schema.json:74 carries the rule and its reasoning
as a `$comment`:

- `uniqueItems: true`. The same string cited twice is one piece of evidence.
- item `pattern: "[A-Za-z0-9]"`: at least one ASCII letter or digit. The
  reason: every form this field names (a path with a line number, a command,
  an exit code, a run URL, a sentence) contains one, and a string of only
  punctuation and spaces refers to nothing. The rule is a floor on
  referring, not a judgement of quality.
- Residue, stated in the schema too: uniqueness compares exact strings (`a`
  and `a ` are two items); a lone `x` passes; nothing checks that an entry
  resolves or is true. Those remain a reviewer's.

The example (templates/final-report.example.yaml) still validates, exit 0.

T-012 owes a timing test for a pattern change. The pattern is a single
character class with no quantifier, so it has no backtracking; the test
"the evidence content pattern is linear on a long punctuation near-miss"
measures it regardless, on 200000 characters with no letter or digit and requires
under 2000 ms.

Witness, new test in test/report-contract.test.ts: "a delivered claim whose
evidence repeats one entry or is only punctuation is refused by tiphys
validate". Two structurally different members: one real-looking entry twice,
and entries of only punctuation. A distinct honest pair goes green.

### CR-003: the implementer does not write delivered-outcome

roles/implementer.md now says the implementer answers the phase intent in its
report, and the orchestrator carries that answer into the final report's
`delivered-outcome` object. AGENTS.md, which holds the orchestrator's
obligations, gains the matching instruction: one delivered outcome per phase,
checked against the reviews, with no score. `tiphys validate --type
role-brief` exits 0 on both files, and check-agents-references is green.

### CR-002: roles/README.md did not describe the composed brief

The Composition section now shows `[--charter <file>]`, lists the Intent
section in the composed order, and states the charter-location rule above.

### Declaration grants

Added on the head, additive only, printed by the scope gate by name:
src/charter.ts, src/commands/doctor.ts, roles/README.md, AGENTS.md and
`witness/`. Behaviors added to test/behaviors.json by name:
p2-charter-fleet-init, p2-charter-fleet-ambiguous, p2-evidence-distinct,
p2-evidence-pattern-linear. Witness specs, one per src/ or schema behavior,
each with two structurally different mutation members:

| spec | member 1 | member 2 |
|---|---|---|
| witness/p2-charter-fleet-init.json | src/charter.ts: drop `found.push(entry.path)` | brief.ts: locate under a nonexistent root |
| witness/p2-charter-fleet-ambiguous.json | brief.ts: several charters not refused | brief.ts: stray YAML not refused |
| witness/p2-evidence-distinct.json | schema: `uniqueItems` false | schema: pattern back to `\S` |

No member touches src/commands/doctor.ts: it spawns git, so a phase-owned
member there would owe captured external output (rule f). Doctor's retention
behavior is guarded by the twenty existing doctor specs; every one of their
`find` strings still occurs exactly once in the changed doctor.ts, measured by
a script over witness/*.json. One pre-existing spec,
witness/doctor-kernel-artifacts-resolution.json, has a member whose `find`
occurs twice in doctor.ts; it occurs twice on origin/main as well, so this
round did not cause it.

### Red before, green after

Measured in a scratch copy of the tree (not the worktree), node v26.6.0.
Counts are from the node test reporter; its leading U+2139 glyph is omitted
from each count line quoted here, and nothing else was changed.

Before the fix (the round-0 src and schema, the new tests): tests 5, pass 0,
fail 5. The five were the four new tests plus the updated undeclared test.
The fleet test's message: "implementer: the fleet charter was not found".

Each witness member, applied alone to the fixed tree, named test only:

| member | tests | fail |
|---|---|---|
| charter.ts found dropped | 1 | 1 |
| locate under a nonexistent root | 1 | 1 |
| several charters not refused | 1 | 1 |
| stray YAML not refused | 1 | 1 |
| `uniqueItems` false | 1 | 1 |
| pattern `\S` (also reddens the timing test) | 2 | 2 |

With no mutation: tests 3, pass 3, fail 0.

### Gates, fix round 1

At head cc18e20, node v26.6.0, npm 11.18.0, this worktree:

- `npm run build`: exit 0; `git status --porcelain` empty afterwards.
- `npm test` (dist built): exit 0, tests 1404, pass 1404, fail 0, skipped 0.
- `./scripts/m2-exit-test.sh --base origin/main --head HEAD --phase m5-p2
  --bundle pr --no-build <scratch>`: exit 0. Bundle line:
  `declared 15 applicable 10 verdict 10 green 10 red 0 not-applicable 5 error 0 vacuous 0`.
  red-witness: 26 witnesses evaluated (3 own, 23 stored re-evaluated), green.
  scope: green, 19 changed paths, and it names the 5 entries added at head
  (AGENTS.md, roles/README.md, src/charter.ts, src/commands/doctor.ts, and
  `witness/` as a directory prefix). Required gate not applicable: citations
  only, with "no changed path under the configured documents globs". The
  registry form (`--only citations`) reports the same not-applicable.
  m2-assert: 15 records, zero red, zero error, zero vacuous.
- `node scripts/check-authored-bytes.mjs`: exit 0.
- `git merge-tree --write-tree origin/main HEAD`: exit 0 (tree d73921f).
- Claim grep, both forms, over this file: 0 line hits and 0 wrap-insensitive
  hits. Two hits in this section's first draft were restated before the run.

## Open questions for the orchestrator

1. No charter declared composes green with an explicit "no charter declared"
   sentence. Should it be an error instead? My recommendation is to keep it,
   because a fleet before its charter is normal (doctor WARN), and the sentence
   is visible in the brief.
2. delivery/evidence/m3-exit-test/e1/e1-9/final-report.yaml no longer
   validates. It is exit-test evidence and I did not rewrite it. Recommendation:
   leave it as a record.
3. The charter path in the brief is absolute (cwd joined), so a composed brief
   carries a machine path. Harmless for dispatch; noted in case briefs are ever
   compared across machines.

## Log

- Branch created from origin/main (e81c5e8). Beacon committed.
- delivery/plan/m5-conflict-pre-pass.md, mandated reading 4 in the common
  brief, does not exist at e81c5e8 (`cat` reports "No such file or
  directory"). The plan entry's `conflicts-with: [M5-P3]` and the phase brief's
  collision note were used instead.
