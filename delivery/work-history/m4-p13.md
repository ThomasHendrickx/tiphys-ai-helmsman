# M4-P13: the migration-table re-disposition

- phase id: M4-P13
- branch: `claude/m4-p13-migration-redisposition`
- cut from: `plan/pstack-borrow-review` at `6961186`
- started: 2026-09-15

This file was opened in the first minutes and appended to as the work
happened, per the T-008 dispatch contract, so a death would have left a
partial result rather than nothing.

## 0. Note on captured output, declared before anything is quoted

Node's test reporter prints non-ASCII glyphs at the head of its result and
summary lines. Binding convention 3 requires authored files to be pure ASCII,
and the red-witness rule requires real captured output rather than
hand-written strings. Per the resolution CLAUDE.md records, every captured
run below is TRANSLITERATED and the transliteration is declared:

| codepoint | name | rendered as | occurrences replaced |
|---|---|---|---|
| U+2714 | HEAVY CHECK MARK | `+` | TRANSLIT_CHECK |
| U+2716 | HEAVY MULTIPLICATION X | `x` | TRANSLIT_CROSS |
| U+2139 | INFORMATION SOURCE | `i` | TRANSLIT_INFO |

Nothing else in any captured output in this file was changed. No captured
command, exit code, count or assertion message was edited, reordered or
reflowed.

## 1. What this phase is

Three decided records converge on the M4 half of Appendix A:

- **DR-0028**: the kernel ships the gate CONTRACT and ZERO project gates.
- **DR-0029**: Tiphys owns the PROCESS, the project owns the PREDICATE
  (delivery/decisions/DR-0029-the-ownership-boundary-and-the-applicability-envelope.md:38).
- **DR-0037**: the kernel is M4's only subject.

Together they make most of clause 4's seven project-gate rows undeliverable
as written, not because the work is hard but because the artifact they name
is one DR-0028 says the kernel never ships. This phase re-dispositions all
thirteen M4 migration-table rows against those three records, moves every
pinned count in one change, and adds the cross-document check that the plan
has asserted in prose since v1 and that no gate could see.

## 2. The four pinned sites, derived rather than taken from the brief

The brief said there are FOUR pinned sites and told me to find them all
myself. The MECHANISM is: **a literal restatement of the per-milestone or
per-kind bucket distribution, in an artifact that must still be TRUE after the
re-disposition.** The derivation separates that from a HISTORICAL CAPTURE of a
past gate run, which restates the same numbers and must NOT be edited, because
editing it would falsify evidence about a head where those numbers were right.

### 2.1 The derivation commands, with their full output

```
$ git grep -n -l -E 'perMilestone|perKind|per-milestone|per-kind' -- .
delivery/evidence/m2-exit-test/main-bundle.summary.json
delivery/evidence/m2-exit-test/pr-bundle.summary.json
delivery/evidence/m3-exit-test/e1-1-to-e1-5.md
delivery/evidence/m3-exit-test/e1/e1-6/gate-bundle-summary.json
delivery/evidence/m3-exit-test/e1/records/E1.4-gate-coverage.json
delivery/evidence/m3-exit-test/e1/records/E1.6-gate-bundle.json
delivery/evidence/m3-exit-test/e1/records/E1.6b-gate-bundle.json
delivery/evidence/m3-exit-test/e1/records/branch-local-gate-bundle.json
delivery/plan/kernel-plan-m2.md
delivery/plan/kernel-plan-m4.md
delivery/plan/m4-conflict-pre-pass.md
delivery/plan/m4-intake.md
delivery/review/arbitration-m1-p6.md
delivery/review/clean-room-m2-p6-criteria.md
delivery/review/clean-room-m2-p6-hazard.md
delivery/review/clean-room-m2-p6-round2-criteria.md
delivery/review/clean-room-m2-p6-round2-hazard.md
delivery/review/evidence/clean-room-m3-p3-r8-criteria/gates-summary-full-mode.json
delivery/work-history/exit-test-assertion-direction.md
delivery/work-history/m2-p6.md
delivery/work-history/m3-p3.md
delivery/work-history/m3-p9.md
src/gates/coverage.ts
src/gates/schemas/coverage-config.schema.json
test/behaviors.json
test/coverage-gate.test.ts
test/fixtures/gate-runner-capture.summary.json
```

That is the loose net. The tight one, matching the distribution as a literal:

```
$ git grep -nE 'M1 = 11|M1 11, M2 16|perMilestone\["M4"\]|perKind\["milestone"\]|M4 = 13|M4 13' -- .
delivery/decisions/DR-0011-early-parallelism.md:59: [counts M1 = 11, M2 = 16, M3 = 74, M4 = 13, M5 = 1]
delivery/evidence/m2-exit-test/main-bundle.summary.json:39
delivery/evidence/m2-exit-test/pr-bundle.summary.json:34
delivery/evidence/m3-exit-test/e1-1-to-e1-5.md:179
delivery/evidence/m3-exit-test/e1/e1-6/gate-bundle-summary.json:48
delivery/evidence/m3-exit-test/e1/records/E1.4-gate-coverage.json:28
delivery/evidence/m3-exit-test/e1/records/E1.6-gate-bundle.json:28
delivery/evidence/m3-exit-test/e1/records/E1.6b-gate-bundle.json:28
delivery/evidence/m3-exit-test/e1/records/branch-local-gate-bundle.json:28
delivery/plan/kernel-plan-m2.md:412
delivery/plan/kernel-plan-m2.md:685
delivery/plan/kernel-plan-m4.md:2120
delivery/plan/kernel-plan-m4.md:2121
delivery/plan/kernel-plan-m4.md:2207
delivery/plan/kernel-plan-v1.md:435
delivery/plan/m4-intake.md:971
delivery/review/clean-room-m2-p6-criteria.md:17
delivery/review/clean-room-m2-p6-criteria.md:22
delivery/review/clean-room-m2-p6-hazard.md:223
delivery/review/clean-room-m2-p6-round2-hazard.md:87
delivery/review/evidence/clean-room-m3-p3-r8-criteria/gates-summary-full-mode.json:47
delivery/review/evidence/clean-room-m3-p3-r8-criteria/units-baseline-18c335a.json:240
delivery/review/evidence/clean-room-m3-p3-r8-criteria/units-head-108eed0.json:240
delivery/review/evidence/clean-room-m3-p3-r9-criteria/units-baseline-18c335a.json:240
delivery/review/evidence/clean-room-m3-p3-r9-criteria/units-head-b5c01f0.json:240
delivery/review/plan-review-m2-r1.md:220
delivery/review/plan-review-m2-r1.md:222
delivery/review/plan-review-m2-r1.md:224
delivery/review/plan-review-m2-r1.md:317
delivery/review/plan-review-r1.md:123
delivery/work-history/m2-p6.md:132
test/coverage-gate.test.ts:156
test/coverage-gate.test.ts:160
test/fixtures/gate-runner-capture.summary.json:34
```

(The line numbers are the grep's; the matched text is elided on all but the
first for length, and every one of them carries the same distribution.)

And the third, for anything in EXECUTABLE scope that asserts on the counts:

```
$ git grep -nE '115 inventory|per-milestone|milestone 104|totalInventoryIds|expectedUnits' -- src/ test/ scripts/ bin/ *.yaml *.json
src/checks.ts:1802            (prose about a config-stated anchor)
src/gates/coverage.ts:55,59,159,194,400,543,550,554,559,894,913,924,937,951,953,957
src/gates/schemas/coverage-config.schema.json:50,71
test/behaviors.json:209
test/coverage-gate.test.ts:51,59,146,153,342,499,507,514,538,555
test/fixtures/gate-runner-capture.summary.json:34
```

### 2.2 The classification, and the two sites the plan did NOT name

| # | site | live pin? | gate-coupled | what I did |
|---|---|---|---|---|
| 1 | the Milestone column, 13 rows in delivery/requirements/migration-table.md | YES | NO | 12 rows rewritten (R-001a's is byte-identical, see below) |
| 2 | the counts sentence at delivery/plan/kernel-plan-v1.md:435 | YES | NO | rewritten |
| 3 | the 13 Appendix A bucket rows in the same file | YES | YES (coverage) | 12 rewritten |
| 4 | test/coverage-gate.test.ts:153 to :162 | YES | YES (the suite) | six pins collapsed to one `deepEqual` |
| 5 | the R-001a migration-table row, pinned VERBATIM at test/coverage-gate.test.ts:483 | YES | YES (the suite) | left byte-identical, see 2.3 |
| 6 | delivery/decisions/DR-0011-early-parallelism.md:59 | NO | NO | NOT edited, see 2.4 |
| 7 | delivery/plan/kernel-plan-m2.md:412 and :685 | NO | NO | NOT edited, see 2.4 |
| 8 | delivery/plan/m4-intake.md:971 | NO | NO | NOT edited, see 2.4 |
| 9 | 13 files under `delivery/evidence/` and `delivery/review/` | NO | NO | NOT edited, see 2.4 |
| 10 | `test/fixtures/gate-runner-capture.summary.json` | NO | YES, but not on this string | NOT edited, see 2.5 |

The plan's four are rows 1 to 4 and I reproduce them. **Row 5 is one the plan
did not name and it is a real live pin**: it would have gone red if I had
touched R-001a's Notes.

### 2.3 Site 5, the verbatim row pin

test/coverage-gate.test.ts:483 asserts, before it mutates anything:

```
  const targetLine =
    "| R-001a | S0, 15-17 | Orchestrator never writes feature code in projects (infra hotfixes excepted) | L1 | project-write block hook (Claude Code plugin) | M4 | Hook named in M4 contents; infra-hotfix carve-out needs a defined bypass |";
  assert.ok(inventoryText.includes(targetLine), "fixture assumption failed: R-001a line not found verbatim");
```

That is the WHOLE row, not just its Milestone cell. R-001a stays M4 under the
disposition, so I left the row byte-identical rather than annotating it, which
is why the migration table shows twelve rewritten rows and not thirteen. The
alternative was to update the pinned literal as well, and it is a worse trade:
the pin exists so the duplicate-id witness is mutating a line that really
exists, and moving it for a cosmetic note edit spends a real guard for nothing.

### 2.4 Sites 6 to 9: historical, and NOT edited

These restate the pre-M4-P13 distribution and are all TRUE statements about
the head they were written at.

- **DR-0011:59** is a decided owner decision record. CLAUDE.md forbids an
  agent reopening one, and the sentence is the REASON the decision was taken
  ("coverage weighting that makes M3 planning the highest-value parallel
  work"), which stays true of the weighting as it stood.
- **kernel-plan-m2.md:412** is M2-P6's acceptance criterion, already passed
  and evidenced. **:685** is M2's own assertion that M2 changed no bucket,
  which remains true of M2.
- **m4-intake.md:971** is the sentence the M4 plan already measured as wrong
  in both halves (delivery/plan/kernel-plan-m4.md:2113). Correcting the intake
  is not this phase's job and the file is not on its list.
- The thirteen files under `delivery/evidence/` and `delivery/review/` are
  captured gate output and review records. Editing a capture so it agrees with
  a later head is the fabrication the red-witness rule exists to prevent.

**This is not a judgment call dressed up as a rule.** The test is mechanical:
a site is LIVE if some program or gate must still find that number true after
the change. None of 6 to 9 is read by anything.

### 2.5 Site 10, a near miss worth writing down

`test/fixtures/gate-runner-capture.summary.json` carries the old detail string
and IS read by a live test (test/gate-registry.test.ts:459). That combination
looks exactly like a fifth gate-coupled pin. It is not. The consumer asserts on
the `deploy` gate's precondition fields only:

```
$ sed -n '459,470p' test/gate-registry.test.ts
  const captured = JSON.parse(
    readFileSync(join(fixturesDir, "gate-runner-capture.summary.json"), "utf8"),
  ) as { gates: { id: string; status: string; units: number }[] };
  const evidenceRoot = join(fixturesDir, "gate-runner-capture.deploy-result.json");
  const deploy = JSON.parse(readFileSync(evidenceRoot, "utf8")) as {
    status: string;
    units: number;
    precondition: { id: string; met: boolean; reason: string };
  };
  assert.equal(deploy.status, "not-applicable");
  assert.equal(deploy.units, 0);
  assert.equal(deploy.precondition.met, false);
  assert.match(deploy.precondition.id, /STRUCTURAL/);
```

Nothing reads the coverage row's `detail`. The fixture's own comment says it is
"a REAL run of the M2 runner stored verbatim", so it is a capture, and the
correct action is to leave it alone. The green full-suite run below is the
evidence that this reading is right rather than merely argued.

## 3. The disposition, thirteen rows

The rule: a row survives in M4 only if some artifact inside M4's declared
scope discharges it AND DR-0028 and DR-0029 permit that artifact to exist.

| row | from | to | why |
|---|---|---|---|
| R-001a | M4 | **M4** | The subject exists (the kernel clone under the fleet home). Authority enforcement owns it, not this workstream. Row untouched. |
| R-041 | M4 | **M4** | The subject exists: `tsc -b` via package.json's `build`. Retargeted to a kernel-declared CLASS whose command the kernel's OWN registry names, which is DR-0029's dogfooding sentence rather than a kernel-shipped project gate. |
| R-042 | M4 | **DR-0029** | No linter and no `lint` script in the only subject. What lint, format and style mean is the project's. |
| R-045 | M4 | **DR-0029** | Zero i18n files; the kernel ships no i18n command at any milestone. |
| R-046 | M4 | **DR-0029** | Zero analytics files. Supersedes the L1 half of plan decision D-12; D-12's semantic-accuracy residue via R-059 is untouched. |
| R-047 | M4 | **M4** | The subject exists and is already green: `agent-rules-drift` and `brief-drift` are regenerate-and-compare checks. What is left is the class declaration plus a red witness. |
| R-050a | M4 | **DR-0029** | Zero e2e files; same as R-045. |
| R-051 | M4 | **DR-0029** | The counter-case is recorded rather than suppressed: the kernel's `usage:` strings ARE a copy surface and a gate could be built. Nothing in M4 consumes one, and building one is the kernel inventing a predicate DR-0029 assigns to the project. |
| R-064 | M4 | **M4** | Already discharged: ruleset `main-protection` is enforcement active with `required_status_checks` naming `gates`. M4 records the evidence; the note is rewritten, the bucket is not. |
| R-065a | M4 | **M4** | Owner-deferred and carried as an M4 exit-criteria footnote. A deferral inside M4 is not a re-disposition. |
| R-071 | M4 | **DR-0029** | The kernel has no migrations (one test fixture). A project's CI design rule and the clearest case of a predicate DR-0029 hands to the project. |
| R-072 | M4 | **M5** | The kernel's own instance shipped in M1-P1 per D-15. What is left is the PRODUCTIZED bootstrap check, which needs a project to bootstrap. |
| R-097 | M4 | **M5** | Same artifact as R-072, and the row's own note already said the greenfield bootstrap path is M5 before the bucket agreed with it. |

Result: M4 5, M5 3, `decision` 6. Both documents now say so, verified by
counting rather than by assertion:

```
$ grep -oE '^\| R-[0-9]+[a-z]? \|[^|]*\|[^|]*\|[^|]*\|[^|]*\|[^|]*\|' delivery/requirements/migration-table.md | awk -F'|' '{print $7}' | sort | uniq -c | sort -rn
     74  M3
     16  M2
     11  M1
      6  DR-0029
      5  M4
      3  M5

$ grep -oE '^\| R-[0-9]+[a-z]? \| [^|]+ \|' delivery/plan/kernel-plan-v1.md | awk -F'|' '{print $3}' | sed -E 's/ (M[0-9]+)-P[0-9]+ /\1-Pn/' | sort | uniq -c | sort -rn
     74  M3
     16  M2
     11 M1-Pn
      6  DR-0029
      5  M4
      3  M5
```

**Why the four subject-less gate rows go to DR-0029 and not to M5.** Bucketing
them to M5 would say a kernel deliverable falls due when a second project
onboards. DR-0029 says the opposite: under the boundary at
delivery/decisions/DR-0029-the-ownership-boundary-and-the-applicability-envelope.md:50
the kernel ships no lint, i18n, analytics or e2e command at any milestone.
Moving them to M5 recreates the same false premise one milestone later, which
is the hazard the change is meant to close, reproduced by the change.

## 4. Sites 2 and 3: the plan text

The counts sentence gained a bucket kind in its enumeration, the new counts,
and one further change that is not bookkeeping. The sentence has always said
"Buckets follow the migration table's milestone column", and until this phase
nothing checked it. It now names the checker, so a reader can see the claim is
mechanised rather than conventional.

Plan decision D-12 gained one sentence recording that its L1 half is
superseded and its semantic-accuracy residue is not.

**Not touched, deliberately:** delivery/plan/kernel-plan-v1.md:368, the M4
paragraph itself. The M4 plan assigns that rewrite to a different unit and
says two phases editing one file is a conflict its pre-pass must record rather
than discover (delivery/plan/kernel-plan-m4.md:2199).

## 5. The gates, BEFORE and AFTER

Both runs are on node v26.6.0 (a scratch floor-satisfying toolchain, see
section 8), from a COMMITTED head, `--mode full`.

### 5.1 coverage, BEFORE (head `bd92a01`, the beacon commit, documents untouched)

```
$ node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full \
    --only coverage --evidence /tmp/m4p13-ev/before-coverage \
    --base origin/main --head HEAD
gates: run e04249ed040da065011525ed
gates: 2 registry gate(s) declared verified-by clean-room-checklist and NOT executed by this runner: unit-tests-for-changed-service-methods (probe unit-tests-for-changed-service-methods), fixtures-for-changed-component-states (probe fixtures-for-changed-component-states)
gates: registry gate-registry.yaml mode full
gates: declared 1 applicable 1 verdict 1 green 1 red 0 not-applicable 0 error 0 vacuous 0
gates: coverage: green: 115 inventory id(s) checked; per-kind: milestone 104, phase 11; per-milestone: M1 11, M2 16, M3 74, M4 13, M5 1
gates: every applicable gate is green
GATE_EXIT=0
```

`result.json` from that run, which is the gate's own record rather than a
bundle line:

```
{
  "gate": "coverage",
  "status": "green",
  "units": 115,
  "unitLabel": "finding ids checked",
  "detail": "115 inventory id(s) checked; per-kind: milestone 104, phase 11; per-milestone: M1 11, M2 16, M3 74, M4 13, M5 1",
  "evidence": ["counts.json"]
}
```

### 5.2 coverage, AFTER

AFTER_COVERAGE_BLOCK

**The per-milestone line changed the way it was meant to.** Reading the two
details side by side, and this is the comparison a green-after alone does not
support:

| view | before | after | intended |
|---|---|---|---|
| `units` | 115 | 115 | unchanged, no row added or removed |
| per-kind `milestone` | 104 | 98 | minus the six that left a milestone bucket |
| per-kind `decision` | absent | 6 | the `decision` kind's FIRST users in this repository |
| per-kind `phase` | 11 | 11 | unchanged, the M1 rows |
| per-milestone `M4` | 13 | **5** | the disposition's number |
| per-milestone `M5` | 1 | **3** | R-026a plus R-072 and R-097 |
| per-milestone `decision` | absent | 6 | a kind with no milestone capture group buckets by its own name |

### 5.3 clause-map, BEFORE and AFTER

The phase section says the earlier analysis did not run this gate and that the
intake claims it asserts the same counts. It does not. Run at the beacon head:

```
$ node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full \
    --only clause-map --evidence /tmp/m4p13-ev/before-clausemap \
    --base origin/main --head HEAD
gates: run 21e98dc348c000afd67fba9c
gates: registry gate-registry.yaml mode full
gates: declared 1 applicable 1 verdict 1 green 1 red 0 not-applicable 0 error 0 vacuous 0
gates: clause-map: green: 74 rows checked, 0 pending a phase not yet in force
gates: every applicable gate is green
GATE_EXIT=0
```

AFTER_CLAUSEMAP_BLOCK

**Why it cannot see these counts, measured rather than argued.** Its two inputs
are Appendix A of delivery/plan/kernel-plan-m3.md and
delivery/requirements/clause-map.json, and it is entirely M3-scoped:

```
$ grep -c M4 delivery/requirements/clause-map.json
0
$ grep -nE '"(R-001a|R-041|R-042|R-045|R-046|R-047|R-050a|R-051|R-064|R-065a|R-071|R-072|R-097)"' delivery/requirements/clause-map.json
(no output, exit 1)
```

Not one of the thirteen rows appears in it. That is why
`delivery/requirements/clause-map.json` is on this phase's files-to-touch list
and is NOT edited: the pre-pass grants it to M4-P13 exclusively so that no
other wave-1 unit may edit it, and the measurement says there is nothing in it
to edit. The grant is declared and unused, which is the honest outcome.

## 6. Red witnesses

Two classes, two structurally different members each, every capture from a
real run with its exit code.

### 6.1 Class A: the two documents disagree while every gate stays green

This is the phase's real red witness and the plan names it as hazard H-B.

**The MECHANISM, not the finding.** The finding would be "the migration table
could say M4 while Appendix A says DR-0029". The mechanism is that
`extractIdRows` keeps the FIRST CELL of a table row and discards the rest
(src/gates/coverage.ts:356), so the coverage gate reads the migration table for
IDS ONLY, and every bucket comes from the coverage table. The Milestone column
is not an input to any gate at all. A guard whose condition does not test the
property that matters is green and worthless.

Both members are seeded into a scratch copy of the tree exported with
`git archive HEAD`, never on disk in the worktree.

**Control, unmutated scratch copy:**

```
$ node --test --test-name-pattern 'Appendix A bucket agrees' test/coverage-gate.test.ts
+ every Appendix A bucket agrees with the migration table's milestone cell for the row, and the decision-bucketed rows agree verbatim (1367.988617ms)
i tests 1
i suites 0
i pass 1
i fail 0
i cancelled 0
i skipped 0
i todo 0
i duration_ms 1591.381561
EXIT=0
```

**Member (i): a row THIS PHASE MOVED.** R-072's migration-table cell reverted
to M4 while Appendix A keeps M5. First, the gate against it:

```
$ node src/gates/coverage.ts --result /tmp/m4p13-ev/red-m1/result.json --evidence /tmp/m4p13-ev/red-m1
coverage: green (115 finding ids checked)
115 inventory id(s) checked; per-kind: decision 6, milestone 98, phase 11; per-milestone: M1 11, M2 16, M3 74, M4 5, M5 3, decision 6
GATE_EXIT=0
```

Green, with the **identical** detail to the correct tree. That is the danger.
Now the new test:

```
$ node --test --test-name-pattern 'Appendix A bucket agrees' test/coverage-gate.test.ts
x every Appendix A bucket agrees with the migration table's milestone cell for the row, and the decision-bucketed rows agree verbatim (324.465712ms)
i tests 1
i pass 0
i fail 1

x failing tests:

test at test/coverage-gate.test.ts:988:1
x every Appendix A bucket agrees with the migration table's milestone cell for the row, and the decision-bucketed rows agree verbatim (324.465712ms)
  AssertionError [ERR_ASSERTION]: Expected values to be strictly deep-equal:
  + actual - expected

  + [
  +   'R-072: Appendix A bucket M5 implies milestone cell M5, migration table says M4'
  + ]
  - []
EXIT=1
```

**Member (ii): a row THIS PHASE NEVER TOUCHED.** R-026a has been M5 in both
documents since v1; its Appendix A bucket drifts to M4. Fresh scratch tree, so
member (i)'s mutation is not present.

```
$ node src/gates/coverage.ts --result /tmp/m4p13-ev/red-m2/result.json --evidence /tmp/m4p13-ev/red-m2
coverage: green (115 finding ids checked)
115 inventory id(s) checked; per-kind: decision 6, milestone 98, phase 11; per-milestone: M1 11, M2 16, M3 74, M4 6, M5 2, decision 6
GATE_EXIT=0

$ node --test --test-name-pattern 'Appendix A bucket agrees' test/coverage-gate.test.ts
x every Appendix A bucket agrees with the migration table's milestone cell for the row, and the decision-bucketed rows agree verbatim (916.546349ms)
i tests 1
i pass 0
i fail 1

  AssertionError [ERR_ASSERTION]: Expected values to be strictly deep-equal:
  + actual - expected

  + [
  +   'R-026a: Appendix A bucket M4 implies milestone cell M4, migration table says M5'
  + ]
  - []
EXIT=1
```

**Why these two are structurally different rather than the same shape twice.**
Member (i) is a half-finished re-disposition on a row this change moved, in the
direction the phase was working; its distribution is the CORRECT one, so no
count anywhere is wrong. Member (ii) is drift on a row this change never
looked at, and it does move the distribution (M4 6, M5 2) while still leaving
the gate green. One is invisible to every count; the other is visible to a
count that no gate compares against anything. A witness that only covered the
first would say nothing about rows a later phase touches.

### 6.2 Class B: a partial re-disposition read through one number

**The MECHANISM.** Six separate `assert.equal` calls over one report abort at
the first mismatch, so the later five never execute and the failure names one
number. A reader then learns that `milestone` moved and nothing about where the
rows went.

**Measured, not asserted.** The pre-change test file (from `HEAD~1`) placed on
a tree whose documents ARE fully re-dispositioned:

```
$ node --test --test-name-pattern 'units 115 with per-kind' test/coverage-gate.test.ts
x the coverage gate against the real migration table and appendix reports units 115 with per-kind and per-milestone breakdowns matching the plan (1896.80443ms)
i tests 1
i pass 0
i fail 1

test at test/coverage-gate.test.ts:146:1
  AssertionError [ERR_ASSERTION]: Expected values to be strictly equal:

  98 !== 104

      at TestContext.<anonymous> (file:///tmp/m4p13-oldpins/test/coverage-gate.test.ts:156:10)
EXIT=1
```

It stops at line 156, `perKind["milestone"]`. The M4, M5 and `decision`
assertions never run at all. The plan's criterion 2 predicted "five passing and
one failing"; measured, it is worse than that, because execution stops and the
distribution is never computed.

The replacement is ONE `assert.deepEqual` over one literal object. Two
structurally different partial re-dispositions against it:

**Member (i): one row left behind in the DECISION direction.** R-071 never
moved, consistently in BOTH documents, so the cross-document test in 6.1 is
green and only the count is wrong.

```
$ node --test --test-name-pattern 'units 115 with per-kind' test/coverage-gate.test.ts
  AssertionError [ERR_ASSERTION]: Expected values to be strictly deep-equal:
  + actual - expected

    {
      perKind: {
  +     decision: 5,
  +     milestone: 99,
  -     decision: 6,
  -     milestone: 98,
        phase: 11
      },
      perMilestone: {
        M1: 11,
        M2: 16,
        M3: 74,
  +     M4: 6,
  -     M4: 5,
        M5: 3,
  +     decision: 5
  -     decision: 6
      },
      totalInventoryIds: 115
    }
EXIT=1
```

**Member (ii): one row left behind in the MILESTONE direction.** R-097 never
moved. `perKind` is IDENTICAL to the correct tree here, which is what makes
this a different member and not a repeat: the whole signal is inside
`perMilestone`.

```
$ node --test --test-name-pattern 'units 115 with per-kind' test/coverage-gate.test.ts
  AssertionError [ERR_ASSERTION]: Expected values to be strictly deep-equal:
  + actual - expected
  ... Skipped lines

    {
      perKind: {
        decision: 6,
        milestone: 98,
        phase: 11
  ...
        M3: 74,
  +     M4: 6,
  +     M5: 2,
  -     M4: 5,
  -     M5: 3,
        decision: 6
      },
      totalInventoryIds: 115
    }
EXIT=1
```

Both print the whole distribution in one failure. The reader sees which
direction the re-disposition went incomplete without re-running anything.

### 6.3 The exemption list is declared, and asserted BY NAME

Criterion 3 requires the decision-bucketed rows to be a declared list rather
than a skip. The test declares the six ids literally and asserts each one is
still non-milestone-bucketed. It does NOT assert the list has six members,
because the decision-bucketed set grows as later phases re-disposition rows and
a count is a claim about every future phase. The normalization is also stricter
than a skip: a decision or parked bucket must match the migration table's cell
VERBATIM, so the six DR-0029 rows are required to say DR-0029 in BOTH documents
rather than being excused from comparison.

`compared` is asserted to be 115, so the test cannot pass by having quietly
skipped rows.

## 7. Scope: what I did NOT touch, and why

- **`delivery/plan/kernel-plan-m4.md`.** The phase section lists it, and
  delivery/plan/m4-conflict-pre-pass.md:19 does not. The brief says the
  pre-pass is what says what I may touch, and criterion 6 asks that the
  thirteen-row disposition table be committed there before the first M4 gate
  phase dispatches. **It already is**, at
  delivery/plan/kernel-plan-m4.md:2168, on the branch this one is cut from, so
  the criterion is met without an edit. Recording the outcome there would be
  an improvement and is left to the orchestrator; making it here risks a
  collision the pre-pass did not clear.
- **`delivery/requirements/clause-map.json`.** Granted exclusively, measured
  empty of M4 content (section 5.3), left alone.
- **`delivery/plan/kernel-plan-v1.md:368`**, the M4 paragraph. Another unit's.
- **Any source file.** The brief forbids it and none was needed.
- **Sites 6 to 10** in section 2.2, for the reasons given there.

## 8. Toolchain, build state and invocation

**The complete sentence, all three axes.** The container default is node
v22.22.2 at `/opt/node22/bin` and the declared floor is `>=26`. I fetched a
floor-satisfying toolchain rather than reason about skips:
`node-v26.6.0-linux-x64.tar.xz` from nodejs.org, extracted to a scratch prefix,
`bin` put first on PATH. `npm --version` reports 11.18.0 under it and `npm ci`
produced no EBADENGINE line.

**The scratch-prefix trap bit, and the fix is recorded because the standing
warning's own remedy was not enough.** CLAUDE.md warns that
`runCliUnprivileged` (test/gates.test.ts:3530) drops to an unprivileged uid and
spawns `process.execPath`, which is unreachable when the interpreter sits under
a `drwx------` directory, and that a clone outside `/tmp` does not get traversal
opened for it incidentally. This worktree is under `/home/user`, so it bit:

```
    actual: Error: spawnSync /tmp/claude-0/.../scratchpad/m4p13/node-v26.6.0-linux-x64/bin/node EACCES
        at runCliUnprivileged (file:///.../test/gates.test.ts:3538:10)
TEST_EXIT=1
```

`chmod o+x` on the chain was NOT a durable fix: re-reading the chain minutes
later showed `/tmp/claude-0` back at `drwx------`, so something in the harness
resets it. The fix that held was to install the toolchain outside the
session scratch prefix entirely, at `/tmp/m4p13-node26`, whose whole chain is
world-traversable:

```
$ namei -m /tmp/m4p13-node26/bin/node
 drwxr-xr-x /
 drwxrwxrwt tmp
 drwxr-xr-x m4p13-node26
 drwxr-xr-x bin
 -rwxr-xr-x node
```

This is an environment property, not a branch property: the same tree passes
under `/opt/node22/bin/node` and under this interpreter, and failed only under
one whose PATH was unreachable to an unprivileged uid.

### 8.1 BASE, before any edit

Head `bd92a01`, interpreter node v26.6.0, `dist/` BUILT (`npm run build` exit
0, `git status` clean afterwards), invocation `npm test`:

```
i tests 849
i suites 0
i pass 849
i fail 0
i cancelled 0
i skipped 0
i todo 0
i duration_ms 620781.778687
TEST_EXIT=0
```

**849 tests, 849 pass, 0 fail, 0 SKIPPED, exit 0.** The base is green, so any
failure after the change is attributable to the change.

### 8.2 AFTER

AFTER_SUITE_BLOCK

### 8.3 The single changed test file

```
$ node --test test/coverage-gate.test.ts
i tests 19
i suites 0
i pass 19
i fail 0
i cancelled 0
i skipped 0
i todo 0
i duration_ms 13547.829584
EXIT=0
```

19 tests, 19 pass, 0 fail, 0 skipped, exit 0. Seventeen before, nineteen after.

### 8.4 ASCII and authored bytes

```
$ node scripts/check-authored-bytes.mjs
EXIT=0
```

The script refuses to run against a tree that differs from the index, which is
worth knowing: the first invocation exited 2 with "tracked working tree differs
from the index; stage or revert it before checking authored bytes", and the run
above is after staging.

### 8.5 citations

CITATIONS_BLOCK

## 9. What the derivation did NOT cover

Stated because a search whose scope is wrong returns an empty result that is
indistinguishable from an absence of defects.

1. **UNTRACKED files.** All three derivation greps are `git grep`, which reads
   the index. A pinned count in an untracked file would not appear. This is
   deliberate: an untracked file is not part of the delivery.
2. **A count written as WORDS, or split across a line wrap.** The greps match
   digits adjacent to their labels. "thirteen M4 rows" in prose, or a table
   whose `13` and whose `M4` land on different lines, is invisible to them. I
   did not find such a site, and I did not prove none exists.
3. **A count computed at run time from a different pair of documents.** The
   third grep covers `src/`, `test/`, `scripts/`, `bin/` and the root-level
   `*.yaml` and `*.json`. A consumer under `roles/`, `checklists/`,
   `templates/` or `witness/` that derives the distribution itself would be
   outside it. Those trees are prose and specification rather than executable
   readers of the coverage report, which is why I scoped it that way, and it is
   a scope choice rather than a proof.
4. **The M4 paragraph at delivery/plan/kernel-plan-v1.md:368** enumerates the
   seven gate rows by id and is now inconsistent with their buckets. That is
   KNOWN and is another unit's work by the M4 plan's own assignment. I did not
   check whether any OTHER prose paragraph enumerates the same rows.
5. **CI.** Every capture in this file is local. The `pull_request` and `push`
   arms of the `gates` workflow run different bundles and I observed neither.
6. **The scope gate.** It reads the phase declaration from the MERGE BASE, and
   `delivery/plan/phase-declarations/m4-p13.json` does not exist there yet, so
   it cannot pass on this branch today. That is the condition the M4 plan
   describes at delivery/plan/kernel-plan-m4.md:2070 and it is the
   orchestrator's to resolve, not a defect in this work.
7. **Whether `decision` as a bucket kind has any downstream consumer beyond
   the coverage gate.** It is the kind's first use in this repository. The full
   suite is green, which is evidence and not a proof over code paths no test
   reaches.

## 10. The claim grep

CLAIM_GREP_BLOCK
