# M2-P1 work history: gate contract, manifest, runner, and run pinning

Branch: `claude/m2-p1-gate-contract-and-runner`
Base: `main` = 037477e
Started: 2026-08-06

This file is written INCREMENTALLY (T-008 dispatch contract). Its mtime is the
liveness beacon for this phase.

## Running log

- Created worktree beacon; reading mandatory materials (CLAUDE.md, MECHANISMS.md,
  kernel-plan-m2.md M2-P1, implementer-brief, T-003/T-006/T-007/T-008, STATE.md).

## Step 1: M2-C-1 verification-first check (recorded BEFORE any code)

The plan assumes four delivered M1 contracts. Each was checked against the tree
at `main` = 037477e, verbatim, before writing a line.

### 1a. `src/cli.ts` dispatch shape and EX_USAGE

```
$ grep -n "EX_USAGE\|CommandHandler\|commands = new Map\|export async function run" src/cli.ts
10:/** Exit code for usage errors, per BSD sysexits EX_USAGE. */
11:export const EX_USAGE = 64;
13:type CommandHandler = (args: string[]) => Promise<number> | number;
18:const commands = new Map<string, CommandHandler>([
37: * line to stderr and returns EX_USAGE.
39:export async function run(argv: string[]): Promise<number> {
44:    return EX_USAGE;
```

MATCHES the plan's assumption. `EX_USAGE` is exported and is 64; the dispatch
table is a `Map<string, CommandHandler>` with handler signature
`(args: string[]) => Promise<number> | number`; an unknown subcommand prints
the usage line to stderr and returns 64. Registering `gates` is a one-line
addition to that Map plus an import. No divergence.

### 1b. `src/task.ts` M2-C-6 helpers: exported names and signatures

```
$ grep -n "^export function classifyEntry\|^export function refuseOpenForWrite\|^export function readRegularFileIfPresent\|^export type EntryClass\|^export type RegularRead" src/task.ts
81:export type EntryClass =
118:export function classifyEntry(path: string): EntryClass {
158:export function refuseOpenForWrite(path: string): string | undefined {
167:export type RegularRead =
179:export function readRegularFileIfPresent(path: string): RegularRead {
```

MATCHES, with one correction to the plan's wording that is worth recording
rather than passing over. Section 1.1's table and step 7 name `classifyEntry`
and `refuseOpenForWrite`; the dispatch brief names `classifyEntry` and
`readRegularFileIfPresent`. All THREE exist and are exported:

- `classifyEntry(path: string): EntryClass` where `EntryClass` is
  `absent | dangling | regular | irregular{reason} | unexaminable{reason}`.
- `refuseOpenForWrite(path: string): string | undefined` (reason, or undefined
  when the path may be opened, absent included).
- `readRegularFileIfPresent(path: string): RegularRead` where `RegularRead` is
  `read{body} | absent | refused{reason}`, a UTF-8 text read.

No divergence; the plan named a subset. This phase uses all three and writes no
second implementation of the probe (T-005).

### 1c. Workflow job layout and check-run names

```
$ grep -n "^  [a-z-]*:" .github/workflows/gates.yml
14:  test:
89:  gates:
```

Two jobs: `test` (matrix `node: [26]`) and `gates` (`needs: test`, `if:
always()`), and no `env:` block:

```
$ grep -n '^\s*env:' .github/workflows/gates.yml
(no output, exit 1)
```

**On check-run names, restated as what it is.** GitHub's documented behaviour
is that check-run contexts come from job names and matrix values rather than
from step names, which would make the published contexts `test (26)` and
`gates` and leave DR-0004's required context untouched by a step addition.
That is DOCUMENTATION, not a measurement, and it is not readable from this
tree. **The measurement is criterion 14 and it is CI-DEFERRED.** What IS
established here is only the structure: two jobs, steps added inside the
existing one, no job added.

```
$ command -v gh || echo "gh ABSENT"
gh ABSENT
```

```
$ command -v gh || echo "gh ABSENT"
gh ABSENT
```

### 1d. `test/behaviors.json` shape

```
$ node -e "const b=JSON.parse(require('fs').readFileSync('test/behaviors.json','utf8')); console.log('keys:',Object.keys(b).length)"
keys: 162
```

A flat JSON object mapping behavior id to a one-line description. Append-only,
union-mergeable, 162 keys at the base. No divergence.

### Verdict

**No divergence between the delivered M1 contracts and what the plan assumes.**
Nothing to escalate under M2-C-1. One clarification recorded (1b: three helpers,
not two).

### Observation raised, not acted on

`delivery/STATE.md` at this base still carries the owner's HARD STOP at the M1
boundary ("Do not dispatch M2-P1"). The dispatch that created this phase came
from the orchestrator, which holds dispatch authority and states M1 is complete
with exit evidence merged at `7e1b5f1`. STATE.md's own preamble says that when
it disagrees with reality, reality wins. This is recorded so it is visible to
the reviewer rather than assumed away; the implementer does not hold the
authority to resolve it and did not treat it as blocking.

## Step 5: verification-first sub-step, MEASURED not assumed

Question: does `tsc -b` at the pinned compiler emit `src/gates/schemas/*.json`
into `dist/`, or is an explicit copy step required?

```
$ rm -rf dist && npm run build     # tsc -b only, before any copy step was added
build exit=0
$ ls -la dist/src/gates/
manifest.d.ts manifest.js pin.d.ts pin.js result.d.ts result.js
run.d.ts run.js validate.d.ts validate.js
$ find dist -name '*.json'
(no output)
```

**MEASURED: tsc emits no JSON.** `dist/src/gates/schemas/` did not exist and no
`.json` file appeared anywhere under `dist/`. An explicit copy step is
required, so one was added:

```
"build": "tsc -b tsconfig.src.json tsconfig.test.json && npm run build:schemas",
"build:schemas": "node --input-type=module -e \"import { cpSync } from 'node:fs'; cpSync('src/gates/schemas', 'dist/src/gates/schemas', { recursive: true });\""
```

The `files` allowlist needed NO change: it is already `["dist"]`, and the
copied documents land under `dist/`. Confirmed after the copy step:

```
$ rm -rf dist && npm run build; ls dist/src/gates/schemas/
build exit=0
gate-manifest.schema.json
gate-result.schema.json
```

Schemas are resolved at runtime from `import.meta.url`, not imported as
modules, which is checked rather than asserted:

```
$ grep -rn 'from ".*\.json"\|require(.*\.json\|resolveJsonModule' src/ tsconfig.src.json tsconfig.test.json
src/gates/manifest.ts:27: * They are deliberately NOT imported as modules: `resolveJsonModule` is off
(the only hit is that comment; no import, no require, no compiler option)
```

Turning `resolveJsonModule` on would INLINE the schema into the compiled
bundle, which would make the document shipped in the package and the document
actually enforced two different things. Both entry forms were then exercised and resolve their own copy:

```
$ node bin/tiphys.ts gates self-check --manifest gates.manifest.json --result /tmp/sc.json
manifest-self-check: green (3 schema documents validated)
... src/gates/schemas/gate-manifest.schema.json, src/gates/schemas/gate-result.schema.json ...
exit=0
$ node dist/bin/tiphys.js gates self-check --manifest gates.manifest.json --result /tmp/sc2.json
manifest-self-check: green (3 schema documents validated)
... dist/src/gates/schemas/gate-manifest.schema.json, dist/src/gates/schemas/gate-result.schema.json ...
exit=0
```

## Step 6: the destructiveCommands derivation, published in full

CLAUDE.md's fix-round contract item 2 requires the DERIVATION, not a summary.
The plan seeds the list with `pool destroy`, `teardown`, "and any command
accepting `--discard`, `--force` or `--delete-branch-force`". The third clause
is the one that needs deriving rather than asserting.

Derivation command and its FULL output:

```
$ grep -rn -- 'flag === "--discard"\|flag === "--force"\|flag === "--delete-branch-force"\|flag === "--salvage"' src/commands/
src/commands/pool.ts:51:    } else if (flag === "--discard") {
src/commands/pool.ts:53:    } else if (flag === "--delete-branch-force") {
src/commands/teardown.ts:43:    } else if (flag === "--salvage") {
```

Cross-checked against the closed set of accepted flags, which every command
states in its own usage line:

```
$ grep -rn 'usage: tiphys' src/commands/*.ts
src/commands/doctor.ts:364:      process.stderr.write("usage: tiphys doctor [--for <profile>]\n");
src/commands/init.ts:52:    process.stderr.write("usage: tiphys init <dir>\n");
src/commands/lock.ts:24:  "usage: tiphys lock <acquire [--take-over] [--duration <seconds>] | " +
src/commands/pool.ts:14:  "usage: tiphys pool <create --task <id> --project <path> [--offline] | " +
src/commands/spawn.ts:29:  "usage: tiphys spawn --task <id> --project <path> --brief <file> " +
src/commands/teardown.ts:20:const USAGE = "usage: tiphys teardown --task <id> [--salvage]";
src/commands/watch.ts:51:  "usage: tiphys watch [--once] [--interval <seconds>] [--poll <seconds>] " +
```

Result: exactly ONE command parses a destructive flag, `pool destroy`
(`--discard`, `--delete-branch-force`). No command parses a bare `--force`.
`teardown` parses `--salvage` and is seeded by name because it destroys a
worktree and a branch regardless of flags. The seeded list is therefore:

    "pool destroy", "teardown", "src/pool.ts", "src/teardown.ts"

the last two being the module paths M2-P2 rule (e) matches a behavior name
against.

**WHAT THIS DERIVATION DOES NOT COVER**, stated because a search whose scope is
wrong returns an empty result indistinguishable from an absence of defects:

1. It searched `src/commands/` only, which is where flags are PARSED. A
   destructive operation reachable without a flag, or a flag parsed elsewhere,
   is outside it. `src/pool.ts` and `src/teardown.ts` mention the same flags
   (they construct git argv from them) but parse none; that was checked by the
   wider `grep -rn -- '--discard|--force|--delete-branch-force' src/`, whose
   hits outside `src/commands/` are all comments or git argv construction.
2. It does not cover `scripts/`, which holds shell harnesses, nor anything a
   gate subprocess may invoke. Those are not kernel CLI commands and the
   manifest field is documented as a list of CLI commands and module paths.
3. It cannot see a command that will become destructive LATER. That is the
   half kernel plan M2 section 2 item 11 hands to M3-P6's implementer brief
   ("add the command to the manifest's destructiveCommands list"), and M2
   claims only the machine-readable list.

## Deviations from the plan's letter, declared rather than silent

Three, each with the reason and what would falsify it.

### D1. One manifest field the plan does not list: `parameters`

Step 7 requires that "a gate whose command requires `--phase` reports `error`
when that parameter is absent". The runner can learn that in exactly two ways:
the gate declares it, or the runner pattern-matches the gate's command line.
`MECHANISMS.md`'s row "Deciding what another program will do by
pattern-matching the text of a file it consumes" records four M1-P6 rounds
paid for the second option, and its rule is to PIN the accepted shapes and
fail closed. So `gates[].parameters` is a closed enum (`base`, `head`,
`phase`), validated by the schema, read by the runner, and passed through to
the gate's argv. Falsifier: if a reviewer can show a way for the runner to
derive the requirement without reading either a declaration or the command
text, this field is unnecessary.

### D2. One result field the plan does not list: `vacuous`

Step 8 requires the summary to count `vacuous` as a strict subset of `error`.
After the M2-C-2 rewrite has fired, a vacuous error and an ordinary error have
the same status, so the runner would have to recover the distinction from the
detail TEXT of a record another program wrote. Same mechanism row, same rule.
`vacuous` is therefore an optional boolean on the record, set only by the two
places that perform the rewrite.

### D3. `manifest-self-check` is a subcommand, `tiphys gates self-check`

The plan names the gate and requires it to validate every schema document and
the manifest, but does not say where it lives. It lives in
`src/commands/gates.ts`, which is on the files-to-touch list, rather than in a
new script, which is not.

Two readings recorded so a reviewer is not left to guess whether they were
noticed:

- **Criterion 5's fixture carries a second, applicable gate.** Read with a
  single-gate manifest, criterion 5's `conditional` direction ("the runner
  exits 0") and criterion 9 ("a manifest whose every gate is not-applicable
  exits nonzero with reason `no applicable gate`") contradict each other.
  Criterion 9 is the aggregate rule and names its authority (M2-C-2 at the
  aggregate level, M2R-012), so criterion 5's manifest must contain something
  applicable. It does.
- **"No record is written for a gate that was not executed" (step 8) means
  not REACHED, not "did not run its command".** Criterion 2 requires four
  records including the `not-applicable` one, and a precondition that was
  evaluated and found unmet is a measurement. The runner writes a record for
  every gate it evaluated, and none for a gate excluded by `--only`.

## Red-witness table

Every witness below was demonstrated red against the DANGEROUS state, not
against the absent feature, by copying the target file out of tree, applying
the defang, running the named test with `--test-name-pattern` BEFORE the
positional path (warning 7), and restoring by copying back with a sha256
equality check. Warning 8 says `git checkout --` is destructive even naming a
single path, so the harness does not use git at all to restore:

```
$ grep -n 'checkout\|git ' ../rw/defang.py
4:Copies the target file out of tree (CLAUDE.md warning 8: git checkout -- is
(the only hit is that docstring line)
$ grep -n 'copy2' ../rw/defang.py
24:        shutil.copy2(src, dst)
47:        shutil.copy2(dst, os.path.join(REPO, t))
```

| id | dangerous state | test | result |
|---|---|---|---|
| W1 | the runner's M2-C-2 ingest rewrite disabled | gate-vacuous-green-is-error | RED, exit 1 |
| W2 | the constructor's M2-C-2 rewrite disabled (the OTHER enforcement point) | gate-constructor-refuses-vacuous-green | RED, exit 1 |
| W3 | a crash with no record classified `red` instead of `error` | gate-crash-without-record-is-error | RED, exit 1 |
| W4 | an unrunnable precondition command classified `unmet` instead of `error` | gate-precondition-unevaluable-is-error | RED, exit 1 |
| W5 | `requiredParameters` emptied | gate-missing-run-parameter-is-error | **GREEN, see below** |
| W5b | `requiredParameters` emptied | gate-declared-parameter-missing-is-error (NEW) | RED, exit 1 |
| W5c | `requiredParameters` ignores the declaration (a different member) | gate-declared-parameter-missing-is-error | RED, exit 1 |
| W6 | the zero-applicable-gates rule disabled | gate-zero-applicable-is-error | RED, exit 1 |
| W7 | a required not-applicable gate no longer fails the run | gate-precondition-required-vs-conditional | RED, exit 1 |
| W8 | the `applicable` counter double-counts | gate-runner-status-mapping | RED, exit 1 |
| W9 | no record written for a not-applicable gate (a different bucket defect) | gate-runner-status-mapping | RED, exit 1 |
| W10 | the exit-code versus record-status check disabled | gate-exit-code-record-mismatch-is-error | RED, exit 1 |
| W11 | M2-C-6 at the precondition target: irregular read as absent | gate-unprobed-open-refused | RED, exit 1 |
| W12 | M2-C-6 pre-spawn record probe removed | gate-unprobed-open-refused | RED, exit 1 (not a hang, see below) |
| W12b | pre-spawn probe AND stale-record clear removed | gate-unprobed-open-refused | **RED by TIMEOUT, exit 124 after 40s** |
| W13b | the manifest read bypasses `readRegularFileIfPresent` | gate-unprobed-open-refused | **RED by TIMEOUT, exit 124 after 40s** |
| W14 | unknown schema keywords silently ignored | gate-schema-closed-keyword-set-loud | RED, exit 1 |
| W15 | the merge sort in `validateManifestDocument` removed | gate-diagnostic-order-deterministic | **GREEN, see below** |
| W15b | the contract sort in `validate()` removed | gate-diagnostic-order-deterministic | GREEN first, RED after the test was strengthened |
| W15c | the merge sort removed, against the strengthened test | gate-diagnostic-order-deterministic | RED, exit 1 |
| W16 | the `required` diagnostic removed | gate-manifest-missing-id-diagnostic | RED, exit 1 |
| W17 | the `enum` diagnostic removed | gate-result-status-enum-diagnostic | RED, exit 1 |
| W18 | the pin stops comparing `mtimeMs` (the T-004 shape walks past) | pin-mtime-only-difference | RED, exit 1 |
| W19 | the pin stops reporting added and removed paths | pin-added-and-removed | RED, exit 1 |
| W20 | `process.kill` introduced into the runner (C-2) | gate-no-process-probing | RED, exit 1 |
| W21 | `detached: true` introduced into the runner (C-3) | gate-no-process-probing | RED, exit 1 |
| W22 | the comment strip eats the whole file (the control on the control) | gate-no-process-probing | RED, exit 1 |
| W23 | unknown CLI flags accepted | gate-usage-errors | **GREEN, see below** |
| W23b | unknown CLI flags accepted, against the isolated test | gate-usage-errors | RED, exit 1 |
| W23c | a value flag with no value accepted | gate-usage-errors | GREEN, see the scope note below |
| W24 | the CI bundle step DELETED | gate-bundle-step-wired-and-falsifiable | RED, exit 1 |
| W25 | `\|\| true` appended to the CI step, every asserted string PRESERVED | gate-bundle-step-wired-and-falsifiable | RED, exit 1 |
| W26 | the CI step's command prefixed with `echo`, argv text preserved | gate-bundle-step-wired-and-falsifiable | RED, exit 1 |
| W27 | the schema copy step removed from `npm run build`, `dist/` rebuilt | gate-dist-entry-resolves-schemas, gate-pack-contains-schemas | RED, both, exit 1 |

### One witness is not a class: which classes have two structurally different members

| class | member 1 | member 2 | member 3 |
|---|---|---|---|
| M2-C-2 (never green by omission) | W1, at the runner's ingest | W2, at the constructor | W6, at the aggregate level |
| M2-C-6 (establish the type before opening) | W11, precondition target | W12b, record path (hang) | W13b, manifest path (hang) |
| C-2 and C-3 structural | W20, a signal call | W21, a detached child | W22, the strip's own control |
| the diagnostic ordering contract | W15b, the schema walk's sort | W15c, the merged list's sort | |
| the CI step, guard-that-asserts-text | W24, deletion | W25, `\|\| true`, text preserved | W26, `echo` prefix, text preserved |
| the runner's counters | W8, a bucket that double-counts | W9, a record that is not written | |

### Three witnesses came back GREEN, and each one was a real finding

This is the part of the campaign that mattered. In every case the PROPERTY
survived the defang because a SECOND guard held it, while the mechanism the
test was supposed to guard was gone.

1. **W5.** `requiredParameters` could be emptied entirely and
   `gate-missing-run-parameter-is-error` stayed green, because
   `evaluatePrecondition` re-checks `--base` for `diff-touches` on its own
   account. The uncovered path is a gate that DECLARES `parameters` and has no
   precondition, which reaches only the first guard. That arm had no test at
   all (T-006's "an arm no test reaches is a reportable state"). A test was
   added and W5b and W5c redden it. The duplication itself is left in place
   and is defence in depth, recorded here so a later reader does not remove
   one half believing the other is the only one: **W5d confirms the
   `diff-touches` arm stays green with `requiredParameters` emptied.**
2. **W15.** The ordering test measured that ten runs agreed with each other,
   which they would have done with no sort at all, and neither of the two
   sorts could be deleted visibly: `validateManifestDocument` re-sorts what
   `validate()` already sorted. The test now measures both producers, one
   through a direct `validateToLines` call and one through a fixture whose two
   diagnostics come from different producers and must interleave. This matters
   beyond this phase: the ordering IS the DR-0013 clause 6 contract, and
   without this the promise that M3-P1 can swap Ajv in and re-run these tests
   unchanged would have rested on a test that could not tell an ordered list
   from an unordered one.
3. **W23.** `gates run --no-such-flag x` exits 64 even when unknown flags are
   accepted, because the same invocation is also missing `--manifest` and
   `--evidence`. The test now runs a complete, valid invocation with one
   unknown flag added, and W23b reddens it.

**W12 deserves its own note, because the first version of this work history
would have overstated it.** Removing the pre-spawn `refuseOpenForWrite` alone
does NOT hang: the runner's next step clears any stale record with
`rmSync(..., { force: true })`, which deletes the named pipe, and the gate
then writes an ordinary file. The test still reddens, but by the wrong
mechanism. W12b removes BOTH and the run hangs, `timeout 40` kills it at
exit 124. So the claim "a gate handed a named pipe to write into would block
forever" is settled by construction rather than asserted, and the honest
version of it is that TWO things stand between the runner and that block.

### Scope note on W23c, a negative result stated with its scope

`--evidence` with no value following it is accepted by the defanged parser and
the run still exits 64, because the required-flag check fires instead. The
outcome the criterion names (exit 64) is guarded; the specific parse rule is
not isolated by any test. The search that established this was a single defang
of one branch of `parseFlags`; it did not enumerate every parse path, and a
reviewer should treat "the parser is fully witnessed" as UNPROVEN.

## Pre-submit hazard self-review (binding)

**The plan's declared hazard class for M2-P1:** the spine every later record
rests on, so a defect here is invisible in every gate downstream. Named
shapes: a runner that writes a record for a gate it did not execute; a counter
whose buckets double-count or hide one another; a pin that passes a tree that
changed; a status that can be constructed as green with nothing examined; and
a configured path that is not a regular file and blocks the runner instead of
reporting.

The question the brief calls the highest-yield one in this repository, asked
of this component: **can the runner report a green bundle while the gates it
reports on are broken?** Five attacks, and what each found.

### Attack 1: can a green bundle be produced with nothing examined?

Attacked at three levels because M2-C-2 has three of them.

- Gate level, constructor: `makeGateResult` with green and units 0. FOUND
  NOTHING WRONG, and the arm had no test; one was added (W2). Also probed
  negative and fractional unit counts, which are now folded to zero rather
  than accepted as a smaller measurement.
- Gate level, ingest: a fixture gate that does not use the constructor and
  writes green with units 0 by hand. FOUND NOTHING WRONG (W1).
- Bundle level: a manifest with zero gates, and a manifest whose every gate is
  not-applicable. FOUND NOTHING WRONG (W6).

**Scope of the negative:** these three are the enforcement points that exist.
A gate that reports units 1 having examined nothing is NOT caught by anything
here. **Restated as an open question rather than as an impossibility:** I did
not find a way for the runner to audit a gate's own unit count without redoing
the gate's work, since `units` is the gate's claim about its own labour and the
runner has no independent view of it. If a reviewer finds one, that is a
finding against this design. The residue as it stands belongs to each gate's
own criteria.

### Attack 2: can a record exist for a gate that did not run?

The runner writes a record for every gate it EVALUATED and none for a gate
excluded by `--only`. Attacked by defanging the write so a not-applicable gate
leaves no record (W9, RED) and by making the `applicable` counter increment
unconditionally (W8, RED). Also checked by hand that `--only` naming an
unknown id is an error rather than a silently smaller run, and that
`counts.declared` is the SELECTED set with `manifestGates` recorded alongside
it so a reader is not left to infer which one a count refers to:

```
$ node bin/tiphys.ts gates run --manifest gates.manifest.json --evidence /tmp/ev3
$ node -e 'const s=require("/tmp/ev3/summary.json"); console.log(JSON.stringify({manifestGates:s.manifestGates, declared:s.counts.declared, only:s.only}))'
{"manifestGates":1,"declared":1,"only":[]}
```

**Scope of the negative:** a gate that writes a record and then crashes before
exiting reddens the exit-code/record consistency check, witnessed at W10 in
the table above (defang: `if (false && exitCode !== expected)`, result RED,
exit 1). A gate
that writes a record for a DIFFERENT gate id is refused, and that arm is
reached only by the gate-id check, which has no test of its own. **Stated as
an unwitnessed arm rather than as a covered one.**

### Attack 3: can the pin pass a tree that changed?

The T-004 shape is a byte-identical rewrite. `pin-mtime-only-difference`
stages exactly that, asserts the content hash is UNCHANGED across the rewrite
(so the witness is a witness and not a restatement of "the file changed"), and
W18 shows that dropping `mtimeMs` from the comparison makes it green.

**Scope of the negative:** the pin has one-second-resolution exposure it does
not have here (mtimeMs is milliseconds on this filesystem, measured by the
test passing), and a filesystem with coarser mtime granularity could produce a
rewrite the pin cannot see. Not reproduced, not refuted: **UNPROVEN on other
filesystems.** The pin also does not follow directory symlinks, so a tree
whose contents change behind a symlinked directory is outside every pin taken
here, by design and stated rather than discovered later.

### Attack 4: can a configured path block the runner instead of reporting?

Three placements, all staged with real `mkfifo`. All three report and RETURN.
The proof that the block is REAL and the guards load-bearing is W12b and
W13b, which remove the guards and hang until `timeout` kills them at 40
seconds (exit 124). That is the difference between "the guard is there" and
"the guard is doing something".

**Scope of the negative:** the audit below covers `src/gates/` and
`src/commands/gates.ts`. It does NOT cover `src/task.ts`'s own residual
probe-then-open window (documented in that module and inherited unchanged),
does not cover the file access of the `git` subprocesses the runner starts,
whose own opens were NOT enumerated here and are outside the scope of this
audit, and does not close the twelve M1 instances in `src/lock.ts`, `src/pool.ts` and
`src/brief.ts`, which have no M2 owner (plan section 1.1).

The audit, published in full per fix-round contract item 2:

```
$ grep -n 'readFileSync\|openSync\|appendFileSync\|renameSync\|writeFileSync\|createReadStream\|createWriteStream\|readFile(' src/gates/*.ts src/commands/gates.ts
src/gates/pin.ts:2:import { lstatSync, readFileSync, readdirSync, statSync } from "node:fs";
src/gates/pin.ts:33: * `readFileSync` in this module sits on the line after the probe, and it is
src/gates/pin.ts:85:  const body = readFileSync(path);
src/gates/run.ts:2:import { lstatSync, mkdirSync, rmSync, writeFileSync } from "node:fs";
src/gates/run.ts:171:  const written = runStep(`writing ${path}`, () => writeFileSync(path, body));
src/commands/gates.ts:16:import { writeFileSync } from "node:fs";
src/commands/gates.ts:140:    writeFileSync(path, renderGateResult(result)),

$ grep -n 'rmSync\|mkdirSync\|cpSync\|unlinkSync\|rmdirSync' src/gates/*.ts src/commands/gates.ts
src/gates/run.ts:2:import { lstatSync, mkdirSync, rmSync, writeFileSync } from "node:fs";
src/gates/run.ts:160:    mkdirSync(path, { recursive: true }),
src/gates/run.ts:462:    rmSync(recordPath, { force: true }),
```

Three raw call sites plus two mutating ones, each with its guard on the line
above it:

| call site | guard | line |
|---|---|---|
| `pin.ts:85` `readFileSync` (bytes, for sha256) | `classifyEntry` | `pin.ts:76` |
| `run.ts:171` `writeFileSync` (evidence capture, summary) | `refuseOpenForWrite` | `run.ts:167` |
| `run.ts:160` `mkdirSync` (evidence directories) | `classifyEntry` | `run.ts:149` |
| `run.ts:462` `rmSync` (clearing a stale record) | `refuseOpenForWrite` | `run.ts:450` |
| `commands/gates.ts:140` `writeFileSync` (the gate's own record) | `refuseOpenForWrite` | `commands/gates.ts:134` |

Every other read of an externally supplied path goes through
`readRegularFileIfPresent` (`manifest.ts:87`, `manifest.ts:235`,
`run.ts:538`, `commands/gates.ts:176`) or `classifyEntry`
(`run.ts:240`, `run.ts:524`, `pin.ts:115`, `pin.ts:141`). **There is no second
implementation of the probe anywhere in this phase's diff** (T-005).

### Attack 5: can a defanged CI step, or a defanged guard, stay green?

This is the class M1-P6 paid four rounds for. The bundle step's own command is
extracted from the workflow and EXECUTED, with a falsifiability arm that runs
the same extracted command against a manifest that cannot pass. Three
structurally different defangs redden it, TWO of which preserve every string a
text assertion would look for (W25, W26).

The C-2/C-3 structural guard strips comments before matching, which is itself
a way for a guard to go green over nothing, so the strip is controlled: each
file must still contain code after stripping, and W22 shows that a strip which
ate the file reddens the test.

**Scope of the negative, and it is a real limit.** Nothing here reads GitHub's
evaluation of `if:`, job-level or workflow-level `defaults`,
`continue-on-error`, or branch protection. None of those is readable from this
tree, and M1-P6's `exit-test-local.test.ts` reaches the same conclusion at
length. Criterion 14's check-run and ruleset evidence is the API-side half of
this and is CI-DEFERRED, because `gh` is absent locally (verified in step 1)
and, per CLAUDE.md warning 6, the `gh` that CAN be installed here reports
`permissions.push` FALSE and refuses GraphQL, so it is not an authority for
this question even when present.

### What the self-review did NOT attack

Stated so absence of checking is distinguishable from absence of defects:

- The `branch-matches` precondition kind has no test. It is implemented and
  schema-validated, and M2-P4 is the phase that uses it. **UNWITNESSED.**
- The `file-absent` precondition kind has no test. Same status. **UNWITNESSED.**
- The `modes` field is validated-if-present and ignored, per section 2 item 1.
  No test covers a manifest that carries it. **UNWITNESSED.**
- Concurrency: nothing in this phase takes a lock, and two runners against one
  evidence directory would interleave. No M2 caller does that, and the plan
  asks for none. **NOT ATTACKED.**
- `--only` filtering has no test. **UNWITNESSED.**

## Gate numbers

Floor toolchain, `.../scratchpad/toolchain/node-v26.6.0-linux-x64/bin` first on
PATH, `node --version` checked in the shell that ran each command.

```
TOOLCHAIN: v26.6.0 / npm 11.18.0
npm ci                        exit 0   (node_modules and dist removed first)
npm run build                 exit 0
git status --porcelain        empty after the build
npm test (dist REMOVED)       exit 0   pass 177, fail 0, skipped 3
npm run build                 exit 0
npm test (dist present)       exit 0   pass 180, fail 0, skipped 0
```

The 3 skips with `dist/` removed are this phase's three dist-dependent tests,
each carrying its reason (`dist/ is absent; run npm run build first (CI builds
before it tests)`). CI builds before it tests, so all 180 run there.

Container default toolchain, reached through `bash -lc` so the login shell
resolves it:

```
TOOLCHAIN: v22.22.2 / npm 10.9.7
npm ci                        exit 0
npm run build                 exit 0
git status --porcelain        empty after the build
npm test                      exit 0   pass 178, fail 0, skipped 2
```

The 2 skips there are the PRE-EXISTING floor-gated doctor tests
(`local Node v22.22.2 is below the kernel floor >=26`), not this phase's.
Every one of this phase's 24 tests runs and passes on BOTH toolchains.

## Registry resolution

```
behaviors registered      : 186
distinct test titles run  : 180
behaviors UNRESOLVED      : 0
test titles unregistered  : 0
keys sharing a title      : 10 across 4 titles (pre-existing at the base)
arithmetic                : 186 keys = 180 titles + 6 surplus keys
base behaviors at 037477e : 162
```

24 behaviors added, 162 to 186, zero overlapping keys, nothing removed or
retitled. The 6-key surplus is arithmetic, not an error, and it is
pre-existing: four titles carry more than one behavior each because one test
witnesses several registered behaviors (for example
`pool-destroy-dirty-refused` and `pool-destroy-discard-removes` share
"pool destroy refuses a dirty worktree and --discard removes it"). The base
had the same surplus, 162 keys against 156 titles. This figure is COMPUTED by
the script above from the TAP output of the run, not counted by hand: CR-580
and CR-565 record a self-reported registry count that was wrong by exactly one
throughout a phase while being described as computed independently.

## Scope audit

```
$ git diff --name-status 037477e HEAD
M	.github/workflows/gates.yml
A	delivery/work-history/m2-p1.md
A	gates.manifest.json
M	package.json
M	src/cli.ts
A	src/commands/gates.ts
A	src/gates/manifest.ts
A	src/gates/pin.ts
A	src/gates/result.ts
A	src/gates/run.ts
A	src/gates/schemas/gate-manifest.schema.json
A	src/gates/schemas/gate-result.schema.json
A	src/gates/validate.ts
M	test/behaviors.json
A	test/gates.test.ts
A	test/pin.test.ts
```

16 files. 14 are on the plan's files-to-touch list; `test/behaviors.json` and
`delivery/work-history/m2-p1.md` are the two standing pre-authorized extras.
**Zero files outside scope.** `package.json` is edited under the plan's own
condition ("edit only if step 5 requires a build or files change"), and step 5
measured that it does. `dist/` is not committed, and `git status` is empty
after the build on both toolchains:

```
$ git ls-files dist | wc -l
0
$ git check-ignore -v dist
.gitignore:2:dist/	dist
```

## Conventions

```
$ grep -rP '[^\x00-\x7F]' src/gates/ src/commands/gates.ts test/gates.test.ts test/pin.test.ts gates.manifest.json .github/workflows/gates.yml package.json delivery/work-history/m2-p1.md
(no output, exit 1: pure ASCII)
$ grep -rn -- '(em dash)' <same set>
(no output, exit 1)
```

Commit messages carry no model or tool name. No CLAUDE.md edit (M2-D-12). No
tsconfig change. English only. npm only.

## The claim grep (CLAUDE.md, binding, run before submission)

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m2-p1.md
74:always()`), and no `env:` block:
350:| M2-C-2 (never green by omission) | W1, at the runner's ingest | W2, at the constructor | W6, at the aggregate level |
```

Two hits remain and neither is a claim about the world:

- line 74 is the literal YAML expression `if: always()`, quoted from
  `.github/workflows/gates.yml`.
- line 350 is the NAME of constraint M2-C-2, quoted from kernel plan M2
  section 1.3.

The first run of this grep returned nine hits. Every one of them was settled
rather than reworded:

| hit | disposition |
|---|---|
| "no `env:` block anywhere in the file" | captured `grep -n '^\s*env:'`, exit 1, adjacent |
| "the check-run names GitHub publishes are derived from job names" | **RESTATED.** It is GitHub's documentation, not a measurement, and is not readable from this tree. The measurement is criterion 14 and it is CI-deferred |
| "never imported as modules" | captured `grep` for JSON imports and `resolveJsonModule`, only hit is the comment saying so |
| "`git checkout --` ... so it was never used" | captured `grep` over the red-witness harness source; it restores with `shutil.copy2` and runs no git |
| "no runner can audit it" | **RESTATED as an open question**: I did not find a way; if a reviewer does, it is a finding |
| "the two can never be confused" | captured a real `summary.json` showing `manifestGates` and `counts.declared` as separate fields |
| "is covered by the exit-code/record consistency check" | replaced with the witness id, the exact defang, and its result (W10, RED, exit 1) |
| "paths this runner never opens" | **RESTATED as a scope limit**: the `git` subprocesses' own file access was not enumerated |
| "`dist/` is never committed" | captured `git ls-files dist` (0) and `git check-ignore -v dist` |

## Open questions and forward warnings for M2-P2 to M2-P9

1. **`fetch-depth` for `diff-touches` gates.** The workflow's checkout is
   `actions/checkout@v4` with no `fetch-depth`, so it is shallow. This phase's
   only gate has no precondition and needs no history, so nothing is broken
   today. **The first phase to register a gate whose precondition is
   `diff-touches` (M2-P2, M2-P5) will need `fetch-depth: 0`**, and without it
   the `git diff base...head` in `evaluatePrecondition` will exit nonzero and
   the gate will report `error`, which is the correct failure but an
   avoidable one. Not fixed here because the workflow edit would be a change
   with no gate to justify it.
2. **Three precondition kinds ship unwitnessed**: `branch-matches`,
   `file-absent`, and the `modes` reserved field. Their consumers are M2-P4
   and M2-P6; those phases should treat them as unproven rather than
   delivered.
3. **The gate subprocess contract** is documented at the top of
   `src/gates/run.ts` and is what every later gate must obey: argv gains
   `--result` and `--evidence` plus one flag per declared parameter, the gate
   writes exactly one record to `--result`, and its exit code must AGREE with
   its record's status or the runner reports `error`.
4. **`--only` has no test.** It is implemented and refuses an unknown gate id.
5. **The claim grep is worth running early, not at the end.** Three of its
   nine hits in this phase were genuine overstatements that had already been
   written down as settled facts.
6. **The red-witness campaign found three green witnesses**, and each was a
   test that guarded a property some other guard was also holding. The cheap
   way to find them is to defang the mechanism rather than delete the feature,
   which is what the strengthened red-witness rule already says.

---

# FIX ROUND 1

Reviews on `ac3b2f6`: criteria contract APPROVE (16/16, 9/9 mutations reddened);
hazard contract FIX-ROUND-NEEDED, 1 high, 5 medium, 8 low. Arbitration adopted
an intended reading for the high. This section is written INCREMENTALLY.

- Fetching `claude/m2-era-paperwork` to read the three review artifacts.

## CR-800 (HIGH): the spine could certify nothing as everything

### The MECHANISM, not the finding

The finding is "a conditional gate that exits 20 with a not-applicable record
makes the bundle exit 0". The MECHANISM is:

> **`counts.applicable` was set from WHETHER A CHILD WAS STARTED, not from what
> the gate concluded, and the aggregate anti-vacuity rule consulted it.**

Two consequences follow from the one mechanism, and fixing only the first would
have left the second. `applicable` was a proxy for "in play", and it silently
stopped being one on the single path where the GATE, not the runner, decides
applicability. And the rule that protects M2-C-2 at the aggregate level was
reading a proxy at all, when the quantity it means is "did any gate reach a
verdict".

So the fix is two named counts with two meanings, and the rule reads the one it
means:

- `applicable`: reached, and did not report `not-applicable`, whichever side
  decided that. Read off the RESULT (`src/gates/run.ts`, `runOneGate`).
- `verdict`: `green + red`. The only count the anti-vacuity rule consults.

Plus a third thing, because CR-800 was a READING of the branch conditions that
turned out not to hold: the success path now ASSERTS a non-empty green bucket
rather than being unreachable-by-inspection, and the whole decision was
extracted into `decideAggregate`, a pure function that can be handed states the
runner cannot currently produce.

### The derivation, published in full

Every site that decides applicability or reads a count to reach a verdict:

```
$ grep -n 'applicable\|counts\.\|verdict' src/gates/run.ts
145:  applicable: boolean;                    <- GateSummaryRow
165:    applicable: number;
166:    /** green + red: gates that reached a verdict. The anti-vacuity count. */
167:    verdict: number;
459:  applicable: boolean;                    <- GateOutcome
477:      applicable: false,                  <- evidence dir refusal
496:      applicable: false,                  <- missing run parameter
505:      applicable: false,                  <- precondition error
520:      applicable: false,                  <- precondition unmet (runner)
535:      applicable: false,                  <- record path refusal
544:      applicable: false,                  <- stale record could not be cleared
563:    applicable: ingested.status !== "not-applicable",   <- THE FIXED SITE
769:  if (counts.error > 0) {
775:  } else if (counts.red > 0) {
781:  } else if (counts.verdict === 0) {       <- THE FIXED RULE
789:  } else if (requiredNotApplicable.length > 0) {
797:  if (exitCode === EXIT_GREEN && counts.green === 0) {   <- the invariant
804:  if (counts.vacuous > counts.error) {                   <- the CR-806 invariant
1039:    if (outcome.applicable) { counts.applicable += 1; }
1044:      counts.verdict += 1;
```

Eight sites set `applicable`. Seven were already `false` and correct; the
eighth was the unconditional `true` on the spawn path, and it is the whole
defect. One site read a count to decide vacuity, and it read the wrong one.

### What this derivation did NOT cover

1. It searched `src/gates/run.ts` only. `applicable` and `verdict` appear
   nowhere else in `src/` (checked: `grep -rn 'counts\.applicable\|counts\.verdict' src/`
   returns hits in `run.ts` only), but `summary.json` is a published shape and
   this search says nothing about consumers outside this repository. M2-P9's
   harness is the first, and it does not exist.
2. It does not cover a gate that reports `green` with `units` above zero having
   examined nothing. That remains uncloseable by the runner and is recorded as
   an open question, unchanged from round 1.
3. It does not cover the plan. Criterion 9's restatement and the `verdict`
   count are a plan ripple the arbitration explicitly assigned to the
   ORCHESTRATOR, sequenced after this round. I have not edited the plan.
4. **`counts.verdict` is a NEW key in `summary.json.counts`.** A reviewer
   asserting `deepEqual` over the criterion-2 count set will see it. It is
   named here rather than left to be discovered, and criterion 2's own numbers
   (declared 4, applicable 3, green 1, red 1, not-applicable 1, error 1,
   vacuous 0) are all unchanged, which the suite asserts.

### The contagion the review named, answered

Section 1.4 declares `credential-token` as `conditional` with precondition "env
`TIPHYS_IMPLEMENTER_TOKEN` present", and there is no env precondition kind, so
M2-P8 must either wrap it in `command-exit-zero` or self-declare inside the
gate. **Both are now safe.** The self-declaring shape no longer produces a
green bundle over zero verdicts; it produces `no applicable gate` and exit 21,
identically to the runner-evaluated route. The two routes are now
indistinguishable in the summary, which is the property that was missing.

## CR-801 (MEDIUM): the runner did not obey its own crash discipline

### The MECHANISM

> **A throw escaping the runner reaches Node's top level, whose uncaught
> exception exit code is 1, which is this phase's own `EXIT_RED`.**

The runner enforces exactly this rule on its gates, in a comment at
`src/gates/run.ts` that says so, and did not obey it.

### The derivation, published in full

```
$ grep -rn 'throw ' src/gates/ src/commands/gates.ts
src/gates/manifest.ts:89:    throw new Error(          <- readSchemaDocument, absent or irregular
src/gates/manifest.ts:98:    throw new Error(loaded.reason);   <- readSchemaDocument, bad schema
src/gates/pin.ts:107:    throw new Error(              <- hashFile, vanished or irregular
src/gates/pin.ts:152:    throw new Error(              <- walk, irregular entry
src/gates/pin.ts:167:    throw new Error(              <- takePin, empty root list (new)
src/gates/pin.ts:182:        throw new Error(          <- takePin, vacuity floor (new)
src/gates/pin.ts:190:      throw new Error(            <- takePin, absent root
src/gates/pin.ts:196:    throw new Error(              <- takePin, irregular root

$ grep -rn 'JSON.parse\|new RegExp' src/gates/ src/commands/gates.ts
src/gates/run.ts:357:      expression = new RegExp(...)        <- in a try
src/gates/run.ts:630:    parsed = JSON.parse(read.body);      <- in a try
src/gates/validate.ts:315:        new RegExp(patternSource);   <- in a try, at LOAD (new)
src/gates/validate.ts:469:      matched = new RegExp(pattern)  <- in a try (new)
src/gates/manifest.ts:95:  const parsed = JSON.parse(...)       <- NOT in a try, inside readSchemaDocument
src/gates/manifest.ts:248:    parsed = JSON.parse(read.body);    <- in a try
src/commands/gates.ts:197:      parsed = JSON.parse(read.body);  <- in a try
```

The review's derivation named an asymmetry I had not seen: `new RegExp` on a
CALLER-supplied pattern was guarded and `new RegExp` on a SCHEMA-supplied one
was not. Same call, same failure, one guarded. Both are now guarded, and the
schema-supplied one is compiled at LOAD so an unusable pattern is a load
failure with a reason rather than a throw mid-validation.

The bounding catches. `cmdGates` is the ONLY entry into this subtree, so a
throw from anywhere under it reaches one of these:

```
$ grep -n '"gates", cmdGates' src/cli.ts
23:  ["gates", cmdGates],
$ grep -n "^export function cmdGates" src/commands/gates.ts
258:export function cmdGates(args: string[]): number {
```


```
$ grep -rn 'try {' src/gates/run.ts src/commands/gates.ts
src/gates/run.ts:907:  try {   <- runGates, the public entry: folds to EXIT_GATE_ERROR + aborted summary
src/gates/run.ts:986:  try {   <- runGatesInner: releases the claim in its finally
src/commands/gates.ts:259:  try {   <- cmdGates, the outer backstop for self-check and parsing
```

`manifest.ts:95`'s `JSON.parse` inside `readSchemaDocument` is deliberately
still unguarded at its own site: a shipped schema document that is not JSON is
an installation defect, and it is reached only through `cmdGates`, so the
catches above report it as `error` with the path named. That is a choice, not
an oversight, and it is witnessed: F4b and F5b both stage a missing schema
document and both produce exit 21 with a reason, never exit 1.

### What this derivation did NOT cover

1. `src/task.ts`, whose `classifyEntry`, `readRegularFileIfPresent` and
   `runStep` all catch internally (read, not assumed).
2. The other seven CLI subcommands. `bin/tiphys.ts` and a global
   `uncaughtException` handler are OUT OF SCOPE for this phase: `bin/tiphys.ts`
   is not on the files-to-touch list, and a handler in `src/cli.ts` would
   change the exit behaviour of `lock`, `pool`, `spawn`, `teardown`, `watch`,
   `doctor` and `init`, none of which this phase owns. **The bound is therefore
   the `gates` subcommand only, and the review's third suggested fix (a handler
   at `bin/tiphys.ts`) is DECLINED on scope grounds and recorded here rather
   than silently skipped.** A throw from another subcommand still exits 1.
3. Asynchronous throws. There are none: every path under `gates` is
   synchronous, `spawnSync` only, no promise is created. If a later phase adds
   one, the synchronous catches will not see it.
4. The gate subprocesses, which are children and cannot propagate a throw into
   the runner.

## CR-802, CR-807, CR-808, CR-811: the Ajv seam

### The MECHANISM

> **A KNOWN keyword in a position this validator drops without a word.**

The closed keyword set was loud about UNKNOWN keywords and silent about a known
one in a dropped position, which is the harder half of the same attack: the
module's own comment says a validator that silently skips a constraint "reports
a document valid while never having checked the constraint that mattered".

### The derivation: every place `validateNode` returns early

```
$ grep -n "^\s*return;" src/gates/validate.ts
212:    return;   <- checkSchemaNode: not a schema object (a LOAD problem, already reported)
401:      return;   <- validateNode: $ref cycle       (now a diagnostic, CR-807)
409:      return;   <- validateNode: $ref unresolved  (already a diagnostic)
415:    return;   <- validateNode: after following a $ref  <- THE DROP (CR-802)
427:    return;   <- validateNode: type mismatch, deliberate
```

Five early returns, four already reported something. Line 415 was the only one
that dropped schema content silently, and it dropped every sibling of a `$ref`.

**The choice: REFUSE at load, not implement.** Both were offered. Refusal is
what this module's philosophy already does everywhere else, it is three lines
instead of a re-entrant evaluation, and it makes the Ajv seam SAFE rather than
merely equivalent: a schema this engine cannot evaluate the way the
specification says is rejected, so no schema can be authored here that changes
verdict under Ajv. The one thing forbidden, the silent middle, is gone.

Line 427 (type mismatch) is retained and is deliberate: examining a value of
the wrong type further produces diagnostics about a shape nobody claimed.

### What this derivation did NOT cover

1. It enumerated early returns, which finds keywords dropped by CONTROL FLOW.
   It does not find a keyword that is parsed and then not applied. Cross-checked
   by walking `VALIDATION_KEYWORDS` one by one against `validateNode`: `type`,
   `required`, `properties`, `additionalProperties`, `enum`, `items`,
   `minimum`, `minItems`, `pattern`, `const` and `$ref` all have an applying
   branch. That walk is by reading, not by execution, and is stated as such.
2. It does not cover keywords Ajv applies that this engine does not IMPLEMENT
   at all: those are load errors by construction (M2-D-04), witnessed for
   `oneOf` and `maxLength`.
3. It does not cover annotation keys (`$schema`, `$id`, `title`,
   `description`, `$defs`), which carry no constraint. `$defs` beside a `$ref`
   is deliberately still permitted, since it declares definitions rather than
   constraining the instance.
4. **A control found a real gap the derivation would have missed.** Writing the
   legitimate-recursion control for CR-807 showed that `$ref: "#"`, the
   ordinary way to write a recursive schema, did not resolve AT ALL: the
   resolver required a `#/` prefix. The cycle fix would have shipped beside a
   gap that made every recursive schema unusable. It is fixed and the control
   is a test.

## CR-803 (MEDIUM): the evidence directory had no owner

### The MECHANISM

> **The evidence directory carries no run identity, and every path inside it is
> a pure function of the gate id, so two runs address the same bytes.**

Not "the ingest is racy": `runOneGate` clears the record path, spawns, and
ingests whatever is at that path, and any two runners interleave those three
steps freely.

### The derivation: every fixed path the runner touches in the evidence dir

```
$ grep -n 'join(evidenceDir\|join(options.evidenceDir\|join(gateDir\|RUN_CLAIM_FILE\|\.stage' src/gates/run.ts
472:  const gateDir = join(evidenceDir, entry.id);
480:  const recordPath = join(gateDir, "result.json");
481:  const stdoutPath = join(gateDir, "stdout.txt");
482:  const stderrPath = join(gateDir, "stderr.txt");
832:  const claimPath = join(evidenceDir, RUN_CLAIM_FILE);      <- new
875:  const stagePath = `${summaryPath}.${runId}.stage`;        <- new, runId-scoped
895:    unlinkSync(join(evidenceDir, RUN_CLAIM_FILE));          <- new
955:    join(options.evidenceDir, "summary.json");
1055:  const recordPath = ... join(..., entry.id, "result.json");
1056:  ensureDirectory(join(options.evidenceDir, entry.id));
1105:  const summaryPath = join(options.evidenceDir, "summary.json");
```

Every one of them is a function of the evidence dir and the gate id and of
nothing else, which is why the claim is on the DIRECTORY rather than on each
path: making nine paths run-scoped is nine chances to miss one, and the tenth
is added by the next phase.

**MECHANISMS.md's claim-file row requires a third user to read `src/lock.ts`
first. Done**, and the pattern is followed rather than re-invented:
`writeFileSync(path, body, { flag: "wx" })`, EEXIST is a LOUD refusal that
NAMES THE STUCK FILE, no steal, no age heuristic. One deliberate difference
from `lock.ts`, stated: `lock.ts` waits with a bounded poll because a lease
mutation is genuinely contended; this does not wait at all, because two runs
sharing an evidence directory is an operator error and not a queue, and a
silent wait would be indistinguishable from an absence of contention.

`summary.json` is also replaced atomically through a stage name carrying the
runId, per MECHANISMS.md's "Atomic file replacement" row, whose recorded defect
is exactly a FIXED stage name shared by two passes.

### What this derivation did NOT cover

1. It covers the paths the RUNNER addresses. It does not cover what a GATE
   writes: a gate handed `--evidence <dir>/<gate-id>` may write anything it
   likes there, and two runs of the same gate id would collide inside it. The
   claim prevents that for two runs of this runner, and does not prevent a
   gate from being run by something else.
2. It does not cover a process that deletes the claim file mid-run. That is
   the stranded-claim shape `lock.ts` also cannot prevent, and its consequence
   here is the pre-fix behaviour rather than something worse.
3. It does not cover `gates.manifest.json` (read once, read-only) or the
   repository through the `git` subprocesses.
4. **CI is not exposed either before or after**: the evidence directory is
   `${{ runner.temp }}/gate-evidence`, per job. The exposure was, and the
   protection is, local and any two runs given the same `--evidence`.
5. The review's alternative fix (a `runId` required IN each gate's record) is
   DECLINED and the reason recorded: it changes the gate subprocess contract
   for seven phases that have not been written, to close a hole the directory
   claim already closes. The runId is stamped in the SUMMARY, so a bundle is
   attributable, without binding anyone else's implementation.

## CR-804 (MEDIUM) and CR-809 (LOW): the pin measured nothing, and missed the shape it exists for

### The MECHANISM, part one

> **Nothing in the pin's SHAPE distinguishes "the tree did not change" from "no
> tree was measured", so a caller cannot tell them apart and neither can a
> record.**

`fileCount` is now in the shape and a root that contributed nothing THROWS,
naming the root. The rule lives in the module that carries the mechanism rather
than in each of the two phases that consume it (T-005).

### The MECHANISM, part two

> **M2-C-5 was enforced by each gate's own honesty, at a runner that holds both
> the record's pins and the module that compares them.**

Now enforced at ingest, structurally identically to the M2-C-2 rewrite two
lines away.

### The MECHANISM, part three (CR-809)

> **mtime is SETTABLE FROM USERSPACE.**

`cp -p`, `rsync -a` and `tar -x` all restore it through `utimensat` at
nanosecond precision, so a rewrite performed by any of those three ordinary
commands passed a pin of {sha256, size, mtimeMs} without a mark, and T-004's
incident IS a rewrite. This is judged CLOSABLE at this layer rather than left
as a stated limit, and closed: `ctimeMs` joins the pin. No userspace call sets
ctime, and `utimensat` BUMPS it, so the act of restoring mtime is what makes
the change visible. It also detects replace-by-rename, measured rather than
asserted:

```
$ (write a.ts, pin, write .stage with the SAME bytes, restore its mtime
   exactly with utimesSync, rename .stage over a.ts, pin again)
inode: 2532869 -> 2532922 (changed: true)
sha/size/mtime identical: true true true
comparePins -> [{"path":".../a.ts","kind":"changed","fields":["ctimeMs"]}]
```

It is a strict ADDITION to M2-C-5's four fields, not a substitution. The
comparison in `comparePins` is a disjunction over fields (each field pushes
into `fields` and a non-empty `fields` is a difference), so adding a fifth
disjunct can only add differences. The four M2-C-5 fields are still compared,
unchanged, which `pin-mtime-only-difference` asserts by name. **M2-P2 criterion 7 and M2-P3 criterion 8 are pin
witnesses and must be read against five fields**, which is said in the module
header where those implementers will read it, not only here.

Measured, not asserted:

```
$ cp -p a.ts /stash && cp -p /stash a.ts     (a real round trip)
sha256 unchanged, size unchanged, mtimeMs unchanged
comparePins -> [{ path: ".../lock.ts", kind: "changed", fields: ["ctimeMs"] }]
```

### The derivation: every consumer of the pin in src/

```
$ grep -rn "takePin\|comparePins\|record.pin" src/ --include=*.ts | grep -v "^src/gates/pin.ts"
src/gates/run.ts:13:import { comparePins, describePinDifference } from "./pin.ts";   <- new
src/gates/run.ts:714:  if (record.pin === undefined) {
src/gates/run.ts:717:  if (record.pin.start.fileCount === 0 || record.pin.end.fileCount === 0) {
src/gates/run.ts:720:  const differences = comparePins(record.pin.start, record.pin.end);
src/gates/result.ts:176:  if (fields.pin !== undefined) {   <- carries it into the record
```

Before this round the same grep returned ONE line, the `Pin` TYPE import into
`result.ts`. The module was delivered with no runtime consumer at all.

### What this derivation did NOT cover

1. `takePin` has no consumer in `src/` even now: the runner enforces pins that
   arrive IN A RECORD, and the gates that TAKE pins are M2-P2's and M2-P3's,
   which do not exist. So the vacuity floor is witnessed by `test/pin.test.ts`
   and not by any shipped caller.
2. The residue on ctime, stated rather than left: it is not forgeable from
   userspace, and it is not a cryptographic seal. A privileged actor with raw
   device access can write any inode field. Outside anything this kernel
   measures.
3. A gate that reports no `pin` at all is untouched. M2-C-5 binds the gates
   that execute a test suite, and each of those declares its pins; the runner
   cannot know which gates those are without the plan, and does not guess.

## CR-805 (MEDIUM): branch-matches decided applicability on a weaker predicate

### The MECHANISM

> **A documented TOKEN substitution was being interpolated into regex SOURCE,
> and the compiled expression was unanchored, so the predicate that was
> evaluated was strictly weaker than the one the pattern appears to state.**

Two faults, one line, one mechanism: the pattern's meaning was not what its
author wrote. `{phase}` is now regex-escaped and the expression is compiled as
`^(?:<pattern>)$`.

**Anchoring changes semantics and that is stated, not buried.** A prefix rule
must now be written `claude/{phase}-.*`, not `claude/{phase}-`. My own control
caught this: with the natural pattern the REAL phase branch stopped matching.
An author who forgets the `.*` gets `not-applicable`, which is the safe
direction, and the schema `description` says so where M2-P4 will read it.

### The derivation: every precondition kind, and which had a test

```
$ grep -n 'precondition.kind === ' src/gates/run.ts
304:  if (precondition.kind === "file-exists" || precondition.kind === "file-absent") {
330:  if (precondition.kind === "branch-matches") {
373:  if (precondition.kind === "diff-touches") {
(the fifth, command-exit-zero, is the fall-through)

$ grep -o 'kind: "[a-z-]*"' test/gates.test.ts | sort | uniq -c     (before this round)
      1 kind: "command-exit-zero"
      1 kind: "diff-touches"
      7 kind: "file-exists"
```

Two of five kinds had no test: `branch-matches` (which the review found) and
**`file-absent`, which this derivation found and the review did not**. Both now
have one. `file-absent`'s M2-C-6 arm is the interesting half: a named pipe at
the path is PRESENT, so a naive reading calls the precondition unmet, which is
a verdict about a path nobody examined. It reports `error`.

### What this derivation did NOT cover

1. It enumerated the kinds the RUNNER implements. It says nothing about kinds
   the plan declares and the runner does not, and section 1.4's
   `credential-token` precondition ("env `TIPHYS_IMPLEMENTER_TOKEN` present")
   is exactly that: there is no env kind. That gap is unchanged by this round
   and belongs to M2-P8, which must wrap it in `command-exit-zero`.
2. The anchoring decision is not witnessed against any REAL phase declaration,
   because none exists: M2-P4 authors the first `branch-matches` manifest
   entry. The fixture uses `claude/{phase}-.*`, which is what the schema
   description now prescribes.
3. Unanchored matching is strictly MORE permissive, so no real phase branch can
   have been failing to match before. The exposure was false-applicability
   only, which the review also concluded.

## The lows, each fixed or recorded with a reason

| id | disposition |
|---|---|
| CR-806 `vacuous` not a strict subset of `error` | **FIXED.** The runner deletes an incoming `vacuous` it did not set, and `decideAggregate` asserts `vacuous <= error` and reports an internal inconsistency otherwise. Witnesses F15 and F3c. |
| CR-807 `$ref` cycle is a RangeError | **FIXED.** A ref chain is carried through `validateNode` and reset whenever an instance node is consumed, so a genuine cycle is `INVALID <pointer> schema reference X is cyclic`. The control in the same test validates a legitimate recursive schema (`$ref: "#"`, two levels deep) and asserts the one expected diagnostic at `#/child/child/name`. Witness F13. |
| CR-808 `__proto__` escapes `additionalProperties: false` | **FIXED.** One `ownProperty` helper used in `validateNode`, `checkSchemaNode`, `resolveRef` and the `required` presence test. Witness F14. |
| CR-809 restored-mtime rewrite invisible | **FIXED, not stated as a limit.** `ctimeMs` joins the pin; see the CR-804 section for the mechanism, the measurement and the residue. Witnesses F20, F21. |
| CR-810 the claim-grep transcript does not reproduce | **FIXED.** The grep is now re-run AFTER the disposition table is written and the final output recorded, with the transcript block's self-references named. See the fix-round claim grep below. |
| CR-811 one message outside `DIAGNOSTIC_MESSAGES` | **FIXED.** `duplicateId` is in the table, and so are the two new `$ref` messages. Witness F16, which also asserts the inline string is gone from `manifest.ts`. |
| CR-812 self-check units | **FIXED.** `units` counts schema documents, which is what the unitLabel says; the manifest validation is reported in `detail`. The test derives the expected count by reading the schemas directory, so adding a schema document cannot silently desynchronise it. Witness F17. |
| CR-813 dist runner, source-entry gate | **RECORDED, in the manifest itself.** The manifest schema gained an optional `note`, and `gates.manifest.json` uses it to say that gate commands are the source entry, that `npm pack` does not ship `bin/tiphys.ts`, that the runner in CI is the compiled entry, and that both schema copies are therefore exercised. Not "fixed" because there is nothing wrong: kernel plan M2 step 9 fixes the runner as `dist/bin/tiphys.js`, and a manifest whose gate command were also dist would make every source-mode run require a build first. |
| CR-830-1 `--head` is the merge SHA | **FIXED**, one line, plus a coupling recorded as a DERIVED expectation rather than a measurement: this checkout is shallow, so a diff between the base sha and the PR head sha is not expected to resolve without `fetch-depth: 0`. I could not execute that here (it needs a GitHub runner) and it is written in the workflow comment as the thing M2-P2 and M2-P5 must check, not as something this round proved. Witness F22b, which is a PINNED-SHAPE text assertion and is labelled as one in the test. |

## Red-witness table, fix round 1

Same method as round 1: copy out of tree, defang, run the named test with
`--test-name-pattern` before the positional path, restore by copying back with
a sha256 equality check. No `git checkout` anywhere in the harness.

| id | dangerous state | test | result |
|---|---|---|---|
| F1 | `applicable` reverts to "was spawned" | gate-declared-not-applicable-not-green | RED |
| F2 | the anti-vacuity rule consults `applicable` again | gate-declared-not-applicable-not-green | RED |
| F3 | the success-path invariant removed | gate-success-path-needs-a-green | **GREEN, see below** |
| F3b | the same, against the rewritten behavioural test | gate-success-path-needs-a-green | RED |
| F3c | the `vacuous <= error` invariant removed | gate-success-path-needs-a-green | RED |
| F4 | the `runGates` try/catch removed, NO rebuild | gate-runner-crash-is-error-with-summary | **GREEN, harness error, see below** |
| F4b | the same, WITH rebuild | gate-runner-crash-is-error-with-summary | RED |
| F5b | the throw is folded but no aborted summary is written | gate-runner-crash-is-error-with-summary | RED |
| F6 | the evidence-directory claim is not taken | gate-evidence-directory-is-claimed | RED |
| F7 | the claim is STOLEN (`wx` dropped) instead of refused | gate-evidence-directory-is-claimed | RED |
| F8 | M2-C-5 not enforced at ingest | gate-pin-disagreement-is-error | RED |
| F9 | a vacuous pin accepted | gate-pin-disagreement-is-error | RED |
| F10 | `branch-matches` unanchored again | gate-branch-matches-anchored-and-escaped | RED |
| F11 | the phase id interpolated unescaped again | gate-branch-matches-anchored-and-escaped | RED |
| F12 | `$ref` siblings permitted at load | gate-validator-ajv-seam-divergences | RED |
| F13 | `$ref` cycle detection removed | gate-validator-ajv-seam-divergences | RED |
| F14 | `__proto__` resolves through the prototype chain | gate-validator-ajv-seam-divergences | RED |
| F15 | the gate-authored `vacuous` flag believed | gate-vacuous-flag-not-gate-authored | RED |
| F16 | the duplicate-id message authored inline | gate-duplicate-id-message-in-table | RED |
| F17 | self-check counts the manifest as a schema document | gate-self-check-units-match-label | RED |
| F18 | the per-root pin vacuity floor removed | pin-vacuity-floor | RED |
| F19 | the empty-root-LIST floor removed | pin-vacuity-floor | RED |
| F20 | `ctimeMs` dropped from the comparison | pin-restored-mtime-still-differs | RED |
| F21 | `ctimeMs` dropped from the pin record | pin-restored-mtime-still-differs | RED |
| F22 | the PR step reverted to `github.sha` | gate-bundle-step-wired-and-falsifiable | **GREEN, see below** |
| F22b | the same, against the added pinned-shape assertion | gate-bundle-step-wired-and-falsifiable | RED |
| F23 | `file-absent`'s M2-C-6 arm removed | gate-file-absent-precondition | RED |
| F24 | `file-absent` inverted (present read as met) | gate-file-absent-precondition | RED |

### Two structurally different members per class

| class | member 1 | member 2 | member 3 |
|---|---|---|---|
| CR-800 counting semantics | F1, the site that SETS applicable | F2, the rule that READS it | F3b, the invariant behind both |
| CR-800 aggregate invariants | F3b, empty green bucket | F3c, the subset relation | |
| CR-801 crash discipline | F4b, the throw escapes | F5b, the throw is folded but unrecorded | |
| CR-802/807/808 the Ajv seam | F12, siblings | F13, cycles | F14, `__proto__` |
| CR-803 evidence ownership | F6, no claim taken | F7, claim stolen not refused | |
| CR-804 pin vacuity | F18, per-root floor | F19, empty root list | |
| CR-804 M2-C-5 at ingest | F8, pins disagree | F9, pin measured nothing | |
| CR-805 branch matching | F10, unanchored | F11, unescaped | |
| CR-809 restored mtime | F20, comparison | F21, record | |
| the CI step (carried forward) | F24-era W24 deletion | W25 `\|\| true` | F22b `github.sha` |
| `file-absent` | F23, the M2-C-6 arm | F24, the polarity | |

### Three witnesses came back green again, and two were findings

1. **F3 was a real defect in my own test, and it is the exact class M1-P6 paid
   four rounds for.** The first guard I wrote for the CR-800 invariant asserted
   on the TEXT of `src/gates/run.ts`. Wrapping the invariant in
   `if (false && ...)` PRESERVES every asserted string and left the test green:
   a guard-that-asserts-text, written in the round whose whole subject is a
   guard that passed while the thing it guarded was broken. The fix was not a
   better regex. The aggregate decision is now `decideAggregate`, a pure
   exported function, and the test hands it states the runner cannot currently
   produce, which is what an invariant against internal inconsistency needs.
   F3b and F3c redden it.
2. **F4 was an error in my defang harness, not a defect.** The CR-801 test runs
   the COMPILED entry from a staged copy of `dist/`, so a defang of `src/` that
   is not followed by a rebuild never reaches the executed code. F4b and F5b
   rebuild and both redden. Recorded because the same trap will catch the next
   person red-witnessing anything that runs through `dist/`.
3. **F22 was a real gap.** My workflow test executes only the PUSH variant of
   the bundle step, which carries no `--base`/`--head`, so the PR step's
   `--head` value was unguarded. A GitHub expression has no behaviour to
   execute here, so MECHANISMS.md's SECOND tier applies rather than the first:
   pin the accepted shape, fail closed, and NAME what stays unguarded. The
   assertion added is a text assertion, is labelled as one in the test, and
   claims nothing about how GitHub resolves the expression.

## Gate numbers, fix round 1

Floor toolchain, `node --version` checked in the shell that ran each command:

```
TOOLCHAIN: v26.6.0 / npm 11.18.0
npm ci (node_modules and dist removed first)   exit 0
npm run build                                  exit 0
git status --porcelain after the build         only this round's tracked edits; no dist
npm test (dist REMOVED)                        exit 0   pass 189, fail 0, skipped 4
npm run build                                  exit 0
npm test (dist present)                        exit 0   pass 193, fail 0, skipped 0
```

The 4 skips with `dist/` removed are this phase's dist-dependent tests, each
carrying its reason; CI builds before it tests, so all 193 run there.

Container default, through `bash -lc`:

```
TOOLCHAIN: v22.22.2 / npm 10.9.7
npm ci        exit 0
npm run build exit 0
npm test      exit 0   pass 191, fail 0, skipped 2
```

The 2 skips are the PRE-EXISTING floor-gated doctor tests, unchanged. Every one
of this phase's 37 tests passes on both toolchains.

## Registry, fix round 1

```
behaviors registered      : 199
distinct test titles run  : 193
behaviors UNRESOLVED      : 0
titles with no registry   : 0
surplus keys (shared)     : 6   (pre-existing at the base; four titles carry several behaviors)
```

13 behaviors added this round, 186 to 199. Zero overlapping keys, nothing
removed or retitled. Computed from the run's TAP output by script, not counted
by hand.

## Scope audit, fix round 1

```
$ git diff --name-status 037477e HEAD
M	.github/workflows/gates.yml
A	delivery/work-history/m2-p1.md
A	gates.manifest.json
M	package.json
M	src/cli.ts
A	src/commands/gates.ts
A	src/gates/manifest.ts
A	src/gates/pin.ts
A	src/gates/result.ts
A	src/gates/run.ts
A	src/gates/schemas/gate-manifest.schema.json
A	src/gates/schemas/gate-result.schema.json
A	src/gates/validate.ts
M	test/behaviors.json
A	test/gates.test.ts
A	test/pin.test.ts
```

The same 16 files as round 1. No file was added or removed by this round: 14 on
the plan's files-to-touch list, plus the two standing pre-authorized extras.
**Zero files outside scope**, and in particular no edit to `bin/tiphys.ts`,
`src/task.ts`, `src/lock.ts` or the plan, each of which a suggested fix would
have touched and each of which is declined on scope grounds above.

## What this round did NOT do

- **No plan edit.** Criterion 9's restatement, the `verdict` count, and the D1
  and D2 ripples into steps 6 and 2 are the orchestrator's, sequenced after
  this round by the arbitration.
- **No `uncaughtException` handler at `bin/tiphys.ts`.** Out of scope; the
  bound is the `gates` subcommand only, stated in the CR-801 section.
- **No `runId` in the gate subprocess contract.** Declined with the reason.
- **The unaudited `units` claim is still unclosed**, unchanged from round 1 and
  restated as an open question rather than an impossibility. The hazard
  reviewer independently reached the same conclusion and noted one consequence
  worth carrying: `summary.json` deliberately carries no aggregate units total,
  so no consumer can currently be misled by a sum of unaudited claims. **If
  M2-P9 adds one it must be labelled as exactly that.**

## The claim grep, fix round 1 (CR-810: run AFTER the dispositions, recorded faithfully)

CR-810's finding was that round 1's recorded transcript did not reproduce: two
hits were recorded and fourteen came back, the extra twelve being the grep's
own transcript and its disposition table quoting the phrases they adjudicate.
So this round records BOTH numbers and says which is which.

**Raw, over the whole file, which is what the next reader gets if they run the
binding command verbatim:**

```
$ grep -cEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m2-p1.md
20
```

**With this file's own two grep transcripts and their disposition tables
excluded, which is the set that carries meaning:** 11 hits. Every one settled
below, with nothing reworded to duck the grep.

| line | disposition |
|---|---|
| 74 | the literal YAML expression `if: always()`, quoted from the workflow |
| 350 | the NAME of constraint M2-C-2, quoted from kernel plan M2 section 1.3 |
| 685 | round 1's own disposition line about hit 74 |
| 874 | "the ONLY entry into this subtree": captured `grep` for the dispatch entry and the exported function, adjacent |
| 895, 897 | "bounded by the catches above": settled by F4b and F5b, which stage the missing schema document and measure exit 21, never 1 |
| 913 | conditional and forward-looking ("if a later phase adds one"), and a statement about JavaScript semantics, not about this code. Left as the scope note it is |
| 926 | a quotation of `src/gates/validate.ts`'s own module comment |
| 1185 | "a legitimate recursive schema still validates": restated to name the control's exact assertion (`$ref: "#"`, two levels, one diagnostic at `#/child/child/name`) |
| 1192 | **RESTATED.** "changing either alone leaves the diff wrong or impossible" was an unexecuted claim about a GitHub runner. It now reads as a DERIVED expectation with the reason it could not be measured here, and it sits in the workflow comment as a thing for M2-P2 and M2-P5 to check, not as a thing this round proved |
| 1261 | "never reaches the executed code": settled by the F4/F4b pair, which differ only in whether the rebuild ran |

Three first-run hits in this round's text were changed rather than defended:
"it also catches replace-by-rename" now carries a measured inode and
`comparePins` output; "can only make more runs error, never fewer" now names
the disjunction in `comparePins` that makes it so; and the `fetch-depth`
coupling is restated as derived rather than known.

---

# FIX ROUND 2

Delta reviews on `3c7970b`: criteria APPROVE (amended semantics proven on the
reviewer's own fixtures against the compiled CLI, 8/8 mutations reddened);
hazard FIX-ROUND-NEEDED, 0 high, 2 medium, 5 low. Both mediums are children of
the round-1 CR-803 fix. Written INCREMENTALLY.

- Fetching the three round-2 artifacts from `claude/m2-era-paperwork`.

## CR-860 (MEDIUM): the claim's lifecycle was not tied to what it protects

### The MECHANISM, not the finding

The finding is "the crash path releases before writing, then releases again".
The mechanism, which the arbitration names once for both mediums:

> **Cleanup and writes that are valid only WHILE THE CLAIM IS HELD were
> performed from frames that do not know whether the claim is held, so the
> call graph was the only thing making them safe.**

A call graph is a reading of control flow. This whole phase exists because a
reading of control flow (CR-800's branch conditions) was wrong, and round 1's
answer there was to make the code check the property instead of relying on the
reading. The same answer applies here, and the coordinator is right that the
line deletion is the instance fix. Three things changed:

1. **`releaseEvidenceDirectory(evidenceDir, runId)` verifies holdership.** It
   reads the claim and unlinks ONLY when the runId is its own, which is what
   `src/lock.ts` does before mutating. A release from a frame that no longer
   holds the claim is now a no-op rather than a revocation of a live run's
   exclusion, whatever the call graph does. A claim this run does not hold is
   LEFT IN PLACE: deleting another run's claim is the harm.
2. **The call graph is fixed too**, because the guard alone would leave the
   WRITES outside the claimed region: exactly one release, in one `finally`,
   after every write. `writeAbortedSummary` moved inside the claimed region.
3. **Every write into the evidence directory verifies the claim**, through
   `writeInsideClaim`. This was not in the review's suggested fix and is the
   part that closes the mechanism rather than its two instances: a write is
   valid under exactly the same condition as a release, so the writer checks
   the same thing the releaser does.

Item 3 is not decoration, and the work history would be overstating without
this: **it is what made the ordering red-witnessable at all.** See G2b below.

### The derivation, published in full

Every call site of the release, and every write into the evidence directory:

```
$ grep -n 'releaseEvidenceDirectory\|writeInsideClaim\|guardedWrite\|claimEvidenceDirectory\|claimHolder' src/gates/run.ts
265:function writeInsideClaim(
271:  const holder = claimHolder(evidenceDir);          <- the write-side check
278:  return guardedWrite(path, body);
282:function guardedWrite(path: string, body: string): string | undefined {
606:  const stdoutRefusal = writeInsideClaim(           <- gate stdout capture
612:  const stderrRefusal = writeInsideClaim(           <- gate stderr capture
925:function claimEvidenceDirectory(
947:    const holder = claimHolder(evidenceDir);        <- names the holder in the refusal
998:  const staged = writeInsideClaim(                  <- summary stage
1005:function claimHolder(evidenceDir: string): string | undefined {
1045:export function releaseEvidenceDirectory(evidenceDir, runId): boolean {
1046:  const holder = claimHolder(evidenceDir);          <- the release-side check
1126:  writeInsideClaim(                                 <- aborted summary
1173:    releaseEvidenceDirectory(options.evidenceDir, runId);   <- THE ONLY release
1250:      writeInsideClaim(                             <- per-gate record rewrite
```

**One** release call site, down from two. **Five** writes into the directory,
all through `writeInsideClaim`. The first version of this fix left two of them
(the stdout and stderr captures at 606 and 612) on the unguarded path; running
this derivation is what showed it, and they were moved before submitting rather
than explained away in the work history.

### What this derivation did NOT cover

1. **The gates' own writes.** A gate subprocess writes to the `--result` path
   it was handed, and this runner cannot guard another process's write. That
   boundary is unchanged from round 1 and stated in the CR-803 not-covered
   list. Member (c) below shows the runner refusing to CERTIFY such a run,
   which is the part it can control.
2. **`guardedWrite` still exists and is still called directly, once, by
   `writeInsideClaim` itself.** Any future writer that calls it directly is
   outside this rule. The derivation above is the check for that, and it
   currently shows no such caller.
3. **The two-runner collision inside the old window was never witnessed**, by
   the reviewer or by me: the review measured the ORDERING and the width
   (2,134,947 ns), and my witnesses are ordering witnesses too. What is proven
   is that a write can no longer happen outside the claim and a release can no
   longer revoke a foreign claim; what is not proven is a real interleaving
   loss. **Restated as an open question rather than an impossibility:** I
   did not find a way to force that interleaving deterministically from
   this test suite, and I did not try a probabilistic loop. If a reviewer
   constructs one, that is a finding about the fix.
4. It does not cover `src/lock.ts`'s own claim, which is a different claim on
   a different resource and is not this phase's.

## CR-861 (MEDIUM): a refused run left another run's green in place and never said who it was

### The MECHANISM

> **The refusal path deliberately writes nothing, which is correct, so the only
> statement in the directory about what happened was a DIFFERENT run's; and the
> runId that attribution rests on never left the process.**

The second half is the one that matters, because round 1's decline of a
record-level runId rests on the compensating property "the runId is stamped in
the SUMMARY, so a bundle is attributable". Attribution needs the CALLER to know
the id it should see, and nothing gave it one. The property was false as
written, and the review is right that this is what the decline stood on.

The fix keeps the refusal writing nothing (writing into a directory owned by
another run is the very thing being prevented) and closes both halves:

- **Every `gates run` prints `gates: run <id>` on stdout before anything else
  it has to say**, on success, on failure and on refusal. A caller now knows
  the id it should see in `summary.json`, and a mismatch means the bundle is
  someone else's. `RunOutcome` carries `runId` on every path so the command
  layer always has it.
- **The refusal names BOTH ids and says, in words, what the directory holds**:
  "This run is `<mine>` and it wrote NOTHING: any summary.json in `<dir>`
  belongs to run `<theirs>`, not to this run, and is not a report about this
  invocation."

### The derivation, published in full

Every place the runId can leave the process, and every outcome that carries it:

```
$ grep -rn 'runId' src/commands/gates.ts src/gates/run.ts | grep -i 'write\|stdout\|stderr\|return\|summary:'
src/commands/gates.ts:128:  process.stdout.write(`gates: run ${outcome.runId}\n`);   <- THE EMISSION
src/gates/run.ts:274:      `refusing to write ${path}: this run (${runId}) ...`      <- write refusal
src/gates/run.ts:1012:    return typeof parsed.runId === "string" ? ...             <- claimHolder
src/gates/run.ts:1067:    return runGatesInner(options, runId);
src/gates/run.ts:1132:    return { runId, ... reason: dirRefusal };
src/gates/run.ts:1145:    return { runId, ... reason: claimRefusal, refused: true };
src/gates/run.ts:1158:    return { runId, ... };      (manifest invalid)
src/gates/run.ts:1170:    return { runId, ... };      (runner crashed)
src/gates/run.ts:1297:    return { runId, ... summary, reason: summaryRefusal };
src/gates/run.ts:1299:  return { runId, exitCode, summary, reason };
```

Before this round the same grep produced ONE line that put the id anywhere a
caller could see it, and it was inside `summary.json`. Seven return paths now
carry it, which is every return path `runGates` has.

### What this derivation did NOT cover

1. **It does not make the stale summary go away.** After a refusal the previous
   run's `summary.json` is still there, still saying exit 0 and
   `aborted: false`, because the refused run must not write into a directory it
   does not own. What changed is that a consumer can now TELL: it knows its own
   id and the file names a different one. **A consumer that reads
   `summary.json` without knowing which run it asked for is still misled**, and
   nothing in this phase can fix that, because such a consumer has not said
   what it is asking about. That is now written where M2-P9 will read it.
2. It does not cover consumers outside this tree. The review looked for one
   that reads `summary.json` without checking the exit code and found none
   outside `test/`; the exposure is to consumers M2-P9 will write.
3. It does not add `runId` to the GATE contract. That decline stands, and now
   the property it rests on is true rather than asserted.
4. The emission is a stdout LINE, so a caller that discards stdout does not get
   it. `RunOutcome.runId` is the in-process answer for anything that links the
   module; nothing outside `test/` does yet.

## The lows

| id | disposition |
|---|---|
| **CR-862** | **FIXED, made total, not argued.** The reviewer's own six probes plus fractional and Infinity now all report an internal inconsistency, and the consistent shape still exits 0. Every count is screened for being a non-negative safe integer and for being present at all, BEFORE the success assertion. The reachability argument was available and is deliberately not taken: this function's stated contract is to be handed states the runner cannot produce, and a guard that only rejects the value it was written against is the shape of the defect it was written for. Witnesses G8, G9. |
| **CR-864** | **CORRECTED IN THE RECORD**, since a pushed commit cannot be rewritten. Verified against the object, not through a shell: `git cat-file -p 4d7f5d9 \| od -c` shows `C R - 8 0 2 , ... C R - 8 1 1 :  (SP)(SP) s i b l i n g s   a r e   r e f u s e d   a t   l o a d`. **Two lines of commit `4d7f5d9`'s body are missing the token `$ref`**, eaten by shell interpolation in a double-quoted `-m`. They should read "CR-802, CR-807, CR-808, CR-811: **$ref** siblings are refused at load" and "A control found that **$ref** '#' did not resolve at all". A reader of `git log` should insert those two words. The habit fix: this round's commit messages contain no `$` at all. |
| **CR-865 + CR-880-1** | **FIXED as a habit, not a number.** See the claim grep at the end of this round, which is run LAST, after the file stopped changing, with the RAW OUTPUT pasted rather than a count. The recurrence cause both rounds was the same: the grep was run before the disposition table that quotes the phrases it adjudicates was appended, so the number described a file that no longer existed. A count of a file that contains the count is self-referentially unstable, so this round records output and not a count. |
| CR-863, CR-866 | Not mine; taken by the orchestrator on the paperwork branch. `MECHANISMS.md` and the plan are untouched by this round. |

## Red-witness table, fix round 2

| id | dangerous state | test | result |
|---|---|---|---|
| G1 / G1b | release stops verifying holdership (unlinks a FOREIGN claim) | gate-claim-released-only-by-holder | RED |
| G2 | the release removed entirely | gate-claim-released-only-by-holder | RED (but by the wrong mechanism, see below) |
| G2b | the round-1 ordering reinstated: release BEFORE the write | gate-claim-released-only-by-holder | **GREEN, and it was a finding** |
| G2c / G2d | the same ordering, against the write-side guard | gate-claim-released-only-by-holder | RED |
| G3 | the write-side claim verification removed (correct ordering) | gate-claim-released-only-by-holder | **GREEN, provable, see below** |
| G3b | the same, against member (c) | gate-claim-released-only-by-holder | RED |
| G4 | the runId no longer printed to the caller | gate-refused-run-is-attributable | RED |
| G5 | the refusal stops naming the other run and what it wrote | gate-refused-run-is-attributable | RED |
| G6 | the summary records a different id from the one printed | gate-refused-run-is-attributable | RED |
| G7 | the success assertion back to `counts.green === 0` | gate-aggregate-total-over-bad-counts | **GREEN, provable, see below** |
| G8 | the non-integer/negative count screen removed | gate-aggregate-total-over-bad-counts | RED |
| G9 | the missing-count-key screen removed | gate-aggregate-total-over-bad-counts | RED |

### Two structurally different members per class

| class | member 1 | member 2 | member 3 |
|---|---|---|---|
| CR-860, the claim's lifecycle | G1b, release without holdership | G2d, the write outside the claimed region | G3b, the claim lost mid-run |
| CR-861, attribution | G4, the id never emitted | G5, the refusal says nothing about whose bundle survives | G6, the emitted id and the recorded id decouple |
| CR-862, totality | G8, non-integer and negative | G9, missing key | |

### The greens, judged rather than tidied away

1. **G2b was a real finding in my own test, and it is the round's most useful
   result.** My first CR-860 member asserted only the END STATE (summary
   present, claim gone). That state is reachable BOTH ways, so reinstating the
   exact release-then-write ordering the review measured left the test green.
   An end state cannot see an ordering. The answer was not a cleverer
   assertion: it was to make the unsafe ordering PRODUCE A DIFFERENT OUTCOME,
   by having the write verify the claim. G2c and G2d then redden. This is the
   same lesson as round 1's F3 one level up, and it is why item 3 of the fix is
   in the fix at all.
2. **G3 is green and provably so, not a gap.** Removing the write-side check
   while the ordering is correct changes nothing observable, because with the
   correct ordering the claim IS held at every write. The check earns itself
   under member (c), where the claim is lost mid-run by another process, and
   G3b reddens there. Stated rather than hidden: with correct ordering and no
   external interference, the write-side check is unobservable by construction.
3. **G7 is green and provably so.** With the integer and presence screens in
   place, `counts.green === 0` and `!(counts.green > 0)` agree on every value
   that can reach them, so no input distinguishes the two forms. The `!(x > 0)`
   form is kept as belt and braces because it is the form that still fails
   closed if a later edit weakens the screens, which is exactly how this check
   came to be wrong in the first place. It is recorded as UNWITNESSABLE rather
   than as witnessed.

## Gate numbers, fix round 2

Floor toolchain, `node --version` checked in the shell that ran each command:

```
TOOLCHAIN: v26.6.0 / npm 11.18.0
npm ci (node_modules and dist removed first)   exit 0
npm run build                                  exit 0
npm test (dist REMOVED)                        exit 0   pass 191, fail 0, skipped 5
npm run build                                  exit 0
git status --porcelain after the build         only this round's tracked edits; no dist
npm test (dist present)                        exit 0   pass 196, fail 0, skipped 0
```

The 5 skips with `dist/` removed are this phase's dist-dependent tests, each
carrying its reason; CI builds before it tests, so all 196 run there.

Container default, through `bash -lc`:

```
TOOLCHAIN: v22.22.2 / npm 10.9.7
npm ci        exit 0
npm run build exit 0
npm test      exit 0   pass 194, fail 0, skipped 2
```

The 2 skips are the PRE-EXISTING floor-gated doctor tests. Every one of this
phase's 40 tests passes on both toolchains.

## Registry, fix round 2

```
behaviors registered      : 202
distinct test titles run  : 196
behaviors UNRESOLVED      : 0
titles with no registry   : 0
surplus keys (shared)     : 6   (pre-existing at the base)
```

3 added this round, 199 to 202. Zero overlapping keys, nothing removed or
retitled. Computed from the run's TAP output by script.

## Scope audit, fix round 2

```
$ git diff --name-status 037477e HEAD
M	.github/workflows/gates.yml
A	delivery/work-history/m2-p1.md
A	gates.manifest.json
M	package.json
M	src/cli.ts
A	src/commands/gates.ts
A	src/gates/manifest.ts
A	src/gates/pin.ts
A	src/gates/result.ts
A	src/gates/run.ts
A	src/gates/schemas/gate-manifest.schema.json
A	src/gates/schemas/gate-result.schema.json
A	src/gates/validate.ts
M	test/behaviors.json
A	test/gates.test.ts
A	test/pin.test.ts
```

The same 16 files as rounds 1 and 2's start: 14 files-to-touch plus the two
standing extras. **Zero outside scope.** This round touched four of them:
`src/gates/run.ts`, `src/commands/gates.ts`, `test/gates.test.ts`,
`test/behaviors.json`, plus the work history. `MECHANISMS.md` and the plan are
untouched, as instructed.

## Carried forward for M2-P9 and the concurrent phases

1. **A consumer must know which run it asked for.** `gates run` prints
   `gates: run <id>` and `summary.json` carries the same id. A consumer that
   reads the summary without comparing that id can still be handed another
   run's bundle, and no fix inside this phase can prevent that, because such a
   consumer has not said what it is asking about.
2. **The evidence claim has no expiry, deliberately.** A killed run leaves a
   claim that blocks reuse of that directory until a human deletes the file.
   The refusal names the file and says so. This is the stated residue of not
   importing `src/lock.ts`'s lease, whose expiry an evidence directory must
   not have.
3. **A gate's own writes are not guarded.** The runner verifies its own writes
   against the claim; it cannot verify a subprocess's. What it can do, and now
   does, is refuse to certify a run whose claim vanished underneath it.
4. Unchanged from round 1: `fetch-depth: 0` is needed with the `--head` fix
   before any `diff-touches` gate registers; there is no env precondition kind
   for M2-P8's `credential-token`; and a gate that reports `units` above zero
   having examined nothing is still uncloseable by the runner.

## The claim grep, fix round 2 (CR-865 and CR-880-1: run LAST, raw output, named commit)

The same finding was made twice, independently, and it RECURS from CR-810. The
cause was identical both times and it is not arithmetic: the grep was run
BEFORE the disposition table that quotes the phrases it adjudicates was
appended, so the recorded number described a file that no longer existed by the
time anyone could check it. A count of a file that contains the count is
self-referentially unstable.

**The habit, changed rather than the number corrected:** the grep is run LAST,
after the file has stopped changing, its RAW OUTPUT is pasted rather than a
count, and the COMMIT it was taken at is named so a reader can reproduce it
exactly. This block is appended after that commit, so the file you are holding
has exactly these hits plus the lines of this block; anyone can verify the
output below with:

    git show 9d0ad8b:delivery/work-history/m2-p1.md | grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to'

Raw output at `9d0ad8b`, 33 lines, truncated to 125 columns for width only:

```
74:always()`), and no `env:` block:
350:| M2-C-2 (never green by omission) | W1, at the runner's ingest | W2, at the constructor | W6, at the aggregate level |
678:$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery
679:74:always()`), and no `env:` block:
680:350:| M2-C-2 (never green by omission) | W1, at the runner's ingest | W2, at the constructor | W6, at the aggregate level
685:- line 74 is the literal YAML expression `if: always()`, quoted from
697:| "never imported as modules" | captured `grep` for JSON imports and `resolveJsonModule`, only hit is the comment saying 
698:| "`git checkout --` ... so it was never used" | captured `grep` over the red-witness harness source; it restores with `s
700:| "the two can never be confused" | captured a real `summary.json` showing `manifestGates` and `counts.declared` as separ
701:| "is covered by the exit-code/record consistency check" | replaced with the witness id, the exact defang, and its result
702:| "paths this runner never opens" | **RESTATED as a scope limit**: the `git` subprocesses' own file access was not enumer
703:| "`dist/` is never committed" | captured `git ls-files dist` (0) and `git check-ignore -v dist` |
874:The bounding catches. `cmdGates` is the ONLY entry into this subtree, so a
895:catches above report it as `error` with the path named. That is a choice, not
897:document and both produce exit 21 with a reason, never exit 1.
913:   one, the synchronous catches will not see it.
926:a document valid while never having checked the constraint that mattered".
1185:| CR-807 `$ref` cycle is a RangeError | **FIXED.** A ref chain is carried through `validateNode` and reset whenever an i
1192:| CR-830-1 `--head` is the merge SHA | **FIXED**, one line, plus a coupling recorded as a DERIVED expectation rather tha
1261:   is not followed by a rebuild never reaches the executed code. F4b and F5b
1369:$ grep -cEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' deliver
1379:| 74 | the literal YAML expression `if: always()`, quoted from the workflow |
1383:| 895, 897 | "bounded by the catches above": settled by F4b and F5b, which stage the missing schema document and measure
1387:| 1192 | **RESTATED.** "changing either alone leaves the diff wrong or impossible" was an unexecuted claim about a GitHu
1388:| 1261 | "never reaches the executed code": settled by the F4/F4b pair, which differ only in whether the rebuild ran |
1391:"it also catches replace-by-rename" now carries a measured inode and
1392:`comparePins` output; "can only make more runs error, never fewer" now names
1482:3. **The two-runner collision inside the old window was never witnessed**, by
1494:## CR-861 (MEDIUM): a refused run left another run's green in place and never said who it was
1500:> runId that attribution rests on never left the process.**
1515:  layer always has it.
1567:| **CR-864** | **CORRECTED IN THE RECORD**, since a pushed commit cannot be rewritten. Verified against the object, not 
1593:| CR-861, attribution | G4, the id never emitted | G5, the refusal says nothing about whose bundle survives | G6, the em
```

**Disposition of the 33.** Sixteen are inside this file's own three grep
transcripts and their disposition tables (lines 678-703, 1369-1392, and the
round-1 dispositions), quoting the very phrases they adjudicate; they are
self-references, not claims. The seventeen substantive hits are the eleven
dispositioned in round 1's table, unchanged, plus six new ones from this round:

| line | disposition |
|---|---|
| 1482 | "the two-runner collision was never witnessed": a stated NEGATIVE RESULT, which is the form this project asks for, and it is followed on the next line by what I did not try |
| 1494 | a section heading describing the defect that was fixed |
| 1500 | "the runId never left the process": settled by the D-861 derivation, which shows one line before and seven return paths after |
| 1515 | "the command layer always has it": same derivation, same lines |
| 1567 | "a pushed commit cannot be rewritten": true of a pushed commit by construction, and the reason this finding is corrected in the record instead |
| 1593 | a red-witness table row naming witness G4, whose result is in the table above |

One hit in this round's own text was RESTATED rather than defended: "which
needs a scheduler this test suite does not control" is now "I did not find a
way to force that interleaving deterministically from this test suite, and I
did not try a probabilistic loop", because the first version asserted something
about the world that I had not established.
