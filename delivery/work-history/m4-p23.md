# M4-P23: the retirement inventory and the rule re-verification pass

- branch: `claude/m4-p23-retirement-inventory`
- cut from: `plan/pstack-borrow-review` at de9b386
- spec: delivery/plan/kernel-plan-m4.md:3133
- deliverables: `delivery/plan/cutover/retirement-inventory.json` (280 rows),
  `delivery/plan/cutover/retirement-inventory.md` (the reasoning),
  `scripts/check-retirement-inventory.mjs` (the guard),
  `test/retirement-inventory.test.ts` (16 tests), two corrections to `CLAUDE.md`

## Minute-zero record: what I read, and the one contradiction I found

Written incrementally from the first minutes, per the dispatch contract. Read in
order: `CLAUDE.md` in full, the phase section at
delivery/plan/kernel-plan-m4.md:3133, delivery/plan/m4-conflict-pre-pass.md:1,
and delivery/verification/m4-prototype-probes.md:1.

**The dispatch and the spec name two different vocabularies, and they are two
AXES rather than one disagreement.** The dispatch says each row is `PORTED`, a
`GAP`, or `FALSE`. The spec at delivery/plan/kernel-plan-m4.md:3158 says the
closed vocabulary of three is `PORT` naming a kernel artifact, `DELETE` with a
reason, or `KEEP` with the DR-0029 side. Collapsing them loses information
either way: `PORTED` is a FINDING about the world as it is now, and `PORT` is a
DISPOSITION about what happens next. A rule can be a `GAP` today and still be
`KEEP`, because it belongs to this project's predicate rather than to the
kernel's process.

Resolution, recorded before any code: every row carries BOTH, as `status` in
`{PORTED, GAP, FALSE}` and `disposition` in `{PORT, DELETE, KEEP}`, and the
checker enforces the cross-consistency that makes them non-redundant
(`PORTED` if and only if `PORT`; a `GAP` row must name what kernel destination
is missing; a `FALSE` row must say what this phase corrected).

Nothing in my phase section contradicts a measurement in
delivery/verification/m4-prototype-probes.md:1. The probe document overturns
things about the credential scrub, the harness and the deploy gate; none of its
twelve items touches the retirement inventory.

## The grammar, and why the count is 280 rather than 24

Criterion 1 requires the row count to be produced by a committed extraction
command rather than a human count. The extraction is
`node scripts/check-retirement-inventory.mjs --extract`, and the grammar it
implements is declared in the script's own header.

**Choosing the grammar was the one real design decision in this phase, and the
alternatives were measured rather than argued.** A rule anchor is a line at
COLUMN ZERO that is a markdown heading, a top-level enumerated item, a top-level
bullet, a bolded lead-in paragraph, or (in the JavaScript root) a top-level
function or binding declaration. Measured on the three roots:

```
$ sh grammars.sh          # four greps over the six markdown roots, one over the js root
headings only, all markdown roots:
59
headings + numbered:
147
headings + numbered + bold lead-ins:
193
headings + numbered + bold + bullets:
264
javascript root declarations:
16
CLAUDE.md alone, headings only:
24
CLAUDE.md alone, headings + numbered:
82
```

| grammar (the six markdown roots) | rules found | what it drops |
|---|---|---|
| headings only | 59 | the 7 binding conventions, the 14 standing warnings, the 7 `## Never` bullets |
| plus top-level enumerated items | 147 | the `## Never` bullets and every bolded lead-in |
| plus bolded lead-ins | 193 | every top-level bullet |
| **plus top-level bullets (shipped)** | **264** | nothing above column-zero granularity |

The JavaScript root contributes 16 top-level declarations under its own half of
the grammar, so the shipped total is 280. In `CLAUDE.md` alone the first two
grammars find 24 and 82.

The third row is the one that decided it: **the rule this phase was dispatched
to correct is a bolded lead-in**, not a heading and not a numbered item, and a
heading-only grammar would not have given it a row at all. The fourth row is the
one I nearly did not take, because 71 more rows is real work; the `## Never`
bullets settled it, since C-1, C-2 and C-3 are stated there and nowhere else in
that file.

Column zero is load-bearing: an indented `1.` inside a fenced block is
CONTINUATION of the rule above it, not a new rule, and treating it as one would
inflate the count with fragments.

Per-root counts at this head:

```
$ node scripts/check-retirement-inventory.mjs --extract | cut -f2 | cut -d: -f1 | sort | uniq -c
     16 .claude/orchestrator-next.mjs
     24 .claude/skills/adversarial-verification/SKILL.md
     17 .claude/skills/agent-salvage/SKILL.md
     11 .claude/skills/owner-decisions/SKILL.md
     29 .claude/skills/phase-delivery/SKILL.md
     18 .claude/skills/phase-delivery/references/clean-room-brief.md
     32 .claude/skills/phase-delivery/references/implementer-brief.md
    133 CLAUDE.md
```

The checker compares the extraction to the rows BY ID and in BOTH directions,
which is strictly stronger than the count comparison criterion 1 asks for: a
count passes when one rule gains a row and another loses one, and set equality
does not. The full 280-line extraction is at the end of this document.

## What each row asserts

```
{
  "id": "claude-md:tiphys-kernel-repository-rules",
  "root": "CLAUDE.md",
  "rule": "CLAUDE.md:1",
  "kind": "heading",
  "text": "# Tiphys kernel: repository rules",
  "group": "agent-rules-single-source",
  "status": "PORTED",
  "disposition": "PORT",
  "destination": "roles/README.md",
  "note": "The kernel's answer to a single rules file is the per-role brief set ...",
  "probe": "mandated-reading",
  "verified-by": {
    "command": "grep -c 'mandated-reading' roles/README.md",
    "exit": 0,
    "output": "2"
  },
  "negative-witness": {
    "kind": "sibling",
    "command": "grep -c 'mandated-reading' templates/warnings.md",
    "exit": 1,
    "output": "0"
  }
}
```

**Every exit code and every output string in the JSON was produced by RUNNING
the command**, by a generator that executed each one and recorded what came
back. None was written by hand. That is the red-witness rule's requirement about
captured output applied to this artifact: a hand-written `"exit": 0` is
indistinguishable from a measured one after the fact, and the checker re-runs
all 466 commands on every invocation, so a hand-written one would redden at
once. [FIX ROUND: 466 was wrong when it was written and it is wrong now, in
different directions. It was 467 at 7b2b7f1 and it is 468 here. See "Six numbers
the round corrected" below.]

`grep -c` rather than `grep -q` throughout, deliberately. Measured:

```
$ grep -q 'mandated-reading' roles/README.md | wc -c
0
```

`-q` exits 0 and prints nothing, so an empty capture carries no information
about whether the command ran at all. `grep -c` prints a count on both arms, so
the recorded output is evidence on the red arm as well as the green one.

## The four FALSE rows

**1. The measured one the phase section names.** `CLAUDE.md` said scope
declaration grants still need their own pull request because the scope gate
reads the declaration from the MERGE BASE only. src/gates/scope.ts:110 records
that M3-P11 changed exactly that.

```
$ grep -c 'from BOTH the merge base and the head' src/gates/scope.ts
1
$ echo "exit=$?"
exit=0
```

Corrected at CLAUDE.md:794. The plan cites this rule as `CLAUDE.md:766`; at the
head I was given it is at line 790 before my edit and 794 after, because
`CLAUDE.md` gained content between the plan being written and this dispatch.
The MEASUREMENT is the line I found and corrected, not the line the plan
predicted, and this is recorded rather than silently reconciled.

**2. `schemas/`, `roles/` and `tuition/` are not placeholders.** The rule said
they were reserved for M3 deliverables and told every implementer not to
populate them early. They have been populated since M3 landed.

```
$ ls roles/ | wc -l
7
$ ls schemas/ | wc -l
17
$ ls tuition/ | wc -l
17
```

Corrected at CLAUDE.md:61. This one is worth more than its size: it is a rule
that was false for an entire milestone, sitting in the first file every agent is
told to read, and nobody caught it because nobody re-verified it.

**3. Phases are not sequential until M5** (the phase-delivery skill's
pre-dispatch checklist). DR-0011 superseded that, and this phase was itself
dispatched in a concurrent wave (delivery/plan/m4-conflict-pre-pass.md:64).
[FIX ROUND: this cited :60, a blank line. The wave-1b row naming M4-P23 is at
:64. In range, so the gate stayed green and the citation pointed at nothing.]

**4. Merge authority is not unconditionally the owner's** (the same skill's
merge step). DR-0012 delegated it conditional on dual cross-model clean review.

Rows 3 and 4 are in `.claude/skills/phase-delivery/SKILL.md`, which is NOT on
this phase's files-to-touch list, so the text was deliberately not edited. Both
rows record the correction and name M4-P25's deletion as where the text dies.
`CLAUDE.md` already carries the true rule in both cases, so no agent reading the
governing file is currently misled; an agent reading the skill alone is.

## RED WITNESS for the checker

The dangerous state is not an absent checker. It is a checker that reads the
rows and reports green because THE FIELDS ARE PRESENT. That guard is green and
worthless, it is the shape this repository has recorded three times, and it is
what criterion 5 member B is about.

### Member A: an ABSENT field

The fixture is three real PORT rows with one row's `verified-by` deleted. Run
against the real roots:

```
$ node scripts/check-retirement-inventory.mjs --repo <repo> --json <member-a.json>
retirement-inventory: 3 row(s) against 280 derived rule anchor(s), commands EXECUTED
  UNRESOLVED claude-md:tiphys-kernel-repository-rules: no verified-by block
retirement-inventory: 279 unresolved item(s)
exit 1
```

The 279 adds up, and it is worth adding up: 277 rules have no row in this
deliberately tiny fixture, plus the deleted field, plus "no row is marked FALSE"
because three PORT rows carry no FALSE row between them. The orphan arm and the
criterion-6 arm are both firing, which is them working. The line the test
asserts on is the first one.

### Member B: a PRESENT field that asserts nothing, and its control

The fixture replaces a PORT row's negative witness with a multi-file grep whose
subject list still INCLUDES the destination, which is the shape a careless
author actually writes. The field is present, well formed, a different command
from `verified-by`, and it carries the probe. It cannot go red, and the
capture immediately below is what settles that rather than the sentence: the
executing checker reports the witness exiting 0.

Two invocations, one fixture, captured:

```
member B mutates row claude-md:tiphys-kernel-repository-rules
its witness becomes: grep -c 'mandated-reading' AGENTS.md roles/README.md

$ node scripts/check-retirement-inventory.mjs --repo <repo> --json <member-b.json> --no-execute
retirement-inventory: 280 row(s) against 280 derived rule anchor(s), structure only, commands NOT executed
retirement-inventory: every rule in the three roots is resolved
exit 0

$ node scripts/check-retirement-inventory.mjs --repo <repo> --json <member-b.json>
retirement-inventory: 280 row(s) against 280 derived rule anchor(s), commands EXECUTED
  UNRESOLVED claude-md:tiphys-kernel-repository-rules: negative-witness exits 0: the probe does not discriminate, so the PORT is unwitnessed
retirement-inventory: 1 unresolved item(s)
exit 1
```

The mutated witness greps for the row's probe in `AGENTS.md` and in the
DESTINATION that carries it, so it exits 0 whatever `AGENTS.md` contains. The
row's real witness greps `templates/warnings.md` alone and exits 1.

**That pair IS the witness.** The field-checking checker calls the vacuous
inventory clean; the executing checker names it. `--no-execute` exists in the
shipped script for exactly this reason: the control has to be runnable, not
described.

The two members are structurally different in the way the criterion requires:
member A is caught by a field check, member B survives every field check and is
caught only by execution.

**A third arm, found while building the fixture and kept.** My first attempt at
a vacuous witness simply re-ran the `verified-by` command, and the STRUCTURAL
pass caught it as byte-identical. That is a real guard and it is not the one
under test, so the fixture was changed to one that defeats it. Recorded because
a fixture that is caught by the wrong check is a false witness for the right
one.

### The other fourteen

All sixteen tests and what they redden against:

| test | dangerous state it is red against |
|---|---|
| resolves every rule | (the green arm) |
| no verified-by block | member A, an absent field |
| negative witness that exits 0 | member B, a vacuous present field |
| negative witness runs a different probe | a witness that reddens for an unrelated reason |
| a rule with no row | a rule added to a root and never dispositioned |
| a row whose rule is not extractable | a rule reworded or deleted under a surviving row |
| verified-by names no kernel path | a row that re-verifies itself against the document it retires |
| a recorded exit code no longer reproduces | a capture that has gone stale |
| a command outside the allowlist | a row that runs something a checker has no business running |
| no row is marked FALSE | criterion 6 silently unmet |
| PORT with a status other than PORTED | the two axes drifting apart |
| GAP without a gap field | an unresolved row wearing a verdict |
| unreadable inventory exits 2 | a crash rendered as a pass |
| empty derivation exits 2 | a broken extraction reported as a clean sheet |
| extraction deterministic | an unstable id scheme making rows unmatchable |
| roots still present | criterion 7, and the set-equality check going vacuously true |

The command-allowlist test also asserts the named path still exists afterwards,
because a screen that reports a refusal after running the command is not a
screen.

## The defect this phase found in its own checker, with the derivation

**MECHANISM, not the finding: a program that writes a large report to stdout and
then calls `process.exit()` loses the tail of the report, because a pipe makes
that write asynchronous and `process.exit()` does not wait for it.**

The finding was three failing tests. The first run failed tests 3, 7 and 14; the
next run of the same code failed 3, 7 and 8; test 7 passed alone and failed in
company. That is not a flaky test, it is a truncation point moving with timing.
The checker prints one line per unresolved row, so a negative fixture emits tens
of kilobytes and the assertions look for a line near the end.

The fix is `process.exitCode = main(...)` at the foot of
`scripts/check-retirement-inventory.mjs`, quoted rather than cited because the
line number moves with every edit to that file. After it, the test file was run
three consecutive times with 15 of 15 passing and no failure; the sixteenth test
(the GAP-field guard) was added later and the file is 16 of 16 now.

**THE DERIVATION, published in full rather than summarised:**

```
$ grep -rn "process\.exit(" scripts/ bin/ src/
scripts/release-verify.sh:148:        process.exit(0);
scripts/release-verify.sh:167:            process.exit(0);
scripts/m1-exit-test.sh:1162:      process.exit(1);
scripts/check-brief-drift.mjs:456:process.exit(main(process.argv.slice(2)));
scripts/check-retirement-inventory.mjs:543: * `process.exitCode`, NEVER `process.exit()`, AND THIS IS A MEASURED BUG RATHER
scripts/check-retirement-inventory.mjs:544: * THAN A STYLE NOTE. `process.exit()` tears the process down with writes still
scripts/check-retirement-inventory.mjs:548: * what a caller greps for. With `process.exit()` three of fifteen tests failed
scripts/render-agent-rules-gates.mjs:348:process.exit(main(process.argv.slice(2)));
scripts/m2-exit-test.sh:455:  process.exit(2);
scripts/m2-exit-test.sh:479:  process.exit(2);
scripts/m2-exit-test.sh:489:  process.exit(1);
scripts/m2-exit-test.sh:521:  process.exit(1);
scripts/m2-exit-test.sh:634:  process.exit(1);
scripts/m2-exit-test.sh:924:  process.exit(1);
scripts/m2-exit-test.sh:951:process.exit(0);
scripts/m2-exit-test.sh:1023:  process.exit(2);
scripts/m2-exit-test.sh:1248:  process.exit(1);
scripts/m2-exit-test.sh:1251:process.exit(0);
scripts/m2-exit-test.sh:1441:process.exit(0);
scripts/m2-exit-test.sh:1473:          command: ["node", "-e", "process.exit(0)"],
src/hooks.ts:51:  process.exit(64);
src/gates/credentials.ts:691:    process.exit(gateMain(process.argv.slice(2)));
src/gates/run.ts:642: *   node -e "process.exit(1)"      status 1     error undefined
src/gates/run.ts:868: *   NOT SUFFICIENT: inline code (`process.exit(existsSync("/marker")?0:1)`),
src/gates/suite.ts:1142:  process.exit(runSuiteGate(process.argv.slice(2)));
src/gates/red-witness.ts:574:  process.exit(main(process.argv.slice(2)));
$ echo "exit=$?"
exit=0
```

**Five sibling sites have the same shape**, each an `exit-with-the-return-value`
tail on a program that writes a report: `scripts/check-brief-drift.mjs`,
`scripts/render-agent-rules-gates.mjs`, `src/gates/credentials.ts`,
`src/gates/red-witness.ts` and `src/gates/suite.ts`. Measured output sizes
today, which is why none of them has been bitten yet:

```
$ node scripts/check-brief-drift.mjs 2>&1 | wc -c
2110
$ node scripts/render-agent-rules-gates.mjs --check 2>&1 | wc -c
154
```

Both are far below a pipe buffer, so the write completes synchronously enough
that the exit does not truncate it. **I did not find a way to force truncation
at those two sizes**, and I am not claiming they are safe: the trigger is output
VOLUME, which is data-dependent, and a gate runner reporting many findings is
exactly the case that grows. **All five are outside this phase's files-to-touch
list and were NOT changed.** They are reported here for the orchestrator to
route, which is the correct handling of a mechanism whose call sites cross a
scope boundary.

**What the derivation did NOT cover**, stated before any row of it is examined:

- It searched `scripts/`, `bin/` and `src/` only. It did NOT search `test/`,
  where an `process.exit` in a helper would have the same effect on a captured
  child, nor `.claude/`, which contains `orchestrator-next.mjs` and ends with
  `process.exit(exitCode)` after printing its report. That script is in the
  retirement set and row `next-script:exitcode` dispositions it; it is not fixed
  here and it is not fixed anywhere, because it is being deleted.
- It matched the literal `process.exit(`. An indirect call (`const e =
  process.exit; e(1)`) or a re-export would not appear. I did not probe for
  those and I do not know whether any exist.
- It says nothing about `process.stdout.write` callers that exit through an
  uncaught throw, which truncates in the same way for a different reason.

## What the inventory says, in three numbers and one shape

280 rows: 187 PORTED, 89 GAP, 4 FALSE. Dispositions: 187 PORT, 76 KEEP, 17
DELETE. The reasoning, the destination distribution and the gap list are in
delivery/plan/cutover/retirement-inventory.md:134 rather than repeated here.

The one shape worth putting in both places: **the gaps are concentrated on the
process side, and the loudest of them is that constraint C-3 is enforced in
shipped code and stated in no instruction channel at all.**

**[FIX ROUND: THE SENTENCE ABOVE IS FALSE AND IT IS LEFT STANDING SO THE RECORD
SHOWS WHAT WAS BELIEVED.** C-3 is stated at AGENTS.md:497 and bound into a
shipped schema at schemas/assurance-modes.schema.json:37. The row that carried
this is now PORTED/PORT. The mechanism behind it, and the check that now
compares an absence claim's two scopes, are in "The mechanism" below. It is the
same failure this section goes on to describe for C-1, one paragraph later, and
it went uncorrected because widening the TREES searched was mistaken for
widening the SEARCH.]

**AND THE FIRST VERSION OF THAT SENTENCE SAID C-1 AS WELL, AND IT WAS WRONG.**
This is the phase's own instance of the failure the fix-round contract's third
item exists against, committed by the person building the inventory meant to
prevent it, so it is written up rather than quietly fixed.

The first pass searched the briefs and the checklists:

```
$ grep -c 'log tail' AGENTS.md roles/implementer.md checklists/hazard-review.yaml checklists/clean-room.yaml
AGENTS.md:0
roles/implementer.md:0
checklists/hazard-review.yaml:0
checklists/clean-room.yaml:0
$ echo "exit=$?"
exit=1
```

Exit 1, no hits, and I wrote GAP. **The scope of that search was wrong.** C-1 is
in the MECHANISM INDEX, which is a third instruction channel I had not thought
to include:

```
$ grep -n 'Never read current state from the tail of a log' tuition/mechanism-index.yaml
45:      Never read current state from the tail of a log (constraint C-1).
$ echo "exit=$?"
exit=0
$ grep -n 'append-only log' roles/implementer.md
175:`tuition/mechanism-index.yaml` (a claim file, a lease, an append-only log, a
```

roles/implementer.md:172 is the `mechanism-lookup` clause: before writing code
that uses a mechanism the index names, LOOK IT UP. So an implementer IS told,
by a route I did not search. The row is now PORTED with
tuition/mechanism-index.yaml:45 as its destination.

**What made the difference was widening the search, not thinking harder**, which
is exactly what the contract says: an empty result from a wrongly scoped search
is indistinguishable from an absence. The widened search is what stands behind
the C-3 gap:

```
$ grep -rniE 'auto-background|background' AGENTS.md roles/ checklists/ templates/ tuition/ schemas/
roles/implementer.md:93:   `mechanism-lookup` clause below: this is not background reading, it is a
$ echo "exit=$?"
exit=0
```

One hit, and it is the ordinary English word. `AGENTS.md` plus five trees,
searched case-insensitively, including the one that caught C-1 out. C-1 and C-2 both have
mechanism-index entries; C-3 has none, and that asymmetry between three
constraints of the same family is what makes the gap real rather than a phrasing
miss.

**What this search still does not cover**: it looked in the kernel's PROSE and
DATA artifacts. It did not search `src/` for a comment stating C-3 to a reader
of the code, because a comment in the implementation is not an instruction
channel for an agent, and it did not check whether any SCHEMA field would force
an agent to declare compliance.

## What this phase did NOT cover

1. **The verification is per DESTINATION CLAUSE, not per rule.** Where several
   rows share a kernel clause they share its probe, so the row-level claim is
   "this rule's destination is that clause" and the command-level claim is "that
   clause exists and carries the named obligation". 280 rows map to 119
   disposition groups.
2. **"Not weaker" is mechanical in FORM and still a reading in SUBSTANCE.** The
   witness pair proves the probe resolves in the destination and does not
   resolve in a same-kind sibling. It does not prove the destination's clause is
   as strong as the retired rule. The claim-grep row is the exception where the
   mechanical form reaches the substance, because roles/implementer.md:295
   carries the grep PATTERN literally; most rows are not that lucky. This is the
   thing a reviewer should spot-check, and it is named here rather than left to
   be discovered.
3. **The negative witnesses are all `sibling` kind, none `history`.** A witness
   that runs the probe against a pre-port git blob would be literally "red under
   the old rule". I chose sibling because a history witness needs full history
   and fails in a shallow clone, which would make the checker's verdict depend
   on how the repository was fetched. The checker accepts both kinds, so a later
   phase can strengthen individual rows without changing the guard.
4. **The checker does not compare captured OUTPUT, only exit codes.** Byte
   comparison would redden every time a file grew a line, which would train a
   reader to regenerate captures without reading them. The output is recorded
   for a human; the exit code is what is enforced.
5. **Two of the four FALSE rows are recorded and not edited**, because their
   text is in the skills tree and this phase's files-to-touch list does not
   include it.
6. **Nothing in the three roots is deleted** (criterion 7). It needs no separate
   mechanism: a deletion removes an anchor and the surviving row goes red as
   stale, which is what the rule-without-a-row and stale-row tests assert.
7. **The rows were assigned by reading**, in groups, first by section span and
   then pinned by RULE ID. The switch was forced by a bug of my own: correcting
   `CLAUDE.md` shifted every line after the edit, the span table shifted with it
   by hand, and sixteen rows silently took their NEIGHBOUR's disposition. It was
   caught by diffing the regenerated rows against the previous ones rather than
   by the checker, which was green throughout because every row still had a
   well-formed group. Ids are content-derived and immune to line motion, so the
   committed mapping is now by id. The JUDGMENT that a given rule belongs to a
   given kernel clause is still mine and is the reviewable part.
8. **The checker was GREEN while those sixteen rows were misassigned**, which
   is measured rather than argued:

   ```
   $ node scripts/check-retirement-inventory.mjs
   retirement-inventory: 280 row(s) against 280 derived rule anchor(s), commands EXECUTED
   retirement-inventory: every rule in the three roots is resolved
   exit 0
   ```

   That run is the one taken immediately before the row-by-row diff found the
   sixteen. A misassigned row points at the wrong destination with a probe that
   still resolves there, so every mechanical check it faces passes. I did not
   find a check that would separate it from a correct row, and I am not claiming
   none exists. That is the honest limit of this guard: it enforces that every
   rule has a RESOLVED disposition, not that the disposition is the RIGHT one,
   and the row-to-clause judgment stays the reviewable part.

## The correction nearly became a second false rule, and the measurement that stopped it

**This is the most useful thing in the phase and it happened by running the
gate rather than by reading it.** The first version of my CLAUDE.md correction
said the both-declarations read means "a declaration amendment lands WITH the
phase that needs it", full stop. Then I ran the scope gate against my own
branch:

```
$ node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full \
    --only scope --evidence <scratch> --base origin/main --head HEAD --phase m4-p23
gates: declared 1 applicable 1 verdict 1 green 0 red 1 not-applicable 0 error 0 vacuous 0
gates: scope: red: branch claude/m4-p23-retirement-inventory (phase m4-p23) matches the
  phase pattern but no phase declaration exists at
  delivery/plan/phase-declarations/m4-p23.json in the merge base
  3b401182301361700ffa0fd4b2ae099993b3d733 of --base origin/main and --head HEAD;
  the declaration must be committed to main before the phase branch is created
exit 1
```

The relaxation is about ENTRIES INSIDE a declaration. The declaration FILE must
still exist at the merge base, and src/gates/scope.ts:877 is the refusal that
says so. Had I stopped at the first version, this phase would have deleted one
false rule and written another one in its place, in the same edit, in the file
every agent reads first. The correction now states both halves and cites the
line.

**Two consequences, both for the orchestrator rather than for me.**

1. **This branch's scope gate is RED until
   `delivery/plan/phase-declarations/m4-p23.json` is on `main`.** It is on my
   branch (the phase section's files-to-touch list puts it there) and the gate
   wants it at the merge base. That is the residue CLAUDE.md already names as
   "not yet fully available", narrowed by this measurement to the FILE rather
   than to grants. I cannot fix it from here: I do not push to `main`.
2. **The branch was cut from `plan/pstack-borrow-review`, as the dispatch
   instructed, so `git diff --name-only origin/main...HEAD` carries that
   branch's 30 files as well as my 6.** A scope audit taken against `origin/main`
   will see all of them. Against the branch I was told to cut from, my change is
   exactly the six declared paths:

```
$ git diff --name-only plan/pstack-borrow-review...HEAD
CLAUDE.md
delivery/plan/cutover/retirement-inventory.json
delivery/plan/cutover/retirement-inventory.md
delivery/plan/phase-declarations/m4-p23.json
delivery/work-history/m4-p23.md
scripts/check-retirement-inventory.mjs
test/behaviors.json
test/retirement-inventory.test.ts
```

   Eight paths, all eight on the declaration's `filesToTouch`. Sequencing the
   two branches is the orchestrator's call and I have not assumed one.

## An environment warning I created rather than inherited

I stopped a long-running suite with `pkill -f "node --test test/**"`. **That
pattern is broader than it looks and this container runs several agents at
once.** `pkill -f` takes a REGEX, so `test/**` is "test/" followed by any number
of asterisks, which matches EVERY `node --test test/...` process on the machine,
including other agents' worktrees. A `node --test test/cutover-entry.test.ts`
belonging to another unit was visible in `ps` immediately afterwards; I cannot
tell from `ps` alone whether it survived my signal or was restarted after it,
and I am not claiming either.

The rule I should have followed, and the one to carry: **never kill by pattern
in a shared container.** Kill by the PID of the process you started, or let it
finish. This is the same shape as the watchdog rules one level down: a selector
that matches more than its author intends is indistinguishable from a correct
one until it hits something.

## Gates

All commands run from the phase worktree at
`/home/user/tiphys-ai-helmsman/.claude/worktrees/wf_2b81e806-d59-2`.

**Interpreter and build state, stated before any number**, because a bare pass
count is the incomplete sentence this repository has paid for three times. The
container default is node v22.22.2 at `/opt/node22/bin/node`, which is BELOW the
declared floor of >=26. A node v26.6.0 toolchain exists in this session's
scratchpad and was NOT used for the suite, deliberately: standing warning 1
records that `/tmp/claude-0` is `drwx------`, so `runCliUnprivileged` in
`test/gates.test.ts` cannot spawn an interpreter living under it when the
repository is NOT under `/tmp`. This worktree is under `/home/user`, so the
scratch toolchain would fail that test for a reason that has nothing to do with
this branch. It WAS used for the one-test control below, where that helper is
not involved.

| gate | command | result |
|---|---|---|
| install | `npm ci` | exit 0 |
| build | `npm run build` | exit 0 |
| clean tree after build | `git status --short` | 0 lines |
| suite | `npm test` | **exit 1, 2 failures, both established as not this branch** |
| authored bytes | `node scripts/check-authored-bytes.mjs` | exit 0 |
| citations | `tiphys gates run --only citations --base origin/main --head HEAD` | exit 0, green, 521 resolved |
| the phase's own checker | `node scripts/check-retirement-inventory.mjs` | exit 0 |
| scope | `tiphys gates run --only scope ...` | exit 1, declaration not on `main` yet |

**THE COMPLETE SENTENCE FOR THE SUITE RESULT.** Interpreter node v22.22.2
(`/opt/node22/bin/node`, the container default, below the >=26 floor); `dist/`
BUILT by `npm run build` immediately before the run; invocation `npm test`,
which is `node --test "test/**/*.test.ts"` and therefore excludes the tracked
`sandbox/` fixture that a bare `node --test` picks up; head 6eae9b02df7a.
Result: **865 tests, 861 pass, 2 fail, 2 SKIPPED, exit 1**, duration 1513830ms.

The skipped count is quoted beside the pass count because a skipped test is not
a passing test.

### The two failures, and what established that neither is this branch

**Failure 1: test/doctor.test.ts:934, "a staged install of the built package
reproduces the captured contract live".** This is the floor-DEPENDENT test that
standing warning 12 already records as failing on the container default at a
head whose CI is green. One variable changed, the interpreter, same head, same
working tree, same single test:

```
$ node --test --test-name-pattern "a staged install of the built package reproduces the captured contract live" test/doctor.test.ts
not ok 1 - a staged install of the built package reproduces the captured contract live
  location: '.../test/doctor.test.ts:934:1'
  error: the unpromoted arm must not fail the fleet
    1 !== 0
# tests 1
# pass 0
# fail 1
exit 1

$ /tmp/.../node-v26.6.0-linux-x64/bin/node --test --test-name-pattern "..." test/doctor.test.ts
[PASS] a staged install of the built package reproduces the captured contract live (1785.03032ms)
[i] tests 1
[i] pass 1
[i] fail 0
exit 0
```

TRANSLITERATION DECLARED, as the ASCII rule requires. The node v26.6.0 capture
above is real output from `node --test` with exactly two codepoints replaced:
U+2714 (heavy check mark), 1 occurrence, rendered `[PASS]`; and U+2139
(information source), 8 occurrences, rendered `[i]`. Nothing else in any
captured output in this document was altered. The v22.22.2 capture needed no
substitution: it contains no non-ASCII byte.

**Failure 2: test/coverage-gate.test.ts:146, the coverage gate through the real
manifest.** It asserted the runner's coverage row was `green` and got `error`.
Re-run in isolation at the SAME head, the same test file is clean:

```
$ node --test test/coverage-gate.test.ts
# tests 17
# pass 17
# fail 0
# skipped 0
exit 0
```

And the gate itself, run directly rather than through the nested runner:

```
$ node src/gates/coverage.ts --result <scratch>/result.json --evidence <scratch>
coverage: green (115 finding ids checked)
115 inventory id(s) checked; per-kind: milestone 104, phase 11; per-milestone: M1 11, M2 16, M3 74, M4 13, M5 1
exit 0
```

**The condition that differed is measured, not guessed: machine load.** This
container was running nine other agent worktrees while the suite ran.
`uptime` during the failing run reported a load average of **51.22 on 4 CPUs**;
at the isolated re-run it was 30.07 and falling. That test spawns a FULL nested
gate-runner bundle from inside a test that is itself one of 46 concurrent test
processes, so it is the most spawn-heavy assertion in the suite and the first to
suffer.

**What that does NOT establish**, stated rather than left comfortable: I have
not reproduced the `error` verdict on demand, so "load caused it" is the
explanation the evidence favours and not one I forced. The detail string that
would name the cause is written into the evidence directory the test deletes in
its `finally`, so the failing run left nothing to read. **A test that discards
the only diagnostic it produces when it fails is worth a finding of its own**,
and it is outside this phase's files-to-touch list, so it is reported here
rather than changed.

**Neither failure touches anything this branch changes.** The branch's whole
diff against the branch it was cut from is eight files:

```
$ git diff --name-only plan/pstack-borrow-review...HEAD
CLAUDE.md
delivery/plan/cutover/retirement-inventory.json
delivery/plan/cutover/retirement-inventory.md
delivery/plan/phase-declarations/m4-p23.json
delivery/work-history/m4-p23.md
scripts/check-retirement-inventory.mjs
test/behaviors.json
test/retirement-inventory.test.ts
```

`test/doctor.test.ts` reads `dist/` and the fleet layout; `test/coverage-gate.test.ts`
reads `delivery/requirements/migration-table.md`, the plan and
`gates.manifest.json`. None of those eight paths is among them.

### The rest, captured

```
$ node scripts/check-authored-bytes.mjs
$ echo "exit=$?"
exit=0

$ node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full \
    --only citations --evidence <scratch> --base origin/main --head HEAD
gates: declared 1 applicable 1 verdict 1 green 1 red 0 not-applicable 0 error 0 vacuous 0
gates: citations: green: linted 17 changed document(s) at 985054d59e25ff410d25f2ad09499e4536457a90:
  521 citation(s) resolved, 0 self-citation(s), 0 unverifiable-external
exit 0

$ node scripts/check-retirement-inventory.mjs
retirement-inventory: 280 row(s) against 280 derived rule anchor(s), commands EXECUTED
retirement-inventory: every rule in the three roots is resolved
exit 0

$ node --test test/retirement-inventory.test.ts
# tests 16
# pass 16
# fail 0
# skipped 0
```

The citations run lints 17 documents rather than my two, because this branch is
cut from `plan/pstack-borrow-review` as the dispatch instructed and that branch
is not on `main` yet, so its documents are in the diff as well.

**The checker is enforced by the SUITE, not by a registry gate.** Wiring it into
`gate-registry.yaml` and `gates.manifest.json` would have needed two files this
phase's files-to-touch list does not carry. `node --test` is step 3 of the gate
list, `test/retirement-inventory.test.ts` runs the checker against the real
roots, and that is the path by which a broken inventory reddens CI. Whether it
should ALSO be a registry gate is a real question and it belongs to whoever owns
the registry, not to this phase.

**Commits after the suite head are PROSE ONLY, which is the resolution the
phase-delivery procedure prescribes for the recording recursion.** The suite
result above belongs to head `6eae9b0`; every commit after it touches
`delivery/work-history/m4-p23.md` and
`delivery/plan/cutover/retirement-inventory.md` and nothing else, and that is
checkable rather than asserted with `git diff --name-only 6eae9b0..HEAD`.

The citations, ASCII, build and checker results above were taken at `985054d`,
later than the suite head. That is deliberate rather than the recursion in
disguise: those four are cheap enough to re-run at the tip, and the only commit
after `985054d` edits `delivery/work-history/m4-p23.md`, which the citations
gate does not lint. Its `documents` list reaches `delivery/plan/**`,
`delivery/verification/**`, `delivery/decisions/**`, `delivery/tuition/**`,
`delivery/requirements/**` and `delivery/STATE.md`, and not
`delivery/work-history/**`. So the 521 is still the count at the tip, which is a
fact to re-run rather than to trust.

**Backgrounding the suite was a declared act, not an auto-background.** The run
takes twenty-five minutes, longer than a foreground call here allows, so it was
started detached and then waited on explicitly, in a blocking call, before this
section was written. C-3 forbids a kernel COMMAND from putting long-running work
out of an operator's sight; this is the opposite, a measurement whose completion
is the thing being waited for.


## Appendix: the derivation, in full

Criterion 1 asks for the extraction command's output in the work history. This
is it, verbatim, at the phase head, with no line omitted. The three columns after
the id are the rule's location, the anchor kind that matched it, and the line
itself truncated by the terminal rather than by me.

```
$ node scripts/check-retirement-inventory.mjs --extract
claude-md:tiphys-kernel-repository-rules	CLAUDE.md:1	heading	# Tiphys kernel: repository rules
claude-md:what-this-repository-is	CLAUDE.md:11	heading	## What this repository is
claude-md:delivery-intake-orchestrated-delivery-process-md	CLAUDE.md:21	numbered	1. `delivery/intake/orchestrated-delivery-process.md`, the process being run.
claude-md:delivery-plan-kernel-plan-v1-md-the-owner-approv	CLAUDE.md:22	numbered	2. `delivery/plan/kernel-plan-v1.md`, the owner-approved plan. Its binding
claude-md:delivery-decisions-owner-decision-records-a-deci	CLAUDE.md:25	numbered	3. `delivery/decisions/`, owner decision records. A decided record is
claude-md:this-file	CLAUDE.md:27	numbered	4. This file.
claude-md:durability-rule-why-this-file-exists	CLAUDE.md:29	heading	## Durability rule (why this file exists)
claude-md:where-things-live	CLAUDE.md:56	heading	## Where things live
claude-md:delivery-is-the-build-s-own-paperwork-it-is-not	CLAUDE.md:58	bullet	- `delivery/` is the build's own paperwork. It is not shipped in the npm
claude-md:src-bin-test-are-the-kernel-itself	CLAUDE.md:60	bullet	- `src/`, `bin/`, `test/` are the kernel itself.
claude-md:schemas-roles-tuition-at-the-repository-root-are	CLAUDE.md:61	bullet	- `schemas/`, `roles/`, `tuition/` at the repository root are SHIPPED M3
claude-md:claude-skills-holds-the-procedures-for-running-t	CLAUDE.md:70	bullet	- `.claude/skills/` holds the procedures for running this repository's
claude-md:binding-conventions	CLAUDE.md:75	heading	## Binding conventions
claude-md:english-only	CLAUDE.md:77	numbered	1. English only.
claude-md:npm-only-never-pnpm-or-yarn	CLAUDE.md:78	numbered	2. npm only, never pnpm or yarn.
claude-md:no-em-dashes-in-any-authored-text-commas-colons	CLAUDE.md:79	numbered	3. No em dashes in any authored text (commas, colons, parentheses instead).
claude-md:a-citation-is-path-ext-line-and-only-outside-bac	CLAUDE.md:159	numbered	3b. **A CITATION IS `path.ext:LINE`, AND ONLY OUTSIDE BACKTICKS. A bare path is
claude-md:falsifiable-acceptance-criteria-only-works-corre	CLAUDE.md:225	numbered	4. Falsifiable acceptance criteria only; "works correctly" is banned; the
claude-md:one-phase-one-branch-one-pr-always-parallelism-i	CLAUDE.md:227	numbered	5. One phase = one branch = one PR, always. Parallelism is ON where a
claude-md:milestone-exit-tests-are-hard-gates-no-milestone	CLAUDE.md:254	numbered	6. Milestone exit tests are hard gates: no milestone starts before the
claude-md:commit-messages-carry-no-ai-model-or-tool-names	CLAUDE.md:256	numbered	7. Commit messages carry no AI model or tool names.
claude-md:gates	CLAUDE.md:258	heading	## Gates
claude-md:gate-registry-yaml-is-the-canonical-gate-registr	CLAUDE.md:260	bold-lead	**`gate-registry.yaml` is the canonical gate registry and the source this
claude-md:r-094-is-partially-delivered-and-the-half-that-i	CLAUDE.md:269	bold-lead	**R-094 is PARTIALLY delivered, and the half that is not is stated here rather
claude-md:npm-ci-install-exactly-the-lockfile-npm-only-nev	CLAUDE.md:284	numbered	1. `npm ci` (install exactly the lockfile, npm only, never pnpm or yarn)
claude-md:npm-run-build-the-type-gate-tsc-b-emits-dist-whi	CLAUDE.md:285	numbered	2. `npm run build` (the type gate (tsc -b); emits dist/, which is never committed, and git status must be clean afterwards)
claude-md:node-test-sources-are-typescript-run-natively-vi	CLAUDE.md:286	numbered	3. `node --test` (sources are TypeScript run natively via Node type stripping, so the suite needs no prior build)
claude-md:red-witness-rule	CLAUDE.md:324	heading	## Red-witness rule
claude-md:fix-round-contract-measured-2026-08-05	CLAUDE.md:337	heading	## Fix-round contract (measured, 2026-08-05)
claude-md:name-the-mechanism-not-the-finding-a-fifo-at-the	CLAUDE.md:353	numbered	1. **Name the MECHANISM, not the finding.** "A FIFO at the beacon hangs the
claude-md:publish-the-derivation-the-exact-command-that-en	CLAUDE.md:356	numbered	2. **Publish the derivation.** The exact command that enumerates every call
claude-md:state-what-the-derivation-did-not-cover-the-regi	CLAUDE.md:358	numbered	3. **State what the derivation did NOT cover.** The regions the search
claude-md:the-reviewer-s-first-check-is-item-3-before-exam	CLAUDE.md:366	bold-lead	**The reviewer's FIRST check is item 3**, before examining any row.
claude-md:the-claim-grep-also-binding	CLAUDE.md:373	heading	### The claim grep, also binding
claude-md:the-command-above-is-line-based-and-this-prose-i	CLAUDE.md:388	bold-lead	**THE COMMAND ABOVE IS LINE-BASED AND THIS PROSE IS HARD-WRAPPED, so a hit
claude-md:one-witness-is-not-a-class	CLAUDE.md:420	heading	### One witness is not a class
claude-md:dispatch-contract-no-agent-without-a-beacon-and	CLAUDE.md:427	heading	## Dispatch contract: no agent without a beacon and a guard (T-008, binding)
claude-md:three-rules-all-mechanical-binding-on-every-disp	CLAUDE.md:445	bold-lead	**THREE rules, all mechanical, binding on every dispatch:**
claude-md:every-dispatched-agent-writes-its-output-increme	CLAUDE.md:447	numbered	1. **Every dispatched agent writes its output INCREMENTALLY.** It creates its
claude-md:a-freshness-watchdog-is-armed-in-the-same-turn-a	CLAUDE.md:451	numbered	2. **A freshness watchdog is armed in the SAME TURN as the dispatch.** It
claude-md:the-watchdog-itself-expires-and-re-arming-it-is	CLAUDE.md:455	numbered	3. **THE WATCHDOG ITSELF EXPIRES, AND RE-ARMING IT IS PART OF THE RULE.**
claude-md:and-the-newest-mtime-under-the-agent-s-working-d	CLAUDE.md:484	bold-lead	**AND "THE NEWEST MTIME UNDER THE AGENT'S WORKING DIRECTORY" IS NOT MECHANICAL
claude-md:where-does-this-agent-write-measure-it-do-not-pr	CLAUDE.md:495	numbered	1. **Where does THIS agent write? MEASURE IT, DO NOT PREDICT IT.** An earlier
claude-md:what-is-the-baseline-before-its-first-write-disp	CLAUDE.md:515	numbered	2. **What is the baseline before its first write?** DISPATCH TIME, never the
claude-md:what-does-silence-mean-here-state-which-of-dead	CLAUDE.md:519	numbered	3. **What does silence mean here?** State which of "dead", "in a long run" and
claude-md:and-an-unisolated-agent-takes-your-clone-measure	CLAUDE.md:524	bold-lead	**AND AN UNISOLATED AGENT TAKES YOUR CLONE, measured 2026-09-15.** Six
claude-md:isolate-every-dispatched-implementer-including-t	CLAUDE.md:533	numbered	1. **Isolate EVERY dispatched implementer, including the ones that only write
claude-md:the-orchestrator-takes-its-own-worktree-before-d	CLAUDE.md:536	numbered	2. **The orchestrator takes its own worktree before dispatching**, so its
claude-md:exclude-the-orchestrator-s-own-worktrees-from-an	CLAUDE.md:548	bold-lead	**Exclude the orchestrator's own worktrees from any agent watchdog.** Including
claude-md:green-is-scoped-to-the-run-that-produced-it-t-00	CLAUDE.md:553	heading	## Green is scoped to the run that produced it (T-009, binding)
claude-md:the-mechanism-a-gate-result-is-evidence-only-for	CLAUDE.md:565	bold-lead	**The mechanism: a gate result is evidence only for the configuration it ran
claude-md:a-merge-is-not-complete-until-the-post-merge-pus	CLAUDE.md:571	numbered	1. **A merge is not complete until the post-merge `push` run on the new `main`
claude-md:where-behavior-forks-on-the-ci-event-both-arms-n	CLAUDE.md:575	numbered	2. **Where behavior forks on the CI event, BOTH arms need a witness.** One
claude-md:a-green-bundle-is-not-evidence-that-a-particular	CLAUDE.md:585	heading	### A green BUNDLE is not evidence that a PARTICULAR gate asserted anything
claude-md:the-gate-id-is-in-the-gates-manifest-json-on-tha	CLAUDE.md:608	numbered	1. the gate id is in the `gates.manifest.json` **on that branch** (the harness
claude-md:the-bundle-s-declared-count-equals-that-manifest	CLAUDE.md:610	numbered	2. the bundle's `declared` count equals that manifest's gate count;
claude-md:the-required-gate-s-not-applicable-line-does-not	CLAUDE.md:611	numbered	3. the `required gate(s) not applicable:` line does NOT name the gate (a
claude-md:the-assertion-line-reports-zero-error-zero-vacuo	CLAUDE.md:614	numbered	4. the assertion line reports `zero error; zero vacuous`.
claude-md:say-which-half-is-observed-and-which-is-deduced	CLAUDE.md:620	bold-lead	**Say which half is observed and which is deduced.** In that example the units
claude-md:branch-names-are-load-bearing-not-labels-binding	CLAUDE.md:627	heading	## Branch names are load-bearing, not labels (binding)
claude-md:claude-m3-p1-prereqs-a-prerequisites-branch-deri	CLAUDE.md:637	bullet	- `claude/m3-p1-prereqs` (a prerequisites branch) derived phase `m3-p1` and
claude-md:claude-m3-p1-reviews-a-review-evidence-branch-de	CLAUDE.md:640	bullet	- `claude/m3-p1-reviews` (a review-evidence branch) derived `m3-p1`, found the
claude-md:rule-only-the-phase-s-own-implementation-branch	CLAUDE.md:644	bold-lead	**Rule: only the phase's own implementation branch may match that pattern.**
claude-md:identifier-schemes	CLAUDE.md:659	heading	## Identifier schemes
claude-md:a-retired-id-is-never-reused-in-any-of-these-sch	CLAUDE.md:663	bold-lead	**A RETIRED id is never reused, in ANY of these schemes.** The rule is stated
claude-md:sc-nnn-spec-coherence-findings-intake-verificati	CLAUDE.md:683	bullet	- `SC-nnn` spec-coherence findings (intake verification)
claude-md:r-nnn-requirement-rows-migration-table	CLAUDE.md:684	bullet	- `R-nnn` requirement rows (migration table)
claude-md:fm-nnn-firstmate-scout-findings	CLAUDE.md:685	bullet	- `FM-nnn` firstmate scout findings
claude-md:pr-nnn-internal-plan-review-findings-ext-f-nn-ex	CLAUDE.md:686	bullet	- `PR-nnn` internal plan-review findings; `EXT-F-nn` external review findings
claude-md:cr-nnn-clean-room-review-findings-on-a-pr	CLAUDE.md:687	bullet	- `CR-nnn` clean-room review findings on a PR
claude-md:v-n-and-u-n-verification-findings-and-unrefuted	CLAUDE.md:688	bullet	- `V-n` and `U-n` verification findings and unrefuted candidates
claude-md:dr-nnnn-owner-decision-records	CLAUDE.md:689	bullet	- `DR-nnnn` owner decision records
claude-md:t-nnn-tuition-entries	CLAUDE.md:690	bullet	- `T-nnn` tuition entries
claude-md:c-n-binding-implementation-constraints-declared	CLAUDE.md:691	bullet	- `C-n` binding implementation constraints declared in the plan
claude-md:d-nn-decisions-taken-inside-the-plan	CLAUDE.md:692	bullet	- `D-nn` decisions taken inside the plan
claude-md:a-n-owner-actions-things-only-the-owner-can-perf	CLAUDE.md:693	bullet	- `A-n` owner ACTIONS: things only the owner can perform because they need
claude-md:delivery-protocol	CLAUDE.md:705	heading	## Delivery protocol
claude-md:when-to-involve-the-owner-dr-0016-binding-escala	CLAUDE.md:711	bold-lead	**When to involve the owner (DR-0016, binding).** Escalate ONLY when two or
claude-md:a-commit-a-pull-request-and-a-ci-run-are-three-d	CLAUDE.md:746	heading	## A commit, a pull request and a CI run are three different things (DR-0031, binding)
claude-md:a-commit-is-a-unit-of-work	CLAUDE.md:752	numbered	1. **A commit is a unit of work.**
claude-md:a-pull-request-is-a-unit-of-self-contained-value	CLAUDE.md:753	numbered	2. **A pull request is a unit of SELF-CONTAINED VALUE and carries ALL its
claude-md:ci-enforces-that-main-stays-green-it-is-not-how	CLAUDE.md:758	numbered	3. **CI enforces that `main` stays green. It is NOT how you find out whether you
claude-md:main-is-the-record-of-work-actually-done-evidenc	CLAUDE.md:761	bold-lead	**`main` is the record of work actually DONE.** Evidence about an abandoned
claude-md:the-mirror-of-t-019-and-neither-the-gate-nor-any	CLAUDE.md:767	heading	### The mirror of T-019, and neither the gate nor any review caught it
claude-md:local-green-before-opening-and-it-is-stronger-th	CLAUDE.md:779	heading	### Local green before opening, and it is STRONGER than the CI run
claude-md:if-ci-tells-you-something-you-did-not-already-kn	CLAUDE.md:785	bold-lead	**If CI tells you something you did not already know locally, that is a defect in
claude-md:corrected-2026-09-16-by-measurement-m4-p23-the-r	CLAUDE.md:794	bold-lead	**CORRECTED 2026-09-16 BY MEASUREMENT (M4-P23): the rule that stood here was
claude-md:be-exact-about-what-changed-because-the-correcti	CLAUDE.md:806	bold-lead	**BE EXACT ABOUT WHAT CHANGED, because the correction is easy to over-read and
claude-md:standing-environment-warnings	CLAUDE.md:821	heading	## Standing environment warnings
claude-md:three-node-versions-are-installed-and-which-one	CLAUDE.md:825	numbered	1. THREE Node versions are installed and which one you get depends on how
claude-md:typescript-is-pinned-exact-do-not-remove-types-n	CLAUDE.md:884	numbered	2. `typescript` is pinned exact. Do not remove `"types": ["node"]` from
claude-md:tsbuildinfo-is-gitignored-deliberately-tsc-b-wri	CLAUDE.md:886	numbered	3. `*.tsbuildinfo` is gitignored deliberately; `tsc -b` writes one at the
claude-md:importing-a-src-module-from-test-with-a-literal	CLAUDE.md:889	numbered	4. Importing a `src` module from `test/` with a literal relative path fails
claude-md:tests-that-create-scratch-git-repositories-must	CLAUDE.md:893	numbered	5. Tests that create scratch git repositories must set command-scoped
claude-md:gh-is-absent-locally-and-present-in-ci-use-a-det	CLAUDE.md:897	numbered	6. `gh` is absent locally and present in CI. Use a deterministic gh-free
claude-md:test-name-pattern-must-precede-the-positional-te	CLAUDE.md:962	numbered	7. `--test-name-pattern` must precede the positional test path, or it is
claude-md:git-checkout-wipes-uncommitted-sibling-edits-cop	CLAUDE.md:964	numbered	8. `git checkout --` wipes uncommitted sibling edits. Copy before
claude-md:git-remote-set-url-resolves-relative-paths-again	CLAUDE.md:971	numbered	9. `git remote set-url` resolves relative paths against the repository, not
claude-md:concurrent-git-operations-against-one-clone-cont	CLAUDE.md:985	numbered	10. Concurrent git operations against one clone contend on ref locks, and
claude-md:suite-wall-time-grows-with-real-clock-lease-wait	CLAUDE.md:989	numbered	11. Suite wall time grows with real-clock lease waits. Budget harness
claude-md:running-the-suite-without-building-first-silentl	CLAUDE.md:991	numbered	12. **Running the suite without building first SILENTLY SKIPS NINE TESTS, and
claude-md:git-diff-main-branch-is-not-a-merge-preview-and	CLAUDE.md:1064	numbered	13. **`git diff main..branch` IS NOT A MERGE PREVIEW, and on a branch that has
claude-md:this-container-cannot-delete-a-remote-ref-and-dr	CLAUDE.md:1088	numbered	14. **THIS CONTAINER CANNOT DELETE A REMOTE REF, AND `--dry-run` WILL TELL YOU
claude-md:the-orchestrator-does-not-decide-when-it-is-fini	CLAUDE.md:1144	heading	## The orchestrator does not decide when it is finished (binding)
claude-md:the-keep-going-mechanism-was-not-durable-croncre	CLAUDE.md:1154	numbered	1. **The keep-going mechanism was not durable.** `CronCreate` jobs are
claude-md:even-when-the-kick-fired-the-orchestrator-decide	CLAUDE.md:1158	numbered	2. **Even when the kick fired, the orchestrator decided whether work existed.**
claude-md:the-mechanism-and-it-is-two-layers-because-one-i	CLAUDE.md:1162	bold-lead	**The mechanism, and it is two layers because one is not enough:**
claude-md:claude-orchestrator-next-mjs-computes-the-stop-c	CLAUDE.md:1164	bullet	- `.claude/orchestrator-next.mjs` computes the stop condition from git and
claude-md:a-server-side-routine-create-trigger-hourly-the	CLAUDE.md:1171	bullet	- A server-side Routine (`create_trigger`, hourly, the durable floor) whose
claude-md:none-of-these-is-a-reason-to-stop-and-every-one	CLAUDE.md:1177	bold-lead	**None of these is a reason to stop, and every one has been used:** having just
claude-md:agent-concurrency-is-per-workflow-and-the-fix-is	CLAUDE.md:1183	heading	## Agent concurrency is PER WORKFLOW, and the fix is more workflows (binding)
claude-md:measured-2026-09-15-nproc-returns-4-and-the-work	CLAUDE.md:1188	bold-lead	**Measured 2026-09-15: `nproc` returns 4, and the workflow cap is
claude-md:the-fix-is-n-workflows-of-2-not-one-workflow-of	CLAUDE.md:1194	bold-lead	**The fix is N workflows of 2, not one workflow of 2N.** Each workflow gets its
claude-md:write-the-script-once-with-an-args-filter-so-one	CLAUDE.md:1199	numbered	1. Write the script ONCE with an args filter, so one script serves every slice:
claude-md:launch-it-inline-the-first-time-the-tool-result	CLAUDE.md:1206	numbered	2. Launch it inline the first time. The tool result returns a `scriptPath`.
claude-md:re-invoke-with-scriptpath-args-once-per-pair-fou	CLAUDE.md:1207	numbered	3. Re-invoke with `{scriptPath, args: [...]}` once per PAIR. Four extra calls
claude-md:before-dispatching-any-fan-out-state-in-writing	CLAUDE.md:1210	bold-lead	**Before dispatching any fan-out, state in writing: how many agents, in how many
claude-md:model-choice-is-per-agent-and-costs-nothing-to-s	CLAUDE.md:1216	bold-lead	**Model choice is per agent and costs nothing to set.** `agent(prompt, {model:
claude-md:reporting-to-the-owner-binding-2026-09-15	CLAUDE.md:1221	heading	## Reporting to the owner (binding, 2026-09-15)
claude-md:the-owner-s-screen-is-an-interface-for-decisions	CLAUDE.md:1226	bold-lead	**The owner's screen is an interface for DECISIONS, not a log of the work.**
claude-md:a-decision-the-owner-must-take-with-the-options	CLAUDE.md:1234	numbered	1. A decision the owner must take, with the options and a recommendation.
claude-md:an-action-only-the-owner-can-perform-because-it	CLAUDE.md:1235	numbered	2. An action only the owner can perform, because it needs access an agent does
claude-md:a-finished-result	CLAUDE.md:1239	numbered	3. A finished result.
claude-md:a-blocker-in-one-line-naming-what-is-blocked-and	CLAUDE.md:1240	numbered	4. A blocker, in one line, naming what is blocked and by what.
claude-md:plain-language-is-part-of-this-the-owner-reads-e	CLAUDE.md:1246	bold-lead	**Plain language is part of this.** The owner reads English fluently and is not
claude-md:never	CLAUDE.md:1253	heading	## Never
claude-md:never-push-to-main-directly-never-merge-your-own	CLAUDE.md:1255	bullet	- Never push to `main` directly; never merge your own work.
claude-md:never-reopen-a-decided-owner-decision-record-rai	CLAUDE.md:1256	bullet	- Never reopen a decided owner decision record; raise a new one instead.
claude-md:never-improvise-an-irreversible-choice-the-plan	CLAUDE.md:1257	bullet	- Never improvise an irreversible choice the plan is silent on. Stop and
claude-md:never-use-pid-process-liveness-signals-or-proc-f	CLAUDE.md:1259	bullet	- Never use pid, process liveness, signals, or `/proc` for identity or
claude-md:never-read-current-state-from-the-tail-of-an-app	CLAUDE.md:1261	bullet	- Never read current state from the tail of an append-only log (C-1).
claude-md:never-auto-background-a-long-running-process-c-3	CLAUDE.md:1262	bullet	- Never auto-background a long-running process (C-3).
claude-md:never-soften-a-work-history-it-is-the-artifact-a	CLAUDE.md:1263	bullet	- Never soften a work history. It is the artifact a later reviewer trusts,
skill-adversarial-verification:adversarial-verification	.claude/skills/adversarial-verification/SKILL.md:6	heading	# Adversarial verification
skill-adversarial-verification:when-to-spend-this	.claude/skills/adversarial-verification/SKILL.md:12	heading	## When to spend this
skill-adversarial-verification:foundation-work-every-later-phase-inherits-the-l	.claude/skills/adversarial-verification/SKILL.md:17	bullet	- Foundation work every later phase inherits (the lock, the executor
skill-adversarial-verification:any-fix-round-at-least-in-a-reduced-form-a-fix-r	.claude/skills/adversarial-verification/SKILL.md:19	bullet	- Any fix round, at least in a reduced form. A fix round closing four
skill-adversarial-verification:work-where-a-green-suite-is-known-to-be-weak-evi	.claude/skills/adversarial-verification/SKILL.md:22	bullet	- Work where a green suite is known to be weak evidence, for example
skill-adversarial-verification:shape	.claude/skills/adversarial-verification/SKILL.md:30	heading	## Shape
skill-adversarial-verification:lenses-parallel-independent-four-to-six-agents-e	.claude/skills/adversarial-verification/SKILL.md:34	bold-lead	**Lenses (parallel, independent).** Four to six agents, each with ONE angle
skill-adversarial-verification:completeness-of-a-claimed-propagation-or-contrac	.claude/skills/adversarial-verification/SKILL.md:39	bullet	- Completeness of a claimed propagation or contract (enumerate every path,
skill-adversarial-verification:the-design-that-was-salvaged-rewritten-or-reason	.claude/skills/adversarial-verification/SKILL.md:41	bullet	- The design that was salvaged, rewritten, or reasoned about rather than
skill-adversarial-verification:data-loss-and-destructive-authority-always-its-o	.claude/skills/adversarial-verification/SKILL.md:43	bullet	- Data loss and destructive authority (always its own lens where any
skill-adversarial-verification:the-false-negative-introduced-by-a-narrowing-or	.claude/skills/adversarial-verification/SKILL.md:45	bullet	- The false negative introduced by a narrowing or optimization (the
skill-adversarial-verification:suite-integrity-determinism-registry-honesty-sco	.claude/skills/adversarial-verification/SKILL.md:47	bullet	- Suite integrity, determinism, registry honesty, scope, conventions, and
skill-adversarial-verification:refutation-parallel-per-finding-two-refuters-per	.claude/skills/adversarial-verification/SKILL.md:56	bold-lead	**Refutation (parallel, per finding).** Two refuters per finding, each with
skill-adversarial-verification:synthesis-one-agent-writes-the-report-verdict-a	.claude/skills/adversarial-verification/SKILL.md:68	bold-lead	**Synthesis.** One agent writes the report: verdict, a section per
skill-adversarial-verification:investigating-an-unexplained-failure	.claude/skills/adversarial-verification/SKILL.md:78	heading	## Investigating an unexplained failure
skill-adversarial-verification:brute-reproduction-in-the-exact-failing-configur	.claude/skills/adversarial-verification/SKILL.md:84	bullet	- Brute reproduction in the exact failing configuration, many runs, full
skill-adversarial-verification:the-instrument-is-lying-the-test-seam-harness-or	.claude/skills/adversarial-verification/SKILL.md:86	bullet	- The instrument is lying (the test seam, harness, or fixture produced a
skill-adversarial-verification:the-system-is-genuinely-broken-try-to-force-the	.claude/skills/adversarial-verification/SKILL.md:88	bullet	- The system is genuinely broken (try to force the invariant violation with
skill-adversarial-verification:cross-component-interference-shared-paths-leaked	.claude/skills/adversarial-verification/SKILL.md:90	bullet	- Cross-component interference (shared paths, leaked environment, resource
skill-adversarial-verification:hard-rules	.claude/skills/adversarial-verification/SKILL.md:105	heading	## Hard rules
skill-adversarial-verification:agents-work-in-isolated-copies-never-in-a-worktr	.claude/skills/adversarial-verification/SKILL.md:107	bullet	- Agents work in isolated copies, never in a worktree another agent is
skill-adversarial-verification:nothing-an-agent-finds-is-committed-by-that-agen	.claude/skills/adversarial-verification/SKILL.md:109	bullet	- Nothing an agent finds is committed by that agent; the orchestrator
skill-adversarial-verification:a-finding-without-a-concrete-fix-is-half-a-findi	.claude/skills/adversarial-verification/SKILL.md:111	bullet	- A finding without a concrete fix is half a finding.
skill-adversarial-verification:do-not-merge-around-an-unexplained-failure-in-fo	.claude/skills/adversarial-verification/SKILL.md:112	bullet	- Do not merge around an unexplained failure in foundation code. Settle it
skill-agent-salvage:agent-salvage	.claude/skills/agent-salvage/SKILL.md:6	heading	# Agent salvage
skill-agent-salvage:procedure	.claude/skills/agent-salvage/SKILL.md:17	heading	## Procedure
skill-agent-salvage:1-inspect-before-touching	.claude/skills/agent-salvage/SKILL.md:19	heading	### 1. Inspect before touching
skill-agent-salvage:2-measure-its-real-state-do-not-assume	.claude/skills/agent-salvage/SKILL.md:31	heading	### 2. Measure its real state, do not assume
skill-agent-salvage:3-commit-with-the-mandated-label	.claude/skills/agent-salvage/SKILL.md:44	heading	### 3. Commit with the mandated label
skill-agent-salvage:4-resume-with-a-binding-verify-or-rewrite-instru	.claude/skills/agent-salvage/SKILL.md:63	heading	### 4. Resume with a binding verify-or-rewrite instruction
skill-agent-salvage:that-it-died-and-what-was-salvaged-and-committed	.claude/skills/agent-salvage/SKILL.md:68	bullet	- That it died, and what was salvaged and committed on its behalf, by SHA.
skill-agent-salvage:the-measured-state-of-the-salvage-marked-as-the	.claude/skills/agent-salvage/SKILL.md:69	bullet	- The measured state of the salvage, marked as the orchestrator's
skill-agent-salvage:your-reading-of-what-the-salvage-contains-and-wh	.claude/skills/agent-salvage/SKILL.md:71	bullet	- Your reading of what the salvage contains and what is missing, explicitly
skill-agent-salvage:the-binding-rule-it-must-verify-or-rewrite-the-s	.claude/skills/agent-salvage/SKILL.md:73	bullet	- The binding rule: it must VERIFY OR REWRITE the salvaged content, and may
skill-agent-salvage:the-full-original-task-restated-do-not-assume-th	.claude/skills/agent-salvage/SKILL.md:77	bullet	- The full original task, restated. Do not assume the agent remembers its
skill-agent-salvage:5-expect-the-salvage-to-be-wrong	.claude/skills/agent-salvage/SKILL.md:80	heading	### 5. Expect the salvage to be wrong
skill-agent-salvage:record-it	.claude/skills/agent-salvage/SKILL.md:91	heading	## Record it
skill-agent-salvage:related-failure-shapes	.claude/skills/agent-salvage/SKILL.md:98	heading	## Related failure shapes
skill-agent-salvage:stalled-not-dead-no-transcript-activity-for-a-lo	.claude/skills/agent-salvage/SKILL.md:100	bullet	- **Stalled, not dead**: no transcript activity for a long period with no
skill-agent-salvage:died-after-pushing-nothing-to-salvage-but-the-br	.claude/skills/agent-salvage/SKILL.md:103	bullet	- **Died after pushing**: nothing to salvage, but the branch may carry
skill-agent-salvage:killed-mid-write-a-file-may-be-truncated-check-t	.claude/skills/agent-salvage/SKILL.md:106	bullet	- **Killed mid-write**: a file may be truncated. Check that every touched
skill-owner-decisions:owner-decisions	.claude/skills/owner-decisions/SKILL.md:6	heading	# Owner decisions
skill-owner-decisions:when-to-raise-one	.claude/skills/owner-decisions/SKILL.md:12	heading	## When to raise one
skill-owner-decisions:the-record	.claude/skills/owner-decisions/SKILL.md:27	heading	## The record
skill-owner-decisions:recommendation-one-option-with-the-reasoning-tha	.claude/skills/owner-decisions/SKILL.md:37	bold-lead	**Recommendation** (one option, with the reasoning that would change if the
skill-owner-decisions:reporting-to-the-owner	.claude/skills/owner-decisions/SKILL.md:49	heading	## Reporting to the owner
skill-owner-decisions:recording-an-answer	.claude/skills/owner-decisions/SKILL.md:59	heading	## Recording an answer
skill-owner-decisions:owner-action-items	.claude/skills/owner-decisions/SKILL.md:77	heading	## Owner action items
skill-owner-decisions:never	.claude/skills/owner-decisions/SKILL.md:87	heading	## Never
skill-owner-decisions:never-reopen-a-decided-record-raise-a-new-one-th	.claude/skills/owner-decisions/SKILL.md:89	bullet	- Never reopen a decided record. Raise a new one that cites it.
skill-owner-decisions:never-improvise-an-irreversible-choice-because-a	.claude/skills/owner-decisions/SKILL.md:90	bullet	- Never improvise an irreversible choice because a phase is blocked. A
skill-owner-decisions:never-bury-a-decision-inside-a-status-report-it	.claude/skills/owner-decisions/SKILL.md:92	bullet	- Never bury a decision inside a status report. It gets its own record and
skill-phase-delivery:phase-delivery	.claude/skills/phase-delivery/SKILL.md:6	heading	# Phase delivery
skill-phase-delivery:0-before-dispatch	.claude/skills/phase-delivery/SKILL.md:17	heading	## 0. Before dispatch
skill-phase-delivery:the-previous-phase-s-pr-is-merged-phases-are-seq	.claude/skills/phase-delivery/SKILL.md:21	bullet	- The previous phase's PR is merged. Phases are sequential until M5.
skill-phase-delivery:the-plan-section-for-this-phase-exists-and-its-b	.claude/skills/phase-delivery/SKILL.md:22	bullet	- The plan section for this phase exists and its `blocked-by` decisions are
skill-phase-delivery:if-a-milestone-boundary-is-being-crossed-the-pre	.claude/skills/phase-delivery/SKILL.md:25	bullet	- If a milestone boundary is being crossed, the previous milestone's exit
skill-phase-delivery:1-create-the-phase-worktree	.claude/skills/phase-delivery/SKILL.md:28	heading	## 1. Create the phase worktree
skill-phase-delivery:2-brief-and-dispatch-the-implementer	.claude/skills/phase-delivery/SKILL.md:41	heading	## 2. Brief and dispatch the implementer
skill-phase-delivery:3-receive-the-report-and-audit-scope	.claude/skills/phase-delivery/SKILL.md:51	heading	## 3. Receive the report and audit scope
skill-phase-delivery:4-open-the-pr	.claude/skills/phase-delivery/SKILL.md:70	heading	## 4. Open the PR
skill-phase-delivery:5-clean-room-review	.claude/skills/phase-delivery/SKILL.md:81	heading	## 5. Clean-room review
skill-phase-delivery:6-fix-rounds	.claude/skills/phase-delivery/SKILL.md:92	heading	## 6. Fix rounds
skill-phase-delivery:dispute-is-allowed-with-concrete-evidence-surfac	.claude/skills/phase-delivery/SKILL.md:96	bullet	- Dispute is allowed with concrete evidence, surfaced for the orchestrator
skill-phase-delivery:record-the-observed-ci-conclusion-in-the-work-hi	.claude/skills/phase-delivery/SKILL.md:98	bullet	- **"RECORD THE OBSERVED CI CONCLUSION IN THE WORK HISTORY" IS SELF-INVALIDATING
skill-phase-delivery:one-push-per-fix-round-not-six-a-fix-round-is-1	.claude/skills/phase-delivery/SKILL.md:111	bullet	- One push per fix round, not six. A fix round is 1 to 2 pushes.
skill-phase-delivery:every-new-regression-test-must-be-red-on-the-pre	.claude/skills/phase-delivery/SKILL.md:132	bullet	- Every new regression test must be red on the pre-fix code and green after.
skill-phase-delivery:applying-a-fix-must-not-break-what-already-passe	.claude/skills/phase-delivery/SKILL.md:133	bullet	- Applying a fix must not break what already passed review; completeness
skill-phase-delivery:a-fix-round-is-not-lower-risk-than-the-work-it-f	.claude/skills/phase-delivery/SKILL.md:136	bold-lead	**A fix round is not lower risk than the work it fixes.** In this project a
skill-phase-delivery:7-merge	.claude/skills/phase-delivery/SKILL.md:143	heading	## 7. Merge
skill-phase-delivery:8-post-merge-duties	.claude/skills/phase-delivery/SKILL.md:153	heading	## 8. Post-merge duties
skill-phase-delivery:unsubscribe-from-the-pr-and-cancel-the-phase-s-s	.claude/skills/phase-delivery/SKILL.md:157	numbered	1. Unsubscribe from the PR and cancel the phase's self check-in.
skill-phase-delivery:remove-the-phase-worktree	.claude/skills/phase-delivery/SKILL.md:158	numbered	2. Remove the phase worktree.
skill-phase-delivery:commit-any-review-artifacts-still-uncommitted-an	.claude/skills/phase-delivery/SKILL.md:159	numbered	3. Commit any review artifacts still uncommitted, and make sure the
skill-phase-delivery:carry-forward-environment-warnings-the-phase-dis	.claude/skills/phase-delivery/SKILL.md:161	numbered	4. Carry forward: environment warnings the phase discovered, and any
skill-phase-delivery:create-the-next-phase-s-worktree-from-the-newly	.claude/skills/phase-delivery/SKILL.md:163	numbered	5. Create the next phase's worktree from the newly merged `main`.
skill-phase-delivery:failure-handling	.claude/skills/phase-delivery/SKILL.md:165	heading	## Failure handling
skill-phase-delivery:an-agent-that-dies-or-stalls-use-the-agent-salva	.claude/skills/phase-delivery/SKILL.md:167	bullet	- An agent that dies or stalls: use the `agent-salvage` skill immediately.
skill-phase-delivery:a-finding-that-cannot-be-explained-do-not-merge	.claude/skills/phase-delivery/SKILL.md:169	bullet	- A finding that cannot be explained: do not merge around it. Dispatch an
skill-phase-delivery:ci-red-read-the-log-before-re-running-a-real-bug	.claude/skills/phase-delivery/SKILL.md:172	bullet	- CI red: read the log before re-running. A real bug and a flake look
skill-phase-delivery:an-implementer-reporting-that-the-plan-is-wrong	.claude/skills/phase-delivery/SKILL.md:175	bullet	- An implementer reporting that the plan is wrong: stop, do not improvise.
ref-clean-room-brief:clean-room-reviewer-brief-template	.claude/skills/phase-delivery/references/clean-room-brief.md:1	heading	# Clean-room reviewer brief template
ref-clean-room-brief:environment-node-version-note-gh-presence-git-ve	.claude/skills/phase-delivery/references/clean-room-brief.md:19	bold-lead	**Environment**: `<Node version note, gh presence, git version, suite wall
ref-clean-room-brief:read-first	.claude/skills/phase-delivery/references/clean-room-brief.md:22	bold-lead	**READ FIRST**:
ref-clean-room-brief:claude-md	.claude/skills/phase-delivery/references/clean-room-brief.md:24	numbered	1. `CLAUDE.md`.
ref-clean-room-brief:delivery-plan-kernel-plan-v1-md-header-section-3	.claude/skills/phase-delivery/references/clean-room-brief.md:25	numbered	2. `delivery/plan/kernel-plan-v1.md`: header, section 3 preamble (constraints
ref-clean-room-brief:any-external-or-plan-review-whose-findings-this	.claude/skills/phase-delivery/references/clean-room-brief.md:28	numbered	3. `<any external or plan review whose findings this phase realizes>`.
ref-clean-room-brief:decision-records-this-phase-implements	.claude/skills/phase-delivery/references/clean-room-brief.md:29	numbered	4. `<decision records this phase implements>`.
ref-clean-room-brief:the-pr-body-declares-these-deviations-list-the-w	.claude/skills/phase-delivery/references/clean-room-brief.md:30	numbered	5. The PR body declares these deviations: `<list>`. The work history
ref-clean-room-brief:your-review	.claude/skills/phase-delivery/references/clean-room-brief.md:33	bold-lead	**YOUR REVIEW**:
ref-clean-room-brief:criteria-as-contract-every-criterion-precisely-r	.claude/skills/phase-delivery/references/clean-room-brief.md:35	numbered	1. **Criteria as contract**: every criterion, precisely referenced,
ref-clean-room-brief:test-honesty-mutation-test-break-the-behavior-co	.claude/skills/phase-delivery/references/clean-room-brief.md:41	numbered	2. **Test honesty**: mutation-test. Break the behavior, confirm the named
ref-clean-room-brief:deviations-one-by-one-necessity-or-convenience-d	.claude/skills/phase-delivery/references/clean-room-brief.md:46	numbered	3. **Deviations one by one**: necessity or convenience? Does each serve the
ref-clean-room-brief:scope-audit-every-changed-file-on-the-phase-list	.claude/skills/phase-delivery/references/clean-room-brief.md:49	numbered	4. **Scope audit**: every changed file on the phase list, or one of the two
ref-clean-room-brief:blast-radius-who-else-consumes-what-this-changed	.claude/skills/phase-delivery/references/clean-room-brief.md:51	numbered	5. **Blast radius**: who else consumes what this changed? Name the specific
ref-clean-room-brief:fix-shapes-hunt-the-state-that-can-never-exit-an	.claude/skills/phase-delivery/references/clean-room-brief.md:54	numbered	6. **Fix shapes**: hunt the state that can never exit and the quiet
ref-clean-room-brief:constraints-and-conventions-c-1-no-current-state	.claude/skills/phase-delivery/references/clean-room-brief.md:58	numbered	7. **Constraints and conventions**: C-1 (no current state from a log tail),
ref-clean-room-brief:verdict-approve-or-fix-round-needed-findings-cr	.claude/skills/phase-delivery/references/clean-room-brief.md:63	bold-lead	**VERDICT**: APPROVE or FIX-ROUND-NEEDED, findings `CR-nnn` severity-ranked
ref-clean-room-brief:deliverable-write-the-review-to	.claude/skills/phase-delivery/references/clean-room-brief.md:71	bold-lead	**DELIVERABLE**: write the review to
ref-implementer-brief:implementer-brief-template	.claude/skills/phase-delivery/references/implementer-brief.md:1	heading	# Implementer brief template
ref-implementer-brief:working-directory-worktree-path-a-git-worktree-a	.claude/skills/phase-delivery/references/implementer-brief.md:14	bold-lead	**WORKING DIRECTORY**: `<worktree path>`, a git worktree already on branch
ref-implementer-brief:mandated-reading-in-order-before-any-code	.claude/skills/phase-delivery/references/implementer-brief.md:18	bold-lead	**MANDATED READING, in order, before any code**:
ref-implementer-brief:claude-md-conventions-gates-red-witness-rule-env	.claude/skills/phase-delivery/references/implementer-brief.md:20	numbered	1. `CLAUDE.md` (conventions, gates, red-witness rule, environment warnings,
ref-implementer-brief:delivery-plan-kernel-plan-v1-md-the-header-and-b	.claude/skills/phase-delivery/references/implementer-brief.md:22	numbered	2. `delivery/plan/kernel-plan-v1.md`: the header and binding rule; the
ref-implementer-brief:the-decision-records-this-phase-depends-on-list	.claude/skills/phase-delivery/references/implementer-brief.md:28	numbered	3. The decision records this phase depends on: `<list DR files>`.
ref-implementer-brief:the-merged-code-this-phase-extends-list-files-ex	.claude/skills/phase-delivery/references/implementer-brief.md:29	numbered	4. The merged code this phase extends: `<list files>`. Extend its patterns;
ref-implementer-brief:prior-work-histories-for-inherited-warnings-list	.claude/skills/phase-delivery/references/implementer-brief.md:31	numbered	5. Prior work histories for inherited warnings: `<list>`.
ref-implementer-brief:any-review-or-scout-report-that-shaped-this-phas	.claude/skills/phase-delivery/references/implementer-brief.md:32	numbered	6. Any review or scout report that shaped this phase: `<list>`.
ref-implementer-brief:scope-exactly-the-phase-s-files-to-touch-list-pl	.claude/skills/phase-delivery/references/implementer-brief.md:34	bold-lead	**SCOPE**: exactly the phase's files-to-touch list, plus
ref-implementer-brief:key-plan-points-you-must-honor-the-two-to-five-r	.claude/skills/phase-delivery/references/implementer-brief.md:39	bold-lead	**KEY PLAN POINTS YOU MUST HONOR**: `<the two to five requirements
ref-implementer-brief:binding-conventions-english-only-npm-only-no-em	.claude/skills/phase-delivery/references/implementer-brief.md:42	bold-lead	**BINDING CONVENTIONS**: English only; npm only; no em dashes; no non-ASCII
ref-implementer-brief:commit-and-push-protocol-per-step-local-commits	.claude/skills/phase-delivery/references/implementer-brief.md:46	bold-lead	**COMMIT AND PUSH PROTOCOL**: per-step local commits with meaningful
ref-implementer-brief:gates-all-before-your-report-exit-codes-captured	.claude/skills/phase-delivery/references/implementer-brief.md:52	bold-lead	**GATES, all before your report, exit codes captured**:
ref-implementer-brief:npm-ci-from-a-clean-state	.claude/skills/phase-delivery/references/implementer-brief.md:54	numbered	1. `npm ci` from a clean state.
ref-implementer-brief:npm-run-build	.claude/skills/phase-delivery/references/implementer-brief.md:55	numbered	2. `npm run build`.
ref-implementer-brief:npm-test-with-dist-removed-no-prior-build	.claude/skills/phase-delivery/references/implementer-brief.md:56	numbered	3. `npm test` with `dist/` removed (no prior build).
ref-implementer-brief:every-acceptance-criterion-walked-in-order-with	.claude/skills/phase-delivery/references/implementer-brief.md:57	numbered	4. Every acceptance criterion walked in order, with the command and its exit
ref-implementer-brief:the-deliberate-failure-witnesses-the-criteria-de	.claude/skills/phase-delivery/references/implementer-brief.md:60	numbered	5. The deliberate-failure witnesses the criteria demand, actually performed,
ref-implementer-brief:registry-name-check-every-behavior-in-test-behav	.claude/skills/phase-delivery/references/implementer-brief.md:62	numbered	6. Registry name check: every behavior in `test/behaviors.json` resolves to
ref-implementer-brief:work-history-delivery-work-history-phase-md-comm	.claude/skills/phase-delivery/references/implementer-brief.md:64	numbered	7. Work history `delivery/work-history/<phase>.md` committed: prompt context,
ref-implementer-brief:red-witness-every-new-test-guarding-a-behavior-m	.claude/skills/phase-delivery/references/implementer-brief.md:68	bold-lead	**RED WITNESS**: every new test guarding a behavior must be demonstrated red
ref-implementer-brief:environment-warnings-paste-the-full-current-list	.claude/skills/phase-delivery/references/implementer-brief.md:73	bold-lead	**ENVIRONMENT WARNINGS**: `<paste the full current list from CLAUDE.md plus
ref-implementer-brief:reporting-contract-your-final-message-is-consume	.claude/skills/phase-delivery/references/implementer-brief.md:76	bold-lead	**REPORTING CONTRACT**: your final message is consumed by the orchestrator,
ref-implementer-brief:pre-submit-hazard-self-review-binding-from-2026	.claude/skills/phase-delivery/references/implementer-brief.md:85	heading	## Pre-submit hazard self-review (binding from 2026-08-05)
ref-implementer-brief:reads-files-it-does-not-own-what-if-the-path-is	.claude/skills/phase-delivery/references/implementer-brief.md:103	bullet	- reads files it does not own -> what if the path is not a regular file, is a
ref-implementer-brief:holds-exclusion-what-if-the-holder-is-gone-the-l	.claude/skills/phase-delivery/references/implementer-brief.md:105	bullet	- holds exclusion -> what if the holder is gone, the lease expired, two
ref-implementer-brief:destroys-anything-what-can-lose-committed-work-a	.claude/skills/phase-delivery/references/implementer-brief.md:107	bullet	- destroys anything -> what can lose committed work, and is the destructive
ref-implementer-brief:consumes-another-program-s-output-what-if-the-fo	.claude/skills/phase-delivery/references/implementer-brief.md:109	bullet	- consumes another program's output -> what if the format differs, the program
ref-implementer-brief:can-wait-what-if-it-waits-forever-and-is-the-tim	.claude/skills/phase-delivery/references/implementer-brief.md:111	bullet	- can wait -> what if it waits forever, and is the timeout loud or silent
ref-implementer-brief:certifies-something-can-it-pass-while-the-thing	.claude/skills/phase-delivery/references/implementer-brief.md:112	bullet	- certifies something -> can it pass while the thing it certifies is broken
ref-implementer-brief:can-this-pass-while-the-thing-it-guards-is-broke	.claude/skills/phase-delivery/references/implementer-brief.md:118	bold-lead	**"Can this pass while the thing it guards is broken?" is the single highest
next-script:milestone	.claude/orchestrator-next.mjs:38	binding	const MILESTONE = (() => {
next-script:stale-seconds	.claude/orchestrator-next.mjs:43	binding	const STALE_SECONDS = 420;
next-script:scratch	.claude/orchestrator-next.mjs:44	binding	const SCRATCH =
next-script:git	.claude/orchestrator-next.mjs:47	function	function git(args) {
next-script:onmain	.claude/orchestrator-next.mjs:55	function	function onMain(path) {
next-script:newestmtime	.claude/orchestrator-next.mjs:65	function	function newestMtime(dir) {
next-script:derivephasenumbers	.claude/orchestrator-next.mjs:117	function	function derivePhaseNumbers() {
next-script:phasenumbers	.claude/orchestrator-next.mjs:140	binding	const phaseNumbers = derivePhaseNumbers();
next-script:phase-count	.claude/orchestrator-next.mjs:150	binding	const PHASE_COUNT = phaseNumbers.length;
next-script:phases	.claude/orchestrator-next.mjs:152	binding	const phases = [];
next-script:done	.claude/orchestrator-next.mjs:176	binding	const done = phases.filter((p) => p.merged);
next-script:pushednotmerged	.claude/orchestrator-next.mjs:177	binding	const pushedNotMerged = phases.filter((p) => !p.merged && p.ahead > 0);
next-script:notstarted	.claude/orchestrator-next.mjs:178	binding	const notStarted = phases.filter((p) => !p.merged && p.ahead === 0);
next-script:lines	.claude/orchestrator-next.mjs:180	binding	const lines = [];
next-script:next	.claude/orchestrator-next.mjs:200	binding	let next;
next-script:exitcode	.claude/orchestrator-next.mjs:201	binding	let exitCode;
# 280 rule anchor(s) across 3 retirement root(s)
$ echo "exit=$?"
exit=0
```


# Fix round 1

Reviewed head 7b2b7f1. Two clean-room reviews, both FIX-ROUND-NEEDED, both
produced by Opus 5: `M4-P23-opus5-cleanroom.md` (prose) and
`M4-P23-opus5-structured.md` (structured). Seven findings between them, one
HIGH, four MEDIUM, two LOW, plus one pre-existing MEDIUM re-raised for routing.

## What I inherited, and what I checked before building on it

The branch tip was `12a1b41`, subject `WIP-UNREVIEWED: salvaged from an agent
killed by the session limit`. Two files, 350 insertions, 51 deletions,
`scripts/check-retirement-inventory.mjs` and
`delivery/plan/cutover/retirement-inventory.json`. It had not been reviewed, its
gates had not been run, and nothing in it was verified. It also touched neither
the test file nor either prose document, so the guards it added had NO red
witness and the documents still asserted the things the reviewers refuted.

I checked every claim in it rather than trusting any:

| claim in the salvaged commit | how I settled it | verdict |
|---|---|---|
| the narrowed allowlist refuses none of the inventory's commands | enumerated the leading token of every segment of all 468 commands: `grep`, and nothing else | TRUE |
| `sed -i` executed against the audited tree before the fix | re-measured end to end in a scratch root, both arms | TRUE, and the file was rewritten |
| fence skipping changes nothing on the real roots today | diffed `--extract` output with and without the fence state machine over the real roots: byte-identical, 280 both ways | TRUE |
| C-3 is stated at `AGENTS.md` | read AGENTS.md:497 and schemas/assurance-modes.schema.json:37 | TRUE |
| `gate-registry.yaml` carries the three preflight commands as YAML sequences | read gate-registry.yaml:50, :52 and :54 | TRUE |
| DR-0012 writes `delegated` in lower case | `grep -c 'delegated'` gives 2, `grep -c 'DELEGATED'` gives 0 | TRUE |
| each of the ten `widened` `read` fields says something true | read every cited line: src/gates/run.ts:1720, src/gates/citations.ts:173, src/gates/run.ts:1685, checklists/plan-review.yaml:24, tuition/T-016.yaml:26 | TRUE |
| "all 90 absence rows ... 27 distinct patterns" (a comment in the salvaged code) | 82 and 24 at this head | STALE, corrected |

So I built on it. The two things it got wrong are recorded above rather than
silently repaired: a stale count in a comment, and the omission of every test.

## The MECHANISM

One reviewer grouped five of its findings as a family: "a write or a verdict
whose scope is wider or narrower than the sentence describing it, with no test
over the difference". I tested that grouping rather than adopting it, and it
holds for five of the seven findings and NOT for the other two. Stating that
precisely is the point, because a grouping that quietly absorbs everything is
not a mechanism, it is a mood.

| finding | scope of the verdict | scope of the sentence | same family |
|---|---|---|---|
| 1, the C-3 row | the token `auto-background` is absent from five named files | constraint C-3 is stated in NO instruction channel | YES, narrower |
| 2, the R-094 row | grep refused an argument and searched nothing | "Re-verified in this phase and still TRUE" | YES, and it is the degenerate case: the verdict's scope is EMPTY |
| 3, the command screen | first token is on a list of tools someone thought harmless | "a careless row cannot write to the tree it is auditing" | YES, wider |
| 5, the extraction grammar | every column-zero line matching four regexes | "a rule anchor" | YES, wider |
| 4, the destructive red arm | (not a verdict at all: a test whose red arm writes outside its subject) | | NO, different defect |
| 6, two numbers | (a stale count and a wrong line number) | | NO, different defect |

The mechanism I fixed, in one sentence: **AN EXIT CODE, A TOKEN LIST OR A
PATTERN MATCH IS A NARROW FACT, AND THE PROSE BESIDE IT MAKES A WIDER CLAIM;
NOTHING IN THE ARTIFACT COMPARED THE TWO.** The fix is not to correct the wide
sentences. It is to make the comparison executable, so that the next wide
sentence reddens.

Three checks now perform that comparison, and each is a different arm of it:

1. **Widened absence.** A `verified-by` that exits nonzero proves a token is
   absent from the files that command named. The checker re-runs the row's own
   pattern, case-insensitively and as a fixed string, over a DECLARED wider
   surface (`WIDENED_SURFACE`: the briefs, the checklists, the templates, the
   tuition feed, the schemas, `src`, `bin`, the gate registry) AND over the
   row's own argument files. Any hit must be named in a `widened.hit-paths`
   array and explained in a `widened.read` sentence. By name and as a SUPERSET,
   never by count, so a file that GAINS the token reddens and a reviewed file
   that loses it does not.
2. **The row's own files are part of that surface.** This is the arm the
   salvaged commit did not have, and it exists because widening the TREES cannot
   catch a search that was wrong in the files it already had.
   `grep -c 'DELEGATED' delivery/decisions/DR-0012-delegated-merge-authority.md`
   exits 1 against a file carrying `delegated` twice: right surface, wrong
   search. Re-running the row's own pattern case-insensitively over the row's
   own files catches it, demonstrated red and green by the fixture named
   "only an absence of that spelling" in the red-witness capture below, and it
   costs nothing.
3. **A command that failed to RUN is not a command that answered.** Every
   recorded exit, on `verified-by` and `negative-witness` alike, is classified.
   126 and 127 are the shell's, and per tool: grep, diff and cmp exit 2 only on
   error. A non-answer is a finding whatever else is true of the row. The
   negative-witness case is the sharper one, because there the contract is only
   "exit nonzero" and a usage error satisfies it perfectly while discriminating
   nothing.

And two changes that are not verdict-comparisons but are the same round:

4. **The allowlist is now a list of tools that CANNOT WRITE**, which is a
   checkable property, rather than a list of tools that looked harmless.
   `git`, `sed`, `awk`, `node` and `sort` are off it, each with the write it
   offers written next to it in the source.
5. **Fenced blocks are skipped** by the extraction grammar.

## The DERIVATION

The mechanism's call sites ARE the recorded exit codes, because every one of
them is a verdict with a sentence beside it. All 468 are enumerated and
classified. The script is
`/tmp/claude-0/.../scratchpad/derive.mjs` (scratch, not committed; it imports
the checker's exported `nonAnswerExit`, `liftPattern`, `argumentPathsNamed`,
`kernelPathsNamed` and `WIDENED_SURFACE` so that it measures the shipped logic
and not a re-implementation of it).

```
$ node derive.mjs
# call sites (recorded exit codes): 468

## class 1: the exit is not an ANSWER (the command did not run)
  total: 0

## class 2: verified-by exits nonzero, so the row asserts an ABSENCE
  total: 82

## class 2a: the absence pattern could not be lifted (fails closed)
  total: 0

## class 2b: the absence pattern IS carried somewhere on the widened surface
  claude-md:delivery-intake-orchestrated-delivery-process-md
    pattern  "precedence"
    hits     src/gates/citations.ts, src/gates/run.ts
    verdict  declared and read
  claude-md:delivery-plan-kernel-plan-v1-md-the-owner-approv
    pattern  "precedence"
    hits     src/gates/citations.ts, src/gates/run.ts
    verdict  declared and read
  claude-md:delivery-decisions-owner-decision-records-a-deci
    pattern  "precedence"
    hits     src/gates/citations.ts, src/gates/run.ts
    verdict  declared and read
  claude-md:this-file
    pattern  "precedence"
    hits     src/gates/citations.ts, src/gates/run.ts
    verdict  declared and read
  claude-md:r-094-is-partially-delivered-and-the-half-that-i
    pattern  "--registry"
    hits     gate-registry.yaml, roles/implementer.md, src/commands/gates.ts, src/gates/run.ts, src/gates/suite.ts, src/roles.ts
    verdict  declared and read
  claude-md:the-gate-id-is-in-the-gates-manifest-json-on-tha
    pattern  "required gate"
    hits     src/gates/run.ts
    verdict  declared and read
  claude-md:the-bundle-s-declared-count-equals-that-manifest
    pattern  "required gate"
    hits     src/gates/run.ts
    verdict  declared and read
  claude-md:the-required-gate-s-not-applicable-line-does-not
    pattern  "required gate"
    hits     src/gates/run.ts
    verdict  declared and read
  claude-md:the-assertion-line-reports-zero-error-zero-vacuo
    pattern  "required gate"
    hits     src/gates/run.ts
    verdict  declared and read
  next-script:lines
    pattern  "CANNOT SEE"
    hits     checklists/plan-review.yaml, schemas/assurance-modes.schema.json, schemas/finding.schema.json, schemas/report.schema.json, schemas/role-brief.schema.json, schemas/tuition.schema.json, schemas/verdict.schema.json, src/checks.ts, src/gates/coverage.ts, src/gates/credentials.ts, src/gates/run.ts, src/gates/suite.ts, tuition/T-016.yaml
    verdict  declared and read
  total carried: 10, of which declared and read: 10

## class 3: the two path helpers disagree about what a command named
  claude-md:where-things-live verified-by
    unstripped ["delivery/","AGENTS.md","roles/implementer.md","templates/charter.example.yaml"]
    arguments  ["AGENTS.md","roles/implementer.md","templates/charter.example.yaml"]
  claude-md:delivery-is-the-build-s-own-paperwork-it-is-not verified-by
    unstripped ["delivery/","AGENTS.md","roles/implementer.md","templates/charter.example.yaml"]
    arguments  ["AGENTS.md","roles/implementer.md","templates/charter.example.yaml"]
  claude-md:src-bin-test-are-the-kernel-itself verified-by
    unstripped ["delivery/","AGENTS.md","roles/implementer.md","templates/charter.example.yaml"]
    arguments  ["AGENTS.md","roles/implementer.md","templates/charter.example.yaml"]
  claude-md:gates verified-by
    unstripped ["gate-registry.yaml","roles/implementer.md"]
    arguments  ["roles/implementer.md"]
  claude-md:gates negative-witness
    unstripped ["gate-registry.yaml","roles/investigator.md"]
    arguments  ["roles/investigator.md"]
  claude-md:gate-registry-yaml-is-the-canonical-gate-registr verified-by
    unstripped ["gate-registry.yaml","roles/implementer.md"]
    arguments  ["roles/implementer.md"]
  claude-md:gate-registry-yaml-is-the-canonical-gate-registr negative-witness
    unstripped ["gate-registry.yaml","roles/investigator.md"]
    arguments  ["roles/investigator.md"]
  total: 7; none loses its last kernel path

## class 4: every tool that can lead a command segment, and whether it can write
  grep
```

That is the full output, not a summary. Read with the classes above:

- class 1 is empty, so the non-answer guard reddens nothing today. It is a trip
  wire, and its red witness is a fixture rather than a live row. The one live
  instance, the R-094 row's exit 2, was corrected in the salvaged commit.
- class 2b is the interesting one: ten of the 82 absence rows assert an absence
  of a token that IS carried somewhere on the widened surface. All ten now carry
  a `widened` block that names every hit and reads it. Before this round, none
  of them did, and the checker was green.
- class 3 shows the seven commands where stripping the quotes changes which
  files the command is taken to have named. None of the seven loses its last
  kernel path, so the older, looser `kernelPathsNamed` check is not weakened by
  the difference; it is simply answering a different question and is left alone.

## What the DERIVATION did NOT cover

Stated first because it is the reviewer's first check, and stated as regions
rather than as reassurance.

1. **It covers only claims that have a recorded exit beside them.** A sentence
   in `delivery/plan/cutover/retirement-inventory.md` or in this work history
   that overstates what a command showed has no exit code attached and is not in
   any class above. Those were handled by reading, which is weaker, and the
   claim greps below are the only mechanical part of it.
2. **It does not cover the `read` fields' TRUTH.** The checker requires that
   every hit be named and that a sentence exist. It cannot tell a true reading
   from a plausible one. I read all ten myself and spot-verified five of the
   cited lines; that is a human check, and it is exactly the limit the phase
   already recorded as item 8 of its own not-covered list, one level in.
3. **The widened surface is a DECLARED list and `delivery/` is not on it.**
   That is deliberate (the inventory quotes the rules back at itself, so
   including `delivery/` makes every absence row hit its own document, measured:
   three rows reddened that way during this round before the quote-stripping
   fix), and the consequence is real: an absence claim about a rule carried only
   somewhere under `delivery/` is not re-established by this check. The row's
   own argument files are the only part of `delivery/` that is reached.
4. **The pattern lifting handles ONE single-quoted pattern with no regex
   metacharacter.** Measured at this head: 82 of 82 absence rows qualify, 0
   unliftable. A row that used a double-quoted pattern, two patterns, or a
   metacharacter FAILS CLOSED (it must carry a `widened.read` saying so) rather
   than being skipped, which I verified by reading the branch at
   scripts/check-retirement-inventory.mjs:570 and by the fact that class 2a
   enumerates them and is empty.
5. **The non-answer table is per tool and covers three tools.** grep, diff and
   cmp. `test`, `ls`, `wc`, `comm`, `head`, `tail` and `cat` carry only the
   universal 126 and 127, because their exit codes do not distinguish an error
   from an answer in a way I could state. That is a gap in coverage, not a claim
   that those tools are safe, and it is written down rather than papered over.
6. **The spelling family is only half covered.** Case is caught, because the
   widening is case-insensitive. SPELLING is not: `grep -c 'npm ci'
   gate-registry.yaml` exits 1 against a file that carries `- command: [npm,
   ci]`, and no case-insensitive fixed-string search finds that. Three rows had
   that defect; all three were corrected by hand in the salvaged commit, and
   I FOUND NOTHING MECHANICAL THAT WOULD CATCH THE NEXT ONE. I did not find a
   check that separates a spelling artefact from a true absence, and I am not
   claiming none exists. The open question is stated as an open question: what
   would have reddened `grep -c 'npm ci' gate-registry.yaml` against a file
   carrying `- command: [npm, ci]`?
7. **Nothing here touches `src/`, `bin/`, `schemas/`, `roles/` or `tuition/`.**
   The pre-existing `process.exit()` truncation the phase found and routed is
   untouched by this round. It is routed, not closed, and the routing target
   is whichever phase owns `src/gates/`.

## The red witnesses

Every new or changed guard, with the DANGEROUS state forced and two structurally
different members of each class. The method for all four: copy the fix-round
checker, change exactly one thing back, run the new tests against it unchanged.
The four dangerous variants are built by a scratch script that fails loudly if
its anchor text is not found verbatim, so a variant cannot silently be the
fixed checker wearing a different name.

```
$ bash redrun.sh
############ DANGEROUS: danger-1-no-nonanswer.mjs
---- recorded exit is a grep usage error
not ok 1 - retirement checker reddens on a verified-by whose recorded exit is a grep usage error
# tests 1
# pass 0
# fail 1
---- recorded exit is the shell's not-found
not ok 1 - retirement checker reddens on a negative witness whose recorded exit is the shell's not-found
# tests 1
# pass 0
# fail 1

############ DANGEROUS: danger-2-no-widened.mjs
---- no widened block when the token is carried
not ok 1 - retirement checker reddens on an absence row with no widened block when the token is carried
# tests 1
# pass 0
# fail 1
---- hit-paths omit a carrying file
not ok 1 - retirement checker reddens on a widened block whose hit-paths omit a carrying file
# tests 1
# pass 0
# fail 1
---- only an absence of that spelling
not ok 1 - retirement checker reddens on an absence that is only an absence of that spelling
# tests 1
# pass 0
# fail 1

############ DANGEROUS: danger-3-no-fence.mjs
---- does not read rules out of fenced code blocks
not ok 1 - retirement extraction does not read rules out of fenced code blocks
# tests 1
# pass 0
# fail 1

############ DANGEROUS: danger-4-old-allowlist.mjs
---- reached through an in-place editor flag
not ok 1 - retirement checker refuses a write-capable tool reached through an in-place editor flag
# tests 1
# pass 0
# fail 1
---- reached through an output flag on a filter
not ok 1 - retirement checker refuses a write-capable tool reached through an output flag on a filter
# tests 1
# pass 0
# fail 1

############ checker restored; verifying it is byte-identical to the good copy
IDENTICAL
```

The two members of each class, and why they are structurally different:

- **non-answer exit**: a TOOL-SPECIFIC error code (grep's 2) on a `verified-by`,
  and the SHELL's own not-found (127) on a `negative-witness`. Different
  producer, different block, and the second defeats a contract the first does
  not touch.
- **widened absence**: the declaration is ABSENT, and the declaration is PRESENT
  BUT INCOMPLETE. The second is the member that matters, because a checker that
  only looks for a field is the guard this repository has shipped three times
  and the one that cannot go red. A third member, the case artefact, is a
  different arm again: right surface, wrong search.
- **write-capable tool**: an IN-PLACE EDITOR FLAG (`sed -i`) and an OUTPUT FLAG
  on an ordinary filter (`sort -o`). The write is reached by different means,
  which is what makes the allowlist's property ("no tool on this list can
  write") the thing under test rather than one flag.
- **fenced block**: a column-zero BULLET and a column-zero HEADING, inside one
  fence. The grammar has four markdown kinds and a fence swallows more than one
  of them, so one member would have proved nothing about the others.

The destruction itself, end to end, because "the screen refuses it" is a
different claim from "it would have destroyed something":

```
=== DANGEROUS (old allowlist) + sed -i ===
BEFORE: IMPORTANT ORIGINAL CONTENT
retirement-inventory: 4 row(s) against 4 derived rule anchor(s), commands EXECUTED
AFTER : IMPORTANT DESTROYED CONTENT

=== FIXED (write-incapable allowlist) + sed -i ===
BEFORE: IMPORTANT ORIGINAL CONTENT
retirement-inventory: 4 row(s) against 4 derived rule anchor(s), commands EXECUTED
  UNRESOLVED claude-md:lab-rules: verified-by command segment starts with "sed", which is not on the allowlist
AFTER : IMPORTANT ORIGINAL CONTENT

=== DANGEROUS (old allowlist) + sort -o ===
BEFORE: IMPORTANT ORIGINAL CONTENT
retirement-inventory: 4 row(s) against 4 derived rule anchor(s), commands EXECUTED
AFTER :

=== FIXED + sort -o ===
BEFORE: IMPORTANT ORIGINAL CONTENT
retirement-inventory: 4 row(s) against 4 derived rule anchor(s), commands EXECUTED
  UNRESOLVED claude-md:lab-rules: verified-by command segment starts with "sort", which is not on the allowlist
AFTER : IMPORTANT ORIGINAL CONTENT
```

`sort -o` truncated the victim to zero bytes, which is why the AFTER line is
empty. Every arm above passed `--repo <scratchpad>/lab/root`, a `mkdtemp`-style
tree built by the lab script, so the file that was destroyed was the lab's
`src/victim.txt`.

**One thing the red-witness runs DID do to this worktree, stated rather than
left to be discovered.** `redrun.sh` copies each dangerous variant OVER
`scripts/check-retirement-inventory.mjs` for the duration of one test
invocation, because the test file resolves the checker by a relative URL and
there is no override. It takes a byte copy first and restores from it after each
arm, and the last line of the captured output is `cmp` confirming the restored
file is IDENTICAL to that copy. No VCS restore was used anywhere in this round,
per standing warning 8. The alternative, editing the test file to take a
checker path, would have changed the thing under test, which is worse.

**Two probes that did NOT demonstrate what I wanted, recorded because a failed
probe is evidence about the probe.** I tried twice to make `node -e` write under
the old allowlist and failed both times, for two different reasons: with a
semicolon the segment splitter refused the second segment, and without one the
shell stripped the single quotes so `node -e` received
`require(fs).writeFileSync(...)` and threw. A reviewer measured `node -e
process.exit(0)` as ALLOWED, which is true and is not the same as a write. So
`node` is off the allowlist on the strength of `node -e` existing, not on a
demonstration, and I did not find a formulation that both passes the screen and
writes. I am not claiming none exists. `git checkout -- .` is likewise refused
by first token and I did not force its destructive arm, because the lab root is
not a repository and this container's sandbox refuses to make one outside the
worktree; CLAUDE.md standing warning 8 is the evidence that it destroys, not a
measurement of mine.

The fence witness, on the real roots, so the trip wire is known to be a trip
wire and not a correction:

```
$ node scripts/check-retirement-inventory.mjs --extract > anchors-fixed.txt
$ node checker-NO-FENCE.mjs --repo <this worktree> --extract > anchors-nofence.txt
$ diff anchors-fixed.txt anchors-nofence.txt ; echo "diff exit=$?"
diff exit=0
$ tail -1 anchors-fixed.txt
# 280 rule anchor(s) across 3 retirement root(s)
$ tail -1 anchors-nofence.txt
# 280 rule anchor(s) across 3 retirement root(s)
```

Byte-identical. ZERO anchors fall inside a fence at this head, so the 280 was
right by CONTENT and is now right by GRAMMAR.

## The seven findings

| # | severity | disposition | evidence |
|---|---|---|---|
| 1 | HIGH | FIXED | row `claude-md:never-auto-background-a-long-running-process-c-3` is PORTED/PORT with destination `AGENTS.md`, probe "C-3 forbids a kernel COMMAND", and a sibling negative witness. All three sites corrected: the row, delivery/plan/cutover/retirement-inventory.md:157 and the sentence in this document, which is left standing with a marked correction beside it. Mechanism fixed by the widened-absence check. |
| 2 | MEDIUM | FIXED | the command is now `grep -c -- '--registry' scripts/m2-exit-test.sh`, exit 1, output `0`, and the row's note records what the old evidence was. Mechanism fixed by the non-answer-exit check, which the reviewer recommended and the salvaged commit did not do. |
| 3 | MEDIUM | FIXED | five write-capable tools removed from `ALLOWED_FIRST_TOKENS`; the header now states the property the list is chosen for and what it does NOT guarantee. Two members demonstrated destroying, both refused now. |
| 4 | MEDIUM | FIXED | the destructive fixtures run against `scratchRoot()`, a `mkdtemp` tree carrying the three roots and a victim file. The old `rm -rf delivery/plan/cutover` aimed at the real repository root is gone. |
| 5 | LOW | FIXED | fence state machine in `extractAnchors`, with the two-member witness above. |
| 6 | LOW | FIXED | see "Six numbers the round corrected". |
| 7 | MEDIUM | NOT FIXED, ROUTED | `src/gates/suite.ts:1142`, `src/gates/credentials.ts:691`, `src/gates/red-witness.ts:574`. Outside this phase's files-to-touch list and outside its subject. I did not demonstrate truncation in any of the three either; the mechanism is present and the trigger is output volume. Routed, not closed; the target is whichever phase owns `src/gates/`. |

## Six numbers the round corrected

Because this repository's own rule is that a quoted number starts an
investigation, and I would rather the next reader not spend one.

| where | was | is | why |
|---|---|---|---|
| this document, "the checker re-runs all N commands" | 466 | 468 | it was 467 at 7b2b7f1 and the C-3 row gained a negative witness |
| this document, the pre-pass citation | :60, a blank line | :64 | the wave-1b row naming M4-P23 |
| retirement-inventory.md totals | 187 PORTED, 89 GAP / 187 PORT, 76 KEEP | 188 PORTED, 88 GAP / 188 PORT, 75 KEEP | the C-3 row moved |
| retirement-inventory.md KEEP split | 47 process, 29 predicate | 46 process, 29 predicate | the same row |
| retirement-inventory.md gap groups | twenty-six | twenty-five | the same row |
| checker comment on absence rows | "all 90 ... 27 distinct patterns" | 82 and 24 | stale in the salvaged commit; both are now derived by the published derivation rather than asserted |

## Claim greps

The binding line-based form, over this document at the fix-round head, and the
EXTENDED form the dispatch requires, whose vocabulary adds the passive
assertions-of-handling that the binding one has only one of.

The dispatch's EXTENDED vocabulary is the one that matters here, because a
verification document measured that the binding grep's vocabulary carries
exactly one passive assertion-of-handling and misses seven others. That document
is named `delivery/verification/the-claim-grep-catches-one-passive-assertion-and-misses-seven.md`
and it is NOT in this branch's tree, so it is quoted rather than cited.

```
$ grep -nEi 'cannot be|impossible|needs a|catches|would catch|recovers|anyway|always|never|no way to|is (covered|refused|validated|checked|handled|guarded|rejected|enforced|prevented|caught)' delivery/work-history/m4-p23.md | wc -l
51
$ grep -oEi '<the same alternation>' delivery/work-history/m4-p23.md | wc -l
85
$ tr '\n' ' ' < delivery/work-history/m4-p23.md | grep -oEi '<the same alternation>' | wc -l
87
```

Counted as OCCURRENCES so the two numbers are comparable: 85 line-visible
against 87 wrap-insensitive, so TWO hits are hidden by a wrap, and both were
located by diffing the two `uniq -c` tables rather than by eye.

The first is at delivery/work-history/m4-p23.md:276, where "member B survives
every field check and is" wraps before "caught only by execution". That sentence
belongs to the first round and is backed by the fixture described in the same
section, so the wrap hid nothing that was not already settled.

The second is in THIS section, at delivery/work-history/m4-p23.md:1586, where
the phrase I removed as an over-claim wraps between "WOULD" and "CATCH" in the
paragraph recording that I removed it. So the wrap trap bit the very paragraph
describing the wrap trap, which is a small joke and a real data point: the
exposed set really is the multi-word alternatives, and it really does need the
second command.

Some of the hits are the grep vocabulary printed above, which is unavoidable:
a document that quotes the pattern matches the pattern. The rest divide into
fenced captured output, which is data, and prose.

Every prose hit in the fix-round section, and how each is settled:

| hit | sentence | settled by |
|---|---|---|
| `never` | "by name and as a SUPERSET, never by count" | a design statement about code in this diff, with the test "hit-paths omit a carrying file" red without it |
| `catches` | "re-running the row's own pattern ... catches it" | the fixture "only an absence of that spelling", red and green, in the capture above |
| `is caught` | "Case is caught, because the widening is case-insensitive" | the same fixture, and the sentence goes on to say SPELLING is not caught |
| `would catch` | "I FOUND NOTHING MECHANICAL THAT WOULD CATCH THE NEXT ONE" | restated as an open question in the sentence after it, with the exact command that defeats the check |
| `cannot be` | "the absence claim cannot be re-established" | quoting scripts/check-retirement-inventory.mjs:570, which is the code's own comment on its fail-closed arm |
| `cannot write` | "a list of tools that CANNOT WRITE" | five tools removed with the write each offers named; two demonstrated writing before the fix |
| `never against` | the destruction arms | the `--repo` argument every arm passed, named in the same paragraph |
| `never by count` (behaviors) | the registry assertions | asserted by name in both directions, per convention 5 |

Two phrases I removed from this section rather than settle, because the grep
found them and they were over-claims: "still needs a phase that owns
`src/gates/`" became "routed, not closed", twice, and "NOTHING MECHANICAL WOULD
CATCH THE NEXT ONE" became "I FOUND NOTHING MECHANICAL THAT WOULD". The grep is
mechanical and a reminder is not, which is why it caught them and I did not.

## Citations

Every resolving-form citation (`path.ext:LINE`, outside backticks, fenced
captured output excluded) in the two prose documents this round touched, opened
at the fix-round head and checked against the line it names.

```
delivery/work-history/m4-p23.md                 29/29 distinct citations resolve to a non-blank line
delivery/plan/cutover/retirement-inventory.md   17/17
```

**Hit rate 46/46 = 100%.** A review of 7b2b7f1 measured 21/22 = 95.5% and named
the one miss, `delivery/plan/m4-conflict-pre-pass.md:60`, a blank line whose
real target is :64. That wrong form is QUOTED here rather than written as a
citation, because writing it plainly would make this paragraph a miss of its
own, which is the trap in miniature. Checking it myself found that the SAME wrong line number appears
in retirement-inventory.md as well, which the review did not report, and two
more misses this round created: my own citation into the checker drifted when
the file grew, and one path lost its `test/` prefix. All four are fixed.

The lesson is the one the round is about: an in-range citation resolves
SILENTLY against the wrong line, so a green citations gate says nothing about
whether a citation points at what it claims. The only check is opening it, and
the script that does that is
`/tmp/claude-0/.../scratchpad/cites.mjs` (scratch, not committed).

One citation deliberately does NOT resolve and is quoted instead:
`delivery/verification/wall-clock-budgets-are-load-dependent.md`, named by the
dispatch and not present in this branch's tree. Quoting is the correct form for
a file I am not asserting exists here.

## Suite

The complete sentence, all four axes, because a bare number starts an
investigation here.

**Interpreter** node v26.6.0 (`/home/user/.toolchains/node-v26.6.0-linux-x64/bin/node`,
the floor-satisfying scratch toolchain). **Build state** `dist/` built, `npm ci`
exit 0, `npm run build` exit 0, `git status --short` shows only my five modified
files and no build output. **Invocation** `npm test`, which is
`node --test "test/**/*.test.ts"`. **Head** the fix-round head on
`claude/m4-p23-retirement-inventory`. **Result** 873 tests, 872 pass, 1 fail,
**0 skipped**.

The one failure is `test/coverage-gate.test.ts:233`, "deleting an appendix row is
red naming the orphan id, and restoring it is green", failing inside
`boundedExec` at src/gates/coverage.ts:260 with "pattern ... did not complete
within 250ms against a value of length 2 (possible catastrophic backtracking)".

**The base was established before the failure was attributed.** One variable
changed, the same head and the same interpreter and the same built `dist/`:

| invocation | load average at start | result |
|---|---|---|
| `npm test` (the whole suite, 873 tests concurrent) | 12.50 | 1 fail, at test/coverage-gate.test.ts:233 |
| `node --test test/coverage-gate.test.ts` alone | 27.73 | 17 tests, 17 pass, 0 fail, 0 skipped |

The machine-wide load was HIGHER for the passing run, which is the point: what
matters is the concurrency inside the suite, not the number `uptime` prints. The
budget at src/gates/coverage.ts:260 is a 250ms WALL CLOCK measurement used as a
proxy for regex backtracking, so it is a timing assertion and it reddens under
contention. `src/gates/coverage.ts` and `test/coverage-gate.test.ts` are not in
this branch's diff against `origin/main` (`git diff --name-only
origin/main...HEAD -- src/gates/coverage.ts test/coverage-gate.test.ts` prints
nothing), so this branch cannot have caused it and I have not touched it. The
dispatch named a verification document about this budget,
`delivery/verification/wall-clock-budgets-are-load-dependent.md`; it is NOT
present in this branch's tree, so it is quoted here rather than cited.

The phase's own file, separately, at the same head and toolchain:
`node --test test/retirement-inventory.test.ts` gives **24 tests, 24 pass, 0
fail, 0 skipped**, exit 0, 15327ms. Sixteen before this round, eight added.

The checker itself against the real roots: `node scripts/check-retirement-inventory.mjs`
prints "280 row(s) against 280 derived rule anchor(s), commands EXECUTED" and
"every rule in the three roots is resolved", exit 0.
