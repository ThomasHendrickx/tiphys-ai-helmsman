# Work history: M2-P3, full-suite wrapper with parity counting

- Phase: M2-P3
- Branch: claude/m2-p3-suite-wrapper, off origin/main at 4c9bfbc
  (contains M2-P1 at 8718852)
- Implementer session started: 2026-08-06
- Plan section: kernel-plan-m2.md section 3, "M2-P3: Full-suite wrapper
  with parity counting", steps 1 to 10, criteria 1 to 11
- Status: FIX ROUND ONE APPLIED (this file is appended incrementally; T-008)
- Fix round one: applied per delivery/review/arbitration-m2-p3.md against
  head 6fe8066, addressing CR-1306 (code medium), CR-M2P3-1 and CR-M2P3-2
  (record mediums). Branch updated onto origin/main FIRST (merge commit
  b5f1cee), which also brings in M2-P6 (coverage checker); the merge base
  with origin/main is now 8439c88.

## Mechanism lookup (T-005 interim obligation)

Read MECHANISMS.md in full. Rows bearing on this phase:

1. "Parsing another program's reporter output": PIN the format as a
   controlled input rather than widening the parse. This is the core of
   step 2: the reporter is REQUESTED by name for the child, the received
   stream is validated to be that format before any count is parsed, and a
   stream in any other format is `error` naming expected and observed.
   Requires: no format-agnostic regex, no acceptance of whichever format
   arrived.
2. "Deciding what another program will do by pattern-matching the text of
   a file it consumes": the wrapper must run the suite the way the
   repository configures it (`package.json` scripts.test), and parsing that
   script text to re-derive what npm would run would be exactly this
   mechanism. The row's tier 1 applies instead: EXECUTE the real thing.
   The gate runs scripts.test verbatim through /bin/sh -c, as npm does,
   and never interprets its text (decision recorded at the end of the
   step 1 survey below).
3. "Reading a path whose type is not established" (M2-C-6): every path this
   gate reads that it did not create (package.json, test/behaviors.json,
   walked test files, the reporter stream file) routes through the
   delivered `classifyEntry` / `readRegularFileIfPresent` /
   `refuseOpenForWrite` from src/task.ts. No bare readFileSync on an
   externally supplied path.
4. "Classifying another program's errors" / CLAUDE.md warning 10: parse
   fixtures and format-validation fixtures must be REAL captured output
   from the pinned Node's reporters, not hand-written strings. The captured
   streams are recorded below and committed as test fixtures.
5. "Claim file": not used by this gate (the runner owns the evidence-claim
   mechanism; this gate only writes inside the directory the runner hands
   it). Noted so the absence is a decision, not an oversight.

## M2-C-1 step 1 verification of delivered contracts (before any code)

Verified at origin/main 4c9bfbc, in this worktree:

- `src/gates/result.ts`: `makeGateResult` is the only constructor; green
  with units 0 is rewritten to `error` + `vacuous` (M2-C-2). Exit code
  table: 0 green, 1 red, 20 not-applicable, 21 error, 64 usage
  (lines 65-68). Record fields: gate, status, units, unitLabel, startedAt,
  endedAt, vacuous?, precondition?, pin?, detail, evidence[].
- `src/gates/pin.ts`: five-field pin {path, sha256, size, mtimeMs,
  ctimeMs} (PinFile, lines 73-81), `takePin(roots)` throws on empty root
  (vacuity floor, line 181), `comparePins` reports per-field differences
  including mtimeMs-only (byte-identical rewrite). Matches the plan's
  M2-C-5 plus the delivered CR-809 ctimeMs addition the shared brief
  names. The phase brief's statement "five fields including ctimeMs" is
  CONFIRMED as delivered.
- `src/gates/run.ts`: the runner appends `--result <abs> --evidence <dir>`
  plus one `--<name> <value>` per parameter the gate DECLARES in the
  manifest (`parameters` field, requiredParameters lines 367-377).
  A declared parameter missing at invocation is `error` before spawn
  (lines 603-615). `pinRefusal` (lines 880-895) re-checks a green
  record's pins. The gate subprocess contract is as the module comment
  states (lines 40-48).
- `src/gates/manifest.ts`: GateEntry fields id, command, unitLabel,
  applicability, parameters?, precondition?, modes?. So the suite entry
  can declare `"parameters": ["base"]` and the runner enforces --base
  presence; the gate ALSO enforces it itself for direct invocation
  (criterion 9 must hold without the runner in front).
- `package.json` delivered scripts block (last edited by M2-P1),
  verified verbatim before editing:
  build, build:schemas, test = `node --test "test/**/*.test.ts"`,
  prepack. `npm test` is bare as M2-D-09 requires; this phase ADDS
  `gate:suite` and touches nothing else in the file.
- `test/behaviors.json`: flat JSON object mapping behavior-id to a
  description string. RESOLUTION CONVENTION VERIFIED: the description
  string is byte-identical to the `test("...")` name. Checked
  mechanically: 207 registry entries, and every description matches a
  test name extracted from test/*.test.ts (two-regex extraction handling
  both quote styles; 0 unmatched). So "resolves by name to a test in the
  run" means: the registry VALUE equals the name of a reported test.
- `gates.manifest.json`: version 1, one gate (manifest-self-check),
  destructiveCommands present. Append-only; the suite entry is appended
  to `gates` and nothing existing is touched.
- No divergence found between the plan's assumptions and the delivered
  shapes. No escalation needed.

## Step 1: reporter survey on the pinned Node (recorded BEFORE choosing)

The survey was performed on BOTH installed toolchains before the reporter
was chosen (the phase brief's emphasis). Probe scripts and full captured
output live in the session scratchpad (probe1.sh to probe5.sh); the
load-bearing findings and the verbatim decisive excerpts are recorded
here, and the captured streams used as test fixtures are committed inside
test/suite-gate.test.ts.

Toolchains measured:
- container default: node v22.22.2 (login shell and `bash -lc` both,
  checked in the shell that ran each probe)
- floor: node v26.6.0 from the pre-fetched scratch prefix, put first on
  PATH for the floor runs

Reporter inventory (measured, not recalled): `--test-reporter=<name>`
accepts tap, junit, spec, dot, lcov on both toolchains (each ran with
exit 0/1 per test outcomes); a non-built-in name is resolved as a MODULE
specifier (`--test-reporter=bogus` fails ERR_MODULE_NOT_FOUND, exit 7),
which means the documented custom-reporter interface is itself part of
what the pinned Node emits. Machine-readable candidates: tap, junit, and
the custom-reporter event stream. spec and dot are human-facing; lcov is
coverage only.

Decisive measurements, identical on v22.22.2 and v26.6.0:

1. TAP carries NO file attribution. A two-file suite (test/a.test.ts,
   test/sub/x.test.ts) reports all tests FLAT at the top level:
   `ok 1 - alpha passes` ... `ok 6 - sub x passes`, `1..6`. No file path
   appears anywhere in the stream for a passing test (only failing tests
   carry a `location:` in their YAML block). So TAP cannot support
   criterion 2's "naming the file", nor the file-set comparison of step 3,
   at all.
2. junit carries `file="..."` ONLY for non-nested testcases. With a
   parent test carrying subtests, and with describe/it, the nested
   `<testcase>` elements have no `file` attribute (measured, fix2 probe).
   And the counterfeit raw line printed by a test body does NOT appear
   anywhere in the junit stream (measured, fix1 probe), so criterion 6's
   "captured verbatim in the evidence" is unsatisfiable from junit alone.
3. The custom-reporter event stream carries, for EVERY test at every
   nesting level, on both toolchains: `data.file` (absolute path),
   `data.name`, `data.nesting`, `data.skip` (true or the reason string),
   `data.todo`, `data.details.type` ('test' or 'suite'), and on failure
   `data.details.error.failureType`. Raw test-body output arrives as
   `test:stdout` / `test:stderr` events with the verbatim `message`
   ("pass 999\n" captured exactly). File-level synthetic entities emit
   test:enqueue/test:complete but NEVER test:pass or test:fail (verified
   by grep over the captured event streams of both fixtures), so counting
   pass/fail events with details.type === 'test' reproduces node's own
   "# tests" semantics: fix2 gave 6 by this rule and `# tests 6` in the
   TAP summary of the same run.
4. Counterfeit routing: with a tap destination file, a test body's
   `console.log("pass 999")` lands INSIDE the tap stream as the comment
   line `# pass 999`, byte-identical in shape to the REAL summary line
   `# pass 3` seven lines below it in the same capture. This is the
   measured demonstration of why a summary-line parse is banned (C-1):
   the counterfeit and the authority are the same grammar. In the event
   stream the same bytes arrive as a `test:stdout` event whose payload is
   a JSON STRING, so an injected line cannot form an event: JSON.stringify
   escapes it inside the message field. Structural immunity rather than a
   cleverer regex.
5. A `data:text/javascript,<urlencoded module>` reporter works on both
   toolchains, both as a CLI flag and via child-scoped NODE_OPTIONS
   through /bin/sh -c (all four combinations measured; the header line
   the probe reporter emitted carried process.version v22.22.2 and
   v26.6.0 respectively, proving the stream records which node actually
   ran the suite).
6. `node --test` with a pattern matching NOTHING exits 0 and reports
   `1..0` / `# tests 0` (measured on v22). An exit-code-only check passes
   on an empty suite, which is CR-604's shape; the walk plus M2-C-2
   close it here.
7. Cancellation vocabulary differs between toolchains for the same
   fixture: the never-resolving-promise test under --test-timeout=500 is
   `failureType: 'cancelledByParent'` on v22.22.2 and
   `failureType: 'testTimeoutFailure'` on v26.6.0. The did-not-run bucket
   is therefore PINNED to `cancelledByParent` (the only shape node itself
   counted under `# cancelled 1` in the same run's TAP summary), and every
   other test:fail is `fail`. This mapping is recorded in every run's
   counts.json.

REPORTER CHOSEN, after the survey and because of it: a gate-owned NDJSON
event reporter named `tiphys-suite-events-v1`, delivered as a data: URL
module embedded as a string constant in src/gates/suite.ts, requested
explicitly for the child via child-scoped NODE_OPTIONS
(`--test-reporter=data:... --test-reporter-destination=<evidence path>`),
with ambient NODE_OPTIONS deliberately dropped from the child environment
(an inherited reporter option is precisely the ambient default the pin
exists to exclude). The stream is validated as that format before any
count is parsed: header line first, every line a JSON object with a known
event field, explicit stream-end trailer last; anything else is `error`
naming the expected and the observed format. This is a DECLARED DEVIATION
from the plan's singular "chosen machine-readable reporter" being one of
the named built-ins: the measurements above show tap cannot satisfy
criteria 2 and 3 (no file attribution) and junit cannot satisfy criterion
6 (raw output dropped) or step 3 under nesting (file attribution lost),
so no single built-in satisfies the phase's criteria, and requesting BOTH
built-ins would mean two parsers and a file set that still goes dark on
nested tests. The custom-reporter interface is documented, pinned Node
behavior, measured on both toolchains; the format it emits is owned by
this gate and versioned in its name. Decided under DR-0016 (recommendation
was unambiguous) and recorded here rather than escalated.

How the suite is EXECUTED (also decided from the mechanism index): the
gate runs the repository's configured `scripts.test` from package.json
verbatim through `/bin/sh -c`, exactly as npm does, rather than parsing
the script text and re-executing a reconstruction of it. Reading the
script and re-deriving "what npm would run" is the mechanism
"Deciding what another program will do by pattern-matching the text of a
file it consumes", and the survey's sh -c + NODE_OPTIONS measurement
(finding 5) shows verbatim execution with an injected reporter works on
both toolchains. Residues, stated: npm's PATH prepending of
node_modules/.bin and npm lifecycle hooks (pretest/posttest) are not
reproduced; the kernel's test script is a bare `node --test` glob with no
lifecycle hooks, and a repository whose test script depends on either
will fail loudly (stream absent -> error), never silently pass; that
path is witnessed by the test "a test command that produces no reporter
stream is error naming the request" and red-witnessed as W14 in the
ledger below.

## What was built

- `src/gates/suite.ts`: the suite gate. Executes the repository's
  `scripts.test` verbatim through /bin/sh -c with a child-scoped
  NODE_OPTIONS requesting the pinned reporter `tiphys-suite-events-v1`
  (an NDJSON event reporter embedded as a data: URL constant in the same
  module, so reporter and parser cannot drift apart). Validates the
  received stream as that format before parsing any count; derives counts
  from the stream plus the child's exit code only; walks the declared
  test roots for the declared suffix independently of the runner's
  pattern; compares walk and reporter file sets in both directions;
  resolves every registered behavior by name against reported test names;
  compares the head registry against the merge-base copy (git merge-base,
  git ls-tree, git show); enforces skip reasons; takes the M2-C-5 pin
  pair over the declared pin roots around the run and errors on any
  difference; and writes evidence (stream, child stdout/stderr, raw test
  output reassembled verbatim, counts.json) into the evidence directory.
- `test/suite-gate.test.ts`: sixteen tests against fixture suites in
  scratch git repositories with command-scoped git identity.
- `package.json`: ADDED `gate:suite` (node src/gates/suite.ts with the
  kernel's pin roots). `npm test` verified still the bare runner
  (M2-D-09); no other script touched.
- `gates.manifest.json`: appended the `suite` entry per section 1.4
  (required, no precondition, unitLabel "tests reported",
  parameters ["base"]); `tiphys gates self-check` re-run against the
  amended manifest: exit 0, "manifest-self-check: green (2 schema
  documents validated)".
- `test/behaviors.json`: appended 16 entries (suite-gate-*), append-only,
  nothing existing touched.

## Declared deviations and additions (each with its reason)

1. The pinned reporter is the gate-owned `tiphys-suite-events-v1` over
   the documented custom-reporter interface, not a built-in name. Reason
   and measurements in the step 1 survey above: no built-in stream can
   satisfy criteria 2, 3 and 6 simultaneously on the pinned toolchains.
2. The suite is executed verbatim via /bin/sh -c, not by re-deriving the
   command from the script text (mechanism row, step 1 survey). npm's
   node_modules/.bin PATH entry and lifecycle hooks are not reproduced;
   failure under such a script is loud (absent stream is error).
3. The gate accepts pinned-configuration flags beyond the plan's list:
   --test-root (default test), --suffix (default .test.ts), --pin-root
   (default src, test), --registry (default test/behaviors.json). The
   declared roots the plan speaks of have to be declared somewhere; the
   manifest command carries the kernel's declaration (pin roots src, bin,
   test) so the declaration is reviewable in one place.
4. The child environment drops inherited NODE_OPTIONS (ambient reporter
   defaults are the pin's enemy) and NODE_TEST_* (a nested node --test
   inheriting NODE_TEST_CONTEXT switches protocol; measured by M1-P6,
   comment in test/exit-test-local.test.ts lines 111-115). Without the
   second scrub, criterion 1's run of the gate ON THIS REPOSITORY would
   hand every bare `node --test` spawned by these very tests the OUTER
   run's stream destination, and the outer evidence would be clobbered
   mid-run. The same scrub is applied by the tests to every child they
   spawn, for the same reason.
5. The record carries no free-form fields (gate-result.schema.json is
   additionalProperties: false), so the requested reporter name, the
   child node version, the mapping statement and the full arithmetic are
   in `detail` (naming) and `counts.json` (full), with counts.json listed
   in `evidence`.
6. Step 6's identity is recorded as two checks rather than one
   unit-mixing equation: (a) every reported test lands in exactly one
   bucket and pass + fail + skipped + todo + did-not-run == reported is
   asserted (violation is `error`); (b) the walk's file set and the
   reporter's file set must be equal, both directions (violation is
   `red` naming files). `discovered` is the walk's file count and the
   walk's file list is recorded. A literal test-count == file-count
   equality is not computable from any reporter the pinned Node emits
   and would be false for every suite with more than one test per file;
   the mapping statement recorded in every counts.json says exactly what
   is enforced (MAPPING_STATEMENT in src/gates/suite.ts).
7. The did-not-run bucket is pinned to failureType `cancelledByParent`
   (survey finding 7: the one shape node itself counted as cancelled,
   measured; v26 classifies a timed-out test as `testTimeoutFailure`,
   which ran and failed and is counted as fail).

## Red-witness ledger (strong form: red against the dangerous state)

Method: for each witness, the pristine committed src/gates/suite.ts was
replaced by a variant with ONE mechanism defanged, the named test was run
via `node --test --test-name-pattern <name> test/suite-gate.test.ts`, and
the file was restored by copying the pristine bytes back (never git
checkout, warning 8; `diff` against the pristine copy confirmed
byte-identical restoration and `git status --porcelain` was clean after
the last run). Driver and full output: scratchpad m2-fanout/m2-p3/
defang.py; summary below, every row measured on 2026-08-06:

| W | Defanged mechanism (the dangerous state) | Test | Result |
|---|---|---|---|
| W1 | walked-but-unreported parity finding disabled | pattern-missed-file | fail 1 (red) |
| W2 | reported-outside-roots parity finding disabled | file-outside-roots | fail 1 (red) |
| W3 | registry resolution disabled | behavior-renamed | fail 1 (red) |
| W4 | merge-base deletion comparison disabled | behavior-deleted-at-base | fail 1 (red) |
| W5 | skip accounting disabled | skip-reason | fail 1 (red) |
| W6 | counts.pass taken from a summary-shaped "pass N" line in raw output (the C-1 violation; the counterfeit 999 was believed) | counterfeit-immune | fail 1 (red) |
| W7 | truncation detection disabled | truncated-stream-error | fail 1 (red) |
| W8 | format pin widened to accept an arriving TAP stream via a format-agnostic regex (the historic M1-P6 shape) | foreign-format-error | fail 1 (red) |
| W9 | M2-C-5 pin comparison disabled | pin-rewrite-error | fail 1 (red) |
| W10 | absent --base silently defaulted to HEAD | base-required | fail 1 (red) |
| W11 | exit-0-with-failing-stream cross-check disabled | exit-stream-disagree | fail 1 (red) |
| W12 | registry read through bare readFileSync instead of the delivered probe-then-open (CR-520 shape) | registry-fifo-refused | fail 1 (red); wall time 2m0.8s, which is the harness 120s kill bound: the defanged gate HUNG on the pipe until killed, measured, so the hang is real and the probe is what prevents it |
| W13 | walk type refusal disabled (irregular entry pushed) | fifo-refused | fail 1 (red) |
| W14 | absent stream treated as an empty stream instead of a named refusal | absent-stream-error | fail 1 (red) |

Class coverage (one witness is not a class): discovery parity reddens
under two structurally different members (W1 selection-too-narrow, W2
selection-too-wide); M2-C-6 reddens under two structurally different
members (W12 a path the gate opens itself, W13 a path the walk
classifies; the walk witness's pin root is narrowed to src precisely so
its own guard, not takePin's identical refusal, is the guarded
mechanism); the counterfeit class has two structurally different members
inside one test (a summary-shaped line and a byte-exact forged event
line, the second defeated structurally by JSON string escaping rather
than by pattern); the stream-integrity class reddens under W7
(truncation) and W8 (foreign format), which are different failure shapes
of the same pinned-format rule.

Fixture provenance (warning 10): the truncated stream, the foreign
stream and the disagreeing stream are all REAL captured output produced
at test runtime by the pinned Node (a real green run's stream minus its
trailer; a real `--test-reporter=tap` capture asserted to begin
"TAP version 13"; a real failing run's stream), never hand-written
strings.

## Divergence found and reconciled (M2-C-1 report, cross-phase, orchestrator attention required)

MEASURED, not inferred: with the `suite` entry registered per section 1.4
(`parameters: ["base"]`), the delivered M2-P1 wiring test "the workflow's
gate bundle step runs the gate runner and is able to fail"
(test/gates.test.ts:1289) FAILED with captured output
`gates: declared 2 applicable 1 verdict 1 green 1 red 0 not-applicable 0
error 1 vacuous 0` / `gates: 1 gate(s) reported error: suite`. The test
extracts the workflow's PUSH bundle step (the one with no `--base`) and
executes it against this repository asserting exit 0; the runner refuses
to spawn a gate whose declared parameter is absent (M2-C-3), so ANY
registration of a parameterized required gate turns the push bundle
nonzero, which is a permanently red main after merge and a red suite on
every branch.

The MECHANISM, named: a required gate whose declared invocation
parameters cannot be supplied by an existing bundle invocation. The
derivation over the current and planned gate set, from section 1.4's
table against the two delivered workflow invocations (push: no --base,
no --phase; pull request: --base and --head, no --phase):

| gate | needs | push bundle | PR bundle |
|---|---|---|---|
| manifest-self-check | nothing | runs | runs |
| suite (this phase) | --base | error | runs |
| red-witness (M2-P2) | --base (diff-touches) | error | runs |
| scope (M2-P4) | --phase (branch-matches) | error | ERROR TOO |
| citations (M2-P5) | --base (diff-touches) | error | runs |

Not covered by this derivation: gates whose entries do not yet exist in
any branch (their final parameters could differ from the table), and
M2-P9's replacement of both steps, which the plan says supersedes this
wiring (workflow comment, M2R-026). NOTE THE SCOPE ROW: M2-P4's
registration will redden the PULL REQUEST bundle of every PR by the same
mechanism, and no interim --only on the push step fixes that; the
orchestrator must either have the PR step pass --phase or scope the PR
bundle's --only when P4 lands.

There is also a PLAN inconsistency to arbitrate at M2-P9: section 1.4's
main-bundle column says `suite` is green there, section 4 step 4's main
bundle passes no --base, and criterion 9 makes --base absent an error.
Those three cannot all hold; either the main bundle supplies a base (for
a push, github.event.before has force-push edge cases) or the suite row
becomes "not run (needs --base)" in the main-bundle column.

RECONCILIATION APPLIED, as its own commit (56db4e6) so one revert
removes it: the push bundle step gains `--only manifest-self-check`,
which keeps the push bundle measuring exactly what it measured before
this phase registered anything, and moves the interim wiring toward the
plan's own end-state main-bundle design, which is an --only list with
absences asserted rather than errored (section 4, M2-P9 step 4). This
touches `.github/workflows/gates.yml`, which is NOT on this phase's
files-to-touch list, and is declared here as a scope deviation: the
plan's own conflicts-with row for M2-P1 names gates.yml as contended
with M2-P3 (kernel-plan-m2.md line 244), so the files-to-touch omission
reads as the clerical shape of clean-room-m1-p2 deviation 1 rather than
an intended prohibition; the alternative, submitting with a
deterministically red delivered test and a bundle that reddens main on
merge, was rejected because criterion 10 and the standing CI gate cannot
hold under it. After the edit the wiring test passes again (1 test, 1
pass, measured), all its assertions intact including the falsifiability
arm (--only over the empty-manifest fixture exits nonzero via "--only
names no such gate").

## Pre-existing default-toolchain failures, proven pre-existing

The full suite on the container default toolchain (v22.22.2) failed 3,
then 4, tests across two runs before any reconciliation: the wiring test
above (mine, fixed), plus "doctor and the guard return one verdict about
one beacon" (test/liveness.test.ts:633) and intermittently two watcher
tests. Proof the liveness failure is pre-existing and not this phase's:
a scratch clone of this repository at origin/main 4c9bfbc (no M2-P3
changes), `npm ci` then the single test on v22.22.2: `not ok 1`, fail 1
(captured in the scratchpad, preexisting-check.sh). Its failure text on
v22 is `CHECK node FAIL v22.22.2 does not satisfy kernel engines ">=26"`,
a doctor-vs-guard disagreement that exists on every sub-floor toolchain;
on the floor toolchain the same test passed 3 of 3 isolated runs and
failed once under full-suite load on a beacon age off by one second
(expected /age 13s/, observed age 14s). The two watcher tests
("a resident watcher keeps running and backs off with growing beacon
gaps", "the heartbeat schedule is on disk and shared by single passes")
each passed 3 of 3 isolated floor runs and failed only under full-suite
concurrency. All three files are untouched by this phase; CI on Node 26
remains the authority (CLAUDE.md warning 1) and the second full floor
run was 216 of 216 with exit 0.

## Criteria walked

1. **Green on this repository.** Floor toolchain (the authority), gate
   run at head with `--base origin/main`, pin roots src bin test:
   exit 0, status green, units 216 equal to tests reported, detail:
   "reported 216 test(s) from 13 file(s) (pass 216, fail 0, skipped 0,
   todo 0, did-not-run 0); discovered 13 file(s) walking test for
   .test.ts; 222 behavior(s) resolve; merge base 4c9bfbc". The recorded
   arithmetic satisfies the step 6 mapping with discovered from the
   independent walk (13 files both sides, sets equal); pins present and
   equal across the run. Evidence: scratchpad c1-floor/{result.json,
   counts.json}. The default-toolchain run of the same command is
   recorded red with the pre-existing failures named (above), each
   failure NAMED BY THE GATE from the stream, which is the gate doing
   its job on an unstable local suite rather than a gate defect.
2. **Pattern-missed file, both directions.** Test
   "suite gate names a test file the configured pattern missed and
   passes once the pattern is corrected": bare runner asserted exit 0
   with `test/*.test.ts` while test/sub/x.test.ts exists; gate red
   naming test/sub/x.test.ts; corrected pattern exit 0. Plus the second
   parity direction (a reported file outside the declared roots) in its
   own test. Red-witnessed W1 and W2.
3. **Renamed behavior, both directions.** Bare runner exit 0 asserted;
   gate red naming the behavior and its unresolved test name; restored
   name exit 0. Red-witnessed W3.
4. **Deleted behavior.** Registered at merge base, deleted from head
   registry (test also deleted): bare runner exit 0 asserted, gate red
   naming subx and the merge-base sha. Red-witnessed W4.
5. **Skip accounting, both directions.** skip: true red naming the
   test; skip with reason exit 0. Red-witnessed W5.
6. **Counterfeit, C-1.** Two members: "pass 999" and a byte-exact
   forged event line; verdict green, units 3 unchanged, pass count 2
   unchanged, both lines captured verbatim in suite-raw-output.txt,
   forged point absent from the stream (asserted by parsing the stream).
   Red-witnessed W6 (a summary-line parse believed the 999). The
   measured tap contrast (counterfeit comment byte-identical in grammar
   to the real summary line) is in the step 1 survey.
7. **Truncated stream with runner exit 0 is error.** Staged with a REAL
   green run's stream minus its trailer, replayed by a stub that exits
   0: exit 21, error, detail names truncation and the missing trailer.
   Red-witnessed W7.
8. **Pin, both directions.** Byte-identical rewrite of src/lib.ts with
   restored-forward timestamps DURING the run: exit 21, error, detail
   names src/lib.ts and mtimeMs (and ctimeMs, the delivered fifth
   field). Healthy fixture asserts start and end pins recorded and
   deep-equal. Red-witnessed W9.
9. **--base absent is error.** Exit 21, status error, never
   not-applicable, detail names --base and the merge-base comparison
   (M2-C-3). Enforced twice: by the gate itself (tested) and by the
   runner via `parameters: ["base"]` in the manifest entry.
   Red-witnessed W10.
10. **Suite and registry criterion.** CR-M2P3-1 CORRECTED (was "216
    tests, 216 pass" at submission; the clean-room criteria review
    independently measured 217 twice at head 6fe8066, on both toolchains,
    and this fix round's own measurement at the CURRENT head b5f1cee
    (after merging origin/main, which brings in M2-P6's own new tests) is
    a THIRD number, 238, reproduced on BOTH toolchains: floor `npm test`
    (v26.6.0) "tests 238, pass 237, fail 1" (the one failure being the
    named pre-existing real-clock beacon-age flake, see below); default
    `npm test` (v22.22.2) "tests 238, pass 235, fail 1, skipped 2" (the
    one failure being the named pre-existing node-engine floor flake, and
    the 2 skips being the floor-gated tests that do not run under v22).
    Full logs: scratchpad/m2-fanout/m2-p3/fix/floor-full.log and
    default-full.log. The number moves between reviews because it counts
    the WHOLE kernel suite at the head measured, not this phase's own
    18-test file, and this branch has now merged main (M2-P6 alone adds
    tests); it is not evidence of nondeterminism in the count at any one
    fixed commit. `git status --porcelain` clean after build on both
    toolchains; `npm ci` on the floor toolchain shows zero EBADENGINE
    lines. Every behavior in the head registry resolves by name: witnessed
    mechanically by re-running the suite GATE ITSELF against this
    repository (criterion 1, --base 8439c88, floor toolchain): status
    red, units 238 (identical to the raw `npm test` total above),
    discoveredFiles 14 == reportedFiles 14 (parity holds, unaffected by
    the CR-1306 fix because the kernel has no empty .test.ts files),
    244 behaviors resolve, one finding named
    ("failing test: \"doctor and the guard return one verdict about one
    beacon\" (test/liveness.test.ts)"), which is the same real-clock
    beacon-age flake named by the raw run, correctly identified rather
    than mis-reported. Evidence:
    scratchpad/m2-fanout/m2-p3/fix/c1-floor/{result.json,counts.json}.
    CR-1306's own fix left this criterion's real count UNCHANGED, as
    required: the phantom-filter only removes points for a file whose
    ONLY reported point is the nesting-0 self-named wrapper, and no file
    in this repository is empty, so `points` and `stream.points` are
    identical here (counts.json's reportedFiles 14 matches the
    independent walk's discoveredFiles 14 exactly, as they did before the
    fix).
11. **Reporter pin, both directions, both toolchains.** A REAL captured
    TAP stream replayed into the gate: exit 21, error, detail names
    tiphys-suite-events-v1 (expected) and "TAP version 13" (observed);
    red-witnessed W8 with the historic widened parse accepting it. The
    pinned format parses to correct counts on both installed
    toolchains: the same fixture run under v22.22.2 and v26.6.0
    produced BYTE-IDENTICAL counts objects
    {"reported":3,"pass":2,"fail":0,"skipped":1,"todo":0,"didNotRun":0,
    "discoveredFiles":2,"reportedFiles":2,"behaviors":3,
    "mergeBaseBehaviors":3} and the same requested-reporter name, with
    each record carrying the child node version measured FROM INSIDE
    the child (stream header: v22.22.2 / v26.6.0); both runs and both
    `node --version` outputs captured (scratchpad c11-both.sh output).
    READING, stated openly: on the kernel suite itself byte-identical
    counts across toolchains are impossible BY DESIGN, because
    CLAUDE.md warning 1 requires floor-gated tests, which are skipped
    (with reasons) on v22 and run on v26; the kernel's two records show
    that divergence honestly (skipped 2 versus 0) through an identical
    parse, which is the property the criterion protects; the M1-P6
    defect was a parse that differed between toolchains, and this
    criterion's fixture-level discharge shows the parse does not.

## Pre-submit hazard self-review (T-007, the declared hazard class)

The class: **a measurement that can shrink silently.** Each named hazard,
walked:

- "a checker whose enumeration shares the runner's own selection
  pattern": the walk enumerates declared roots for the declared suffix
  and never reads the pattern; witnessed by criterion 2's fixture where
  the pattern is wrong and the walk is not, and by W1.
- "a reporter whose default format differs between the two toolchains":
  no default is ever consumed; the reporter is requested per child with
  ambient NODE_OPTIONS dropped, the stream is format-validated before
  counting, and the same fixture parses byte-identically on both
  toolchains (criterion 11). W8 witnesses the widened-parse dangerous
  state.
- "a skip with no reason": criterion 5, W5.
- "a count taken from prose": no summary line is read anywhere
  (test:diagnostic events are deliberately not emitted by the
  reporter); W6 witnesses the prose-count dangerous state believing a
  counterfeit.
- "a behaviour deleted from the registry rather than from the code":
  criterion 4 against the merge-base copy, W4; --base required so the
  comparison cannot be silently skipped (criterion 9, W10).
- "a wrapper defect that makes the suite itself unrunnable": npm test
  is untouched (M2-D-09); verified in the delivered scripts block
  before editing and by the full floor suite running bare.
- Residual, named rather than hidden: a test file whose every test is
  silently absent from the stream while the file still reports at least
  one point would evade file-set parity; the registry resolution is the
  compensating control for registered behaviors (plan step 6 says
  exactly this), and unregistered logic remains uncovered (plan's own
  not-proven item 8, section 1.5 row 7 "in part"). A gate subprocess
  hostile enough to forge the ENTIRE stream file after the child exits
  is outside what this gate can measure (its own child wrote the
  destination; the runner-level pin re-check and the claim mechanism
  bound what a foreign writer can do to the evidence directory, and
  M2-P2's harness owns witness-level assurance).

## Fix round one (CR-1306, CR-M2P3-1, CR-M2P3-2)

Source: delivery/review/arbitration-m2-p3.md (round one), against head
6fe8066, cross-referencing delivery/review/clean-room-m2-p3-hazard.md
(Opus, hazard contract, CR-1306) and clean-room-m2-p3-criteria.md
(Sonnet, criteria contract, CR-M2P3-1/2). Both fetched at
origin/claude/m2-phase-reviews.

### Setup: branch update, done FIRST as instructed

`git fetch origin main claude/m2-p3-suite-wrapper`, checked out the phase
branch, `git merge origin/main`. Three conflicts, all resolved as unions
or as instructed:

- `test/behaviors.json`: both sides' keys kept (this phase's 16
  suite-gate-* entries plus M2-P6's coverage-* entries and two gate-* wiring
  entries); validated with `node -e "JSON.parse(...)"` after resolution,
  242 keys (then 244 after this round's own two additions below).
- `gates.manifest.json`: both gate entries kept as separate array elements
  (`suite` from this phase, `coverage` from M2-P6); validated as JSON.
- `.github/workflows/gates.yml`: reconciled ENTIRELY to origin/main's
  version (`git checkout --theirs .github/workflows/gates.yml`), dropping
  this phase's interim `--only manifest-self-check` edit, because
  origin/main already carries the schema self-check fix from PR #13.
  Verified: `git diff origin/main -- .github/workflows/gates.yml` is
  EMPTY after resolution and after the merge commit (b5f1cee). This
  phase's diff against origin/main no longer touches gates.yml at all.

CR-M2P3-2 is CLOSED by this reconciliation, not argued: the file this
phase used to touch is now byte-identical to main's, so the scope
question (was gates.yml an authorized contention for M2-P3) is moot for
the delivered diff. No pre-pass amendment is needed, matching the
arbitration's instruction ("no pre-pass amendment needed once the file
leaves P3's diff").

### CR-1306: the mechanism, not the instance

**Finding as named by the reviewer:** an empty `.test.ts` file's phantom
pass is counted as a real test.

**MECHANISM, stated at the level a fix must close:** every site in
src/gates/suite.ts that decides "this point is a reported test" tests
`point.entityType === "test"` alone. That predicate is true for BOTH a
real test AND the one synthetic point node itself emits for a file that
defines zero tests (a "file-wrapper phantom"), because node does not
expose a THIRD entityType for that case. The comment at (pristine)
suite.ts:335-337 asserted the false generalization "file wrappers emit no
pass/fail", which is true for `entityType === "suite"` wrappers
(`describe()`) but not for the zero-test-file case, which node reports as
`entityType === "test"` masquerading as a real test.

**Derivation: every call site of the mechanism**, run against the
PRISTINE (pre-fix) file at head 6fe8066:

```
$ grep -n 'entityType' src/gates/suite.ts
136:  "        entityType: details.type,\n" +
159:  entityType: "test" | "suite";
251:      entityType?: unknown;
284:      (event.entityType !== "test" && event.entityType !== "suite")
287:        `stream line ${String(index + 1)} is a ${event.event} event missing name, file, nesting or entityType`,
295:      entityType: event.entityType,
335: * `reported` counts points whose entityType is `test`, which reproduces
336: * node's own "# tests" semantics (measured: suites are not tests, file
337: * wrappers emit no pass/fail). The identity
375:    if (point.entityType !== "test") {
941:        .filter((point) => point.entityType === "test")
966:      .filter((point) => point.entityType === "test")
```

Of these, three are the COUNTING sites (the ones a fix must change), and
they are the three the CR-1306 finding traces: line 375 (`bucketPoints`,
feeds `counts.reported` and every derived bucket), line 941 (discovery
parity's `reportedFiles` set), line 966 (registry resolution's
`reportedTestNames` set). Lines 136, 159, 251, 284, 287, 295 are the
stream's own shape (parsing and typing `entityType`, unchanged; the value
itself is not wrong, only what the three counting sites DO with a
`"test"` value that happens to be a phantom). A second grep over the
whole repository confirms no fourth production call site exists:

```
$ grep -rn 'entityType' --include='*.ts' . | grep -v node_modules
./src/gates/suite.ts: (the 12 lines above)
./test/suite-gate.test.ts:372:    '{"event":"test:pass","name":"forged test","file":"test/a.test.ts","nesting":0,"entityType":"test"}';
```

The one test-file hit is a hand-built forged-line FIXTURE for the
counterfeit-immunity test (criterion 6), not a counting site; it is not
in scope for this fix and was not touched.

**NOT COVERED by this derivation, stated:** the grep is over `*.ts` files
only, so a gate written in another language reusing the same reporter
protocol would not appear; none exists in this repository. The grep also
does not search compiled `dist/`, which is never committed and is
regenerated from the same source, so a hit there would be redundant with
one already listed. Both exclusions are the whole universe of what could
have been missed and neither hides a fourth site.

**Identifying the phantom itself, measured directly against the pinned
node** (default toolchain shown; the floor toolchain produces
byte-identical shapes modulo the node version string), using a probe
reporter that prints every raw node:test event for a scratch fixture:

```
$ node --version
v22.22.2
$ : > test/empty.test.ts
$ node --test --test-reporter=./reporter.mjs --test-reporter-destination=stdout "test/empty.test.ts"
{"type":"test:pass","data":{"name":"test/empty.test.ts","nesting":0,"testNumber":1,
 "details":{"duration_ms":135.5,"type":"test"},"file":".../test/empty.test.ts"}}
```

For a file with ONE real top-level test (`test("a real top-level test", () => {})`):

```
{"type":"test:pass","data":{"name":"a real top-level test","nesting":0,"testNumber":1,
 "details":{"duration_ms":2.2,"type":"test"},"file":".../test/real.test.ts"}}
```

For a file with a `describe()` wrapping one test:

```
{"type":"test:pass","data":{"name":"nested test","nesting":1,...,"details":{"type":"test"},...}}
{"type":"test:pass","data":{"name":"group","nesting":0,...,"details":{"type":"suite"},...}}
```

For a file with an EMPTY `describe()` (zero tests inside it):

```
{"type":"test:pass","data":{"name":"empty group","nesting":0,...,"details":{"type":"suite"},...}}
```

**Reading:** nesting alone does not distinguish the phantom (both the
phantom and a real top-level test are nesting 0); entityType alone does
not either (both are "test"; only a `describe()` wrapper, empty or not,
is "suite" and is already excluded by the existing filter). The ONE fact
that does distinguish the phantom from every real test observed is that
the phantom's `name` is exactly the file's own path as invoked
(`relative(cwd, point.file)`), which node sets only because it has
nothing else to name the file-level entity after when the file contains
no test or describe calls at all. A real test can only produce this same
string by being deliberately named after its own file, which is the
documented residual false-negative (see below).

**The fix:** `isFileWrapperPhantom(point, cwd)` in src/gates/suite.ts
tests exactly this (`entityType === "test" && nesting === 0 && name ===
relative(cwd, file)`) and is applied ONCE, filtering `stream.points` into
a `points` array immediately after the pin check and before any of the
three counting sites read it, so bucketPoints, the discovery-parity
`reportedFiles` set, and the registry-resolution `reportedTestNames` set
all see the cleaned array. The false comment at (pristine) lines 335-337
is corrected in place to state the real distinction rather than the false
generalization.

**Effect on the existing machinery, not a new finding message:** filtering
the phantom out of `points` means an emptied file no longer appears in
`reportedFiles`, so the EXISTING discovery-parity check (unchanged, step
3) reports it as "test file discovered by the walk but absent from the
reporter", which is `red`, naming the file. No new finding string was
invented; the fix routes the phantom into a mechanism the phase already
had for exactly this shape (a walked file the reporter did not really
report on).

### Red witness, strong form, both directions

Two structurally different members (one witness is not a class):

1. **Mixed suite** (test/suite-gate.test.ts, "a file that defines zero
   tests is named, not counted as a passing test (CR-1306, mixed)"):
   one real file (alpha passes, beta skipped-with-reason) plus one empty
   file, registry naming only alpha and beta.
2. **Total suite** (test/suite-gate.test.ts, "a suite of only empty test
   files is red naming every one, never a counted green (CR-1306,
   total)"): two empty files, empty registry, reproducing the
   arbitration's own captured shape ("three empty .test.ts files plus an
   empty registry -> GREEN, units 3") with two files instead of three.

Method, same as the phase's own red-witness ledger (never `git checkout
--`, warning 8): pristine `src/gates/suite.ts` sha256 recorded, the ONE
line `const points = stream.points.filter((point) =>
!isFileWrapperPhantom(point, cwd));` replaced by `const points =
stream.points;` (the dangerous, pre-fix state: no phantom filter at all),
both new tests run via `--test-name-pattern "CR-1306" --test
test/suite-gate.test.ts`, pristine bytes restored by `cp` from a saved
copy (never `git checkout --`), sha256 re-verified byte-identical,
`git status --porcelain` clean.

```
$ sha256sum src/gates/suite.ts
2cd0c3720f49d961b777657f2da34b7b20d15d5af3b698712780b3a4ed4b937f  (pristine, before defang)

# defanged: const points = stream.points; (no filter)
$ node --test-name-pattern "CR-1306" --test test/suite-gate.test.ts
not ok 1 - a file that defines zero tests is named, not counted as a passing test (CR-1306, mixed)
  Expected values to be strictly equal: 0 !== 1
not ok 2 - a suite of only empty test files is red naming every one, never a counted green (CR-1306, total)
  Expected values to be strictly equal: 0 !== 1
1..2
# pass 0
# fail 2

$ cp <pristine copy> src/gates/suite.ts
$ sha256sum src/gates/suite.ts
2cd0c3720f49d961b777657f2da34b7b20d15d5af3b698712780b3a4ed4b937f  (identical to before)
$ git status --porcelain
 M src/gates/suite.ts    (still only the committed fix diff; no leftover defang)

# restored: pristine fix in place
$ node --test-name-pattern "CR-1306" --test test/suite-gate.test.ts
ok 1 - a file that defines zero tests is named, not counted as a passing test (CR-1306, mixed)
ok 2 - a suite of only empty test files is red naming every one, never a counted green (CR-1306, total)
1..2
# pass 2
# fail 0
```

Both tests are RED against the dangerous state (the run's recorded
status was "green" in the defanged run, where the test asserts "red")
and GREEN with the fix restored, satisfying the strong form: the
assertion `run.record.status === "red"` failed (actual "green") without
the fix and passed with it, over a suite that genuinely runs zero real
tests, which is the DANGEROUS state (a suite reporting nothing real is
exactly what "never green by omission" exists to catch), not merely the
absent-feature state.

Full 18-test suite-gate.test.ts run, fix restored: default toolchain 18
pass 0 fail (isolated); floor toolchain 18 pass 0 fail (isolated,
confirmed as part of the full floor `npm test` below, where none of the
18 suite-gate tests are among the failures).

**Not covered by this red-witness pair, stated:** a real test deliberately
named identically to its own file's invocation path (for example a test
literally named the same as its file's relative path) would ALSO match
`isFileWrapperPhantom` and be wrongly filtered as a phantom; no test in
this repository or its fixtures does this, and no criterion in the plan
requires distinguishing it, because node itself gives the gate no other
signal (measured above: nesting and entityType are identical between the
phantom and a real top-level test). This is the same class of documented
residual the ORIGINAL submission already named in its pre-submit hazard
self-review ("a test file whose every test is silently absent from the
stream while the file still reports at least one point would evade
file-set parity"), narrowed here to the one shape node's own event stream
makes indistinguishable.

### Criterion 1 re-verified: the real kernel count is UNCHANGED by the fix

Ran the delivered gate directly against this repository (floor toolchain,
`--base 8439c88`, the merge base with origin/main after this round's
merge), exactly as criterion 1 requires:

```
$ node src/gates/suite.ts --result .../result.json --evidence .../c1-floor \
    --base 8439c88 --pin-root src --pin-root bin --pin-root test
suite: red (238 tests reported)
1 finding(s): failing test: "doctor and the guard return one verdict about one beacon" (test/liveness.test.ts)
```

counts.json: `{"reported":238,"pass":237,"fail":1,"skipped":0,"todo":0,
"didNotRun":0,"discoveredFiles":14,"reportedFiles":14,"behaviors":244,
"mergeBaseBehaviors":226}`. `discoveredFiles` equals `reportedFiles` (14
== 14): the independent walk and the (now-filtered) reporter set still
agree exactly, because this repository has zero empty `.test.ts` files,
so `points` and the pristine `stream.points` are IDENTICAL here and the
fix changes nothing about this run's arithmetic. `units` (238) is
byte-identical to the raw `npm test` total measured independently on the
same toolchain in the same session (below), and the ONE finding named is
the same known real-clock liveness flake the raw run also failed on,
correctly identified rather than mis-reported as a parity or counting
defect. Evidence:
scratchpad/m2-fanout/m2-p3/fix/c1-floor/{result.json,counts.json}.

### Gates, both toolchains

Default toolchain (v22.22.2): `npm ci` exit 0 (EBADENGINE warning only,
expected), `npm run build` exit 0, `git status --porcelain` clean after
build, `npm test`: 238 tests, 235 pass, 1 fail, 2 skipped, 0 cancelled,
0 todo. The one failure is test/liveness.test.ts:633 ("doctor and the
guard return one verdict about one beacon"), `CHECK node FAIL v22.22.2
does not satisfy kernel engines ">=26"`, the SAME named pre-existing
default-toolchain floor-gate failure this work history already
documented before this fix round. Full log:
scratchpad/m2-fanout/m2-p3/fix/default-full.log.

Floor toolchain (v26.6.0, PATH-prefixed so the spawned child `sh -c` also
resolves `node` to floor): `npm ci` exit 0 (zero EBADENGINE lines),
`npm run build` exit 0, `git status --porcelain` clean after build,
`npm test`: 238 tests, 237 pass, 1 fail, 0 skipped, 0 cancelled, 0 todo.
The one failure is the SAME test/liveness.test.ts:633, this time with
`CHECK node PASS` (floor satisfies >=26) but a beacon-age assertion off
by one second under load (`expected /age 13s/, observed age 14s`),
exactly the real-clock timing-sensitivity shape both clean-room reviews
already documented for this test under heavy concurrent load (this
container ran 14+ concurrent `node --test` processes from other phases'
sessions during this run, confirmed by `ps aux` at the time). Full log:
scratchpad/m2-fanout/m2-p3/fix/floor-full.log.

Both failures are in test/liveness.test.ts, which is NOT in this phase's
diff. This round's diff against origin/main
(`git diff origin/main...HEAD --name-only`) is: gates.manifest.json,
src/gates/suite.ts, test/behaviors.json, test/suite-gate.test.ts,
delivery/work-history/m2-p3.md; `.github/workflows/gates.yml` is ABSENT
from this list (see CR-M2P3-2 below) and package.json is unchanged by
this round (this phase's own gate:suite script addition from the
original submission is untouched). No failure occurred in
test/suite-gate.test.ts on either toolchain, in the full run or in
isolation.

### CR-M2P3-2 re-confirmed closed

`git diff origin/main -- .github/workflows/gates.yml` is EMPTY at this
round's head. This phase's diff against origin/main no longer includes
`.github/workflows/gates.yml` at all. The scope concern is closed by the
file leaving the diff, not by an argument about which document authorized
it; no amendment to `delivery/plan/m2-conflict-pre-pass.md` is needed,
matching the arbitration's instruction.

### Claim grep, run over this file after all edits

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m2-p3.md
```

Hits from the ORIGINAL submission (pre-existing, each already carries an
adjacent captured command or is a restated open question in that
section, unchanged by this round): the pre-submit hazard self-review's
"no default is ever consumed", "never a hang", and the "M2-C-3 ...
never not-applicable and never green" lines in the criteria walk, all of
which name the enforcing code path or an adjacent test alongside the
claim. New hits introduced by this round, each checked:

- "no fourth production call site exists" (derivation section): the
  adjacent command is the repository-wide grep pasted immediately above
  it, with its full output; the claim is exactly what that command's
  output shows, not an inference beyond it.
- "no criterion in the plan requires distinguishing it" (residual
  paragraph): restated as an open question, not a settled impossibility;
  the adjacent sentence names WHY (node gives no other signal, measured)
  rather than asserting no other signal could ever exist.
- "never a hang" (M2-C-6, pre-existing, unchanged): already qualified by
  the harness-timeout witness in the original criteria walk.

No hit in this round's new text asserts an unqualified impossibility
without an adjacent command or an explicit restatement as open.
