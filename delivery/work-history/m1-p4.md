# Work history: M1-P4 (spawn and teardown guard)

## Prompt context

Implementer for phase M1-P4 of kernel plan v1 (delivery/plan/kernel-plan-v1.md, revision 7), branch claude/m1-p4-spawn-and-teardown, worktree based on origin/main at 54ceb6e (M1-P1, P2 and P3 merged). Mandated reading completed in order before any code: CLAUDE.md (conventions, gates, the strengthened red-witness rule, the eleven environment warnings, the Never list); the plan header and binding rule; the full section 3 preamble (shared phase fields, the invocation form tiphys <cmd> = node bin/tiphys.ts, constraints C-1, C-2 and C-3, the test accounting rule and test/behaviors.json); the full M1-P4 section (intent, grounding, all six steps, files-to-touch, all 14 acceptance criteria, citations); delivery/STATE.md's in-flight section and the four obligations it names; the merged code this phase extends (src/fleet.ts, src/lock.ts, src/commands/lock.ts, src/pool.ts, src/commands/pool.ts, src/cli.ts); delivery/work-history/m1-p1.md, m1-p2.md and m1-p3.md; and delivery/tuition/T-002 and T-003.

Scope: exactly the M1-P4 files-to-touch list plus test/behaviors.json and this file (the two standing pre-authorized extras). Content-level necessity deviations are declared below, not left silent.

## Verify-before-editing

Confirmed ABSENT at branch point 54ceb6e before writing: src/task.ts, src/brief.ts, src/hooks.ts, src/spawn.ts, src/teardown.ts, src/commands/spawn.ts, src/commands/teardown.ts, test/spawn.test.ts, test/teardown.test.ts, delivery/work-history/m1-p4.md. Confirmed PRESENT and consumed unmodified: src/cli.ts (the one dispatch table), src/fleet.ts (loadFleet, tasksDir, worktreesDir, lockPath), src/lock.ts (leaseStatus), src/pool.ts (poolCreate, poolDestroy, readPoolRecord, worktreePath, TASK_ID_PATTERN). src/pool.ts was verified as needing no edit, exactly as the files-to-touch note anticipated (PR-010): --discard and --delete-branch-force both already ship, and teardown consumes them.

## Files touched

Created: src/task.ts, src/brief.ts, src/hooks.ts, src/spawn.ts, src/teardown.ts, src/commands/spawn.ts, src/commands/teardown.ts, test/spawn.test.ts, test/teardown.test.ts. Edited: src/cli.ts (two lines of imports, two dispatch-table entries; nothing else), test/behaviors.json (pure append), and this file. `git diff --name-status origin/main...HEAD` lists exactly those eleven paths plus this one.

## Per-step commits

- 9078c84 M1-P4 step 1: task meta, task paths, and the shared holdership guard
- 08d6802 M1-P4 step 2: brief assembly with the fleet warnings file appended verbatim
- b21fb0a M1-P4 step 3: generated turn-end hook writing the payload exit code
- d2ac5a4 M1-P4 step 4: spawn with the executor adapter seam and scoped rollback
- e0e5413 M1-P4 step 5: teardown guard with fetch-first landedness and salvage
- 1dd9f78 M1-P4 step 6: register spawn and teardown in the dispatch table
- c25c670 M1-P4 step 6: spawn and teardown tests, behavior registry entries
- 83c2156 M1-P4 step 6: guard the scout carve-out against silently discarding commits
- (this file's commit follows)

## Key decisions and the why that is invisible in the diff

1. meta.branch is the TASK branch (task/<id>), which is deliberately not what the pool record calls "branch" (the project's default branch; the pool's task branch field is branchName). Teardown judges the task branch, so meta records the branch the task works on. Both names are documented at their definitions so the mismatch cannot be read as a bug.
2. baseSha and baseOffline are COPIED from the pool record poolCreate returned. baseOffline is never recomputed from spawn's own --offline flag, because pool create attempts the fetch even under --offline and records offline false when it succeeds. A recomputing spawn would mark a perfectly fetched base as offline whenever a contended fetch succeeded on retry, which is the exact provenance inversion measured as V-2 in M1-P3. Witness W2 below is red against that dangerous state, not against an absent field.
3. Holdership uses M1-P3's transport unchanged: lock acquire prints the opaque holderId, the operator carries it, and it arrives as TIPHYS_HOLDER_ID. No second identity mechanism was invented, nothing is derived from the running program (C-2), and the check is a read of the lease FILE through the merged leaseStatus. Both tests parse the holder id out of lock acquire's real captured stdout rather than asserting a hand-written string.
4. The holdership check runs BEFORE pool create even though the plan's prose introduces it later in the step, because criterion 12 requires the refusal to create nothing. Witness W8 (check moved after pool create) is red 3/3 with "the refusal created a worktree".
5. checkHoldership lives in src/task.ts. The files-to-touch list has no shared-guard module, and the rule is a precondition of every task mutation, so the task-state module is the least-bad home for it; spawn and teardown share one implementation rather than two copies of a security-relevant rule.
6. The fleet's own lease being ABSENT is a pass (the plan's M1 test contexts). Corrupt, expired, or held-by-another all refuse: fail closed, because each of those states means the caller cannot prove it is the one orchestrator.
7. The executor seam is ExecutorAdapter with exactly the plan's contract (write tasks/<id>/executor.json at launch, launch the payload in the worktree, ensure the turn-end file is written with the payload exit code). Everything crosses the boundary as files and exit codes; nothing inspects a terminal (FM-055, FM-060). M1 ships one adapter, subprocess.
8. The subprocess adapter runs the payload WITHOUT a shell. Under a shell a missing payload binary arrives as an ordinary exit code 127, indistinguishable from a payload that ran and failed, and spawn's rollback rule turns on exactly that distinction (criterion 5). The cost is that --exec is tokenised on whitespace with no quoting; that is documented at the parser and is an adapter's business later, not a string this kernel should re-parse.
9. The payload runs to completion before spawn returns (C-3). Witness W1 (payload backgrounded and detached) is red 3/3 with "spawn returned before the payload had exited", so the no-backgrounding property has a real guard rather than a comment.
10. A NONZERO payload exit is not a failed spawn. The payload's outcome lives in the turn-end record, which with meta.json status is the single current-state authority (C-1); spawn exits 0 and reports the code on stdout. Rolling back a worktree because the payload failed would destroy the work an operator needs to inspect, which is M1-P3's V-1 defect in a new place. Witness W7 makes that concrete: making a nonzero payload trigger the rollback turns the turn-end test red 3/3.
11. Rollback scope, stated narrowly on purpose. Before pool create: nothing is removed because nothing was created, and in particular nothing under tasks/<id>/ is written until the create has succeeded, so a duplicate id leaves a pre-existing task byte-identical (criterion 4, PR-005). After pool create and before the payload starts: exactly the files this invocation wrote, the task directory only if this invocation created it, and the pool worktree through poolDestroy WITHOUT --discard and WITHOUT --delete-branch-force, so a worktree that is somehow not pristine refuses and survives rather than being destroyed by a cleanup path. Once the payload has started: nothing is ever rolled back.
12. The turn-end hook is a generated .mjs script in the TASK directory, never in the worktree (FM-059, absolute invariant), taking the payload exit code as its single argument with the turn-end path baked in. The .mjs extension makes it parse as ESM regardless of the fleet home's package.json, and the adapter invokes it with process.execPath rather than relying on a node shebang resolving on PATH.
13. Brief assembly appends the fleet's warnings.md verbatim, with a single newline inserted only when the brief does not already end in one, and writes nothing else. With no warnings file present, brief.md is byte-identical to the brief file (asserted directly, not by substring).
14. Teardown reads the pool record for the project, remote and default branch rather than re-deriving them, so EXT-F-03's remote resolution has one implementation. A missing or unreadable record refuses rather than guessing.
15. Teardown's git runner is local to src/teardown.ts rather than an export added to src/pool.ts, because src/pool.ts is explicitly out of this phase's edit scope. It has no contention retry: nothing in M1 runs teardown concurrently with anything (parallelism is off until M5), and adding retry machinery for a state M1 never reaches is the shape that cost M1-P3 four rounds.
16. Landedness is is-ancestor first, then the merge-tree tree-equality procedure against the FETCHED default ref, fail-closed on anything inconclusive (an unexpected exit from either git command refuses). Conflicts (merge-tree exit 1) are a plain unlanded verdict. Witnesses W3 (judge against the stale local ref) and W4 (ancestor only, no merge-tree) are each red 3/3.
17. THE SALVAGE COMMIT HAPPENS AFTER THE LANDED CHECK, and this is the one place where the plan's step ordering and its own criteria pull in different directions. Step 5's prose introduces the salvage inside the dirty-refusal clause ("unless --salvage: commit leavings ... then proceed"), before the landed clause. But a salvage commit is by construction NOT landed: it introduces content the default branch does not have, so a merge-tree comparison after it can never be a no-op. Committing first would make criterion 8's final requirement ("after the branch is landed ... teardown --salvage exits 0") unsatisfiable for every salvage. Deciding first and acting afterwards also keeps every refusal a true no-op: a refused teardown never commits, never pushes and never removes. The criteria therefore fix the meaning, and the code follows the criteria. Witness W5 (salvage moved before the check) is red 3/3, and the assertion that catches it is that the branch tip and the remote were unchanged by the refusal.
18. The scout path passes --discard and NOT --delete-branch-force. --discard's plan-defined meaning is the dirty-tree override; conflating it with branch deletion would make a scout that committed to its scratch branch lose those commits silently, which is precisely M1-P3's V-1. A committed scout is refused by the pool's own branch gate, with the commit intact. This is a live decision, so it has its own test and witness W9 (scout path forcing the delete) is red 3/3 with "committed scout work was destroyed without a word".
19. The ship path passes --delete-branch-force after its landedness judgement, which is exactly the dependency src/pool.ts documents as coming from here. After a salvage the branch carries the WIP commit, which has been pushed, so removing the local branch loses nothing.
20. Teardown never re-frames a destroy failure. M1-P3's destroy has two failure kinds that mean opposite things: a stage-2 refusal is a true no-op, a stage-3 partial failure has already removed the worktree and enumerates the survivors. Teardown passes the destroy's own reason through verbatim, prefixed only with "pool destroy did not complete:" and suffixed with the one thing this layer knows for certain ("task <id> stays open"). Witness W6 (the failure dressed up as "teardown refused: nothing was changed") is red 3/3.
21. The salvage commit sets command-scoped GIT_AUTHOR_* and GIT_COMMITTER_* to the machine identity exported by src/commands/init.ts (one authority for that constant), because CI runners have no git identity and the fleet never reads or writes user or global git configuration (EXT-F-02 option B).
22. The liveness-guard seam is a documented no-op function called in the right position, so M1-P5 adds a body rather than a new call site in the middle of the spawn sequence.
23. A payload terminated by a signal has no exit code; the turn-end record uses the shell convention 128 + signal number. M1 never drives that path, and this is recorded as an interpretation rather than left as a guess in the code.

## Deviations from the plan's letter

1. `--offline` added to spawn's flag list (passed straight through to pool create). The plan's step 4 enumerates spawn's flags without it, but this phase's named inherited obligation is that a worktree created under --offline reaches meta.json as baseOffline true, and spawn is the only command that creates the worktree a task's meta describes: without the flag the obligation is unwitnessable in the true direction. The flag is additive, defaults off, and both directions are tested.
2. `executor.json`'s deadline is recorded as an absolute ISO-8601 instant (launchedAt plus the requested seconds), not as the raw seconds. The plan fixes the field name and its optionality, not its units. An instant is what the M1-P5 watcher compares against the clock, the same way it already compares lease expiry. Declared because "present and correct" in criterion 11 does not itself decide the units; the test asserts the exact relationship.
3. test/behaviors.json edited although not on the files-to-touch list: the standing clerical gap. The section 3 test accounting rule and criterion 14 both mandate the append. Pure append; nothing existing was modified or removed.
4. Registered tests beyond the criteria-named behaviors: spawn-usage-errors, teardown-usage-errors, teardown-fetch-failure-refused (the fail-closed half of step 5's fetch-first rule), teardown-partial-destroy-not-refusal (the destroy contract this phase drives), teardown-scout-committed-work-preserved (key decision 18). All registered; nothing previously registered was removed.
5. Salvage ordering relative to the landed check, as argued in key decision 17. This is a deviation from the prose's order and a conformance to criterion 8; it is called out rather than buried.

No other deviations: no tsconfig changes, no dependency changes, CI workflow untouched, src/pool.ts and src/lock.ts consumed unmodified, no delivery/ writes beyond this file.

## Gate evidence (local, Node v22.22.2, npm 10.9.7, git 2.43.0; CI on Node 26 is the authority)

1. npm ci from a clean state (node_modules and dist removed first): exit 0 (EBADENGINE warnings expected below the floor, ignored).
2. npm run build: exit 0; dist/src/task.js, dist/src/brief.js, dist/src/hooks.js, dist/src/spawn.js, dist/src/teardown.js, dist/src/commands/spawn.js, dist/src/commands/teardown.js emitted; `git status --porcelain` empty afterwards.
3. npm test with dist/ removed first (no prior build): exit 0; tests 99, pass 97, fail 0, cancelled 0, skipped 2, todo 0. The 2 skips are the unchanged M1-P2 floor-gated doctor exit-0 witnesses with their recorded reason; zero unaccounted. Wall time 67.6s (the P3 figure was 63.4s; budget harness timeouts off 68s).
4. Registry name check by script over that same run: 100 mappings, 0 missing, 99 test titles in the run; every P1, P2 and P3 mapping still resolves and nothing was removed. (Two pool behaviors deliberately map to the one test that witnesses both halves, as before.)
5. Non-ASCII scan and em-dash scan over all authored files of this phase: clean (grep exit 1).
6. C-2 structural scan over the new sources (process.kill, /proc/, signal-0, pid): clean (grep exit 1). Liveness is lease freshness; nothing here probes a process.
7. Scope audit: `git diff --name-status origin/main...HEAD` lists exactly the files-to-touch list plus test/behaviors.json (plus this file).

## Acceptance criteria walk (independent of the test suite; commands and exit codes captured)

Walked end to end in a scratch fleet with a real upstream repo and clone. Full transcript reproduced by the walk script kept with this round's evidence; the results:

1. PASS. spawn with a stub that writes its cwd: exit 0; the recorded cwd equals the realpath of <fleet>/worktrees/t1; meta.json parses with exactly the nine documented fields, status open, baseSha equal to the upstream head and to the pool record's baseSha, baseOffline false; brief.md contains the brief text followed by the full warnings.md text; the completion marker written after the stub's 0.3s sleep existed at the moment spawn returned. (Test spawn-runs-payload-in-worktree; the no-warnings half is spawn-brief-without-warnings, which asserts byte equality.)
2. PASS. tasks/t2/turn-end parsed as {"endedAt": "2026-08-04T22:07:42.613Z", "exitCode": 7} for a stub exiting 7; endedAt round-trips through Date exactly. (Test spawn-turn-end-record.)
3. PASS. spawn without --exec: exit 64, usage on stderr, stdout empty, and neither tasks/t3/ nor worktrees/t3 nor the pool record exists. (Test spawn-requires-exec.)
4. PASS. Duplicate id: exit 1 with "task id already used: t1"; md5 of every file in tasks/t1/ identical before and after; the worktrees listing unchanged. (Test spawn-duplicate-id-preserves-task.)
5. PASS. Nonexistent exec binary: exit 1 with "executor launch failed: ... ENOENT"; worktrees/t4, tasks/t4/, the pool record and refs/heads/task/t4 all gone; a neighbouring task's files byte-identical; the id is re-usable afterwards. (Test spawn-launch-failure-rollback.)
6. PASS. Ship branch with a pushed commit absent from the fetched default: exit 1, exactly one reason line naming task/t5, worktree still present. (Test teardown-unlanded-refused.)
7. PASS. Squash-merged branch with the clone's local main AND its origin/main both still at the pre-squash sha: teardown exit 0. Walk case carried one commit; the registered test carries TWO and asserts the count as a precondition, which is the form the criterion requires so a per-commit patch-id implementation cannot pass. (Test teardown-squash-landed.)
8. PASS, all three directions. Dirty ship tree: teardown exit 1 naming the uncommitted files. teardown --salvage while unlanded: exit 1 naming the branch, with the branch tip and the remote ref proved unchanged and the leavings still uncommitted (the refusal is a true no-op). After the squash land: teardown --salvage exit 0, and the remote branch tip message is "WIP-UNREVIEWED (do not treat as reviewed): leavings salvaged by tiphys teardown for task t6" with leavings.txt in that commit. (Test teardown-dirty-and-salvage.)
9. PASS. Scout with a dirty scratch tree and no report: exit 1 naming report.md, worktree intact. With report.md present: exit 0, worktree removed, and `git ls-remote origin` byte-identical before and after (diff exit 0), so nothing was pushed. (Test teardown-scout-report-gate.)
10. PASS. After the successful teardown, meta.json status is closed and `git worktree list --porcelain` no longer contains the worktree path. (Test teardown-closes-meta.)
11. PASS. tasks/t1/executor.json is {"adapter": "subprocess", "launchedAt": "..."} with no deadline key; with --deadline 300 the record carries a deadline exactly 300000ms after launchedAt. (Test spawn-executor-launch-record.)
12. PASS, both directions. With a lease held: spawn with TIPHYS_HOLDER_ID unset exits 1 with a single reason line naming state/orchestrator.lock and its holder, and creates no worktree; with a foreign holder id it exits 1 naming the real holder; with the id lock acquire printed it exits 0. The test additionally witnesses the expiry direction with a real 1s lease waited out. (Test spawn-holdership-both-directions.)
13. PASS, both directions, same shape for teardown: refused with no holder id (worktree intact, meta still open), refused with a foreign id, exit 0 with the matching id and meta closed. (Test teardown-holdership-both-directions.)
14. PASS. npm test exit 0, 99 tests, 0 failing, zero unaccounted (2 skips with their recorded reason); the registry check resolves all 100 mappings by name in that run and nothing previously registered disappeared.

No CI-DEFERRED criteria: every M1-P4 criterion is locally witnessable and was witnessed. The two skips in the run belong to M1-P2's doctor criteria and are re-witnessed on CI (Node 26) exactly as before; no new floor-gated assertion was added, because neither spawn nor teardown reads the node version.

## Deliberate-failure witnesses (red against the dangerous state, measured, reverted)

Each sabotage was applied to the shipped source, the guarding test run three times, and the tree restored and verified clean (`git diff --quiet`) afterwards. Every one is deterministic at 3/3; none is probabilistic.

| ID | Sabotage (the dangerous state) | Test that went red | Rate |
|---|---|---|---|
| W1 | Payload spawned detached and unwaited (a C-3 violation) | spawn-runs-payload-in-worktree ("spawn returned before the payload had exited") | 3/3 |
| W2 | baseOffline recomputed from spawn's own --offline flag | spawn-base-offline-not-recomputed | 3/3 |
| W3 | Landedness judged against the stale local default ref | teardown-squash-landed | 3/3 |
| W4 | Landedness by is-ancestor only, merge-tree removed | teardown-squash-landed | 3/3 |
| W5 | Salvage commit performed before the landed check | teardown-dirty-and-salvage | 3/3 |
| W6 | Partial destroy reported as "teardown refused: nothing was changed" | teardown-partial-destroy-not-refusal | 3/3 |
| W7 | Rollback fired on a nonzero payload exit (destroying a completed task's worktree) | spawn-turn-end-record | 3/3 |
| W8 | Holdership checked after pool create (a refusal that creates state) | spawn-holdership-both-directions ("the refusal created a worktree") | 3/3 |
| W9 | Scout path passing --delete-branch-force (silently discarding committed scout work) | teardown-scout-committed-work-preserved | 3/3 |
| W10 | A registered test renamed | registry name check reports MISSING spawn-requires-exec | 1/1 |

Where a behavior consumes another program's output, the assertions use real captured output rather than hand-written strings: the holdership tests parse `lock acquire`'s actual stdout; the baseOffline provenance test drives the verbatim concurrent-ref-update stderr captured in M1-P3 through a stub git; and the partial-failure test asserts against pool destroy's real emitted message, including the sha the branch is actually left at.

Criteria 3, 4, 5, 6, 8, 9, 12 and 13 are themselves negative witnesses and were each executed as failing commands with captured nonzero exits, listed in the walk above.

## Disposition of the four inherited obligations

1. Criterion 13's remaining clause (spawn writes meta.json carrying baseOffline, copied from the pool record): DISCHARGED. Copied, never recomputed (key decision 2), witnessed in both directions (spawn-base-offline-copied for the true direction, spawn-base-offline-not-recomputed for the false-under-retry direction), with W2 red 3/3 against the recomputing implementation. Required deviation 1 (--offline on spawn) to be reachable at all.
2. M1-P3's holder-identity transport: DISCHARGED, unchanged and not duplicated. lock acquire's printed holderId travels in TIPHYS_HOLDER_ID and is compared against the lease file read through leaseStatus; both commands share one implementation (checkHoldership); both tests consume acquire's real output.
3. Tuition T-002 (an abandoned task should be detectable): PARTIALLY DISCHARGED, exactly as far as the plan reaches, and no further. This phase ships the designed route's P4 half: the executor launch record with its optional deadline (criterion 11), and a turn-end contract whose absence can never be read as success (documented at src/task.ts and src/hooks.ts). DETECTION IS NOT IN THIS PHASE: the plan's grounding assigns deadline-based non-completion detection to the M1-P5 watcher, and states that abandonment of a task launched without a deadline is not auto-detected in M1 (recovery is manual teardown, section 4 not-proven list, PR-207). I did not improvise a detector. Worth carrying into P5: because M1's only adapter runs the payload to completion in-process, a task can only be left open-with-no-turn-end if the spawn process itself dies, which is precisely the T-002 incident shape; P5's wake condition ("task open, no turn-end, worktree dirty") therefore has a real M1 trigger and should be exercised against a killed spawn rather than a synthesized file state.
4. M1-P3's destroy contract: DISCHARGED. Teardown drives destroy and never describes a stage-3 partial failure as a stage-2 refusal (key decision 20), with a test that opens the window for real (a stub git moves the branch immediately after the worktree removal) and asserts the enumeration against the true remaining state: worktree gone, branch at the moved sha, record surviving, meta still open. W6 is red 3/3 against the flattening implementation.

## Simplicity, and what was deliberately NOT built

The standing lesson from M1-P3 (five rounds, each hardening machinery for a scenario M1 never enters) was applied as a filter. Left out on purpose, each with the reason:

- No contention retry in teardown's git calls: M1 never runs teardown concurrently with anything.
- No automatic repair of a partial destroy: the destroy already prints the exact command that finishes the job, and re-implementing that decision one layer up is how the last phase grew defects.
- No rollback after the payload has started, and no force flags on the rollback path: a cleanup that can delete real work is the defect, not the protection.
- No exec-less spawn, no daemonize flag, no background mode: PR-013 and C-3.
- No second identity mechanism, no pid, no process probing: C-2.

## Environment warnings (inherited plus new, for future phases)

1. Inherited and confirmed: local Node v22.22.2 against the ">=26" floor; EBADENGINE warnings ignored; type stripping runs every local gate; CI on Node 26 is the authority. Neither spawn nor teardown reads the node version, so no new floor-gated skip was needed.
2. Inherited and confirmed (warning 5): every commit-producing git call in the new tests carries command-scoped GIT_AUTHOR_*/GIT_COMMITTER_*, and so does teardown's own salvage commit.
3. Inherited and confirmed (warning 7): `--test-name-pattern` must precede the positional test path. Every witness run in the table above used that order.
4. New: `git merge-tree --write-tree` exits 1 for a conflicting merge and prints the conflicted tree id anyway, so exit 1 must be read as "not landed" and only other nonzero exits as "inconclusive". Verified against git 2.43 in a scratch repo before the code was written (squash-landed: exit 0, tree equal to the default head's tree; unlanded: exit 0, tree different; conflicting: exit 1).
5. New: a spawn payload inherits the CLI process's environment, so a test that sets PATH or TIPHYS_HOLDER_ID for the CLI also sets it for the payload. Tests strip TIPHYS_HOLDER_ID from the base environment so an ambient value cannot make a holdership assertion pass by accident.
6. New: the generated turn-end hook is invoked with process.execPath rather than through its `#!/usr/bin/env node` shebang, so it works under the deterministic PATHs tests build (inherited warning 6) and in a fleet home whose package.json is not this package's.
7. New: pushing a task branch to a NON-BARE scratch upstream works because the task branch is not the branch checked out there; a test that pushed the default branch instead would be refused by git. The squash-land helper merges into the upstream's checked-out main directly, which is why the criterion 7 and 8 scenarios need the upstream to be a working clone rather than a bare repo.
8. New: suite wall time is now 67.6s (was 63.4s at the end of M1-P3). The added time is real git and real subprocess work in the two new files; budget harness timeouts off 68s, not off the older figures.
