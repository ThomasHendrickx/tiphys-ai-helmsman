# M3-P8 work history: tuition flow and the mechanism index

- phase: M3-P8
- branch: claude/m3-p8-tuition-flow
- base: origin/main at ec77c7d (M3-P6 merged)
- toolchain: node v26.6.0 from the scratch prefix

Written incrementally from the first minutes; the file's mtime is the beacon.

## Dispatch-time resolutions (the plan requires these resolved here, not copied)

- **Migration-ticket ids: T-021 and T-022.** The lowest two free ids across BOTH
  `T-nnn` directories. Retirement was checked against the whole history rather
  than the tree (CLAUDE.md:643), and both commands printed nothing:

  ```
  git log --all --oneline -S'T-021' | head -3
  git log --all --oneline -- 'delivery/tuition/T-021*' 'tuition/T-021*' \
    'delivery/tuition/T-022*' 'tuition/T-022*' | head -3
  ```

- **`delivery/tuition/` at dispatch holds 20 entries**, T-001 to T-020
  (`ls delivery/tuition | wc -l` reports 20).

- **The promoted set is 13, and the selection rule is mechanical rather than
  editorial.** An entry is promoted when (a) its own header declares
  `kernel-relevant: yes`, or (b) it is the incident that paid for a mechanism
  the generated index must carry. Rule (a) selects T-001 to T-009, T-015, T-016
  and T-017 (twelve). Rule (b) adds T-018, which is the M3-P6 brief-drift
  incident and the only home for the mechanism
  `checking-a-generated-artifact-against-its-own-generator` that M3-P6's seed
  index already ships.

  **The eight NOT promoted are named rather than left implicit**: T-010 to
  T-014, T-019 and T-020 carry no `kernel-relevant` declaration in their own
  headers, and T-018 was the only one of the eight that rule (b) reached.
  Promoting the others would be this implementer asserting a kernel consequence
  their authors did not, so they are left for a later promotion round. Measured:

  ```
  grep -L -i 'kernel-relevant' delivery/tuition/T-*.md
  ```

  Feed total 15 = 13 promoted + 2 tickets, which is criterion 1's relation.

## What was built

`schemas/tuition.schema.json` (create), `schemas/mechanism-index.schema.json`
(EDIT: `machine-readable-form` added), `src/tuition.ts` and
`src/commands/tuition.ts` (`add`, `list`, `index [--check]`), two derived checks
in `src/checks.ts`, the `retention` doctor check in `src/commands/doctor.ts`,
the `tuition` type row in `src/commands/validate.ts`, the command registration
in `src/cli.ts`, the feed and the generated index in `tuition/`, and
`MECHANISMS.md` deleted with its verbatim `037477e` capture checked in at
test/fixtures/mechanisms-interim.md:1.

`package.json` needed NO edit: `tuition` is already in its `files` array from
M1-P1, so the declared path is listed and untouched (the scope gate reports it
as such, quoted below).

## Criteria walk

Every command below was run on node v26.6.0 in this worktree.

1. **Every promoted entry plus the two tickets validate.** `for f in
   tuition/T-*.yaml; do node bin/tiphys.ts validate --type auto --context .
   "$f"; done` exits 0 for all fifteen, and `validate --type mechanism-index
   --context . tuition/mechanism-index.yaml` exits 0. `--context` is required
   because this phase registers two context-requiring derived checks for these
   types; without it the command prints `SKIPPED <check> no context` and exits
   1, which is M3-P1's rule that a cross-document check must never pass by not
   running. The count relation is asserted at run time by the registered test,
   never as a literal.
2. **Kind A rejections, each witnessed by removing and restoring the guarding
   keyword.** (a) `kernel-relevant: true` with `structural-consequence: []`
   gives `INVALID #/structural-consequence array has 0 items, fewer than the
   required minimum 1`; removing that `minItems` accepts it. A SECOND
   structurally different member, the field ABSENT rather than empty, is caught
   by the `then` branch's `required` and defanged by removing that. (b) a
   `mechanisms[]` entry with `evidence: []` gives `INVALID
   #/mechanisms/0/evidence ...`; removing that `minItems` accepts it.
   Note: deleting `then` WHOLESALE is not a defang. It makes the schema fail
   compilation ("schema is refused by this validator's strict policy"), so the
   document is still rejected, for a different reason. Measured, and the
   witness targets the keyword instead.
3. **Kind B rejections, each witnessed by deregistering and restoring the
   check.** (a) an `applied` consequence targeting `roles/there-is-no-such-brief.md`
   gives `INVALID #/structural-consequence/0/target ... (check:
   tuition-target-exists)`. (b) evidence naming a non-existent file gives
   `INVALID #/mechanisms/0/evidence/0 ... (check:
   mechanism-rule-evidence-resolves)`. Both are green again with the check
   deregistered and red again once restored, and the shipped entries are the
   control.
4. **`tuition index --check`** exits 0 against the committed index
   (`15 mechanism(s) projected from 15 entr(ies); the committed index
   matches`). Adding a `mechanisms[]` entry without regenerating exits nonzero
   with `DRIFT mechanism rebasing-a-branch-under-review is declared by tuition
   entry T-004 and is missing from the committed index`; regenerating returns
   0. The converse (a row left in the index that no entry declares) and a hand
   edit of a rule are both reported too, each naming the mechanism and the
   entry.
   4b. The `destructive-git-operation` row's `machine-readable-form` names
   `gates.manifest.json` and `destructiveCommands`, and both resolve. Renaming
   the key in a FIXTURE manifest (never the real one) makes the check name the
   key; restoring it returns green.
   4c. All twelve interim mechanisms resolve in the generated index, derived
   from the capture rather than from a list in the test. Removing T-005's
   `mechanisms` block and regenerating makes the same derivation name exactly
   `claim-file-mutual-exclusion-by-o-excl`; restoring returns it.
   4d. `MECHANISMS.md` is absent, `tuition/README.md` names the generated index,
   and the shipped document families are clean. **The scope of "shipped
   artifact" is resolved by measurement, which the orchestrator's record at
   delivery/plan/m3-p8-declaration-gap.md:1 asked this phase to do**: the
   criterion's own enumeration is "briefs, `AGENTS.md`, checklists, schemas", so
   the test walks `roles/`, `schemas/`, `checklists/`, `tuition/`, `templates/`
   and `AGENTS.md`, which is the DIRECTORY reading. Under that reading the tree
   is clean today and the criterion is satisfiable as declared. Under the other
   reading (any `*.schema.json`) it is not, because
   `src/gates/schemas/gate-manifest.schema.json` carries the string, and that
   file is not on this phase's declaration. See the residues section: fifteen
   tracked files outside `delivery/` still name the deleted file and are listed
   there with the measurement.
5. The index carries `claim-file-mutual-exclusion-by-o-excl` with T-005's
   loud-failure rule, its cited files resolve, and deleting the entry's
   `mechanisms` block removes the row while restoring brings it back with byte
   identical rule text.
6. `brief compose --role implementer --phase templates/plan.example.yaml
   --phase-id M9-P1` exits 0 and names `tuition/mechanism-index.yaml`, which
   resolves to the generated file (`tuition index --check` exits 0 on it). The
   test also asserts the FRONTMATTER `mandated-reading` list carries the path,
   because the brief's prose names it too and a text match alone stays green
   with the frontmatter entry deleted (measured; see the shadowed-guard note).
7. `tuition list --kernel-relevant` prints one `T-nnn <date> targets=N` line per
   kernel-relevant entry and omits a staged non-kernel-relevant one. `tuition
   add` on an invalid entry exits 1, prints `is not a valid tuition entry` plus
   the INVALID line, and leaves the directory listing byte-identical; a second
   add of an existing id is refused and leaves the first file unchanged.
8. `doctor` in a fleet whose charter declares three retention paths that exist
   and are tracked prints `CHECK retention PASS 3 declared retention path(s)
   present and tracked`. Adding one path to `.gitignore` gives `CHECK retention
   FAIL ... is git-ignored and will not survive a clone` and exit 1; removing
   the ignore returns PASS. Deleting a declared path gives `... does not exist`.
   A fleet whose charter declares no retention is `CHECK retention WARN` under
   the generic profile and `CHECK retention FAIL ... (required for profile
   full)` under `--for full`, which is the vacuous-pass end the hazard table
   asked to be checked in both directions.
9. The registered duplicate-id test scans both directories and asserts T-021 and
   T-022 are claimed by the feed and not by the delivering log.
10. Suite and clause map: see below. The `clause-map` gate is green over this
    phase's three new rows (R-070, R-091, R-098) and every earlier mapping.

## Witnesses

Sixteen specs under `witness/`, thirty-one members, every one measured RED
against its named tests and GREEN with the mutation restored. The probe applies
each member in place, runs the named tests with `--test-name-pattern` before the
positional path, and restores the original bytes from memory (T-013: a killed
mutation harness leaves the mutant installed, so the restore is in a `finally`
and `git status` was checked clean afterwards). Result lines, one per member:

```
RED  doctor-retention-check member 0/1/2 (src/commands/doctor.ts)
RED  implementer-brief-reads-generated-index member 0 (roles/implementer.md)
RED  implementer-brief-reads-generated-index member 1 (tuition/mechanism-index.yaml)
RED  interim-index-removed-and-redirected member 0/1 (tuition/README.md)
RED  mechanism-evidence-resolves member 0/1 (src/checks.ts)
RED  mechanism-index-claim-file-rule member 0 (tuition/mechanism-index.yaml)
RED  mechanism-index-claim-file-rule member 1 (tuition/T-005.yaml)
RED  mechanism-index-covers-interim-rows member 0/1 (src/tuition.ts)
RED  mechanism-index-projection-drift member 0/1 (src/tuition.ts)
RED  mechanism-machine-readable-form-resolves member 0/1 (src/checks.ts)
RED  tuition-add-refuses-non-regular-entry-path member 0 (src/tuition.ts)
RED  tuition-add-rejects-invalid member 0/1 (src/commands/tuition.ts)
RED  tuition-applied-target-exists member 0/1 (src/checks.ts)
RED  tuition-conditional-consequence member 0/1 (schemas/tuition.schema.json)
RED  tuition-entry-validates member 0 (src/commands/validate.ts)
RED  tuition-entry-validates member 1 (schemas/tuition.schema.json)
RED  tuition-ids-unique-across-directories member 0/1 (tuition/T-021.yaml, T-022.yaml)
RED  tuition-list-filters-kernel-relevant member 0/1 (src/commands/tuition.ts)
RED  tuition-mechanism-evidence-required member 0 (schemas/tuition.schema.json)
```

**The first probe run had SIX green members, and every one was a real defect in
the test or the code rather than a bad mutation.** They are recorded because
each is a finding this phase found against itself:

| member | why it stayed green | what changed |
|---|---|---|
| `tuition-list-filters-kernel-relevant` (both) | every entry in the shipped feed is kernel-relevant, so the filter had no negative instance to remove. Green, registered and worthless, which is T-003's rule | the test stages a feed carrying one `kernel-relevant: false` entry |
| `tuition-add-refuses-non-regular-entry-path` | the decode of an undefined body failed right behind the type guard, so removing the guard still exited 1 naming the path | the test asserts the message names the observed entry type ("is a named pipe, not a regular file, so it was not opened") |
| `tuition-add-rejects-invalid` (the validate-first member) | with the guard removed the command CRASHED on `loaded.entry.id`, which also exits nonzero and also writes nothing | the test asserts the refusal text and the INVALID line, so a crash is distinguishable from a refusal |
| `implementer-brief-reads-generated-index` | the brief's PROSE names the index path, so the text match stayed green with the frontmatter entry deleted | the test asserts the frontmatter `mandated-reading` list |
| `mechanism-index-projection-drift` (the command member) | the byte comparison behind the structural diff caught the same drift, so removing either left the other rejecting it | the member was DROPPED; `src/commands/tuition.ts` is covered by another spec's member |

Two of those five are the T-018 shape (two checks catching one input make each
other unwitnessable) and one of them was fixed in the CODE rather than in the
test: `tuition add` used to read the entry path twice, once through `loadEntry`
and once for the bytes to write, so the type refusal was duplicated. `loadEntry`
now returns the bytes with the entry, leaving one read and one guard.

## Suite

Node v26.6.0 (scratch prefix, `node --version` checked in the shell that ran
it), `dist/` BUILT, invocation `npm test` (which is
`node --test "test/**/*.test.ts"`, so it excludes the tracked `sandbox/`
fixture that a bare `node --test` picks up):

SUITE_SENTENCE_PLACEHOLDER

`npm run build` exits 0 and `git status --porcelain` is empty afterwards;
`dist/` is gitignored and not committed. `node scripts/check-authored-bytes.mjs`
exits 0 with the tree staged.

## Gates run locally

```
node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full \
  --only clause-map --phase m3-p8 --evidence <scratch> --base origin/main --head HEAD
gates: declared 1 applicable 1 verdict 1 green 1 red 0 not-applicable 0 error 0 vacuous 0
```

The `scope` gate is RED, for exactly two files, and that is the one thing this
phase cannot fix from inside itself:

```
red
touched path(s) outside the declared scope: test/implementer-brief.test.ts,
test/liveness.test.ts (declaration delivery/plan/phase-declarations/m3-p8.json
at merge base ec77c7d..., sha256 16609b78...) (1 declared path(s) not touched:
package.json)
```

**Both edits are forced by the phase and neither is avoidable.**

- `test/implementer-brief.test.ts` READS `MECHANISMS.md` from the repository
  root (M3-P6's superset test). This phase deletes that file, so the test throws
  unless it is repointed at the capture the criterion itself required. The
  capture's mechanism-name column was compared against the deleted file's and is
  identical, so the test's subject is unchanged. The same test also gains
  `--context`, because this phase registers a context-requiring check for the
  `mechanism-index` type.
- `test/liveness.test.ts` pinned doctor's check count at the literal `8` in two
  assertions. Doctor's check list is append-only across phases and this phase
  adds the ninth, which is CLAUDE.md convention 5's rule about registries
  applied to this one. The literal is replaced by a count derived from the
  command's own list, so the property (doctor printed its WHOLE diagnosis) is
  unchanged.

The declaration therefore needs those two paths added before this branch can
pass the scope gate. Recorded rather than worked around.

## Residues, stated rather than left to be discovered

- **Fifteen tracked files outside `delivery/` still name `MECHANISMS.md`**, none
  of them on this phase's declaration. Measured after the deletion:

  ```
  git ls-files | grep -v '^delivery/' | xargs -d '\n' grep -c 'MECHANISMS\.md' | grep -v ':0$'
  src/commands/gates.ts:1      src/gates/credentials.ts:1   src/gates/pin.ts:1
  src/gates/result.ts:1        src/gates/run.ts:6           src/gates/scope.ts:1
  src/gates/suite.ts:2         src/witness/run.ts:2         test/credentials-gate.test.ts:1
  test/exit-test-local.test.ts:1  test/gates.test.ts:3      test/m2-exit-test.test.ts:1
  src/gates/schemas/gate-manifest.schema.json:1
  witness/captures/git-name-status-real.txt:1  witness/witness-tap-reporter-pin.json:1
  ```

  Every one is a COMMENT or a provenance note citing a mechanism row by name;
  each of those rows survives in `tuition/mechanism-index.yaml` under the same
  name, so a reader can still find the rule. The rule applied here was
  mechanical: **references in files this phase owns were repointed**
  (`src/validate.ts` now names the index row `reading-a-path-whose-type-is-not-established`),
  and the rest are listed here rather than silently half-fixed.
  `witness/captures/git-name-status-real.txt` is REAL captured `git` output and
  must not be edited at all. The two files added by this phase that carry the
  string do so deliberately: the redirect test and its witness spec.
- **`tuition-target-exists` checks that an `applied` target EXISTS, never that
  it contains the change claimed.** The plan's hazard table assigns the second
  half to review; the check's header says so at the site.
- **`mechanism-rule-evidence-resolves` says nothing about prose-only
  evidence.** "M1-P5 round 4, verified pre-existing against a pristine build"
  names no path, so nothing is resolvable; requiring a path would redden real
  evidence that is a measurement rather than a document. The registered test
  asserts the boundary in both directions rather than describing it.
- **The retention check resolves a declared path against the fleet root and
  against `projects/<charter identity name>` when that clone exists.** A
  project whose clone lives elsewhere is not covered, and the check reports FAIL
  for it rather than passing, which is the safe direction.
- **`tiphys tuition add` writes into `<cwd>/tuition` (or `--into`).** It does not
  consult the charter's `retention.tuition` path; coupling the writer to the
  charter is not in this phase's steps and was not improvised.
- **Promotion into a kernel pull request is not a subcommand**, per step 6: the
  kernel never opens pull requests.

## Claim grep

```
grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m3-p8.md
tr '\n' ' ' < delivery/work-history/m3-p8.md | grep -oEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to'
```

Both were run. Every hit is either a statement with its measurement beside it in
the same paragraph (the scope-gate output, the six green members, the fifteen
files) or a rule quoted from `CLAUDE.md` or the plan. The one claim that is mine
and universal, "neither edit is avoidable", is settled by the two mechanisms
named directly under it: a deleted file a merged test reads, and a literal count
of an append-only list.

## Fix round 1

CI at head `151410f` reported `declared 12 applicable 8 verdict 8 green 5 red 3`:
`suite`, `scope` and `red-witness`. All three findings sets were re-derived from
the gate runner at that head rather than read out of the dispatch summary; the
evidence directory is the runner's own, and the twelve `suite` findings came
from `counts.json`, which carries all of them where `result.json` truncates at
ten.

### `suite`: the mechanism, and it is not the twelve rows

**The mechanism is that a behaviour's registered DESCRIPTION is matched against
reported test names by EXACT SET MEMBERSHIP, so a description that paraphrases
its test resolves to nothing.** src/gates/suite.ts:1040 builds a set of reported
test names and looks the description up in it; there is no fuzzy match and no
substring arm. The suite itself was never broken: the same run reported 646
tests, 646 pass, 0 fail, 0 skipped. Sixteen behaviours were registered and four
resolved, those four being the ones whose description happened to be typed
identically to the test.

Derivation, the whole set rather than the row CI printed first:

```
node -e '...JSON.parse(counts.json).findings...'   # 12 of 12, none omitted
```

What each became. Eleven are the description corrected to the test's exact
reported name (the test was already the better sentence, and renaming eleven
passing tests to match a paraphrase would have been the wrong direction):

| behaviour | became |
|---|---|
| `tuition-kernel-relevant-requires-consequence` | "... rejected naming the field, and removing each guarding keyword accepts it" |
| `tuition-applied-target-must-exist` | "... and is accepted with the check deregistered" |
| `mechanism-evidence-resolves` | "... and is accepted with the check deregistered" |
| `mechanism-machine-readable-form-resolves` | "a machine-readable form naming a renamed key ... and restoring the key returns exit 0" |
| `mechanism-index-projection-drift` | "adding a mechanism to a tuition entry without regenerating ... and regenerating returns exit 0" |
| `mechanism-index-covers-every-interim-row` | "... and removing one from its entry makes the check fail naming it" |
| `mechanism-index-contains-claim-file-rule` | "... rather than from a hand-written file" |
| `interim-index-removed-and-redirected` | "... and no shipped document family mentions the deleted file" |
| `implementer-brief-reads-generated-index` | "... in its mandated reading and the path resolves to the generated file" |
| `tuition-mechanism-rule-requires-evidence` | renamed on BOTH sides, see below |
| `tuition-entry-validates` | renamed on BOTH sides, see below |

Two did not become a copied name.

**`tuition-entry-validates` carried a COUNT over an append-only feed**, in the
description ("the feed count is the promoted set plus the two tickets") and in
the test itself, so copying the name would have registered the antipattern
rather than fixed it. `tuition/` is append-only across every future phase, which
makes any number written about it a claim about phases that have not happened,
false the moment the next one appends (CLAUDE.md convention 5). Two counts were
there, and neither is a literal-versus-relation distinction that rescues it:

- `assert.equal(tickets.length, 2)`, a literal.
- `assert.ok(promoted.length >= declared.length)`, a relation between two
  directory sizes, which is a claim about future phases in the same way and is
  additionally WEAK: it is satisfied by a feed carrying the right number of
  entirely different entries.

Both are replaced by id resolution at test/tuition.test.ts:1. The tickets are
asserted BY ID (`T-021`, `T-022` present, each with a stage naming it a
migration ticket), and the delivering log's kernel-relevant entries are asserted
to resolve BY ID in the feed. The replacement is strictly stronger than the
relation it replaces. Its cost is stated rather than hidden: a future phase that
records a kernel-relevant entry in `delivery/tuition/` and does not promote it
reddens this test. That is R-091's duty and the reason it is checked here, but
it is a real obligation on later phases and is listed again under residues.

**`tuition-mechanism-rule-requires-evidence` was renamed because its witness
needed a second member and the test could not supply one** (see `red-witness`
below). The test now covers the absent-key case as well as the empty-array one,
so the name changed and the description follows it.

**A seventeenth behaviour was registered.** `doctor-retention-check` was one id
over TWO tests, and one description cannot resolve to two names. The second test
got its own id, `doctor-retention-absent-path-and-full-profile`. Nothing was
merged or dropped to make the arithmetic work.

Result at head: `suite green ... reported 646 test(s) from 40 file(s) (pass 646,
fail 0, skipped 0, todo 0, did-not-run 0); ... 644 behaviour(s) resolve`.

### `scope`: both files are forced, and the fix is not on this branch

`touched path(s) outside the declared scope: test/implementer-brief.test.ts,
test/liveness.test.ts`. Reverting is the default and neither revert survived
measurement. Each file was reverted to `origin/main` on its own, the affected
tests run, and the file restored:

- `test/liveness.test.ts` reverted, `node --test --test-name-pattern "named pipe
  at the (beacon|lease file)"`: **FAIL, `9 !== 8`**, with doctor's nine CHECK
  lines printed including `CHECK retention WARN`. The literal `8` is a count
  over doctor's append-only check list, and this phase is the append (R-098).
  This is the case CLAUDE.md convention 5 names outright: a phase that extends a
  registry may have to edit the test that over-asserts on it.
- `test/implementer-brief.test.ts` reverted, `node --test --test-name-pattern
  "seed mechanism index validates"`: **FAIL, `SKIPPED
  mechanism-rule-evidence-resolves no context`, `1 !== 0`**. The M3-P6 test
  invokes `validate --type mechanism-index` with no `--context`, and this phase
  registers a context-requiring derived check for that type; a context-requiring
  check with no context skips and exits nonzero, which is M3-P1's rule. The same
  file also reads `MECHANISMS.md`, which this phase deletes (`git diff
  --name-status` reports `R079 MECHANISMS.md test/fixtures/mechanisms-interim.md`).

**The declaration was NOT edited, and editing it on this branch would not have
worked anyway.** The gate loads the declaration with `git show <mergeBase>:<path>`
(src/gates/scope.ts:43), so the amendment has to be on `main` before it counts.
This is handed back as an orchestrator action: add `test/liveness.test.ts` and
`test/implementer-brief.test.ts` to `declaredExtras` in
`delivery/plan/phase-declarations/m3-p8.json` on `main`, with the two
measurements above as the reason.

`package.json` is reported as declared-but-untouched. **It is not a missing
deliverable.** `main` already ships `tuition` in `package.json`'s `files` array,
measured with `git show origin/main:package.json`, so the shipped feed is
packaged and there was nothing for this phase to add. The untouched note is
informational in the gate: only `violations` set the red status
(src/gates/scope.ts:781).

### `red-witness`: four witnesses, three distinct causes

0. **`doctor-retention-check` had a SECOND defect that the first gate run could
   not report, because rule (f) refused the witness before any member ran.** With
   the capture added, the witness reached execution and every member came back
   `no named test reaches this arm ... stayed green: <the other test>`. The
   mechanism is src/witness/run.ts:886: a member counts as red only when
   `exitCode !== 0 && failed.length === tests.length`, so **EVERY named test must
   fail under EVERY member.** The witness named two tests and each member reaches
   one arm, so no member could ever satisfy it. Fixed by splitting one witness
   per test: `doctor-retention-check` keeps the ignored-path test with the two
   members that reach it (the branch that acts on the classification, and
   `isGitIgnored` itself so the real exit code is never read), and
   `doctor-retention-absent-path-and-full-profile` takes the absent-path and
   `full`-profile members. Both carry the capture, because both mutate the
   spawning file. Every other witness on this branch was then checked for the
   same latency: all seventeen name exactly one test.
1. **`doctor-retention-check`, rule (f).** The phase diff touches
   src/commands/doctor.ts:1, the spawn/parse derivation matched it, so a capture
   is required and the witness had none. The consumed program is `git
   check-ignore -q -- <path>`, and because `-q` suppresses stdout its EXIT CODE
   is the entire contract, which is exactly the case where a hand-written
   expectation is indistinguishable from a fabricated one. Real output was
   captured (git 2.43.0, scratch repository ignoring `notes/evidence/`) into
   witness/captures/doctor-git-check-ignore-resolution.txt:1: ignored exits 0,
   unignored exits 1, outside a repository exits 128. The first retention test
   now reads that capture and asserts a live scratch repository reproduces the
   first two codes before exercising the check.
2. **`implementer-brief-reads-generated-index`, rule (d), both members.** Member
   0 mutated `roles/implementer.md`, which M3-P6 merged and this phase does not
   touch; member 1 mutated `tuition/mechanism-index.yaml` at its header, which is
   in the file but not inside a changed hunk. Both were replaced with mutations
   of the generated index inside changed hunks, and they are the two DIRECTIONS
   of the drift class rather than two spellings of one: an entire projected row
   deleted (omission) and a line the projection never emits inserted (addition).
3. **`tuition-add-refuses-non-regular-entry-path` and
   `tuition-mechanism-evidence-required`, rule (g), single-member collapse.**
   Both declared one member while being text-asserting. Neither was fixed by
   deleting the witness or by weakening the declared class.

   For the add path the class is the T-018 shape this phase already recorded:
   the second member keeps the type guard but replaces its reason with a generic
   string, so the path is CLASSIFIED but the observed type is not named. That is
   structurally different from member 0, which removes the guard entirely, and
   it is the property the test's own comment says is the witnessable one.

   For the evidence rule there was no second member available in the test as
   written: `required` catches an ABSENT evidence key and `minItems` catches an
   EMPTY one, and the test only exercised the empty case, so removing `required`
   left the test green. Two guards catching one input make each other
   unwitnessable, which is the same T-018 shape one level down. **The test was
   strengthened rather than the witness weakened**: it now asserts the absent
   case too, and that each guard removed on its own accepts the entry. The
   second member removes `evidence` from the mechanisms item's `required` array.

**Every declared member of every phase witness was measured red before the gate
was re-run**: seventeen witnesses, thirty-four members. Each mutation was applied
to the working tree, that witness's named tests run, and the file restored from
`HEAD` (revert exit 0 recorded per member, tree verified clean afterwards). All
thirty-four reported `1/1 named test(s) failed -> RED`.

**The first version of this probe was itself wrong, in the direction that
matters.** It ran all of a witness's named tests under one pattern and accepted
"the run exited nonzero", which reported the three original doctor members as
red when the harness reports them green. A probe whose condition does not test
the property that matters is green and worthless, which is the red-witness rule
one level up and the same shape T-008's postscript records. The probe now
computes the harness's own predicate from the TAP stream, per named test.

### What this round did NOT cover

- **`scope` is still red at this head and cannot go green from this branch.**
  The declaration lives at the merge base. Stated as an action, not as a
  finding I resolved.
- **The eleven copied descriptions were not re-derived from the criteria.** They
  are the test names as written by the previous round; this round checked that
  they resolve, not that each is the best sentence for its criterion.
- **`git check-ignore` exit 1 and exit 128 are conflated by the retention
  check.** Both are nonzero and both are read as "not ignored", which is correct
  for 1 and merely lucky for 128: a declared retention path in a directory that
  is no repository at all is reported as tracked. The capture records the
  distinction, the check does not act on it, and no test in this round forces
  that arm.
- **The by-id promotion assertion places a duty on later phases**, as described
  above. It was chosen over the size relation deliberately, and a later phase
  finding it inconvenient should promote the entry rather than loosen the test.
- **The probe that measured the thirty-four members ran in the working tree**,
  not in the gate's scratch clone, so it establishes that each mutation reddens
  its named tests and not that the harness reproduces that under its own clone.
  The gate run is the authority on the second half.
- **A red-witness run was lost to the environment and is reported as lost, not
  as evidence.** One run returned `spawnSync git ENOENT` for eighteen stored
  witnesses after `/tmp/tiphys-witness-<id>` vanished mid-run; the shell had
  `/usr/bin/git` on PATH before and after, so the cause was the scratch root
  disappearing, not the toolchain. The re-run sets `TMPDIR` under the session
  scratchpad. What that lost run DID establish, because it happened before the
  corruption, is the multi-test defect in item 0.

### Gate results at head `6c0cae9`

Run with the scratch-prefix node v26.6.0, npm 11.18.0, `TMPDIR` under the
session scratchpad.

| gate | status | units |
|---|---|---|
| `suite` | **green** | 646 tests reported |
| `red-witness` | **green** | 54 witnesses evaluated |
| `scope` | **red** | 54 changed paths audited |

- `suite`: `reported 646 test(s) from 40 file(s) (pass 646, fail 0, skipped 0,
  todo 0, did-not-run 0); discovered 40 file(s) walking test for .test.ts; 644
  behavior(s) resolve; merge base ec77c7d48698`.
- `red-witness`: `54 witness(es) evaluated (17 own, 37 stored re-evaluated in
  350674ms); every witness red against every declared dangerous state and green
  at head`.
- `scope`: unchanged and unfixable from this branch, see above.

The rest of the bundle was RUN rather than assumed unaffected, so this is a
per-gate measurement and not a bundle-level green being read as a gate-level one
(T-009, one scope smaller):

| gate | status | units |
|---|---|---|
| `manifest-self-check` | green | 8 schema documents validated |
| `coverage` | green | 115 finding ids checked |
| `credential-scrub` | green | 7 credential sources probed |
| `clause-map` | green | 50 clause-map rows checked |
| `agent-rules-drift` | green | 18 rendered gate rows compared |
| `brief-drift` | green | 15 generated brief gate rows compared |
| `citations` | not-applicable | 0 citations resolved |
| `credential-token` | not-applicable | 0 tokens probed |
| `deploy` | not-applicable | 0 release verifications satisfied |
| `migrations` | not-applicable | 0 migrations compared |

So at head `6c0cae9` every gate this branch can settle is green or legitimately
not-applicable, and `scope` is the single red, waiting on a `main`-side
declaration amendment. These were run LOCALLY on the scratch toolchain; the
authority remains the CI `pull_request` run on this head, and the post-merge
`push` run on the new `main` tip after that (T-009 rule 1), neither of which this
round can observe.

`citations` is not-applicable on this diff (`no changed path under the
configured documents globs (53 changed path(s) total)`), so this document's
`path:line` citations were checked by hand instead: each was read back with
`sed -n '<line>p'` and one was WRONG, `src/witness/run.ts:888` pointing two
lines past the `red:` predicate it claimed. It resolved silently because it was
in range, which is the failure mode CLAUDE.md's rule 3b names as the dangerous
one. Corrected to src/witness/run.ts:886.

**Suite sentence, with all three axes named.** Toolchain node v26.6.0 (scratch
prefix, npm 11.18.0, above the `>=26` floor); build state `dist/` PRESENT
(`npm run build` exit 0, `git status --porcelain` empty afterwards); invocation
`npm test`: **646 tests, 646 pass, 0 fail, 0 SKIPPED, exit 0.**

Both invocations were measured at that head, same toolchain and same build
state, because a bare number is what starts an investigation here:

| invocation | tests | pass | fail | SKIPPED | exit |
|---|---|---|---|---|---|
| `npm test` | 646 | 646 | 0 | **0** | 0 |
| bare `node --test` from the repository root | **648** | 648 | 0 | **0** | 0 |

The two-test gap is the one CLAUDE.md standing warning 12 already accounts for:
`package.json`'s test script is `node --test "test/**/*.test.ts"`, which excludes
the tracked sandbox fixture `sandbox/test/greet.test.js`, and the `suite` GATE runs
that script. 646 is what CI and the gate mean; 648 is what gate-list step 3
literally asks for. The gap is explained here rather than averaged away, and it
matches the recorded difference exactly rather than being a new discrepancy.

### Claim grep, fix round 1

```
grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m3-p8.md
tr '\n' ' ' < delivery/work-history/m3-p8.md | grep -oEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to'
```

Both were run against this section, the second because the first is line-based
and this prose is hard-wrapped. The wrap-insensitive form found no hit the
line-based one missed. Every hit in this section carries its measurement in the
same paragraph: the two revert runs and their exact assertion failures, the
eleven-member probe table, the `git show origin/main:package.json` read, and the
`git check-ignore` capture. The one universal that is mine, that `scope` cannot
go green from this branch, is settled by src/gates/scope.ts:43, which reads the
declaration from the merge base.

## Fix round 2 (final round under DR-0027)

One blocking finding: CI step 12, the M1 exit test in local mode, fails at step
A2 because `tiphys doctor --for full` on a freshly `tiphys init`ed fleet reports
`CHECK retention FAIL no charter in <fleet>/charter declares retention paths`.

### The mechanism, not the instance

The finding is "a fresh fleet FAILs". The MECHANISM is that the check had ONE
condition id for TWO different states, and the profile table can only promote or
not promote an id, so the two states had to share a verdict:

- state A, no charter document exists at all;
- state B, a charter EXISTS and declares no `retention` paths.

Round 1 gave both `retention-undeclared` and promoted it under `full`. State B is
the SC-011 vacuity the plan's hazard-to-criterion row names verbatim, "a charter
with NO retention path", and it should be promoted. State A is a fleet before
realization, and promoting it turns the first three commands a new user runs
(install, `tiphys init`, `tiphys doctor --for full`) into a failure on a healthy
fleet.

### Which of the three readings is true: NONE cleanly, and reading 1 is refuted by measurement

Reading 1, "init should seed the charter's retention paths", is the one I most
wanted to be true and it is false. Measured on this branch:

```
$ node bin/tiphys.ts init fleet
$ find fleet/charter -mindepth 0 -printf '%y %p\n'
d fleet/charter
f fleet/charter/.gitkeep
```

`tiphys init` writes NO charter document, only a keep file, so there is no
charter for init to seed retention paths INTO. Three further facts, each
citable rather than argued:

- Charter authorship is an owner duty, not a command's:
  delivery/intake/orchestrated-delivery-v1.md:224 lists "charter authorship"
  among the owner's standing duties.
- The charter schema's required list is eleven fields
  (schemas/charter.schema.json:8), including `identity.repo`, `product-intent`,
  `irreversible-decisions` and `release-verification`. Every one is a project
  fact init does not hold at `tiphys init <dir>` time; a seeded charter would be
  invented content.
- `src/commands/init.ts` is NOT on this phase's files-to-touch list
  (delivery/plan/phase-declarations/m3-p8.json:4), so the edit is out of scope as
  well as incoherent.

Reading 3, "the exit test's fixture is wrong", is also false, and I did not edit
`scripts/m1-exit-test.sh`. Its A2 expectation is correct for what `--for full`
meant before this phase: it provisions a real `file://` remote precisely so
`remote-missing` passes honestly (scripts/m1-exit-test.sh:35), and the two
conditions M1 promoted under `full`, `gh-missing` and `remote-missing`, are both
TOOLING readiness. Round 1 silently widened `--for full` from "this environment
can run full mode" to "a project has been realized in this fleet". Plan step 7
authorized only "promoting `retention` to FAIL under the `full` profile", not the
redefinition.

Reading 2 is the closest, but NOT in the form "demote the FAIL to a WARN". The
plan's own hazard row, delivery/plan/kernel-plan-m3.md:4042, already states the
acceptable outcomes: a charter with no retention path must "report FAIL or
**not-applicable-with-a-reason**, never a silent pass". So the answer is to stop
conflating the two states and give state A the not-applicable disposition the
plan already permits, while state B keeps the FAIL it earned.

### What changed

`src/commands/doctor.ts` only. Three edits:

1. A new condition id `retention-not-applicable` for state A, returned when
   `charter/` holds no `.yaml`/`.yml` candidate at all, and when `charter/` is
   absent (the `layout` check already FAILs on that, src/fleet.ts:61).
2. `retention-undeclared` retained, unchanged, for state B, and additionally for
   a new third case the split exposed: YAML present in `charter/` but no document
   carrying `kind: charter`. That is a misconfigured fleet, not an unrealized
   one, so it stays promoted.
3. `PROFILES.full` is BYTE-IDENTICAL to round 1's:
   `["gh-missing", "remote-missing", "retention-undeclared"]`. The new id is
   deliberately absent from it, with the reason in a comment beside the table.

The three states, measured on the built branch:

| fleet state | generic | `--for full` |
|---|---|---|
| `charter/` holds only `.gitkeep` | `WARN ... retention is not applicable` | `WARN` (unchanged, not promoted) |
| `charter/notes.yaml`, no `kind: charter` | `WARN ... none with kind: charter` | `FAIL ... (required for profile full)` |
| `charter/charter.yaml`, `kind: charter`, no `retention:` | `WARN ... declares no retention paths` | `FAIL ... (required for profile full)` |

In none of the three rows measured above does the check print PASS, so the two
states a reader might confuse (nothing declared, everything declared and
present) do not print the same word in any state this round measured, which is
what the check's header promised and what SC-011 is about.

### What this costs, stated in the same paragraph

`tiphys doctor --for full` no longer exits nonzero on a fleet whose `charter/`
holds no charter document. So the profile no longer asserts "a project has been
realized here", and NOTHING ELSE in the kernel at M3 asserts it either: I looked
for a charter-presence check in `runChecks` and there is none, the `layout` check
tests only that the six `FLEET_DIRS` exist. That is a real gap and I am leaving
it rather than inventing a check the plan does not name. The narrower thing the
gap does NOT cover: a charter that exists and omits the duty, and a `charter/`
holding YAML that is not a charter, are both still caught and still promoted.

### The witness

New registered behaviour `doctor-retention-not-applicable-without-a-charter`
(test/behaviors.json, by name, no count), test
`doctor reports retention not applicable, never FAIL under --for full, in a fleet
that tiphys init just created`, witness spec
`witness/doctor-retention-not-applicable-without-a-charter.json`, THREE
structurally different members, one per way the fix can be undone: fold the new
condition id back into the promoted one; promote the new id in the profile table
instead (different file region, same outcome); turn the not-applicable verdict
into a silent PASS (the SC-011 shape the split must not reintroduce). Rule (f)
capture: the members mutate `src/commands/doctor.ts`, which spawns
`git check-ignore -q`, so the existing capture is cited, and the citation is
load-bearing rather than ceremonial, because the test asserts that none of the
three verdicts downstream of the git-consulting loop appears in the
not-applicable line.

The existing test for criterion 8's promotion was RESTRENGTHENED, not weakened.
It used a bare fleet (state A) to witness the `full` promotion; it now writes a
charter that exists and omits `retention` (state B), which is the state the
plan's hazard row names and the only one an omission can actually reach.

RED AGAINST THE DANGEROUS STATE, at the level the finding was reported. The exact
CI failure was reproduced by restoring `src/commands/doctor.ts`,
`test/doctor.test.ts` and `test/behaviors.json` to 4bfa540 and removing the new
witness, with a stub `gh` on PATH so the harness takes the gh-PRESENT arm CI
takes (`gh` is absent in this container, so the default local run takes the other
arm, and T-009 says both arms need a witness):

```
m1-exit-test: A2 ok (tiphys init a fresh fleet home)
...
CHECK retention FAIL no charter in /tmp/tiphys-m1-exit-gUleCS/fleet/charter declares retention paths (required for profile full)
m1-exit-test: FAILED: step A2 (tiphys doctor --for full (gh present)): expected exit zero, got 1
```

Character for character the failure in the dispatch. Step A1 (`kernel npm test`)
passed in that same run, so the reproduction isolates A2 rather than aborting
earlier. Restoring the fix returns the tree byte-identical (`diff -q` against a
pre-revert copy, exit 0) and `git status --short` empty after `npm run build`.

### Both M1-exit-test arms, green

| arm | how reached | exit |
|---|---|---|
| gh absent (this container's default) | `scripts/m1-exit-test.sh --mode local` | **0** |
| gh present (the CI arm) | same, with a stub `gh --version` first on PATH | **0** |

The gh-absent arm is not a weaker witness here: it asserts "exactly 1 FAIL line,
and it is CHECK gh FAIL" (scripts/m1-exit-test.sh:571), which round 1 broke by
producing two, so both arms detect this defect and both now pass.

### What the derivation did NOT cover

- I enumerated the consumers of the condition id, not of the doctor check
  generally: `grep -rn 'retention-undeclared'` over the tree minus `dist/` and
  `node_modules` returned six hits, all in `src/commands/doctor.ts` and
  `witness/doctor-retention-absent-path-and-full-profile.json`. The witness file
  pins the `PROFILES.full` line as a mutation `find` string, which is why that
  line was kept byte-identical.
- I did NOT audit the other four profiles (`generic`, `local-only`,
  `direct-pr`, `watch`) for the same one-id-two-states shape. The mechanism could
  exist elsewhere in the profile table; I checked only that no other condition id
  is returned from two branches meaning different things in `checkRetention`.
- I did NOT test the state where `charter/` contains a charter whose `retention`
  is present but EMPTY (`retention: {}`). It takes the declarations path with
  zero paths and reports `0 declared retention path(s) present and tracked`,
  which is a PASS on a vacuous declaration. That is pre-existing to this round,
  it is the SC-011 shape one level down, and I am naming it rather than fixing it
  in a final round whose scope is the blocking failure.
- `scripts/m1-exit-test.sh` and `src/commands/init.ts` were not read for defects,
  only for the facts quoted above; neither is on the files-to-touch list and
  neither was edited.

### Claim grep, fix round 2

```
awk 'NR>=612' delivery/work-history/m3-p8.md | grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to'
awk 'NR>=612' delivery/work-history/m3-p8.md | tr '\n' ' ' | grep -oEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to'
```

Both forms were run over this section, the second because the first is line-based
and this prose is hard-wrapped. Three hits line-visible, three hits
wrap-insensitive, so the wrap missed nothing here. All three are the word
`never`, and each is settled adjacently:

| hit | settled by |
|---|---|
| "never a silent pass" | a verbatim quote of delivery/plan/kernel-plan-m3.md:4042, not my claim |
| "do not print the same word in any state this round measured" | the three-row measured table immediately above it, scoped to those three rows and no further |
| "never FAIL under `--for full`" | the registered test's own name, asserted by the test and reddened by all three witness members |

### Verification, fix round 2

Toolchain node v26.6.0 throughout (`node --version` in the shell that ran each
command, CLAUDE.md warning 1). `npm run build` exit 0 and `git status --short`
empty afterwards.

THE SUITE SENTENCE, all three axes named (CLAUDE.md warning 12): at HEAD
`4ca7807`, node **v26.6.0**, with **`dist/` built**, invocation **`npm test`**:
**647 tests, 647 pass, 0 fail, 0 SKIPPED**, exit 0. Same head, same toolchain,
same build state, invocation **bare `node --test`**: **649 tests, 649 pass, 0
fail, 0 SKIPPED**, exit 0. The two-test gap is the documented one and is not a
new discrepancy: `package.json`'s `test` script is
`node --test "test/**/*.test.ts"`, which excludes the tracked sandbox fixture
`sandbox/test/greet.test.js`, and the `suite` GATE runs that script. Each number
is one higher than round 1's 646 and 648, and the one test is
`doctor reports retention not applicable, never FAIL under --for full, in a fleet
that tiphys init just created`, confirmed present in the `npm test` output rather
than inferred from the delta.

M1 EXIT TEST, the failure this round exists to fix:
`scripts/m1-exit-test.sh --mode local <evidence>` exit **0**, on both arms (table
above). Nothing in `scripts/m1-exit-test.sh` was edited.

GATE BUNDLE at HEAD `4ca7807`,
`node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full --evidence <dir> --base origin/main --head HEAD --phase m3-p8`:

| gate | verdict | units |
|---|---|---|
| `agent-rules-drift` | green | 18 rendered gate rows compared |
| `brief-drift` | green | 15 generated brief gate rows compared |
| `citations` | not-applicable | 0 citations resolved |
| `clause-map` | green | 50 clause-map rows checked |
| `coverage` | green | 115 finding ids checked |
| `credential-scrub` | green | 7 credential sources probed |
| `credential-token` | not-applicable | 0 tokens probed |
| `deploy` | not-applicable | 0 release verifications satisfied |
| `manifest-self-check` | green | 8 schema documents validated |
| `migrations` | not-applicable | 0 migrations compared |
| `red-witness` | green | 55 witnesses evaluated (18 own, 37 stored) |
| `scope` | green | 55 changed paths audited |
| `suite` | green | 647 tests reported |

Every applicable gate green with non-zero units, zero red, zero vacuous. THE
TABLE IS COMPOSITE AND THIS SAYS SO. The bundle at HEAD `4ca7807` printed
`declared 13 applicable 9 verdict 8 green 8 red 0 not-applicable 4 error 1
vacuous 0`, the one error being `red-witness`, and the `red-witness` row above is
its standalone re-run at the SAME head. The next paragraph gives the error and
why it is not a verdict. For comparison the same bundle at `560cc88`, which is
this branch's code with only the work-history text differing, printed
`declared 13 applicable 9 verdict 9 green 9 red 0 not-applicable 4 error 0
vacuous 0` in one pass, `red-witness` green with the same 55 units.

TWO HONEST FOOTNOTES ON THAT TABLE, because a bundle-level green is not a
gate-level one (T-009, one scope down).

- `citations` is not-applicable for the same reason round 1 recorded: the
  precondition is met (`delivery/requirements/clause-map.json` is in the diff)
  but no changed path is under the gate's own documents globs, which exclude
  `delivery/work-history/`. So the six `path:line` citations added this round
  were NOT machine-checked, and I read each back by hand instead. Every one
  resolves, and every cited file is byte-identical on `origin/main` and this
  branch, so the CLAUDE.md 3b silent-resolution trap does not apply:
  `git diff --name-only origin/main...HEAD` lists 54 paths and none of
  delivery/intake/orchestrated-delivery-v1.md:224, schemas/charter.schema.json:8,
  delivery/plan/phase-declarations/m3-p8.json:4, src/fleet.ts:61,
  scripts/m1-exit-test.sh:571 or delivery/plan/kernel-plan-m3.md:4042 is among
  them. The bundle exits 21 (`EXIT_NOT_APPLICABLE`) purely because of this row.
- `red-witness` reported `error` on the first attempt at this head, and the error
  was infrastructure, not a verdict: `ENOTEMPTY, Directory not empty:
  /tmp/tiphys-witness-0oH0tV/...` while the harness removed its own scratch root.
  The gate was re-run alone at the same head and reported green in 442299ms:
  `every witness red against every declared dangerous state and green at head`.
  Both attempts are recorded rather than only the green one. The disk was at 74
  percent with 9.8G free and no stale `/tmp/tiphys-*` directory remained, so I
  did not establish the cause and am not claiming one.

### Claim grep, verification section

```
awk 'NR>=810' delivery/work-history/m3-p8.md | grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to'
awk 'NR>=810' delivery/work-history/m3-p8.md | tr '\n' ' ' | grep -oEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to'
```

Both forms over the verification section, which starts at line 810. Every hit
except one is the grep PATTERN itself echoed inside the code block above, which
is why the wrap-insensitive tally shows each phrase exactly twice. The single
real hit is `never`, three occurrences of which two are pattern text, leaving one:
the word inside the registered test's own name, which the test asserts and which
all three of its witness members redden. No other over-claim survives in this
section, and the wrap-insensitive form found nothing the line-based one missed.

## Merge integration: M3-P7 into this branch

Not a fix round. The phase's own work was finished and green at e1088ee; M3-P7
merged to `main` while this phase was in flight and the two edited four files in
common. Merge commit a38b768, `git merge origin/main`, merge base e3aad93.

Four conflicts, all both-sides-add. `src/cli.ts` auto-merged, as the pre-pass at
delivery/plan/m3-p7-p8-concurrency-pre-pass.md:68 predicted for the alphabetical
command map.

| file | resolution |
|---|---|
| `test/behaviors.json` | union, 673 keys |
| `delivery/requirements/clause-map.json` | union, 63 keys |
| `src/checks.ts` | both sides' blocks, reconstructed from the merge base |
| `src/commands/validate.ts` | both sides' `--type` rows, disjoint keys |

Both registries are append-only, so each was resolved as a union and the union
was checked rather than assumed: zero keys removed by either side, zero merge
base values changed by either side, and zero keys added by both, so no key
needed a tie-break. Every key present on either side is present in the result,
asserted by name.

`src/checks.ts` is the one that needed care and the first attempt at it was
wrong. Both sides are PURE insertions (`git diff --stat` reports 0 deletions on
each) at the same two merge base anchors, but git's conflict boundaries cut
THROUGH object literals rather than between them, so concatenating the marker
regions produced unbalanced braces: `npm run build` exit 2 with a TS1005
cascade. The file was instead reconstructed as merge base plus each side's
insertion blocks spliced at the base anchors, which are top-level boundaries.
The splice self-checked first: base plus only M3-P8's blocks reproduces
e1088ee's `src/checks.ts` byte for byte, and base plus only M3-P7's blocks
reproduces `main`'s, so the anchors are right. The result is a strict
line-superset of both parents, `diff` reporting zero lines removed from either.

The pre-pass at delivery/plan/m3-p7-p8-concurrency-pre-pass.md:81 asks whoever
resolves a both-sides-add at the registry tail to CONFIRM, not assume, that the
array's order carries no meaning. Confirmed by reading every consumer:
`checksFor` at src/checks.ts:2801 filters by declared type and sorts by id,
`registeredChecks` at src/checks.ts:2808 returns a copy of the whole array whose
four call sites all iterate it as a set, and `deregisterCheck` finds by id. No
consumer reads a position.

### Enumeration: nothing from either phase was dropped

The failure mode guarded against is a merge that builds while silently dropping
one phase's check. That the compiler does not catch it is MEASURED here rather
than asserted, by deleting one entry from the registry array at this head, in a
copy restored immediately afterwards:

```
$ node -e '...remove "  tuitionTargetExists,\n" from src/checks.ts...'
probe: removed `tuitionTargetExists,` from the registry array
$ npm run build >/dev/null 2>&1; echo $?
0
registeredChecks() with the entry dropped: 21 (was 22)
checksFor(tuition) -> mechanism-rule-evidence-resolves
```

Build exit 0 with the check gone and `checksFor("tuition")` silently one
shorter, so the type gate is not evidence about this and the enumeration below
is not redundant with it.

The enumeration is done at RUNTIME rather than textually, with the expected set
derived from the two merge parents rather than hand-listed: 13 checks at the
merge base, plus 7 from M3-P7 (`HEAD^2`), plus 2 from M3-P8 (`HEAD^1`), is 22,
and `registeredChecks()` returns 22 with 22 distinct ids and zero entries
unaccounted for by either parent. Each was then confirmed exported, present in
the registry by object identity, and REACHABLE through `checksFor` for every
type it declares.

| origin | check id | reachable via checksFor |
|---|---|---|
| M3-P7 | `checklist-framing-ids-unique` | checklist |
| M3-P7 | `checklist-probe-ids-unique` | checklist |
| M3-P7 | `gate-probes-resolve` | checklist |
| M3-P7 | `verdict-criteria-complete` | verdict |
| M3-P7 | `verdict-deviations-judged` | verdict |
| M3-P7 | `verdict-finding-references-resolve` | verdict |
| M3-P7 | `verdict-hazard-classes-addressed` | verdict |
| M3-P8 | `mechanism-rule-evidence-resolves` | tuition, mechanism-index |
| M3-P8 | `tuition-target-exists` | tuition |

The 13 merge base checks were enumerated on the same run and are unchanged.

Both phases' notes record that their checks stay unreachable until the `--type`
row resolves, so that link was checked too: `TYPE_TABLE` carries 16 rows
including M3-P7's `checklist` at src/commands/validate.ts:101 and M3-P8's
`tuition` at src/commands/validate.ts:108, the registered checks declare 10
distinct types between them, and zero of those 10 lack a row.

### Merge integration results

- `npm run build` exit 0, `git status` clean afterwards.
- Suite: node v26.6.0, `dist/` built, invocation `npm test`: 710 tests, 710
  pass, 0 fail, **0 skipped**, exit 0. Above both parents, which is the check
  that nothing was dropped: M3-P8 alone measured 647 and `main` with M3-P7
  measured 688, against 625 at the merge base, and 625 + 22 + 63 is 710.
- `node scripts/check-authored-bytes.mjs` exit 0.

No logic from either phase was changed. This was integration only, and the two
phases did not conflict in behaviour anywhere: every shared file was a keyed or
append-only list on both sides.

### Gate bundle after the merge

Run at 7d58d84 on the scratch node v26.6.0 toolchain, `node bin/tiphys.ts gates
run --registry gate-registry.yaml --mode full --base origin/main --head HEAD
--phase m3-p8`, run id 539035353e0d3e6828ea696f:

```
gates: declared 13 applicable 9 verdict 9 green 9 red 0 not-applicable 4 error 0 vacuous 0
gates: required gate(s) not applicable: citations
GATES EXIT=20
```

Two registry gates are `verified-by: clean-room-checklist` and the runner
reports that it did not execute them, which is why 13 is declared against the
registry's 15.

| gate | verdict | units | round 2, before the merge |
|---|---|---|---|
| `manifest-self-check` | green | 8 schema documents validated | 8 |
| `coverage` | green | 115 finding ids checked | 115 |
| `credential-scrub` | green | 7 credential sources probed | 7 |
| `suite` | green | 710 tests reported | 647 |
| `scope` | green | 55 changed paths audited | 55 |
| `clause-map` | green | 63 clause-map rows checked | 50 |
| `red-witness` | green | 63 witnesses evaluated | 55 |
| `agent-rules-drift` | green | 18 rendered gate rows compared | 18 |
| `brief-drift` | green | 15 generated brief gate rows compared | 15 |
| `citations` | not-applicable | 0 citations resolved | not-applicable |
| `credential-token` | not-applicable | 0 tokens probed | not-applicable |
| `deploy` | not-applicable | 0 release verifications satisfied | not-applicable |
| `migrations` | not-applicable | 0 migrations compared | not-applicable |

Every applicable gate is green with non-zero units and zero vacuous. The three
counts that MOVED are the three that should have: the suite, the clause map and
the witness set each gained M3-P7's rows. `scope` staying at 55 is the point of
a merge integration rather than a scope change, and it is 55 because the merge
brings `main` into the branch, so M3-P7's files are identical on both sides and
do not appear in the audited diff.

The bundle exit is 20, `EXIT_NOT_APPLICABLE` at src/gates/result.ts:67, and it
is that purely because the required `citations` gate is not applicable on this
diff, for the reason rounds 1 and 2 both recorded: no changed path falls under
the configured documents globs, which do not include `delivery/work-history/`.
Zero gates errored.

One correction to round 2's own note above, which said the bundle exited 21 and
glossed 21 as `EXIT_NOT_APPLICABLE`. src/gates/result.ts:68 defines 21 as
`EXIT_GATE_ERROR`, and round 2's run did carry a gate error, the red-witness
`ENOTEMPTY` that round recorded and re-ran. So 21 there was the error, not the
not-applicable row, and the two codes are distinct. Left as a correction here
rather than an edit to that round's text.

## Fix round 3 (2026-08-13)

Scoped by the orchestrator to THREE findings that break the shipped package for
a real consumer: HRB-8 (the shipped `tuition/mechanism-index.yaml` is invalid
by its own rules once packed), CR-1 with HRB-6 (one family: `checkRetention`
prints a silent PASS on charters with no usable retention path), and HRB-1
(`mechanism-rule-evidence-resolves` is blind to `path.ext:LINE`, this
repository's binding citation grammar at CLAUDE.md:155).

Eight further findings (CR-2, HRB-2, HRB-3, HRB-4, HRB-5, HRB-7, HRB-9 and the
two remaining LOWs) are TRACKED and merge with the phase as recorded findings.
Not touched in this round.

Beacon opened at the start of the round; appended as the work proceeded.

### Round 3, step 0: reproducing the consumer view (HRB-8)

Packed at `26ee653`, extracted, and validated exactly as a consumer has it.
`npm pack` reports 180 files; `delivery/`, `src/`, `scripts/` and `test/` are
not among them.

```
$ npm pack --pack-destination $SP/P8J-pack        # tiphys-kernel-0.0.0.tgz
$ tar -xzf tiphys-kernel-0.0.0.tgz
$ node bin/tiphys.ts validate --type mechanism-index \
    --context $SP/P8J-pack/package $SP/P8J-pack/package/tuition/mechanism-index.yaml
INVALID #/mechanisms/10/evidence/1 evidence names delivery/verification/cr-520-orchestrator-reproduction.md, which does not exist (check: mechanism-rule-evidence-resolves)
... 16 INVALID lines in total
exit=1
```

That reproduces HRB-8 as written. **It also understates it**, which the
fix-round contract's "name the mechanism, not the finding" is what surfaced.
HRB-8 measured the INDEX. The shipped tuition ENTRIES have the same defect, and
through a SECOND check, twelve lines away from the first:

```
$ for f in package/tuition/T-*.yaml; do ... validate --type tuition --context package "$f"; done
T-001 exit=0 invalid=0    T-002 exit=1 invalid=2    T-003 exit=1 invalid=5
T-004 exit=1 invalid=2    T-005 exit=1 invalid=1    T-006 exit=1 invalid=2
T-007 exit=0 invalid=0    T-008 exit=1 invalid=3    T-009 exit=0 invalid=0
T-015 exit=0 invalid=0    T-016 exit=0 invalid=0    T-017 exit=0 invalid=0
T-018 exit=1 invalid=5    T-021 exit=0 invalid=0    T-022 exit=0 invalid=0
```

EIGHT of the fifteen shipped entries are INVALID in a consumer's tree, and
T-018 shows both checks firing on one document:

```
INVALID #/mechanisms/0/evidence/2 evidence names scripts/render-agent-rules-gates.mjs, which does not exist (check: mechanism-rule-evidence-resolves)
INVALID #/structural-consequence/0/target structural consequence is marked applied and its target scripts/check-brief-drift.mjs does not exist (check: tuition-target-exists)
```

So fixing only `mechanism-rule-evidence-resolves` would have left
`tuition-target-exists` broken in the identical way, which is precisely the
T-009 corollary this repository already paid for once (PR #27 fixed one arm and
left the sibling arm twelve lines away; PR #30 is what that cost).

#### The mechanism

**A citation is relative to the repository that authored it, and the check
resolved it against whatever context it was handed without first establishing
that the context is that repository.**

#### The derivation, and what it did not cover

Every call site of the citation-resolution mechanism, whole tracked tree:

```
$ git grep -n "pathReferencesIn" -- .
src/checks.ts:2689:        for (const path of pathReferencesIn(reference)) {
src/checks.ts:2735:export function pathReferencesIn(reference: string): string[] {
test/tuition.test.ts:63:  pathReferencesIn: (reference: string) => string[];
test/tuition.test.ts:423:      checksModule.pathReferencesIn(
test/tuition.test.ts:429:      checksModule.pathReferencesIn(

$ git grep -nE "join\((contextDirectory|context)\b" -- 'src/*.ts' 'src/**/*.ts'
src/checks.ts:393:  const path = join(contextDirectory, relativePath);
src/checks.ts:1550:        const path = join(contextDirectory, directory);
src/checks.ts:2613:      if (classifyEntry(join(contextDirectory, target)).kind === "absent") {
src/checks.ts:2690:          if (classifyEntry(join(contextDirectory, path)).kind === "absent") {
```

Four sites, classified rather than counted:

| site | carries the mechanism | disposition |
|---|---|---|
| 2690, `mechanism-rule-evidence-resolves` evidence | YES | fixed |
| 2613, `tuition-target-exists` target | YES | fixed (not named by either review) |
| 393, `readContextDocument`, reached by `machine-readable-form.path` | YES | fixed, same predicate |
| 1550, `modeConditionsQuoteGrantedBy` decision-record lookup | NO | scans `DECISION_DIRECTORIES` for a RECORD ID, not a citation path, and already handles an absent directory. Left alone |

**NOT covered by this derivation, stated because an empty result and a wrong
scope look identical:** the search covered `src/**/*.ts` only. It did not cover
`scripts/`, `test/`, or the gate implementations under `src/gates/`, on the
ground that the `citations` gate has its OWN resolution model with a documented
base/head contract (src/gates/citations.ts:453) and is not part of this
mechanism. It also did not cover any path resolution that does not go through a
context directory, so anything resolving against `process.cwd()` is outside it.

#### The decision (HRB-8), stated as the orchestrator asked

Of the three the review offered, the true one is **the check should not resolve
them outside a context that has them**, and the artifact already said so before
this round. schemas/mechanism-index.schema.json:5 states, in the shipped schema
itself, that it "cannot check that `evidence[]` resolves; entries are prose
citations of artifacts that may live in delivery/, which the kernel does not
ship, so resolution is not computable from an installed package". M3-P8 then
registered a check that resolves them unconditionally anyway. The check
contradicts the schema beside it, and the schema is right.

The other two are rejected for stated reasons rather than taste. "The package
must ship what they point at" contradicts CLAUDE.md:58, which makes `delivery/`
not a kernel deliverable; it would also ship this build's paperwork to every
consumer. "The index should not carry repo-local evidence paths at all" destroys
the field's whole value: T-005's rule is that a rule with no citation is not a
rule, and a citation into the repository that paid for it is the citation that
exists.

**The predicate, mechanical rather than a judgment:** a citation rooted in a
top-level tree that the context does not contain is NOT RESOLVABLE HERE, and is
reported with its count and the trees involved. A citation with no directory
component is always resolvable, because the context root always exists. A
citation whose top-level tree IS present must resolve or it is a violation,
exactly as before.

Measured against the real feed, that keeps every bit of the kernel repository's
catching power and gives the consumer an honest answer:

```
targets with a directory:  checklists/ roles/ schemas/ tuition/ present in pkg
                           scripts/ src/ test/ absent in pkg
evidence path tokens:      delivery/ (10) scripts/ (1) src/ (2), all absent in pkg
root-level targets:        every `applied` one ships; the only absentee,
                           AGENTS.md, is `ticketed`, which the check does not read
```

**It is never a silent pass.** The check emitted `reports: []` before this
round, so a green printed nothing at all (HRB-1's second paragraph makes the
same complaint). It now reports what it resolved and what it could not, so
"nothing to check here" and "everything checked and fine" never print the same
line. That is the SC-011 shape the plan's hazard row at
delivery/plan/kernel-plan-m3.md:4042 polices, applied to this check.

#### The three fixes

**HRB-8, src/checks.ts.** `unresolvableCitationTree` returns the top-level tree
a citation is rooted in when the context does not contain it, and undefined
otherwise. Applied at all three sites the derivation found: evidence, the
`structural-consequence[].target` of `tuition-target-exists` (which no review
named), and `machine-readable-form.path`. Both checks now emit `reports`, which
they did not before at all.

Consumer view, before and after, same command, same extraction procedure:

```
BEFORE (26ee653)                        AFTER
index:   16 INVALID, exit=1             index:   exit=0
                                        REPORT mechanism-rule-evidence-resolves 1 citation(s) resolved
                                        REPORT mechanism-rule-evidence-resolves 16 citation(s) not
                                          resolvable in this context: no delivery/, scripts/, src/
                                          tree here, so they name a repository this is not
entries: 8 of 15 INVALID                entries: 15 of 15 exit=0
```

In THIS repository nothing was given up, which is the half that matters and is
measured rather than asserted:

```
$ node bin/tiphys.ts validate --type mechanism-index --context . tuition/mechanism-index.yaml
REPORT mechanism-rule-evidence-resolves 17 citation(s) resolved
exit=0
```

Seventeen is the 16 evidence tokens plus the one `machine-readable-form`, so
every citation that was being resolved before is still being resolved.

**HRB-1, src/checks.ts.** `pathReferencesIn` strips an optional
`:<line>[-<line>][@sha256:<hex>]` suffix before the extension test, matching the
grammar src/gates/citations.ts:453 already defines rather than a second narrower
one. Stripping is conservative: a token whose colon is not a line suffix keeps
it and is judged exactly as before.

**CR-1 and HRB-6, src/commands/doctor.ts.** Two arms, because the family has two
mechanisms. A retention value that is not a non-empty string is its own FAIL
naming its key (closing nested-map, empty-string and non-string values, which
the old filter discarded silently). A charter that yields zero paths by any
route takes `retention-undeclared` (closing `{}` and `[]`, where the old guard
tested `typeof retention !== "object"` and both are objects).

#### Witnesses, all red against the DANGEROUS state

Every witness below was produced by defanging the fix in the working tree,
running, and restoring from an out-of-tree copy. `git diff --quiet` confirmed
the restore each time; no `git checkout --` was used anywhere in this round
(standing warning 8).

**HRB-1.** The pair the finding is made of, an entry whose paths are ALL
fabricated, and the byte-identical entry without the suffixes:

| | defanged | fixed |
|---|---|---|
| T-900, `path.ext:2626` form | **exit 0, no output at all** | exit 1, both citations named |
| T-901, same entry, suffixes removed | exit 1, both citations named | exit 1, both citations named |

The registered test reddens under that defang and stays green otherwise.

**HRB-8.** Three defangs, because a wrong fix here has two shapes and only one
of them is the obvious one:

| defang | HRB-1 test | HRB-8 test |
|---|---|---|
| A: remove the `:LINE` strip | **red** | green |
| B: tree gate never fires (the pre-round behaviour) | green | **red** |
| C: tree gate ALWAYS fires (excuse every citation) | **red** | **red** |

Defang C is the one worth having. A fix that made the check quiet instead of
correct would satisfy "the package is valid" and destroy the check, and the
test's second direction (every citation RESOLVES in this repository, zero
reported unresolvable) is what reddens it.

**CR-1 and HRB-6.** Five structurally different members, both profiles, on a
real `tiphys init` fleet. Defanged, `--for full`:

| charter `retention` | defanged | fixed |
|---|---|---|
| key absent (control) | FAIL ... (required for profile full) | unchanged |
| `retention: {}` | **PASS 0 declared retention path(s) present and tracked** | FAIL ... declares no retention paths |
| `retention: []` | **PASS 0 ...** | FAIL ... declares no retention paths |
| nested-map value | **PASS 0 ...** | FAIL ... key work-history as a map, which names no path |
| empty-string value | **PASS 0 ...** | FAIL ... as an empty string, which names no path |
| number value | **PASS 0 ...** | FAIL ... as a number, which names no path |
| real present tracked path | PASS 1 ... | PASS 1 ... (unchanged) |

That reproduces both reviews' tables and extends them: CR-1 listed three shapes
and HRB-6 four, and the number-valued charter is a fifth that neither ran.

The registered test is red under EACH ARM SEPARATELY, not only under both
removed at once, which is what establishes that the two arms guard different
members rather than one arm carrying the class:

```
defang both arms   -> fail 1
defang count arm   -> fail 1
defang filter arm  -> fail 1
```

The positive control is inside the test: a declared, present, tracked path must
still PASS with its count derived from the fixture, so a fix that reddened
everything fails it.

#### New behaviours, registered BY NAME

Appended to `test/behaviors.json`; no count anywhere asserts over it.

- `mechanism-evidence-resolves-path-line-citation`
- `tuition-feed-valid-in-a-consumer-tree`
- `doctor-retention-no-usable-path-family`

The consumer-tree fixture DERIVES the shipped tree set from `package.json`'s
`files` at run time rather than listing it, so a later phase that starts
shipping a tree changes the fixture automatically instead of leaving it
asserting a world that has moved.

#### Verification

Toolchain node v26.6.0 throughout, `dist/` built, on branch
`claude/m3-p8-tuition-flow`.

**Suite.** The complete sentence, all three axes: node v26.6.0, `dist/` built,
invocation `npm test`.

```
i tests 713   i pass 713   i fail 0   i skipped 0   i todo 0
```

**TRANSLITERATION DECLARED (CLAUDE.md convention 3).** The capture above is a
real `npm test` run, and Node's test reporter prints U+2139 at the head of each
summary line, which fails the non-ASCII check. Hand-writing the line to avoid
the glyph is the fabrication the red-witness rule exists to prevent, so it is
transliterated and declared instead: **U+2139 rendered as `i`, 5 occurrences,
and nothing else in any captured output in this round's section was changed.**
Measured after the substitution, the whole file carries zero non-ASCII bytes.
The `x` and checkmark glyphs (U+2716, U+2714) do not appear because this
round's captured summaries are all-passing runs; the defang tables above were
transcribed as verdict words rather than pasted as reporter lines.

713 is 710 (the round-2 figure at 26ee653) plus this round's three registered
tests, and the SKIPPED count is quoted beside the pass count because a bare
"N pass, exit 0" is the incomplete sentence (standing warning 12).
`npm run build` leaves `git status --porcelain` empty.

**Gate bundle**, `--mode full --phase m3-p8 --base origin/main`, run
73a6d845af285b1f665b3358:

```
declared 13 applicable 9 verdict 9 green 9 red 0 not-applicable 4 error 0 vacuous 0
required gate(s) not applicable: citations
BUNDLE EXIT=20
```

Per-gate, read from `summary.json` DIRECTLY rather than deduced, since this run
was local and its evidence did not have to leave a runner:

| gate | status | units |
|---|---|---|
| `manifest-self-check` | green | 8 schema documents validated |
| `coverage` | green | 115 finding ids checked |
| `credential-scrub` | green | 7 credential sources probed |
| `suite` | green | 713 tests reported |
| `scope` | green | 55 changed paths audited |
| `clause-map` | green | 63 clause-map rows checked |
| `red-witness` | green | 63 witnesses evaluated |
| `agent-rules-drift` | green | 18 rendered gate rows compared |
| `brief-drift` | green | 15 generated brief gate rows compared |
| `credential-token`, `citations`, `deploy`, `migrations` | not-applicable | 0 |

Every applicable gate is green with non-zero units and zero vacuous. Exit 20 is
`EXIT_NOT_APPLICABLE` and is that solely because the required `citations` gate
does not apply to this diff, for the reason rounds 1 and 2 both recorded: the
gate's precondition lists `delivery/plan/`, `delivery/verification/`,
`delivery/decisions/`, `delivery/tuition/`, `delivery/requirements/` and
`delivery/STATE.md`, and this diff touches `src/`, `test/` and
`delivery/work-history/` only.

**A RED I CAUSED AND THEN CHASED, recorded rather than quietly re-run.** The
first bundle attempt reported `suite` RED with five failing tests across
`assurance-modes`, `checklists` and `checks`. I had started
`scripts/m1-exit-test.sh` in the foreground while that bundle was running in the
background, so two heavy jobs shared the tree. Re-run alone at the same head,
`suite` is green with 713 reported, and the full bundle is green.

The obvious alternative explanation is that the runner parallelises gates and
`red-witness`, which MUTATES sources to defang them, overlapped `suite`. The
timings refute it: gates run strictly sequentially, `suite` 07:45:47 to
07:48:57 and `red-witness` 07:48:58 to 07:58:24, no overlap anywhere in the
thirteen.

**The limit, stated because an explanation that was never re-tested is a
hypothesis:** I did not re-run the CONCURRENT configuration to confirm it
reproduces. What is established is that gates do not overlap each other, and
that the suite is green at this head both alone and inside the bundle. That the
concurrent M1 exit test is what broke it is the best-supported explanation, not
a measured one.

**M1 exit test**, which round 2 fixed and this round must not regress:

```
$ bash scripts/m1-exit-test.sh --mode local $SP/P8J-m1ev
m1-exit-test: local mode complete, evidence in .../P8J-m1ev
M1 EXIT TEST EXIT=0
```

**Authored bytes.** `node scripts/check-authored-bytes.mjs` exits 0 with the
work staged. The script refuses to run against a dirty index, which is worth
knowing: run it after committing, not before.

**Citations.** Every `path:line` in this section resolves in the branch tree,
checked rather than assumed. One was wrong on the first writing and is
corrected here rather than left: `CLAUDE.md:71` is the "Binding conventions"
heading, and the sentence making `delivery/` unshipped is CLAUDE.md:58.

**Claim grep**, both forms, over this round's section only:

```
line-based (binding):   6 lines, 7 occurrences
wrap-insensitive:       7 occurrences (ALWAYS 1, always 2, anyway 1, never 3)
```

Zero missed by wrapping. Each hit sits next to the capture that settles it: the
`never a silent pass` claims next to the REPORT lines and the test that asserts
them, the `always resolvable` claim next to the measured classification of every
shipped target and evidence token, and the three in tables next to the defang
they label.

#### Left TRACKED, not fixed

CR-2, HRB-2, HRB-3, HRB-4, HRB-5, HRB-7, HRB-9 and the two remaining LOWs, per
the round's scope. One of them I want to argue with, using a constructed input
rather than an opinion, and I did NOT fix it.

**HRB-2 is a wrong verdict on a real user path, and it is the same mechanism as
the CR-1/HRB-6 family this round was told to fix.** The reviews themselves group
them: "a message word that no condition decides". `checkRetention` prints the
word `tracked` and the only thing it tests is `git check-ignore`. Constructed on
a real `tiphys init` fleet, a retention path that exists, is not ignored, and
git has never heard of:

```
$ git -C <fleet> ls-files --error-unmatch notes/work-history/keep.md
error: pathspec ... did not match any file(s) known to git
exit=1                                   # NOT tracked
$ git -C <fleet> check-ignore -q -- notes/work-history
exit=1                                   # not ignored
$ git -C <fleet> status --porcelain notes/
?? notes/
$ tiphys doctor
CHECK retention PASS 1 declared retention path(s) present and tracked
```

The path will not survive the next clone, which is the exact harm R-098 exists
to prevent, and doctor calls it tracked. It is weaker than the three I fixed in
that it needs the operator to have created the directory and not committed it,
but that is the ordinary state immediately after authoring, which is when a
person runs doctor. Raising it rather than fixing it, because the round's scope
is the orchestrator's call and DR-0027 rule 3 caps the rounds.

Nothing else in the tracked set looks to me like a ship-breaker: CR-2, HRB-3,
HRB-4 and HRB-5 sit in guards and generators rather than on the path a package
consumer walks, and the three LOWs each exit nonzero or are visible in output.

#### What I could not do

- **`npm pack` is not exercised by the registered test.** The consumer-view test
  stages a directory holding the trees `package.json`'s `files` ships, derived at
  run time, rather than packing a real tarball. Packing in the suite would make
  it depend on a network-capable npm and on tarball timing. The real `npm pack`
  extraction was run by hand, before and after, and is quoted above; what the
  suite guards is the property (which trees are present), not the packer.
- **The residue of the tree predicate, named rather than left to be found.** A
  fabricated citation whose TOP-LEVEL segment names a directory that exists
  nowhere (`totally/invented/x.md`) is now reported unresolvable rather than
  violated, in any context. In THIS repository that is caught by the registered
  test's second direction, which requires zero unresolvable citations across the
  whole feed; in a consumer's tree it is not caught at all, and cannot be, since
  the consumer has no way to tell a fabricated tree from an unshipped one. The
  review's own probe used exactly that shape, which is why it is called out
  instead of being quietly rendered untestable.
- **Doctor still does not validate the charter against its schema**, which is
  the deeper reason CR-1's family was reachable. Both reviews name it. The two
  arms make doctor's own verdict correct without it; wiring schema validation
  into doctor is a larger change than this round's scope.
