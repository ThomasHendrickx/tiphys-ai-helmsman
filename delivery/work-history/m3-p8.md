# M3-P8 work history: tuition flow and the mechanism index

- phase: M3-P8
- branch: claude/m3-p8-tuition-flow
- base: origin/main at ec77c7d (M3-P6 merged)
- toolchain: node v26.6.0 from the scratch prefix, npm 11.x

Written incrementally from the first minutes; the file's mtime is the beacon
(CLAUDE.md:440).

## Dispatch-time resolutions (the plan requires these to be resolved here, not
## copied out of it)

- **Migration-ticket ids: T-021 and T-022.** The lowest two free ids across
  BOTH `T-nnn` directories. `delivery/tuition/` holds T-001 to T-020 and the
  root `tuition/` holds none. Retirement was checked against the whole history,
  not the tree (CLAUDE.md:643):

  ```
  git log --all --oneline -S'T-021' | head -3                       # no output
  git log --all --oneline -- 'delivery/tuition/T-021*' 'tuition/T-021*' \
    'delivery/tuition/T-022*' 'tuition/T-022*' | head -3            # no output
  ```

- **`delivery/tuition/` at dispatch holds 20 entries** (`ls delivery/tuition/ |
  wc -l` reports 20), T-001 to T-020.

More sections are appended as the work proceeds.
