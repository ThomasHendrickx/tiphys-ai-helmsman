# M4-P15 work history: the kernel's root charter

- phase id: M4-P15
- branch: `claude/m4-p15-kernel-charter`
- cut from: `plan/pstack-borrow-review` at `8c0d202`, then merged forward to its
  tip `d0f245d` (see "The base moved under me" below)
- files: `charter.yaml` (new, at the repository root), `test/kernel-charter.test.ts`
  (new), `test/behaviors.json` (six appended rows),
  `delivery/plan/phase-declarations/m4-p15.json` (new), this file

**Citation convention used here.** Every path this branch changes relative to
`origin/main` is written in BACKTICKS and therefore does not resolve, per
CLAUDE.md rule 3b and T-019's collision. The changed set was measured, not
assumed:

```
git diff --name-only origin/main...HEAD
```

It lists forty-one paths, including every document the `plan/pstack-borrow-review`
branch adds (the M4 plan, the pre-pass, the probes, DR-0035 through DR-0043) and
all five of this phase's own files. Resolving citations below therefore point
only into files that branch does NOT touch: `src/`, `scripts/`, `schemas/`,
`test/dual-review.test.ts` and DR-0012.

## What this phase is, and how it differs from the plan section that carries its id

`delivery/plan/kernel-plan-m4.md` section 3.4 declares M4-P15 as "the kernel's
fleet-home bring-up" on branch `claude/m4-p15-fleet-bringup`, touching
`src/commands/doctor.ts` and `test/doctor.test.ts`, with nine acceptance
criteria about `tiphys init`, `tiphys doctor` and a real fleet remote. **THAT IS
NOT THIS UNIT.** The dispatch and `delivery/plan/m4-conflict-pre-pass.md`'s
wave-3 table carve the CHARTER out of that section and promote it to its own
unit under the same id, with branch `claude/m4-p15-kernel-charter` and three
files, none of them a source file.

Stated as a contradiction rather than absorbed: the plan section and the wave-3
pre-pass disagree about what M4-P15 is. The pre-pass is dated 2026-09-16, is
later, and is what the dispatch cites, so it governs here.

**Consequence somebody has to pick up:** the fleet bring-up work of plan section
3.4, which is its criteria 2 through 7 plus the `doctor` promotion of
`retention-not-applicable` (src/commands/doctor.ts:439 is the condition, and
src/commands/doctor.ts:50 is the comment explaining why `full` does not promote
it today), is NOT done by this unit and has no phase id of its own. It is also
the reason section 3.4's own criterion 5 exists, so dropping it would leave a
`doctor --for full` that calls a charterless fleet ready. This is a sequencing
gap I am reporting, not one I am closing.

## The mechanism this phase exists against

Not "the repository has no charter". The mechanism is:

**`check-dual-review` is a CONDITIONAL gate whose precondition counts verdict
documents, and the property it guards is therefore unobserved until the moment
it becomes load-bearing for every merge.**

The precondition arm counts `kind: verdict` documents under `delivery/review/`
and returns 1 when there are none (scripts/check-dual-review.mjs:331), so the
gate does not run. Measured against this repository with nothing staged, before
any of the witness work below:

```
$ /opt/node22/bin/node scripts/check-dual-review.mjs --precondition .
check-dual-review: 0 verdict document(s) under delivery/review
exit=1
$ /opt/node22/bin/node scripts/check-dual-review.mjs .
check-dual-review: not-applicable (0 review verdicts examined for decorrelation)
no verdict document exists under delivery/review, so there is no pair of reviews to compare
exit=20
```

test/dual-review.test.ts:687 asserts that same zero count against this
repository as a present-tense fact. The gate arm, when it does run, checks the
two REGIME DOCUMENTS before anything else: scripts/check-dual-review.mjs:198
names them and scripts/check-dual-review.mjs:208 is the refusal. So the first
phase to commit its two clean-room verdicts arms a gate that immediately errors,
and it errors for every subsequent phase too.

This is the "guard that could not go red" class arriving from the other
direction. The usual form is a guard that runs and asserts nothing. This one
asserts perfectly well and does not run, so its firing was deferred to a moment
when it would block everything at once.

## Red witness 1: the arm this phase exists for

**The dangerous state is the CI error, not the absent file.** Both arms ran
against the REAL repository root with the two shipped decorrelated verdict
fixtures staged under the real `delivery/review/`, from the real repository
root, on the same interpreter, with one variable changed.

Script kept at `/tmp/claude-0/-home-user/49c9c4fa-6f01-5020-aa81-c87700265964/scratchpad/m4p15/arm.sh`.

ARM A, before the charter existed:

```
=== ARM A: DANGEROUS STATE, no root charter ===
cwd: /home/user/tiphys-ai-helmsman/.claude/worktrees/wf_325aa631-d2d-1
interpreter: v22.22.2
charter.yaml present: no
--- precondition arm ---
check-dual-review: 2 verdict document(s) under delivery/review
precondition exit=0
--- gate arm ---
check-dual-review: error (0 review verdicts examined for decorrelation)
charter.yaml does not exist, so the declared mode's merge-authority is unknown and no decorrelation verdict can be reached; a merge check that cannot determine the regime reports error, never green
gate exit=21
fixtures removed; residue: ls: cannot access 'delivery/review/verdict-m4-p15-witness-*': No such file or directory
```

ARM B, with the authored charter in place and nothing else changed:

```
=== ARM B: GREEN STATE, root charter present ===
cwd: /home/user/tiphys-ai-helmsman/.claude/worktrees/wf_325aa631-d2d-1
interpreter: v22.22.2
charter.yaml present: yes
--- precondition arm ---
check-dual-review: 2 verdict document(s) under delivery/review
precondition exit=0
--- gate arm ---
check-dual-review: 1 registered check(s) named dual-review-decorrelation ran over 2 verdict(s)
REPORT dual-review-decorrelation 2 verdict(s) for phase M3-P9 are distinct on produced-by, framing, review-contract
check-dual-review: green (2 review verdicts examined for decorrelation)
2 verdict(s) examined by 1 registered check(s); no decorrelation violation
gate exit=0
fixtures removed; residue: ls: cannot access 'delivery/review/verdict-m4-p15-witness-*': No such file or directory
```

Exit 21 to exit 0, one variable. Two things in arm B are worth reading rather
than skimming. `1 registered check(s) ... ran over 2 verdict(s)` is the line
that separates "a guard ran and found nothing" from "no guard ran", which is
SC-011's requirement. And `are distinct on produced-by, framing, review-contract`
is printed ONLY after the declared mode has been located in the mode document
and its `merge-authority` read as the delegated grant
(src/checks.ts:2903 is that constant), so the delegated path was genuinely
taken; a mode with any other authority prints `is not a delegated grant`
instead, and that sentence does not appear.

The staged fixtures were the two shipped ones, `decorrelated-criteria.yaml` and
`decorrelated-hazard.yaml` under `witness/fixtures/dual-review/`, and both were
removed after each arm, which the script's last line prints.

## Red witness 2: a charter that is present but unusable, three members

A class needs at least two structurally different members. Three ran, all
against the real repository root, script kept at
`/tmp/claude-0/-home-user/49c9c4fa-6f01-5020-aa81-c87700265964/scratchpad/m4p15/invalid.sh`.
The file was restored byte for byte afterwards, which the script checks with
`cmp -s` and prints.

| member | what changed | result |
|---|---|---|
| 1 | the charter does not decode at all | red, exit 1, `the charter is present and could not be read` |
| 2 | it decodes and names `invented` | red, exit 1, `declares delivery mode invented, which assurance-modes.yaml does not define` |
| 3 | it decodes and declares no `delivery-mode` | red, exit 1, `declares no delivery-mode, so no mode's merge-authority can be looked up` |

The three verdict lines, verbatim from that run:

```
=== MEMBER 1: the charter does not decode (a truncated block scalar) ===
INVALID #/produced-by the charter is present and could not be read, so the declared mode's merge-authority is unknown and decorrelation could not be evaluated: charter.yaml is not valid YAML: Flow sequence in block collection must be sufficiently indented and end with a ] (check: dual-review-decorrelation) [delivery/review/verdict-m4-p15-witness-criteria.yaml]
check-dual-review: red (2 review verdicts examined for decorrelation)
gate exit=1

=== MEMBER 2: the charter decodes and names a mode no mode document defines ===
42:delivery-mode: invented
INVALID #/produced-by charter.yaml declares delivery mode invented, which assurance-modes.yaml does not define, so its merge-authority is unknown (check: dual-review-decorrelation) [delivery/review/verdict-m4-p15-witness-criteria.yaml]
check-dual-review: red (2 review verdicts examined for decorrelation)
gate exit=1

=== MEMBER 3: the charter decodes and declares NO delivery-mode at all ===
INVALID #/produced-by charter.yaml declares no delivery-mode, so no mode's merge-authority can be looked up and whether the delegated grant applies to phase M3-P9 could not be established (check: dual-review-decorrelation) [delivery/review/verdict-m4-p15-witness-criteria.yaml]
check-dual-review: red (2 review verdicts examined for decorrelation)
gate exit=1

=== restored? ===
charter.yaml restored byte for byte
```

Each member reports the same line twice in the full capture, once per staged
verdict, and one of the pair is shown here. The three print DIFFERENT sentences,
which matters: "could not be read", "does not define" and "declares
no delivery-mode" are three states and collapsing them into one message is how
a reader stops being able to tell which one they are in.

## The finding this phase produced, and it is the reason a test exists at all

**A charter that is present, decodes, and declares a valid mode, but is
SCHEMA-INVALID, is accepted SILENTLY.** The gate reads exactly two things out of
the charter: that it decodes, and its `delivery-mode`. It never validates
against `schemas/charter.schema.json`.

Measured, one file, two programs, script at
`/tmp/claude-0/-home-user/49c9c4fa-6f01-5020-aa81-c87700265964/scratchpad/m4p15/gap.sh`:

```
--- tiphys validate on the schema-invalid charter ---
INVALID #/retention required property retention is missing
validate exit=1
--- check-dual-review on the SAME schema-invalid charter ---
check-dual-review: 1 registered check(s) named dual-review-decorrelation ran over 2 verdict(s)
REPORT dual-review-decorrelation 2 verdict(s) for phase M3-P9 are distinct on produced-by, framing, review-contract
check-dual-review: green (2 review verdicts examined for decorrelation)
2 verdict(s) examined by 1 registered check(s); no decorrelation violation
gate exit=0
```

And nothing on the CI path closes it. The derivation:

```
grep -rn 'validate --type charter\|"charter"' .github/workflows/ gate-registry.yaml gates.manifest.json scripts/
grep exit=1
```

Exit 1 with no hits: no workflow step, no registry gate and no script runs
`tiphys validate --type charter` against the root charter.

**WHAT THAT DERIVATION DOES NOT COVER, stated before anyone relies on it.** It
searched four places: `.github/workflows/`, the gate registry, the gate manifest
and `scripts/`. It did NOT search `src/`, so a validation performed inside a
command rather than by a gate would not appear; the separate `grep -rn "charter"
src/ scripts/ --include=*.ts --include=*.mjs` run at the start of this phase
returned the `src/` hits and none of them validates a ROOT charter (they are the
fleet's `charter/` directory in `src/fleet.ts` and `src/commands/doctor.ts`, the
schema path resolution in `src/commands/validate.ts` and `src/commands/mode.ts`,
and the schema-enum derived check at src/checks.ts:710). It also did not search
`.claude/`, which is harness configuration rather than CI, and it did not search
the M3 exit-test scripts. And it is a TEXT search: a call site that built the
string `"charter"` at run time would be invisible to it.

**So `test/kernel-charter.test.ts` exists for this and not as ceremony.** It
puts the schema check on a path that runs on every suite invocation, which is
the one path that does not depend on a verdict document existing.

## The test, and its own red witness

`test/kernel-charter.test.ts` holds seven tests. The first runs the EXACT command
`gate-registry.yaml` runs, `node scripts/check-dual-review.mjs .`, from the real
repository root, and asserts the regime-unknown sentence is absent. That works
today, with zero verdicts committed, because the regime check runs BEFORE the
verdict count (scripts/check-dual-review.mjs:198 precedes the count), so the
assertion is live now and stays live on the day a phase commits two verdicts.
The others cover the staged pair, its red arm, the unusable class, the schema arm,
the mode-resolves-by-name arm, and the behaviour registration.

**No assertion anywhere in that file is a count.** The schema arm iterates
`required` read out of `schemas/charter.schema.json` at run time, so a field a
later phase adds is exercised without editing the test, and the mode arm reads
the ids out of the mode document at run time. That is binding convention 5
applied to two registries that are not `test/behaviors.json`.

The schema arm carries a CONTROL that is easy to omit: re-serialising the
charter through the YAML library drops comments and rewrites block scalars, so a
rejection could be an artifact of the round trip. The round-tripped charter with
nothing deleted is validated first and must exit 0, and only then is each
required key deleted in turn.

### The test reddens against three dangerous states, and they discriminate

Capture at
`/tmp/claude-0/-home-user/49c9c4fa-6f01-5020-aa81-c87700265964/scratchpad/m4p15/capture-testred.txt`,
interpreter v22.22.2, one file, one variable per arm:

| state | tests | pass | fail | which failed |
|---|---|---|---|---|
| the charter as authored | 7 | 7 | 0 | none, exit 0 |
| no root charter | 7 | 1 | **6** | 1, 2, 3, 4, 5, 6 |
| present but schema-invalid | 7 | 6 | **1** | 5 only |
| present, naming an undeclared mode | 7 | 3 | **4** | 2, 3, 5, 6 |

The third row is the interesting one and it is the whole argument for the file:
that is the state `check-dual-review` calls GREEN with exit 0, measured two
sections above, and test 5 is the only thing in this repository that goes red
against it. The rows also DISCRIMINATE rather than all going red together: test
1 stays green under a schema-invalid charter, correctly, because the regime IS
determinable there, and test 4 (the unusable class) stays green under both
states 2 and 3 because it stages its own charters and does not read the root
one for its assertions.

## The charter, field by field: what I carried, what I changed, and why

The source read was `delivery/evidence/m3-exit-test/e1/charter.yaml`, which is
EXIT-TEST EVIDENCE for a finished run. It was read and used as a reference, not
copied. Eleven top-level fields, `additionalProperties: false`, all seven
irreversible decisions, per schemas/charter.schema.json:8.

| field | disposition |
|---|---|
| `kind` | CARRIED. The schema pins it to the constant `charter`. |
| `identity.name` | CARRIED, `tiphys-kernel`. |
| `identity.repo` | CARRIED. |
| `identity.kernel-version-pin` | CARRIED, `0.1.0`, re-verified against `package.json` on this branch rather than trusted from the exit-test note. Exact, never a range, which is the schema's own `$comment` at schemas/charter.schema.json:34. |
| `delivery-mode` | CARRIED, `full`. Argued below. |
| `assurance-tier` | CARRIED, `full`. |
| `yolo-permissions` | CARRIED, disabled with an empty scope. |
| `irreversible-decisions.stack`, `.language`, `.framework`, `.core-data-model`, `.tenancy` | CARRIED. |
| `irreversible-decisions.auth` | CHANGED, extended. The exit-test text stops at the trusted publisher. DR-0039 decided that release verification may hold a write-capable credential the orchestrator never shares, which is an auth fact about this project and belongs in the field that states auth. |
| `irreversible-decisions.deployment-topology` | CHANGED, extended. DR-0040 makes the Claude Code plugin a second package in this repository, which changes what this project deploys. |
| `product-intent` | REWRITTEN. The exit-test text says "Not in scope before M4: the harness adapter, the Claude Code plugin, and any cutover". M4 has started, so that sentence describes a state this project has left. Replaced with DR-0036's adapter-first ordering, DR-0037's single subject, DR-0041's exit-test binding, and an out-of-scope list that is true now (M5's second project, DR-0043's post-M4 cleanup). |
| `constraints` | CHANGED, and this is the dispatch's first named amendment. |
| `escalation-contract.stop-for` | CHANGED, and this is the dispatch's third named amendment. |
| `escalation-contract.route` | CARRIED, with DR-0016's escalation test appended. |
| `release-verification` | CARRIED AS `reserved`, WITH A NEW REASON. The dispatch's second named amendment. |
| `retention` | CARRIED unchanged. |

### `delivery-mode: full` and `assurance-tier: full`

Both enums are CLOSED at v0.1.0 under DR-0020 and admit only `full`,
`direct-pr` and `local-only`; the derived check `charter-mode-enum-matches-modes`
at src/checks.ts:710 holds the schema's enums equal to the mode document's ids,
so neither field is free.

`full` is not merely a value that validates, it is the only honest one. It is
the mode this project runs, and it is the mode whose `merge-authority` is
`delegated-under-conditions`, which is the constant at src/checks.ts:2903 and
the regime DR-0012 granted. Declaring `direct-pr` or `local-only` would make
`check-dual-review` REPORT that no decorrelation is required of reviews that
delivery/decisions/DR-0012-delegated-merge-authority.md:22 requires to be
decorrelated on model family. That is the fail-open direction, reachable by a
one-word edit, and it is why the charter says so in a comment rather than
leaving the next reader to work it out.

### The constraint DR-0036 replaces

delivery/evidence/m3-exit-test/e1/charter.yaml:40 is "The kernel never runs on
itself before M4, with the M3 exit test as the one controlled exception". M4 has
started and DR-0036 overturned it. The replacement is DR-0036's condition
QUOTED, not paraphrased, so the freeze point is readable from the charter:
"The current process retains planning, review, credentials, pull request, merge,
recovery and closeout authority for every kernel phase delivered this way, until
the cutover workstream says otherwise. Tiphys owns the bounded local lifecycle
and nothing else."

Two constraints were also ADDED, and neither is decoration: C-1 (current state
is never read from the tail of an append-only log), which the exit-test charter
states only obliquely inside `core-data-model`; and T-009's rule that a gate
result is evidence only for the configuration that produced it. Both are binding
on this project and a charter that omits them understates what it is declaring.

### The fix-round number, and why this charter carries NONE

delivery/evidence/m3-exit-test/e1/charter.yaml:49 says "a phase that needs more
than two fix rounds", which is DR-0012's limit at
delivery/decisions/DR-0012-delegated-merge-authority.md:34. DR-0035 has since
made the ceiling a function of declared subject size and impact: one, two or
three rounds. So **any single number written into this field is wrong for some
phases**. The plan's instruction to move the charter and the mode data together
is NOT followed here, and the reason is scope rather than difficulty:
`assurance-modes.yaml` is not on this phase's files list in
`delivery/plan/m4-conflict-pre-pass.md`, and editing it would collide with
whichever unit owns that file.

What I did: the stop-for entry names the BUDGET rather than a number, "a phase
that exceeds the fix-round budget DR-0035 sets for its declared subject size and
impact". That is true under both documents and pins nothing.

**OPEN ITEM, handed on rather than resolved.** `assurance-modes.yaml` still
carries `max-fix-rounds-after-review: 2`, which is DR-0012's number and which
DR-0035 supersedes for subjects it tiers differently. That value and DR-0035
disagree. Closing it is a one-line edit to a root document this phase may not
touch, and it belongs to whichever phase owns `assurance-modes.yaml`. I have not
established what reads that value; the mode document's own comment calls the
escalation bounds "DATA THE ORCHESTRATOR BRIEF CITES, NOT AN ENFORCEMENT ENGINE",
which suggests nothing enforces it, and I did not derive the readers.

### `release-verification` stays `reserved`, and the reason is now written down

The enum admits only `none` and `reserved` (schemas/charter.schema.json:117), and
`none` requires a stated reason. `none` would be a FALSE statement about this
project: 0.1.0 was verified at release. So `reserved` is the only honest value
the vocabulary offers, and what the exit-test note lacked is why it is still
reserved now that DR-0014's stated deferral reason (nothing published yet) has
expired.

**The dispatch says the deploy-gate probe has measured this shape, and I read it
and concluded it measures a DIFFERENT artifact.** `delivery/verification/m4-prototype-probes.md`
item 12 is about `release-verification.json`, the deploy gate's own declaration
file, and its finding is that the observe step took its satisfying value from
configuration rather than from the kernel-owned subject. That is a real,
measured, repaired defect and it is not the charter's `release-verification`
field, whose shape DR-0014 reserved. I have said so in the charter's note rather
than letting a reader infer that the field was designed.

The reason recorded for keeping it reserved is the plan's own recommendation in
its section 3.6: this kernel publishes through a trusted publisher with no
long-lived credential, so designing the field against it would be designing
against a degenerate case with one adapter and one outcome. It falls due at
cutover.

## What else reads a root charter: the derivation, in full

Placing a file at the repository root changes gate behaviour repository wide, so
this was established before writing it rather than after.

```
grep -rn "charter" src/ scripts/ --include=*.ts --include=*.mjs
```

Eighty-seven hits when the same pattern is counted over TRACKED files only, with
`git grep -n "charter" -- 'src/*.ts' 'src/**/*.ts' 'scripts/*.mjs' | wc -l`; the
plain `grep -rn` form above returns more once `dist/` has been built, because it
walks the build output too, and that is worth knowing before anyone reruns it.
Every hit was followed. They fall into five groups:

1. **`scripts/check-dual-review.mjs`**, the merge-path caller. Resolves
   `charter.yaml` inside whatever directory it is given, which the registry
   makes the repository root. THIS is the consumer this phase serves.
2. **`src/checks.ts`, `dual-review-decorrelation`**, which resolves
   `charter.yaml` from its context directory. src/checks.ts:3330 is the presence
   test; the comment above it records that an absent charter REPORTS here and is
   refused in the caller instead, which is why the two behave differently on the
   same input.
3. **`src/checks.ts`, `charter-mode-enum-matches-modes`** at src/checks.ts:710.
   Reads `schemas/charter.schema.json`, not any charter document. A root charter
   does not affect it.
4. **`src/commands/doctor.ts` and `src/fleet.ts`**, which resolve a FLEET's
   `charter/` DIRECTORY, not a root `charter.yaml`. `src/fleet.ts` lists
   `charter` as a durable fleet area. A file at this repository's root is not a
   fleet home and does not reach them.
5. **`src/commands/validate.ts` and `src/commands/mode.ts`**, which resolve the
   SCHEMA path. Unaffected by a charter document anywhere.

A second search widened the net beyond source, because the first was scoped by
extension:

```
git grep -l "charter\.yaml\|charter\.yml" -- .
```

Twenty-six tracked files. Beyond the two source sites and the delivery paperwork, two
matter and both were checked: `test/dual-review.test.ts`, which stages its own
context directories and asserts against this repository only through the
PRECONDITION arm at test/dual-review.test.ts:687 (a verdict count, which a
charter does not change), and `test/doctor.test.ts`, which is fleet-scoped
throughout. Both were re-run and pass.

**WHAT NEITHER SEARCH COVERS.** Both are text searches over tracked files. A
consumer that composes the filename at run time, a consumer in `.github/`
(searched separately: `grep -rn "charter" .github/` exits 1 with no hits), a
consumer in the M3 exit-test shell scripts,
and any consumer in a dependency are all outside them. `manifest-self-check` was
checked separately by reading `src/gates/manifest.ts`: it enumerates
`src/gates/schemas/*.schema.json` by `readdirSync` and validates those and
nothing else, so a new root file is invisible to it.

## The base moved under me

I cut from `plan/pstack-borrow-review` at `8c0d202`. While this phase ran, that
branch advanced to `d0f245d` with four commits. I merged them forward rather
than leaving the branch behind, so that "local green before opening" under
DR-0031 means something. The four commits touch `.claude/orchestrator-next.mjs`,
two tuition entries and two verification documents. No source file, no test, no
root document. The suite result below is from after the merge.

One of those documents is directly relevant and is obeyed here:
`delivery/verification/a-suite-number-needs-a-fourth-qualifier.md` adds a fifth
qualifier to a suite result, whether the tree is a git checkout. It is, and that
is stated in the suite sentence below rather than assumed.

## Suite result, the complete sentence

- interpreter: **node v26.6.0**, a floor-satisfying toolchain fetched to
  `/home/user/node26-scratch/node-v26.6.0-linux-x64/bin`, which is NOT under
  `/tmp/claude-0`. That placement is deliberate: this worktree lives under
  `/home/user`, and CLAUDE.md's standing warning 1 records that an interpreter
  under `/tmp/claude-0` makes `runCliUnprivileged` fail with EACCES for a repo
  that is not itself under `/tmp`.
- build state: `dist/` BUILT. `npm ci` exit 0, `npm run build` exit 0, and
  `git status --porcelain` after the build shows only this phase's own files,
  no build artifact.
- invocation: `npm test`.
- tree: a real git checkout (a worktree; `git rev-parse --is-inside-work-tree`
  prints `true`), not an archive copy.
- result: **855 tests, 855 pass, 0 fail, 0 cancelled, 0 skipped, exit 0.**

**AND THE FIRST RUN WAS NOT THAT, which is recorded rather than dropped.** The
first `npm test` on this head reported `tests 788, pass 786, fail 0, cancelled 2,
skipped 0`, exit 1, with `test/watcher.test.ts` and `test/witness.test.ts`
cancelled at the FILE level with "Promise resolution is still pending but the
event loop has already resolved". Two follow-ups settle it:

```
node --test test/watcher.test.ts test/witness.test.ts
tests 76, pass 76, fail 0, skipped 0, exit 0
```

and the second full run, taken at `load average: 9.64, 16.51, 17.32`, was the
855/855/exit 0 above. 788 cancelled two whole files, so their 76 tests were
never counted; 788 + 76 = 864 against 855 is not an identity and I am not
claiming it is one, because the two runs differ in which tests reached the
reporter.

I did NOT establish the base's result for this symptom by running the suite at
`d0f245d`. What I did instead: the two cancelled files are not read by anything
this phase changes (neither imports `test/behaviors.json`, neither reads
`charter.yaml`), and they pass in isolation at this head. The wall-clock
sensitivity is consistent with `delivery/verification/wall-clock-budgets-are-load-dependent.md`.
**That is an inference, not a measurement of the base**, and a reviewer who
wants it closed should run `npm test` at `d0f245d` under comparable load.

The DEFAULT toolchain arm was not run for the full suite. The phase's own file
was run on it repeatedly (v22.22.2, every capture above names it) and reports 6
tests, 6 pass, 0 fail, 0 skipped. CLAUDE.md standing warning 12 records that the
default toolchain FAILS a floor-dependent test in `test/doctor.test.ts` at recent
heads, so a red there would not have been attributable to this change without a
base run; I ran the floor-satisfying toolchain instead, which is the authority.

## The claim grep, both forms

Run against this file, as CLAUDE.md binds. Occurrences, not matching lines, so
the two numbers are comparable:

| form | occurrences |
|---|---|
| the line-based binding command | 9 |
| the wrap-insensitive `tr` form | 9 |

Zero missed by hard wrapping. The nine, keyed by the section they sit in rather
than by a line number, because a line number in this table goes stale the moment
anything above it is edited and a stale one is worse than none:

| hit, in order | section | settled by |
|---|---|---|
| "cannot determine the regime ... never green" | red witness 1 | it is inside a verbatim capture of the program's own output |
| "a class needs at least two" | red witness 2 | the rule being applied; the three members and their verbatim captures follow immediately |
| "it never validates" | the finding | the measured pair in that same section: `validate` exit 1 and the gate exit 0 on one file, plus the derivation grep exiting 1 with no hits |
| "never a range" | the charter, field by field | a quotation of schemas/charter.schema.json:34 |
| "never shares" | the charter, field by field | a quotation of DR-0039's own title |
| "the kernel never runs on itself" | the constraint DR-0036 replaces | a quotation of delivery/evidence/m3-exit-test/e1/charter.yaml:40 |
| "never read from the tail" | the constraint DR-0036 replaces | a quotation of constraint C-1 |
| "never counted" | suite result | the two suite captures in that same section |
| "never re-serialise phases" | files-to-touch | a quotation of CLAUDE.md's convention 5 |

**This table's own rows are hits too.** That is arithmetic rather than a new
claim, since the table quotes each hit phrase in order to dispose of it, so a
re-run returns more than nine. A reviewer should subtract this table's rows.

## A behaviour row resolves against the TEST NAME, not against a description

This cost a red gate and is written down because the rule is not in CLAUDE.md,
which says only that a behaviour "resolves by name".

The first version of `test/behaviors.json`'s six new rows carried DESCRIPTIONS
of each behaviour. The full gate bundle reported:

```
gates: suite: red: 8 finding(s): behavior charter-root-present-regime-determinable does not resolve: no reported test is named "the gate's own command against the repository root does not report the regime unknown"; ...
```

src/gates/suite.ts:1042 is the line. **The VALUE of a row must be, character for
character, the title of a test the run reports.** Six rows, six reds, one per
row. The fix was to make each value equal its test's title exactly, which also
forced the staged-pair test to split in two, because two behaviours had been
sharing one test and a shared test can only carry one title.

Re-measured after the fix, with the TAP reporter so the titles are printed:

```
ok 1 - the repository root carries a charter, so the merge check can determine the regime
ok 2 - with a committed pair of verdicts the root charter is what lets the check reach a verdict
ok 3 - removing the root charter from that same context makes the merge check error rather than green
ok 4 - a root charter that is present and unusable fails loudly, and two different members do
ok 5 - the root charter validates, and a copy missing any one required key does not
ok 6 - the root charter's declared mode is one assurance-modes.yaml defines, by name
ok 7 - this phase's new behaviors are registered in test/behaviors.json
# tests 7
# pass 7
# fail 0
# skipped 0
```

The in-file test at item 7 asserts the six IDS are PRESENT. It does not and
cannot assert that their values match test titles, which is the property that
actually reddened; only the gate does that. Worth knowing before trusting a
green item 7.

## The two gates that are RED, and neither is closed by this phase

The full bundle was run locally rather than discovered in CI, per DR-0031:

```
node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full \
  --evidence <scratch> --base origin/main --head HEAD --phase m4-p15
gates: declared 16 applicable 11 verdict 11 green 9 red 2 not-applicable 5 error 0 vacuous 0
gates: 2 gate(s) reported red: suite, scope
```

Nine green: `manifest-self-check`, `coverage` (115 ids), `credential-scrub`,
`citations` (518 resolved), `clause-map` (74 rows), `agent-rules-drift`,
`brief-drift`, `check-agents-references` (21 references), `license`. Five
not-applicable with reasons, including `check-dual-review` itself, whose
precondition is still unmet on this branch because this branch commits no
verdict document.

**`scope` is red and it is a sequencing fact, not a defect in this branch:**

```
gates: scope: red: branch claude/m4-p15-kernel-charter (phase m4-p15) matches the phase pattern but no phase declaration exists at delivery/plan/phase-declarations/m4-p15.json in the merge base 3b401182301361700ffa0fd4b2ae099993b3d733 of --base origin/main and --head HEAD; the declaration must be committed to main before the phase branch is created
```

src/gates/scope.ts:875 is that message. The declaration is ADDED by this branch,
which is what the dispatch asked for, and M3-P11 relaxed only ADDITIONS to an
existing declaration, not a first declaration. Closing it is an orchestrator
action (landing the declaration ahead of the branch), and it is the same
constraint the M4 plan's own preamble records for M4-P13, M4-P14 and M4-P15.

**`suite` was red for two reasons and only one was mine.** The six behaviour
findings are fixed above. The other two were:

```
failing test: "the coverage gate against the real migration table and appendix reports units 115 with per-kind and per-milestone breakdowns matching the plan" (test/coverage-gate.test.ts)
failing test: "deleting an appendix row is red naming the orphan id, and restoring it is green" (test/coverage-gate.test.ts)
```

Re-measured in isolation at the same head, same interpreter, same build state:

```
node --test test/coverage-gate.test.ts
tests 17, pass 17, fail 0, skipped 0, exit 0
load average: 35.71, 42.54, 41.92
```

The bundle run puts the whole suite alongside every other gate on a four-CPU
machine, and `delivery/verification/wall-clock-budgets-are-load-dependent.md`
records that `src/gates/coverage.ts` uses a wall-clock budget that reddens under
load. The `coverage` GATE in the very same bundle reported green with `115
inventory id(s) checked`, which is the same subject the failing test asserts
about, so the failure is in the measurement conditions and not in the data.

**Stated as an open question rather than closed:** I did not run the bundle at
the base commit under comparable load, so I have not shown the same two tests
fail there. What I have shown is that they pass in isolation at this head, that
this branch changes nothing they read, and that a documented load-dependence
explains the shape. A reviewer who wants it settled should run the bundle at
`d0f245d`.

## Gates run locally

- `node scripts/check-authored-bytes.mjs` exit 0.
- `node bin/tiphys.ts validate --type charter charter.yaml` exit 0.
- the citations gate, captured in the next section.

## What this phase did NOT do

1. **It did not sweep the registry for other dormant conditional gates.**
   `delivery/plan/m4-charter-blocks-every-merge.md` closes by saying that sweep
   has not been run and should be. It still has not. This phase followed one
   finding; `credential-token`, `deploy`, `migrations`,
   `unit-tests-for-changed-service-methods` and
   `fixtures-for-changed-component-states` are all conditional and none of them
   was examined here.
2. **It did not touch `assurance-modes.yaml`**, so the fix-round disagreement
   above is open.
3. **It did not do the fleet bring-up**, so plan section 3.4's criteria 2
   through 7 and its `doctor` promotion are unowned.
4. **It did not establish what the scope gate will say.** src/gates/scope.ts:875
   is the red it emits, and it reads the phase declaration from the MERGE BASE,
   `delivery/plan/phase-declarations/m4-p15.json` is added BY this branch, so on
   a pull request against `main` the declaration is absent from the merge base.
   The M4 plan's own preamble says the declarations must land with the plan for
   exactly this reason. This is an orchestrator sequencing matter and I am
   naming it rather than working around it.
5. **It did not verify the gate in CI.** Every capture here is local. The
   `check-dual-review` gate runs on `pull_request` only, and it will stay
   NOT-APPLICABLE on this branch because this branch commits no verdict
   document. The first phase whose pull request carries two verdicts is where
   the green arm is witnessed in CI, and until then the suite is what guards it.
6. **It did not enumerate what reads `max-fix-rounds-after-review`.**

## Files-to-touch, and the two the pre-pass did not name

`delivery/plan/m4-conflict-pre-pass.md` names three files for this unit:
`charter.yaml`, the declaration and this work history. Two more are present and
both are declared in `delivery/plan/phase-declarations/m4-p15.json`:

- `test/behaviors.json`, which CLAUDE.md names as a standing pre-authorized
  extra, and which convention 5 describes as append-only and union-resolved, so
  in its own words such registries "never re-serialise phases".
- `test/kernel-charter.test.ts`, a NEW file at a path no other unit names. It is
  not a source file, so the dispatch's "do not touch any source file" holds, and
  the `red-witness` gate's precondition is `diff-touches` on `src/` and `bin/`,
  neither of which this branch changes, so no witness spec is owed.

The disjointness argument in the pre-pass was that this unit "creates one root
file and its own paperwork". Adding one new test file and appending to an
append-only registry does not weaken it: no live unit names either path.
