# M4-P2 work history: `launch` becomes async, and completion becomes checked

- phase: M4-P2
- branch: `claude/m4-p2-async-launch`
- branch point: `6961186` (tip of `plan/pstack-borrow-review`, the branch that
  carries the M4 plan; the dispatch names it as the cut point)
- declaration: `delivery/plan/phase-declarations/m4-p2.json`
- plan section: delivery/plan/kernel-plan-m4.md:323
- conflict pre-pass: delivery/plan/m4-conflict-pre-pass.md:13

This file is written INCREMENTALLY and committed as work proceeds (T-008
dispatch contract, CLAUDE.md:437). Its mtime is this agent's beacon. Entries
are appended in the order the work happened, not reordered afterwards.

## Status

- [x] worktree created, beacon committed
- [ ] step 1: the byte-identity verification, captured
- [ ] step 2: promote the async step runner into `src/task.ts`
- [ ] step 3: `launch` returns a promise
- [ ] step 4: the call site awaits
- [ ] step 5: the completion precondition
- [ ] step 6: the src/spawn.ts:500 comment
- [ ] red witnesses, both directions
- [ ] criteria walk
- [ ] gates

## Log

### Setup

Branch cut from `plan/pstack-borrow-review` per the dispatch. The branch name
matches the phase pattern deliberately: this IS the phase's implementation
branch, which is the one branch CLAUDE.md:604 permits to match it.
