# M3-P5 work history: authoring-role briefs and the finding format

Branch: claude/m3-p5-authoring-role-briefs
Base: a7d5686 (main)
Status: IN PROGRESS. This file is the T-008 beacon and is appended to as the
work happens, not written at the end.

## Contract

The phase contract is delivery/plan/kernel-plan-m3.md:2975 through
delivery/plan/kernel-plan-m3.md:3208. The declaration is
delivery/plan/phase-declarations/m3-p5.json:1.

## Log

- Read CLAUDE.md, the declaration, and the M3-P5 plan section in full.

## 17:30 Reading pass, what it settled

Read in full: CLAUDE.md, delivery/plan/phase-declarations/m3-p5.json:1, the
M3-P5 section delivery/plan/kernel-plan-m3.md:2975 to 3208, M3-P6's section
(delivery/plan/kernel-plan-m3.md:3410 to 3520) because it consumes what this
phase builds, and the conflict pre-pass on the arbitration branch.

Existing code read: src/commands/validate.ts (TYPE_TABLE, the resolver),
src/cli.ts (the dispatch table), src/checks.ts (the derived-check registry and
runChecks), src/validate.ts (readOperatorPath, decodeDocument, validateInstance,
AUTHORING_VOCABULARY), src/task.ts (classifyEntry, refuseOpenForWrite,
readRegularFileIfPresent), src/brief.ts (M1-P4 assembly, CONSUMED not
rewritten), scripts/check-clause-map.mjs, schemas/plan.schema.json,
schemas/README.md, templates/plan.example.yaml.

The seven clause-map rows this phase owns, derived rather than assumed:

```
$ grep -nE '^\|\s*(R-[0-9]+[a-z]?)\s*\|\s*(M3-P5)\s*\|' delivery/plan/kernel-plan-m3.md
6426:| R-004 | M3-P5 | `roles/investigator.md`: root-cause verdict with evidence, fixes nothing |
6427:| R-005 | M3-P5 | `roles/plan-writer.md`: never decides product questions, flags them as decision records |
6428:| R-006 | M3-P5 | `roles/adversarial-plan-reviewer.md` with the blueprint's widened visibility; process doc role table annotated (SC-001, D-14, D-M3-12) |
6431:| R-010a | M3-P5 | verification-pass clause in the investigator and plan-writer briefs; verifier is the M2-P5 citation linter |
6436:| R-015a | M3-P5 | investigator brief clause; made mechanical by the report contract's `repro` requirement (M3-P5 criterion 6) |
6447:| R-029 | M3-P5 | `schemas/finding.schema.json`: verdict, severity-ranked findings, concrete plan edits, `produced-by` (T-001) |
6493:| R-092 | M3-P5 | investigator brief clause: reproduce before fixing; if it will not reproduce, ship the harness and say so |
```

scripts/check-clause-map.mjs:90 declares `roles/plan-writer.md` as the M3-P5
anchor, so creating that file puts the phase IN FORCE and all seven rows become
required at once. That is the reason the clause-map edit ships in the same
commit as the briefs, not after them.

## 17:33 PROBE: can criterion 5(b) be written as `if`/`then` on `minItems`?

Criterion 5(b) (delivery/plan/kernel-plan-m3.md:3146) asks for a finding set
with `findings: []` and no `no-findings-statement` to be REJECTED, and its
parenthetical says the mechanism is "`if`/`then` on `minItems`". I could not
write that, and here is the measurement rather than the assertion.

The obligation is "empty array IMPLIES a sibling property is required". `if`
must therefore match on EMPTINESS. The closed authoring vocabulary
(schemas/README.md:36, src/validate.ts:111) is `$ref additionalProperties const
contains enum if items minItems minLength oneOf pattern properties required
then type uniqueItems`. There is no `not` and no `maxItems`. `minItems: 1` and
`contains` both match the NON-empty case, which is the wrong polarity, and
`then` cannot remove an obligation. So the `if`/`then` form has no way to
express the condition.

`oneOf` is in the vocabulary and does express it. Probe, run from the
repository root on node v22.22.2 against ajv 8.20.0 with the DR-0013 clause 4
options copied from src/validate.ts:526:

```
$ node ./probe-raw.mjs
## oneOf both COMPILE ERROR: strict mode: required property "no-findings-statement" is not defined at "https://tiphys.dev/schemas/probe-oneOf-both.json#/oneOf/1" (strictRequired)
## oneOf, branch2 declares property COMPILED
   empty, no statement -> invalid
   empty, statement -> VALID
   one finding, no statement -> VALID
   one finding, statement -> invalid
```

Three things that output settles, none of them assumed:

1. A `oneOf` branch carrying `required` MUST also declare that property in a
   sibling `properties` at the SAME level, or the schema does not compile at
   all under `strict: true`. That is `strictRequired`, and the first variant is
   the captured proof.
2. With the property declared, the construct rejects exactly the dangerous
   instance criterion 5(b) names.
3. It ALSO rejects `one finding, statement`, a set that carries findings AND a
   no-findings statement. That is a second rejection the criterion did not ask
   for, and it is the same both-directions property M3-P4's report schema
   reaches with a derived check. I am recording it as a consequence rather than
   claiming it was intended.

An earlier form of the same probe, through this repository's own
`validateToLines`, first reported `INVALID # schema uses keyword minItems
without declaring type array, which this validator's strict policy requires`,
which is a fourth measured fact: inside a `oneOf` branch the `type` must be
restated. Both probe scripts are kept at
delivery/work-history/m3-p5.md (this file) and their output is above verbatim.

NEXT: write the schemas, then the briefs, then src/roles.ts.

## 18:05 Artifacts written, first end-to-end run

Written: schemas/role-brief.schema.json, schemas/finding.schema.json,
roles/_shared-dispatch-contract.md, roles/investigator.md,
roles/plan-writer.md, roles/adversarial-plan-reviewer.md, roles/README.md,
src/roles.ts, src/commands/brief.ts, plus the additive edits to src/cli.ts
(one import, one dispatch row), src/commands/validate.ts (two TYPE_TABLE rows
and a role-brief branch), package.json (`roles` into `files`), and
delivery/intake/orchestrated-delivery-process.md (one table cell plus one
footnote, SC-001).

Three design decisions, each with the reason, because each is a place a
reviewer will ask why:

1. **The clause round trip runs inside `tiphys validate --type role-brief` and
   reads the INCLUDE-EXPANDED body.** Criterion 1 says the body is not parsed
   and criterion 6b says deleting a clause heading makes THIS COMMAND exit
   nonzero naming the orphaned id. Both are satisfied by splitting the two:
   the SCHEMA sees the frontmatter alone (criterion 1's parenthetical), and a
   separate round trip reads the body. It must expand the include, because the
   two dispatch-contract clauses live in exactly one file that all five briefs
   include, so a round trip that did not expand would report every brief as
   orphaning both ids.
2. **The anchor form is explicit: `## clause <id>: <title>`.** An anchor rule
   that guessed (a heading whose text looks like an identifier) would classify
   `## Scope` as an anchor and redden any brief with an ordinary heading.
3. **`mandated-reading` resolves against the KERNEL ROOT, not the cwd**, which
   constrains a brief to mandate only what `package.json`'s `files` ships. That
   is why the three briefs mandate `roles/_shared-dispatch-contract.md`,
   `schemas/*.json`, `templates/plan.example.yaml` and `gate-registry.yaml`,
   and why none of them mandates anything under `delivery/`, which is not
   shipped. M3-P6's criterion 8 mandates `tuition/mechanism-index.yaml`, also a
   shipped path, which is the same rule seen from the next phase.

Build and first runs, node v22.22.2 (the default toolchain in this shell,
checked in the same shell that ran the command):

```
$ npm run build
(exit 0, no diagnostics)

$ for r in investigator plan-writer adversarial-plan-reviewer; do
    node bin/tiphys.ts validate --type role-brief roles/$r.md; echo "exit=$?"; done
=== investigator ===
exit=0
=== plan-writer ===
exit=0
=== adversarial-plan-reviewer ===
exit=0

$ node bin/tiphys.ts brief compose --role plan-writer \
    --phase templates/plan.example.yaml --phase-id M9-P1
(exit 0; header with the four resolved mandated-reading paths in order, the
brief body with the shared clause block expanded, then `# Phase M9-P1` with
one `### <field>` section per rendered field)

$ node bin/tiphys.ts brief compose --role plan-writer \
    --phase templates/plan.example.yaml --phase-id M9-P9
tiphys brief compose: templates/plan.example.yaml declares no phase with id M9-P9
exit=1
```

NEXT: the clause map rows, then the three test files, then the witness specs.

## 18:35 test/roles.test.ts green, 7 tests

```
$ node --test test/roles.test.ts
# tests 7
# pass 7
# fail 0
# skipped 0
```

Two failures on the way there, both real and both worth recording rather than
quietly fixing:

- `the shared incremental-output clause does not name: modification time is
  your beacon`. The phrase is present and HARD-WRAPPED across two lines. The
  fix was to normalise whitespace on both sides of every text assertion in the
  file, not to reflow the prose: an assertion against a hard-wrapped phrase
  makes a reflow of the same sentence a red test, which is a guard on
  formatting rather than on content.
- `the SC-001 footnote does not quote the original wording`, same cause in the
  process document's footnote.

NEXT: test/brief-compose.test.ts, which needs a STAGED kernel root (criterion 2
stages a brief naming a path that must not exist in the shipped tree, and
criterion 6c stages a real `mkfifo` at one).

## RESUMED: a second implementer takes over

The first implementer was terminated by a usage limit. Its work was salvaged
verbatim by the orchestrator at b1caeed and this file is what made that
salvage possible, which is T-008's incremental-output rule paying for itself
inside the phase that writes the clause (roles/_shared-dispatch-contract.md:1).

Everything below this heading is the second implementer's. Nothing above it
was edited: the record of what the first one measured stands as it wrote it.

First act, before building on any inherited claim: re-run the green the beacon
records at line delivery/work-history/m3-p5.md:171, because a captured result
that was never re-observed after an interruption is a claim, not evidence.

## 2026-08-12 Verification of the inherited state, before adding anything

Toolchain, build state and invocation are all named, per CLAUDE.md's three
axes. Node v26.6.0 from the fetched floor-satisfying prefix, checked with
`node --version` in the same shell that ran each command below.

```
$ node --version
v26.6.0
```

The beacon's last recorded green (delivery/work-history/m3-p5.md:171) is
re-observed rather than trusted, and TWO MORE FILES turn out to be finished
that the beacon's NEXT line said were still to write. The salvage commit
preserved more than the log had caught up with, which is the difference
between a beacon and a summary.

```
$ node --test test/roles.test.ts
i tests 7
i pass 7
i fail 0
i skipped 0

$ node --test test/brief-compose.test.ts
i tests 9
i pass 9
i fail 0
i skipped 0

$ node --test test/finding-schema.test.ts
i tests 5
i pass 5
i fail 0
i skipped 0
```

TRANSLITERATION DECLARED, per CLAUDE.md's rule for captured output: Node's
test reporter prints U+2139 at the head of each summary line and U+2714 before
each passing test name. In the three captures above U+2139 is rendered `i`,
12 occurrences, and the U+2714 lines are elided rather than transliterated
(they are the per-test names, reproduced in prose where they matter). Nothing
else in any captured output was changed.

### What the verification pass found still missing

Derived from the plan's `new behaviors` list, not from memory:

```
$ for b in <the fourteen declared behavior names>; do
    printf '%-55s %s\n' "$b" "$(grep -c "\"$b\"" test/behaviors.json)"; done
role-brief-frontmatter-validates                        0
brief-compose-missing-reading-fails                     0
brief-compose-renders-phase                             0
brief-compose-unknown-phase-id-fails                    0
reviewer-visibility-agrees-across-documents             0
finding-requires-concrete-edit                          0
finding-empty-set-requires-statement                    0
finding-records-model-family                            0
investigator-report-requires-repro                      0
role-brief-carries-incremental-output-clause            0
brief-compose-refuses-non-regular-reading-path          0
brief-compose-renders-every-required-phase-field        0
brief-compose-dropped-hazard-classes-detected           0
brief-compose-dropped-acceptance-detected               0
```

Zero of fourteen registered, and `git diff a7d5686 -- test/behaviors.json` is
empty. So the tests are written and green and NOTHING resolves them by name.
That is the registration half of the phase, and `witness/` (declared in
delivery/plan/phase-declarations/m3-p5.json:22 and empty of this phase's
specs) is the other half: src/gates/red-witness.ts:52 makes a changed file
under `src/` with no witness spec covering it RED, never not-applicable, and
this phase changes four files under `src/`.

## 2026-08-12 CRITERION 6 CANNOT BE DELIVERED FROM THIS PHASE, and why

Criterion 6 (delivery/plan/kernel-plan-m3.md:3152) asks that
`tiphys validate --type report` accept an investigator report only when a
`repro` reference is present, and reject the same report with the reference
removed. That is an edit to the REPORT schema, which this phase does not own
and which is not present at all.

Measured, not assumed:

```
$ ls schemas/
README.md                     finding.schema.json
assurance-modes.schema.json   gate-registry.schema.json
charter.schema.json           plan.schema.json
decision-record.schema.json   role-brief.schema.json
role-model-config.schema.json status-line.schema.json

$ grep -n 'repro' schemas/report.schema.json
grep: schemas/report.schema.json: No such file or directory

$ grep -n '"report"' src/commands/validate.ts
(no output; TYPE_TABLE has no report row, src/commands/validate.ts:53 to :81)
```

`schemas/report.schema.json` is M3-P4's deliverable and the plan states
`blocked-by: M3-P4 merged` for this phase. M3-P4 is in flight on another
branch and has not merged, so the file does not exist at this head. It is also
absent from delivery/plan/phase-declarations/m3-p5.json:4, so creating it here
would be an undeclared file and a red `scope` gate, and editing another phase's
module from this one is the CR-521 shape the phase's own grounding forbids.

So criterion 6 and its behavior `investigator-report-requires-repro` are
DEFERRED to a follow-up on the M3-P4 base, not CI-deferred and not silently
dropped. What this phase CAN carry, and does, is the brief-side half: R-015a's
and R-092's clauses in roles/investigator.md:1, which the clause map resolves.
The behavior is deliberately NOT registered in test/behaviors.json, because
registering a behavior with no test resolving it by name is a red `suite` gate
(src/gates/suite.ts:1038), and registering one is exactly the false green this
repository has paid for elsewhere. It is reported to the orchestrator instead.

This is stated as an incompleteness rather than worked around. Thirteen of the
fourteen declared behaviors are delivered.

## 2026-08-12 The registration half: 21 behaviors and 9 witness specs

Committed as 8ddacd0. Two things were missing and neither is cosmetic.

**Behaviors.** test/behaviors.json maps a behavior id to the EXACT reported
test name, and src/gates/suite.ts:1038 reddens any registered behavior no
reported test resolves. Names were extracted from the sources rather than
retyped, because a transcription slip here is a red gate that reads like a
missing test:

```
$ node -e 'for (const f of [...]) for (const m of src.matchAll(/^test\(\s*\n?\s*"((?:[^"\\]|\\.)*)"/gm)) console.log(JSON.stringify(m[1]))'
== test/roles.test.ts               7 names
== test/brief-compose.test.ts       9 names
== test/finding-schema.test.ts      5 names
$ node - <<'X' ... X
added 21 behaviors; total 534
```

Twenty-one, not thirteen: the plan's `new behaviors` list is the floor, and
the three test files report twenty-one tests. The eight beyond the list
(role vocabulary agreement, tier agreement, the clause round trip, anchor
uniqueness, the two usage arms, and the two finding-set arms) are real new
behaviors and are registered by name so nothing in the suite is unaccounted.

**Witness specs.** src/gates/red-witness.ts:288 makes a changed file under
`src/` with no witness spec covering it RED. Settled by reading the source
rather than inferred:

```
$ grep -n 'never not-applicable' src/gates/red-witness.ts
53: *     covering it is RED, never not-applicable (step 7).
288:  // red, never not-applicable. Coverage semantics are decision D-P2-2 in
451:    // never not-applicable and never a guess.
```

This phase changes four files under `src/`: src/roles.ts,
src/commands/brief.ts, src/commands/validate.ts and src/cli.ts. Nine specs
now cover all four. Every `find` string was checked for UNIQUENESS before it
was written into a spec, because the harness replaces EVERY occurrence and a
find that matches twice is a wider mutation than the one declared:

```
$ node - <<'X' (indexOf scan over the 14 planned finds) X
OK   src/roles.ts "    if (count === 0) {"
OK   src/roles.ts "    if (!declared.has(anchor)) {"
OK   src/roles.ts "    if (entry.kind === \"irregular\" || entry.kind === \"unexam"
OK   src/roles.ts "        reason: `mandated-reading path ${declared}: ${entry."
OK   src/roles.ts "    if (entry.kind === \"absent\" || entry.kind === \"dangling\""
OK   src/roles.ts "        reason: `mandated-reading path ${declared} does not "
OK   src/roles.ts "  \"hazard-classes\",\n"
OK   src/roles.ts "  \"acceptance\",\n"
OK   src/commands/brief.ts "  if (phase === undefined) {"
OK   src/commands/brief.ts "      reason: `${options.planFile} declares no phase with id"
OK   src/commands/brief.ts "  if (subcommand === \"compose\") {"
BAD(2) src/commands/validate.ts "  const diagnostics = validateInstance(schema, decoded.value"
OK   src/commands/validate.ts "  [\"role-brief\", \"role-brief.schema.json\"],\n"
OK   src/cli.ts "  [\"brief\", cmdBrief],\n"
```

The BAD row is the reason the check was run. `const diagnostics =
validateInstance(schema, decoded.value);` occurs TWICE in
src/commands/validate.ts, once on the generic path that predates this phase
and once inside the new `validateRoleBrief`. A spec naming it would have
defanged validation for every artifact type, not for role briefs, and would
still have gone red, which is a witness that passes for the wrong reason. The
spec names the two-line form with `loadTypeSchema("role-brief")` above it,
which is unique.

Two structurally different members per spec, and the harness enforces the
distinctness itself (rule (g), src/witness/run.ts:1318 collapses two mutations
sharing a file and a find). Criterion 3b names its two members explicitly,
dropping `hazard-classes[]` and dropping `acceptance[]` from
PHASE_FIELD_ORDER, and witness/brief-compose-phase-projection.json is exactly
those two.

ONE CONSTRAINT SHAPED EVERY SPEC AND IT IS NOT IN THE SCHEMA. A member counts
as red only when EVERY named test fails: src/witness/run.ts:886 is
`red: exitCode !== 0 && failed.length === tests.length`. So a spec naming
three tests whose member reddens two of them is GREEN and reports the witness
as not guarding. That is why the three phase-projection criteria are three
separate witnesses naming one test each rather than one witness naming three,
and it was found by reading the redness predicate rather than by watching a
spec fail.

## 2026-08-12 Criteria walked directly, outside the suite

Toolchain node v26.6.0, `dist/` built, commands run from the worktree root.

Criterion 1:

```
$ for r in investigator plan-writer adversarial-plan-reviewer; do
    node bin/tiphys.ts validate --type role-brief roles/$r.md; echo "$r exit=$?"; done
investigator exit=0
plan-writer exit=0
adversarial-plan-reviewer exit=0
```

Criterion 3, the order and the unknown id:

```
$ node bin/tiphys.ts brief compose --role plan-writer \
    --phase templates/plan.example.yaml --phase-id M9-P1 > composed.md
compose exit=0
$ grep -n '^## Mandated reading, in order\|^# Brief body\|^# Phase M9-P1\|^### ' composed.md
7:## Mandated reading, in order
33:# Brief body
163:# Phase M9-P1
165:### id          180:### verified-root-cause   202:### hazard-classes
168:### branch      183:### steps                210:### migrations
171:### intent      188:### files-to-touch       213:### conflicts-with
174:### grounding   193:### extras               216:### parallelizable
177:### severity    196:### acceptance           219:### citations
                                                 223:### fill-in

$ node bin/tiphys.ts brief compose --role plan-writer \
    --phase templates/plan.example.yaml --phase-id M9-P404
tiphys brief compose: templates/plan.example.yaml declares no phase with id M9-P404
exit=1
```

The fifteen `### ` sections at 165 to 219 are exactly the fifteen names in
`schemas/plan.schema.json`'s phase `required` array, which criterion 3b
requires the ASSERTION to read rather than a hand-written list:

```
$ node -e 'console.log(JSON.stringify(JSON.parse(fs.readFileSync("schemas/plan.schema.json","utf8")).$defs.phase.required))'
["id","branch","intent","grounding","severity","verified-root-cause","steps","files-to-touch","extras","acceptance","hazard-classes","migrations","conflicts-with","parallelizable","citations"]
```

Criterion 7, the clause map, with this phase's seven rows now IN FORCE
(scripts/check-clause-map.mjs:97 makes `roles/plan-writer.md` the anchor):

```
$ node scripts/check-clause-map.mjs
clause-map: green (25 clause-map rows checked)
25 rows checked, 49 pending a phase not yet in force
clause-map exit=0
```

Both ASCII checks, over every file this branch changed EXCEPT the
owner-supplied intake document, which is exempt by path:

```
$ FILES=$(git diff --name-only a7d5686 HEAD | grep -v '^delivery/intake/orchestrated-delivery-process.md$')
$ grep -raP '[^\x00-\x7F]' $FILES ; echo exit=$?
exit=1
$ grep -raP '[\x00-\x08\x0B\x0C\x0E-\x1F]' $FILES ; echo exit=$?
exit=1
```

Twenty-seven files, no match on either check. The `-a` is present on both, per
T-010.

## 2026-08-12 The claim grep, walked hit by hit

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m3-p5.md
38:6427:| R-005 | M3-P5 | `roles/plan-writer.md`: never decides product questions, ...
64:`then` cannot remove an obligation. So the `if`/`then` form has no way to
190:NEXT: test/brief-compose.test.ts, which needs a STAGED kernel root (criterion 2
206:that was never re-observed after an interruption is a claim, not evidence.
280:under `src/` with no witness spec covering it RED, never not-applicable, and
283:## 2026-08-12 CRITERION 6 CANNOT BE DELIVERED FROM THIS PHASE, and why
```

Six hits. Each is settled here rather than left for the next reader.

**38** is not prose: it is a line of captured `grep` output quoting the plan's
own clause-map row. Nothing to settle.

**64**, the predecessor's `if`/`then` claim, is the one hit that was a real
assertion with no adjacent command. It is settled now, and the settling
command is the enumeration rather than the argument:

```
$ node --input-type=module -e 'const m = await import(...src/validate.ts);
    const v=[...m.AUTHORING_VOCABULARY].sort(); console.log(v.join(" "));
    console.log("not:", v.includes("not"), "maxItems:", v.includes("maxItems"));'
$ref additionalProperties const contains enum if items minItems minLength oneOf
pattern properties required then type uniqueItems
not: false maxItems: false
```

The vocabulary is fifteen keywords, and `not` and `maxItems` are both absent
from it, measured. The obligation criterion 5(b) states is "empty array implies
a required sibling", so the `if` must match EMPTINESS; the only cardinality
keyword present is `minItems`, which matches the non-empty case. Restated as
the narrower true sentence: WITHIN THIS CLOSED VOCABULARY I found no `if`/`then`
form expressing it, and `oneOf` does, with the compile output at
delivery/work-history/m3-p5.md:72 as the proof that the chosen form works and
that a `oneOf` branch carrying `required` must declare the property beside it.
Whether some `if` form exists that I did not find is open; the criterion's
parenthetical names `minItems`, and this is a departure from it, recorded as
one.

**190** is the predecessor's own NEXT line, and the staged kernel root it said
was needed exists and works: test/brief-compose.test.ts:71 `stageKernel()`
copies `src`, `bin`, `roles`, `schemas` and `templates` into a `mkdtemp`
directory, symlinks `node_modules` (dependency resolution walks UP, and a
scratch directory under the system temp root has nothing above it), and every
arm removes the directory in a `finally`. The staging is what lets criterion 2
name a path that must NOT exist in the shipped tree and criterion 6c place a
real `mkfifo` at one, without either appearing in `roles/`. Settled by the run
at the head of this section: 9 of 9 green.

**206** and **280** are restatements of rules with their sources cited beside
them (the beacon line, and the three `grep` hits on src/gates/red-witness.ts
pasted above). Neither asserts a property of this code.

**283** is a heading whose next twelve lines are the `ls schemas/` and two
`grep` runs that settle it. The narrower true sentence, and the one the
heading should be read as: `schemas/report.schema.json` is absent at this head,
it is not on delivery/plan/phase-declarations/m3-p5.json:4, and I did not
deliver criterion 6. I am not asserting no route exists; I am asserting that
every route I can see from here either creates an undeclared file or edits
another phase's module, and both are refused by rules this repository already
holds.

## 2026-08-12 RED-WITNESS GATE: green, and the per-member rates

The gate was run against the real branch diff, not simulated:

```
$ node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full \
    --only red-witness --evidence <scratch>/rw2 --base a7d5686 --head HEAD --phase m3-p5
gates: run fca3e7066f32d7c1a3654a0b
gates: 2 registry gate(s) declared verified-by clean-room-checklist and NOT executed by
  this runner: unit-tests-for-changed-service-methods, fixtures-for-changed-component-states
gates: registry gate-registry.yaml mode full
gates: declared 1 applicable 1 verdict 1 green 1 red 0 not-applicable 0 error 0 vacuous 0
gates: every applicable gate is green
EXIT=0
```

Per-witness, read out of the gate's own witness-records.json rather than
summarised. `2/2` is the red rate: the member was applied to a scratch clone
and the named tests failed in BOTH repetitions; `headGreen=true` is the same
tests passing once the head is restored in the same clone.

```
brief-compose-acceptance-rendered      green  2/2 headGreen=true | 2/2 headGreen=true
brief-compose-command-registered       green  2/2 headGreen=true | 2/2 headGreen=true
brief-compose-hazard-classes-rendered  green  2/2 headGreen=true | 2/2 headGreen=true
brief-compose-missing-reading          green  2/2 headGreen=true | 2/2 headGreen=true
brief-compose-non-regular-reading      green  2/2 headGreen=true | 2/2 headGreen=true
brief-compose-phase-projection         green  2/2 headGreen=true | 2/2 headGreen=true
brief-compose-unknown-phase-id         green  2/2 headGreen=true | 2/2 headGreen=true
role-brief-clause-round-trip           green  2/2 headGreen=true | 2/2 headGreen=true
role-brief-frontmatter-validates       green  2/2 headGreen=true | 2/2 headGreen=true
```

Nine new witnesses, eighteen members, every member red twice and green at head.
The five STORED witnesses the diff re-triggered (cli-top-level-error-handler,
gate-registry-type-registered, mode-show-resolves-the-named-mode,
modes-type-registered, validate-operator-path-refusal) were re-evaluated and
all stayed red against their own members, so nothing this phase changed
silently stopped an existing witness guarding its behavior.

`role-brief-clause-round-trip` and `role-brief-frontmatter-validates` were
derived TEXT-ASSERTING by the harness (their named tests read documents outside
`src/` and `test/` and assert on them), which puts them under rule (g)'s strong
form: two structurally different members AND at least one preserving every
asserted string while inverting the behaviour. Both pass it, because their
members mutate `src/` only and leave the asserted documents byte-identical.

Coverage, which is the half that would have been red without any of this:
the nine specs' members between them touch src/roles.ts,
src/commands/brief.ts, src/commands/validate.ts and src/cli.ts, which is every
file this phase changes under `src/`.

## 2026-08-12 Two citations in this file did not resolve, and were fixed

Checked by resolution rather than by eye, which is how they were caught:

```
$ for c in ...; do f=${c%:*}; l=${c##*:}; printf '%-48s %s\n' "$c" "$(sed -n "${l}p" "$f")"; done
scripts/check-clause-map.mjs:97                  (blank line)
delivery/plan/kernel-plan-m3.md:3160             imported here). Deleting either heading makes
```

The first was the PREDECESSOR's, at delivery/work-history/m3-p5.md:46: the
clause-map anchor row is scripts/check-clause-map.mjs:90, not 97. The second was
mine: I cited criterion 6 at a line inside criterion 6b. Both corrected to
scripts/check-clause-map.mjs:90 and delivery/plan/kernel-plan-m3.md:3152, and
both re-checked:

```
$ sed -n '90p' scripts/check-clause-map.mjs
  ["M3-P5", "roles/plan-writer.md"],
$ sed -n '3152p' delivery/plan/kernel-plan-m3.md
  6. `tiphys validate --type report` accepts an investigator report only when a
```

A citation that resolves to a blank line is not a citation, and the gate
resolves the LINE. Worth stating because the file was inherited: verifying an
inherited citation is not optional just because someone else wrote it.

## 2026-08-12 Criteria 4 and 5, walked through the shipped CLI

**Criterion 4, SC-001.** The diff is an ANNOTATION and the shape is the one the
hazard row demands: the cell changes, the original wording moves into a footnote,
and the footnote cites the finding and the decision.

```
$ git diff a7d5686 -- delivery/intake/orchestrated-delivery-process.md
-| **Adversarial plan reviewer** | One review | The plan + the code, nothing else | Edits anything |
+| **Adversarial plan reviewer** | One review | the input report, the plan, and the code [^sc-001] | Edits anything |
+
+[^sc-001]: ANNOTATION, NOT A REWRITE. This cell originally read "The plan + the
+code, nothing else". ... Spec-coherence finding SC-001 recorded the
+contradiction ... Kernel plan v1 decision D-14 settled it ...
```

One table cell and one footnote, which is exactly what the files-to-touch entry
authorises. The registered test that keeps the two documents from diverging
again is `the reviewer's settled visibility string occurs in both the role brief
and the process document's role table`, and it asserts the SAME string constant
against both files plus the footnote's preservation of the original wording, so
a future edit to either one alone is red.

**Criterion 5**, all three dangerous instances plus the two acceptances, through
the shipped command rather than through the schema in a test harness:

```
$ for f in ok a b b-ok c; do node bin/tiphys.ts validate --type finding $D/$f.json; echo "exit=$?"; done
--- ok      (complete set)                        exit=0
--- a       (severity high, no concrete-edit)      INVALID #/findings/0/concrete-edit required property concrete-edit is missing
                                                   exit=1
--- b       (findings: [], no statement)           INVALID # value matches no permitted alternative here
                                                   exit=1
--- b-ok    (findings: [], WITH statement)         exit=0
--- c       (no produced-by)                       INVALID #/produced-by required property produced-by is missing
                                                   exit=1
```

`b` and `b-ok` are the both-directions pair criterion 5(b) asks for, and they
differ in exactly one property. The 5(b) diagnostic names the alternation rather
than the missing property, which is what `oneOf` reports and is a real
consequence of the vocabulary departure recorded above: an `if`/`then` form would
have named `no-findings-statement`. It is a worse message for a correct
rejection, and it is stated here rather than left for a reviewer to notice.

## 2026-08-12 THE SUITE RESULT, with all three axes named

CLAUDE.md's standing warning 12 says a bare "N pass, exit 0" is an incomplete
sentence here, because the total moves on three independent axes. All three are
named:

| axis | value |
|---|---|
| toolchain | node v26.6.0 (the fetched floor-satisfying prefix, first on PATH) |
| build state | `dist/` built (`npm run build` exit 0 immediately before) |
| invocation | `npm test`, which is `node --test "test/**/*.test.ts"` |

```
$ node --version
v26.6.0
$ npm test
i tests 528
i suites 0
i pass 528
i fail 0
i cancelled 0
i skipped 0
i todo 0
i duration_ms 198989.073618
NPM_TEST_EXIT=0
```

528 tests, 528 pass, 0 fail, **0 SKIPPED**, exit 0. The skipped count is quoted
because a skipped test is not a passing test and exit 0 does not distinguish
them. It is zero here because `dist/` was built: the nine dist-dependent tests
in test/gates.test.ts and test/m2-exit-test.test.ts all ran, and the two
floor-gated `doctor` tests ran because the toolchain is above the floor.

TRANSLITERATION DECLARED: U+2139 replaced by `i` at the head of the eight
summary lines, 8 occurrences. Nothing else in the capture was changed.

ONE THING IN THAT LOG THAT IS NOT A NODE SKIP, and it would be easy to misread
as one. The run also prints 30 lines beginning `# skipped <file>.json: ...`.
Those are VENDORED JSON-Schema-Test-Suite subtests declining themselves from
inside a passing test (the reasons name DR-0013 clause 4's refusal to fetch
remote identifiers, and the closed authoring vocabulary), not `node --test`
skips. The reporter's own `skipped` count is the authority and it is 0. Named
here because "30 skips in the log, 0 in the summary" is exactly the kind of
unexplained gap this repository has three times refused to average away.

## 2026-08-12 The citations gate is NOT-APPLICABLE here, and the reason is worth writing down

I expected this branch to be citation-gated, because CLAUDE.md warns that a new
`delivery/` document with zero substantive citations is RED and this branch adds
one. Run rather than assumed:

```
$ node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full \
    --only citations --evidence <scratch>/cite --base a7d5686 --head HEAD --phase m3-p5
gates: declared 1 applicable 0 verdict 0 green 0 red 0 not-applicable 1 error 0 vacuous 0
gates: no applicable gate
EXIT=21
```

The gate's own result.json says why, and the wording is the gate's, not mine:

```
"status": "not-applicable",
"detail": "no changed path under the configured documents globs (28 changed path(s)
  total). The diff-touches precondition is a path prefix and the documents config is
  a glob set, so a changed path under a configured tree that is not a configured
  document reaches here with the precondition met"
```

The precondition (gate-registry.yaml:115) is a PREFIX list that includes
`delivery/requirements/`, which this branch touches, so the precondition is MET;
the documents glob set then matches none of the 28 changed paths, and
`delivery/work-history/` is not on the prefix list at all. So a work history is
not a citation-gated document in this repository, and exit 21 here is the
runner's "no applicable gate", not a red.

That does NOT make the citation form optional in this file, and it is the reason
the two non-resolving citations above were found by resolving them by hand
instead of by waiting for a gate. A check that does not run is not a check that
passed.

## 2026-08-12 The intake-document exemption, measured on both sides of the edit

The exemption is scoped BY PATH, never by judgment, and the way to show the
annotation did not smuggle anything in is to count the same property before and
after:

```
$ git show a7d5686:delivery/intake/orchestrated-delivery-process.md | grep -caP '[^\x00-\x7F]'
50
$ grep -caP '[^\x00-\x7F]' delivery/intake/orchestrated-delivery-process.md
50
$ grep -caP '[\x00-\x08\x0B\x0C\x0E-\x1F]' delivery/intake/orchestrated-delivery-process.md
0
```

Fifty non-ASCII lines before the edit and fifty after: the SC-001 footnote and
the changed table cell are pure ASCII, and nothing owner-supplied was
transliterated. Zero control characters.

Tree-wide, with the vendored fixture tree excluded by path and the `-a` present:

```
$ git ls-files | grep -v '^test/fixtures/json-schema-test-suite/' > tracked.txt
$ xargs -a tracked.txt grep -laP '[\x00-\x08\x0B\x0C\x0E-\x1F]'
(no output)
```

No tracked file outside the vendored tree carries a control character, which is
the T-010 check in the form that can actually see NUL.

## 2026-08-12 THE SUITE GATE WENT RED, and the cause is load, proven not asserted

The full bundle's `suite` gate came back RED with one finding:

```
"gate":"suite","status":"red","units":528,"unitLabel":"tests reported",
"detail":"1 finding(s): failing test: \"a resident watcher keeps running and backs
  off with growing beacon gaps\" (test/watcher.test.ts)"
```

The same suite had reported 528 pass, 0 fail, 0 skipped forty minutes earlier.
Two runs of one head disagreeing is exactly the thing this repository refuses to
average, so it was measured rather than explained away.

**What I did wrong, first, because it is the cause.** I launched a second gate
runner (the `--only citations` pre-check) at 00:36:03 WHILE the bundle's suite
gate was running from 00:34:37 to 00:38:57. This container has four CPUs. The
failing test is a REAL-CLOCK measurement: test/watcher.test.ts:269 samples the
watcher's beacon for a fixed 4200ms window and requires at least four beacon
writes to land inside it. Starve the box and fewer land.

**The diagnosis is a claim until it reddens on demand, so it was forced.** Same
head, same toolchain, same build state, only the machine load differing:

```
--- ALONE (control), five consecutive runs, all exit 0; the fifth shown ---
$ node --test --test-name-pattern '^a resident watcher keeps running and backs off with growing beacon gaps$' test/watcher.test.ts
+ a resident watcher keeps running and backs off with growing beacon gaps (4769.812079ms)
i tests 1
i pass 1
i fail 0
i skipped 0
ALONE_EXIT=0

--- UNDER LOAD: 12 busy loops on 4 CPUs, then the same command ---
x failing tests:
test at test/watcher.test.ts:269:1
x a resident watcher keeps running and backs off with growing beacon gaps (7187.370573ms)
  AssertionError [ERR_ASSERTION]: expected at least 4 beacon writes, saw 3
      at TestContext.<anonymous> (file://.../test/watcher.test.ts:293:10)
```

`expected at least 4 beacon writes, saw 3` is the assertion at
test/watcher.test.ts:293, and it is the SAME failure the gate reported. Green
five times alone, red on demand under contention: that is both directions.

TRANSLITERATION DECLARED in the two captures above: U+2714 rendered `+` (1
occurrence), U+2716 rendered `x` (3 occurrences), U+2139 rendered `i` (4
occurrences). Nothing else was changed.

**And the failure cannot be this phase's, because this phase does not touch that
code.** Not argued, hashed:

```
$ git diff --name-only a7d5686 HEAD | grep -E 'watch|beacon'
(no match, grep exit 1)
$ for f in test/watcher.test.ts src/watcher.ts; do ... sha256 base vs head ... done
test/watcher.test.ts       base=d5a47e9c38af head=d5a47e9c38af
src/watcher.ts             base=188743e1cb61 head=188743e1cb61
```

Both files are byte-identical to the merge base. The branch changes no watcher
source and no watcher test.

**What I am NOT claiming.** I am not claiming the test is wrong, and I am not
claiming it can only fail under load. I forced one failure mode and it matched
the gate's finding exactly; whether some other path reaches the same assertion
is open. What is settled is that this branch is not that path. CLAUDE.md's
standing warning 11 already records that suite wall time grows with real-clock
waits, and this is that warning with a measured cost attached.

The operational lesson, which is mine and worth the tuition: **do not run a
second gate runner against the same checkout while a suite gate is in flight.**
The bundle is re-run below with nothing else on the box.

## 2026-08-12 The first bundle in full, for the record

Every gate of the run whose `suite` went red, so the other eleven verdicts are
not lost behind the one that failed:

```
gates: run edd49feba2207471e023bdc3
gates: registry gate-registry.yaml mode full
gates: declared 12 applicable 8 verdict 8 green 7 red 1 not-applicable 4 error 0 vacuous 0
gates: 1 gate(s) reported red: suite
EXIT=1

manifest-self-check    green            8    schema documents validated
coverage               green            115  finding ids checked
credential-scrub       green            7    credential sources probed
credential-token       not-applicable   0    tokens probed
suite                  RED              528  tests reported
citations              not-applicable   0    citations resolved
scope                  green            28   changed paths audited
deploy                 not-applicable   0    release verifications satisfied
migrations             not-applicable   0    migrations compared
clause-map             green            25   clause-map rows checked
red-witness            green            14   witnesses evaluated
agent-rules-drift      green            17   rendered gate rows compared
```

`scope` green over 28 changed paths is the declaration audit passing:
delivery/plan/phase-declarations/m3-p5.json:4 plus the two standing extras
(test/behaviors.json and this file) cover every path this branch touches, and
`witness/` is on the declaration as a directory entry, which is what lets the
nine new specs land inside scope.

`red-witness` green at 14 is the nine new witnesses plus the five stored ones
the diff re-triggered.

A second bundle is running with nothing else on the box, to settle the `suite`
arm at the same head. Both runs are reported, because reporting only the one
that came out the way I wanted is exactly the T-009 failure of quoting a green
without naming the run that produced it.

## 2026-08-12 SECOND BUNDLE, nothing else on the box: suite GREEN at the same head

Same head, same toolchain, same build state, same command; the only thing that
changed is that no second process was competing for the four CPUs.

```
suite                  green            528 tests reported
```

Green at 528 tests reported, at the head the first bundle called red. Two runs,
one head, opposite verdicts, and the difference is measured rather than assumed:
the failing assertion is a real-clock beacon count and the box was starved.

Stated the T-009 way, because a bare "the gates are green" is the incomplete
sentence: the complete one names the run. Run edd49feba2207471e023bdc3 (with a
concurrent gate runner) reported `suite` RED; run over evidence directory
`full2` (alone) reported `suite` GREEN. Both were at commit `fb694a3` on
claude/m3-p5-authoring-role-briefs, and the SECOND is the one whose
configuration matches how CI runs the gate.

## 2026-08-12 THE FULL GATE BUNDLE, final, with its real exit code

```
$ node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full \
    --evidence <scratch>/full2 --base a7d5686 --head HEAD --phase m3-p5
gates: run d0ee0d4de6cd49e52bc49d7e
gates: 2 registry gate(s) declared verified-by clean-room-checklist and NOT executed by
  this runner: unit-tests-for-changed-service-methods, fixtures-for-changed-component-states
gates: registry gate-registry.yaml mode full
gates: declared 12 applicable 8 verdict 8 green 8 red 0 not-applicable 4 error 0 vacuous 0
gates: required gate(s) not applicable: citations
EXIT=20
```

| gate | status | units |
|---|---|---|
| manifest-self-check | green | 8 schema documents validated |
| coverage | green | 115 finding ids checked |
| credential-scrub | green | 7 credential sources probed |
| credential-token | not-applicable | 0 tokens probed |
| suite | green | 528 tests reported |
| citations | not-applicable | 0 citations resolved |
| scope | green | 28 changed paths audited |
| deploy | not-applicable | 0 release verifications satisfied |
| migrations | not-applicable | 0 migrations compared |
| clause-map | green | 25 clause-map rows checked |
| red-witness | green | 14 witnesses evaluated |
| agent-rules-drift | green | 17 rendered gate rows compared |

**Eight green, zero red, zero error, zero vacuous. THE EXIT CODE IS 20, NOT 0,
and I am reporting the number rather than the adjective.** `EXIT_NOT_APPLICABLE`
is 20 (src/gates/result.ts:67) and the runner returns it when a gate whose
applicability is `required` reports not-applicable (src/gates/run.ts:1191). That
is `citations`, for the reason measured above.

**Whether 20 fails CI is a question I answered rather than assumed, because the
honest report needs it.** CI does not compare the runner's exit code to zero. It
records the code and asserts PER-GATE STATUS against an expectation table
(scripts/m2-exit-test.sh:891 captures `rc` with `|| rc=$?`, and
scripts/m2-exit-test.sh:829 is the table). The PR bundle's row is:

```
{"id": "citations", "expect": "green|not-applicable", "required": true, "diffScoped": true},
```

and the comment immediately above it (scripts/m2-exit-test.sh:827) says
red-witness and citations "stay green|not-applicable on every run because each
CAN be legitimately N/A on a head that does not touch its trigger". The
diff-scoped handling additionally requires the not-applicable to carry an
EVALUATED precondition rather than a silent skip, which this one does:

```
$ node -e '...full2/citations/result.json...'
status: not-applicable | precondition present: true | met: false |
  id: citations-diff-touches-a-configured-document
```

So `not-applicable` here is an accepted PR-bundle status, not a failure. Two
further differences between this run and CI's, stated so nobody reads my run as
CI's: CI invokes the runner with `--manifest gates.manifest.json` and not
`--registry` (which is the R-094 half CLAUDE.md records as NOT delivered), and CI
runs BOTH the pull_request and the push arms while this is one local run of one
arm. Under T-009, my green is evidence for exactly the configuration it ran
under, and the post-merge push run on the new `main` head remains unwitnessed
here and belongs to the orchestrator.

## 2026-08-12 HANDBACK

Branch claude/m3-p5-authoring-role-briefs, pushed, no PR opened and nothing
merged, as instructed. Remaining for the orchestrator: the base has moved
(a macOS portability pilot landed after a7d5686) and I did not merge main.

Delivered: criteria 1, 2, 3, 3b, 4, 5, 6b, 6c and 7, with the evidence above.
NOT delivered: criterion 6 and its behavior `investigator-report-requires-repro`,
blocked on M3-P4's unmerged `schemas/report.schema.json`, recorded in full above
rather than worked around.

## 2026-08-12 The claim grep, re-run on the finished file

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m3-p5.md
```

Eleven hits now. Six were walked in the earlier section and are unchanged.
The five added since:

- **358 to 360** are lines of captured `grep` output from
  src/gates/red-witness.ts. Quoted, not asserted.
- **485 to 491** are the earlier claim-grep capture quoting itself. Same.
- **754**, "the exemption is scoped BY PATH, never by judgment", restates
  CLAUDE.md's own rule and is followed immediately by the two counts that
  exercise it.
- **832**, "the failure cannot be this phase's, because this phase does not
  touch that code", is a real claim and it carries its settling command in the
  next four lines: `git diff --name-only a7d5686 HEAD | grep -E 'watch|beacon'`
  returns nothing, and both watcher files hash identically at base and head.
  The narrower form, which is the one I will defend: those two files are
  byte-identical to the merge base, so no edit on this branch reaches them.

No hit is left standing without either a captured command beside it or a
restatement as an open question.


## 2026-08-12 SECOND IMPLEMENTER, criterion 6 unblocked: the merge

Status: IN PROGRESS again. Everything above this line is the first
implementer's account and is not edited. I am appending.

The blockage recorded at delivery/work-history/m3-p5.md:283 was real and its
two causes are both gone: M3-P4 merged, so schemas/report.schema.json exists
on `main`, and this phase's declaration was amended on `main` to include it
(delivery/plan/phase-declarations/m3-p5.json:7). The scope auditor reads the
declaration from the MERGE BASE, so the amendment had to land on `main` before
this branch could touch the file. It has.

First action, per the dispatch: merge origin/main.

```
$ git log --oneline -1 origin/main
52fe657 M3-P4 closeout evidence, and amend M3-P5's declaration for criterion 6 (#95)
$ git merge origin/main --no-edit
Auto-merging delivery/requirements/clause-map.json
CONFLICT (content): Merge conflict in delivery/requirements/clause-map.json
Auto-merging src/commands/validate.ts
CONFLICT (content): Merge conflict in src/commands/validate.ts
Auto-merging test/behaviors.json
CONFLICT (content): Merge conflict in test/behaviors.json
Automatic merge failed; fix conflicts and then commit the result.
MERGE EXIT: 1
```

Three conflicts. Two are the append-only registries the conventions name
(CLAUDE.md's convention 5), and they resolve as a UNION verified by key
accounting, never by picking a side. The third, src/commands/validate.ts, is
real code and is resolved by reading both sides.

### Union resolution, verified by key accounting rather than by inspection

test/behaviors.json is a flat object of behavior name to description, so the
union is over KEYS and the accounting is arithmetic. Captured:

```
$ node -e '<read :1 :2 :3 of test/behaviors.json, compare key sets>'
base 513 ours 534 theirs 568
ours added: 21
theirs added: 55
ours deleted: []
theirs deleted: []
value disagreements on shared keys: []
union size: 589 expected: 589
```

513 + 21 + 55 = 589, both sides deleted nothing, and no key that exists on both
sides carries a different description. So the union loses nothing from either
side, which is the property "keep BOTH sides" actually means.

delivery/requirements/clause-map.json, same method:

```
base 18 ours 25 theirs 27
ours added: [ 'R-004', 'R-005', 'R-006', 'R-010a', 'R-015a', 'R-029', 'R-092' ]
theirs added: [ 'R-035', 'R-049', 'R-052a', 'R-057a', 'R-083a', 'R-085',
                'R-086', 'R-088', 'R-089a' ]
ours deleted: []
theirs deleted: []
value disagreements: []
union size: 34 expected: 34
```

18 + 7 + 9 = 34. Ours are this phase's seven rows, theirs are M3-P4's nine.

src/commands/validate.ts is NOT a registry and was resolved by reading. Both
sides append rows to `TYPE_TABLE`: M3-P4 adds `report`, `final-report` and
`work-history` with the companion-table note, M3-P5 adds `role-brief` and
`finding` with the `--type auto` note. The rows are disjoint and both comment
blocks are kept, M3-P4's first because its `COMPANION_TABLE` note below refers
to it. `report` resolving to report.schema.json in that table is what makes
criterion 6's `tiphys validate --type report` reachable at all.

## 2026-08-12 CRITERION 6: what was built, and every design choice derived

The criterion (delivery/plan/kernel-plan-m3.md:3152) is a Kind A `if`/`then`
over one document, both directions. The top-level `if`/`then` slot of
schemas/report.schema.json:1 was FREE before this: M3-P4 spent its conditionals
inside `$defs` (finding, evidence, claim, gateResult) and wrote none at the
root, so nothing had to be displaced.

The rule, in one sentence: an investigator report that carries a verdict owes a
`repro`.

### Derivation 1: why the condition names the role, and why with a PATTERN

`role` is documented in that schema as "the role brief this report answers",
and the brief ids are a CLOSED enum of six at schemas/role-brief.schema.json:22.
Run against that enum rather than assumed:

```
$ node -e '<read schemas/role-brief.schema.json, test each enum member against
            the investigator pattern>'
false  orchestrator
true   investigator
false  plan-writer
false  adversarial-plan-reviewer
false  implementer
false  clean-room-reviewer
matched: 1 of 6
```

Exactly one of the six matches, so the pattern cannot over-trigger on any role
this process defines. It is a PATTERN and not a `const` so that `Investigator`
and `investigator, round 2` fall INSIDE the rule rather than outside it; every
letter is a two-member character class because ECMA-262 patterns here carry no
flags, which is the reason `$defs/universalQuantifier` already gives at
schemas/report.schema.json:95.

### Derivation 2: why the role is in the condition AT ALL

The obvious simplification is to drop `role` and say "any report with a verdict
owes a repro". That was BUILT AND RUN rather than reasoned about. The variant
schema is the shipped one with `if.required` cut to `["verdict"]` and
`if.properties.role` deleted, validated against this repository's own shipped
example:

```
$ node ./unscoped.mjs
INVALID # value does not satisfy the requirements its own shape triggers here
INVALID #/repro required property repro is missing
```

templates/report.example.yaml:13 is `role: implementer` with a verdict at
templates/report.example.yaml:19 and no repro, and it validates today (exit 0,
captured below). So the unscoped rule rejects the repository's own worked
example of an honest record. That is the measurement behind the scoping, and it
is a measurement rather than a preference.

### Derivation 3: why the condition is verdict PRESENCE and not verdict WORDING

A root-cause-token pattern on `verdict` was the other candidate. It is worse
HERE, and the reason is a property of this role rather than of verdicts
generally: roles/investigator.md:31 makes the root-cause verdict the role's only
output and roles/investigator.md:17 lists `report` as its only artifact, so for
an investigator report "has a verdict" and "has a root-cause verdict" name the
same set of reports. A token list would add a second escape (word the verdict
without the token) and buy nothing.

This CONTRADICTS a sentence M3-P4 shipped. schemas/report.schema.json:39 said
"an investigation report has no verdict and a review report does". Under that
reading the new rule would never fire on an investigator report at all, which
is the green-and-worthless shape. roles/investigator.md:31 is the later and
more specific statement, and it is a file this phase ships, so the comment at
schemas/report.schema.json:39 is CORRECTED IN PLACE rather than left standing
beside a rule that assumes the opposite. The field stays optional either way.

### The two residues, stated because neither is reached by any keyword

1. An investigator who states the cause somewhere other than `verdict`, in
   `findings[].analysis` for instance, carries no `verdict`, so the rule does
   not fire and no repro is owed. Closing it would mean requiring a repro of
   every report carrying findings, which is every review report this process
   produces, and that makes the honest record unwritable.
2. A report whose `role` is outside the six, or misspelt past the pattern,
   escapes.

### The `repro` shape: two branches, and why one branch would have been a defect

`$defs/repro` is a `oneOf` over the two outcomes the two requirement rows
describe. delivery/requirements/migration-table.md:39 (R-015a) demands a
runnable repro that is red on current code. delivery/requirements/migration-table.md:165
(R-092) says what to do when it will not reproduce (ship the harness and say
so), and that row's own note calls R-092 the complement of R-015a.

A ONE-BRANCH `repro` would have enforced R-015a by making R-092's honest record
UNWRITABLE, leaving an investigator who could not force the arm with two bad
options: fabricate a repro, which validates and is invisible, or omit the
verdict, which loses the investigation. That is the authoring-cost hazard the
document's own header names. It is also the shape M3-P4 paid four rounds for at
`$defs/claim`: its round-2 pattern refused the honest open question and round 4
had to add a fourth branch for records a truthful author writes.

Branch 1 (it reproduced) requires `command`, `exit-code`, `red-against` and
`not-covered`. Each is a sentence in the clause it discharges, not a field
invented for shape: roles/investigator.md:67 ("a command someone else can run,
which fails now"), roles/investigator.md:75 (red against the DANGEROUS STATE,
say which state you drove the system into), roles/investigator.md:80 ("State
what the repro does NOT cover").

Branch 2 (it did not reproduce) requires `did-not-reproduce`, `harness` and
`arms-that-stayed-green`, from roles/investigator.md:92, which asks for the
scaffolding, the exact commands, the states driven into and the arms that
stayed green.

**One thing deliberately NOT copied, and it is the round-4 lesson.** The
impossibility-token pattern at `$defs/claim`'s third `oneOf` branch is the
obvious thing to put on `did-not-reproduce`, since roles/investigator.md:94
tells the investigator to write "I did not find a way to force this arm" and
roles/investigator.md:95 tells them not to write "this arm cannot be forced".
It is NOT copied. M3-P4 fix round 4 measured four honest records that the
identical pattern refused outright at the sibling site, and had to add a whole
branch to make them writable. Reproducing a defect that cost four members and a
fix round, in a field whose whole purpose is that the honest non-reproduction
stays cheap, is not a trade worth making. What that leaves open is stated in
the branch's own comment: "this arm cannot be forced" validates here, the brief
refuses it in prose, and M3-P7's checklist is what reads it.

### What the schema CANNOT do, examined rather than counted

This is a floor, not a total. Three members were examined:

1. No keyword makes `exit-code` NONZERO, so a repro recording `exit-code: 0`
   validates while claiming to be red. This is not an omission: the closed
   authoring vocabulary at src/validate.ts:111 is `$ref additionalProperties
   const contains enum if items minItems minLength oneOf pattern properties
   required then type uniqueItems`, with no `not` and no numeric bound, so the
   constraint could not have been written.
2. Nothing relates `command` to the `exit-code` recorded beside it, which is
   the third member of the class M3-P4 names at
   schemas/report.schema.json:1 under `$defs/fixRound`: relating an output to
   the command that produced it needs the command to be RUN.
3. Nothing can tell a true `red-against` or `not-covered` from a false one.

### One thing the strict policy taught, recorded because it cost a cycle

The first version of the `if` declared `required: ["role", "verdict"]` and gave
`properties` for `role` alone. The whole document then failed to compile:

```
$ node ./ajvprobe.mjs schemas/report.schema.json
RAW: strict mode: required property "verdict" is not defined at
"https://tiphys.dev/schemas/report.schema.json#/if" (strictRequired)
```

and through the CLI it surfaced as the one-line `INVALID # schema is refused by
this validator's strict policy`, which names no keyword and no pointer. That
diagnostic is deliberate (src/validate.ts:227 says every entry carries an
identifier and never an Ajv sentence), and the cost is that finding the cause
needs Ajv run directly. Every `if` in this document declares its own required
names for that reason. `verdict` is declared as bare `type: string`, the widest
subschema that satisfies the policy, so the condition stays PRESENCE.

## 2026-08-12 CRITERION 6: both directions, through the real CLI

Toolchain node v26.6.0 on PATH, checked in the shell that ran each command.
Fixtures under the evidence directory, built from the shape roles/investigator.md:63
describes.

### Direction 1, ACCEPTED: an investigator verdict with a repro behind it

```
$ node bin/tiphys.ts validate --type report <accepted.yaml>
ACCEPTED-ARM exit 0
```

### Direction 2, REJECTED: THE SAME REPORT with the reference removed

The criterion's words are "rejects the same report with the reference
removed", so the rejected fixture is DERIVED FROM the accepted one by deleting
the `repro` block and nothing else. The diff is the whole edit:

```
$ diff accepted.yaml rejected.yaml
10,20d9
< repro:
<   command: >-
<     taskset -c 0 node --test test/lock.test.ts
<   exit-code: 1
<   red-against: >-
<     A single-core cgroup with three busy-loop children already running, which
<     is the contended state; verified by reading /proc/loadavg before the run
<     and seeing it above 4.
<   not-covered: >-
<     Only the lease tests were driven. The gate wrapper's own timeout path and
<     every non-lease test file were untouched by this repro.

$ node bin/tiphys.ts validate --type report <rejected.yaml>
INVALID # value does not satisfy the requirements its own shape triggers here
INVALID #/repro required property repro is missing
REJECTED-ARM exit 1
```

The diagnostic NAMES `repro`, which is why the rule is an `if`/`then` and not a
bare `oneOf`; a `oneOf` alone reports only that no alternative matched.

### The three records that must STAY writable, each run

```
--- HONEST A: investigator report with no verdict ---
$ grep -c "verdict:" honest-no-verdict.yaml
0
$ node bin/tiphys.ts validate --type report honest-no-verdict.yaml
exit 0

--- HONEST B: R-092 non-reproduction with a harness ---
$ node bin/tiphys.ts validate --type report honest-did-not-reproduce.yaml
exit 0

--- THE SHIPPED EXAMPLE, role implementer with a verdict and no repro ---
$ node bin/tiphys.ts validate --type report templates/report.example.yaml
template exit 0
```

### The dangerous instance a PRESENCE check would have passed

```
--- DANGEROUS MEMBER 2: repro present but hollow ---
$ node bin/tiphys.ts validate --type report dangerous-hollow.yaml
INVALID # value does not satisfy the requirements its own shape triggers here
INVALID #/repro value matches no permitted alternative here
exit 1
```

## 2026-08-12 RED-WITNESS, strong form, two structurally different members

Behavior `investigator-report-requires-repro`, registered in test/behaviors.json:1
and specified in witness/investigator-report-requires-repro.json:1. ONE named
test carries both members, because src/witness/run.ts:886 counts a member red
only when EVERY named test fails, so a member whose mutation reddened a
different test would not count.

Baseline, head state:

```
$ node --test --test-name-pattern "an investigator report with a verdict is
  rejected without a repro and with a hollow one, and accepted with a real one"
  test/finding-schema.test.ts
tests 1 / pass 1 / fail 0 / skipped 0
exit 0
```

MEMBER 1, THE OMISSION. Guarding keyword `then.required`, emptied:

```
MEMBER 1 applied: "required": ["repro"], -> "required": [],
--- member 1, repetition 1 ---
  AssertionError [ERR_ASSERTION]: Expected "actual" not to be strictly deep-equal to: []
exit 1
--- member 1, repetition 2 ---
  AssertionError [ERR_ASSERTION]: Expected "actual" not to be strictly deep-equal to: []
exit 1
```

MEMBER 2, THE HOLLOW PRESENCE. Guarding keyword is branch 1's own `required`
inside `$defs/repro`, narrowed to the one field the hollow instance carries:

```
MEMBER 2 applied: "required": ["command", "exit-code", "red-against", "not-covered"], -> "required": ["command"],
--- member 2, repetition 1 ---
  AssertionError [ERR_ASSERTION]: Expected "actual" not to be strictly deep-equal to: []
exit 1
--- member 2, repetition 2 ---
  AssertionError [ERR_ASSERTION]: Expected "actual" not to be strictly deep-equal to: []
exit 1
```

Restored, and green again:

```
restored
--- restored green ---
exit 0
```

**THE TWO MEMBERS ARE STRUCTURALLY DIFFERENT AND THAT IS MEASURED, not
asserted.** They fail at DIFFERENT ASSERTIONS in the same test, which is the
mechanical form of the "one witness is not a class" rule:

```
=== member 1: which assertion failed ===
      at TestContext.<anonymous> (.../test/finding-schema.test.ts:307:10)
=== member 2: which assertion failed ===
      at TestContext.<anonymous> (.../test/finding-schema.test.ts:333:10)
```

test/finding-schema.test.ts:307 is the omission arm and test/finding-schema.test.ts:333
is the hollow arm. Member 2 is the DANGEROUS state rather than an absent
feature in the sense T-003 means: `repro` IS present, so `then.required` is
satisfied and a guard that tested only for the field's presence is green on it,
which is the same shape as the T-008 watchdog that tested existence instead of
freshness. What refuses it is a different keyword, one level down, in the
referenced definition.

Both mutations are on schemas/report.schema.json and neither touches
src/ or bin/, so the coverage rule at src/gates/red-witness.ts:145, which audits
`src/` and `bin/` only, is not what this spec discharges; it discharges the
behavior registration.

**The restore was by copy from a pristine snapshot taken before the first
mutation, never by `git checkout --`**, per the standing warning: this tree
held uncommitted work at the time and there is no safe narrow form.
