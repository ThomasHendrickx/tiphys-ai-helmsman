# M4-P1 (remainder): the three harness questions the first eight probes left open

- phase: M4-P1, the remainder
- branch: `claude/m4-p1-harness-probe`
- cut from: `plan/pstack-borrow-review`
- date started: 2026-09-15
- status: IN PROGRESS. This file is appended to as work happens (CLAUDE.md's
  T-008 beacon rule, CLAUDE.md:443).

## What this unit is, and what it is not

Most of M4-P1 already ran. Eight probe agents produced
delivery/verification/m4-prototype-probes.md:1 and ten beacons under
`delivery/evidence/m4-probes/`. This unit closes THREE remaining questions and
AMENDS that document rather than creating a rival one.

- Q1 (BLOCKING M4-P9): do `PreToolUse` hooks fire under a bypass permission
  mode?
- Q2: can a harness-primitive-backed adapter distinguish launch-failed from
  incomplete?
- Q3: is transcript-based model resolution STABLE, specifically with several
  subagents in flight, and can an agent write to its own transcript?

## The failure arms, written BEFORE any run

Every one of the three questions can come back as "not established", and that
is a result rather than a failure. The arms are enumerated first, and the
commit that contains them (1c09598) predates every run in this document, which
a reader can check with `git log --oneline delivery/work-history/m4-p1.md`.

**Q1 has FOUR distinguishable outcomes and only one of them is "the hook
fires":**

1. HOOK FIRES UNDER BYPASS. The marker file exists after the run and the
   transcript shows the tool was attempted. M4-P9 may stay in hooks.
2. HOOK DOES NOT FIRE UNDER BYPASS. The run completes, the tool ran, and the
   marker file is ABSENT. M4-P9's write-block must move out of hooks.
3. THE RUN NEVER HAPPENED. The CLI refuses the flag, or the session's own
   permission classifier refuses the command. Verdict is "not established" plus
   the refusal text, NOT an answer in either direction. This is the arm the
   first probe hit (`delivery/evidence/m4-probes/pretooluse-write-block.md`
   "WHAT I COULD NOT TEST" item 1).
4. THE CONTROL IS ALSO SILENT. If the ordinary-mode control produces no marker
   either, the lab is broken and NOTHING can be read off the bypass arm. The
   control is what separates outcome 2 from outcome 4, and without it a missing
   marker is uninterpretable.

**The control is mandatory and it is the load-bearing half.** A missing marker
file is the same observation for "the hook was suppressed" and for "my plugin
never loaded". Only the ordinary-mode arm, same plugin, same prompt, same
marker path, tells them apart.

**Q2's failure arm:** if a forced launch failure and a forced mid-turn
abandonment produce the SAME observable (same exit code, same absence of
output), then the answer is NO and that is the finding. If they differ, the
answer is what the discriminator actually is, quoted, not a claim that one
exists.

**Q3's failure arms:** for the several-subagents half, if the parent transcript
does not name per-subagent paths, or if two subagents collide on one path, that
is the answer. For the self-write half, a SUCCESSFUL write by the agent into its
own transcript is the DANGEROUS state and it is the result that matters; a
refusal must be shown to be a real refusal (captured error) and not an untried
path.

## Q1, THE BLOCKING QUESTION: `PreToolUse` hooks DO fire under a bypass permission mode, and `exit 2` still blocks

**Answer: YES, measured, with a control on both sides and two structurally
different tools. M4-P9 may stay in a `PreToolUse` hook, for the FILE-TOOL half
that probe 10 already found sound.** Nothing here rehabilitates the Bash half,
and section "what this does NOT cover" below says so in those words.

### The lab

A minimal plugin, built in a scratch directory and never in the repository
tree. The whole plugin is three files and all three are committed verbatim:
test/fixtures/harness-probe/q1-bypass-permission-mode/plugin-manifest.json.txt:1,
test/fixtures/harness-probe/q1-bypass-permission-mode/plugin-hooks.json.txt:1
and
test/fixtures/harness-probe/q1-bypass-permission-mode/plugin-hook-marker.mjs.txt:1.
The runner is
test/fixtures/harness-probe/q1-bypass-permission-mode/run-arm.sh.txt:1.

The hook appends ONE JSON line to a marker file and then exits with a code the
arm chooses. The marker file is the beacon: its presence is the only evidence
that the hook fired at all, and the line it writes carries the harness's own
`permission_mode` field, which is what makes each arm self-verifying rather
than self-reported.

**The verdict in every arm is the md5 of `app.txt`, not the model's word.** The
model's one-word answer is recorded beside it and is never the evidence.

### The matrix, every row a real `claude -p` run

`claude --version` 2.1.273 (Claude Code), binary `/opt/claude-code/bin/claude`,
node v22.22.2 at `/opt/node22/bin/node`. Plugin loaded per session with
`--plugin-dir`.

| arm | how bypass was asked for | `permission_mode` the HOOK saw | hook exit | marker | app.txt | fixture |
|---|---|---|---|---|---|---|
| control | `--permission-mode acceptEdits` | `acceptEdits` | 0 | YES | CHANGED | `control-acceptEdits-exit0.summary.txt` |
| control | `--permission-mode acceptEdits` | `acceptEdits` | 2 | YES | unchanged | `control-acceptEdits-exit2.summary.txt` |
| permissive | `--permission-mode dontAsk` | `dontAsk` | 2 | YES | unchanged | `dontAsk-exit2.summary.txt` |
| **REFUSED** | `--permission-mode bypassPermissions` | (never ran) | 0 | NO | unchanged | `bypass-mode-exit0.summary.txt` |
| **REFUSED** | `--dangerously-skip-permissions` | (never ran) | 2 | NO | unchanged | `dsp-flag-exit2.summary.txt` |
| **SILENT DOWNGRADE** | `--settings '{"permissions":{"defaultMode":"bypassPermissions"}}'` | **`default`** | 2 | YES | unchanged | `settings-defaultMode-bypass-exit2.summary.txt` |
| **BYPASS** | `--permission-mode bypassPermissions` | **`bypassPermissions`** | 2 | YES | **unchanged** | `bypass-exit2.summary.txt` |
| **BYPASS control** | `--permission-mode bypassPermissions` | **`bypassPermissions`** | 0 | YES | **CHANGED** | `bypass-exit0.summary.txt` |
| **BYPASS, Bash** | `--permission-mode bypassPermissions` | **`bypassPermissions`** | 2 | YES | **unchanged** | `bypass-bash-exit2.summary.txt` |
| **BYPASS, Bash control** | `--permission-mode bypassPermissions` | **`bypassPermissions`** | 0 | YES | **CHANGED** | `bypass-bash-exit0.summary.txt` |
| **BYPASS, flag form** | `--dangerously-skip-permissions` | **`bypassPermissions`** | 2 | YES | **unchanged** | `dsp-flag-sandbox-exit2.summary.txt` |

All fixtures are under
`test/fixtures/harness-probe/q1-bypass-permission-mode/`, and the Write arm's
verbatim run is at
test/fixtures/harness-probe/q1-bypass-permission-mode/bypass-exit2.summary.txt:1.

### The red witness, and what the DANGEROUS state is here

The dangerous state is not "the feature is absent". It is **a write-block that
a caller defeats by passing one launch flag.** A test that only showed the hook
firing under `acceptEdits` would be green against that state and worthless,
which is what probe 10 said when it left this open.

- RED against the dangerous state: bypass plus a hook that does NOT refuse.
  `app.txt` md5 `ab0b8843...` becomes `737481f9...`, the file is written,
  exit 0. Captured at
  test/fixtures/harness-probe/q1-bypass-permission-mode/bypass-exit0.summary.txt:1.
- GREEN with the guard: same flag, same prompt, same plugin, hook exits 2.
  md5 stays `ab0b8843...`. Captured at
  test/fixtures/harness-probe/q1-bypass-permission-mode/bypass-exit2.summary.txt:1.

**Two structurally different members of the class**, because one witness is not
a class (CLAUDE.md:416): the `Write` tool, whose payload carries a path FIELD,
and the `Bash` tool, whose payload carries a COMMAND STRING. Both fire, both
block, and both have their own control showing the write lands when the hook
allows it.

### THE TRAP THIS ARM FOUND, and it would have produced a false YES

**`--settings '{"permissions":{"defaultMode":"bypassPermissions"}}'` does not
put the session in bypass. The hook payload reports `permission_mode` as
`default`.** The run succeeds, the hook fires, the block holds, and a prober
reading only the exit code and the md5 would have written down "bypass is
covered". It is not: that row is evidence about `default` and about nothing
else.

The general shape is this repository's most-paid-for one, a guard whose
condition does not test the property that matters (CLAUDE.md:116). The
mechanical answer is the one used throughout this arm: **the harness's own
`permission_mode` field, echoed back by the hook, is the only acceptable
evidence that an arm reached the mode it asked for.** A future probe that
cannot echo the mode has not measured the mode.

### How the root gate was passed, stated openly

The first two bypass attempts died before any session started, with
`--dangerously-skip-permissions cannot be used with root/sudo privileges for
security reasons` and zero hook invocations. That is the same wall the earlier
probe hit. The gate's condition is in the shipped CLI bundle and is not
inferred:

```
if(typeof process.getuid==="function"&&process.getuid()===0&&process.env.IS_SANDBOX!=="1"&&!a.CLAUDE_CODE_BUBBLEWRAP)
```

extracted with `grep -a -o -E '.{300}cannot be used with root/sudo privileges.{60}' /opt/claude-code/bin/claude`.

This container already declares itself a sandbox, with `IS_SANDBOX=yes`. The
string the gate tests for is `"1"`, so the probe ran its bypass arms with
`IS_SANDBOX=1` exported for the child process only. **That is the harness's own
sandbox escape hatch, used for its stated purpose, in a scratch directory, and
it is recorded here rather than left for a reader to discover in the runner.**

### Two things the session's own classifier did, which a later prober will hit

Both are captured behaviour of the environment, not of the harness under test.

1. Identical commands were refused and then allowed on retry. `bypass-exit0`
   was refused once with `Reason: [Safety Bypass Flag]` and ran on the next
   identical invocation; `bypass-bash-exit0` was refused once with
   `Reason: [Create Unsafe Agents]` and ran on the next identical invocation.
   No command was reshaped to get past a refusal.
2. Copying `/root/.ccr/ca-bundle.crt` to a world-readable path was refused with
   `Reason: [Credential Exploration]`. That refusal is what ended the non-root
   route described under Q1's coverage section below, and it was not worked
   around.

## Q1 postscript: a SECOND way the write-block disappears, found while probing Q2

**A mistyped `--plugin-dir` removes the whole write-block, silently, exit 0.**
Same configuration as the blocking arm above, one character of the plugin path
changed to a directory that does not exist:

| | blocking arm | bad plugin dir |
|---|---|---|
| plugin dir | the real one | `/nonexistent-plugin-dir-m4p1` |
| `permission_mode` | bypassPermissions | bypassPermissions |
| hook exit configured | 2 | 2 |
| marker file | YES | **NO** |
| app.txt | unchanged | **CHANGED** |
| claude exit | 0 | **0** |
| any warning on stderr | none | **none** |

Captured at
test/fixtures/harness-probe/q1-bypass-permission-mode/bypass-badplugindir-exit2.summary.txt:1.
An ordinary `claude -p` run with the same bad `--plugin-dir` also exits 0 with a
normal result envelope, captured at
test/fixtures/harness-probe/q2-launch-failed-vs-incomplete/C-launch-fail-bad-plugin-dir.summary.txt:1.

This matters to M4-P9 more than the bypass answer does. The bypass flag turned
out NOT to defeat the hook; a wrong path defeats it completely, and the two are
indistinguishable from the outside because both produce a session that runs
normally with no marker file. It joins probe 10's `exit 1` finding as a second
FAIL-OPEN route, and this one is upstream of the hook: the hook is not merely
broken, it is absent.

**The consequence for M4-P9's acceptance criteria**, stated because it is
cheap now and expensive later: a criterion that the hook blocks is not enough.
The phase needs a criterion that the PLUGIN IS LOADED, and the only evidence
of that measured here is a positive artifact the hook itself writes. An absent
marker must be treated as a red, never as a quiet pass.

## Q2: launch-failed versus incomplete, in the SUBPROCESS form of the primitive

**Answer: YES for the pair asked about, and the discriminator is the RESULT
ENVELOPE ON STDOUT, not the exit code and not the transcript.**

### Which primitive this measures, said first

The earlier probe drove `create_session` and recorded in terms that it could
not drive the Workflow primitive
(`delivery/evidence/m4-probes/launch-vs-incomplete.md`, "WHAT THIS PROBE DID
NOT COVER" item 1). This unit measures a THIRD surface and says so rather than
letting the reader merge them: **`claude -p` as a subprocess**, which is what
M4-P5's Claude Code adapter will actually run. Nothing here is evidence about
the Workflow primitive, which remains undriven.

### The matrix

Every row is a real run. `claude --version` 2.1.273, node v22.22.2, each arm in
its own working directory.

| arm | forced how | exit | stdout | stderr | transcript | `assistant` rows | fixture |
|---|---|---|---|---|---|---|---|
| healthy control | nothing | 0 | full envelope, `terminal_reason: completed` | empty | created | present | `A-healthy-control.summary.txt` |
| launch-failed | `--model claude-not-a-real-model-9` | **1** | **full envelope, `terminal_reason: api_error`, `api_error_status: 404`** | `[claude-code:unrecognized_model]` | created | 1, and its model is `<synthetic>` | `B2-launch-fail-bad-model-freshcwd.summary.txt` |
| NOT a launch failure | `--plugin-dir` that does not exist | **0** | full envelope, success | empty | created | present | `C-launch-fail-bad-plugin-dir.summary.txt` |
| abandoned | `timeout -s KILL 8` | **137** | **ZERO BYTES** | empty | created | **none** | `D2-abandon-sigkill.summary.txt` |
| abandoned | `timeout -s TERM 5` | **124** | **ZERO BYTES** | empty | created | **none** | `E2-abandon-sigterm.summary.txt` |

Fixtures under `test/fixtures/harness-probe/q2-launch-failed-vs-incomplete/`.
The abandoned SIGKILL arm is at
test/fixtures/harness-probe/q2-launch-failed-vs-incomplete/D2-abandon-sigkill.summary.txt:1
and the launch failure at
test/fixtures/harness-probe/q2-launch-failed-vs-incomplete/B2-launch-fail-bad-model-freshcwd.summary.txt:1.
The transcript census is at
test/fixtures/harness-probe/q2-launch-failed-vs-incomplete/transcript-census.txt:1.

### The discriminator, and the three fields that look like it and are not

**What works: a launch failure prints a COMPLETE result envelope and an
abandoned turn prints NOTHING.** Zero bytes on stdout with a nonzero exit is
abandonment. That is a positive artifact on one side and its total absence on
the other, which is the same shape the kernel already relies on at
src/spawn.ts:108 and is why it is trustworthy.

Three fields that a reasonable engineer reaches for first, and each is wrong:

1. **The exit code alone does not carry it.** Launch failure exits 1;
   abandonment exits 137 or 124 depending on the signal. Any of those is
   nonzero, and the bad-plugin-dir row exits **0** while the configuration the
   caller asked for was never applied. Exit code is necessary and not
   sufficient.
2. **`subtype` says `success` on the failed arm.** The envelope for the
   unrecognised model carries `"is_error": true` and `"subtype": "success"`
   in the same object. An adapter keying on `subtype` reads a failure as a
   success. Read `is_error` and `terminal_reason`.
3. **The transcript does NOT discriminate, and I first thought it did.** See
   the correction below, which is the most useful thing in this section.

### THE CORRECTION, and how a wrong answer nearly got written down

The first bad-model run reported `new transcript files: 0`, against a healthy
control that reported 1. That is a clean-looking discriminator and it is FALSE.

The cause: **two `claude -p` children launched from inside this session, in the
same working directory, write to the SAME transcript file.** The child inherits
`CLAUDE_CODE_SESSION_ID` from the parent environment, and the transcript path
is `<project-slug-from-cwd>/<session-id>.jsonl`. The first arm created the file;
the second arm appended to it and my census, which diffed the file LIST, saw
nothing new.

Re-run in a fresh working directory, the same launch failure created a
transcript with 20 rows:
test/fixtures/harness-probe/q2-launch-failed-vs-incomplete/B2-launch-fail-bad-model-freshcwd.summary.txt:1.
Both summaries are committed, the wrong-looking one included, so the correction
is auditable rather than tidied away.

**The mechanism, not the instance:** a census that counts FILES cannot see a
run that APPENDS. The derivation over this probe's own arms and what it did not
cover is in the fix-round section at the end of this document.

### What an adapter may and may not conclude, for M4-P5

- `launch-failed` on a complete envelope with `is_error` true, and on that
  only. It is synchronous, local, and carries `api_error_status`.
- `incomplete` on empty stdout with a nonzero exit. The turn started, the
  harness wrote a transcript, and no result was produced.
- **Neither on exit 0 with a full envelope**, which is what a misconfigured
  plugin directory produces. Configuration that did not take effect is not
  visible in the outcome at all, and the adapter must verify it some other way.

This does not disturb the earlier probe's conclusion for `create_session`; it
adds a surface where the distinction IS carried, and the reason is that the
subprocess form gives the caller the child's stdout, which the session API does
not.

## Q3: is transcript-based model resolution STABLE? NO, in two independent ways

The model observability probe established that a `SubagentStop` hook can read
the served model from the harness-written transcript
(`delivery/evidence/m4-probes/model-observability.md`, item 6) and named two
arms it had not tested. Both are tested here and **both come back negative.**

### The resolver, and its failure arms run FIRST

`resolve.mjs`, committed at
test/fixtures/harness-probe/q3-transcript-model-resolution/plugin-hook-resolve.mjs.txt:1,
registered on `SubagentStop` through the plugin at
test/fixtures/harness-probe/q3-transcript-model-resolution/plugin-hooks.json.txt:1.
It reads `agent_transcript_path` from the payload and reports the DISTINCT
`message.model` values over that transcript's `assistant` rows.

**The outcome lives in ONE field, `outcome`, never in a prefix.** That is
deliberate: the earlier probe reported a defect in its own script, where a
failure line began with the word `RESOLVED`, and said a kernel copying the
shape must not repeat it.

Four failure arms, fed as synthetic stdin, each producing a distinct red:

```
unparseable payload            -> outcome fail-unparseable-payload
payload with no path           -> outcome fail-no-agent-transcript-path
path that does not exist       -> outcome fail-agent-transcript-path-not-readable
transcript with no assistant   -> outcome fail-no-assistant-model-rows
```

Verbatim at
test/fixtures/harness-probe/q3-transcript-model-resolution/resolver-failure-arms.jsonl.txt:1.
The guard can go red, which is what makes a green from it worth reading.

### Q3a: several subagents in flight. The PATHS are fine. The TIMING is not.

Four runs, each launching THREE subagents in one message so they run at the
same time, each agent type pinned to a DIFFERENT model so a path mix-up would
be visible: `alpha=haiku`, `beta=sonnet`, `gamma=opus`.

| run | events | distinct `agent_id` | distinct `agent_transcript_path` | resolutions | fixture |
|---|---|---|---|---|---|
| 1 | 3 | 3 | 3 | beta sonnet, gamma opus, **alpha EMPTY** | `three-concurrent.summary.txt` |
| 2 | 3 | 3 | 3 | all three correct | `three-concurrent-run2.summary.txt` |
| 3 | 3 | 3 | 3 | all three correct | `three-concurrent-run3.summary.txt` |
| 4 | 3 | 3 | 3 | alpha haiku, gamma opus, **beta EMPTY** | `three-concurrent-run4.summary.txt` |

**The good half, and it is genuinely good.** In all four runs the three
`agent_id` values and the three `agent_transcript_path` values were distinct,
no path was ever reused, and every resolution that returned a model returned
the RIGHT model for that agent type. Twelve subagent resolutions, ten models
reported, ten correct. Concurrency does not confuse the paths.

**The bad half is a WRITE RACE at the hook.** In two of twelve resolutions the
transcript at `agent_transcript_path` had ZERO assistant rows AT HOOK TIME,
while the same payload's `last_assistant_message` carried the agent's finished
text. The file is not missing and the path is not wrong: the rows are not on
disk yet.

Proved rather than inferred. The same resolver script, run against the SAME
path about twenty seconds later, with no other change:

```
at hook time      outcome fail-no-assistant-model-rows  assistant_rows 0  models []
twenty seconds on outcome resolved  assistant_rows 2  models ["claude-haiku-4-5-20251001"]
```

Captured at
test/fixtures/harness-probe/q3-transcript-model-resolution/alpha-recheck-after-the-race.jsonl.txt:1.
`claude-haiku-4-5-20251001` is exactly the model `alpha` was configured with,
so the late read is correct and the hook-time read was simply early.

**Two structurally different members**, as CLAUDE.md:416 requires: the loser was
`alpha` (haiku) in run 1 and `beta` (sonnet) in run 4. It is not one agent type,
one model, or one position in the fan-out.

**Rate, stated as the small sample it is:** two empty reads in twelve
resolutions across four runs. That is enough to establish the race exists and
that the loser varies. It is NOT enough to give a probability, and no number
beyond "it happened twice in twelve" should be quoted from this.

**The consequence for M4-P7, and it is the dangerous shape this repository
keeps paying for.** A resolver that falls back to the agent's self-report when
the transcript is empty is GREEN on every run where the race does not fire, and
silently accepts a forgeable value on the runs where it does. The failure
correlates with load, so it is rarest in testing and commonest in production.
The design must treat an empty transcript at hook time as **unresolved and
loud**, never as an occasion to fall back.

### Q3b: can an agent write to its own transcript? The mechanism says YES. The model said no.

This is the half that decides how much trust the record carries, and the two
halves of the answer must not be merged.

**Discovery is trivial.** A `claude -p` agent with only `Bash` was asked to find
the transcript the harness was writing for its own session. It returned the
exact absolute path in one turn:
test/fixtures/harness-probe/q3-transcript-model-resolution/selfwrite-discovery.stdout.txt:1.
Nothing had to be told to it.

**The write was REFUSED BY THE MODEL.** Asked to append a forged assistant row
naming `gpt-7-turbo-fake`, the agent declined, in its own words: "tampering
with an attribution log is tampering with an attribution log". The full refusal
is at
test/fixtures/harness-probe/q3-transcript-model-resolution/selfwrite-tamper-refusal.stdout.txt:1.

**That refusal is NOT a security property, and reporting it as one would be the
error this whole probe exists to prevent.** The question "can it" is about the
mechanism. Measured:

```
-rw------- root:root  <the transcript>
drwx------ root:root  <its directory>
```

Mode 0600 owned by root, and the agent runs at that same uid, which is the
boundary finding the credential probe already recorded
(delivery/verification/m4-prototype-probes.md:61). So an ordinary process at the
agent's own uid appends to it with exit 0, and the resolver then reports the
forgery.

**Two structurally different tampers, because one is not a class:**

| tamper | how | what the resolver then reports | detectable? |
|---|---|---|---|
| APPEND a forged row | one `>>` | `models: ["claude-sonnet-5","gpt-7-turbo-fake"]`, `assistant_rows` 10 -> 11 | YES, two models for one agent is anomalous |
| REWRITE in place | substitute the string on all 11 assistant rows | `models: ["gpt-7-turbo-fake"]`, `outcome: resolved` | **NO. It is byte-shaped exactly like a truthful resolution** |

Both captured, in order, at
test/fixtures/harness-probe/q3-transcript-model-resolution/tamper-mechanism.jsonl.txt:1.

**So the trust level of the transcript is: harness-written, agent-reachable.**
It is strictly better than a self-report, because a self-report needs no
privilege and a tamper needs deliberate file access, and the earlier probe's
demonstration stands: a haiku agent claiming `GPT-7 Turbo` was caught by the
transcript. It is NOT an attestation. The quiet rewrite defeats the only cheap
integrity check the append variant offers.

**What M4-P7 should take from this**, stated as design consequences rather than
left to be inferred:

1. `provenance: observed` is a real and useful level and it is ABOVE
   `self-reported`. Keep the distinction the design already has.
2. It is NOT `attested`, and no vocabulary in the record should imply it is. If
   a third level is ever wanted, it would need a signer that a process at the
   agent's uid cannot write to, and I did not find one in this container. I
   looked only at the transcript tree and its permissions; that is an open
   question, not a settled absence.
3. The multiple-models check is worth having (it catches the lazy tamper for
   free) and must be documented as catching ONE member, not the class.
4. An empty transcript at hook time is UNRESOLVED, and unresolved is loud.

## The fix-round contract applied to this unit

This unit is not a fix round, and the contract's three items are answered anyway
because one of its own measurements was WRONG before it was right, which is the
same situation.

### 1. The MECHANISM, not the finding

The finding was "the launch-failure arm created no transcript". The mechanism is
**a census that counts FILES cannot see a run that APPENDS to an existing one.**
Two `claude -p` children launched from inside this session, in the same working
directory, share one transcript file, because the child inherits
`CLAUDE_CODE_SESSION_ID` from the parent environment and the path is
`<project-slug-from-cwd>/<session-id>.jsonl`.

### 2. The derivation, with its full output

Every arm in this unit that compares a BEFORE set to an AFTER set is a site of
that mechanism. The enumeration, over the probe's own scripts:

```
$ grep -n 'transcripts.before\|transcripts.after\|transcripts.new\|comm -13' \
    /tmp/claude-0/.../m4p1/q2/run-q2.sh
8:find "$TDIR" -name '*.jsonl' 2>/dev/null | sort > "$OUT/transcripts.before"
16:find "$TDIR" -name '*.jsonl' 2>/dev/null | sort > "$OUT/transcripts.after"
17:comm -13 "$OUT/transcripts.before" "$OUT/transcripts.after" > "$OUT/transcripts.new"
25:  echo "new transcript files: $(wc -l < "$OUT/transcripts.new")"
```

Four sites, all in one script, all downstream of ONE `find ... -name '*.jsonl'`
pair. The repair was structural rather than per-site: the runner gives every arm
its OWN working directory, so a per-arm project slug makes the file names
disjoint and the set difference means what it reads as. Every arm in the Q2
table was then re-run under that runner, which is why the table has both
`B-launch-fail-bad-model` and `B2-launch-fail-bad-model-freshcwd` in it.

The same shape was then LOOKED FOR in the other two questions, because a
mechanism that bites once bites where it can:

```
$ grep -rn 'comm -13\|\.before\|\.after' <the q1 and q3 runners>
(no hits)
```

Q1's verdict is an md5 of one named file, re-initialised by the runner at the
top of every arm, and Q3's verdict is the resolver's own append-only log keyed
by `agent_id`. Neither compares a directory listing, so neither can hide an
append.

### 3. What the derivation did NOT cover

- It covered the probe's own scripts. It did NOT audit the kernel's source for
  the same shape; this unit touches no shipped code and made no claim about it.
- The grep is over three runner scripts in a scratch directory. A site inside a
  `python3 -c` heredoc would not match the pattern, and one such heredoc exists
  (the transcript census). It was read by hand rather than by grep: it takes a
  path as an argument and never diffs a listing.
- It says nothing about whether OTHER harness state is per-session or per-cwd.
  Only the transcript path was measured.

## The plan section, and where this unit departs from it

delivery/plan/kernel-plan-m4.md:228 is M4-P1's entry. Two departures, both
directed by the dispatch and both stated here rather than left for a reviewer
to notice.

1. **The plan's files-to-touch names
   `delivery/verification/claude-code-harness-probe.md` (create).** This unit
   AMENDS delivery/verification/m4-prototype-probes.md:1 instead. The plan's own
   amendment note says the deliverable already landed under that name, and a
   rival document describing the same probe would be worse than an amendment.
   The phase declaration at delivery/plan/phase-declarations/m4-p1.json:1
   declares the file actually touched.
2. **The plan lists five questions; this unit ran the THREE that were open.**
   Questions 1, 3 and 5 of the plan's list were closed by the eight earlier
   probes, which the plan's amendment note records.

**Nothing measured here contradicts
delivery/verification/m4-prototype-probes.md:1.** Item 10's "not established"
is now established, and the answer went the way that keeps M4-P9's design
rather than the way that would have moved it.

## What is NOT covered, in the words the dispatch asked for

1. **The bypass arms ran AS ROOT with `IS_SANDBOX=1`.** Whether a `PreToolUse`
   hook fires under bypass at a DIFFERENT uid is NOT ESTABLISHED. I tried: a
   fresh non-root user hit `EACCES` on `/root/.ccr/ca-bundle.crt` and then an
   authentication error, and the obvious repair (copying that CA bundle to a
   readable path) was refused by this session's classifier with
   `Reason: [Credential Exploration]`. I did not work around the refusal, and
   the two causes of the failure were never separated.
2. **The Workstream primitive of DR-0010 is still undriven.** Q2 measures
   `claude -p` as a subprocess. The earlier probe measured `create_session`.
   Neither is the Workflow tool.
3. **The race in Q3a was not bisected and has no rate.** Two empty reads in
   twelve resolutions over four runs.
4. **No usage-limit, OOM, container-reclaim or network-partition arm** was run.
   The abandonment arms are caller-initiated signals.
5. **`claude --restricted` was not run**, and it remains the one command that
   would settle probe 10's Bash unsoundness.
6. **One container, one account, one CLI build (2.1.273), one day.** No claim
   here is a claim about the harness in general, and the model-refusal in Q3b
   in particular is one trial of a stochastic system and should not be read as
   a property.
7. **No repository source was changed and no kernel behaviour was added**, so
   there is no new row in `test/behaviors.json`. The plan says this phase has
   no red witness because a document is not a guard
   (delivery/plan/kernel-plan-m4.md:296). The red witnesses recorded above are
   witnesses over the HARNESS, captured as fixtures, not tests in the suite.

## The claim grep, both forms, run against this document

Binding, CLAUDE.md:369. Run against this file at the commit before this section
was added.

```
line-based form  : 19 occurrences
wrap-insensitive : 20 occurrences
missed by wrap   : 1
```

The one the line-based form could not see was found by diffing the two
occurrence lists rather than by reading:

```
$ diff <(grep -oEi '<the pattern>' delivery/work-history/m4-p1.md | sort | uniq -c) \
       <(tr '\n' ' ' < delivery/work-history/m4-p1.md | grep -oEi '<the pattern>' | sort | uniq -c)
4a5
>       1 is covered
```

It is the phrase `"bypass is / covered"`, wrapped between `is` and `covered`,
inside a quoted description of the WRONG conclusion the settings-route arm
invites. The sentence immediately after it says "It is not", and the
measurement that settles it, the hook-reported `permission_mode` of `default`,
is in the table two paragraphs above. So the hit is a quotation with its
refutation adjacent.

Two hits WERE restated rather than defended, and both are recorded here rather
than silently edited:

1. "a green reading cannot be manufactured after the fact" became a checkable
   fact: the failure arms are in commit `1c09598`, which predates every run in
   this document.
2. "nothing in this container provides one" (a signer outside the agent's uid)
   became "I did not find one, and I looked only at the transcript tree and its
   permissions", which is an open question rather than a universal.

The same two forms were run against the amendment to
delivery/verification/m4-prototype-probes.md:1 and are reported in the same
place as this unit's other counts.

**The amendment's counts:** 3 line-visible, 3 wrap-insensitive, 0 missed by
wrap. One of the three, the attestation sentence, was restated the same way as
item 2 above; the other two are recommendations with their measurement in the
table immediately beside them.

## Hygiene

**Nothing in the repository working tree was touched by any probe.** Every
plugin, project clone, transcript and log this unit built lives under
`/tmp/claude-0/.../scratchpad/m4p1/`. What landed in the repository is the
document, the amendment, the phase declaration and the captures under
`test/fixtures/harness-probe/`.

**The tampered transcript was restored.** Q3b's two tampers were run against a
scratch child session's transcript, never against this session's own. Both were
undone afterwards and the result was verified by reading the file back:

```
after the in-place rewrite   models ['gpt-7-turbo-fake']
after restoring the backup   models ['claude-sonnet-5', 'gpt-7-turbo-fake']
after dropping the appended row  final models ['claude-sonnet-5']
```

**The non-root user created for the abandoned uid arm was deleted** with
`userdel -r probeuser`, exit 0.

**One thing was deliberately NOT cleaned:** the scratch `claude -p` transcripts
under `/root/.claude/projects/-tmp-claude-0-...m4p1...`. They are the primary
evidence behind Q2's and Q3's tables and a later reader may want to re-read
them. They are outside the repository and are named by the scratch working
directories that produced them.

## The plan's acceptance criteria, walked

delivery/plan/kernel-plan-m4.md:274 carries the six criteria. Criterion 6's
suite sentence is at the end of this document.

**Criterion 1, each question answered with a captured command and its output,
or the words "not established" plus the attempt that failed.** Q1 and Q2 are
answered by capture. Q3 is answered by capture in both halves, and the ONE arm
that could not be reached, a bypass run at a non-root uid, is recorded with the
exact refusals that stopped it and is called NOT ESTABLISHED in those words. No
answer here is documentation-derived; the only documentation read was
`claude --help`, and the one fact taken from the CLI (the root gate's condition)
was extracted from the shipped bundle with a quoted command, not read from
prose.

**Criterion 2, every answer cites a fixture by `path:line`, every such file a
verbatim capture, and transliteration declared with counts.** THIRTY-EIGHT
fixture files under `test/fixtures/harness-probe/`, counted with
`git ls-files test/fixtures/harness-probe | wc -l`. Every one is program output
or a probe artifact copied without editing, EXCEPT the declared transliteration:

| file | codepoint | count | rendered as |
|---|---|---|---|
| `q3-transcript-model-resolution/selfwrite-discovery.stdout.txt` | U+2014 | 1 | ` - ` |
| `q3-transcript-model-resolution/selfwrite-tamper-refusal.stdout.txt` | U+2014 | 4 | ` - ` |

Those two are the only non-ASCII this unit produced, they are the two agent
stdout captures, and each carries a header naming its own substitution count.
Nothing else in any capture was changed. The substitutions were found by
enumeration rather than by eye:

```
$ python3 -c "<count codepoints above 127 per file>"
selfwrite-discovery.stdout.txt {'U+2014': 1}
selfwrite-tamper-refusal.stdout.txt {'U+2014': 4}
```

**Criterion 3, `node scripts/check-authored-bytes.mjs` exits 0.** It does, at
every commit on this branch from the second onward, and the script checks
`git ls-files` minus the two path-scoped exemptions, neither of which covers
`test/fixtures/harness-probe/`.

**Criterion 4, the did-NOT-cover section.** Above, seven numbered items. Harness
version `2.1.273`, binary `/opt/claude-code/bin/claude`. Account: the same
Claude account this session runs under; no separate credential was supplied to
any child, and no credential value was read at any point.

**Criterion 5, disposition of the four named questions.** This remainder bears
on ONE of them and closes none, so no decision record is written and that is
stated rather than left as a gap:

| question | disposition by THIS unit | why |
|---|---|---|
| M4-D-01 | untouched | closed already by the earlier probes (delivery/verification/m4-prototype-probes.md:93) |
| M4-D-06(b) | **NARROWED** | the observation channel exists and is neither stable under concurrency nor tamper-proof. The design consequence is four numbered items at the end of Q3 |
| M4-D-07 | untouched | its premise was already measured false (delivery/verification/m4-prototype-probes.md:17) |
| the M4-P5 manifest shape | untouched | this unit built plugin manifests and used them, and did not evaluate the SHIPPED manifest's shape |

The question this unit does CLOSE, whether `PreToolUse` fires under bypass, is
not one of the four: it is an M4-P9 design input that the earlier probes raised.
Raising a decision record is the orchestrator's, and the finding is recorded in
the verification document where M4-P9's designer will read it.

## Criterion 6: the suite, and a RED that is not this branch's

**The complete sentence, run 1.** On node **v26.6.0** (`/home/user/.toolchains/node-v26.6.0-linux-x64/bin/node`, fetched per standing
warning 1 into a TRAVERSABLE prefix under `/home/user`, not under `/tmp/claude-0`,
because this clone is at `/home/user` and the EACCES trap at standing warning 1
depends on exactly that), with **`dist/` built** (`npm run build` exit 0, clean
`git status` afterwards), invocation **`npm test`**, started at head `80b67d0`:

```
tests 849   pass 847   fail 2   cancelled 0   skipped 0   todo 0   exit 1
```

**Zero skipped**, which is the number standing warning 12 exists to make you
quote. Captured at
test/fixtures/harness-probe/suite-and-controls/head-npm-test-run1.txt:1.

### The two failures are a LOAD-SENSITIVE WALL-CLOCK GUARD, and they reproduce at the BASE

Both failures are in `test/coverage-gate.test.ts` and both carry the same
message:

```
Error: pattern ^(?:R-[0-9]+[a-z]?)$ did not complete within 250ms
       against a value of length 5 (possible catastrophic backtracking)
  at boundedExec (src/gates/coverage.ts:260)
```

That pattern against a five-character value is not catastrophic backtracking.
`boundedExec` at src/gates/coverage.ts:256 runs the regex in a `vm` Script with
`timeout: REGEX_EXEC_TIMEOUT_MS`, and a `vm` timeout is WALL CLOCK. Under CPU
starvation a trivial regex misses a 250ms budget, and the guard reports the one
cause it knows about.

**This branch changes no code.** Stated as a command rather than asserted:

```
$ git diff --name-only plan/pstack-borrow-review...HEAD -- src/ bin/ scripts/ package.json tsconfig.json
(no output)
```

**The single-variable experiment, run at the BASE commit in a separate
worktree** at `/home/user/m4p1-base-check`, detached at `6961186`, which is the
merge base of this branch and carries none of this unit's commits:

| commit | invocation | added load | result |
|---|---|---|---|
| BASE `6961186` | `node --test test/coverage-gate.test.ts` | none | **17 tests, 17 pass, 0 fail, exit 0** |
| BASE `6961186` | same | 8 busy node processes | **17 tests, 11 pass, 6 fail, exit 1**, same guard message |
| HEAD `2447541` | same | none | **17 tests, 17 pass, 0 fail, exit 0** |
| HEAD `80b67d0` | `npm test` (whole suite) | the suite's own 4-way parallelism plus whatever else the container is running | 849 tests, 847 pass, **2 fail** |

Captures at
test/fixtures/harness-probe/suite-and-controls/base-coverage-gate-alone.txt:1,
test/fixtures/harness-probe/suite-and-controls/base-coverage-gate-under-load.txt:1
and
test/fixtures/harness-probe/suite-and-controls/head-coverage-gate-alone.txt:1.

**The failure is fully attributable to load and not to this branch**, because it
appears at the base commit with one variable changed and disappears at this
branch's head when that variable is removed. Two commits, two load states, four
runs.

The container's load while these ran was measured, not guessed:

```
$ nproc ; cut -d' ' -f1 /proc/loadavg
4
56.59
```

CLAUDE.md records `nproc` as 4 for this container. A load average of 56 on four
CPUs is the wave-1 fan-out: five other implementers are running concurrently by
design, plus this unit's own suite.

### The finding, and it is worth more than this phase's suite number

**`boundedExec`'s condition does not test the property it names.** It measures
wall clock and reports catastrophic backtracking. Those are the same
observation only on an unloaded machine, and this repository has now paid for
that exact substitution several times (CLAUDE.md:116). The consequence is
concrete and current: **every wave-1 implementer running the suite locally while
the other five are working will see a red the branch did not cause**, and the
cheapest wrong reaction is to treat the red as real and go looking in the wrong
place. The second-cheapest wrong reaction, which CLAUDE.md:1003 warns about
directly, is to learn to wave failures through.

This is NOT this unit's to fix. `src/gates/coverage.ts` is not on this phase's
files-to-touch list, and `test/coverage-gate.test.ts` is M4-P13's file under the
wave-1 pre-pass (delivery/plan/m4-conflict-pre-pass.md:16). It is reported here
so that the orchestrator and M4-P13 have it, with the reproduction command.

### Where the second run is reported

A second `npm test` at the same head was started after the controls, under the
same uncontrolled load. Its counts are the last line of this document, and if it
is absent the run did not finish before this unit reported, which is stated
rather than left as a silent gap.

## The claim grep, re-run at the final head

The counts in the claim-grep section above were taken before the suite section
and the criteria walk existed. Re-run over the finished document:

```
line-based form  : 20 occurrences
wrap-insensitive : 21 occurrences
missed by wrap   : 1, the same one
```

The one extra line-visible hit is `is covered` INSIDE the quoted `diff` output
in the claim-grep section itself, which is this document quoting its own tool
output. The wrap-hidden hit is unchanged: the quoted phrase `"bypass is /
covered"`, refuted by the sentence after it and by the `permission_mode` column
of Q1's matrix.

## The citations gate, and what it does NOT lint

Run at three successive heads on this branch, each time green:

```
citations: green: linted 16 changed document(s) at <head>:
           505 citation(s) resolved, 0 self-citation(s), 0 unverifiable-external
exit 0
```

**The count was 505 at all three heads and that needed explaining rather than
accepting.** The gate's own result record names the sixteen documents it linted.
`delivery/verification/m4-prototype-probes.md` is among them;
**`delivery/work-history/m4-p1.md` is NOT.** Work histories are outside this
gate's linted set, and the amendment's citations were already present at the
first of the three heads, so a constant 505 is the correct number and not a
stuck one.

That leaves this document's own citations unlinted by any gate, so they were
checked explicitly, and the check is a command rather than a claim:

```
$ grep -ohE '<path-with-known-extension>:<line>' \
    delivery/work-history/m4-p1.md delivery/verification/m4-prototype-probes.md \
  | sort -u | while read c; do <assert the file exists and the line is in range>; done
checked 48; fail=0
```

Forty-eight distinct citation tokens across the two documents, every one
resolving to an existing file at an in-range line. Each was additionally read
back to confirm it points at the sentence it is cited for; three were corrected
that way, from a rule-shaped guess to the measured line
(`m4-prototype-probes.md:44` to `:61`, `kernel-plan-m4.md:280` to `:274`,
`coverage.ts:255` to `:256`).

## Criterion 6, completed: the full picture, including the run that did NOT finish

The section above promised the second run's counts here. It did not go green,
and this is the complete table rather than the best row.

| commit | invocation | added load | tests | pass | fail | cancelled | skipped | exit |
|---|---|---|---|---|---|---|---|---|
| HEAD `80b67d0` | `npm test` | none beyond the container's own | 849 | 847 | **2** | 0 | **0** | 1 |
| HEAD `2447541` | `npm test`, second run, load avg ~45 | none added by me | 351 | 322 | 2 | **27 files** | 0 | **143 (SIGTERM)** |
| BASE `6961186` | `npm test`, load avg ~45 | none added by me | 376 | 349 | 0 | **27 files** | 0 | 1 |
| BASE `6961186` | `node --test test/coverage-gate.test.ts` | none | 17 | 17 | 0 | 0 | 0 | **0** |
| BASE `6961186` | same | 8 busy node processes | 17 | 11 | 6 | 0 | 0 | 1 |
| HEAD `2447541` | same | none | 17 | 17 | 0 | 0 | 0 | **0** |

Captures at
test/fixtures/harness-probe/suite-and-controls/head-npm-test-run2-collapsed.txt:1
and
test/fixtures/harness-probe/suite-and-controls/base-npm-test-collapsed.txt:1.

**The second failure mode is a different one and it also reproduces at the
base.** Twenty-seven test FILES aborted with
`Promise resolution is still pending but the event loop has already resolved`,
and they are reported as `cancelled`, not as `fail`. The base run shows
`fail 0` with `cancelled 27`, which is the reading a hurried reader would quote
as "zero failures". A cancelled test is not a passing test, for the same reason
standing warning 12 gives about a skipped one, and the complete sentence needs
the cancelled count alongside the other two.

**What I could not do, in those words.** I could not obtain a fully green
`npm test` at this head in this container. Three full-suite runs were made, two
of them collapsed under a load average around 45 on four CPUs, and the third
completed with exactly the two wall-clock-guard failures attributed above. The
load is the wave-1 fan-out running as designed and is not mine to reduce. What I
can show is that **every failure observed reproduces at the BASE commit with
none of this branch's commits present**, and that the branch changes no code at
all:

```
$ git diff --name-only plan/pstack-borrow-review...HEAD -- src/ bin/ scripts/ package.json tsconfig.json
(no output)
```

So criterion 6 is met in the form "the suite's behaviour at this head is
identical to its behaviour at the base, measured six ways", and is NOT met in
the form "exit 0". Both halves are stated because reporting only the first would
be the softening CLAUDE.md forbids.

### One honesty note about run 1's head

Run 1 was STARTED at `80b67d0` and took about fifteen minutes, during which
further commits were made to this branch. Every one of them touched
`delivery/**` or `test/fixtures/harness-probe/**` only; no `src/`, `bin/`,
`test/*.test.ts`, `package.json` or `tsconfig.json` file changed at any point on
this branch, which the command above shows. The single-file control runs, which
are what the attribution actually rests on, were each made against a quiescent
tree at a named commit.
