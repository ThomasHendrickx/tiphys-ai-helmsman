# M2-P5: Citation linter, work history

Branch: claude/m2-p5-citation-linter. Branched off origin/main at 4c9bfbc
(contains M2-P1 at 8718852, merged via PR #11, and the M2-era paperwork batch
PR #12).

This file is written incrementally from the first minutes of the phase, per
CLAUDE.md's durability rule and the dispatch contract (T-008): its mtime is
the beacon a freshness watchdog is watching.

## Step 0: setup

- Wrote the working directory into the shared scratchpad WORKDIR file and
  appended the first BEACON.log line.
- Read the shared M2 brief in full
  (`scratchpad/m2-fanout/SHARED-BRIEF.md`, outside this repository); its
  contract is followed exactly. This is an implementer-only dispatch (no
  phase-delivery skill orchestrator loop in this session), so the brief and
  CLAUDE.md are the whole procedure.
- `git fetch origin main`: FETCH_HEAD is `4c9bfbc` (M2-era paperwork batch,
  PR #12), ancestor chain `4c9bfbc` -> `8718852` (M2-P1, PR #11) ->
  `037477e` (M1 completion) -> ... Confirmed with
  `git log origin/main --oneline -5`.
- Branched `claude/m2-p5-citation-linter` from `origin/main` (`4c9bfbc`).
- `node --version` in this shell: v22.22.2 (the login-shell resolution per
  CLAUDE.md warning 1, not the stripped-environment v20.20.2 trap).

## Step 1: M2-C-1 verification (before any code)

Plan section 1.1 names the delivered M1 contract this phase consumes:
`classifyEntry` and `refuseOpenForWrite` in `src/task.ts` (M1-P5, merged at
58ac964, now part of `origin/main`'s history). Read `src/task.ts` in full
(lines 1-466).

Verified, matching the plan's assumption exactly:

- `classifyEntry(path): EntryClass` at `src/task.ts:118-149`. lstat then
  stat, `{ kind: "regular" }` only for a real file; `irregular` names the
  observed type in `reason` (`describeType`, lines 93-110, covers directory,
  FIFO, socket, character device, block device); `unexaminable` when lstat or
  stat itself throws something other than ENOENT; `absent` / `dangling` for
  nothing there / a dangling symlink.
- `refuseOpenForWrite(path): string | undefined` at `src/task.ts:158-164`:
  refuses irregular and unexaminable, permits absent (creation is the point).
- `readRegularFileIfPresent(path): RegularRead` at `src/task.ts:179-201`:
  probes via `classifyEntry` before ever calling `readFileSync`, returns
  `{kind:"refused", reason}` naming the path and type for anything not a
  regular file (or unreadable), `{kind:"absent"}` for nothing there,
  `{kind:"read", body}` otherwise.

No divergence from what the plan assumes. This phase's module never opens a
path (document, cited target file, schema document) through a bare
`readFileSync`/`openSync` -- every read in `src/gates/citations.ts` goes
through `readRegularFileIfPresent`, and every directory listing (the
one-shot inventory's walk) uses `readdirSync` for the LISTING only (which
cannot block, exactly the precedent `src/gates/pin.ts:137-156` sets) followed
by `classifyEntry` before any content read, mirroring `pin.ts`'s own pattern
rather than inventing a second one.

Also read `src/gates/manifest.ts`, `src/gates/result.ts`, `src/gates/run.ts`,
`src/gates/validate.ts`, `src/commands/gates.ts` in full to confirm the gate
subprocess contract this phase's executable must obey (documented in
`src/gates/run.ts:40-49`): argv is
`<command...> --result <path> --evidence <dir> [--<param> <value> ...]`,
cwd is the runner's own working directory, exit codes are
0 green / 1 red / 20 not-applicable / 21 error, and exactly one
`GateResult` JSON document is written at `--result`. `requiredParameters`
(`src/gates/run.ts:367-377`) auto-adds `base` for any gate whose precondition
kind is `diff-touches`, so declaring that precondition kind is what makes
`--base` mandatory at the RUNNER level; this phase's own module still checks
`--base` independently (see step 5), because R-025's second call site
invokes the same executable directly, outside the runner.

## MECHANISMS.md rows bearing on this phase

Read `MECHANISMS.md` in full (T-005 interim obligation, mandatory for every
M2 phase). Rows that bear on M2-P5, and what each requires here:

- **"Reading a path whose type is not established."** Direct hit: this
  gate's whole hazard class (T-007, M2-D-18) is "a linter over documents it
  does not own, walking a tree supplied by configuration." Requires:
  lstat-then-stat-then-open-only-regular for every read, via the delivered
  `classifyEntry`/`readRegularFileIfPresent`, never a second implementation.
  Closed here by construction (see step 1 above and the hazard self-review
  below).
- **"Deciding what another program will do by pattern-matching the text of a
  file it consumes."** Bears on this phase in a narrow, specific way: the
  citation GRAMMAR is a regex over document prose, which is the shape this
  row warns about, except that here there is no pre-existing "other program"
  whose behaviour the regex has to match -- the citation grammar is a format
  this phase DEFINES, so the row's "two different functions" hazard does not
  apply to grammar recognition itself. It DOES apply to deciding whether a
  changed path "is a configured document": that decision is made with
  explicit path globs stated in committed config (`documents`,
  `citationRequired`, `roots[].match`, `externalRoots[].match`), never
  inferred, matching the row's prescribed tier 2 (PIN the accepted shapes,
  fail closed on anything else).
- **"Parsing another program's reporter output."** Not applicable: this gate
  does not run or parse another program's test-reporter output; `git diff
  --name-only` is consumed the same way `src/gates/run.ts`'s `gitLines`
  already does (line-split, trimmed, filtered).
- **"Atomic file replacement"** and **"Claim file"**: not applicable. This
  gate performs exactly one write, its own `--result` record, refused
  through `refuseOpenForWrite` before the write (matching
  `src/commands/gates.ts`'s `emit`); it never shares a directory with a
  concurrent writer of the same path.
- **"Classifying another program's errors"**: not applicable in the same way
  as the reporter-output row; the only subprocess this gate spawns is `git
  diff`, and its failure (nonzero exit, thrown spawn error) is reported as
  `error` verbatim (fail closed), never pattern-matched into a narrower
  category.

## Step 2: config schema and default config

Created `src/gates/schemas/citation-config.schema.json` (closed keyword set
only: `type`, `required`, `properties`, `additionalProperties`, `enum`,
`items`, `minItems`, `pattern`, plus `$ref`/`$defs` for the `root` shape,
matching `src/gates/validate.ts`'s `VALIDATION_KEYWORDS`). `CitationConfig`
has `version`, `roots` (local), `externalRoots`, `documents`,
`citationRequired`; each root is `{name, description?, match: string[]}`.
Kind-specific composition (a root cannot express "local roots need no extra
field, external roots need no extra field either" differently) is not
needed here, unlike `precondition`'s kind-specific fields in
`src/gates/manifest.ts`, because both root kinds share one shape; the only
code-side check beyond the schema is "every `citationRequired` glob also
appears in `documents`" (`findOrphanCitationRequired`), the same
code-not-schema split `manifest.ts` uses for precondition fields.

`DEFAULT_CITATION_CONFIG` (`src/gates/citations.ts:98-160`) is the one
committed configuration:

- `externalRoots`: one root, `firstmate`, `match: ["bin/fm-*.sh",
  "bin/fm-session-lock-lib.sh"]`, taken VERBATIM from the plan's own step 2
  and criterion 4 text.
- `roots`: one root, `kernel`, covering `src/**`, `bin/**`, `test/**`,
  `scripts/**`, `delivery/**`, `schemas/**`, `roles/**`, `tuition/**`,
  `*.md`, `*.json`. `scripts/**` was ADDED after the one-shot inventory (step
  5) showed real citations into `scripts/m1-exit-test.sh` reading
  "matches no declared root" with the first cut of this list; see step 5.
- `documents`: the eight `delivery/**` globs (plan, review, verification,
  decisions, work-history, tuition, requirements, `STATE.md`).
- `citationRequired`: plan, review, verification only, matching the plan
  text's own naming ("plans, reviews, verifications") and disposing of
  `STATE.md`, tuition and work-history exactly as step 4 requires.

## Step 3: resolution semantics

`resolveCitation` (`src/gates/citations.ts:~330-380`): external roots are
tried first (`classifyPathAgainstRoots`), then local roots, then unmatched.
A local match reads the target through `readRegularFileIfPresent`; missing
is `unresolved` naming the path, present-but-irregular is `read-error`
(bubbles to a whole-gate `error`, never silently "unresolved"), in range is
checked against `splitLines` (a trailing single newline is not counted as an
extra blank line), and a `@sha256:<64 hex>` suffix is verified by hashing
the cited line range joined with `\n`.

## Step 4: vacuous guard narrowing

`runCitationsGate` treats a `citationRequired` document's TOTAL recognized
citation count (local + external + unresolved, i.e. `lint.total`) as the
guard's input, not its LOCAL-resolved count: a plan document that cites only
firstmate paths still has `total > 0` and is not vacuously red. Verified
directly against `delivery/plan/kernel-plan-v1.md`, whose only recognized
citations under the shipped grammar ARE the firstmate ones (no bare
`path:line` local citations exist in that file), so this is not a
hypothetical: `total=9, resolved=0, unverifiableExternal=9` for that
document today (one-shot inventory row, step 5), and it is correctly not
red for vacuity.

A status word the plan text does not spell out: a changed set whose only
documents are legitimately citation-free (`STATE.md`, tuition, a
citation-less work-history round) resolves zero LOCAL citations, cannot be
`red` (nothing failed) and cannot be `green` (M2-C-2's `makeGateResult`
rewrites a green/units-0 record to `error` even if I emitted one). Decided
and implemented as a GATE-DECLARED `not-applicable` ("nothing in the
configured document set needed checking"), the same route `src/gates/
run.ts`'s CR-800 commentary documents. Consequence, stated rather than
routed around: `citations` is registered `applicability: required`
(section 1.4's table), so this self-declaration still fails the aggregate
bundle exactly as an unmet `branch-matches`/`diff-touches` precondition
would (`decideAggregate`'s `requiredNotApplicable` branch). Tests:
`citation-not-applicable-citation-free-diff`,
`citation-not-applicable-no-matched-document`.

## Step 5: diff scope, CI preconditions, and the one-shot inventory

`runCitationsGate` computes `git diff --name-only --diff-filter=d
<base>...<head>` in the runner's cwd (never a scratch clone: this gate only
READS, so M2-C-4 is satisfied trivially, there is nothing to keep
byte-identical because nothing is written into the tree), filters to paths
matching `documents`, and lints only those. `--base` absent is `error`,
checked independently of the runner's own `diff-touches` precondition (see
step 1's note on R-025's second call site).

**CI preconditions this gate requires** (M2-C-902 carry-forward,
`delivery/STATE.md` "Carried forward from M2-P1"): `fetch-depth: 0` before
the first `diff-touches` gate lands, and an EXPLICIT `--head` on
`pull_request` events (the default checkout SHA there is the synthetic
merge commit, and diffing `base...HEAD` against it does not describe the
PR's real content). `.github/workflows/gates.yml` is not on this phase's
files-to-touch list; documented here and in the module for whichever phase
wires the `citations` step.

**The one-shot inventory (criterion 7).** `listConfiguredDocuments` walks
`delivery/` with `readdirSync` (a directory LISTING, cannot block) plus
`classifyEntry` on each entry before deciding to recurse or read (the same
split `src/gates/pin.ts`'s `walk` uses); `inventoryDeliveryTree` lints every
result with the SAME `lintDocumentBody` the standing gate uses. Run by hand
against this repository at commit `f0afaf1` plus the uncommitted
`citations.ts`/schema/manifest/test changes on top (the tree as it stood
after step 4), via a throwaway script importing the module through a
computed `file://` URL (not committed; the function it calls is committed
and tested).

FIRST RUN, before `scripts/**` was added to the local root: 92 documents,
795 total citation tokens, 571 resolved, 117 unresolved, 107
unverifiable-external. Spot-checking the unresolved rows found a REAL
config gap: `delivery/review/clean-room-m1-p6-hazard.md` cited
`scripts/m1-exit-test.sh:NNN` and `scripts/seed-sandbox.sh:NNN` (both real,
existing files) as "matches no declared root", because the local root's
match list did not include `scripts/**`. Added it (`src/gates/citations.ts`,
`DEFAULT_CITATION_CONFIG.roots[0].match`).

SECOND RUN, after the fix: 92 documents, 795 total, **597 resolved, 91
unresolved**, 107 unverifiable-external (same total, `scripts/**` moved 26
citations from unresolved to resolved). Full per-document table:

```
path                                                            total resolved unresolved unverifiable-external
delivery/STATE.md                                                   3      2      1      0
delivery/decisions/DR-0001-license.md                               0      0      0      0
delivery/decisions/DR-0002-node-floor.md                            0      0      0      0
delivery/decisions/DR-0003-ci-runner.md                             0      0      0      0
delivery/decisions/DR-0004-elevated-permissions.md                  0      0      0      0
delivery/decisions/DR-0005-kernel-language-runtime.md               0      0      0      0
delivery/decisions/DR-0006-schema-technology.md                     0      0      0      0
delivery/decisions/DR-0007-runtime-substrate.md                     0      0      0      0
delivery/decisions/DR-0008-release-registry.md                      0      0      0      0
delivery/decisions/DR-0009-firstmate-source.md                      0      0      0      0
delivery/decisions/DR-0010-harness-orchestration-primitive.md       0      0      0      0
delivery/decisions/DR-0011-early-parallelism.md                     0      0      0      0
delivery/decisions/DR-0012-delegated-merge-authority.md             0      0      0      0
delivery/decisions/DR-0013-schema-validator-implementation.md       0      0      0      0
delivery/decisions/DR-0014-release-verification-interface.md        0      0      0      0
delivery/decisions/DR-0015-owner-out-of-the-merge-path.md           0      0      0      0
delivery/decisions/DR-0016-escalation-threshold.md                  0      0      0      0
delivery/plan/kernel-plan-m2.md                                     4      1      1      2
delivery/plan/kernel-plan-m3.md                                     0      0      0      0
delivery/plan/kernel-plan-v1.md                                     9      0      0      9
delivery/plan/m2-conflict-pre-pass.md                               0      0      0      0
delivery/plan/phase-declarations/README.md                          0      0      0      0
delivery/requirements/firstmate-scout-report.md                    94      0      1     93
delivery/requirements/migration-table.md                            0      0      0      0
delivery/review/arbitration-m1-p5-round4.md                         1      1      0      0
delivery/review/arbitration-m1-p5-third-round.md                    0      0      0      0
delivery/review/arbitration-m1-p6-round2.md                         0      0      0      0
delivery/review/arbitration-m1-p6.md                                0      0      0      0
delivery/review/arbitration-m2-p1-round2.md                         0      0      0      0
delivery/review/arbitration-m2-p1-round3.md                         0      0      0      0
delivery/review/arbitration-m2-p1-round4.md                         1      1      0      0
delivery/review/arbitration-m2-p1.md                                0      0      0      0
delivery/review/clean-room-m1-p1.md                                 0      0      0      0
delivery/review/clean-room-m1-p2.md                                 0      0      0      0
delivery/review/clean-room-m1-p3.md                                11     11      0      0
delivery/review/clean-room-m1-p4-second.md                          7      7      0      0
delivery/review/clean-room-m1-p4.md                                39     39      0      0
delivery/review/clean-room-m1-p5-round4-criteria.md                 4      4      0      0
delivery/review/clean-room-m1-p5-round4-hazard.md                  26     26      0      0
delivery/review/clean-room-m1-p5-second.md                         27     27      0      0
delivery/review/clean-room-m1-p5-third-criteria.md                  2      2      0      0
delivery/review/clean-room-m1-p5-third-hazard.md                   27     27      0      0
delivery/review/clean-room-m1-p5.md                                30     30      0      0
delivery/review/clean-room-m1-p6-criteria.md                        1      1      0      0
delivery/review/clean-room-m1-p6-final-criteria.md                  1      1      0      0
delivery/review/clean-room-m1-p6-final-hazard.md                    4      4      0      0
delivery/review/clean-room-m1-p6-hazard.md                         25     17      8      0
delivery/review/clean-room-m1-p6-round2-criteria.md                 1      0      1      0
delivery/review/clean-room-m1-p6-round2-hazard.md                  17      6     11      0
delivery/review/clean-room-m1-p6-round3-criteria.md                 0      0      0      0
delivery/review/clean-room-m1-p6-round3-hazard.md                   2      0      2      0
delivery/review/clean-room-m1-p6-round4-criteria.md                 0      0      0      0
delivery/review/clean-room-m1-p6-round4-hazard.md                   0      0      0      0
delivery/review/clean-room-m2-p1-criteria.md                        3      0      3      0
delivery/review/clean-room-m2-p1-hazard.md                         68     42     26      0
delivery/review/clean-room-m2-p1-round2-criteria.md                 8      5      3      0
delivery/review/clean-room-m2-p1-round2-hazard.md                   6      6      0      0
delivery/review/clean-room-m2-p1-round3-criteria.md                 0      0      0      0
delivery/review/clean-room-m2-p1-round3-hazard.md                  15     12      3      0
delivery/review/clean-room-m2-p1-round4-criteria-regression.md      4      4      0      0
delivery/review/clean-room-m2-p1-round4-derivation-audit.md        30     16     14      0
delivery/review/final-review-m1-p3.md                               2      2      0      0
delivery/review/plan-review-m2-r1.md                                3      0      0      3
delivery/review/plan-review-m3-r1.md                                0      0      0      0
delivery/review/plan-review-r1.md                                   0      0      0      0
delivery/review/plan-review-r2.md                                   0      0      0      0
delivery/review/plan-review-r3.md                                   0      0      0      0
delivery/review/plan-review-r4-external.md                          0      0      0      0
delivery/review/verification-m1-p3-fix-round-2.md                  19     19      0      0
delivery/review/verification-m1-p3-fix-round.md                    34     34      0      0
delivery/tuition/T-001 .. T-008 (8 files)                           0      0      0      0
delivery/verification/cr-520-orchestrator-reproduction.md           0      0      0      0
delivery/verification/cr-661-orchestrator-reproduction.md           0      0      0      0
delivery/verification/m1-exit-test-evidence.md                      1      1      0      0
delivery/verification/release-verification-interface.md            56     56      0      0
delivery/verification/spec-coherence-report.md                      0      0      0      0
delivery/verification/u2-race-flake-investigation.md                30     30      0      0
delivery/work-history/m1-p1.md .. m1-p4.md (4 files)                 0      0      0      0
delivery/work-history/m1-p5.md                                      5      5      0      0
delivery/work-history/m1-p6.md                                     27     27      0      0
delivery/work-history/m2-p1.md                                    142    108     34      0
delivery/work-history/m2-p5.md (this file, at the time of the run)   6      6      0      0
```

**Findings from the inventory, REPORTED, nothing outside this phase's own
files edited (criterion 7's second half; verified by `git status
--porcelain` after the run, see the gates section).**

1. `delivery/requirements/firstmate-scout-report.md:186` cites
   `README.md:226`; this repository has no `README.md` at its root. Matches
   the local `*.md` root, reads absent: unresolved, correctly (not a tool
   defect: `ls README.md` confirms nothing there).
2. The same line cites bare `LICENSE:1-21`, which the grammar does not
   recognize at all (no extension in the closed list): silently uncounted,
   a stated limitation (module header, "what remains unguarded"), not a
   false positive or a false negative against the corpus.
3. `delivery/review/clean-room-m1-p6-hazard.md:399,670` cite
   `stub-payload.sh:114-119` and `:115-118` WITHOUT the `scripts/` prefix;
   the only file with that basename is `scripts/stub-payload.sh`. Correctly
   unresolved (the citation as written names no path that exists), a real,
   pre-existing imprecision in that review document, not something this
   phase's config can or should paper over by guessing a directory.
4. A large, recurring pattern across `delivery/review/clean-room-m2-p1-*`
   and `delivery/work-history/m2-p1.md`: citations written as BARE
   BASENAMES relative to the file under discussion, for example `run.ts:171`
   inside a review of `src/gates/run.ts`, or `liveness.test.ts:671` inside a
   STATE.md line about `test/liveness.test.ts`. None of these resolve
   against the repository root (there is no top-level `run.ts` or
   `liveness.test.ts`), so all are correctly `unresolved: matches no
   declared root`. This is the majority of the 91 unresolved citations. It
   is a real, pre-existing documentation convention gap across the M1/M2
   corpus (citing a file by basename when the surrounding prose already
   names its directory), predating this phase, and not something a root
   config can safely paper over: guessing "run.ts probably means
   src/gates/run.ts" is exactly the "deciding what a program will do by
   pattern-matching text" hazard MECHANISMS.md forbids, made worse because
   several such basenames (`gates.ts`, `run.ts`) are not even unique across
   the tree in general (though they happen to be here). Reported, not fixed:
   M2-D-10 governs.
5. `delivery/plan/kernel-plan-m2.md`'s own criterion-1 fixture text,
   `src/nope.ts:1`, is picked up by the grammar and correctly reported
   `unresolved: cites a file that does not exist (src/nope.ts)` -- the
   inventory finding the plan's own worked example of "this should be red"
   is a small, welcome confirmation that the mechanism matches the
   specification's intent rather than a coincidence of the fixture tests.

No document outside this phase's five files-to-touch entries was modified to
produce or apply any of the above; `git status --porcelain` at the time of
the inventory run showed only this phase's own files.

## Step 6: registration and tests

Registered `citations` in `gates.manifest.json` per section 1.4: `command:
["node", "src/gates/citations.ts"]`, `unitLabel: "citations resolved"`,
`applicability: "required"`, `precondition: {kind: "diff-touches", paths:
[...]}` mirroring the `documents` globs as literal directory prefixes (the
`diff-touches` precondition kind only supports prefix matching, per
`src/gates/manifest.ts`'s own `paths` field description; the finer glob
filtering happens inside `citations.ts` itself). `requiredParameters`
(`src/gates/run.ts:367-377`) auto-adds `--base` for this precondition kind,
so no explicit `parameters` field is needed. Verified with `tiphys gates
self-check` against the updated manifest: green, 2 schema documents
validated (unaffected: `manifest-self-check` only validates the two M2-P1
schemas, never this phase's citation-config schema; see the cross-phase
finding below).

`test/citation-gate.test.ts`: 21 tests, all criteria 1 through 9 walked (see
the criteria section below), plus unit coverage for the glob matcher, root
classification, the grammar, `lintDocumentBody`'s bucketing, and CLI usage
errors. `node --test test/citation-gate.test.ts` in isolation: 21 pass, 0
fail, both on the default toolchain (Node v22.22.2) and on the floor
toolchain fetched to
`/tmp/claude-0/.../scratchpad/toolchain/node-v26.6.0-linux-x64` (Node
v26.6.0), same 21/21.

## Step 7: second call site, R-025

Documented in the module header (`src/gates/citations.ts`, top doc
comment): a plan review invokes the SAME executable, `node
src/gates/citations.ts --result <file> --evidence <dir> --base <ref>
[--head <ref>]`, with `--base`/`--head` naming whatever two revisions the
review wants compared (for example the plan's prior reviewed commit and its
current one). No second implementation exists for that call site; it is
this file, invoked again.

## Cross-phase finding: two PRE-EXISTING tests in test/gates.test.ts break
## once a required, parameterized gate or a second schema file is added to
## the shared registries (NOT a defect in this phase's own gate)

Both failures below are isolated and reproduced with `git stash`/file-move
derivations (commands and full output captured; neither `test/gates.test.ts`
nor `.github/workflows/gates.yml` is on this phase's files-to-touch list, so
neither is edited here).

**Finding A.** `test/gates.test.ts:1289` ("the workflow's gate bundle step
runs the gate runner and is able to fail") executes the REAL push-event
bundle step from `.github/workflows/gates.yml` (`node dist/bin/tiphys.js
gates run --manifest gates.manifest.json --evidence ...`, no `--base`)
against this repository and asserts exit 0. Once `gates.manifest.json`
carries the `citations` gate (`applicability: required`, precondition
`diff-touches`), the runner's own `requiredParameters` check
(`src/gates/run.ts:367-377`, `runOneGate:601-615`) correctly reports `error`
for `citations` because `--base` is absent (M2-C-3: an absent required
parameter is `error`, never `not-applicable`), which makes the aggregate
exit 21. This is NOT a bug in `citations.ts`: it is the documented,
intended M2-C-3 behaviour, exposing that the push-bundle workflow step
(owned by M2-P1/M2-P9) currently runs the FULL manifest unfiltered, an
assumption valid only while the manifest carried zero parameterized gates
(the workflow's own comment: "The bundle carries one gate today,
manifest-self-check, and that is deliberate"). `red-witness` (M2-P2, also
`diff-touches`, `required`) and `scope` (M2-P4, `branch-matches` needing
`--phase`, `required`) will trip the IDENTICAL failure independently the
moment either lands, regardless of this phase.

Derivation:

```
$ git stash push -- gates.manifest.json
$ node --test --test-name-pattern "the workflow's gate bundle step runs the gate runner and is able to fail" test/gates.test.ts
# ok 1 - ... (passes with the manifest reverted to main's shape)
$ git stash pop
```

With the manifest carrying `citations`, the same command fails with:
`gates: 1 gate(s) reported error: citations` and the test's own assertion
`21 !== 0`.

**Finding B.** `test/gates.test.ts:2360-2382` ("manifest-self-check reports
one unit per schema document") counts every `*.schema.json` file physically
present in `src/gates/schemas/` via `readdirSync` and asserts
`manifest-self-check`'s reported `units` equals that count. `manifest-self-
check`'s actual validated set is the FIXED two-entry list
`schemaDocumentPaths()` returns in `src/gates/manifest.ts:122-127`
(`gate-manifest.schema.json`, `gate-result.schema.json`), which this phase
does not touch and is not directed to touch. Once this phase's own
`src/gates/schemas/citation-config.schema.json` exists in that directory
(named explicitly in this phase's files-to-touch list, kernel plan M2
section 3 step 2), the directory holds three `*.schema.json` files while
`manifest-self-check` still validates two, so `record.units` is 2 but the
test expects 3.

Derivation:

```
$ mv src/gates/schemas/citation-config.schema.json /tmp/citation-config.schema.json.bak
$ node --test --test-name-pattern "manifest-self-check reports one unit per schema document" test/gates.test.ts
# ok 1 - ... (passes with the schema file removed)
$ mv /tmp/citation-config.schema.json.bak src/gates/schemas/citation-config.schema.json
```

With the schema file present, the same command fails with `2 !== 3`.

**What this does NOT mean.** It does not mean this phase's `citations.ts`,
its schema, or its own 21 tests are broken: `node --test
test/citation-gate.test.ts` run alone is 21/21 green on both toolchains
(step 6), and both findings above disappear when the file each depends on
is reverted, which is the derivation, not a summary of one. It means `npm
test` on the FULL repository, with this phase's changes applied and no
other M2 phase's changes present, does not exit 0, because of two
assumptions baked into a file this phase does not own and is not authorized
to edit (`test/gates.test.ts`, not on this phase's files-to-touch list, and
not one of CLAUDE.md's two standing pre-authorized extras). The scope audit
would flag an edit to it as an out-of-declaration change, and a unilateral
edit risks colliding with M2-P2 and M2-P4, which are converging on the same
shared surfaces concurrently and independently.

**What the derivation did NOT cover** (fix-round contract item 3, applied
here even though this is a cross-phase finding rather than a fix round): I
did not check whether M2-P2's `red-witness` gate or M2-P4's `scope` gate,
once ALSO appended to `gates.manifest.json` by their own phases, reproduce
Finding A a second, independent way (a third structurally different
member of the same class); I only demonstrated it with `citations`, because
`red-witness` and `scope` are not present on this branch (each is being
implemented concurrently on its own branch, per the M2 conflict pre-pass,
and this phase does not merge or preview their diffs). I also did not
search `test/gates.test.ts` for a THIRD assumption of the same two shapes
(a hardcoded gate count, or a hardcoded schema-document count elsewhere in
that file); the two above are the ones this phase's own additions
triggered, found by running the full suite once, not by a scoped grep of
that file for every place a shared-registry assumption could live.

**Recommendation (DR-0016: written so the orchestrator can see whether this
was ever a genuine tie).** Not an owner decision: this is a mechanical,
low-risk fix with an obvious shape, not two comparable options. The
orchestrator (or a small reconciliation phase) should, before or alongside
merging any of M2-P2, M2-P4 or M2-P5: (a) change
`test/gates.test.ts:2373-2382`'s expected count from a `readdirSync` of the
schemas directory to `schemaDocumentPaths().length` (the module's own
declared list, which is what `manifest-self-check` actually validates); and
(b) either add `--only manifest-self-check` to the push-bundle step in
`.github/workflows/gates.yml` until M2-P9 replaces it with the exit
harness (the workflow's own comment already says M2-P9 does this), or
relax `test/gates.test.ts:1289`'s assertion to tolerate a required,
parameterized gate reporting `error` on the no-`--base` push path. Both are
mechanical edits to files no M2-P2/P4/P5 implementer's files-to-touch list
includes, so none of the three can make them unilaterally without a scope
amendment.

**ORCHESTRATOR NOTICE RECEIVED, mid-phase, addressed here.** The
orchestrator independently confirmed Finding B (`test/gates.test.ts:2378`,
self-check's schema count) and reported that a dedicated mechanism fix
(self-check enumerates the directory rather than a hardcoded list) is
landing on `main` separately and will be absorbed by this branch's rebase.
Recorded per the orchestrator's instruction: neither `test/gates.test.ts`
nor `src/gates/manifest.ts` is edited by this phase, and this phase's own
test file, `test/citation-gate.test.ts`, is unaffected by (and does not
depend on) that mechanism either way -- it never invokes `manifest-self-
check` or `schemaDocumentPaths()`. Confirmed clean in isolation again after
receiving the notice: `node --test test/citation-gate.test.ts` is 21/21,
0 fail, unchanged from step 6's run.

**Finding A is DIFFERENT from Finding B and was NOT named in the
orchestrator's notice.** Restated plainly so it is not lost under a notice
that named only one of the two: `test/gates.test.ts:1289` ("the workflow's
gate bundle step runs the gate runner and is able to fail") fails for a
SEPARATE reason (the push-bundle workflow step invoking the full manifest
without `--base`, tripped by ANY required `diff-touches`/`branch-matches`
gate, not specifically by a schema-count mismatch), at a different location
in the file, with a different fix shape (a workflow or test change, not a
self-check mechanism change). The full-suite run after this notice still
shows BOTH failures, not one; see the exact captured re-run immediately
below.

## Acceptance criteria, walked

1. `src/cli.ts:1` resolved, `src/cli.ts:<lineCount+1>` unresolved naming the
   count, `src/nope.ts:1` unresolved naming the missing file: test
   `citation-line-resolution-three-directions`, green.
2. `path:12-40` (real `src/cli.ts`, 49 lines today) resolved; `path:12-
   <lineCount+1>` unresolved naming the end/count: test
   `citation-range-resolution-both-directions`, green.
3. Matching `@sha256:` suffix resolved; one hex character flipped
   unresolved naming the mismatch: test `citation-hash-suffix-both-
   directions`, green.
4. Real text of `delivery/plan/kernel-plan-v1.md`: firstmate citations
   `unverifiable-external` with the external config present, `unresolved`
   (against this repo's own `bin/`, per the collision the plan names) with
   it removed: test `citation-firstmate-external-root-real-document`,
   green.
5. A glob under two roots is `error` naming the glob, two structurally
   different members (external+local, and two local roots): tests
   `citation-ambiguous-glob-external-local`,
   `citation-ambiguous-glob-two-local-roots`, both green.
6. `citationRequired` + zero citations = red; + one valid citation = green;
   configured non-required + zero citations = not red: test
   `citation-vacuous-guard-three-directions`, green, all three directions
   in one scratch-repo sequence.
7. One-shot inventory over the real `delivery/**` tree, per-document counts
   recorded above (step 5); `git status --porcelain` showed only this
   phase's five files-to-touch entries throughout. Mechanism witnessed by
   `citation-inventory-fixture-tree-counts` (fixture correctness) and
   `citation-inventory-fifo-refused-loudly` (fail-closed on the walk).
8. `node --test test/citation-gate.test.ts`: 21 tests, 21 pass, 0 fail, on
   both toolchains (step 6). The FULL suite (`npm test`) does not exit 0 on
   this branch, for the two cross-phase reasons recorded above, neither of
   which is a defect in this phase's own files.
9. M2-C-6, both directions, over a real `mkfifo`: test
   `citation-fifo-diff-scoped-both-directions`. The process returns
   (`signal: null`, distinguishing a genuine return from a harness-forced
   kill) with exit 21 and a result naming the path and "named pipe" when
   the diff-named path is a FIFO; the same path as a regular document
   lints green with 1 unit in the same scratch repository beforehand.

## Hazard self-review (T-007, M2-D-18)

The plan section names five ways this hazard class can be defeated. Walked
against this implementation, each with its evidence pointer:

1. **"A path that resolves under two declared roots, guessed rather than
   refused."** Closed: `findAmbiguousGlobs` refuses at config-load time,
   before any citation is resolved, naming the glob and every root that
   claims it (`error`, never a guess). Tests
   `citation-ambiguous-glob-external-local`,
   `citation-ambiguous-glob-two-local-roots` redden two structurally
   different members. Residual, stated rather than hidden: this check is
   over LITERAL glob strings, not over "any two globs that could ever match
   an overlapping path" (there is no decidable general answer to that for
   arbitrary globs); the plan's own wording ("a path glob listed under two
   roots") is the narrower, decidable question this answers, and
   `bin/**`(local) vs `bin/fm-*.sh`(external) legitimately overlap in what
   they can match WITHOUT being an error, resolved instead by the stated
   precedence rule (external first), which is criterion 4's whole point.
2. **"A document with zero citations reading as clean."** Closed for
   `citationRequired` documents (red, test
   `citation-vacuous-guard-three-directions` direction 1); a NON-required
   document with zero citations is recorded (zero units contributed) and,
   at the aggregate level, cannot make the bundle falsely green either
   (M2-C-2's constructor rewrite plus this gate's own `not-applicable`
   self-declaration when the whole changed set resolves nothing local; test
   `citation-not-applicable-citation-free-diff`).
3. **"An external root whose match list is implied rather than stated."**
   Closed by construction: `externalRoots` is committed configuration with
   an explicit `match` array; there is no code path that infers "external"
   from a path's shape (no heuristic like "starts with bin/fm"). The ONE
   external root shipped uses the exact glob list the plan's own grounding
   states (step 2, criterion 4), not a derived or guessed one.
4. **"A whole-corpus scope that reddens merged history and tempts an
   implementer to edit documents outside the phase."** Closed by M2-D-10:
   the registered `citations` gate is diff-scoped only (`runCitationsGate`
   reads `git diff`, never walks `delivery/**`); the ONLY full-corpus walk
   is the one-shot inventory (step 5), which is READ-ONLY (`inventory
   DeliveryTree` and `listConfiguredDocuments` contain no
   `writeFileSync`/`renameSync`/`unlinkSync` call, confirmed by reading
   `src/gates/citations.ts` in full) and whose findings are reported here,
   not applied: `git status --porcelain` after the inventory run showed only
   this phase's own five files, never a change to any document the
   inventory read.
5. **"An entry in the walked tree that is not a regular file (the M1-P5
   class, CR-520, applied to a gate rather than to the kernel)."** Closed at
   BOTH placements the hazard can occur: the diff-scoped path (a changed
   document is a FIFO: `readRegularFileIfPresent` refuses, the whole gate
   returns `error` naming the path and type, criterion 9, test
   `citation-fifo-diff-scoped-both-directions`) and the one-shot inventory's
   walk (a document under `delivery/` is a FIFO: `walkDocuments` throws with
   the same `classifyEntry` reason, test
   `citation-inventory-fifo-refused-loudly`). Both tests assert the process
   RETURNS (`signal: null` for the CLI form; a synchronous throw rather than
   a hang for the inventory form) rather than blocks, and both are bounded
   by an explicit `spawnSync` `timeout: 10_000` (the CLI test) or by running
   in-process with no I/O wait at all (the inventory test, which cannot hang
   the test runner even if the probe were missing, because `readdirSync`
   itself cannot block on a FIFO; the property this test actually witnesses
   is the CORRECT ERROR, not the absence of a hang, which the CLI test
   witnesses separately). CITED TARGET files (the file a citation POINTS
   AT, as opposed to the document being linted) go through the identical
   `readRegularFileIfPresent` call in `resolveCitation`; no test stages a
   FIFO as a citation TARGET specifically, which is the same mechanism as
   criterion 9 and is recorded here as coverage by mechanism (one shared
   function, two call sites, one of the two call sites witnessed directly)
   rather than claimed as independently witnessed at both call sites.

## The claim grep

Run against this file at the commit this work history is committed with
(see the final commit sha in the branch log; the grep below was run against
the working tree immediately before that commit):

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m2-p5.md
```

Hits and their adjacent evidence, or restated as open questions:

- "never a second implementation" (module header, several places) / "never
  a guess" / "never opens a path... through a bare readFileSync" / "never
  shares a directory with a concurrent writer" / "never walks delivery/**"
  (in `runCitationsGate`) / "it never invokes manifest-self-check": settled
  by direct greps against the committed file, run just now:

  ```
  $ grep -n "readFileSync\|openSync\b" src/gates/citations.ts
  208: * through `readRegularFileIfPresent` rather than a bare `readFileSync`.
  $ grep -n "writeFileSync" src/gates/citations.ts
  3:import { lstatSync, readdirSync, writeFileSync } from "node:fs";
  932:  const written = runStep(`writing ${path}`, () => writeFileSync(path, renderGateResult(result)));
  $ grep -n "unlinkSync\|renameSync" src/gates/citations.ts
  (no output)
  $ grep -n "self-check\|schemaDocumentPaths\|manifest-self-check" test/citation-gate.test.ts
  (no output)
  $ grep -n "runCitationsGate" -A 60 src/gates/citations.ts | grep -n "listConfiguredDocuments\|walkDocuments\|inventoryDeliveryTree"
  (no output)
  ```

  Confirms: the only `readFileSync`/`openSync` mention in this file is the
  comment naming what NOT to do; the only `writeFileSync` call is the one
  CLI-write of this gate's own `--result` record, reached only after
  `refuseOpenForWrite`; no `unlinkSync`/`renameSync` exists in the file at
  all; this phase's own test file never touches `manifest-self-check`'s
  machinery; and `runCitationsGate`'s body (the 60 lines after its
  declaration) contains no call to the tree-walking functions, confirming
  the registered gate is diff-scoped only and the one-shot inventory is a
  separate, unregistered code path.
- "never opened" / "cannot block" (readdirSync discussion, step 5 and the
  hazard review): `readdirSync` lists directory entries via `getdents`,
  which does not open file content and is not documented or observed to
  block on a FIFO in this project's own prior art (`src/gates/pin.ts`'s
  identical use, shipped and tested in M2-P1). Not independently
  re-measured against a FIFO in THIS phase's own tests beyond the existing
  M2-P1 precedent; restated as: this phase relies on M2-P1's established
  use of the same primitive rather than re-deriving it.
- "cannot resolve against the repository root" / basename citations
  (step 5, finding 4): this is a report about the SOURCE TEXT of pre-
  existing review documents (bare basenames with no directory), evidenced
  by the inventory's per-citation output quoted in step 5, not an
  assertion about what the tool can or cannot do.
- "this is NOT a defect" (cross-phase finding): backed immediately by the
  `git stash`/file-move derivation commands and their full output, both
  reproduced above.
- "None of the three can make them unilaterally" (recommendation):
  restated precisely as a fact about files-to-touch lists, verified by
  reading `delivery/plan/phase-declarations/m2-p2.json`,
  `delivery/plan/phase-declarations/m2-p4.json` and this phase's own
  `m2-p5.json`; none names `test/gates.test.ts` or
  `.github/workflows/gates.yml`.

## A third category: real-clock flakes under concurrent host load, distinct
## from Findings A and B, reported per the orchestrator's mid-phase request

The orchestrator's mid-phase notice asked me to confirm the full-suite
re-run showed exactly Finding B's failure "and nothing else", and to
investigate any other failure before finishing. TWO full runs were captured
against the IDENTICAL, unchanged tree (no commits between them):

Run 1 (`full-test-run.log`, `duration_ms: 122257`): 218 pass, **2 fail** (65,
78: exactly Findings A and B), 2 skipped, 222 total.

Run 2 (`full-test-run-2.log`, `duration_ms: 423283`, more than 3x slower
wall-clock than run 1): 215 pass, **5 fail** (65, 78, plus 104, 200, 204), 2
skipped, 222 total.

The three NEW failures in run 2 (104, 200, 204) are not in Findings A or B's
files, not in this phase's files, and this phase touches none of their code:

- 104, `test/liveness.test.ts:633` ("doctor and the guard return one verdict
  about one beacon"): asserts an EXACT beacon age string (`age 13s`)
  computed from `Date.now()` minus a fixed 13000ms offset. Observed THREE
  different values across three separate invocations, none matching: `age
  15s` inside the full run captured above, then `age 14s` and `age 14s`
  again on two immediately following isolated re-runs of that one test with
  nothing else of mine competing. The assertion requires the whole `doctor`
  subprocess spawn plus its own filesystem round-trip to complete within a
  1-second window of wall clock from when the beacon file was written, which
  a loaded host does not guarantee; the varying observed value (15, 14, 14
  against an expected 13) is consistent with scheduling delay, not with a
  deterministic defect.
- 200, `test/watcher.test.ts:269`: `expected at least 4 beacon writes, saw
  3` from a resident watcher process on a real-clock cadence.
- 204, `test/watcher.test.ts:419`: `Expected values to be strictly equal: 0
  !== 3` (heartbeat count), same shape.

`git diff --stat origin/main -- test/liveness.test.ts test/watcher.test.ts
src/liveness.ts src/watcher.ts src/commands/doctor.ts` is EMPTY: this phase
has made zero changes to any file these three tests exercise. Re-running
104 in isolation, with nothing else of mine running, reproduced the same
failure shape on the first two attempts (`age 14s` both times), confirming
it is not specific to full-suite contention alone but is a genuinely tight,
real-clock-dependent tolerance (CLAUDE.md warning 11: "suite wall time
grows with real-clock lease waits"). `git worktree list` at the time of run
2 showed EIGHT other worktrees on this same host, several actively running
their own M2 phase suites concurrently (M2-P1 through M2-P8), which is the
mechanism for run 2 being 3.5x slower in wall clock than run 1 and for it
uncovering three additional timing-tight tests that run 1's lighter
contention did not trip.

**Conclusion, stated as a claim with its derivation rather than an
assertion:** Findings A and B are the only DETERMINISTIC, attributable-to-
this-phase failures: both are reproduced identically across both runs, both
are isolated precisely to a single file each via the `git stash`/`mv`
derivations above, and both disappear when the one file each depends on
(`gates.manifest.json`'s `citations` entry; `src/gates/schemas/citation-
config.schema.json`'s presence) is reverted. The other three (104, 200, 204)
are NOT reproduced identically (run 1 had zero of them; run 2 had all
three; none is in a file this phase touches; each asserts an exact,
sub-2-second real-clock tolerance around a subprocess or resident-process
measurement), and are host-contention flakes from the seven-plus concurrent
sibling M2 phase sessions sharing this container, not a defect this phase
introduced or is responsible for fixing.

## Gates

- `npm ci`: exit 0 (EBADENGINE warning expected, Node v22.22.2 against the
  declared >=26 floor, per CLAUDE.md warning 1).
- `npm run build`: exit 0; `git status --porcelain` after build showed only
  source changes (this phase's own files), no `dist/`, no `*.tsbuildinfo`
  tracked.
- `node --test test/citation-gate.test.ts`: 21/21 pass on Node v22.22.2
  (default toolchain) and on Node v26.6.0 (floor toolchain fetched to the
  shared scratchpad), confirmed again after the orchestrator's mid-phase
  notice.
- Full `npm test`, run 1: 218 pass, 2 fail (65, 78; Findings A and B), 2
  skipped, 222 total, `duration_ms: 122257`.
- Full `npm test`, run 2 (re-run after the orchestrator's notice, host under
  heavier concurrent load from sibling M2 phase sessions): 215 pass, 5 fail
  (65, 78, 104, 200, 204; Findings A and B plus three unrelated real-clock
  flakes in files this phase does not touch, see above), 2 skipped, 222
  total, `duration_ms: 423283`.
- Neither run's non-Finding-A/B failures are in this phase's files-to-touch
  list, and `git diff --stat origin/main -- <their files>` is empty for all
  of them.
