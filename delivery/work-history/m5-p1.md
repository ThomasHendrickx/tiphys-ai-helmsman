# M5-P1 work history: pulse value proof

Phase M5-P1, branch claude/m5-p1-pulse-value-proof, cut from origin/main at
e81c5e8. Governing entry: delivery/plan/value-delivery-plan.yaml (M5-P1),
declaration delivery/plan/phase-declarations/m5-p1.json:1.

## Log

- Branch created from origin/main e81c5e8. `git rev-parse
  --is-shallow-repository` printed `false` in this worktree.

## Step 1: the kernel's fleet home, and the trigger (2026-09-23)

Toolchain for every command below: node v26.6.0 from the scratch prefix
first on PATH, checked with `node --version` in the same shell. `npm ci`
exit 0.

### The fleet copy, and what the kernel says to do about a clone

The orchestrator's clone /home/user/tiphys-ai-helmsman-fleet was read only
(`ls`, `cat .gitignore`, and as the source of a local `git clone`); no command in
this log writes to it. A local copy was made without
network:

```
$ git clone /home/user/tiphys-ai-helmsman-fleet $S/m5p1/fleet     exit 0
$ git checkout main                                                "Already on 'main'"
$ git rev-parse HEAD                                               20584905c5c3ad9d0c707aa3df8f2eb549a0e275
$ git for-each-ref
refs/heads/main 2058490
refs/remotes/origin/HEAD 2058490
refs/remotes/origin/main 2058490
refs/remotes/origin/probe-lease-a ddc5b10
```

`$S` is this session's scratchpad. The clone carries `charter/`,
`decisions/`, `tasks/` (holding only `.gitkeep`), `backlog.md`,
`package.json` and `.gitignore`, which ignores `state/`, `worktrees/` and
`projects/`.

What the kernel itself does about a clone, read before creating anything:
src/fleet.ts:173 says a clone carries the durable half and not the ephemeral
one, "which is the fact `tiphys resume` exists to act on"; `tiphys init`
refuses an initialised directory and names `tiphys resume` as the remedy
(src/commands/init.ts:125). So the kernel's own command was used and nothing
was created by hand:

```
$ node bin/tiphys.ts init $S/m5p1/fleet
tiphys init: .../m5p1/fleet is already initialized; run tiphys resume to rebuild the ephemeral directories a clone does not carry
exit 1
$ node bin/tiphys.ts resume $S/m5p1/fleet
REBUILT state/
REBUILT worktrees/
REBUILT projects/
exit 0
```

`tiphys doctor`, run with the fleet copy as working directory, exit 0; its
non-PASS rows were `gh WARN not found`, `beacon WARN watcher not running`,
`retention WARN no charter document`, and `branches WARN 1 of 2 pushed
branch(es) unmerged into origin/main: origin/probe-lease-a`. `CHECK tasks PASS
0 open of 0`, `CHECK lock PASS no lease present`.

Is the orchestrator's clone current against the real remote? Read-only:

```
$ git ls-remote https://github.com/ThomasHendrickx/tiphys-ai-helmsman-fleet.git     exit 0
ddc5b1030535d47dc113778aef6e193aa477ae24	HEAD
20584905c5c3ad9d0c707aa3df8f2eb549a0e275	refs/heads/main
970cd5bdc98686c8bf89973cdbe41d6b67507c7f	refs/heads/probe-fleet-home
ddc5b1030535d47dc113778aef6e193aa477ae24	refs/heads/probe-lease-a
ddc5b1030535d47dc113778aef6e193aa477ae24	refs/heads/tiphys/lease
302528d5b4b00c7e587d8a69b105a62b2701980c	refs/heads/tiphys/lease-r1
4b1c3bd2229f58c291008453680ef660954df8d7	refs/heads/tiphys/lease-r2
e3df60c000a7117b553c9816305697b4caa05abf	refs/heads/tiphys/lease-r3
$ git ls-remote --symref <same> HEAD
ref: refs/heads/probe-lease-a	HEAD
```

Findings, none of which changes a verdict below:

- `main` on the remote equals the copy's `main` (2058490), so the durable half
  used here is current.
- The remote carries six branches besides `main`; the orchestrator's clone
  knows one of them. They are left over from the M4 cross-environment
  exclusion probes. Drain does not count branches (M4-D-15), so this does not
  move arm a, and branch cleanup is an owner action (CLAUDE.md standing
  warning 14).
- **The remote's default branch is `probe-lease-a`, not `main`.** A fresh
  clone of the fleet remote without `git checkout main` lands on a probe
  lease commit whose tree holds only `lease.json`. Recorded as an open
  question for the orchestrator; changing a default branch needs repository
  admin access.

### What a clone can and cannot say about drain

Drain counts live worktrees under `worktrees/` and open tasks under `tasks/`
(src/cutover.ts:518). `worktrees/` is ephemeral and `resume` rebuilds it
empty, by the kernel's design ("rebuilt, not restored",
src/commands/resume.ts:18). So on this copy the worktree half of drain is
empty by construction and only the tasks half reads committed state. That
tasks half is the part that survives a reclaim. A live worktree on some other
machine running this fleet would not be visible here. Neither clone checked
here holds `state/` or `worktrees/`, so I found no live fleet home in this
container to read instead.

### The trigger

```
$ git rev-parse --is-shallow-repository
false
$ node scripts/check-cutover-entry.mjs --fleet $S/m5p1/fleet
ARM a drain satisfied -- cutover status reports DRAIN clean
ARM b exclusion satisfied -- all 6 required behavior name(s) resolve and 2 exclusion test file(s) pass with a nonzero pass count
ARM c retirement satisfied -- all 199 retirement row(s) are ported
ARM d pre-freeze-ruleset satisfied -- delivery/plan/cutover/pre-freeze-ruleset.json is present and is newer than delivery/plan/cutover/retirement-inventory.json by commit order (1789726959 against 1789717584)
STEP 1 preconditions: preconditions-satisfied-owner-action-pending
[STEP 2, 3, 4 lines as printed; STEP 3 reads "HALT, OWNER ACTION"]
exit 0
```

The raw report arm a read:

```
$ node bin/tiphys.ts cutover status --fleet $S/m5p1/fleet
SWITCH planning-and-scope current
SWITCH review-and-arbitration current
SWITCH credentials-and-refs current
SWITCH salvage-and-recovery current
SWITCH closeout current
DRAIN clean
BRANCHES 1 pushed and unmerged, informational: drain does not count branches (M4-D-15)
PRE-FREEZE not-required no switch reads kernel, so there is no freeze to have captured
CANNOT-SEE open pull requests
CANNOT-SEE CI conclusions
CANNOT-SEE post-merge push runs
CANNOT-SEE whether the pilot's own fleet is drained (DR-0037: the pilot is not this orchestrator's subject)
exit 3
```

Exit 3 is `EX_WORK_REMAINS` because no switch reads `kernel`
(src/commands/cutover.ts:320); arm a reads the DRAIN row only, so the two are
consistent. `BRANCHES 1` counts the copy's view (`origin/probe-lease-a`), not
the remote's six.

## Step 2: the pilot re-probe (2026-09-23)

```
$ node scripts/probe-pilot-readonly.mjs --out delivery/verification/pulse-re-probe-m5.md
TARGET ThomasHendrickx/pulse refused -- record refused: https://api.github.com/repos/ThomasHendrickx/pulse answered HTTP 403; commits refused: https://api.github.com/repos/ThomasHendrickx/pulse/commits?per_page=1 answered HTTP 403
TARGET ThomasHendrickx/pulse-fleet refused -- record refused: https://api.github.com/repos/ThomasHendrickx/pulse-fleet answered HTTP 403; commits refused: https://api.github.com/repos/ThomasHendrickx/pulse-fleet/commits?per_page=1 answered HTTP 403
OVERALL refused
EVIDENCE .../delivery/verification/pulse-re-probe-m5.md
exit 3
```

The evidence document records `gitRef=satisfied` for both targets, heads
`d4e491b` and `7656f67`. A plain-GET control (a scratch script, GET only)
showed the 403 body names the session's repository allowlist and `add_repo`,
while the kernel repository and the kernel fleet repository answered 200 and
`/rate_limit` answered 200 in the same shell. The remedy the body names is a
push-scoped attach; I did not do it (DR-0037), and it is open question 2
below.

Independent reads, all exit 0: `git ls-remote --symref` on both pilot
repositories (default branch `main` for both), `git ls-remote --heads` on
`pulse` (106 branches), and `git clone --depth 1` of both into the session
scratchpad to read the head commit dates (both 2026-08-29) and
`pulse-fleet/package.json` (pins `@tiphys/kernel` 0.1.0). The comparison with
the 2026-08-20 and 2026-09-16 readings is appended to the probe document
under a heading that says it was not written by the script.

**p1-probe is NOT met: exit 3, not 0.** I stopped short of treating the git
read as success, as the brief requires.

## Step 3 and 4: has the reboot happened, and the A-n allocation

Reboot: no record in delivery/STATE.md (`grep -n -i reboot` gives only the
DR-0042 summary row at line 1581) or in DR-0042, and neither pilot head has
moved since 2026-09-16. So no reboot has produced pushed work; whether a
reboot without pushed work happened is not observable from here.

Allocation, after `git fetch origin` so other sessions' pushed branches are
visible to `--all`:

```
$ grep -n -o "\bA-[0-9]\+\b" delivery/STATE.md | ... | sort -u | tail     highest: A-10
$ git log --all --format='%h %s' -G'\bA-1[0-9]\b' -- delivery/STATE.md
85deb36 M4 paperwork: ... (#150)
6b42d67 Correct T-028: my salvage reverted a completed fix round, and register A-9 and A-10
$ git log --all --oneline -S'A-11'     a7b7b07, 976da5f (M3-P4)
$ git log --all --oneline -S'A-12'     a7b7b07 (M3-P4)
$ git log --all --oneline -S'A-13'     a7b7b07, 976da5f (M3-P4)
$ git log --all --oneline -S'A-14'     (no output)
```

The M3-P4 hits are row labels in a fixture table
(delivery/work-history/m3-p4.md:3599), not owner actions. The highest owner
action ever registered is A-10. I took **A-14** rather than A-11 so that the
allocation command the rules prescribe returns no false hits for the new id.
That is a judgment and it is recorded as one; the orchestrator may prefer
A-11. A sibling wave-A phase that also allocates an A-n in its own unmerged
STATE.md could pick the same id; `git log --all -S'A-14'` after the fetch
showed none at 2026-09-23.

A-10 already registers the fleet remote's probe branches and changed default
branch, so the `probe-lease-a` default-branch finding needs no new id.

## Step 5 and 6: the documents

- delivery/verification/m4-exit-test-pulse.md: Part 1 with the trigger, the
  probe, the reboot check and three open questions; Part 2 PENDING with every
  field empty and the read-only verification rule for each written in
  advance.
- delivery/STATE.md: one new register entry (A-14) directly after A-10, and
  one new block at the end, "M5 HAS STARTED". No other section edited. The
  pre-pass and DR-0050 are QUOTED, not cited: `git branch -a --contains
  ffa5f56` names only `claude/m5-plan-readiness-y2jjo5`, so neither file is on
  `main` at `e81c5e8`, and the common brief's instruction to read the pre-pass
  at its main path does not work at this head. I read it from that branch with
  `git show ffa5f56:delivery/plan/m5-conflict-pre-pass.md`.

Then I stopped, as the brief says. I did not reboot, start or contact the
pilot, and I did not simulate the reboot.

## Pre-submit hazard self-review

The plan's three hazard classes (delivery/plan/value-delivery-plan.yaml:82),
each attacked with "can this pass while the thing it guards is broken?"

- **stale-pilot-evidence.** Attack: is any pilot fact here copied from an
  older record rather than read today? Every head in the new documents comes
  from a 2026-09-23 command quoted with its exit code; older readings appear
  only in a column labelled with their date and source. A second attack: is
  "the heads have not moved" a stale reading passed off as fresh? No. The
  sameness is itself the 2026-09-23 observation, and the commit dates
  (2026-08-29) come from depth-1 clones made today. Residue: REST was refused,
  so default branch and visibility come from git `--symref` only, and
  visibility was not read at all.
- **simulated-owner-action.** Attack: does any line record the reboot as done
  or imply it? A-14 reads "OPEN, not yet done"; the exit-test Part 2 row for
  the reboot reads PENDING; p1-boundary's second half is reported PENDING, not
  passed. A second attack: could the trigger's exit 0 read as permission? Its
  own output prints STEP 3 as "HALT, OWNER ACTION", and the documents quote
  that line.
- **green-without-delivery.** Attack: do the green kernel gates below get
  offered as evidence for p1-value? No. p1-value is PENDING and the exit-test
  document says a green kernel gate is evidence for no row in Part 2.

One attack found something: arm a satisfied is weaker than it reads, because
a rehydrated clone has an empty `worktrees/` by construction. It is stated in
both the work history and the exit-test document rather than left for a
reviewer to find.

## Acceptance criteria

| id | result | evidence |
|---|---|---|
| p1-trigger | PASS | `check-cutover-entry.mjs --fleet <kernel fleet home copy>` exit 0, four arms `satisfied`, step 1 above; the fleet is the kernel's (remote `main` `2058490`), rehydrated by `tiphys resume`, not a scratch `tiphys init` |
| p1-probe | **FAIL** | `probe-pilot-readonly.mjs --out delivery/verification/pulse-re-probe-m5.md` exit 3, `OVERALL refused`; the document does name every target, head (git), transport and the excluded writes |
| p1-value | PENDING owner action A-14 | Part 2 of delivery/verification/m4-exit-test-pulse.md is empty by design |
| p1-boundary | first half PASS, second half PENDING | `git diff --stat origin/main...HEAD` lists four files, all in this repository's `delivery/`; no pulse file is added, and nothing was pushed except this branch to this repository. The documents quote pulse commit subjects, sha values and one `package.json` pin line as observations. The owner has not performed the reboot, so this report does not say it was performed |

## Gates (node v26.6.0 from the scratch prefix, `node --version` checked in each shell)

| gate | command | result |
|---|---|---|
| install | `npm ci` | exit 0 |
| build | `npm run build` | exit 0; `git status --short` empty afterwards |
| suite | `npm test`, `dist/` built | exit 0; tests 1394, pass 1394, fail 0, cancelled 0, SKIPPED 0, todo 0 |
| authored bytes | `node scripts/check-authored-bytes.mjs` after staging | exit 0 |
| merge check | `git merge-tree --write-tree origin/main HEAD`, origin/main `e81c5e8` | exit 0, tree `dc7f03a` |
| PR bundle | `scripts/m2-exit-test.sh --base origin/main --head HEAD --phase m5-p1 --bundle pr --no-build <scratch>` | exit 0 |

The bundle's lines, quoted:

```
gates: declared 15 applicable 10 verdict 10 green 10 red 0 not-applicable 5 error 0 vacuous 0
gates: suite: green: ... reported 1394 test(s) from 68 file(s) (pass 1394, fail 0, skipped 0, todo 0, did-not-run 0) ...
gates: citations: green: linted 3 changed document(s) at 51a519a...: 29 citation(s) resolved, 0 self-citation(s), 0 unverifiable-external
gates: scope: green: 4 changed path(s) audited against declaration delivery/plan/phase-declarations/m5-p1.json at merge base e81c5e8... (1 declared path(s) not touched: delivery/plan/phase-declarations/m5-p1.json)
gates: required gate(s) not applicable: red-witness
m2-assert (PR bundle): OK. 15 gate record(s) match section 1.4; ... zero red; zero error; zero vacuous.
```

`red-witness` is not applicable because no path under `src/`, `bin/` or
`plugin/` changed; this phase adds no test and no behavior, so there is no
red witness to show and nothing was added to test/behaviors.json. The
`gate-classes` line notes that the `review` class is satisfied only by naming
`check-dual-review`, which asserts nothing until reviews exist.

## Deviations

1. The pre-pass the common brief names at delivery/plan/ is not on `main`; I
   read it from the orchestrator's unmerged branch and quoted it in STATE.md.
2. A-14 instead of A-11, with the reason above.
3. The probe evidence document carries a second section below the script's
   output, clearly headed, with the comparison the plan step asks for. Its
   first section is the script's output unchanged.

## Open questions for the orchestrator

1. Which kernel release is "v1" for the pilot's phase? Only 0.1.0
   (2026-08-15, before M4) is published, and both fleet homes pin it. Settle
   this before A-14 goes to the owner.
2. Should the session attach `pulse` and `pulse-fleet` with push access, to
   get read-only REST (check runs, deployments) for step 4? That grants write
   authority DR-0037 keeps out of reach. Without it, p1-probe cannot exit 0
   in a session like this one.
3. p1-probe failed only on the exit code. The plan may want to accept a git
   read as the probe's success when REST is refused; that is a plan change,
   not mine to make.

## Resumption, 2026-09-23 08:01Z: p1-probe re-run

The orchestrator reported that `pulse` and `pulse-fleet` are now attached to
this session with push access. It asked for p1-probe to be re-run as the
criterion states, and for p1-value not to be started. Everything above this
heading is left as written, including the exit-3 run and its diagnosis. The
diagnosis is corrected here, not edited there.

### REST reachability, probed first

```
$ bash <scratch>/rest-control.sh            (curl, GET only, through HTTPS_PROXY)
GET /repos/ThomasHendrickx/pulse -> 200 : { "id": 1335801685, ... "full_name": "ThomasHendrickx/pulse", "private": false, ...
GET /repos/ThomasHendrickx/pulse-fleet -> 200 : { "id": 1336274599, ... "full_name": "ThomasHendrickx/pulse-fleet", ...
GET /rate_limit -> 200 : { "resources": { "core": { "limit": 15000, ...
2026-09-23T08:01:47Z
```

### Three invocations of the probe, all node v26.6.0

```
$ node scripts/probe-pilot-readonly.mjs --out delivery/verification/pulse-re-probe-m5.md
probe-pilot-readonly: .../delivery/verification/pulse-re-probe-m5.md already exists and this run would truncate it before its first read. Pass --force to overwrite it, or --out <path> to write elsewhere. Nothing was probed and nothing was written.
exit 64

$ node scripts/probe-pilot-readonly.mjs --out delivery/verification/pulse-re-probe-m5.md --force
TARGET ThomasHendrickx/pulse refused -- record refused: https://api.github.com/repos/ThomasHendrickx/pulse answered HTTP 403; commits refused: https://api.github.com/repos/ThomasHendrickx/pulse/commits?per_page=1 answered HTTP 403
TARGET ThomasHendrickx/pulse-fleet refused -- record refused: https://api.github.com/repos/ThomasHendrickx/pulse-fleet answered HTTP 403; commits refused: https://api.github.com/repos/ThomasHendrickx/pulse-fleet/commits?per_page=1 answered HTTP 403
OVERALL refused
exit 3

$ NODE_USE_ENV_PROXY=1 node scripts/probe-pilot-readonly.mjs --out delivery/verification/pulse-re-probe-m5.md --force
TARGET ThomasHendrickx/pulse satisfied -- record, newest commit and HEAD ref all read, and the two heads agree
TARGET ThomasHendrickx/pulse-fleet satisfied -- record, newest commit and HEAD ref all read, and the two heads agree
OVERALL satisfied
EVIDENCE .../delivery/verification/pulse-re-probe-m5.md
exit 0
```

The first refusal is the probe protecting run 1's record (entry-trigger step
2). I then passed `--force`, the documented deliberate overwrite, after
copying run 1's file to the scratchpad; its committed copy is 618544b.

Why run 2 was refused although curl read 200 a minute earlier: Node's
built-in `fetch` ignores `HTTPS_PROXY` unless `NODE_USE_ENV_PROXY=1` is set
(the container's proxy README says so at its line 71). A scratch GET-only
script with the probe's exact headers got `403 API rate limit exceeded for
34.45.210.119` for `pulse` AND for `tiphys-ai-helmsman` without the variable,
and 200 for both with it. So the probe was going straight to GitHub with no
credentials. **Correction to step 2 above:** run 1's 403 was not shown to be
the session allowlist. That diagnosis came from curl, which takes a different
network path. The probe's own 403 bodies were not read in run 1, because the script
classifies any 403 as `refused` without reading it.

Run 3 is the criterion's command with two additions, and both are stated
rather than hidden. `--force` is needed because the output path already
existed. `NODE_USE_ENV_PROXY=1` routes Node's `fetch` through this
container's proxy, as `PATH` selects the toolchain. The script and its
arguments are otherwise exactly the criterion's.

Run 3's record: `pulse` head `d4e491b`, default branch `main`, public,
updated 2026-08-29T16:29:21Z; `pulse-fleet` head `7656f67`, `main`, public,
updated 2026-08-29T17:04:48Z. REST and git agree on both. Neither head has
moved since 2026-09-16.

**p1-probe: PASS on run 3**, exit 0. The document names both targets, their
heads, the three transports and the excluded writes; the last two are in the
appended section, since the script's output does not list them.

### What still blocks

- p1-value was not started, as instructed. It waits on A-14 (the pilot
  reboot). The orchestrator says A-14 waits on kernel 0.2.0. At
  2026-09-23T08:03:36Z, `npm view @tiphys/kernel versions --json` returned
  `["0.0.0","0.1.0"]`, exit 0, so 0.2.0 was not yet visible from here.
- p1-boundary's second half still waits on the owner.
- Open question 2 (push-scoped attach) is answered by the orchestrator's
  attach. Open question 3 (accepting a git-only read) no longer applies,
  because REST now reads.
- A lesson for any Node REST caller in this container: set
  `NODE_USE_ENV_PROXY=1`, or it bypasses the proxy and fails as a 403 that
  looks like an authorization refusal.

## Resumption 2, 2026-09-24: A-14 reported done, steps 3 to 4

The orchestrator reports the owner says A-14 is done: pulse ran a full phase
cycle on kernel 0.2.0, and pulse `main` is `b7036d7`. That is the trigger,
not the evidence; everything below is observed read-only. First action:
merge origin/main (78 commits ahead of this branch's base) into this branch.

### The merge

`git merge origin/main` at `69a7a60` produced one conflict, in
`delivery/STATE.md`. Resolved by taking main's compacted file whole and
replacing its A-14 marker comment (the one naming this branch, directly after
A-10 in the register) with this branch's A-14 entry, by a script that refuses
unless the marker occurs exactly once. Merge commit `77d5122`. After it, on
node v26.6.0: `npm ci` exit 0, `npm run build` exit 0, `git status --short`
0 lines after build. Suite result recorded below when it finishes.

### Pulse evidence, observed read-only

Everything in this section is from scratch clones of `pulse` and `pulse-fleet`
(plain `git clone`, then reads) and from REST GETs made with
`NODE_USE_ENV_PROXY=1`. Nothing was pushed, and no pulse ref or file changed.

Refs and the kernel pin:

- pulse `main` is `b7036d7cdb482237643196d65bcd7f83f5344df1`, committed
  2026-09-24T00:18:49+02:00, "Merge pull request #22 from
  ThomasHendrickx/claude/tender-albattani-2btl5u", parents `03625ee` and
  `ec519614d85ced5705e12e631f461ca79ca2989b`.
- `package.json` at `b7036d7` line 31 pins `"@tiphys/kernel": "0.2.0"`; at
  `d4e491b` it pinned `0.1.0`. The lock resolves `kernel-0.2.0.tgz` with
  integrity `sha512-0e2js72o...U+5bk3Q==`, equal to
  `npm view @tiphys/kernel@0.2.0 dist.integrity`. npm gitHead for 0.2.0 is
  `cb5de0d29061ab5c50a6023658f18eee931e74a7`, published
  2026-09-23T09:01:47.626Z.
- `charter.yaml` line 9 at `b7036d7`: `kernel-version-pin: 0.2.0`.
- The pin moved in pull request #21 (`348f3a3`, with pulse DR-0032 in
  `839644e`), merged as `03625ee` at 2026-09-23T19:41:15Z. Pulse DR-0032 is
  the owner's decision to apply the 0.2.0 verdict contract to current and
  future records only.
- `pulse-fleet` has NOT moved: `git ls-remote` shows only `HEAD` and `main`,
  both `7656f67` (2026-08-29), and its `package.json` still pins
  `"@tiphys/kernel": "0.1.0"`. So the "bump pulse-fleet" half of the A-14
  summary line on `main` is not observed.

The phase: pulse M3-P4, "the import flow is usable at phone width". Its work
history (`delivery/work-history/m3-p4.yaml` at `b7036d7`) says it was run "by
the orchestrator session directly, as the first phase under @tiphys/kernel
0.2.0 (DR-0032)", with the two clean-room reviews dispatched to separate
agents. Its commits, in order: `787055b`, `fdedf35`, `be990d3` (work history),
`593c2e5` (fix round 1), `dd6adff` (round one verdicts), `5a48bc8` (carries
what round two left open), `ec51961` (records the green gates).

Verdicts, read with `grep -H -E '^(head|produced-by|framing):'` over
`delivery/review/m3-p4-*.yaml`:

| file | head | produced-by | framing | verdict |
|---|---|---|---|---|
| m3-p4-criteria.yaml | be990d3 | claude | criteria-contract | FIX-ROUND-NEEDED |
| m3-p4-hazard.yaml | be990d3 | claude | regression-and-leak-paths | FIX-ROUND-NEEDED |
| m3-p4-criteria-round2.yaml | dd6adff | claude | criteria-contract | APPROVE, two lows |
| m3-p4-hazard-round2.yaml | dd6adff | claude | regression-and-leak-paths | FIX-ROUND-NEEDED, one medium (HZ-M3P4-R2-01) and three lows |

The last reviewed head is `dd6adff`. The merged head `ec51961` was not
reviewed. `git diff --stat dd6adff ec51961`: 7 files, 944 insertions, 30
deletions, of which the code-bearing part is `src/app/globals.css` (17),
`messages/fr.json` (4), `test/e2e/mobile-import.spec.ts` (7) and
`test/e2e/pressed-and-disabled.spec.ts` (5); the rest is the two round-2
verdict files and the work history. `5a48bc8`'s message says the two-round
rule allows no third review, so the round-2 findings were "done here as
work". Both reviews are the same model family (`claude`); pulse DR-0032 keeps
DR-0003 (single family, discharged by the orchestrator by hand).

REST, all `HTTP 200` (log at the scratch `pulse-api.log`):

- `GET /pulls/22`: merged true, merged_at 2026-09-23T22:18:50Z,
  merge_commit_sha `b7036d7cdb48...`, head `ec51961...`, head ref
  `claude/tender-albattani-2btl5u`, base `main`, merged_by `ThomasHendrickx`.
- `GET /actions/workflows`: zero workflows. The clone has no `.github`
  directory. `GET /actions/runs?head_sha=b7036d7...`: total 0; the same for
  `ec51961`. So pulse has NO post-merge push run of any CI workflow to
  observe: the row cannot be filled with a GitHub Actions run, because none
  exists.
- `GET /commits/b7036d7.../status`: combined `success`, one status, context
  `Vercel`, `success`, 2026-09-23T22:20:06Z. `GET /commits/b7036d7.../check-runs`:
  total 0.
- `GET /deployments?per_page=10`: newest is id 6625673861, sha `b7036d7`,
  environment `Production`, created 2026-09-23T22:20:06Z by `vercel[bot]`.
  `ec51961`, `dd6adff` and `be990d3` each have a `Preview` deployment.

Deploy verification: pulse's `charter.yaml` declares
`release-verification: mode: reserved`, with the note that the owner has not
decided its shape and the working candidate is "the Playwright golden journey
plus the fast gate, green on the release commit". The last recorded
Playwright and fast-gate runs in the work history are at `5a48bc8` (fast 758
of 758, slow 128 pass 1 skipped of 129). No gate record names `ec51961` or
`b7036d7`. So a release verification in the charter's own sense is not
declared and not observed; what is observed is Vercel's production deploy
status `success` on the merged head.

### Suite on the merge commit, and one red that was mine

The background run after the merge (node v26.6.0, `dist/` built, `npm test`)
reported 1486 tests, 1485 pass, **1 fail**, 0 skipped, exit 1. The failing test
was "the open-action exemption is an id list, so a bullet's wording exempts
nothing". It was not a merge defect: I edited `delivery/STATE.md` (removing
A-14 from "Owner actions open") while that run was still going, and the test's
old member B asserted `openActionFindings(now, [])` names A-14. That
assertion was true only while A-14 was open. So the red is a witness that the
old member B depended on A-14 being open, which is why it had to change with
the closure. The full suite was re-run at the committed head below.

### What changed on this branch in this resumption

- `delivery/verification/m4-exit-test-pulse.md`: Part 2 filled with observed
  evidence (heading now at delivery/verification/m4-exit-test-pulse.md:180).
  The rules that were written before the result are kept verbatim under
  their own heading, and each is checked against what was observed. The
  status line, the p1-boundary paragraph and open question 1 carry dated
  updates; the 2026-09-23 text above them is left as it was. The STATE.md
  citation in open question 1 pointed at line 2640 of the pre-diet file and
  now points at the M4 closure, delivery/STATE.md:550. The trigger-capture
  citation pointed one line early and now points at
  delivery/work-history/m5-p1.md:108.
- `delivery/STATE.md`: the A-14 register item is closed as DONE, with what
  was and was not observed. A-14 is removed from "Owner actions open". The
  "In flight" line and the M4 closure line now say A-14 is done and point at
  Part 2. The standing heading and everything else of main's structure is
  unchanged.
- `test/retirement-inventory.test.ts`: `OPEN_ACTION_EXEMPT` is now empty,
  typed `readonly string[]`. Member B of "the open-action exemption is an id
  list" no longer names A-14. It cuts A-15's register item and asserts the
  cut is refused with an empty list and passes with `["A-15"]` on it. A-15 is
  already the subject of member A in the same test, so no new dependency on
  a live id was added.
- `delivery/plan/phase-declarations/m5-p1.json`: `test/retirement-inventory.test.ts`
  added to `filesToTouch`. This is a declaration GRANT on the phase branch;
  the scope gate reads both declarations and prints the addition by name
  for review (src/gates/scope.ts:110). Nothing was removed.

Red witness for the rewritten member B, two structurally different mutants
of the exemption filter in `openActionFindings`, each applied by `sed` to the
committed file, run alone, and restored from a byte copy (`cmp` equal after):

| state | tests | pass | fail | exit |
|---|---|---|---|---|
| unmutated | 1 | 1 | 0 | 0 |
| exemption ignored (`.filter((id) => id === id)`) | 1 | 0 | 1 | 1 |
| every id exempt (`.filter((id) => id !== id)`) | 1 | 0 | 1 | 1 |

The first mutant reddens on `deepEqual(openActionFindings(cut, ["A-15"]), [])`;
the second on the `includes("open owner action A-15 has no register item")`
assertion. Script and logs are in the scratch `m5p1/` directory
(`witness.sh`, `witness-*.log`).

### p1-value, walked

Criterion (delivery/plan/value-delivery-plan.yaml:73): the evidence document
"names one pulse phase, its reviewed head, merged pull request, post-merge
push run and deploy verification, with every referenced artifact resolving
and every conclusion tied to an exit code or API result."

| part | named in Part 2 | tied to |
|---|---|---|
| one pulse phase | pulse M3-P4 | the pilot's own work history at `b7036d7` |
| its reviewed head | `dd6adff` (round 2), and that it is not the merged head | four verdict files, `grep` output |
| merged pull request | pulse #22, merge `b7036d7`, head `ec51961` | `GET /pulls/22`, HTTP 200 |
| post-merge push run | NONE EXISTS | `GET /actions/workflows` zero, `GET /actions/runs?head_sha=b7036d7...` total 0, HTTP 200 |
| deploy verification | none in the charter's sense (`reserved`); Vercel production deploy `success` observed instead | `GET /commits/b7036d7.../status`, `GET /deployments`, HTTP 200 |

**Verdict for p1-value: NOT MET as worded.** The document names every part,
and each part is tied to an API result or a command. But two parts are named
as absences: there is no post-merge push run and no deploy verification to
name. A row saying none exists does not meet a criterion that asks for a
run. I have not relabelled Vercel's deploy status as a "deploy
verification" or as a "push run" to make the row fill. That substitution is
the plan's green-without-delivery hazard one step removed
(delivery/plan/value-delivery-plan.yaml:89).

"Every referenced artifact resolving": the pilot's artifacts live in another
repository, so they are quoted, not cited, and do not resolve through this
repository's citations gate. What resolves is the pull request, run and
deployment by API (all HTTP 200) and the pilot files at a named sha in a
clone. Every in-repository citation in the document is checked by the
citations gate below.

### p1-boundary, second half

"The report states that the owner performed the reboot action": now true, and
stated. Evidence is pulse `main` moving from `d4e491b` to `b7036d7`, observed.
The first half stands as before: `git diff origin/main...HEAD --name-only` on
this branch lists only this repository's files (checked below), and nothing
was pushed to `pulse` or `pulse-fleet`.

### Acceptance, as of this resumption

Appended rather than editing the earlier table, which recorded p1-value as
PENDING and was true when written.

| criterion | status | evidence |
|---|---|---|
| p1-trigger | met (unchanged) | Part 1, step 1 |
| p1-probe | met by run 3 (unchanged) | Part 1, step 2 |
| p1-value | **NOT MET as worded**: post-merge push run and deploy verification are absent in the pilot | Part 2 and the walk above |
| p1-boundary | met: no pilot content or ref update; the owner performed the reboot | Part 2 first row; diff check below |

### Hazards, re-read against this resumption

- stale-pilot-evidence: every pilot reading here is from 2026-09-24, and each
  names its sha.
- simulated-owner-action: the reboot is recorded as done because the pilot's
  `main` moved, not because the orchestrator relayed the owner's word.
- green-without-delivery: no kernel gate result is offered for any Part 2
  row, and the p1-value verdict above refuses the Vercel substitution.

### Open questions for the orchestrator, from this resumption

1. Rows 3 to 5 of Part 2's rules are not met. Either they are accepted as
   they stand for the M4 exit, with the reason recorded, or the pilot runs a
   further phase that meets them (its reviewed head merged as reviewed, a CI
   workflow with a post-merge push run, and a decided release verification).
   That choice looks like one for the owner under DR-0016, since it decides
   whether M4's exit test is discharged. I have not made it.
2. `pulse-fleet` still pins kernel 0.1.0. No `A-n` was allocated for it; the
   register allocates, and whether it is owed is the orchestrator's call.

### Checks at head 311bf0a (node v26.6.0, from the scratch `checks.sh`)

```
node v26.6.0 head 311bf0a
build exit 0
status lines after build: 0
npm test exit 0
i tests 1486
i pass 1486
i fail 0
i cancelled 0
i skipped 0
i todo 0
authored-bytes exit 0
render --check exit 0
agent-rules-drift: green (24 rendered gate rows compared)
gate citations exit 0
gates: citations: green: linted 3 changed document(s) at 311bf0a0e39d04379035725b2be8ba4861149117: 26 citation(s) resolved, 0 self-citation(s), 0 unverifiable-external
gate scope exit 0
gates: scope: green: 6 changed path(s) audited against declaration delivery/plan/phase-declarations/m5-p1.json at merge base 69a7a607abb2932ff87bc580df22f304a38c2a0b ... DECLARATION AMENDED AT HEAD: 1 entry/entries ADDED ... filesToTouch test/retirement-inventory.test.ts.
```

Transliteration note: U+2139 was rendered `i`, 6 times, in the six count
lines. Some lines of the gate output are left out (the run ids, the registry
line, and the two clean-room-checklist gates the runner does not execute).
The scope line is shortened where `...` appears. Nothing else was changed.
The suite is `npm test` with `dist/` built. The gates ran with `--mode full
--phase m5-p1 --base origin/main --head HEAD`, one gate per run.

`git diff origin/main...HEAD --name-only` lists six paths, all in this
repository: `delivery/STATE.md`, `delivery/plan/phase-declarations/m5-p1.json`,
`delivery/verification/m4-exit-test-pulse.md`,
`delivery/verification/pulse-re-probe-m5.md`, `delivery/work-history/m5-p1.md`,
`test/retirement-inventory.test.ts`. No pulse content and no ref update.

## Resumption 3, 2026-09-24 evening: Part 2 re-run against pulse M3-P5

The orchestrator reports that the owner did A-17's steps in the pilot:
kernel 0.2.1 (pulse PR #23), CI (pulse PR #24), and pulse M3-P5 merged at its
reviewed head (pulse PR #25, then its verdicts in PR #26). That is the
trigger, not the evidence. The task: apply the rules written in advance in
delivery/verification/m4-exit-test-pulse.md:189 (Part 2) to M3-P5, add the
result as a new dated part, and keep the PR #22 record as it was.

### The merge of main, again

`git fetch origin claude/m5-p1-pulse-value-proof main`, then
`git checkout -B claude/m5-p1-pulse-value-proof origin/claude/m5-p1-pulse-value-proof`,
then `git merge --no-edit origin/main`: one conflict, `delivery/STATE.md`,
two hunks (the "In flight" list and "Owner actions open"). Resolved by taking
main's file whole (`git checkout --theirs delivery/STATE.md`) and then, by a
script that asserted each target occurs exactly once:

- replacing the `<!-- A-14 lands here ... -->` marker after A-10 in the
  register with this branch's A-14 bullet, taken byte for byte from
  `git show origin/claude/m5-p1-pulse-value-proof:delivery/STATE.md`;
- replacing main's "M5-P1 carries it; it waits on A-14." in the M4 closure
  paragraph with this branch's "A-14 is done" sentence.

`grep -c '^<<<<<<<\|^>>>>>>>' delivery/STATE.md` printed 0. Merge commit
`18618eb`. Toolchain: node v26.6.0 from the scratch prefix (`node --version`
printed `v26.6.0` in the shell that ran `npm ci`), `npm ci` exit 0.

### How the pilot was read, and one environment note

All reads are REST GETs through `curl` with the proxy supplying credentials,
from two scratch scripts (`api.sh` for JSON, `raw.sh` for file contents at a
ref, `Accept: application/vnd.github.raw`). Every request is logged with its
code in the scratch `m5p1-rerun/api.log`: 58 requests, all HTTP 200
(`cut -d' ' -f2- api.log | sort -u | grep -v 'HTTP 200'` printed nothing).
No clone was made this time: this agent's harness refused `git clone` into
the scratchpad as a git operation outside its worktree, so the REST contents
API was used instead. Nothing was pushed, and no pilot ref, file, comment or
pull request was created (DR-0037).

### What was read, with the output

Pull requests (`jq '{number,merged,merged_at,merge_commit_sha,head:.head.sha,base:.base.ref,merged_by:.merged_by.login,commits}'`):

```
PR 25  merged true  merged_at 2026-09-24T17:12:07Z
       merge_commit_sha 35d2e55798139604549a0dde26ada2cce4781feb
       head 98fbadc46e7018abc6480a8914c4ef92aeeb24dc  base main  merged_by ThomasHendrickx  commits 4
PR 26  merged true  merged_at 2026-09-24T17:39:41Z
       merge_commit_sha 1796ff8d5750ca185190e699daa24214dcf21bdd
       head 305575272b65eda900eff9d4a81688208298efb5  base main  merged_by ThomasHendrickx  commits 1
```

So the relay's "PR #26 (merge 3055752)" was PR #26's head; its merge is
`1796ff8`, which `GET /branches/main` reports as pulse `main` now.

Commits (`jq '{sha,tree:.commit.tree.sha,parents:[.parents[].sha],committer:.commit.committer.date}'`):

```
35d2e55  tree 8edba11701ff0f6c2eb2574091395ae02dcccfe5  parents dff0824 98fbadc  2026-09-24T17:12:06Z
98fbadc  tree 8edba11701ff0f6c2eb2574091395ae02dcccfe5  parents 31fd73d          2026-09-24T16:37:09Z
3055752  tree 0e7b9547e752f65a3e4ed019385878dd5af73048  parents 35d2e55          2026-09-24T17:12:44Z
1796ff8  tree 0e7b9547e752f65a3e4ed019385878dd5af73048  parents 35d2e55 3055752  2026-09-24T17:39:40Z
```

`GET /pulls/25/commits`: `fada6fb` 15:53:26Z, `98b4f0e` 15:53:46Z, `31fd73d`
16:06:26Z, `98fbadc` 16:37:09Z. `GET /compare/35d2e55...1796ff8`: ahead 2,
behind 0, files `delivery/review/m3-p5-criteria-round2.json` (added),
`delivery/review/m3-p5-hazard-round2.json` (added),
`delivery/work-history/m3-p5.yaml` (+49 -0).

Verdicts at `1796ff8` (`jq -c '{head:.head[0:7],verdict,pb:."produced-by",framing,contract:."review-contract",tv:."tiphys-version",sev:[.findings[]?.severity]}'`):

```
m3-p5-criteria-round2.json {"head":"98fbadc","verdict":"APPROVE","pb":"claude","framing":"criteria-contract","contract":"criteria","tv":"0.2.1","sev":["low","low","low"]}
m3-p5-criteria.json        {"head":"98b4f0e","verdict":"FIX-ROUND-NEEDED","pb":"claude","framing":"criteria-contract","contract":"criteria","tv":"0.2.1","sev":["medium","low","low"]}
m3-p5-hazard-round2.json   {"head":"98fbadc","verdict":"APPROVE","pb":"claude","framing":"unauthenticated-write-and-cache-paths","contract":"hazard","tv":"0.2.1","sev":["low","low","low"]}
m3-p5-hazard.json          {"head":"98b4f0e","verdict":"FIX-ROUND-NEEDED","pb":"claude","framing":"unauthenticated-write-and-cache-paths","contract":"hazard","tv":"0.2.1","sev":["high","low","low","low","low"]}
```

No verdict file has a timestamp key (`jq -r 'keys|join(",")'` on each).

CI, statuses and deployments (the scratch `ci.sh` and `deploy.sh`; full
output in `ci.out` and `deploy.out`):

```
== workflows
{"id":366168847,"name":"CI","path":".github/workflows/ci.yml","state":"active"}
== runs 35d2e55
total 1
36032643312  CI  push  completed  cancelled  main  35d2e55  attempt 1  2026-09-24T17:12:10Z  2026-09-24T17:40:00Z
== check-runs 35d2e55
107744934920  slow gate (Playwright)  completed  cancelled  github-actions  2026-09-24T17:12:13Z  2026-09-24T17:39:59Z
107744934341  fast gate               completed  success    github-actions  2026-09-24T17:12:13Z  2026-09-24T17:13:15Z
== runs 1796ff8
36035853808  CI  push  completed  success  main  1796ff8  attempt 1  2026-09-24T17:39:44Z  2026-09-24T18:12:51Z
== check-runs 1796ff8
107755742737  slow gate (Playwright)  completed  success  github-actions  2026-09-24T17:40:02Z  2026-09-24T18:12:49Z
107755742507  fast gate               completed  success  github-actions  2026-09-24T17:40:02Z  2026-09-24T17:41:05Z
== runs 98fbadc
36028645734  CI  pull_request  completed  success  claude/tender-albattani-2btl5u  98fbadc  attempt 1
== push runs on main
total 3
36035853808  CI  push  completed  success    1796ff8  1
36032643312  CI  push  completed  cancelled  35d2e55  1
36021312781  CI  push  completed  success    dff0824  1
== status 35d2e55: combined success, 1 context: Vercel success 17:13:29Z "Deployment has completed"
== deployments 35d2e55: 6643693281  Production  vercel[bot]  2026-09-24T17:13:29Z; its status: success 17:13:31Z
== deployments 1796ff8: 6644236912  Production  vercel[bot]  2026-09-24T17:40:59Z; its status: success 17:40:59Z
```

The cancelled run's jobs (`GET /actions/runs/36032643312/jobs`): fast gate
all steps success; slow gate steps 1 to 10 success, step 11
`Run npm run test:e2e` cancelled. `ci.yml` at `1796ff8` sets
`concurrency: group: ci-${{ github.ref }}` and `cancel-in-progress: true`.
Some columns above are shortened for width (the run created and updated
times for `98fbadc` and for the push list); nothing else was changed.

Deploy verification, looked for:

- `charter.yaml` at `1796ff8` lines 101 to 107: `release-verification:`,
  `mode: reserved`, and the note naming the candidate. The fleet's
  `charter/pulse.yaml` at `8fa9a82`: the same.
- `GET /git/trees/1796ff8?recursive=1`, `truncated false`; paths matching
  `release|deploy|evidence|verif|vercel`: `delivery/evidence`,
  `delivery/evidence/.gitkeep`, `playwright.deploy.config.ts`. The last is a
  wrapper for "the fleet's deploy-verify stage"; `ci.yml` does not run it.
- `grep -n -i 'deploy\|vercel\|production\|release'` over the M3-P5 work
  history: no output.
- `pulse-fleet` `notes/deployed-infrastructure.md` at `8fa9a82`: section
  headings run from the M1-P1 deploy-verify record to the M3-P2 deploy-verify
  saga; `grep -n -i 'M3-P5\|M3-P4\|35d2e55\|98fbadc\|b7036d7'`: no output.

The kernel pins:

- pulse at `35d2e55`: `package.json` line 31 `"@tiphys/kernel": "0.2.1"`;
  `package-lock.json` line 3474 resolves `kernel-0.2.1.tgz`, integrity
  `sha512-ONgrxG3KE+5DJ9bxOpYVYfSX0UP6zzF0urYA7LRPTwplftyQEkHI947cP60dHSP6ZqFRhOXV+FK++MYYwAQkqw==`;
  `charter.yaml` line 9 `kernel-version-pin: 0.2.1`.
- `npm view @tiphys/kernel@0.2.1 dist.integrity` printed the same string.
- pulse-fleet `main` is `8fa9a82` (`GET /branches/main`, "Merge pull request
  #1", 2026-09-24T17:13:22Z); `package.json` line 7 pins
  `"@tiphys/kernel": "0.2.1"`, and its lock resolves the same tarball and
  integrity. Its recent commits: `b675118` "kernel: move the fleet pin from
  0.1.0 to 0.2.1" (15:03:05Z), `ad4538f` "notes: the M3-P5 merge at the
  approved commit..." (17:13:10Z).
- In this repository: `git rev-parse v0.2.1^{commit}` printed
  `2c49ab39d25b08783c8dd6422e96adc38de222f2`;
  `git merge-base --is-ancestor de1664d v0.2.1` exit 0.

The pilot's review-family rule: `pulse-fleet` `decisions/DR-0003.yaml` at
`8fa9a82`, `status: decided`, "Same family, varied lenses (owner,
2026-08-17), for this pilot only", provided review-contract and framing are
distinct across the pair.

### The rows, judged

The judgment is written out in Part 3 of the exit-test document
(delivery/verification/m4-exit-test-pulse.md:317). In one line each:

| rule | verdict | decisive evidence |
|---|---|---|
| 1 reboot | met | pulse `main` moved (unchanged from Part 2) |
| 2 ran on v1 | met | pins, lock integrity, charter and verdicts all 0.2.1; `de1664d` is an ancestor of `v0.2.1` |
| 3 reviewed head equals merged head | met | round-two verdicts both name `98fbadc` and APPROVE; `35d2e55` parents `dff0824 98fbadc`, trees equal `8edba11` |
| 4 post-merge push run | met, by T-009's cancelled-run discharge (CLAUDE.md:548) | `35d2e55` run cancelled by the `1796ff8` push; `1796ff8` push run 36035853808 completed success, and `1796ff8` differs from `35d2e55` only in three records files |
| 5 deploy verification | NOT met | charter `release-verification: mode: reserved`; no pushed record verifies `35d2e55` or `1796ff8` |

Two judgment calls, stated so a reviewer can contest them rather than find
them:

1. Rule 4. The pre-written rule says "per T-009" and "a cancelled run is
   neither green nor red". I applied T-009's own procedure for a cancelled
   run (CLAUDE.md:548): verify the current `main` head's push run to
   completion, and say the head's own run was cancelled. A stricter reading
   (only a completed run whose head sha IS `35d2e55` counts) would make row 4
   fail. The compare output above is why I took the T-009 reading: the
   `1796ff8` run tested the M3-P5 code byte for byte.
2. Rule 3, ordering. The rule asks that the reviewed head named in the
   pilot's review evidence equal the merged head, and it does. Whether the
   round-two reviews were produced before the merge is the pilot's recorded
   claim only; the files carry no timestamp, and the 37-second gap from the
   merge to the verdict commit is a deduction in its favour, not an
   observation.

Decorrelation: this test's rules, DR-0037, DR-0041 and DR-0042 are silent on
it. I did not add a rule. The pilot's DR-0003 is met by the pair (distinct
contract and framing).

### p1-value, walked again

Criterion (delivery/plan/value-delivery-plan.yaml:73), for pulse M3-P5:

| part | named in Part 3 | tied to |
|---|---|---|
| one pulse phase | pulse M3-P5 | pulse PR #25 title and `delivery/work-history/m3-p5.yaml` in pulse |
| its reviewed head | `98fbadc`, equal to the merged head | two round-two verdict files; `GET /commits/35d2e55` parents and tree |
| merged pull request | pulse #25, merge `35d2e55` | `GET /pulls/25`, HTTP 200 |
| post-merge push run | `1796ff8` run 36035853808 success, with `35d2e55`'s run 36032643312 cancelled by it | `GET /actions/runs?head_sha=...`, `GET /actions/runs/<id>/jobs`, HTTP 200 |
| deploy verification | NONE in the charter's sense; Vercel production deploys observed instead | charter text; tree, grep and deployment reads above |

**Verdict for p1-value: still NOT MET as worded**, on the deploy-verification
part alone. I did not relabel Vercel's deploy status, or the green push run,
as a deploy verification (delivery/plan/value-delivery-plan.yaml:89).

### Acceptance, as of this resumption

| criterion | status | evidence |
|---|---|---|
| p1-trigger | met (unchanged) | Part 1, step 1 |
| p1-probe | met by run 3 (unchanged) | Part 1, step 2 |
| p1-value | **NOT MET as worded**: deploy verification absent in the pilot; the other four parts are now named and met for M3-P5 | Part 3 and the walk above |
| p1-boundary | met: no pilot content or ref update; the owner performed the reboot | diff check below |

### What changed on this branch in this resumption

- `delivery/verification/m4-exit-test-pulse.md`: the status line now names
  the M3-P5 re-run and the one failing row, and keeps the PR #22 status as
  "earlier status". The "two parts" sentence now says three. Part 3 is new,
  after Part 2's residue; Parts 1 and 2 are otherwise unchanged. Part 2's
  heading moved from line 180 to line 189 because of the status-line edit.
  The earlier entry above that says "heading now at ...:180" was true when
  written and is left as it was; the line it cites now falls inside the
  Part 1 open questions.
- `delivery/STATE.md`: from the merge, main's structure with this branch's
  A-14 bullet at the marker. Then: the M5-P1 "In flight" bullet says the
  re-run leaves one failing row and cites Part 3; A-17's short entry gains a
  dated correction of its 17:38 reading (round-two verdicts, trees equal,
  pulse-fleet pin, the cancelled and green push runs, and that pulse's CI
  does not run `tiphys gates`); the A-14 bullet's citation now points at
  Part 2's new line; the M4 closure sentence cites Parts 2 and 3.
- No declaration grant was needed: every changed path is already on the
  declaration (checked by the scope gate below).

### Open questions for the orchestrator

1. Row 5 is the only failing row. What closes it is in the pilot: the owner
   decides pulse's `release-verification` shape (pulse DR-0014), and the
   pilot pushes a record of that verification run against a merged sha
   (`35d2e55` qualifies; its production deployment is 6643693281). A-17's
   three steps do not include this. Whether it becomes a new `A-n`, or
   whether the orchestrator judges row 5 differently, is not mine to decide.
2. Row 4 rests on T-009's cancelled-run discharge. If the orchestrator or a
   reviewer holds to the stricter reading, row 4 fails too, and what closes
   it is one completed push run whose head is a phase merge.
3. A-17 step 2 asked for CI "that runs the Tiphys gates". Pulse's CI runs the
   pilot's own gates. Whether that meets the step is the orchestrator's call;
   it does not affect any exit-test rule, since none asks for kernel gates.

### Suite at d2b557a: three reds, all in one history-window test

`npm test`, node v26.6.0, `dist/` built (`npm run build` exit 0), from this
worktree at d2b557a: tests 1512, pass 1509, **fail 3**, cancelled 0, skipped
0, exit 1. All three are in test/verdict-head.test.ts (tests at lines 514,
727 and 856), with "git is available but no pre-head commit was found" and
"git is available but the pre-change tree could not be staged".

They are not caused by this resumption's files, which are all under
`delivery/`. Measured:

| head | `node --test test/verdict-head.test.ts` |
|---|---|
| origin/main `13322e8` (detached checkout, same worktree and toolchain) | tests 41, pass 41, fail 0 |
| this branch, d2b557a | tests 41, pass 38, fail 3 |

The mechanism, as far as measured: `preHeadCommit` walks
`git rev-list --max-count=200 HEAD` (test/verdict-head.test.ts:252) looking
for the newest ancestor whose verdict schema lacks the `head` property.
`git rev-list --count HEAD ^origin/main` printed 17, and exactly 17 commits
of origin/main's 200-commit window are absent from HEAD's window (`comm -23`
over the two sorted lists). So this branch's own 17 commits push the
qualifying ancestor out of the window. I did not confirm which of the 17
dropped commits is the one the test needs (the loop that would read each
one's schema was refused by this agent's harness); that step is the one that
would settle it. The same failure would be expected on the pull request run,
whose merge ref has these 17 commits as ancestors; that is a prediction, not
an observation.

test/verdict-head.test.ts is not on this phase's declaration, so I did not
change it. Open question 4 for the orchestrator: widen or remove the window in
that test (a kernel change, outside M5-P1), or reduce this branch's
commit count by how it lands.

Environment note: while this worktree was detached for that measurement,
another worktree (`agent-a09bc9ff2a19d1777`) checked out
`claude/m5-p1-pulse-value-proof` at d2b557a (`git worktree list`). This
agent then worked detached at d2b557a and pushed by sha.
