# M5-P1 work history: pulse value proof

Phase M5-P1, branch claude/m5-p1-pulse-value-proof, cut from origin/main at
e81c5e8. Governing entry: delivery/plan/value-delivery-plan.yaml (M5-P1),
declaration delivery/plan/phase-declarations/m5-p1.json:1.

## Log

- Branch created from origin/main e81c5e8. `git rev-parse
  --is-shallow-repository` printed `false` in this worktree.

## Step 1: the kernel's fleet home, and the trigger (2026-09-23)

Toolchain for every command below: node v26.6.0 from the scratch prefix
first on PATH, checked with `node --version` in the same shell. `npm ci`
exit 0.

### The fleet copy, and what the kernel says to do about a clone

The orchestrator's clone /home/user/tiphys-ai-helmsman-fleet was read only
(`ls`, `cat .gitignore`, and as the source of a local `git clone`); no command in
this log writes to it. A local copy was made without
network:

```
$ git clone /home/user/tiphys-ai-helmsman-fleet $S/m5p1/fleet     exit 0
$ git checkout main                                                "Already on 'main'"
$ git rev-parse HEAD                                               20584905c5c3ad9d0c707aa3df8f2eb549a0e275
$ git for-each-ref
refs/heads/main 2058490
refs/remotes/origin/HEAD 2058490
refs/remotes/origin/main 2058490
refs/remotes/origin/probe-lease-a ddc5b10
```

`$S` is this session's scratchpad. The clone carries `charter/`,
`decisions/`, `tasks/` (holding only `.gitkeep`), `backlog.md`,
`package.json` and `.gitignore`, which ignores `state/`, `worktrees/` and
`projects/`.

What the kernel itself does about a clone, read before creating anything:
src/fleet.ts:173 says a clone carries the durable half and not the ephemeral
one, "which is the fact `tiphys resume` exists to act on"; `tiphys init`
refuses an initialised directory and names `tiphys resume` as the remedy
(src/commands/init.ts:125). So the kernel's own command was used and nothing
was created by hand:

```
$ node bin/tiphys.ts init $S/m5p1/fleet
tiphys init: .../m5p1/fleet is already initialized; run tiphys resume to rebuild the ephemeral directories a clone does not carry
exit 1
$ node bin/tiphys.ts resume $S/m5p1/fleet
REBUILT state/
REBUILT worktrees/
REBUILT projects/
exit 0
```

`tiphys doctor`, run with the fleet copy as working directory, exit 0; its
non-PASS rows were `gh WARN not found`, `beacon WARN watcher not running`,
`retention WARN no charter document`, and `branches WARN 1 of 2 pushed
branch(es) unmerged into origin/main: origin/probe-lease-a`. `CHECK tasks PASS
0 open of 0`, `CHECK lock PASS no lease present`.

Is the orchestrator's clone current against the real remote? Read-only:

```
$ git ls-remote https://github.com/ThomasHendrickx/tiphys-ai-helmsman-fleet.git     exit 0
ddc5b1030535d47dc113778aef6e193aa477ae24	HEAD
20584905c5c3ad9d0c707aa3df8f2eb549a0e275	refs/heads/main
970cd5bdc98686c8bf89973cdbe41d6b67507c7f	refs/heads/probe-fleet-home
ddc5b1030535d47dc113778aef6e193aa477ae24	refs/heads/probe-lease-a
ddc5b1030535d47dc113778aef6e193aa477ae24	refs/heads/tiphys/lease
302528d5b4b00c7e587d8a69b105a62b2701980c	refs/heads/tiphys/lease-r1
4b1c3bd2229f58c291008453680ef660954df8d7	refs/heads/tiphys/lease-r2
e3df60c000a7117b553c9816305697b4caa05abf	refs/heads/tiphys/lease-r3
$ git ls-remote --symref <same> HEAD
ref: refs/heads/probe-lease-a	HEAD
```

Findings, none of which changes a verdict below:

- `main` on the remote equals the copy's `main` (2058490), so the durable half
  used here is current.
- The remote carries six branches besides `main`; the orchestrator's clone
  knows one of them. They are left over from the M4 cross-environment
  exclusion probes. Drain does not count branches (M4-D-15), so this does not
  move arm a, and branch cleanup is an owner action (CLAUDE.md standing
  warning 14).
- **The remote's default branch is `probe-lease-a`, not `main`.** A fresh
  clone of the fleet remote without `git checkout main` lands on a probe
  lease commit whose tree holds only `lease.json`. Recorded as an open
  question for the orchestrator; changing a default branch needs repository
  admin access.

### What a clone can and cannot say about drain

Drain counts live worktrees under `worktrees/` and open tasks under `tasks/`
(src/cutover.ts:518). `worktrees/` is ephemeral and `resume` rebuilds it
empty, by the kernel's design ("rebuilt, not restored",
src/commands/resume.ts:18). So on this copy the worktree half of drain is
empty by construction and only the tasks half reads committed state. That
tasks half is the part that survives a reclaim. A live worktree on some other
machine running this fleet would not be visible here. Neither clone checked
here holds `state/` or `worktrees/`, so I found no live fleet home in this
container to read instead.

### The trigger

```
$ git rev-parse --is-shallow-repository
false
$ node scripts/check-cutover-entry.mjs --fleet $S/m5p1/fleet
ARM a drain satisfied -- cutover status reports DRAIN clean
ARM b exclusion satisfied -- all 6 required behavior name(s) resolve and 2 exclusion test file(s) pass with a nonzero pass count
ARM c retirement satisfied -- all 199 retirement row(s) are ported
ARM d pre-freeze-ruleset satisfied -- delivery/plan/cutover/pre-freeze-ruleset.json is present and is newer than delivery/plan/cutover/retirement-inventory.json by commit order (1789726959 against 1789717584)
STEP 1 preconditions: preconditions-satisfied-owner-action-pending
[STEP 2, 3, 4 lines as printed; STEP 3 reads "HALT, OWNER ACTION"]
exit 0
```

The raw report arm a read:

```
$ node bin/tiphys.ts cutover status --fleet $S/m5p1/fleet
SWITCH planning-and-scope current
SWITCH review-and-arbitration current
SWITCH credentials-and-refs current
SWITCH salvage-and-recovery current
SWITCH closeout current
DRAIN clean
BRANCHES 1 pushed and unmerged, informational: drain does not count branches (M4-D-15)
PRE-FREEZE not-required no switch reads kernel, so there is no freeze to have captured
CANNOT-SEE open pull requests
CANNOT-SEE CI conclusions
CANNOT-SEE post-merge push runs
CANNOT-SEE whether the pilot's own fleet is drained (DR-0037: the pilot is not this orchestrator's subject)
exit 3
```

Exit 3 is `EX_WORK_REMAINS` because no switch reads `kernel`
(src/commands/cutover.ts:320); arm a reads the DRAIN row only, so the two are
consistent. `BRANCHES 1` counts the copy's view (`origin/probe-lease-a`), not
the remote's six.

## Step 2: the pilot re-probe (2026-09-23)

```
$ node scripts/probe-pilot-readonly.mjs --out delivery/verification/pulse-re-probe-m5.md
TARGET ThomasHendrickx/pulse refused -- record refused: https://api.github.com/repos/ThomasHendrickx/pulse answered HTTP 403; commits refused: https://api.github.com/repos/ThomasHendrickx/pulse/commits?per_page=1 answered HTTP 403
TARGET ThomasHendrickx/pulse-fleet refused -- record refused: https://api.github.com/repos/ThomasHendrickx/pulse-fleet answered HTTP 403; commits refused: https://api.github.com/repos/ThomasHendrickx/pulse-fleet/commits?per_page=1 answered HTTP 403
OVERALL refused
EVIDENCE .../delivery/verification/pulse-re-probe-m5.md
exit 3
```

The evidence document records `gitRef=satisfied` for both targets, heads
`d4e491b` and `7656f67`. A plain-GET control (a scratch script, GET only)
showed the 403 body names the session's repository allowlist and `add_repo`,
while the kernel repository and the kernel fleet repository answered 200 and
`/rate_limit` answered 200 in the same shell. The remedy the body names is a
push-scoped attach; I did not do it (DR-0037), and it is open question 2
below.

Independent reads, all exit 0: `git ls-remote --symref` on both pilot
repositories (default branch `main` for both), `git ls-remote --heads` on
`pulse` (106 branches), and `git clone --depth 1` of both into the session
scratchpad to read the head commit dates (both 2026-08-29) and
`pulse-fleet/package.json` (pins `@tiphys/kernel` 0.1.0). The comparison with
the 2026-08-20 and 2026-09-16 readings is appended to the probe document
under a heading that says it was not written by the script.

**p1-probe is NOT met: exit 3, not 0.** I stopped short of treating the git
read as success, as the brief requires.

## Step 3 and 4: has the reboot happened, and the A-n allocation

Reboot: no record in delivery/STATE.md (`grep -n -i reboot` gives only the
DR-0042 summary row at line 1581) or in DR-0042, and neither pilot head has
moved since 2026-09-16. So no reboot has produced pushed work; whether a
reboot without pushed work happened is not observable from here.

Allocation, after `git fetch origin` so other sessions' pushed branches are
visible to `--all`:

```
$ grep -n -o "\bA-[0-9]\+\b" delivery/STATE.md | ... | sort -u | tail     highest: A-10
$ git log --all --format='%h %s' -G'\bA-1[0-9]\b' -- delivery/STATE.md
85deb36 M4 paperwork: ... (#150)
6b42d67 Correct T-028: my salvage reverted a completed fix round, and register A-9 and A-10
$ git log --all --oneline -S'A-11'     a7b7b07, 976da5f (M3-P4)
$ git log --all --oneline -S'A-12'     a7b7b07 (M3-P4)
$ git log --all --oneline -S'A-13'     a7b7b07, 976da5f (M3-P4)
$ git log --all --oneline -S'A-14'     (no output)
```

The M3-P4 hits are row labels in a fixture table
(delivery/work-history/m3-p4.md:3599), not owner actions. The highest owner
action ever registered is A-10. I took **A-14** rather than A-11 so that the
allocation command the rules prescribe returns no false hits for the new id.
That is a judgment and it is recorded as one; the orchestrator may prefer
A-11. A sibling wave-A phase that also allocates an A-n in its own unmerged
STATE.md could pick the same id; `git log --all -S'A-14'` after the fetch
showed none at 2026-09-23.

A-10 already registers the fleet remote's probe branches and changed default
branch, so the `probe-lease-a` default-branch finding needs no new id.

## Step 5 and 6: the documents

- delivery/verification/m4-exit-test-pulse.md: Part 1 with the trigger, the
  probe, the reboot check and three open questions; Part 2 PENDING with every
  field empty and the read-only verification rule for each written in
  advance.
- delivery/STATE.md: one new register entry (A-14) directly after A-10, and
  one new block at the end, "M5 HAS STARTED". No other section edited. The
  pre-pass and DR-0050 are QUOTED, not cited: `git branch -a --contains
  ffa5f56` names only `claude/m5-plan-readiness-y2jjo5`, so neither file is on
  `main` at `e81c5e8`, and the common brief's instruction to read the pre-pass
  at its main path does not work at this head. I read it from that branch with
  `git show ffa5f56:delivery/plan/m5-conflict-pre-pass.md`.

Then I stopped, as the brief says. I did not reboot, start or contact the
pilot, and I did not simulate the reboot.

## Pre-submit hazard self-review

The plan's three hazard classes (delivery/plan/value-delivery-plan.yaml:82),
each attacked with "can this pass while the thing it guards is broken?"

- **stale-pilot-evidence.** Attack: is any pilot fact here copied from an
  older record rather than read today? Every head in the new documents comes
  from a 2026-09-23 command quoted with its exit code; older readings appear
  only in a column labelled with their date and source. A second attack: is
  "the heads have not moved" a stale reading passed off as fresh? No. The
  sameness is itself the 2026-09-23 observation, and the commit dates
  (2026-08-29) come from depth-1 clones made today. Residue: REST was refused,
  so default branch and visibility come from git `--symref` only, and
  visibility was not read at all.
- **simulated-owner-action.** Attack: does any line record the reboot as done
  or imply it? A-14 reads "OPEN, not yet done"; the exit-test Part 2 row for
  the reboot reads PENDING; p1-boundary's second half is reported PENDING, not
  passed. A second attack: could the trigger's exit 0 read as permission? Its
  own output prints STEP 3 as "HALT, OWNER ACTION", and the documents quote
  that line.
- **green-without-delivery.** Attack: do the green kernel gates below get
  offered as evidence for p1-value? No. p1-value is PENDING and the exit-test
  document says a green kernel gate is evidence for no row in Part 2.

One attack found something: arm a satisfied is weaker than it reads, because
a rehydrated clone has an empty `worktrees/` by construction. It is stated in
both the work history and the exit-test document rather than left for a
reviewer to find.

## Acceptance criteria

| id | result | evidence |
|---|---|---|
| p1-trigger | PASS | `check-cutover-entry.mjs --fleet <kernel fleet home copy>` exit 0, four arms `satisfied`, step 1 above; the fleet is the kernel's (remote `main` `2058490`), rehydrated by `tiphys resume`, not a scratch `tiphys init` |
| p1-probe | **FAIL** | `probe-pilot-readonly.mjs --out delivery/verification/pulse-re-probe-m5.md` exit 3, `OVERALL refused`; the document does name every target, head (git), transport and the excluded writes |
| p1-value | PENDING owner action A-14 | Part 2 of delivery/verification/m4-exit-test-pulse.md is empty by design |
| p1-boundary | first half PASS, second half PENDING | `git diff --stat origin/main...HEAD` lists four files, all in this repository's `delivery/`; no pulse file is added, and nothing was pushed except this branch to this repository. The documents quote pulse commit subjects, sha values and one `package.json` pin line as observations. The owner has not performed the reboot, so this report does not say it was performed |

## Gates (node v26.6.0 from the scratch prefix, `node --version` checked in each shell)

| gate | command | result |
|---|---|---|
| install | `npm ci` | exit 0 |
| build | `npm run build` | exit 0; `git status --short` empty afterwards |
| suite | `npm test`, `dist/` built | exit 0; tests 1394, pass 1394, fail 0, cancelled 0, SKIPPED 0, todo 0 |
| authored bytes | `node scripts/check-authored-bytes.mjs` after staging | exit 0 |
| merge check | `git merge-tree --write-tree origin/main HEAD`, origin/main `e81c5e8` | exit 0, tree `dc7f03a` |
| PR bundle | `scripts/m2-exit-test.sh --base origin/main --head HEAD --phase m5-p1 --bundle pr --no-build <scratch>` | exit 0 |

The bundle's lines, quoted:

```
gates: declared 15 applicable 10 verdict 10 green 10 red 0 not-applicable 5 error 0 vacuous 0
gates: suite: green: ... reported 1394 test(s) from 68 file(s) (pass 1394, fail 0, skipped 0, todo 0, did-not-run 0) ...
gates: citations: green: linted 3 changed document(s) at 51a519a...: 29 citation(s) resolved, 0 self-citation(s), 0 unverifiable-external
gates: scope: green: 4 changed path(s) audited against declaration delivery/plan/phase-declarations/m5-p1.json at merge base e81c5e8... (1 declared path(s) not touched: delivery/plan/phase-declarations/m5-p1.json)
gates: required gate(s) not applicable: red-witness
m2-assert (PR bundle): OK. 15 gate record(s) match section 1.4; ... zero red; zero error; zero vacuous.
```

`red-witness` is not applicable because no path under `src/`, `bin/` or
`plugin/` changed; this phase adds no test and no behavior, so there is no
red witness to show and nothing was added to test/behaviors.json. The
`gate-classes` line notes that the `review` class is satisfied only by naming
`check-dual-review`, which asserts nothing until reviews exist.

## Deviations

1. The pre-pass the common brief names at delivery/plan/ is not on `main`; I
   read it from the orchestrator's unmerged branch and quoted it in STATE.md.
2. A-14 instead of A-11, with the reason above.
3. The probe evidence document carries a second section below the script's
   output, clearly headed, with the comparison the plan step asks for. Its
   first section is the script's output unchanged.

## Open questions for the orchestrator

1. Which kernel release is "v1" for the pilot's phase? Only 0.1.0
   (2026-08-15, before M4) is published, and both fleet homes pin it. Settle
   this before A-14 goes to the owner.
2. Should the session attach `pulse` and `pulse-fleet` with push access, to
   get read-only REST (check runs, deployments) for step 4? That grants write
   authority DR-0037 keeps out of reach. Without it, p1-probe cannot exit 0
   in a session like this one.
3. p1-probe failed only on the exit code. The plan may want to accept a git
   read as the probe's success when REST is refused; that is a plan change,
   not mine to make.

## Resumption, 2026-09-23 08:01Z: p1-probe re-run

The orchestrator reported that `pulse` and `pulse-fleet` are now attached to
this session with push access. It asked for p1-probe to be re-run as the
criterion states, and for p1-value not to be started. Everything above this
heading is left as written, including the exit-3 run and its diagnosis. The
diagnosis is corrected here, not edited there.

### REST reachability, probed first

```
$ bash <scratch>/rest-control.sh            (curl, GET only, through HTTPS_PROXY)
GET /repos/ThomasHendrickx/pulse -> 200 : { "id": 1335801685, ... "full_name": "ThomasHendrickx/pulse", "private": false, ...
GET /repos/ThomasHendrickx/pulse-fleet -> 200 : { "id": 1336274599, ... "full_name": "ThomasHendrickx/pulse-fleet", ...
GET /rate_limit -> 200 : { "resources": { "core": { "limit": 15000, ...
2026-09-23T08:01:47Z
```

### Three invocations of the probe, all node v26.6.0

```
$ node scripts/probe-pilot-readonly.mjs --out delivery/verification/pulse-re-probe-m5.md
probe-pilot-readonly: .../delivery/verification/pulse-re-probe-m5.md already exists and this run would truncate it before its first read. Pass --force to overwrite it, or --out <path> to write elsewhere. Nothing was probed and nothing was written.
exit 64

$ node scripts/probe-pilot-readonly.mjs --out delivery/verification/pulse-re-probe-m5.md --force
TARGET ThomasHendrickx/pulse refused -- record refused: https://api.github.com/repos/ThomasHendrickx/pulse answered HTTP 403; commits refused: https://api.github.com/repos/ThomasHendrickx/pulse/commits?per_page=1 answered HTTP 403
TARGET ThomasHendrickx/pulse-fleet refused -- record refused: https://api.github.com/repos/ThomasHendrickx/pulse-fleet answered HTTP 403; commits refused: https://api.github.com/repos/ThomasHendrickx/pulse-fleet/commits?per_page=1 answered HTTP 403
OVERALL refused
exit 3

$ NODE_USE_ENV_PROXY=1 node scripts/probe-pilot-readonly.mjs --out delivery/verification/pulse-re-probe-m5.md --force
TARGET ThomasHendrickx/pulse satisfied -- record, newest commit and HEAD ref all read, and the two heads agree
TARGET ThomasHendrickx/pulse-fleet satisfied -- record, newest commit and HEAD ref all read, and the two heads agree
OVERALL satisfied
EVIDENCE .../delivery/verification/pulse-re-probe-m5.md
exit 0
```

The first refusal is the probe protecting run 1's record (entry-trigger step
2). I then passed `--force`, the documented deliberate overwrite, after
copying run 1's file to the scratchpad; its committed copy is 618544b.

Why run 2 was refused although curl read 200 a minute earlier: Node's
built-in `fetch` ignores `HTTPS_PROXY` unless `NODE_USE_ENV_PROXY=1` is set
(the container's proxy README says so at its line 71). A scratch GET-only
script with the probe's exact headers got `403 API rate limit exceeded for
34.45.210.119` for `pulse` AND for `tiphys-ai-helmsman` without the variable,
and 200 for both with it. So the probe was going straight to GitHub with no
credentials. **Correction to step 2 above:** run 1's 403 was not shown to be
the session allowlist. That diagnosis came from curl, which takes a different
network path. The probe's own 403 bodies were never read, because the script
classifies any 403 as `refused` without reading it.

Run 3 is the criterion's command with two additions, and both are stated
rather than hidden. `--force` is needed because the output path already
existed. `NODE_USE_ENV_PROXY=1` routes Node's `fetch` through this
container's proxy, as `PATH` selects the toolchain. The script and its
arguments are otherwise exactly the criterion's.

Run 3's record: `pulse` head `d4e491b`, default branch `main`, public,
updated 2026-08-29T16:29:21Z; `pulse-fleet` head `7656f67`, `main`, public,
updated 2026-08-29T17:04:48Z. REST and git agree on both. Neither head has
moved since 2026-09-16.

**p1-probe: PASS on run 3**, exit 0. The document names both targets, their
heads, the three transports and the excluded writes; the last two are in the
appended section, since the script's output does not list them.

### What still blocks

- p1-value was not started, as instructed. It waits on A-14 (the pilot
  reboot). The orchestrator says A-14 waits on kernel 0.2.0. At
  2026-09-23T08:03:36Z, `npm view @tiphys/kernel versions --json` returned
  `["0.0.0","0.1.0"]`, exit 0, so 0.2.0 was not yet visible from here.
- p1-boundary's second half still waits on the owner.
- Open question 2 (push-scoped attach) is answered by the orchestrator's
  attach. Open question 3 (accepting a git-only read) no longer applies,
  because REST now reads.
- A lesson for any Node REST caller in this container: set
  `NODE_USE_ENV_PROXY=1`, or it bypasses the proxy and fails as a 403 that
  looks like an authorization refusal.
