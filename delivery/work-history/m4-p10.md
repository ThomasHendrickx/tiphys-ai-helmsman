# M4-P10: the verdict head, the medium escalation, and the first non-vacuous dual review

- phase: M4-P10
- branch: `claude/m4-p10-verdict-head-and-medium`
- base: `plan/pstack-borrow-review` at `6961186`
- spec: delivery/plan/kernel-plan-m4.md:1619
- conflict pre-pass: delivery/plan/m4-conflict-pre-pass.md:15

## 1. The migration cost, re-derived tree-wide, and it is NOT zero

The plan asserts the migration cost is "measurably ZERO"
(delivery/plan/kernel-plan-m4.md:1628) on the strength of two greps, and asks
for both to be re-run at dispatch. They were. They still exit 1, and they are
both scoped wrongly.

```
$ grep -rln '^kind: verdict' delivery/review/ ; echo "exit=$?"
exit=1
$ grep -rl '"kind": *"verdict"' delivery/ ; echo "exit=$?"
exit=1
```

The first searches only `delivery/review/`. The second searches all of
`delivery/` but only for the JSON spelling, and every verdict document in this
repository is YAML. So between them the two greps never look for the YAML
spelling outside one directory. The tree-wide search:

```
$ git ls-files -z | xargs -0 grep -ln -E '"kind"[[:space:]]*:[[:space:]]*"verdict"|^kind:[[:space:]]*verdict'
delivery/evidence/m3-exit-test/e1/e1-7/verdict-criteria.yaml
delivery/evidence/m3-exit-test/e1/e1-7/verdict-hazard.yaml
witness/fixtures/dual-review/decorrelated-criteria.yaml
witness/fixtures/dual-review/decorrelated-hazard.yaml
witness/fixtures/dual-review/shared-contract-criteria.yaml
witness/fixtures/dual-review/shared-family-hazard.yaml
witness/fixtures/dual-review/shared-framing-hazard.yaml
exit=0
```

**SEVEN verdict documents exist and none carried a head.** The number that
matters at dispatch is seven, not zero. The plan's claim is a true statement
about the two commands it names and a false statement about the repository.

**Which of the seven are LIVE was then measured, because the cost of a required
field is the cost of migrating the documents something still reads.**

```
$ git grep -ln 'fixtures/dual-review' -- .
delivery/evidence/m4-probes/m4-intake-review.md
delivery/evidence/m4-probes/model-observability.md
delivery/plan/m4-intake.md
delivery/work-history/m3-p9.md
test/dual-review.test.ts

$ git grep -ln 'm3-exit-test' -- test/ scripts/ src/ bin/ gate-registry.yaml gates.manifest.json ; echo "exit=$?"
exit=1
```

So the five under `witness/fixtures/dual-review/` are LIVE: test/dual-review.test.ts:121
validates every one of them against the shipped verdict schema. The two under
`delivery/evidence/m3-exit-test/e1/e1-7/` are a FROZEN record of a run that
happened in August; no test, script, gate registry or manifest names that tree.

## 2. The decision the plan left open: `head` is REQUIRED

Stated first, with its cost, because the dispatch asked for the decision and
not for a survey.

**REQUIRED, `type: string`, `pattern: ^[0-9a-f]{40}$`.** The edit is at
schemas/verdict.schema.json:12 (the `required` list) and
schemas/verdict.schema.json:33 (the property).

The argument is one sentence and the rest is consequence: **an OPTIONAL head
leaves the old convention in force for every document that omits it, and every
document omits it.** Before this phase the only thing tying a pair of reviews to
one commit was the DIRECTORY the operator pointed the gate at, a reading
declared in delivery/work-history/m3-p9.md:1 and enforced by nothing. If `head`
is optional, the check must keep a fallback for head-less verdicts, that
fallback is the directory, and the directory is exactly what
delivery/decisions/DR-0012-delegated-merge-authority.md:22 was being satisfied
by assertion rather than by check. Optional would ship the field and keep the
hazard. It would also redden criterion 2, which asks for exit 1 naming `head`.

**Full length rather than abbreviated, for a reason that is about the check and
not about tidiness.** A short sha and a full sha are two spellings of ONE fact,
and nothing here can reconcile them. The check's whole input is an instance and
a context DIRECTORY, src/checks.ts:92, so resolving a prefix against a commit
graph is not something it has the arguments to attempt. Two spellings would
therefore form two groups of one, and a group of one is refused rather than
compared, at src/checks.ts:3645 and src/checks.ts:3859; the captured
criterion-3 run in section 5 shows both refusals firing on one directory.

So the abbreviation is refused at the document boundary
(schemas/verdict.schema.json:33) and refused again inside the check
(src/checks.ts:3252), and the two layers agree rather than one relying on the
other. Case is refused by the pattern and ALSO folded by `establishField`, so an
upper-case sha is one key with its lower-case spelling rather than a second
group; both directions are witnessed, the refusal at test/verdict-head.test.ts:628
and the folding at test/verdict-head.test.ts:652.

**What required cost, exactly.** Five files migrated and two left alone:

- the five live fixtures under `witness/fixtures/dual-review/` gained
  `head: dcbe6704813e861736c8d394dca35f7dc31b4f93`, which is the real head of
  `origin/claude/m3-p9-agents-policy`, the branch those fixtures model. A real
  sha rather than a row of zeroes, so nobody has to wonder whether the pattern
  would have taken a placeholder.
- the two frozen evidence documents under
  `delivery/evidence/m3-exit-test/e1/e1-7/` were NOT touched. They are a
  captured record of a run that happened before the field existed; nothing
  validates them (the grep above, exit 1); and editing historical evidence so it
  satisfies a later schema would make an altered record indistinguishable from
  an honest one, which is the property CLAUDE.md:153 protects when it requires a
  transliteration to be declared. They will not validate against the shipped
  schema and that is the correct state for them.

**The declared deviation this forces.** `witness/fixtures/dual-review/` is not
on the plan's files-to-touch list for this phase (delivery/plan/kernel-plan-m4.md:1687)
and the migration cannot be done without it. Measured rather than argued: the
un-migrated fixtures, taken from the pre-change tree, each produce exactly the
head diagnostic against the shipped schema, and test/dual-review.test.ts:121
asserts that no fixture produces an INVALID line at all.

```
$ for f in decorrelated-criteria decorrelated-hazard shared-contract-criteria \
           shared-family-hazard shared-framing-hazard; do
    printf '%s: ' "$f"
    node bin/tiphys.ts validate --type verdict \
      "$OLD/witness/fixtures/dual-review/$f.yaml" 2>&1 \
      | grep -c 'INVALID #/head required property head is missing'
  done
decorrelated-criteria: 1
decorrelated-hazard: 1
shared-contract-criteria: 1
shared-family-hazard: 1
shared-framing-hazard: 1
```

It is added to
delivery/plan/phase-declarations/m4-p10.json:1. No other wave-1 unit may touch
it: the pre-pass at delivery/plan/m4-conflict-pre-pass.md:15 lists M4-P1 at
`test/fixtures/harness-probe/` and M4-P20 at `test/fixtures/exclusion/`, and
`witness/` appears in no other row.

## 3. What changed, and the mechanism behind each change

### 3a. The schema: `head`, and the escalation widening

The escalation `contains` enum went from `["high", "critical"]` to
`["medium", "high", "critical"]` (schemas/verdict.schema.json:101). The
mechanism, stated as a mechanism rather than as the finding:
**a predicate written in two places drifted, and the narrower copy is the one
that decides.** delivery/decisions/DR-0012-delegated-merge-authority.md:23 bars
an unresolved finding at high or MEDIUM; the schema stopped at `high`. So a
verdict reading APPROVE beside a finding the review itself ranked medium
validated at exit 0, and the merge precondition the schema exists to carry was
one severity wider than the schema.

`low` is deliberately still mergeable, which is what that same line of DR-0012
says, and a test asserts the low arm as a control so a widening that overshot
would redden.

The two places are now held equal by a test rather than by care:
test/verdict-head.test.ts:513 compares the schema's enum against
src/checks.ts:3721 (`BLOCKING_SEVERITIES`) as sets. That is the hazard row "the
medium widening in the schema only" and its mirror, in one assertion.

### 3b. The check: the join key is `(phase, head)`

`dual-review-decorrelation` grouped by `phase` alone. It now groups by
`(phase, head)` through src/checks.ts:3288.

**A sibling that cannot be keyed is a VIOLATION, not a skip**, and that is the
part worth reading. A `filter` would have dropped a same-phase verdict whose
head is unreadable, which SHRINKS the group; a shrinking group is the fail-open
shape this file has already been bitten by twice, at `loadCommittedVerdicts`
and at the `phase` canonicalisation, both recorded in its own comments. With
three verdicts, two of them sharing a model family, giving the third an
unreadable head would otherwise leave a compared pair of two and a green run.
So `headGroupFor` returns the members AND the refusals, and the caller seeds its
violation list with the refusals. The refusal is captured in section 5 as
member two of the criterion-3 class, where a third verdict carrying an empty
head reddens a directory whose other two verdicts are a good pair.

### 3c. The new check `verdict-pair-approves`

src/checks.ts:3799. DR-0012 condition 2 made into a predicate. Before it, the
gate could not see a verdict's VALUE at all:

```
$ OLD=/home/user/node26-scratch/pre-head       # git archive of c236f41
$ grep -c 'APPROVE'  "$OLD/scripts/check-dual-review.mjs"
0
$ grep -c 'severity' "$OLD/scripts/check-dual-review.mjs"
0
$ grep -c 'findings' "$OLD/scripts/check-dual-review.mjs"
0
```

so two properly decorrelated reviews that both REFUSED the merge passed the
gate green (captured in section 5).

It evaluates the COMMITTED group rather than the instance's own fields
(src/checks.ts:3859 onward reads only `group`), and there is no membership
test, which is where its shape differs from its sibling's.
That is deliberate and it is not a hole: condition 2 is a property of the two
reviews written to `delivery/review/` and committed, a stray document is not
among them and cannot break a predicate about a set it is not in, and a
`(phase, head)` selecting fewer than two committed verdicts is refused by the
pair-size rule.

### 3d. The gate script prints the VALUES, not only the count

scripts/check-dual-review.mjs:85 adds the second check id, and the runner now
prints one line per verdict carrying its value, its head and its `produced-by`.
Real output, section 5.

**Why `produced-by` is printed although nothing refuses it.** The dispatch asked
me to address or explain the measured fact that the decorrelation check compares
canonicalised STRINGS and not model FAMILIES, so the two real `produced-by`
values in this repository's own reviews pass as decorrelated while both name one
vendor (delivery/verification/m4-prototype-probes.md:1, item 7, and item 11 for
the forgeability half).

**I did not change the comparison, and the reason is scope rather than
disagreement.** Closing it is M4-P11's declared work: delivery/plan/kernel-plan-m4.md:1803
puts the `produced-by` exception arm and its two falsifiers in that phase, and
delivery/plan/kernel-plan-m4.md:1964 records M4-P10 and M4-P11 as SERIALISE on
`src/checks.ts` and `scripts/check-dual-review.mjs`, which are the two files
both would edit. Doing it here would have taken M4-P11's subject and left it a
phase with nothing in it.

What I did instead is the half that is mine under step 6 and costs a reader
nothing to use: the gate now PRINTS both `produced-by` values, so a single-vendor
pair is visible in the gate's own output rather than requiring someone to open
two files. That is a mitigation, not a refusal, and this document says so.

## 4. The fix-round contract, applied to the one refactor in this change

**The MECHANISM, not the finding.** Adding a second check that applies exactly
where the delegated grant applies meant a second reader of the merge REGIME
(charter, delivery mode, `merge-authority`). Two readers of one fail-closed fact
is the shape this repository's contract calls fixing the instance rather than
the mechanism: a later correction to one leaves the other fail-open. That is not
speculative here. The regime block carries THREE such corrections in its own
comments, labelled "SITE TWO" and "SITE THREE" by the round that made them, one
of which had made a mode with no `merge-authority` green and authorising.

So the block was LIFTED into `establishDelegatedRegime` (src/checks.ts:3352) and
both checks call it. Every message is the one the shipped check printed, byte
for byte, except that the two REPORT lines now name the CALLING check, because
a reader seeing `REPORT verdict-pair-approves ... declares no delivery mode`
must be able to tell which guard declined to run.

**The derivation, with its full output.** Every reader of the regime, by the
constant only a regime reader uses:

```
$ grep -rn 'DELEGATED_MERGE_AUTHORITY' src/ scripts/ test/
src/checks.ts:2903:export const DELEGATED_MERGE_AUTHORITY = "delegated-under-conditions";
src/checks.ts:3443:  if (authority !== DELEGATED_MERGE_AUTHORITY) {
test/dual-review.test.ts:48:  DELEGATED_MERGE_AUTHORITY: string;
test/dual-review.test.ts:267:    checksModule.DELEGATED_MERGE_AUTHORITY,
```

and every reader of the charter, which is the other half of the same fact:

```
$ grep -rn '"charter.yaml"' src/ scripts/
src/checks.ts:3358:    classifyEntry(join(contextDirectory, "charter.yaml")).kind !== "absent";
src/checks.ts:3369:  const charter = readContextDocument(contextDirectory, "charter.yaml");
scripts/check-dual-review.mjs:212:const REGIME_DOCUMENTS = ["charter.yaml", "assurance-modes.yaml"];
```

One site in the derived checks after the lift, plus the script's own separate
fail-closed refusal, which is a DIFFERENT rule at a different layer (it refuses
a directory outright, with gate status `error`) and is deliberately left where
it is: M3-P9 recorded why, at the `REGIME_DOCUMENTS` comment, and this phase
gives no reason to move it.

**What the derivation did NOT cover, and the exclusions are real.**

- It did not search `dist/`, which is generated by the build and is not
  committed (CLAUDE.md:281).
- It did not search `delivery/`, which is prose about the code rather than
  code. A sentence in a work history that describes the regime reading is not a
  second implementation of it.
- It searched for the CONSTANT and for the FILENAME, which finds a reader that
  compares against either. It would NOT find a reader that hard-codes the string
  `delegated-under-conditions` inline. That second search was run and is empty
  outside the constant's own definition:

  ```
  $ grep -rn "delegated-under-conditions" src/ scripts/ bin/
  src/checks.ts:871: * `merge-authority: delegated-under-conditions` requires `conditions[]` and a
  src/checks.ts:2903:export const DELEGATED_MERGE_AUTHORITY = "delegated-under-conditions";
  ```

  Two hits and neither is a second reader: src/checks.ts:871 is a comment on a
  DIFFERENT check (`mode-conditions-quote-granted-by`), and src/checks.ts:2903
  is the constant's own definition. The root `assurance-modes.yaml` also carries
  the value at its line 167, which is the DECLARATION being read rather than a
  reader of it.
- It did not cover a reader that reaches the regime through a different concept
  entirely, for example by asking a mode for a gate set rather than for its
  merge authority. Nothing in this phase touches that and I have not audited it.

## 5. Red witnesses

Every witness below is run against the REAL pre-change tree rather than against
a mutant written in a test file. The tree is obtained with
`git archive <sha> | tar -x`, where `<sha>` is derived as the newest ancestor of
HEAD whose `schemas/verdict.schema.json` does not require `head`. The staging
asserts that the tree it produced really lacks this phase's code, because a
staging step that silently produced the CURRENT tree would be a witness that
cannot go red.

```
$ node .../derive-base.mjs
pre-head commit: c236f41a50260997b6b7ccad219fee5266330647 script present: true
c236f41 M4-P10 beacon: the tree-wide migration derivation, which is seven documents and not zero
$ grep -c 'headGroupFor\|verdictPairApproves' $OLD/src/checks.ts
0
```

**A pinned `HEAD~1` was rejected deliberately.** After the first fix round the
parent of the tip is this phase's own earlier commit, which already carries the
field, and the witness would quietly become a comparison of the new rule against
itself. The derivation is right whatever the branch's shape becomes.

### Criterion 2, `head` required

```
===== CRITERION 2: validate --type verdict, no head =====
--- SHIPPED schema ---
INVALID #/head required property head is missing
shipped exit=1
--- PRE-CHANGE tree (dangerous state) ---
SKIPPED dual-review-decorrelation no context
SKIPPED verdict-criteria-complete no context
SKIPPED verdict-deviations-judged no context
SKIPPED verdict-hazard-classes-addressed no context
pre-change exit=1

===== CRITERION 2 control: the same document WITH a head =====
SKIPPED dual-review-decorrelation no context
SKIPPED verdict-criteria-complete no context
SKIPPED verdict-deviations-judged no context
SKIPPED verdict-hazard-classes-addressed no context
SKIPPED verdict-pair-approves no context
shipped exit=1
```

**READ THE EXIT CODES CAREFULLY, BECAUSE THEY CONTRADICT THE PLAN AND THE
CONTRADICTION IS MINE TO DECLARE.** delivery/plan/kernel-plan-m4.md:1694 asks for
"exits 0 for the same document with one" and for a pre-change arm that
"validates at exit 0". Neither is reachable: `tiphys validate` with no
`--context` exits 1 WHATEVER the document says, because four context-requiring
derived checks report `SKIPPED ... no context` and a command that passed by not
running would be the vacuous pass M2-C-3 exists against. test/dual-review.test.ts:121
records the same thing for its own fixtures.

So the measurement that carries the criterion is **the presence or absence of an
INVALID line**, which is what the shipped tests assert, plus a genuine exit-0
arm through the schema itself (`validateToLines` returning the empty list),
which is what "the document is valid" actually means. The pre-change arm above
prints ZERO INVALID lines on a head-less document; the shipped arm prints
exactly one and it names `head`.

### Criterion 4, APPROVE beside a medium finding

```
===== CRITERION 4: APPROVE beside a medium finding =====
--- SHIPPED schema ---
INVALID # value does not satisfy the requirements its own shape triggers here
INVALID #/verdict value "APPROVE" is not one of the permitted values "FIX-ROUND-NEEDED"
shipped exit=1
--- PRE-CHANGE tree (dangerous state), same document minus the head the old schema forbids ---
SKIPPED dual-review-decorrelation no context
SKIPPED verdict-criteria-complete no context
SKIPPED verdict-deviations-judged no context
SKIPPED verdict-hazard-classes-addressed no context
pre-change exit=1
```

`head` is removed from the document on the pre-change arm and that is NOT a
weakening. The pre-change schema is `additionalProperties: false` and has no
`head` property, so it refuses the field outright; validating a head-carrying
document there would measure the absence of the property rather than the
escalation rule. Everything else is held fixed, and the head-less-ness of the
old corpus is exactly what the criterion-2 witness measures.

The class, not the instance: test/verdict-head.test.ts:491 asserts `medium`,
`high` and `critical` all force FIX-ROUND-NEEDED and that `low` does not, so a
widening written as a REPLACEMENT rather than an extension reddens.

### Criterion 3, two verdicts for two different heads

```
===== CRITERION 3, two verdicts for DIFFERENT heads =====
--- PRE-CHANGE gate (dangerous state) ---
check-dual-review: 1 registered check(s) named dual-review-decorrelation ran over 2 verdict(s)
REPORT dual-review-decorrelation 2 verdict(s) for phase M3-P9 are distinct on produced-by, framing, review-contract
check-dual-review: green (2 review verdicts examined for decorrelation)
2 verdict(s) examined by 1 registered check(s); no decorrelation violation
pre-change exit=0
--- SHIPPED gate ---
check-dual-review: 1 registered check(s) named dual-review-decorrelation ran over 2 verdict(s)
check-dual-review: 1 registered check(s) named verdict-pair-approves ran over 2 verdict(s)
check-dual-review: verdict APPROVE at head dcbe6704813e861736c8d394dca35f7dc31b4f93 produced-by family-a [.../decorrelated-criteria.yaml]
check-dual-review: verdict APPROVE at head 1945d69a1a1a1a1a1a1a1a1a1a1a1a1a1a1a1a1a produced-by family-b [.../decorrelated-hazard.yaml]
INVALID #/phase only 1 verdict document(s) exist under delivery/review for phase M3-P9 at head 1945d69a1a1a1a1a1a1a1a1a1a1a1a1a1a1a1a1a, and a delegated grant requires two independent clean-room reviews of the exact head (check: dual-review-decorrelation) [.../decorrelated-hazard.yaml]
INVALID #/phase only 1 verdict document(s) exist under delivery/review for phase M3-P9 at head dcbe6704813e861736c8d394dca35f7dc31b4f93, and a delegated grant requires two independent clean-room reviews of the exact head (check: dual-review-decorrelation) [.../decorrelated-criteria.yaml]
INVALID #/verdict only 1 verdict document(s) exist under delivery/review for phase M3-P9 at head 1945d69... and DR-0012 condition 2 is a property of the PAIR ... (check: verdict-pair-approves) [.../decorrelated-hazard.yaml]
INVALID #/verdict only 1 verdict document(s) exist under delivery/review for phase M3-P9 at head dcbe670... and DR-0012 condition 2 is a property of the PAIR ... (check: verdict-pair-approves) [.../decorrelated-criteria.yaml]
check-dual-review: red (2 review verdicts examined for decorrelation)
shipped exit=1
```

**The captured pre-change green is the whole point of this witness.** Two
reviews of two DIFFERENT commits, sitting in one directory, exited 0 and printed
that they are decorrelated. That sentence was true about the three dimensions
and silent about the thing DR-0012 condition 1 actually requires.

Two long absolute temporary paths were shortened to `...` in the block above and
four INVALID lines were truncated at `...`; nothing else in any captured output
in this document was changed, no codepoint was substituted anywhere, and the
untruncated run is reproducible with the staging described at the head of this
section.

**The second structurally different member of the class**, because one witness
is not a class. Member one is "the key ignores the head". Member two is "the key
uses the head and a document with no usable one falls out of the group", which
reaches the same dangerous place by a different route. Both are asserted in
test/verdict-head.test.ts:580 and test/verdict-head.test.ts:601, and the second
one reddens on the message naming the unreadable sibling.

### Criterion 5, a pair that refuses the merge

```
===== CRITERION 5, a pair in which BOTH verdicts refuse the merge =====
--- PRE-CHANGE gate (dangerous state) ---
check-dual-review: 1 registered check(s) named dual-review-decorrelation ran over 2 verdict(s)
REPORT dual-review-decorrelation 2 verdict(s) for phase M3-P9 are distinct on produced-by, framing, review-contract
check-dual-review: green (2 review verdicts examined for decorrelation)
2 verdict(s) examined by 1 registered check(s); no decorrelation violation
pre-change exit=0
--- SHIPPED gate ---
check-dual-review: verdict FIX-ROUND-NEEDED at head dcbe6704813e861736c8d394dca35f7dc31b4f93 produced-by family-a [.../decorrelated-criteria.yaml]
check-dual-review: verdict FIX-ROUND-NEEDED at head dcbe6704813e861736c8d394dca35f7dc31b4f93 produced-by family-b [.../decorrelated-hazard.yaml]
INVALID #/verdict .../decorrelated-criteria.yaml reads FIX-ROUND-NEEDED for phase M3-P9 at head dcbe670..., so the pair does not approve this head and the delegated grant's condition 2 is not met (check: verdict-pair-approves)
INVALID #/verdict .../decorrelated-hazard.yaml reads FIX-ROUND-NEEDED for phase M3-P9 at head dcbe670..., so the pair does not approve this head and the delegated grant's condition 2 is not met (check: verdict-pair-approves)
check-dual-review: red (2 review verdicts examined for decorrelation)
shipped exit=1
```

Two reviews that both said the work needs another round were a green merge
gate. That is the captured green the criterion asks for.

The second structurally different member of "the pair is not clean" is a
BLOCKING FINDING rather than a refusing verdict, and it is asserted separately,
because the two reach the predicate through different fields. It matters that
the check establishes the severity itself rather than borrowing the schema's
guarantee: nothing on the gate's path validates the committed siblings, which
the shipped check records in its own comment, so a document that has not been
through `tiphys validate` reaches the gate unrefused. That is exercised rather
than asserted: test/verdict-head.test.ts:756 writes exactly such a document into a staged
`delivery/review/` and the gate reddens on it.

### Two fail-opens I found in my OWN check, and the mechanism behind both

Found while re-reading the finished check rather than by a reviewer, and
recorded here because the mechanism is one this file had already reasoned about
in the opposite direction.

**THE MECHANISM: the SAFE direction of a canonical collapse depends on the
DIRECTION OF THE COMPARISON, and `src/checks.ts` had settled that question once
for a check that compares the other way round.** `dual-review-decorrelation`
REFUSES when two values are the SAME, so folding case and collapsing lookalikes
makes more values compare equal and produces MORE refusals; its own comment at
src/checks.ts:3030 works that through and concludes that aggressive
canonicalisation is fail-closed. `verdict-pair-approves` APPROVES when a value
EQUALS one particular word. The same collapse therefore produces more
APPROVALS. Inheriting the sibling's reasoning without re-deriving it for the new
direction is what opened both holes.

Two members, both measured against the code one commit earlier (`e6db0dc`),
staged the same way as every other witness here:

```
== staged e6db0dc ==
VERDICT_VOCABULARY present in the staged tree: 0
SEVERITY_VOCABULARY present in the staged tree: 0

===== FAIL-OPEN A: a sibling spelled 'Approve' =====
--- PRE-FIX gate (dangerous state) ---
REPORT verdict-pair-approves 2 verdict(s) for phase M3-P9 at head dcbe670... read APPROVE and carry no finding at medium, high, critical
check-dual-review: green (2 review verdicts examined for decorrelation)
2 verdict(s) examined by 1 registered check(s) named dual-review-decorrelation and 1 named verdict-pair-approves; no decorrelation violation and the pair approves
pre-fix exit=0
--- SHIPPED gate ---
INVALID #/verdict .../decorrelated-criteria.yaml declares verdict Approve, which is not one of the two words the closed vocabulary admits (APPROVE, FIX-ROUND-NEEDED), so it cannot be read as an authorisation however it is spelled (check: verdict-pair-approves)
check-dual-review: red (2 review verdicts examined for decorrelation)
shipped exit=1

===== FAIL-OPEN B: a finding ranked 'blocker' =====
--- PRE-FIX gate (dangerous state) ---
REPORT verdict-pair-approves 2 verdict(s) for phase M3-P9 at head dcbe670... read APPROVE and carry no finding at medium, high, critical
check-dual-review: green (2 review verdicts examined for decorrelation)
pre-fix exit=0
--- SHIPPED gate ---
INVALID #/findings/0/severity .../decorrelated-criteria.yaml ranks finding CR-001 blocker, which is not one of the four severities the kernel's vocabulary admits (low, medium, high, critical), so whether it blocks the merge could not be established (check: verdict-pair-approves)
check-dual-review: red (2 review verdicts examined for decorrelation)
shipped exit=1
```

**The pre-fix REPORT line is the part to look at.** It says the two verdicts
"read APPROVE and carry no finding at medium, high, critical" beside an exit 0,
and in fail-open B that sentence is FALSE: there is a finding, and the check
could not tell whether it blocked. A guard printing a false sentence beside a
green is the shape CLAUDE.md:477 names as green and worthless, and worse than no
guard because it is trusted.

**Why the schema is not the guard for either.** Both `Approve` and `blocker` are
refused by `schemas/verdict.schema.json`, and nothing on this gate's path
validates the committed siblings; that is recorded in the shipped check's own
comment and is exercised by test/verdict-head.test.ts:756. A document that has
not been through `tiphys validate` reaches the gate unrefused.

**The control, so the two refusals are not a check that reddens on anything.** A
committed pair carrying only a `low` finding stays GREEN, which is what
delivery/decisions/DR-0012-delegated-merge-authority.md:23 permits. Asserted at
test/verdict-head.test.ts:827.

**What this derivation did NOT cover.** I re-read the two comparisons this check
makes against a fixed word and fixed both. I did NOT re-audit every `===` and
`includes` in `src/checks.ts` for the same inversion; the other checks in that
file compare for DISTINCTNESS, where the collapse is fail-closed and the
existing reasoning holds, but I have not walked them one by one and this
sentence is the honest scope of the fix.

### Criterion 6, the Kind B falsification

`deregisterCheck("verdict-pair-approves")` makes the one-refusing pair pass, and
re-registering makes it fail again, asserted through the script's own `evaluate`
rather than a copy of its loop, in test/verdict-head.test.ts:842. The gate prints
the two registered-check counts SEPARATELY, so `0 registered check(s) named
verdict-pair-approves` beside a green tells a reader that the deregistration
witness is running rather than that the pair was examined and found clean.

## 6. Criterion 7 is NOT dischargeable by this phase, and there are TWO reasons

Stated plainly rather than reported as done, because a criterion reported met
that is not is the exact hazard this phase exists against.

**Reason one: I must not write my own clean-room reviews.** Step 7 of the plan
(delivery/plan/kernel-plan-m4.md:1683) says the two verdict documents come from
the two clean-room reviews OF THIS PHASE. An implementer writing an APPROVE
verdict about its own work is not a clean-room review and would be fabricated
review evidence. This repository has already paid for one fabricated sign-off:
CLAUDE.md:640 records a decision record created and then deleted as a fabricated
owner decision, and the id it burned is still cited by the documents about the
retirement. CLAUDE.md:1213 forbids softening a work history for the same reason
one level down. So `delivery/review/` carries no verdict from me.

**Reason two, and it is measured rather than argued: this repository has no
`charter.yaml`, so the gate ERRORS even with two good verdicts present.**

```
$ node scripts/check-dual-review.mjs .
check-dual-review: error (0 review verdicts examined for decorrelation)
charter.yaml does not exist, so the declared mode's merge-authority is unknown and no decorrelation verdict can be reached; a merge check that cannot determine the regime reports error, never green
exit=21
```

and with two real verdicts staged beside the repository's own
`assurance-modes.yaml` and no charter:

```
$ node scripts/check-dual-review.mjs --precondition <dir>
check-dual-review: 2 verdict document(s) under <dir>/delivery/review
precondition exit=0
$ node scripts/check-dual-review.mjs <dir>
check-dual-review: error (0 review verdicts examined for decorrelation)
<dir>/charter.yaml does not exist, ...
gate exit=21
```

**This behaviour is NOT introduced by this phase.** The same staged directory
against the pre-change tree gives the identical error and `exit=21`, so the
refusal is M3-P9's fail-closed rule working exactly as written.

**The consequence the orchestrator needs before merging anything.** The gate is
conditional and its precondition today exits 1 (`0 verdict document(s) under
delivery/review`), so it is NOT APPLICABLE in this repository's CI. The moment
two verdict documents land in `delivery/review/`, the precondition passes, the
gate runs, and it goes to `error` on the missing charter. **Committing the two
verdicts without also adding a root `charter.yaml` turns a not-applicable gate
into a hard CI error.**

A root `charter.yaml` declaring this repository's own delivery mode is a
governance artifact, not an implementer's edit, it is on no phase's
files-to-touch list, and it would change what regime every future run of this
gate reads. I have not created one. This is recorded for the orchestrator to
decide, with the recommendation that the charter and the two verdicts land in
the same pull request as this phase, per DR-0031, so the gate's first
non-vacuous run happens on the head it is about.

What each reviewer needs to write, so the criterion can be discharged at review
time rather than rediscovered: a `kind: verdict` YAML under `delivery/review/`,
`phase: M4-P10`, `head` set to the forty-character sha of the commit they
REVIEWED (which is an ancestor of the commit that carries their verdict, because
adding the file changes the tree and therefore the sha), distinct `produced-by`,
distinct `framing`, and one each of `review-contract: criteria` and
`review-contract: hazard`.

## 6b. The criterion walk

Every acceptance criterion at delivery/plan/kernel-plan-m4.md:1691 onward, with
where its evidence is. "MET" means walked with a capture in this document or an
assertion in the shipped suite, not that it looked right.

| # | criterion | verdict | evidence |
|---|---|---|---|
| 1 | `node --test test/verdict-head.test.ts` exits 0 and reports N tests, N > 0 | MET | section 9: 29 tests, 29 pass, 0 fail, 0 skipped, exit 0, node v26.6.0, `dist/` built |
| 2 | validate exits 1 naming `head`; exits 0 with one; head-less validates at exit 0 against the pre-change state | MET WITH A DECLARED CORRECTION | section 5. The exit codes in the criterion are not reachable through the CLI for any verdict; the INVALID line is the falsifiable form, plus a real empty diagnostic list through the schema. test/verdict-head.test.ts:342, :359, :383 |
| 3 | two different heads are two groups of one; the same head is one group of two; the pre-change gate compares the first pair green | MET | section 5, both runs captured with exit codes. test/verdict-head.test.ts:549, :562, :580, :601 |
| 4 | APPROVE beside `severity: medium` fails naming `verdict`; the same document validates at exit 0 against the pre-change schema | MET | section 5, and the pre-change arm has `head` removed with the reason stated. test/verdict-head.test.ts:456, :465, :491 |
| 5 | a pair with ONE FIX-ROUND-NEEDED reddens `verdict-pair-approves`; a pair with BOTH passes the pre-change gate green, and the test asserts that captured green | MET | section 5, pre-change `exit=0` captured. test/verdict-head.test.ts:692, :709, :735, and the two fail-open arms at :780 and :806 with their own captured pre-fix greens |
| 6 | deregistering `verdict-pair-approves` makes the criterion-5 fixture pass | MET | test/verdict-head.test.ts:842, through the script's own `evaluate` |
| 7 | `check-dual-review` against this phase's own `delivery/review/` reports GREEN with `units` 2 | **NOT MET, AND NOT DISCHARGEABLE BY THIS PHASE** | section 6, two measured reasons, both with captures |

## 7. Where I contradicted the plan

Four places, each already argued above and listed here so a reviewer does not
have to find them.

1. **"The migration cost is measurably ZERO"** (delivery/plan/kernel-plan-m4.md:1628)
   is false. It is seven documents, five of them live. Section 1.
2. **Criterion 2's "exits 0"** (delivery/plan/kernel-plan-m4.md:1694) is not
   reachable through the CLI for any verdict document at all, because
   context-requiring checks skip and the command exits 1. The falsifiable form
   is the INVALID line. Section 5.
3. **Criterion 7** (delivery/plan/kernel-plan-m4.md:1713) cannot be discharged
   by the implementer, and cannot be discharged by anyone in this repository
   without a root `charter.yaml` that does not exist. Section 6.
4. **The files-to-touch list** (delivery/plan/kernel-plan-m4.md:1687) omits
   `witness/fixtures/dual-review/`, which a required `head` forces, and omits
   `test/verdict-schema.test.ts` and `test/dual-review.test.ts`, which the two
   schema edits force. All three are on the phase declaration. Section 2.

## 8. What this phase did NOT cover

- **`produced-by` is still a STRING comparison, not a family one**, and a
  `produced-by` line is still written by the reviewing agent and therefore
  forgeable. Both are measured at delivery/verification/m4-prototype-probes.md:1.
  Closing the first is M4-P11's declared scope; the second belongs to M4-D-06.
  This phase prints both values and refuses neither.
- **"Unresolved" is read as "present in the file".** A finding raised, fixed in
  a later round and left in the document still reddens `verdict-pair-approves`.
  That is the fail-closed direction and it is a real cost, taken deliberately,
  because the alternative is a resolution field an author sets on their own
  finding.
- **Nothing checks that a `head` names a commit that exists**, or that it is the
  commit the reviewer read. The field makes the claim explicit and comparable
  between two documents; it does not make it true.
- **Nothing checks that a `severity` was ranked honestly.** A review that calls a
  critical defect `low` passes both checks.
- **The two frozen evidence verdicts are now invalid against the shipped
  schema** and were left that way on purpose (section 2). No gate reads them
  today; if one ever does, it will redden and that is the right signal.
- **`verdict-pair-approves` is NOT in the Kind B table at
  delivery/plan/kernel-plan-m3.md:1044**, and I did not add it. That table is
  M3's, D-M3-22 says a check belonging in it and missing from it is a PLAN
  DEFECT to escalate rather than a script to add quietly, and an implementer
  editing the plan is not a thing this process allows. The check itself IS
  planned, at delivery/plan/kernel-plan-m4.md:1677. Nothing mechanical enforces
  the table: the only test reading that file parses Appendix A's clause
  inventory (test/checks.test.ts:615), not the check list, so this will not
  redden anything and will simply stay wrong until someone adds the row. Raised
  for the orchestrator.
- **The gate's UNIT LABEL is now narrower than what it examines**, and I left it
  alone on purpose. The root `gate-registry.yaml` declares the unit as "review
  verdicts examined for decorrelation" and the gate now also examines them for
  DR-0012 condition 2. Changing that string means editing the registry, which
  means re-rendering the generated gate block in `CLAUDE.md` with
  `scripts/render-agent-rules-gates.mjs`, or the `agent-rules-drift` gate
  reddens. Both files are outside this phase's declaration and one of them is
  the agent-rules single source, so this is left for the orchestrator rather
  than taken. The label is imprecise; nothing it feeds is wrong.
- **I did not run the full gate bundle.** The `citations` gate was run and is
  reported in section 9; the rest were not, and the orchestrator should not read
  this document as a bundle result.

## 9. Gates and the suite

Toolchain: node v26.6.0 fetched to a scratch prefix under `/home/user`
(NOT under `/tmp/claude-0`, which is `drwx------` and breaks the unprivileged
spawn at test/gates.test.ts:3530 when the interpreter lives there. The trap is
recorded at CLAUDE.md:803 and it bites a clone that is NOT under `/tmp`, which
this worktree is not. `namei -m` on the interpreter shows every component
world-traversable, so the arm runs rather than skipping.

```
$ node --version
v26.6.0
$ npm ci          # exit 0, no EBADENGINE
$ npm run build   # exit 0
```

SUITE RESULT, complete sentence: see the block below for the interpreter, the
build state, the invocation, the pass count AND the skipped count, and the exit
code.

**THE COMPLETE SENTENCE.** Interpreter node v26.6.0 from a scratch prefix under
`/home/user`; `dist/` BUILT immediately before the run, with `git status` clean
afterwards; invocation `npm test`, which is `node --test "test/**/*.test.ts"`
and is the command the `suite` gate runs; at commit `2010de1`.

```
$ npm test
tests 878
suites 0
pass 877
fail 1
cancelled 0
skipped 0
todo 0
duration_ms 1031922.755481
npm test exit=1
```

**The SKIPPED count is 0 and it is quoted because a skipped test is not a
passing test** (CLAUDE.md:958). The bare `node --test` invocation from the
repository root reports two MORE than this, from the tracked
`sandbox/test/greet.test.js` fixture that `package.json`'s pattern excludes; that is
CLAUDE.md's third axis and it is why the invocation is named above rather than
left implied.


### The one failing test, and why it is not this change

**THE FULL RUN REPORTS `fail 1`, AND SAYING "it is not mine" NEEDS A CAUSE, NOT A
PLEA.** The cause is established below; it is not left at "it passes alone".

The failing test is test/coverage-gate.test.ts:233, `deleting an appendix row is
red naming the orphan id, and restoring it is green`. The captured error:

```
test at test/coverage-gate.test.ts:233:1
x deleting an appendix row is red naming the orphan id, and restoring it is green (1469.852467ms)
  Error: pattern ^(?:R-[0-9]+[a-z]?)$ did not complete within 250ms against a value of length 5 (possible catastrophic backtracking)
      at boundedExec (.../src/gates/coverage.ts:260:11)
      at extractIdRows (.../src/gates/coverage.ts:376:9)
      at Module.checkCoverage (.../src/gates/coverage.ts:417:24)
      at TestContext.<anonymous> (.../test/coverage-gate.test.ts:250:40)
```

**THE MECHANISM: a WALL-CLOCK budget is being used as a BACKTRACKING guard, and
wall clock is a property of the machine's load rather than of the pattern.**
src/gates/coverage.ts:260 gives `^(?:R-[0-9]+[a-z]?)$` 250ms against a
five-character value. That pattern cannot backtrack catastrophically; it is
linear. What exceeded 250ms is the process not being scheduled, because
`npm test` had 40 test-file workers on a 4-CPU container. Measured during the
run: `nproc` is 4 and `ps -eo cmd | grep -c 'node --test'` returned between 26
and 45 throughout.

**THREE FACTS SEPARATE THIS FROM MY CHANGE, and they are commands rather than
assertions.** First, this branch touches none of the three files involved:

```
$ git diff --stat plan/pstack-borrow-review...HEAD -- test/coverage-gate.test.ts \
    src/gates/coverage.ts delivery/requirements/ delivery/plan/kernel-plan-v1.md
(no output: none of them is touched)
```

Second, run alone it passes on the PRE-CHANGE tree:

```
$ node --test --test-name-pattern 'deleting an appendix row is red naming the orphan id' \
    test/coverage-gate.test.ts        # in the git-archived pre-change tree
tests 1
pass 1
fail 0
exit=0
```

Third, run alone it passes on THIS branch, same command, same interpreter:

```
tests 1
pass 1
fail 0
exit=0
```

**What I am NOT claiming.** I have not reproduced the failure on demand by
loading the machine, so "contention is the cause" is a reading of the error
message and the process count rather than a controlled experiment. What IS
controlled is the single-variable pair above: same test, same interpreter, two
trees, both green. A reviewer should re-run the full suite rather than take this
section as a clearance, and the orchestrator should treat the 250ms wall-clock
budget in `src/gates/coverage.ts` as a guard that can fire for a reason that has
nothing to do with the property it guards, which is this repository's own
recurring shape one direction along: not a guard that cannot go red, a guard
that goes red without cause.

The new file alone:

```
$ node --test test/verdict-head.test.ts     # node v26.6.0, dist/ built
tests 29
suites 0
pass 29
fail 0
cancelled 0
skipped 0
todo 0
exit=0
```

The ASCII and control-character check, and the citations gate, both at the
committed head rather than the working tree (CLAUDE.md:210 records that the
citations gate lints HEAD, so staging a fix and re-running gives the old
verdict):

```
$ node scripts/check-authored-bytes.mjs
exit=0

$ node bin/tiphys.ts gates run --registry gate-registry.yaml --mode full \
    --only citations --evidence <scratch> --base origin/main --head HEAD
gates: declared 1 applicable 1 verdict 1 green 1 red 0 not-applicable 0 error 0 vacuous 0
gates: citations: green: linted 16 changed document(s) at 29ba17087cf486a9dd85dcc58ec197b83240aba6: 504 citation(s) resolved, 0 self-citation(s), 0 unverifiable-external
gates: every applicable gate is green
citations exit=0
```

**The head named there is `29ba170`, and this paragraph is the ONE thing that
changed after it**, because a document cannot quote a gate run over itself
without becoming a later commit than the one the gate read. The commit carrying
this sentence differs from `29ba170` in this file and in nothing else; the code,
the schema, the tests and the declaration are byte-identical, which
`git diff 29ba170 HEAD --stat` shows. The same shape is why a verdict's `head`
names the commit that was REVIEWED rather than the one carrying the verdict
(section 2).

No codepoint was substituted in any captured output in this document, so no
transliteration note is owed. Two long absolute temporary paths were shortened
to `...` in section 5 and four INVALID lines were truncated at `...`; that is
the only alteration and it is declared here as well as at the block.

## 10. The claim grep

**THIS SECTION QUOTES THE GREP, SO RUNNING THE GREP OVER THE FINISHED DOCUMENT
MATCHES THIS SECTION'S OWN QUOTATION.** That is not a trick to get a lower
number: the binding command in CLAUDE.md:374 is a grep over the work history,
and a work history that prints the command and its output necessarily contains
every alternative of the pattern twice. So the measurement below is taken over
the document with SECTION 10 REMOVED, the removal is one contiguous slice from
the `## 10` heading to the end, and the raw whole-document number is given
underneath so nothing is hidden by the choice.

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' <this document, sections 1 to 9>
25:repository is YAML. So between them the two greps never look for the YAML
119:and the migration cannot be done without it. Measured rather than argued: the
173:**A sibling that cannot be keyed is a VIOLATION, not a skip**, and that is the
457:Two reviews that both said the work needs another round were a green merge
502:INVALID #/verdict .../decorrelated-criteria.yaml declares verdict Approve, which is not one of the two words the closed vocabulary admits (APPROVE, FIX-ROUND-NEEDED), so it cannot be read as an authorisation however it is spelled (check: verdict-pair-approves)
572:charter.yaml does not exist, so the declared mode's merge-authority is unknown and no decorrelation verdict can be reached; a merge check that cannot determine the regime reports error, never green
644:3. **Criterion 7** (delivery/plan/kernel-plan-m4.md:1713) cannot be discharged
645:   by the implementer, and cannot be discharged by anyone in this repository
743:**THE FULL RUN REPORTS `fail 1`, AND SAYING "it is not mine" NEEDS A CAUSE, NOT A

$ tr '\n' ' ' < <the same> | grep -oEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' | sort | uniq -c
      1 NEEDS A
      5 cannot be
      1 needs a
      2 never
```

**9 line-visible hits and 9 total occurrences**, so nothing straddled a
wrap. The line numbers are the ones in the file with section 10 removed, which
for sections 1 to 9 are the same as in the finished document, because section 10
is the last thing in it.

Each hit, settled:

| what it says | what settles it |
|---|---|
| "the two greps never look for the YAML spelling outside one directory" | the two captured greps immediately above it and the tree-wide one immediately below |
| "the migration cannot be done without it" | the captured five-fixture run in the very next block |
| "a sibling that cannot be keyed is a VIOLATION" | a description of the shipped code, and the sentence after it points at the captured member-two run in section 5 |
| "two reviews that both said the work NEEDS Another round" | FALSE POSITIVE. `needs a` matches the prefix of `needs another`. Recorded rather than silently dismissed |
| "SAYING it is not mine NEEDS A CAUSE, NOT A PLEA" | FALSE POSITIVE of the same kind, and the sentence is the opposite of an over-claim: it is the heading of the section that then gives the cause |
| the `Approve` INVALID line, and the `charter.yaml does not exist ... never green` line | both inside captured program output, quoted verbatim |
| "criterion 7 cannot be discharged", twice | the two captured runs in section 6, one against this repository and one against a staged directory, both `exit=21` |

**The raw whole-document numbers, for completeness**, which include this
section's quotation of the command and of its own output:

```
$ grep -cEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m4-p10.md
32     # matching LINES over the whole document

$ tr '\n' ' ' < delivery/work-history/m4-p10.md \
    | grep -oEi '<the same pattern>' | wc -l
64     # total OCCURRENCES over the whole document
```

An earlier draft had thirteen hits across sections 1 to 9. Six were rewritten
rather than justified: each had been a statement about the code that a reader
could only take on trust, and each now names a line or points at a capture.
