# M3-P6 work history: delivery-role briefs (implementer and clean-room reviewer)

- phase: M3-P6
- branch: `claude/m3-p6-delivery-role-briefs`
- base: `main` at 086b8df (M3-P5 merged)
- plan section: delivery/plan/kernel-plan-m3.md:3210, criteria at
  delivery/plan/kernel-plan-m3.md:3420 through delivery/plan/kernel-plan-m3.md:3533
- declaration: delivery/plan/phase-declarations/m3-p6.json:1

This file is written incrementally from the first minutes, per the
`incremental-output` clause at roles/_shared-dispatch-contract.md:17. Its mtime
is the beacon.

## Log

### Reading (start of work)

Read in the dispatch's stated order: the dispatch brief, `CLAUDE.md`, the plan's
M3-P6 section, the declaration, delivery/plan/m3-p6-dispatch-read.md:1, then
M3-P5's shipped artifacts: roles/_shared-dispatch-contract.md:1,
roles/investigator.md:1, roles/plan-writer.md:1,
roles/adversarial-plan-reviewer.md:1.

Environment, measured in the shell that will run the commands:

```
$ bash -lc 'node --version'
v22.22.2
```

### ESCALATION 1, found in the first half hour: `CLAUDE.md` is the sixth site

The dispatch's addendum measured five probes and found no declaration amendment
needed (delivery/plan/m3-p6-dispatch-read.md:162), and said in its own limits
section that a sixth site of the same shape would not appear there
(delivery/plan/m3-p6-dispatch-read.md:176). There is one, and it is forced
rather than discretionary.

THE MECHANISM, not the instance: **any edit to `gate-registry.yaml` obliges an
edit to `CLAUDE.md`**, because M3-P2 made the gate block in `CLAUDE.md` a
GENERATED projection of the registry and wired `--check` as a workflow step on
both CI events (.github/workflows/gates.yml:111). This phase's criterion 3 and
step 3 require a new registry gate. `gate-registry.yaml` is on the declaration
(delivery/plan/phase-declarations/m3-p6.json:17); `CLAUDE.md` is not, and it is
not one of the two standing extras, which are `test/behaviors.json` and the
phase work history (src/gates/scope.ts:740).

Measured, against an UNTOUCHED working tree by rendering from a scratch copy of
the registry carrying one added gate:

```
$ cp gate-registry.yaml <scratch>/reg.yaml   # then append one brief-drift entry
$ node scripts/render-agent-rules-gates.mjs --check --registry <scratch>/reg.yaml \
    --agent-rules CLAUDE.md
agent-rules-drift: red (18 rendered gate rows compared)
CLAUDE.md's gate block has drifted from <scratch>/reg.yaml: the registry has a
row the file does not: | `brief-drift` | script | required | full, direct-pr,
local-only | pull_request, push | generated brief blocks compared |. Re-render
with node scripts/render-agent-rules-gates.mjs --write
EXIT=1
```

WHAT THE DERIVATION DID NOT COVER: it probes the ONE consequence of a registry
edit that this repository has wired (the rendered block in `CLAUDE.md`). It did
not enumerate every other consumer of `gate-registry.yaml`; the enumeration is
below under "the registry's consumers", run before the registry was edited.

The registry entry is not avoidable by dropping down to `gates.manifest.json`
alone: test/gate-registry.test.ts:302 asserts every manifest gate is promoted
into the registry, so a manifest-only gate is red.

`CLAUDE.md` therefore has to go on M3-P6's `filesToTouch`, and the scope auditor
reads the declaration from the MERGE BASE, so the amendment must land on `main`
and the branch must be brought up to it before the phase's PR gate runs. This is
reported to the orchestrator as the first item of the phase report. The
`CLAUDE.md` change is kept in its OWN commit, containing nothing but the
regenerated block, so it can be dropped or re-ordered without touching the rest.

### The registry's consumers, enumerated before the registry was edited

The mechanism above ("a registry edit obliges a `CLAUDE.md` edit") is one member
of a class: things that read `gate-registry.yaml` and can redden when a gate is
added. The enumeration, run at 086b8df:

```
$ git grep -n "gate-registry\.yaml" -- ':!delivery/' ':!CLAUDE.md'
```

The consumers it found that can redden on an ADDED gate, and what each does:

| consumer | reaction to a new gate |
|---|---|
| scripts/render-agent-rules-gates.mjs:131 | renders the block; `--check` reddens until `CLAUDE.md` is re-rendered (measured above) |
| test/gate-registry.test.ts:302 | every `gates.manifest.json` entry must be promoted into the registry, so a manifest-only gate is red |
| test/gate-registry.test.ts:420 | for a gate in BOTH manifest and registry, `events` must include `push` exactly when the gate is in the hard-coded main-bundle `--only` list in scripts/m2-exit-test.sh:944 |
| test/gate-registry.test.ts:1014 | `REGISTRY_ONLY_SCRIPT_GATES`, a hand-written register: a script gate in the registry and NOT in the manifest is red unless it carries a recorded reason there |
| src/checks.ts:610 `mode-gate-sets-resolve` | forward direction only (every `gate-sets[]` entry resolves); it does NOT require a new registry gate to appear in `assurance-modes.yaml`, so it stays green |

WHAT THE ENUMERATION DID NOT COVER. It searched for the literal string
`gate-registry.yaml` outside `delivery/` and `CLAUDE.md`. A consumer that
reaches the registry through a variable, a `--registry` flag value supplied at
run time, or a path built by concatenation would not appear; the two flag-driven
callers (`src/gates/run.ts`, `src/commands/gates.ts`) DID appear because they
name the default in prose, but that is luck rather than method. It also did not
search `delivery/`, deliberately: prose there is not a gate. The residual risk
was retired differently, by RUNNING the whole suite against the edit rather than
by reading (below).

### DECISION: how `brief-drift` is wired, and the two options that were measured

Step 3 says "mirroring M3-P2's agent-rules drift check" and the revision-3
files-to-touch note (delivery/plan/kernel-plan-m3.md:3410) says the check is
"registered as a gate entry with `events: [pull_request, push]` rather than
wired as a raw workflow step". Those two point at different wirings, and the row
at test/gate-registry.test.ts:420 makes the stated `events` value unreachable
without editing scripts/m2-exit-test.sh, which is on no phase declaration here.

Two options were built and MEASURED in a throwaway clone rather than argued:

- **Option A**, registry-only with `events: [pull_request, push]` plus a raw
  workflow step, which is agent-rules-drift's exact shape. Needs a row in
  `REGISTRY_ONLY_SCRIPT_GATES` at test/gate-registry.test.ts:1014, a file on no
  declaration.
- **Option B**, an entry in BOTH `gates.manifest.json` and `gate-registry.yaml`
  with `events: [pull_request]`, plus the workflow step criterion 11 requires.

Option B measured, at 086b8df in a clone, node v22.22.2, `dist/` built, bare
`node --test "test/**/*.test.ts"`:

```
1..590
# tests 590
# pass 588
# fail 0
# skipped 2
```

The two skips are the floor-gated `doctor` tests, which is what the default
toolchain skips at any head (CLAUDE.md standing warning 12). So Option B needs
NO out-of-scope test edit, and Option A needs one.

Option B is taken. Its second reason is closer to the plan's intent than to its
letter: D-M3-34 asks for checks to be GATES rather than raw steps, and under
Option B the runner genuinely executes `brief-drift` as a gate on the pull-request
arm, with a `GateResult` and M2-C-2's vacuity protection, which agent-rules-drift
does not get. `events: [pull_request]` is then the TRUE statement about the
harness bundles: the push arm is not in the main bundle's hard-coded `--only`
list, and it is covered instead by the workflow step, which carries no `if:` and
therefore runs on both events (T-009).

THE COST, named rather than left to be found: on a pull request the check runs
TWICE, once as a manifest gate and once as the workflow step. That is a few
seconds of runner time. The alternative was an `if:` on the step, which is the
shape kernel plan M3 section 2.3 rule 7 lists as a defang, so it was not taken.

### What was built, and the mandated-reading question asked per entry

Both briefs exist and both validate. The dispatch's second warning was that the
output-contract check reaches only the FIRST direction (an output whose schema is
absent from the reading list) and cannot see the second (an entry that resolves
and is the wrong document, which is where M3-P5's defect actually lived). So the
question "is this the contract of something I PRODUCE, or of something I am
GIVEN?" was asked per candidate entry, and the answers are recorded here rather
than left implicit.

Implementer, `outputs: [work-history]`. KEPT:

| entry | produce, given, or neither |
|---|---|
| roles/_shared-dispatch-contract.md | neither: it is part of this brief, included at compose time. Kept because all three merged briefs list it and dropping it would make this brief the odd one; the include resolves it regardless |
| schemas/work-history.schema.json | PRODUCE. Required by the output-contract check and it is the only entry that check can see |
| tuition/mechanism-index.yaml | GIVEN, and criterion 8 requires it by path |
| gate-registry.yaml | GIVEN: the gate-list section is rendered from it |
| gates.manifest.json | GIVEN, and criterion 8b requires the `destructiveCommands` path to resolve through this same check |

DROPPED, and each was a live candidate:

- `schemas/finding.schema.json`. This is the contract of what a REVIEWER
  produces. An implementer never emits one. Listing it would have been the exact
  shape of the M3-P5 defect, an entry that resolves and is the wrong document.
- `schemas/plan.schema.json`. The implementer reads the plan INSTANCE, which is
  supplied per phase; the schema governs the plan-writer's output.
- `schemas/report.schema.json`. The investigator's output contract.
- `assurance-modes.yaml`. The implementer does not select a mode.

Clean-room reviewer, `outputs: [finding]`. KEPT:
`roles/_shared-dispatch-contract.md`, `schemas/finding.schema.json` (PRODUCE),
`assurance-modes.yaml` (GIVEN: it is where the two review contracts and what
full mode requires are declared). DROPPED: `gate-registry.yaml`, because the
reviewer neither produces gates nor is handed the registry; the gate RESULTS it
is handed are a different artifact.

### DEVIATION 1: the clean-room brief's `review-contract` is not a frontmatter field

Step 2b (delivery/plan/kernel-plan-m3.md:3355) says the clean-room brief "carries
a `review-contract` frontmatter field whose value is `criteria` or `hazard`".
It is not delivered as a frontmatter field, and the reason is mechanical rather
than a preference.

`schemas/role-brief.schema.json` declares `additionalProperties: false`
(schemas/role-brief.schema.json:8) and is M3-P5's, not on this phase's
declaration. An unknown frontmatter key therefore fails criterion 1, which
requires `tiphys validate --type role-brief` to exit 0 on both briefs. Measured:

```
$ node bin/tiphys.ts validate --type role-brief roles/clean-room-reviewer.md
EXIT=0
```

There is also a reason not to want the field. The contract is a COMPOSITION-TIME
choice: one file, one frontmatter, two contracts, and a static field cannot say
which one this composition is running. Criterion 10, which is the binding
acceptance test and is newer than step 2b, specifies exactly a `--review-contract`
FLAG on `brief compose` and says nothing about frontmatter. So the declaration is
delivered where it can be true: the composed brief carries a `review-contract:`
header line, and the non-selected contract's clause block is dropped from the
composed body, so the brief a reviewer reads names one contract and carries one
contract's instructions. Measured:

```
$ node bin/tiphys.ts brief compose --role clean-room-reviewer \
    --phase templates/plan.example.yaml --phase-id M9-P1 --review-contract criteria | sed -n 6p
review-contract: criteria
$ ... --review-contract hazard | sed -n 6p
review-contract: hazard
$ ... --review-contract adversarial
tiphys brief compose: unknown review contract adversarial; the contracts are criteria, hazard
BOGUS=1
$ node bin/tiphys.ts brief compose --role investigator ... --review-contract criteria
tiphys brief compose: --review-contract applies to clean-room-reviewer and investigator declares no review contracts
WRONGROLE=1
```

The flag DEFAULTS to `criteria` for that role rather than being required, and
that is load bearing rather than lenient: test/roles.test.ts:502 composes every
brief in `roles/` with no such flag and asserts exit 0, so a required flag would
have reddened a merged phase's test.

### DEVIATION 2: `outputs` names `finding`, not `verdict`

Step 2 says the clean-room brief "outputs a verdict instance (the schema lands in
M3-P7, referenced here by type name)". `outputs: [verdict]` is red TODAY, and not
because of the shipped check, which skips an output type no schema is registered
for (src/roles.ts:306). It is red because test/roles.test.ts:263 asserts
`file !== undefined` for every declared output, which is STRICTER than both the
schema and the check, and that file is on no phase declaration here.

So `outputs` names `finding`, which is correct rather than a workaround: the
finding format is what a clean-room review produces and
`schemas/finding.schema.json` says so in its own words. The brief's body names
the forthcoming `verdict` type and states why it is not yet declared, so M3-P7
adds one line to `outputs` when it ships the schema.

### The `package.json` files entry

`tuition` is added, which the plan asks for. `gates.manifest.json` is added too,
and that is not in the plan text: criterion 8b requires the destructive-authority
clause's path to resolve through `brief compose`'s mandated-reading resolution,
which resolves against the INSTALLED KERNEL ROOT, so a manifest absent from the
published set would make every composition of the implementer brief fail from an
installed package while passing here. That is the loud-failure shape the check is
for, aimed at the wrong target.

---

# RESUME: second implementer, from 239978e

The implementer above died mid-sentence (its last section ends at "The
`package.json` files entry"). Its work was committed and pushed by the
orchestrator. Everything below this line is the RESUMING implementer's, and it
follows R-081b, which is a clause this very phase ships
(roles/implementer.md:261): salvaged work is VERIFIED OR REWRITTEN, never
trusted. "It builds and the suite is green" is not verification; this project
has a recorded case of a salvaged change that built, passed the full suite,
carried a plausible rationale, and held on one of three code paths.

Method for the resume, stated before any of it is done so it can be checked
against what follows:

1. Re-derive each acceptance criterion from the plan text, not from the
   predecessor's account of it, and record the derivation.
2. Re-derive the three recorded deviations rather than inherit them.
3. Execute every both-directions witness the criteria ask for, capturing both
   arms.
4. Run the gate bundle and the suite on all three axes.

Environment for this session, measured in the shell that runs the commands:

```
$ node --version
v22.22.2
```

## Resume log

### R1. Orientation, and what is already present

Salvage state at 239978e, measured by me rather than taken from the handover.

```
$ npm run build; echo BUILD_EXIT=$?; git status --porcelain
BUILD_EXIT=0
(clean apart from this work history)
$ npm test 2>&1 | tail
1..614
# tests 614
# pass 612
# fail 0
# skipped 2
NPM_TEST_EXIT=0
```

Toolchain node v22.22.2, `dist/` BUILT, invocation `npm test` (which is
`node --test "test/**/*.test.ts"`). The two skips are the floor-gated `doctor`
tests, which is what the default toolchain skips at any head. The third axis
(bare `node --test` from the root, which additionally selects the tracked
sandbox fixture) is measured separately below.

A green suite is not verification of salvaged work, which is the whole point of
R-081b. What follows is the re-derivation.

### R2. Constraint check first: is the READ-ONLY file still untouched?

```
$ git diff --stat origin/main...HEAD -- roles/_shared-dispatch-contract.md
(no output: zero changed lines)
$ grep -n 'MECHANISMS' roles/implementer.md roles/clean-room-reviewer.md roles/_shared-dispatch-contract.md
GREP_EXIT=1
```

Both constraints hold: the read-only shared block is byte-unchanged against
`origin/main`, and neither shipped brief names the interim index file M3-P8
deletes. The seed index does carry `supersedes: MECHANISMS.md`
(tuition/mechanism-index.yaml:38), which is a YAML field in the index rather
than text in a brief, and it is what M3-P8 removes when it deletes the file.

### R3. The three recorded deviations, RE-DERIVED rather than inherited

The handover named these as the judgments a resumed implementer must not
inherit. Each was re-derived by execution against the tree, not by re-reading
the predecessor's reasoning. All three are CONFIRMED, and the derivations are
below so a reviewer can refute them rather than believe them.

**DEVIATION 1 (`review-contract` is not a frontmatter field): CONFIRMED.**
The claimed mechanism is that `schemas/role-brief.schema.json` is closed, so an
unknown frontmatter key fails criterion 1. Measured, on a scratch copy:

```
$ grep -n 'additionalProperties' schemas/role-brief.schema.json
8:  "additionalProperties": false,
$ sed '2i review-contract: criteria' roles/clean-room-reviewer.md > <scratch>/cr.md
$ node bin/tiphys.ts validate --type role-brief <scratch>/cr.md
INVALID #/review-contract property review-contract is not permitted here
EXIT=1
```

The second half of the claim was that a REQUIRED flag would redden a merged
phase's test. Re-derived from the test's own source rather than from the line
number quoted: test/roles.test.ts:72 builds `AUTHORING_ROLES` by reading
`roles/` off disk and keeping every `.md` whose first line is `---`, so the two
briefs this phase adds are in that set the day they land, and the loop at
test/roles.test.ts:502 composes each of them with NO `--review-contract` flag
and asserts exit 0. A required flag reddens there. The flag therefore defaults
to `criteria`, which is what src/commands/brief.ts:168 does.

I also checked that `tiphys validate --type role-brief` is not vacuous on these
files, because "exit 0" from a check that examines nothing is the shape this
repository has recorded twice:

```
$ cp roles/clean-room-reviewer.md roles/_shared-dispatch-contract.md <scratch>/
$ node bin/tiphys.ts validate --type role-brief <scratch>/clean-room-reviewer.md
BASE_EXIT=0
$ printf '\n## clause bogus-unclaimed\n\nText.\n' >> <scratch>/clean-room-reviewer.md
$ node bin/tiphys.ts validate --type role-brief <scratch>/clean-room-reviewer.md
INVALID #/clauses body heading anchor bogus-unclaimed is not declared in frontmatter
STRAY_EXIT=1
```

**DEVIATION 2 (`outputs` names `finding`, not `verdict`): CONFIRMED.**
The claim is that `verdict` has no registered schema and that a merged test
asserts every declared output resolves to a file. Both halves measured:

```
$ node --input-type=module -e 'const m = await import(...src/commands/validate.ts); ...'
TYPE_TABLE.get(verdict) = undefined
TYPE_TABLE.get(finding) = finding.schema.json
```

and test/roles.test.ts:256 asserts `file !== undefined` for every output every
authoring brief declares, over the disk-derived role set above. So
`outputs: [verdict]` is red today and `outputs: [finding]` is the only value
that both resolves and is true: a clean-room review's product is a set of
findings, and roles/clean-room-reviewer.md:48 states in the brief's own body
that the `verdict` type is named by type name and deliberately not declared
until its schema ships at M3-P7.

**DEVIATION 3 (`gates.manifest.json` added to `package.json`'s files): CONFIRMED,
and it is not a widening of scope but a consequence of criterion 8b.**
`resolveMandatedReading` resolves each entry against `kernelRoot()`
(src/roles.ts:380), and `kernelRoot()` walks up from the compiled module to the
directory carrying `roles/*.md` (src/roles.ts:330), which in an installed
package is the package root. So every mandated-reading entry of a shipped brief
must be in the published set or `brief compose` fails from an install while
passing here. Derived from the briefs rather than asserted, against a REAL
tarball:

```
$ npm pack --pack-destination <scratch>
npm notice total files: 149
$ tar -tzf <scratch>/tiphys-kernel-0.0.0.tgz | ...
IN  roles/_shared-dispatch-contract.md
IN  schemas/work-history.schema.json
IN  tuition/mechanism-index.yaml
IN  gate-registry.yaml
IN  gates.manifest.json
MISSING_FROM_PUBLISHED_SET=0
IN  roles/_shared-dispatch-contract.md
IN  schemas/finding.schema.json
IN  assurance-modes.yaml
CR_MISSING=0
```

The counterfactual, so the two `files` entries are shown to be load bearing
rather than assumed to be: with `gates.manifest.json` and `tuition` removed from
`package.json`'s files list, the same pack carries ZERO of the three paths, and
with them it carries three.

```
$ (files entries removed) npm pack; tar -tzf ... | grep -cE '^(gates\.manifest\.json|tuition/)'
0
$ (files entries restored) tar -tzf ... | grep -cE '^(gates\.manifest\.json|tuition/)'
3
```

`package.json` was restored from a copy taken before the edit, not with
`git checkout --`, which standing warning 8 records as destructive in a tree
holding uncommitted work even when it names one path. `git status --porcelain`
after the restore showed only this work history modified.

VERDICT ON THE THREE DEVIATIONS: all three confirmed, none rewritten. Deviation
3 is the one worth a reviewer's attention, because it is the only one that
widens a shipped artifact beyond the plan text; the derivation above is why it
is a consequence of criterion 8b rather than a preference.

### R4. Criterion 11, re-derived: the wired step EXECUTED under three defangs

The handover doubted this was done. It was, and I did not take the test's word
for it: the step was extracted from the workflow independently and executed
against a staged kernel tree, with THREE structurally different defangs rather
than the two the criterion requires.

```
$ node -e '(parse .github/workflows/gates.yml, filter steps whose run names check-brief-drift.mjs)'
JOBS=["gates"]
MATCHING_STEPS=1
NAME=Implementer brief gate-list drift (gate-registry.yaml is the single source, R-094)
IF=undefined
RUN="node scripts/check-brief-drift.mjs --check"
```

One job, one matching step, and NO `if:`, so both CI events take it (T-009's
requirement that a forked arm have a witness on each side). The `run:` string
above, executed verbatim in a staged tree:

```
--- ARM 0 clean:
brief-drift: green (18 generated brief gate rows compared)
EXIT=0
--- ARM 1 (defang A): registry gains a gate, brief not re-rendered
brief-drift: red (19 generated brief gate rows compared)
... the registry has a row the brief does not: | `resume-probe-gate` | script | required | probes counted |
EXIT=1
--- ARM 2 (defang B): an EXISTING gate's unitLabel changed in the registry only
brief-drift: red (18 generated brief gate rows compared)
... the registry has a row the brief does not: | `suite` | script | required | tests counted |;
    the brief has a row the registry does not: | `suite` | script | required | tests reported |
EXIT=1
--- ARM 3 (defang C): the brief's generated block deleted entirely
brief-drift: error (0 generated brief gate rows compared)
roles/implementer.md carries no generated gate-list begin marker naming a mode
EXIT=21
--- ARM 4 restore:
brief-drift: green (18 generated brief gate rows compared)
EXIT=0
```

The three are structurally different in the sense section 2.3 rule 6 asks for.
Arm 1 is an ADDITION the brief lacks. Arm 2 is a MUTATION of a row present in
both, which an addition-only comparison would miss. Arm 3 is the check LOSING
ITS SUBJECT, which is the guard-condition failure this repository has recorded
twice (the watchdog that tested existence, and the byte check blind to NUL);
it exits 21 rather than 1 because it is an `error` and not a `red`, and a check
that answered "no drift" there would be the green-and-worthless shape.

Arm 2 is mine and is not in the registered test, which uses the block-edited
direction instead. I ran it because "the registry has a row the brief does not"
is one message and a MUTATION exercises both halves of `describeDrift`
(scripts/check-brief-drift.mjs:140), and I wanted to see the second half fire.

### R5. The gate bundle

Run: the CI PR bundle harness itself, which is what the `gates` workflow runs on
the `pull_request` event, rather than only a bare `gates run`.

```
$ bash scripts/m2-exit-test.sh --base origin/main --head HEAD --phase m3-p6 \
    --bundle pr --no-build <evidence>
gates: declared 12 applicable 8 verdict 8 green 8 red 0 not-applicable 4 error 0 vacuous 0
gates: required gate(s) not applicable: citations
m2-assert (PR bundle): OK. 12 gate record(s) match section 1.4; counts re-derived
  and equal to summary.json; zero error; zero vacuous.
m2-green: red-witness GREEN with 4 unit(s) against M2-P2 merged diff (real history)
m2-green: scope GREEN with 2 unit(s) against scratch repo
m2-green: citations GREEN with 1 unit(s) against scratch repo
m2-green: OK. 3 diff-scoped gate(s) demonstrated green on a triggering state.
m2-exit-test: OK.
EXITTEST_EXIT=0
```

Per-gate, from the bundle's own `result.json` records:

| gate | status | units |
|---|---|---|
| manifest-self-check | green | 8 schema documents validated |
| suite | green | 614 tests reported (pass 612, fail 0, skipped 2, 612 behaviours resolve) |
| coverage | green | 115 finding ids checked |
| credential-scrub | green | 7 credential sources probed |
| clause-map | green | 47 clause-map rows checked, 27 pending a phase not in force |
| scope | green | 24 changed paths audited, 0 off-list |
| red-witness | green | 19 witnesses evaluated (5 own, 14 stored) |
| brief-drift | green | 18 generated brief gate rows compared |
| citations | not-applicable | precondition evaluated and unmet |
| deploy, migrations | not-applicable | structural |
| credential-token | not-applicable | no token here |

TWO THINGS IN THAT RUN NEED SAYING RATHER THAN GLOSSING.

**The bare runner exits 20 and the harness exits 0, and both are correct.**
`gates run` exits 20 for "required gate(s) not applicable: citations". The
citations gate's `documents` globs (src/gates/citations.ts:232) are six trees
under `delivery/`, and this branch's only changed path under any of them is
`delivery/requirements/clause-map.json`, which is `.json` and not `**/*.md`, so
the precondition is EVALUATED and legitimately unmet. `scripts/m2-exit-test.sh`
records the runner exit as a fact and lets its assertion table decide; that
table marks citations `green|not-applicable` with `diffScoped: true` under
DR-0018. Note the consequence for the work history you are reading:
`delivery/work-history/**` is not in the documents globs at all, so nothing in
this file is checked by the citations gate on this branch.

**`brief-drift`'s teeth in CI are the WORKFLOW STEP, not the assertion table,
and I am recording that because the registry comment can be read as claiming
more than it says.** Measured from the harness source: `PR_EXPECT_JSON`
(scripts/m2-exit-test.sh:829) lists eleven gate ids and `brief-drift` is not
one of them, and `m2-assert` iterates `expect.gates` and `expect.absent` only,
so a record for a gate in NEITHER list is not compared against anything. Its
global checks are "zero error" and "zero vacuous" over ALL rows, with no
equivalent global "zero red". So:

| brief-drift outcome in a PR run | caught by m2-assert? | caught by the workflow step? |
|---|---|---|
| `error` (for instance the marker is gone) | YES, the zero-error check | YES, exit 21 |
| `red` (registry and brief disagree) | NO | YES, exit 1 |
| `green` | n/a | n/a |

The workflow step covers every case on both events, which is why it is there and
why it carries no `if:`. The registry `$comment` says the runner "executes it as
a real gate with a GateResult", which is TRUE and which I measured
(`pr-bundle/brief-drift/result.json`, green, 18 units); it does not say the
assertion table enforces it, and a reader could take it that way. Under R-087 I
have added one sentence to the registry entry saying which half is the teeth,
rather than leaving the next reader to derive it. `scripts/m2-exit-test.sh` is
on no M3 phase's declaration, so adding a table row is not this phase's to do.

### R6. Criterion 6, derived rather than counted

The plan says thirteen rows. Derived with the clause-map gate's OWN row pattern
over the plan's inventory table, so the number is read off the inventory rather
than copied from the criterion:

```
M3P6_INVENTORY_ROWS=13
R-007, R-009b, R-031, R-033a, R-034, R-037a, R-038, R-039, R-040, R-074, R-081b, R-082a, R-087
MAPPED_ROWS=13
UNMAPPED=
EXTRA=
```

M3-P6's anchor is roles/implementer.md (scripts/check-clause-map.mjs:91), which
exists on this branch, so the phase is IN FORCE and its rows are checked rather
than deferred: an unmapped row would be "owned by M3-P6, which is in force, and
has no clause-map entry". Non-vacuity, measured against a mutated map:

```
$ node scripts/check-clause-map.mjs --map <scratch>/map.json --evidence <scratch>/ev
CLAUSE-MAP R-007 names clause R-007-not-really-here, which does not occur in roles/implementer.md
clause-map: red (47 clause-map rows checked)
EXIT=1
```

### R7. The mechanism lookup this phase's own brief obliges

The plan's grounding names three interim rows that bind this phase's clauses,
so the lookup is performed here rather than left as an instruction the brief
gives other people.

- **Reading a path whose type is not established.** Rule: lstat the link, stat
  what it resolves to, open ONLY a regular file; a block is not an exception, so
  try/catch does not touch it. Satisfied: every read added by this phase goes
  through `readRegularFileIfPresent` or `classifyEntry` (src/roles.ts:45,
  src/roles.ts:381, src/commands/brief.ts:50, scripts/check-brief-drift.mjs:73)
  and every write through `refuseOpenForWrite`. The row also records the class as
  still OPEN in src/brief.ts, which is M1-P4's fleet-side module and which this
  phase does not touch; the row is correct as carried and this phase does not
  close it. A live probe is below.
- **Asserting a CI step is wired.** Rule: assert BEHAVIOUR, not text; extract the
  step and execute it against stubs; a class witness reddens under two
  structurally different members. Satisfied by criterion 11 above, executed under
  three. The seed index already names test/implementer-brief.test.ts as a sibling
  of this mechanism, which is the `mechanism-sibling` clause applied to the
  phase's own work.
- **Worktree removal and force branch delete.** Rule: resolve, evaluate, then
  apply; destructive authority is never inherited. NOT USED: this phase adds no
  destructive command. `check-brief-drift.mjs --write` rewrites one file in
  place, which is a write and not a removal, and it is refused through
  `refuseOpenForWrite` first. The `destructiveCommands` list in
  `gates.manifest.json` is unchanged by this phase, which is the correct answer
  and not an omission.

The dangerous state for that first mechanism is a path of the wrong TYPE, and
the rule says a block is not an exception, so try/catch does not reach it. Three
live probes, each with a ten-second timeout so that a hang is distinguishable
from a refusal (exit 124 would be a hang):

```
$ mkfifo tuition/mechanism-index.yaml   # a mandated-reading path
$ timeout 10 node bin/tiphys.ts brief compose --role implementer ...
tiphys brief compose: mandated-reading path tuition/mechanism-index.yaml: ... is a named pipe, not a regular file, so it was not opened
EXIT=1
$ mkfifo roles/implementer.md           # the brief the drift check reads
$ timeout 10 node scripts/check-brief-drift.mjs --check
brief-drift: error (0 generated brief gate rows compared)
roles/implementer.md is a named pipe, not a regular file, so it was not opened
EXIT=21
$ mkfifo gate-registry.yaml             # the registry it renders from
$ timeout 10 node scripts/check-brief-drift.mjs --check
brief-drift: error (0 generated brief gate rows compared)
gate-registry.yaml is a named pipe, not a regular file, so it was not opened
EXIT=21
```

None hung and all three refused by type. Note the two `error` results carry
`units: 0`, which is what M2-C-2's vacuity protection is for: the check does not
report "no drift" when it could not read its subject.

### R8. The remaining criteria, walked independently of the registered tests

Each was re-run by hand rather than read off a green suite, because a test and
the thing it tests can both be wrong in the same direction.

**Criterion 1.** `node bin/tiphys.ts validate --type role-brief` exits 0 on both
briefs, and `--type mechanism-index tuition/mechanism-index.yaml` exits 0.
Non-vacuity shown above (an undeclared anchor exits 1).

**Criterion 2, one witness per section plus the hollow state.** All six deleted
in turn in a staged tree, each refused by name; restored, exit 0.

```
mandated-reading       deleted -> EXIT=1
phase-scope            deleted -> EXIT=1
push-protocol          deleted -> EXIT=1
gate-list              deleted -> EXIT=1
environment-warnings   deleted -> EXIT=1
reporting-contract     deleted -> EXIT=1
--- restored: EXIT=0
```

and the message names the section rather than only failing:

```
required section gate-list is missing from the brief body, and R-033a requires
all six of mandated-reading, phase-scope, push-protocol, gate-list,
environment-warnings, reporting-contract
```

The hollow arm, which is the dangerous state a presence check would pass:

```
required section gate-list is present and empty, so the brief carries the
heading and none of the instruction
EXIT=1
```

**Criterion 3.** Byte compare of the COMPOSED brief's block against the
registry's own rendering, taken from two different commands:

```
BYTE_IDENTICAL=true
BLOCK_BYTES=1880 RENDER_BYTES=1880
```

The added-gate and hand-edited-block directions are in R4 above.

**Criterion 4.** Over the composed output: `gh pr create` ABSENT, `pr merge`
ABSENT, `open the PR` ABSENT.

**Criterion 5.** No warnings file: zero occurrences of the `# Environment
warnings` wrapper. With one: the wrapper, then the file's own first line AND its
LAST line, so the check distinguishes "read" from "truncated".

**Criterion 7, both directions, both briefs, exactly-once:**

```
implementer: declared=19 anchors=19 (own 17 shared 2)
  DECLARED_WITHOUT_ANCHOR=(none)  ANCHOR_UNDECLARED=(none)  DUPLICATE_ANCHORS=(none)
clean-room-reviewer: declared=6 anchors=6 (own 4 shared 2)
  DECLARED_WITHOUT_ANCHOR=(none)  ANCHOR_UNDECLARED=(none)  DUPLICATE_ANCHORS=(none)
```

**Criterion 8, the superset, derived from the interim file rather than counted:**

```
INTERIM_ROWS=12
SEED_ROWS=12
MISSING_FROM_SEED=(none)
```

**Criterion 9(a), and a hardening I made to its test.** `CLAUDE.md` carries
EXACTLY ONE `grep -nEi '...'` command and the clause carries it verbatim. The
test derived its expected command with `.exec`, taking the FIRST match; that is
correct today and silently compares against something else the day the
agent-rules file grows a second one. I changed it to collect all matches and
assert there is exactly one, so an ambiguous subject reddens instead of being
resolved by ordering. Green at head, and the red arm measured in memory:

```
$ node --test --test-name-pattern 'claim-grep clause carries' test/implementer-brief.test.ts
# pass 1  # fail 0
MATCHES_WITH_A_SECOND_GREP=2 (the assertion requires 1, so this reddens)
```

**Criterion 9(b).** All three items, the FULL-output half, the reviewer-first
sentence, and all five figures of the M1 measurement present in the composed
brief.

**Criterion 9(d), in BOTH composed briefs**, all five pinned phrases present.
The two weakenings are executed by the registered tests against a staged copy of
the shared block (a generic restatement, and the specific-and-wrong liveness
phrasing), which is the two-structurally-different-members rule.

**Criterion 10.** Both contracts compose, each declares itself on line 6, each
opens on its own contract clause, and the other contract's clause is absent
rather than merely later; an unknown value exits 1 naming it.

```
criteria: review-contract: criteria  FIRST_CLAUSE=## clause review-contract-criteria: ...
hazard:   review-contract: hazard    FIRST_CLAUSE=## clause review-contract-hazard: ...
vibes:    tiphys brief compose: unknown review contract vibes; the contracts are criteria, hazard
          EXIT=1
```

**Criterion 11.** R4 above, three defangs, executed.

### R9. What I changed, and why

I kept the salvage. Three edits, all small and all recorded:

1. gate-registry.yaml:223, the `brief-drift` `$comment`: one paragraph saying
   which half of the wiring fails the build, because the existing text can be
   read as claiming the assertion table enforces the gate and it does not. R-087
   in the form the brief itself states it: correct it in place, loudly.
2. scripts/check-brief-drift.mjs:40, the same correction at the module that
   makes the claim, with a line telling the next reader not to delete the
   workflow step on the grounds that the gate covers it.
3. test/implementer-brief.test.ts:512, the claim-grep subject hardening above.

Neither comment edit changes either generated block, which is a property worth
measuring rather than assuming, since a registry edit normally forces a
`CLAUDE.md` re-render:

```
$ node scripts/render-agent-rules-gates.mjs --check
agent-rules-drift: green (18 rendered gate rows compared)
AGENT_RULES_DRIFT_EXIT=0
$ node scripts/check-brief-drift.mjs --check
brief-drift: green (18 generated brief gate rows compared)
BRIEF_DRIFT_EXIT=0
```

### R10. Suite, all three axes, with the skipped counts

One head (`dist/` rebuilt after the three edits above), one toolchain, three
states. Every number below is from a run I made in this session.

| toolchain | build state | invocation | tests | pass | SKIPPED | exit |
|---|---|---|---|---|---|---|
| node v22.22.2 | `dist/` built | `npm test` | 614 | 612 | 2 | 0 |
| node v22.22.2 | `dist/` built | bare `node --test` from the root | 616 | 614 | 2 | 0 |
| node v22.22.2 | `dist/` ABSENT | `npm test` | 614 | 603 | 11 | 0 |

The two-test gap between the invocations is `sandbox/test/greet.test.js`, the
tracked sandbox fixture at the repository root; `package.json`'s `test` script
is `node --test "test/**/*.test.ts"`, which excludes it, and the `suite` GATE
runs that script. This is the third axis CLAUDE.md's standing warning 12
records, and 614 is what CI and the gate mean.

THE ELEVEN NEEDS EXPLAINING RATHER THAN AVERAGING, because standing warning 12
records NINE for the dist-absent state and this run says eleven. It decomposes
exactly, and it is not a new skip:

```
$ grep -c 'skip: existsSync(distEntry' test/gates.test.ts          -> 5
$ grep -c 'dist entry .* is absent' test/m2-exit-test.test.ts      -> 4
```

Nine dist-gated, plus the TWO floor-gated `doctor` tests that skip on this
toolchain at any head and in any build state. The warning's table was measured
on node v26.6.0, where the floor-gated pair does NOT skip, so it recorded the
nine alone. 9 + 2 = 11, and this phase added no dist-gated test. The two axes
compose, which is what the warning says one line further down.

### R11. The claim grep, both forms, and every hit settled

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m3-p6.md
121:  workflow step, which is agent-rules-drift's exact shape. Needs a row in
148:list, and it is covered instead by the workflow step, which carries no `if:` and
179:  produces. An implementer never emits one. Listing it would have been the exact
272:(roles/implementer.md:261): salvaged work is VERIFIED OR REWRITTEN, never
623:  apply; destructive authority is never inherited. NOT USED: this phase adds no
```

and the wrap-insensitive form, over the whitespace-flattened document, because a
phrase can straddle a line break and escape the line-based grep:

```
FLATTENED_HITS=5
```

Same five, so nothing was hiding across a wrap. Settled, in order:

1. **"Needs a row in `REGISTRY_ONLY_SCRIPT_GATES`"** (the predecessor's, about
   the rejected Option A). SETTLED by derivation rather than left as a claim.
   The test at test/gate-registry.test.ts:1043 compares the set of registry
   script gates absent from the manifest against that hand-written map's keys:

   ```
   REGISTRY_SCRIPT_GATES=13
   MANIFEST_GATES=12
   REGISTRY_ONLY_AS_SHIPPED=["agent-rules-drift"]
   REGISTERED_REASON_KEYS=["agent-rules-drift"]
   OPTION_A_WOULD_ADD=brief-drift -> ["agent-rules-drift","brief-drift"], not equal to the keys, so the test reddens
   ```

2. **"it is covered instead by the workflow step, which carries no `if:`".**
   SETTLED in R4: the step was parsed out of the workflow, `IF=undefined`, and
   executed. R5 states precisely what that coverage does and does not reach.
3. **"An implementer never emits one"** (about `schemas/finding.schema.json`).
   RESTATED as what it is, a reading of the two shipped contracts rather than a
   measurement: the finding format is declared as the clean-room reviewer's
   output at roles/clean-room-reviewer.md:19, and the implementer's declared
   output is `work-history` at roles/implementer.md:24. Nothing in the kernel
   FORCES an implementer not to write a finding; the claim is about the role
   contracts, and it is true of them.
4. **"salvaged work is VERIFIED OR REWRITTEN, never trusted"** is a quotation of
   the clause at roles/implementer.md:261, not an assertion of my own.
5. **"destructive authority is never inherited"** is a quotation of the mechanism
   index rule, whose full text is captured in R7 above.

### R12. What I did NOT cover

- **The push arm in a real CI run.** Everything above ran locally on node
  v22.22.2. The floor is `>=26` and CI is the authority; I did not fetch a
  floor-satisfying toolchain, and I did not observe the post-merge `push` run,
  which is the orchestrator's duty under T-009 rule 1 and is not mine to do
  from a branch.
- **`scripts/m2-exit-test.sh`'s expectation table.** R5 records that a `red`
  from `brief-drift` is caught by the workflow step and not by the assertion
  table. Adding a table row would close that asymmetry; the file is on no M3
  phase's declaration, so I did not touch it, and I have named it rather than
  left it.
- **Witness specs for three of this phase's behaviours.** Five `witness/*.json`
  specs exist and the red-witness gate evaluated all five red against every
  declared dangerous state. `dispatch-clause-text-is-specific-in-both-briefs`,
  `beacon-clause-rejects-liveness-phrasing` and
  `briefs-include-shared-dispatch-block-not-a-copy` have no spec file; their
  both-directions arms are executed INSIDE their tests against a staged copy of
  the shared block, which is a real red arm but is not re-evaluated by the gate
  on later heads. I did not add specs, because the natural dangerous state is a
  mutation of roles/_shared-dispatch-contract.md, which this phase is forbidden
  to change.
- **The `citations` gate over this document.** `delivery/work-history/**` is not
  in the citations gate's `documents` globs, so no citation in this file was
  resolved by a gate. I wrote them in the resolving form (`path.ext:LINE`,
  outside backticks) and resolved all 49 of them with a script of my own, which
  reported `TOTAL=49 UNRESOLVED=0` and printed the line each one lands on; ten
  pointed at a neighbouring line rather than the intended one and were corrected
  before this was written. That is my check and not a gate's.
- **The composed brief as read by a real agent.** I verified the text mechanically.
  Whether these clauses actually change implementer behaviour is not something
  this phase can measure, and no criterion claims it does.

### R13. The claim grep, re-run after the settlements above

Re-running it now returns MORE hits than the five in R11, and that is the grep
working rather than a regression: the settlement section quotes each claim in
order to settle it, and a pasted grep output is itself text the grep matches.

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m3-p6.md
121, 148, 179, 272, 623   the five original claims, settled one by one in R11
825-830                   the pasted output of the R11 run, quoting those five
842, 855, 858, 865, 867   R11's own settlement lines, each quoting its claim
```

Every hit outside the original five is a QUOTATION sitting immediately beside
its settlement, which is the shape the rule asks for rather than one it forbids.
One hit was neither: a filler "anyway" in R12's citations bullet, carrying no
claim at all. It has been rewritten out rather than explained away, because a
word that survives the grep only by being meaningless is the kind of hit that
teaches a reader to skim the next one.

The wrap-insensitive form over the flattened document returns the same set, and
the two counts are compared LIKE WITH LIKE rather than by eye, because the
line-based grep counts LINES and the flattened one counts OCCURRENCES and those
two numbers differ for reasons that have nothing to do with wrapping:

```
RAW_OCCURRENCES=38
FLATTENED_OCCURRENCES=38
MATCHING_LINES=18
```

38 equals 38, so no phrase is hiding across a line break. Comparing 18 against 38
would have looked like twenty missed hits and would have been a false alarm.

### R14. Final state

Head `aaff4b6`, on `claude/m3-p6-delivery-role-briefs`, pushed. Working tree
clean apart from this final append.

```
$ node scripts/check-authored-bytes.mjs
AUTHORED_BYTES_EXIT=0
$ LC_ALL=C grep -nP '[^\x00-\x7F]' $(git diff --name-only origin/main...HEAD)
NONASCII_EXIT=1 (1 = none)
$ LC_ALL=C grep -naP '[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]' $(git diff --name-only origin/main...HEAD)
CONTROL_EXIT=1 (1 = none)
$ git diff --stat origin/main...HEAD | grep -c 'Bin'
0
```

The `-a` form is the one that can see NUL, and the `Bin` count is the
independent check that no changed file lost its diff. Nothing in this branch's
captured output needed transliteration: no run pasted here came from Node's test
reporter summary lines, and the suite totals are quoted as numbers rather than
as reporter output.

Final gate bundle, the CI pull-request harness, at this head:

```
$ bash scripts/m2-exit-test.sh --base origin/main --head HEAD --phase m3-p6 --bundle pr --no-build <evidence>
gates: declared 12 applicable 8 verdict 8 green 8 red 0 not-applicable 4 error 0 vacuous 0
m2-assert (PR bundle): OK.
m2-green: OK. 3 diff-scoped gate(s) demonstrated green on a triggering state.
m2-exit-test: OK.
EXITTEST_EXIT=0

scope: green (24 changed paths audited)
red-witness: green (19 witnesses evaluated)
suite: green (614 tests reported)
brief-drift: green (18 generated brief gate rows compared)
clause-map: green (47 clause-map rows checked)
```

And the constraint that had to hold at the end as well as at the start:

```
$ git diff --stat origin/main...HEAD -- roles/_shared-dispatch-contract.md
(no output: zero changed lines)
```

## Fix round 1 (in progress, appended incrementally)

Started 2026-08-12 by a FRESH implementer, because the round-0 implementer is
no longer reachable in this session. This is NOT a DR-0016 escalation: M3-P6
has had zero prior fix rounds and this is the first, well inside DR-0012's
bound of two.

Inputs: delivery/review/clean-room-m3-p6-criteria.md:1 (contract A),
delivery/review/clean-room-m3-p6-self-comparison.md:1 (contract B) and
delivery/review/arbitration-m3-p6.md:116 (the disposition table), all on
`origin/claude/reviews-m3-p6`. Three findings to fix (CV-1, A-1, A-2); F-B3
is TRACKED and deliberately not fixed; F-B1 is an orchestrator-side round on
another branch and is out of my declaration.

This section is written as the work happens, not at the end. Anything below
this line that is not yet finished says so.


### The three fixes, and what each one changed

| finding | mechanism named | changed |
|---|---|---|
| CV-1 (A's F3 and B's F-B2, convergent) | A CHECK WHOSE SUBJECT IS SELECTED BY A VALUE READ FROM THE ARTIFACT IT AUDITS CAN BE SILENTLY NARROWED BY EDITING THAT ARTIFACT, and a unit count that does not measure what was compared cannot make the vacuity guard fire | src/roles.ts:524 (the pin), src/roles.ts:672 (units), scripts/check-brief-drift.mjs:224 (the refusal) |
| A-1 (criterion 9(b) proved in memory) | A CHECK PROVED BY CONSTRUCTING ITS INPUT RATHER THAN BY RE-RUNNING ITSELF OVER THAT INPUT proves the input is constructible and says nothing about whether the check reddens | test/implementer-brief.test.ts:735 |
| A-2 (wiring witness spec is not a class) | A WITNESS WHOSE MEMBERS ALL DISABLE THE STEP NEVER RE-EVALUATES THE ARM THAT EXECUTES IT, so the executed half is guarded by the test and not by the gate that re-evaluates the test | witness/brief-drift-check-wired-executably.json:22 |

### CV-1: the mechanism, and why the obvious fix was not enough

The obvious fix is "assert the mode string equals full". That closes the
instance contract A demonstrated and leaves the mechanism, which has TWO faces
and only one of them is the mode string.

**Face one, the SUBJECT.** The mode is read out of the brief's own begin marker.
That is deliberate and it is half right: it stops a CALLER pointing the
comparison at a mode the brief never claimed, which is why a `--mode` flag was
rejected (scripts/check-brief-drift.mjs:218). It leaves the EDITOR of the brief
holding exactly the same power. The pin therefore moved OUT of the artifact
under test, to `BRIEF_GATE_BLOCK_MODE` in src/roles.ts:524, and the script now
refuses a disagreement in EVERY mode including `--write`, because `--write` is
the command that turns a narrowed marker into a self-consistent smaller table.

It is not a registry key. `gate-registry.yaml` is closed
(`additionalProperties: false` at schemas/gate-registry.schema.json:11) and its
schema belongs to M3-P2 and is not on this phase's declaration, so a registry
key would be this phase editing another phase's merged contract. That is the
same reasoning that put the mode in the marker rather than the frontmatter.

WHY `full` is not left as a bare literal: the registered test DERIVES from the
registry that the pinned mode selects every gate any mode selects, per mode and
per gate, never by count. Narrowing is then the only direction the value can
move, and the invariant lives in the test rather than in the check, so a future
registry that legitimately grows a mode costs a test failure and a deliberate
update rather than a false red in CI.

**Face two, the COUNT.** `units` was `registry.preflight.length +
selected.length` and `preflight` is mode-independent, so the number had a floor
of three. M2-C-2 rewrites green-with-zero-units and nothing else, so the vacuity
guard could not fire over an EMPTY gate table, and the script header's promise
that "a run that compared ZERO rows becomes error with vacuous: true" was true of
the plumbing and false of the behaviour. `units` is now `selected.length`, which
is what `unitLabel` ("generated brief gate rows compared") names. The gate's
own report moves from 18 to 15 for that reason; the 18 every prior capture
quotes was 15 gate rows plus 3 preflight steps.

Fixing face two closes the empty-subject arm for EVERY mode rather than for the
one contract B used, which is why it is the mechanism fix and the mode pin alone
would not have been.

### CV-1: red witnesses, three dangerous states, exit codes both ways

Toolchain for every capture below: node v26.6.0 from the scratch prefix, checked
in the shell that ran it. Tree verified clean before and after by
`git status --porcelain`; every defang was restored by copying back from a
snapshot directory, never with `git checkout --`.

Baseline, all three new tests green:

```
=== BASELINE (fixed) ===
ok 1 - the shipped brief's gate-list block declares the mode the kernel pins, and that mode selects every gate any mode in the registry selects
ok 2 - narrowing the brief's declared gate-list mode makes the drift check refuse in both --write and --check, rather than re-rendering a smaller table and calling it green
ok 3 - the rendering counts the gate rows it produced, so a registry declaring no gate for the brief's mode makes the drift check a vacuous error and never a green over an empty table
# pass 3
# fail 0
```

DANGEROUS STATE 1, the pre-round unit arithmetic restored
(`units: registry.preflight.length + selected.length`):

```
ok 1 - the shipped brief's gate-list block declares the mode the kernel pins, and that mode selects every gate any mode in the registry selects
ok 2 - narrowing the brief's declared gate-list mode makes the drift check refuse in both --write and --check, rather than re-rendering a smaller table and calling it green
not ok 3 - the rendering counts the gate rows it produced, so a registry declaring no gate for the brief's mode makes the drift check a vacuous error and never a green over an empty table
# pass 2
# fail 1
--- what the gate itself said under that state ---
brief-drift: green (18 generated brief gate rows compared)
roles/implementer.md's full gate block matches gate-registry.yaml row for row (18 row(s) compared)
EXIT=0
--- restored ---
brief-drift: green (15 generated brief gate rows compared)
roles/implementer.md's full gate block matches gate-registry.yaml row for row (15 row(s) compared)
EXIT=0
```

DANGEROUS STATE 2, the mode pin deleted from the script (the pre-round check),
followed by the editor move contract A demonstrated, end to end:

```
ok 1 - the shipped brief's gate-list block declares the mode the kernel pins, ...
not ok 2 - narrowing the brief's declared gate-list mode makes the drift check refuse ...
ok 3 - the rendering counts the gate rows it produced, ...
# pass 2
# fail 1
--- and what an editor could then do, end to end ---
check-brief-drift: rewrote the local-only gate block in roles/implementer.md (5 row(s) from gate-registry.yaml)
WRITE_EXIT=0
brief-drift: green (5 generated brief gate rows compared)
roles/implementer.md's local-only gate block matches gate-registry.yaml row for row (5 row(s) compared)
CHECK_EXIT=0
ROWS=5
--- restored ---
brief-drift: green (15 generated brief gate rows compared)
EXIT=0
```

Five rows where the brief must advertise fifteen, with the gate green: contract
A's finding reproduced exactly, and closed.

DANGEROUS STATE 3, structurally different from both: the SHIPPED brief narrowed
to `local-only` and re-rendered (the pin removed first so `--write` would
legitimise it, which is the pre-round world), then the pin restored:

```
ROWS_IN_SHIPPED_BRIEF=5
not ok 1 - the shipped brief's gate-list block declares the mode the kernel pins, ...
not ok 2 - narrowing the brief's declared gate-list mode makes the drift check refuse ...
not ok 3 - the rendering counts the gate rows it produced, ...
# pass 0
# fail 3
--- and the gate on the shipped tree ---
brief-drift: error (0 generated brief gate rows compared)
roles/implementer.md's generated gate-list block declares mode local-only and the shipped brief must declare full: a narrowed mode renders a smaller gate table that this check would then find in agreement with itself
EXIT=21
=== restored ===
# pass 3
# fail 0
TREE: 0 modified paths
```

Three structurally different members: the arithmetic, the check's refusal, and
the shipped artifact itself. One witness is not a class, and these are not one
member three times: they redden different tests and the third reddens all three.

### A-1: the fix-round clause check now executes

The registered arm built the two-item weakening with `String.replace` and
asserted the result no longer CONTAINED item 3. That proves a weakening is
CONSTRUCTIBLE. It never re-ran the requirements over the weakened text, so it
said nothing about whether the check reddens, and it is the exact shape
criterion 11 exists to prevent one file over.

The requirements are now one predicate, `fixRoundClauseFindings`, returning the
list of unmet requirements rather than throwing, so an arm can observe WHAT the
check said. Each weakening is written to a FILE, read back off disk, and put
through that same predicate. Four weakenings, structurally different because the
four requirements fail independently: a dropped ITEM, a softened item whose
heading survives, a dropped ORDERING sentence, and a dropped MEASUREMENT figure.
Each names the finding it expects, so an arm reddening for another reason fails.

```
ok 1 - the fix-round-mechanism clause names all three items and cites the M1 measurement, and every weakening of it reddens the same check when it is re-run over the weakened file
# pass 1
# fail 0
```

The test NAME changed, so `test/behaviors.json`'s existing
`implementer-brief-fix-round-three-items` value was updated to match. That is an
edit to a registry row rather than an append, and it is required: the `suite`
gate resolves behaviours BY NAME and a renamed test with a stale row reddens.

### A-2: the wiring witness spec gains its running-but-toothless member

Contract A verified that the TEST is red against a step that runs and is inert;
what the spec's two members did not do was reach the executed half, so the
red-witness GATE, which is what re-evaluates these arms on later heads, never
exercised it. Both existing members replace the step wholesale.

Member three replaces only the `run:` line, dropping `--check` so the script runs
in print mode and exits 0. The step is still present, still named, and still
references the script, so a text assertion over the workflow is GREEN against it.
Its `find` is a different string from the other two members', so rule (g)'s
same-file-same-find collapse (src/witness/run.ts:1315) cannot fold it into them.

```
occurrences of the member-3 find in gates.yml: 1
=== BASELINE wiring test ===
ok 1 - the brief-drift step wired into the gates workflow is executed against stubs and reddens under two structurally different defangs
# pass 1
# fail 0
=== MEMBER 3 APPLIED (step present, named, references the script, and toothless) ===
136:      - name: Implementer brief gate-list drift (gate-registry.yaml is the single source, R-094)
137-        run: node scripts/check-brief-drift.mjs
138-      - name: M2 exit test (pull request)
not ok 1 - the brief-drift step wired into the gates workflow is executed against stubs and reddens under two structurally different defangs
  error: 'the wired step exited 0 with the registry ahead of the brief'
# pass 0
# fail 1
=== restored ===
ok 1 - the brief-drift step wired into the gates workflow is executed against stubs and reddens under two structurally different defangs
# pass 1
# fail 0
```

The test's own name still says "two structurally different defangs" and that
remains ACCURATE: it counts the defangs the TEST applies (a registry ahead of the
brief, and a removed block), which is a different set from the workflow mutations
the WITNESS applies. The name was not changed for that reason.

### Suite and gates for this round

Complete sentence, all four axes: node **v26.6.0** (scratch prefix, checked with
`node --version` in the shell that ran it), `dist/` **BUILT** (`npm run build`
exit 0, `git status --porcelain` zero lines after it), invocation **`npm test`**:

```
node v26.6.0
i tests 617
i suites 0
i pass 617
i fail 0
i cancelled 0
i skipped 0
i todo 0
i duration_ms 157042.418832
SUITE_EXIT=0
```

TRANSLITERATION DECLARED: Node's test reporter prints U+2139 INFORMATION SOURCE
at the head of each summary line. Seven occurrences appear in the block above and
each is rendered `i`. U+2716 does not occur (there were no failures). Nothing
else in any captured output in this section was changed.

617 is 614 plus this round's three new tests, and 0 SKIPPED because `dist/` was
built. The full PR bundle on this head:

```
$ git branch --show-current
claude/m3-p6-delivery-role-briefs
$ bash scripts/m2-exit-test.sh --base origin/main --head HEAD --phase m3-p6 --bundle pr --no-build <evidence>
m2-exit-test: recorded (observation: scope expectation resolved for this run)
gates: run f9113437abe2cfb62df41ea0
gates: declared 12 applicable 8 verdict 8 green 8 red 0 not-applicable 4 error 0 vacuous 0
gates: required gate(s) not applicable: citations
m2-assert (PR bundle): OK. 12 gate record(s) match section 1.4; counts re-derived and equal to summary.json; zero error; zero vacuous.
m2-green: red-witness GREEN with 4 unit(s) against M2-P2 merged diff 1b6f0963b62f^..1b6f0963b62f (real history)
m2-green: scope GREEN with 2 unit(s) against scratch repo: declaration governs claude/m2-p4-scope-auditor, diff touches only src/a.ts and src/b.ts
m2-green: citations GREEN with 1 unit(s) against scratch repo: changed delivery/plan/fixture.md cites src/target.ts:1 which resolves
m2-green: OK. 3 diff-scoped gate(s) demonstrated green on a triggering state.
m2-exit-test: OK.
EXITTEST_EXIT=0
```

Per gate, read out of the run's own `summary.json` rather than retyped:

| status | units |
|---|---|
| green | 8 schema documents validated |
| green | 115 finding ids checked |
| green | 7 credential sources probed |
| not-applicable | 0 tokens probed |
| green | **617 tests reported** |
| not-applicable | 0 citations resolved |
| green | **24 changed paths audited** |
| not-applicable | 0 release verifications satisfied |
| not-applicable | 0 migrations compared |
| green | 47 clause-map rows checked |
| green | **19 witnesses evaluated** |
| green | **15 generated brief gate rows compared** |

`scope: green (24 changed paths audited)` is the scope audit passing on this
round's head, which is the row contract A could not reproduce from a detached
worktree. `red-witness: green (19 witnesses evaluated)` covers the third member
added above. `citations` reports not-applicable in this bundle and is run
separately below, because a not-applicable is not evidence that citations
resolve.


### The derivation, published in full, and the region it did not cover

Fix-round contract item 2. The mechanism, restated so the search is over it and
not over the finding: **a check whose subject is selected by a value read from
the artifact it audits**, and its second face, **a unit count that does not
measure what was compared, so the vacuity guard has a floor it cannot fall
below.** Each face was enumerated separately, because a single grep over both
would have been a grep over neither.

**D1: every generated-block marker family in tracked files.** This is the
universe for "a block regenerated from a source", which is where the first face
lives. `delivery/` is excluded because it is paperwork and renders nothing.

```
$ git grep -n "BEGIN GENERATED" -- $(git ls-files | grep -v '^delivery/')
CLAUDE.md:244:<!-- BEGIN GENERATED GATE LIST: rendered from gate-registry.yaml by scripts/render-agent-rules-gates.mjs. Do not edit by hand; edit the registry. -->
roles/implementer.md:369:<!-- BEGIN GENERATED GATE LIST (mode: full): rendered from gate-registry.yaml by scripts/check-brief-drift.mjs. Do not edit by hand; edit the registry. -->
scripts/render-agent-rules-gates.mjs:83:  "<!-- BEGIN GENERATED GATE LIST: rendered from gate-registry.yaml by scripts/render-agent-rules-gates.mjs. Do not edit by hand; edit the registry. -->";
src/roles.ts:520:    `<!-- BEGIN GENERATED GATE LIST (mode: ${mode}): rendered from gate-registry.yaml ` +
src/roles.ts:557:  /<!-- BEGIN GENERATED GATE LIST \(mode: ([a-z][a-z0-9-]*)\): rendered from gate-registry\.yaml by scripts\/check-brief-drift\.mjs\. Do not edit by hand; edit the registry\. -->/;
test/gate-registry.test.ts:803:    const begin = rules.indexOf("<!-- BEGIN GENERATED GATE LIST");
```

**D1b: which of those markers carry a PARAMETER that selects what is rendered.**
This is the discriminator: a marker with no parameter cannot narrow anything.

```
$ git grep -nE "BEGIN GENERATED [A-Z ]+\((mode|[a-z]+): " -- $(git ls-files | grep -v '^delivery/')
roles/implementer.md:369:<!-- BEGIN GENERATED GATE LIST (mode: full): rendered from gate-registry.yaml by scripts/check-brief-drift.mjs. Do not edit by hand; edit the registry. -->
src/roles.ts:520:    `<!-- BEGIN GENERATED GATE LIST (mode: ${mode}): rendered from gate-registry.yaml ` +
```

TWO families exist and exactly ONE carries a selecting parameter. `CLAUDE.md`'s
block renders EVERY gate with no mode at all, so
`scripts/render-agent-rules-gates.mjs` has no subject to narrow. The site the
reviewers found is the only member of this family.

**D2: every call site of the two functions that carry the selector,** across all
tracked files including tests, because a test is a caller too:

```
$ git grep -n "renderBriefGateBlock\|locateGateBlock" -- $(git ls-files)
scripts/check-brief-drift.mjs:15: * green forever. `renderBriefGateBlock` in src/roles.ts takes the decoded
scripts/check-brief-drift.mjs:96:  locateGateBlock,
scripts/check-brief-drift.mjs:97:  renderBriefGateBlock,
scripts/check-brief-drift.mjs:230:  const located = locateGateBlock(briefRead.body, options.brief);
scripts/check-brief-drift.mjs:264:  const rendered = renderBriefGateBlock(decoded.value, located.mode);
src/roles.ts:572:export function locateGateBlock(text: string, path: string): GateBlockLocation {
src/roles.ts:657:export function renderBriefGateBlock(
test/implementer-brief.test.ts:51:  locateGateBlock: (
test/implementer-brief.test.ts:55:  renderBriefGateBlock: (
test/implementer-brief.test.ts:229:  const located = rolesModule.locateGateBlock(composed.stdout, "the composed brief");
test/implementer-brief.test.ts:240:  const rendered = rolesModule.renderBriefGateBlock(registry, located.mode);
test/implementer-brief.test.ts:319:  const located = rolesModule.locateGateBlock(readFileSync(briefPath, "utf8"), briefPath);
test/implementer-brief.test.ts:354:    const located = rolesModule.locateGateBlock(original, path);
test/implementer-brief.test.ts:411:  const rendered = rolesModule.renderBriefGateBlock(
test/implementer-brief.test.ts:450:    const table = rolesModule.locateGateBlock(readFileSync(briefAt(dir), "utf8"), briefAt(dir));
test/implementer-brief.test.ts:483:    const located = rolesModule.locateGateBlock(original, path);
test/implementer-brief.test.ts:933:    const located = rolesModule.locateGateBlock(brief, path);
```

ONE production site feeds a located mode into the renderer
(scripts/check-brief-drift.mjs:264) and it is now behind the pin at
scripts/check-brief-drift.mjs:224. test/implementer-brief.test.ts:240 is the
composed-brief comparison and does the same thing DELIBERATELY, because its job
is to prove the composed block equals the registry's rendering FOR THE DECLARED
MODE; the shipped brief's declared mode is now pinned by
test/implementer-brief.test.ts:319, so that arm can no longer be narrowed either.

**D3: the universe of checks, stated before searching it,** so an empty result
below is a result and not an absence of scope. Every gate declared in the
registry and in the manifest, plus the direct workflow steps that run a check
outside the runner:

```
$ node -e '<read gate-registry.yaml and gates.manifest.json, print every id and command>'
registry manifest-self-check: node bin/tiphys.ts gates self-check --manifest gates.manifest.json
registry coverage: node src/gates/coverage.ts
registry credential-scrub: node src/gates/credentials.ts credential-scrub
registry credential-token: node src/gates/credentials.ts credential-token
registry suite: node src/gates/suite.ts --pin-root src --pin-root bin --pin-root test
registry citations: node src/gates/citations.ts
registry scope: node src/gates/scope.ts --declarations delivery/plan/phase-declarations
registry deploy: node src/gates/deploy.ts
registry migrations: node src/gates/migrations.ts
registry clause-map: node scripts/check-clause-map.mjs
registry red-witness: node src/gates/red-witness.ts
registry agent-rules-drift: node scripts/render-agent-rules-gates.mjs --check
registry brief-drift: node scripts/check-brief-drift.mjs --check
registry unit-tests-for-changed-service-methods: (checklist)
registry fixtures-for-changed-component-states: (checklist)
manifest manifest-self-check: node bin/tiphys.ts gates self-check --manifest gates.manifest.json
manifest coverage: node src/gates/coverage.ts
manifest credential-scrub: node src/gates/credentials.ts credential-scrub
manifest credential-token: node src/gates/credentials.ts credential-token
manifest suite: node src/gates/suite.ts --pin-root src --pin-root bin --pin-root test
manifest citations: node src/gates/citations.ts
manifest scope: node src/gates/scope.ts --declarations delivery/plan/phase-declarations
manifest deploy: node src/gates/deploy.ts
manifest migrations: node src/gates/migrations.ts
manifest clause-map: node scripts/check-clause-map.mjs
manifest red-witness: node src/gates/red-witness.ts
manifest brief-drift: node scripts/check-brief-drift.mjs --check

$ git grep -nE "run: node scripts/" -- .github/workflows/
.github/workflows/gates.yml:112:        run: node scripts/render-agent-rules-gates.mjs --check
.github/workflows/gates.yml:137:        run: node scripts/check-brief-drift.mjs --check
.github/workflows/macos-smoke.yml:23:      - run: node scripts/check-authored-bytes.mjs
```

**D4: where each of those checks gets its comparison SCOPE**, by listing every
flag each parses. A check whose scope comes only from argv cannot have it
narrowed by the artifact:

```
bin/tiphys.ts
src/gates/coverage.ts                    "--config" "--evidence" "--result"
src/gates/credentials.ts                 "--evidence" "--get-all" "--result"
src/gates/suite.ts                       "--base" "--evidence" "--head" "--pin-root" "--registry" "--result" "--suffix" "--test-root"
src/gates/citations.ts                   "--base" "--evidence" "--head" "--name-only" "--no-color" "--result" "--verify"
src/gates/scope.ts                       "--abbrev-ref" "--base" "--declarations" "--evidence" "--head" "--is-ancestor" "--name-status" "--phase" "--result" "--verify"
src/gates/deploy.ts
src/gates/migrations.ts
scripts/check-clause-map.mjs             "--evidence" "--inventory" "--map" "--result"
src/gates/red-witness.ts                 "--base" "--baseline" "--evidence" "--head" "--is-shallow-repository" "--phase" "--result"
scripts/render-agent-rules-gates.mjs     "--agent-rules" "--check" "--evidence" "--registry" "--result" "--write"
scripts/check-brief-drift.mjs            "--brief" "--check" "--evidence" "--registry" "--result" "--write"
scripts/check-authored-bytes.mjs         "--batch" "--no-ext-diff" "--quiet"
```

**D6: every place a check assigns its own scope option**, so a default read out
of an audited document would appear here:

```
$ git grep -nE "^\s+options\.[a-zA-Z]+ = " -- scripts/ src/gates/ | grep -v "\.test\."
scripts/check-brief-drift.mjs:127:      options.mode = flag.slice(2);
scripts/check-brief-drift.mjs:135:      options.registry = value;
scripts/check-brief-drift.mjs:137:      options.brief = value;
scripts/check-brief-drift.mjs:139:      options.result = value;
scripts/check-brief-drift.mjs:141:      options.evidence = value;
scripts/render-agent-rules-gates.mjs:104:      options.mode = flag.slice(2);
scripts/render-agent-rules-gates.mjs:112:      options.registry = value;
scripts/render-agent-rules-gates.mjs:114:      options.agentRules = value;
scripts/render-agent-rules-gates.mjs:116:      options.result = value;
scripts/render-agent-rules-gates.mjs:118:      options.evidence = value;
```

Every assignment takes an argv value. `options.mode` in both scripts is the
print/check/write MODE and not the gate-set mode, which is the collision of
names that made this worth checking rather than assuming.

**D7: every check that passes a value parsed out of an input document as a
selector to its own renderer or filter:**

```
$ git grep -nE "\b(render|select|filter|find)[A-Za-z]*\([^)]*\b(located|parsed|decoded|declared|read)[A-Za-z]*\.[a-zA-Z]+" -- src/ scripts/ bin/ | grep -v "\.test\."
scripts/check-brief-drift.mjs:264:  const rendered = renderBriefGateBlock(decoded.value, located.mode);
src/commands/mode.ts:151:  for (const line of renderMode(mode, { shippedDocument: parsed.options.file === undefined })) {
src/witness/run.ts:760:  const lines = findOccurrenceLines(read.body, member.find);
```

Three hits, two of them NOT the mechanism and both checked rather than dismissed.
src/commands/mode.ts:151 passes `parsed.options.file === undefined`, which is an
argv fact about whether the caller supplied a path, not a value read out of the
document. src/witness/run.ts:760 takes `member.find` from the WITNESS SPEC and
searches the SOURCE FILE, which are two different artifacts, and the spec is
guarded separately by rule (g) at src/witness/run.ts:1291.

**D5: the second face, the units arithmetic.** Run at the pre-round head so it
is a search that FOUND something, then at this head:

```
$ git grep -n -E 'units: [^,;]+ \+ ' 16bab6f -- src scripts bin
16bab6f:src/roles.ts:644:  return { text: lines.join("\n"), units: registry.preflight.length + selected.length };
EXIT=0

$ git grep -n -E 'units: [^,;]+ \+ ' HEAD -- src scripts bin
EXIT=1

$ git grep -n -E 'units: [^,;]+ \+ ' HEAD
HEAD:delivery/work-history/m3-p6.md:1070:(`units: registry.preflight.length + selected.length`):
EXIT=0
```

The whole-tree run is there so the path filter is not doing the work: the only
remaining occurrence anywhere is this document quoting the removed line. The
sum-of-two-lengths units shape had exactly ONE site in the repository and it was
brief-drift's.

#### A BROKEN GREP THAT RETURNED EMPTY AT BOTH HEADS, recorded rather than fixed quietly

D5's first form was:

```
git grep -nE "units: [A-Za-z_.\[\]()]+ \+ " -- src/ scripts/ bin/
```

It returned NOTHING at this head, which is the answer I wanted, and it also
returned nothing at 16bab6f, where the site demonstrably exists. The `\[` and
`\]` inside a double-quoted shell string reach ERE as literal brackets and break
the character class. I caught it only because I ran the search against the
pre-round head as a CONTROL, expecting one hit, and got zero.

That is the exact failure the fix-round contract's item 3 names: a search whose
scope or syntax is wrong returns an empty result indistinguishable from an
absence of defects, and this repository has now been bitten by it four times. The
control was not optional and every negative derivation above has one: D1b
discriminates two families and returns one, D7 returns three and two are
examined, D5 returns one at the control head and zero here.

#### What the derivation did NOT cover

The reviewer's first check, so it is stated in full and in the negative.

- **`delivery/**` is excluded from D1.** It is the build's own paperwork, it
  renders no generated block from any source, and it ships in no package. If a
  future `delivery/` document grew a generated block, D1's exclusion would hide
  it.
- **D3's `unit-tests-for-changed-service-methods` and
  `fixtures-for-changed-component-states` are clean-room CHECKLIST probes, not
  script gates**, so they have no program to search and D4, D6 and D7 say nothing
  about them. A checklist probe's subject is chosen by a human reviewer, which is
  the mechanism in a form no grep reaches.
- **D4 lists flags, not defaults.** It proves each check ACCEPTS a caller-supplied
  scope; D6 is what proves none of those options is assigned from a parsed
  document. Neither covers a scope value that never passes through an `options`
  object at all, and `src/gates/deploy.ts` and `src/gates/migrations.ts` parse no
  flags, so for those two the enumeration rests on D7 alone.
- **D7 is a syntactic proxy.** It matches a selector passed to a call whose name
  begins render/select/filter/find on ONE line. A selector assigned to an
  intermediate variable first, or passed to a differently named function, or
  spanning two lines, is invisible to it. This is the gap most likely to hide a
  fourth instance and I did not close it; what bounds it is D3, which states that
  the universe is twelve script gates, and D1, which states that the
  generated-block family has two members.
- **I did not attack the checks this phase does not touch.** `coverage`,
  `credential-scrub`, `credential-token`, `deploy`, `migrations`,
  `manifest-self-check` and `citations` were enumerated in D3, D4, D6 and D7 and
  were not otherwise probed, and none of them is on this phase's declaration.
- **I did not run the `gates` workflow as GitHub would run it.** The
  running-but-toothless witness member was applied by hand and observed to redden
  the registered test; whether the `push` event reaches that step is the open
  question contract A recorded and this round does not close it either. It needs
  an observed CI run on a `main` head, which is T-009's duty on the orchestrator.
- **I did not re-verify contract A's or contract B's other findings.** F-B1 is on
  another branch and F-B3 is tracked; see below.
- **The three new tests were not run on the default toolchain.** Every capture
  here is node v26.6.0 from the scratch prefix. Nothing in the three is
  floor-gated, and the whole suite runs on the default toolchain in the axis table
  below, but the three were not individually isolated there.

### F-B3: tracked, and I agree with the call

`describeDrift` compares line SETS (scripts/check-brief-drift.mjs:158), so a
reordered or duplicated row falls to the "blank-line placement or line order"
branch and the message names nothing. I did NOT fix it, and I looked at it
before agreeing: the exit code is correct in every case, the defect is confined
to the DETAIL string, and the reader is told to re-render either way. Fixing it
would mean replacing a set comparison with a positional one inside a function
this round already has three witnesses pointed at, for no change in any exit
code. The orchestrator's tracked-low disposition is right and I have nothing to
add against it.

### F-B1: not mine, and I did not touch it

`scripts/m2-exit-test.sh` is not on this phase's declaration and an
orchestrator-side round is in flight on `claude/exit-test-harness-assertion-direction`.
I ran the harness (the PR bundle above) and edited nothing in it. The one place
our work meets is `test/behaviors.json`, which is append-only and resolved as a
union against the merge base.

### The suite on all four axes, measured rather than reasoned about

A bare number starts an investigation here, so all four axes are quoted
together. Every row was run at this round's head with the tree verified clean
before and after.

| toolchain | dist | invocation | tests | pass | SKIPPED |
|---|---|---|---|---|---|
| node v26.6.0 (scratch prefix) | BUILT | `npm test` | 617 | 617 | **0** |
| node v26.6.0 (scratch prefix) | BUILT | bare `node --test` from the repository root | **619** | 619 | 0 |
| node v22.22.2 (default, `bash -lc`) | BUILT | `npm test` | 617 | 615 | **2** |
| node v26.6.0 (scratch prefix) | **ABSENT** | `npm test` | 617 | 608 | **9** |

Every difference decomposes exactly as CLAUDE.md records and none of them is
unexplained. 617 is 614 at 16bab6f plus this round's three new tests. 619 is
617 plus `sandbox/test/greet.test.js`'s two, which `package.json`'s `test`
script excludes and a bare invocation from the root does not. The 2 skipped on
the default toolchain are the floor-gated `doctor` tests. The 9 skipped with
`dist/` absent are the five in `test/gates.test.ts` and four in
`test/m2-exit-test.test.ts` that need the built CLI. **617 is what CI and the
`suite` gate mean**, and the gate's own record above says `617 tests reported`.

Raw captures, in the order run:

```
=== AXIS: node v26.6.0, dist BUILT, bare 'node --test' from the repository root ===
i tests 619
i suites 0
i pass 619
i fail 0
i cancelled 0
i skipped 0
i todo 0
i duration_ms 219763.82445
EXIT=0

=== AXIS: DEFAULT toolchain (bash -lc), dist BUILT, npm test ===
node v22.22.2
1..617
# tests 617
# suites 0
# pass 615
# fail 0
# cancelled 0
# skipped 2
# todo 0
# duration_ms 229798.541218

=== AXIS: node v26.6.0, dist ABSENT, npm test ===
i tests 617
i suites 0
i pass 608
i fail 0
i cancelled 0
i skipped 9
i todo 0
i duration_ms 213149.242161
EXIT=0
=== rebuilding dist ===
BUILD_EXIT=0
TREE: 0 modified paths
```

TRANSLITERATION DECLARED for this block: Node's default reporter prints U+2139
INFORMATION SOURCE at the head of each summary line and it appears **sixteen**
times above, each rendered `i`. U+2716 HEAVY MULTIPLICATION X does not occur,
because no arm had a failure. The default-toolchain arm ran under the TAP
reporter and carries no glyphs at all, which is why its lines start `#`. Nothing
else in any captured output above was changed.

### The claim grep, both forms, and the hit only the second form saw

Run over the fix-round section alone (lines 983 onward of this file), because
the rest of the document was greped by the round-0 implementer and re-greped by
contract A.

**FORM 1, CLAUDE.md's line-based command:** 9 hits.

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' <this section>
25:| A-2 ... A WITNESS WHOSE MEMBERS ALL DISABLE THE STEP NEVER RE-EVALUATES THE ARM THAT EXECUTES IT ...
35:comparison at a mode the brief never claimed, which is why a `--mode` flag was
50:per gate, never by count. Narrowing is then the only direction the value can
74:snapshot directory, never with `git checkout --`.
82:ok 3 - ... a vacuous error and never a green over an empty table
93:not ok 3 - ... a vacuous error and never a green over an empty table
159:CONSTRUCTIBLE. It never re-ran the requirements over the weakened text, so it
186:red-witness GATE, which is what re-evaluates these arms on later heads, never
514:  document. Neither covers a scope value that never passes through an `options`
FORM1_HITS=9
```

**FORM 2, wrap-insensitive**, whitespace flattened before matching: **10 hits.**
The gap is real and it is not rounding.

```
10  needs a  ...stion contract A recorded and this round does not close it either. It
             needs an observed CI run on a `main` head, which is T-009's duty on th...
FORM2_HITS=10
```

Hit 10 is INVISIBLE to form 1 because "It" ends one line and "needs an" begins
the next. Contract A found the same one-hit gap on its own report and located
the wrapped occurrence rather than averaging it away; the same thing happened
here, which is two independent instances of the same wrap and is why form 2 is
not optional.

Every hit, settled:

| # | claim | settled by |
|---|---|---|
| 1 | "A WITNESS WHOSE MEMBERS ALL DISABLE THE STEP NEVER RE-EVALUATES THE ARM THAT EXECUTES IT" | the pre-round spec's two members, printed below: both `replace` an entire step. Not an argument, a reading of the file |
| 2 | "a mode the brief never claimed" | restates the shipped design comment at scripts/check-brief-drift.mjs:218, which is the existing source and not a new claim |
| 3 | "never by count" | describes what the new test does; test/implementer-brief.test.ts:342 iterates modes and gate ids and asserts no count |
| 4 | "never with `git checkout --`" | a statement about MY OWN method, and the captured `TREE: 0 modified paths` after each defang is its evidence |
| 5, 6 | inside a captured test NAME | not my prose. These are the reporter's verbatim echo of a test title and altering them would be fabrication |
| 7 | "It never re-ran the requirements over the weakened text" | the pre-round source, printed below |
| 8 | "never exercised it" | the pre-round spec, printed below, plus contract A having had to force the arm by hand |
| 9 | "Neither covers a scope value that never passes through an `options` object" | this is a NEGATIVE claim about my own derivation's reach and is already filed under what it did not cover, which is the correct home for it |
| 10 | "It needs an observed CI run on a `main` head" | RESTATED as an open question, and it already is one: I did not run the workflow, I say so, and I name what would close it. It is not a claim that nothing else could |

The two settling captures:

```
$ git show 16bab6f:test/implementer-brief.test.ts | sed -n '565,580p'
  const weakened = items
    .slice(0, 2)
    .every((item) => clause.replace(items[2] as string, "").includes(item));
  assert.ok(weakened, "the two-item weakening could not be constructed");
  assert.equal(
    clause.replace(items[2] as string, "").includes(items[2] as string),
    false,
    "removing item 3 left item 3 in the text, so this arm proves nothing",
  );
});

$ git show 16bab6f:witness/brief-drift-check-wired-executably.json | <print each member's replace>
member 0: replace=""
member 1: replace="      - name: Implementer brief gate-list drift (gate-registry.yaml is the single source,
members: 2
```

Member 0 deletes the step and member 1 replaces it with a step carrying an
`if:`. Neither leaves a `run:` that executes, which is the finding, read off the
file rather than argued.

### Byte checks, and the constraint that had to hold at the end as well as the start

`roles/_shared-dispatch-contract.md` had to stay untouched. Verified by BLOB
HASH at three points rather than by reading a diff summary:

```
$ git merge-base origin/main HEAD
307ed2f4e02d7a6f2b05388308dadedbaaf8e617
merge-base blob: f8784f2256cad1ac0e934bf7b628ca7519d81758
HEAD blob:       f8784f2256cad1ac0e934bf7b628ca7519d81758
worktree hash:   f8784f2256cad1ac0e934bf7b628ca7519d81758
DIFF_LINES=0
```

Three identical hashes, so the file is byte-identical at the merge base, at this
round's head, and in the working tree.

Authored bytes, STAGED FIRST because the script exits 2 without checking when
the working tree differs from the index, and a 2 reads exactly like a pass if
only the absent output is looked at:

```
$ git status --porcelain | wc -l
0
$ git add -A
$ node scripts/check-authored-bytes.mjs
AUTHORED_BYTES_EXIT=0
```
