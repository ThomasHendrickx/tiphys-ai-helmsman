# M3-P7 work history: review checklists, probe injection, and the verdict contract

- phase: M3-P7
- branch: claude/m3-p7-review-checklists
- base: origin/main at ec77c7d
- toolchain: node v26.6.0 from the scratch prefix, checked in every shell

Appended as the work happens. Started 2026-08-12.

## Status

Survey done. Implementation starting.

## The captured red-witness evidence (criterion 5)

Ran the real M2-P2 harness rather than authoring an example of its output:

    node src/gates/red-witness.ts --result <scratch>/result.json \
      --evidence <scratch> --base ce819fd --head ec77c7d
    exit 0
    red-witness: green (19 witness(es) evaluated (5 own, 14 stored
    re-evaluated in 150432ms); every witness red against every declared
    dangerous state and green at head)

It wrote witness-records.json, 8519903 bytes. One evaluation
(`modes-type-registered`) was extracted verbatim into
test/fixtures/red-witness-evidence.json; the extraction and the four pin
objects removed are declared field by field in
test/fixtures/red-witness-evidence.txt:36. Nothing in the kept subtree was
edited.
