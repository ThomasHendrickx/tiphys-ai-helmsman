# M4-P8 work history: the audited credential route for an agent payload

Branch `claude/m4-p8-payload-credentials`, cut from `main` at 9d0a6b8. Plan
section: delivery/plan/kernel-plan-m4.md:1396.

**TRANSLITERATION DECLARATION, read this before any capture below.** Node's
test reporter prints three non-ASCII glyphs at the head of its lines, and this
repository's authored bytes must be pure ASCII. Every captured `node --test`
block below is REAL OUTPUT with exactly three codepoints replaced, and nothing
else in any capture is altered:

| codepoint | glyph name | rendered here as | occurrences replaced |
|---|---|---|---|
| U+2716 | heavy multiplication x | `x` at the start of a line | 7 |
| U+2139 | information source | `i` at the start of a line | 10 |
| U+2714 | heavy check mark | not quoted anywhere below | 0 |

Every replacement is a line-leading reporter glyph and every one of them is
counted above; the counts are the output of `grep -c` on this file. Captures
that are already pure ASCII (the step-1 probe, the gate bundle, the shell
transcripts) are verbatim with nothing replaced.

## What this phase is, and the one thing it deliberately does not do

The hazard the plan names is **a credential reaches a project payload and no
artifact says so**. The answer is not a wider allowlist, and this phase builds
no part of one: `DEFAULT_CHILD_ENV_ALLOWLIST` gains no name and is
byte-identical to its merge-base form. What is built is the ROUTE around it:
who may ask for the escape hatch, what a per-invocation widening costs, what a
widening leaves behind for a reader, and how a widening applied AFTER the
kernel handed the environment over is detected.

### Files touched

| File | What |
|---|---|
| src/spawn.ts | `payloadClass`, `extraAllowlist`, `checkCredentialPolicy`, `compareHandover`, the launched-name-set report, the meta record |
| src/exec/env.ts | the extension entry type, `refuseExtraAllowlist`, the refusal wired into `buildChildEnv` |
| src/task.ts | `PayloadClass` and the `TaskCredentialRecord` shape on `TaskMeta` |
| src/gates/credentials.ts | ONE COMMENT, recording that the kernel now imports the vocabulary. No gate decides anything differently |
| scripts/credential-witness.mjs | created: the payload that probes from inside the child |
| test/payload-credentials.test.ts | created: eight tests |
| test/behaviors.json | eight rows |
| src/commands/spawn.ts | **DEVIATION 1**, one field at the CLI call site |
| test/spawn.test.ts | **DEVIATION 2**, one key and two call sites |
| test/credentials-gate.test.ts | **DEVIATION 3**, one field and its local type |
| delivery/plan/phase-declarations/m4-p8.json | three `filesToTouch` entries added at head |

## Step 1: the probe, and what it measured

The plan marks this phase PROTOTYPE-BLOCKED on one question only, and
criterion 7 is the only criterion that depends on it. **The probe ran.** A real
agent payload was launched THROUGH the subprocess adapter, by `tiphys spawn`,
under the default scrub, which is the part the earlier M4 probe did not do (it
called `buildChildEnv` directly and ran arms with `env -i`, recorded at
delivery/evidence/m4-probes/credential-scrub.md:310).

Fleet, upstream and clone staged under
`/tmp/claude-0/.../w8-m4-p8/probe`; payload
`agent-probe.sh`; interpreter `/tmp/claude-0/n26/bin/node` (v26.6.0).

```
$ cd $PROBE/fleet && node $REPO/bin/tiphys.ts spawn --task probe-agent \
    --project $PROBE/fleet/projects/demo --brief $PROBE/brief.md \
    --shape ship --exec "$PROBE/agent-probe.sh $PROBE/inside-scrubbed.txt"
spawn_exit=0
spawned probe-agent worktree .../probe/fleet/worktrees/probe-agent exec exited 0
```

The child wrote this, verbatim:

```
=== arm: environment names visible to the child ===
GH_CONFIG_DIR GIT_CONFIG_GLOBAL GIT_CONFIG_NOSYSTEM GIT_CONFIG_SYSTEM HOME PATH PWD SHLVL XDG_CONFIG_HOME _
count=10
HOME=.../probe/fleet/tasks/probe-agent/scrub-env/home
cwd=.../probe/fleet/worktrees/probe-agent
=== arm: model authentication (claude -p, nonce Q77213) ===
claude_exit=0
claude_stdout=[Q77213 ]
claude_stderr_head=[]
=== arm: github REST reachability (curl) ===
curl_api_github_user_http=403
=== arm: git push to the task upstream ===
git_push_exit=0
git_push_out=[To .../probe/upstream  * [new branch]      HEAD -> task/probe-agent ]
=== arm: gh presence ===
gh=absent
```

The same payload, same worktree, with the FULL parent environment as the
control arm:

```
count=174
HOME=/root
claude_exit=0
claude_stdout=[Q77213 ]
curl_api_github_user_http=200
git_push_exit=0
```

**So a scrubbed agent payload authenticates to the model, and the one thing it
loses is HTTPS egress to the GitHub API.** That reproduces, through the spawn
route this time, the reversal recorded at
delivery/verification/m4-prototype-probes.md:33.

### The name list criterion 7 asks for, derived rather than guessed

Four arms were then run through `spawnTask` itself, differing only in
`extraAllowlist`, each launching the same curl-and-model payload:

| arm | extension | child env names | `curl -o /dev/null -w %{http_code} https://api.github.com/user` | model nonce |
|---|---|---|---|---|
| A | none | 10 | **403** | echoed, exit 0 |
| B | `HTTPS_PROXY` | 11 | **200** | echoed, exit 0 |
| C | `HTTPS_PROXY`, `CURL_CA_BUNDLE` | 12 | 200 | echoed, exit 0 |
| D | `GH_TOKEN` | REFUSED, nothing created | n/a | n/a |

Arm D's refusal, verbatim from the driver:

```
--- arm-d-ghtoken: ok=false reason=the allowlist extension entry GH_TOKEN is a
documented gh token variable and may never cross into a child environment; the
default allowlist gains no credential name and neither may an extension;
nothing was created
$ ls -d $PROBE/fleet/tasks/arm-d-ghtoken $PROBE/fleet/worktrees/arm-d-ghtoken
ls: cannot access '.../fleet/tasks/arm-d-ghtoken': No such file or directory
ls: cannot access '.../fleet/worktrees/arm-d-ghtoken': No such file or directory
```

**THE NAME LIST IS ONE NAME LONG, and arm C is why it is one and not two.**

| name | reason | evidence |
|---|---|---|
| `HTTPS_PROXY` | egress to the GitHub REST endpoint goes through the agent proxy; without it the child gets HTTP 403 | arm A 403 against arm B 200, one variable changed |

`CURL_CA_BUNDLE` is NOT on the list: arm C adds it and the result is the same
200 arm B already had, so it buys nothing measurable here and a name that buys
nothing is a widening with no reason. Model authentication needs NO name: arm A
has ten variables and the nonce came back.

**The count in `meta.json` equals the count above.** Arm B's record, read off
disk:

```
  "credentials": {
    "payloadClass": "project",
    "scrubMode": "scrubbed",
    "extensions": [
      {
        "name": "HTTPS_PROXY",
        "reason": "egress to the model and GitHub endpoints goes through the agent proxy"
      }
    ],
    "handover": { "status": "compared", "added": [], "removed": [] }
  }
```

One extension in the file, one row in the table.

**What this name list is NOT.** It is a measurement of THIS container on
2026-09-17, where egress is proxied and `GH_TOKEN` is inert (the proxy
substitutes credentials on the way out, which CLAUDE.md's standing warning 6
records from a different direction). A different runner may need a different
list, and nothing here should be read as the kernel declaring a default: the
default allowlist gains no name and `extraAllowlist` stays a per-invocation
argument the caller supplies with a reason.

## The decisions that are invisible in the diff

### Step 4's question: import the vocabulary, do not move it

The plan offers two shapes and requires the choice be recorded. **The
vocabulary stays in src/gates/credentials.ts and `src/exec/env.ts` imports
it.** `GH_TOKEN_VARIABLES`, `DANGEROUS_ENV_VOCABULARY` and
`isDangerousEnvName` were already exported, so the gate module's only change is
a comment saying who else reads them now. A copy was never an option: two lists
drift, and they drift toward the permissive side.

That import makes the two modules a cycle. Before choosing it, the existing
graph was measured rather than assumed:

```
$ node -e '<walk every relative import under src/, report cycles>'
cycles: 13
cli.ts -> commands/doctor.ts -> cli.ts
...
spawn.ts -> adapters/load.ts -> spawn.ts
```

Thirteen cycles already, one of them inside the spawn path itself. So this is
the repository's existing shape rather than a new hazard class. The rule the
cycle DOES impose is real and is now mechanical rather than remembered: nothing
in `src/exec/env.ts` may read an imported binding at module-evaluation time.
The eighth test imports the two modules in both orders, in a fresh child per
order, and the mutation that breaks the rule is captured below.

### `meta.json` has no schema, and this phase does not give it one

The plan asks this be verified in step 1 rather than assumed. Measured:

```
$ ls schemas/ | wc -l
17
$ grep -rln 'meta.json\|task-meta\|taskMeta' schemas/ ; echo "exit=$?"
exit=1
```

`schemas/executor-record.schema.json` exists and covers `executor.json`, a
different document. `meta.json` is validated by nothing, and the plan says in
terms not to create a schema in this phase. Recorded, not fixed.

**That absence had one design consequence.** The adapter's report of what it
launched with is carried on the `LaunchOutcome` return value and NOT on
`ExecutorRecord`, because `executor.json` has a shipped schema with
schemas/executor-record.schema.json:7 set to `false` for additional
properties, and that schema is not this phase's to edit.

### The step-5 refusal cannot appear in `meta.json`, and the record says so

Step 6 asks that the refusal be recorded in `meta.json` "if one fired". Step 5
asks that the escape-hatch refusal happen "before the worktree is touched".
Those two cannot both be satisfied for the SAME refusal: a spawn refused before
pool create has no task directory, so there is no `meta.json` to write into,
and creating one would undo the property that a refused spawn creates nothing.

The resolution, stated in the type rather than left implicit: the recorded
`refusal` field is for refusals that fire AFTER the record exists, which is the
criterion-6 handover refusal. The step-5 refusal's artifact is its reason line
plus the measurable absence of a task directory and a worktree, which is what
the first test asserts.

### `handover` is absent until the launch has reported

`meta.json` is written BEFORE the payload starts, which is what makes a failure
to write it safe to roll back. At that moment no adapter has launched anything,
so `handover` is optional and its absence is a fact rather than an omission,
the same reason `ExecutorRecord` carries no resolved model.

## Acceptance criteria, walked

### Criterion 1: the test file runs

Interpreter `/tmp/claude-0/n26/bin/node` v26.6.0, `dist/` PRESENT (built by
`npm run build` immediately before), invoked as
`node --test test/payload-credentials.test.ts` from the repository root:

```
i tests 8
i suites 0
i pass 8
i fail 0
i cancelled 0
i skipped 0
i todo 0
```

Exit 0, eight tests, none skipped.

### Criterion 2: the escape hatch and the payload class

Guarded by "spawnTask refuses the credential escape hatch on a project payload,
naming both fields, and accepts it on an orchestrator payload". The project arm
asserts the reason names both fields, that no worktree and no task directory
exist, and that the payload NEVER RAN (the probe file the payload would have
written is absent). The orchestrator arm asserts the same call succeeds, that
the record says `inherited`, and that its child DOES receive the parent's
`GH_TOKEN` value, so the refusal is demonstrably about the pairing rather than
about the hatch being unusable.

RED WITNESS, and it is the one the plan specifies. Against the merge base the
first call SUCCEEDS and the child-written probe names the inherited token:

```
$ node lab-base/base-hatch-probe.mjs
init: 0
spawnTask ok = true
probe file exists = true
child GH_TOKEN = "ghp_parent_escape_hatch_canary"
child env name count = 174
child environment probe = resolvable | pull-request-capable token variable(s) present in the child environment: GH_TOKEN
child verdict = red
meta.credentials = undefined
```

At head the same call is refused and the probe file does not exist.

### Criterion 3: two structurally different members

Guarded by "buildChildEnv refuses an allowlist extension naming a gh token
variable and one naming a code-execution variable, each reason naming the
entry". `GH_TOKEN` is refused by the gh-token half of the vocabulary and
`NODE_OPTIONS` by the code-execution half. These are the two members the plan
names, and they are not two credentials.

RED WITNESS, against the merge base, in the BARE-STRING shape. That shape is
load-bearing: the old field was `readonly string[]`, so an object entry reaching
the old code is used as a key, coerces to `[object Object]` and matches
nothing. A witness written only in the object shape would be red against the
old SIGNATURE rather than the old BEHAVIOUR, so the test runs the string arm
first:

```
x buildChildEnv refuses an allowlist extension naming a gh token variable and one naming a code-execution variable, each reason naming the entry
  AssertionError [ERR_ASSERTION]: GH_TOKEN crossed into the child environment through the bare-string extension
  + actual - expected
  + 'ghp_extension_canary'
  - undefined
```

Both members, measured directly against the merge base so neither rests on the
other:

```
$ node --input-type=module -e '<buildChildEnv with each member as a bare extension>'
GH_TOKEN: ok=true value_in_child="ghp_extension_canary"
NODE_OPTIONS: ok=true value_in_child="--require /tmp/evil.js"
```

### Criterion 4: a reason is data

Guarded by "an allowlist extension with a blank reason is refused naming the
entry, and the same entry with a reason reaches meta.json verbatim". Three
blank forms are refused (`""`, spaces, a tab) and the same entry with a real
reason is accepted, with `assert.deepEqual` on the recorded extensions so a
paraphrase would fail.

RED WITNESS, merge base:

```
x an allowlist extension with a blank reason is refused naming the entry, and the same entry with a reason reaches meta.json verbatim
  AssertionError [ERR_ASSERTION]: a reason of "" was accepted
```

### Criterion 5: the assertion is on a file the child wrote

**This is the criterion that distinguishes the phase from the shipped gate, and
its mutant is the interesting one.** The mutant widens a COPY of the
environment inside `subprocessAdapter` AFTER the kernel handed it over, and
reports the kernel's own name set. Every kernel-side check therefore sees
nothing: `buildChildEnv` returned the same object, and the handover comparison
finds no difference.

Mutation applied to the shipped adapter in an isolated worktree (never
committed; the full mutant is quoted at the end of this section):

```
x the child-written credential probe reddens against an adapter that widens the environment after the kernel handed it over
  AssertionError [ERR_ASSERTION]: a token reached the child under the built-in adapter
  true !== false
i tests 8
i pass 7
i fail 1
```

**Seven of eight stayed green under that mutant, which is the point.** The
criterion-3 and criterion-4 witnesses, which read `buildChildEnv`'s return
value, are green. The criterion-6 handover comparison is green. Only the
child-written file goes red.

And the SHIPPED GATE stays green under the same mutant, run in the same
worktree:

```
$ node src/gates/credentials.ts credential-scrub --result ev/result.json --evidence ev
gate exit=0
status= green units= 7
```

The mutant, so a reviewer can reproduce it:

```
    const mutantEnv =
      request.env === undefined
        ? undefined
        : { ...request.env, GH_TOKEN: "ghp_mutant_adapter_widened" };
    const result = spawnSync(program, args, {
      cwd: request.worktree,
      stdio: "inherit",
      ...(mutantEnv === undefined ? {} : { env: mutantEnv }),
    });
```

### Criterion 6: the handover is compared by name set

Guarded by "a spawn refuses when the adapter reports launching with a different
environment name set than the kernel handed it". TWO structurally different
members of "the sets differ": a name the kernel never handed over (a widening,
`added`) and a name it handed over that the adapter dropped (a narrowing,
`removed`). Both refuse, both record the difference in `meta.json`, and both
name the offending adapter and variable in the reason.

Three further states are asserted so the green ones are not a silence: an
honest adapter records `compared` with empty lists, an adapter that reports
nothing records `unreported` rather than a comparison nobody made, and a spawn
under the escape hatch records `not-applicable` because the kernel handed over
no environment to differ from.

RED WITNESS, merge base (there is no comparison at all there, so the widening
adapter's spawn succeeds):

```
x a spawn refuses when the adapter reports launching with a different environment name set than the kernel handed it
  AssertionError [ERR_ASSERTION]: expected a refusal and got a success
```

The comparison function has its own test and its own two mutants, applied one
at a time in the isolated worktree:

Mutant A, `unreported` recorded as `compared`:

```
x compareHandover distinguishes a compared handover from an unreported one and from one there was never anything to compare
  AssertionError [ERR_ASSERTION]: Expected values to be strictly deep-equal:
  + actual - expected
    {
      added: [],
      removed: [],
  +   status: 'compared'
  -   status: 'unreported'
    }
```

Mutant B, position-by-position comparison instead of set comparison:

```
x compareHandover distinguishes a compared handover from an unreported one and from one there was never anything to compare
  AssertionError [ERR_ASSERTION]: Expected values to be strictly deep-equal:
  + actual - expected
    {
  +   added: [ 'HOME', 'PATH' ],
  +   removed: [ 'HOME', 'PATH' ],
  -   added: [],
  -   removed: [],
```

### Criterion 7: the name list, with the captured output that established it

Discharged above, not struck: the probe ran, its output is quoted verbatim, the
list is one name long with its reason, and `meta.json`'s extension count for
the arm that used it is 1.

### The cycle guard, which is not a plan criterion but is this phase's own hazard

Guarded by "the refused-extension vocabulary resolves whichever of the kernel
and the gate module is imported first". Mutation: read the imported constant at
module-evaluation time in `src/exec/env.ts`, which is the one thing the cycle
forbids.

```
x the refused-extension vocabulary resolves whichever of the kernel and the gate module is imported first
  AssertionError [ERR_ASSERTION]: importing ../src/gates/credentials.ts first failed:
  const MUTANT_TDZ_PROBE = GH_TOKEN_VARIABLES.length;
                           ^
  ReferenceError: Cannot access 'GH_TOKEN_VARIABLES' before initialization
```

Note WHICH order failed: importing the gate module first. The other order
passed under the same mutant, which is exactly the asymmetry that makes an
import cycle bite in production and not in the test that happened to be written
first.

## Scope: three declared deviations, none silent

All three are FORCED by the plan's own steps, and all three were added to
`filesToTouch` at head, which the scope gate prints by name for a reviewer to
sign off (src/gates/scope.ts:110 is the both-sides read that makes a head
amendment legal at all). None of them is a file the concurrent phase M4-P18
touches.

**DEVIATION 1, src/commands/spawn.ts.** Step 2 requires `payloadClass` to be
REQUIRED with no default. A required field on `SpawnOptions` makes every call
site a compile error until it passes one, and the only production call site is
the CLI. One line added: `payloadClass: "project"`. The CLI has no flag for the
other class and no flag for `allowPrCredentials`, so an operator cannot reach
the pairing step 5 refuses.

**DEVIATION 2, test/spawn.test.ts.** Step 6 puts a new key in `meta.json`, and
that test pins the meta key set with `assert.deepEqual(Object.keys(meta).sort(),
[...])`. It is the shape CLAUDE.md's rule 5 describes one level over: a test
that over-asserts on a document another phase extends. One key added to the
list, plus `payloadClass` at two `spawnTask` call sites in that file.

**DEVIATION 3, test/credentials-gate.test.ts.** That file drives `spawnTask`
with `allowPrCredentials: true` through the library seam. With the runtime
payload-class check it must now declare `orchestrator`, which is the correct
class for that test, and the assertion that test makes, that the parent
environment crosses unchanged under the hatch, is unaffected by declaring it.
One field and one line in its local type.

## Gate evidence

Bundle run at head, after every edit was committed, on node v26.6.0:

Bundle run at head c38fcb2, after every edit was committed, on node v26.6.0.
Run id 3ea1f1b9bc06c355fc6a11c0. **The bundle exits 20 and every gate that ran
is green**, and both halves of that sentence are stated because the first reads
like the opposite of the second. The five lines the brief asks for, verbatim,
with only the absolute scratch path shortened to `<clone>`:

```
gates: declared 16 applicable 13 verdict 13 green 13 red 0 not-applicable 3 error 0 vacuous 0
gates: scope: green: 12 changed path(s) audited against declaration delivery/plan/phase-declarations/m4-p8.json at merge base 9d0a6b84d5b8e5d40cff7a5138a7b05dff749d68 (sha256 28b878001382dbfc21b13f0359804688447f52a7ad05cb0dc7b321161ca6e6c5) DECLARATION AMENDED AT HEAD: 3 entry/entries ADDED at head c38fcb2dcfefd4c2d0a015d074d50716282b5e94 that are absent from the merge-base declaration, allowed and NAMED here for a reviewer to sign off (this gate does not sign them off): filesToTouch src/commands/spawn.ts, filesToTouch test/credentials-gate.test.ts, filesToTouch test/spawn.test.ts.
gates: suite: green: suite green via tiphys-suite-events-v1 (child node v26.6.0): reported 1191 test(s) from 58 file(s) (pass 1189, fail 0, skipped 2, todo 0, did-not-run 0); discovered 58 file(s) walking test for .test.ts; 1075 behavior(s) resolve; merge base 9d0a6b84d5b8
gates: typecheck: green: tsc -b tsconfig.src.json tsconfig.test.json --force --listFiles exited 0 and reported 420 distinct file(s); the unit count is those printed paths, not a constant
gates: gate-classes: green: phase M4-P8 declares all 3 required gate class(es) in delivery/plan/phase-declarations/m4-p8.json: correctness: asserted by suite, typecheck, red-witness; scope: asserted by scope; review: asserted by check-dual-review
gates: red-witness: green: 15 witness(es) evaluated (0 own, 15 stored re-evaluated in 144284ms); every witness red against every declared dangerous state and green at head
```

**WHY THE EXIT IS 20, stated rather than left for a reader to rediscover.** The
last line of the run is:

```
gates: required gate(s) not applicable: citations
```

`citations` is REQUIRED and reports:

```
gates: citations: not-applicable: no changed path under the configured documents globs (12 changed path(s) total).
```

The configured document set is six globs over `.md` files under
`delivery/plan/`, `delivery/verification/`, `delivery/decisions/`,
`delivery/tuition/`, `delivery/requirements/` and `delivery/STATE.md`. This
phase changes one file under `delivery/plan/`, its phase declaration, and that
is a `.json`. **`delivery/work-history/` is not a configured document tree**, so
a work history cannot make this gate applicable however many citations it
carries, and nothing on this phase's files-to-touch list can either.

This is not new and it is not this phase's doing. Three of the last eight
merges have the same shape, measured rather than asserted:

```
$ for c in $(git log --oneline -8 --merges --format=%H); do
    echo "--- $(git log -1 --format=%s $c | cut -c1-56)"
    git diff --name-only ${c}^1 ${c} \
      | grep -E '^delivery/(plan|verification|decisions|tuition|requirements)/.*\.md$|^delivery/STATE.md$' | head -3
  done
--- Merge pull request #84: M3-P4 fix round 2 delta verification
--- Merge pull request #82: M3-P4 dual clean-room review and rou
--- Merge pull request #78: M3-P3 round-10 arbitration and the m
```

Three merged pull requests whose diff contains no configured document, so
`citations` was not-applicable on each. The other two not-applicables in this
run are `credential-token` (owner action A-3, no implementer token) and
`check-dual-review` (no verdict documents on the branch, which is the
orchestrator's to produce).

The full run, including the eight gates not quoted above, is at
`<scratch>/w8-m4-p8/gates2.txt` and every one of them is green.

**RE-RUN AT THE PUSHED HEAD.** The capture above is from head c38fcb2, this
work history's first commit. The bundle was run again after the last edit to
this file: every count above was identical (13 applicable, 13 green, 12 changed
paths, 1191 tests with 1189 passing and 2 skipped, 420 type-checked files, 15
witnesses) and the only difference was the head sha the scope line echoes. That
sha is reported with the phase rather than written here: a sha written into the
file it commits is not its own.

**The two skipped tests are not this phase's.** They are the M4-P20 cross
environment exclusion witnesses, which carry an explicit skip reason saying
`src/exclusion.ts` is absent until M4-P21. They skip identically at the merge
base.

## What this phase did NOT cover

A reviewer's first check, and this section is written to be checked rather than
believed.

1. **The boundary is still environment-only, and the environment is not the
   boundary.** The M4 probes measured that the real credential boundary here is
   the uid and the filesystem: a scrubbed child runs at the same uid and keeps
   READ access to an absolute credential path outside `$HOME`. Nothing in this
   phase changes that, and the step-1 probe reproduces it: the scrubbed child
   authenticated to the model with ten variables. **No evidence in this phase
   should be read as saying a project payload cannot reach a credential.** It
   says the ENVIRONMENT route is audited.
2. **The `credential-scrub` gate's inverted verdict is NOT fixed here.** Probe
   item 2 recorded that the gate greens `HTTPS_PROXY`, which grants real GitHub
   reach, and reddens `GIT_CONFIG_*`, which grants only URL rewriting. This
   phase measured the same thing from the other side (arm B took `HTTPS_PROXY`
   through the audited route and got HTTP 200, and the gate stayed green), and
   deliberately did not change it: the brief makes `src/gates/credentials.ts`
   edit-to-export-only, and a new gate arm would be an escalation rather than a
   quiet registry edit. Recorded as an open item for whoever owns that defect.
3. **A dishonest adapter is not caught by the kernel.** Criterion 6 compares
   what the adapter REPORTS. An adapter that widens the environment and reports
   the kernel's set passes that comparison, by construction, and the
   criterion-5 test carries exactly such an adapter to prove it. The
   child-written probe is what settles it, and that probe is a TEST payload, not
   something the kernel runs on every spawn. A kernel that verified every launch
   from inside the child is a different design and is not in this plan section.
4. **No witness spec was added under `witness/`.** That directory is not on this
   phase's files-to-touch list. The `red-witness` gate is green and selected 15
   stored specs against this diff, so the coverage rule is satisfied by existing
   specs; the red witnesses for the new behaviour are the captures above, each
   reproducible from an isolated worktree.
5. **`meta.json` is still unvalidated.** The new `credentials` record has a
   TypeScript shape and no schema, so a hand-edited or foreign-written
   `meta.json` can carry any credentials block it likes and `readTaskMeta` will
   accept it. `readTaskMeta` deliberately does not require the field, so an
   older record still reads; the cost is that a consumer cannot rely on its
   presence. Giving the document a schema is explicitly out of scope per the
   plan.
6. **The name list is one container, one day.** See the caveat under step 1.
   Nothing was measured on a second runner, and the probe could not distinguish
   "this list is the general answer" from "this list is what this proxy
   requires".
7. **`extraAllowlist` has no CLI surface.** It is reachable only from the
   library seam, which is where the plugin sits. That is deliberate for this
   phase (the CLI would need a flag whose reason argument an operator would
   type, and a typed reason is not an audited one), and it means the arms in
   step 1 were driven through `spawnTask` directly rather than through
   `tiphys spawn`.
8. **The bare-string extension form still accepts an entry with no reason.** It
   is refused for a dangerous NAME exactly as the object form is, so the safety
   half is whole, but a bare string carries no reason and nothing demands one of
   it. It survives because the kernel's own tests model a widened allowlist with
   that shape and that file is not this phase's to edit. The audited route
   cannot produce one: `SpawnOptions.extraAllowlist` is typed to the object form
   and a string does not typecheck there.
9. **Not measured on macOS or on the Node floor's lower bound.** Everything
   above is node v26.6.0 on Linux in this container.

## Claim grep

Both binding forms were run against this document. **This section quotes the
two commands, and each command text contains all eleven phrases, so a
whole-file run counts them too.** That is the reason the numbers below are
given twice, once for the document's own prose and once for the whole file: a
section that grades itself will otherwise report a number that grows every time
it is edited, and a reader who re-runs the command will get a different answer
from the one written down.

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m4-p8.md
$ tr '\n' ' ' < delivery/work-history/m4-p8.md \
  | grep -oEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to'
```

**In the document's prose, that is everything above this heading, there are
SEVEN occurrences on SEVEN lines, and both forms agree on seven, so no hit is
hidden by a wrap.** All seven are the single word `never`, which is the phrase
family CLAUDE.md records as effectively immune to the wrap problem; none of the
multi-word phrases occurs at all.

| line | text | settled by |
|---|---|---|
| 124 | "may never cross into a child environment" | captured program output, not this document's claim, and arm D's `ls` immediately below it shows that the refusal created no task directory and no worktree |
| 178 | "A copy was never an option" | a statement about this phase's CHOICE, not about what is possible. The rejected alternative and its cost are written down in the same paragraph, and the cycle it avoids is measured there |
| 266 | "the payload NEVER RAN" | the assertion is `existsSync(probeFile) === false`, and the merge-base capture in the same subsection shows that same file PRESENT there, carrying the parent's token |
| 345 | "in an isolated worktree (never committed" | `git diff --name-only origin/main...HEAD` lists twelve paths and no mutation; the mutants were applied in detached worktrees under the scratch directory and reverted with `git checkout --` in that worktree |
| 389 | "a name the kernel never handed over" | descriptive of the test's own member; the capture below it exercises exactly that member and shows the refusal |
| 414, 428 | "there was never anything to compare" | verbatim test titles inside captured reporter output |

**Whole-file, including the two commands quoted above and the table's own
quotations, there are 37 occurrences on 17 lines**, of which 30 are inside this
section: 22 from the eleven phrases in each of the two quoted commands, 7 from
the table quoting the seven prose hits back, and 1 from the paragraph above the
table naming the word those seven share. 30 + 7 = 37, and the seven prose hits
are the only ones that are claims rather than quotations of a command, a
capture, or each other.

One claim a reader should push on that neither grep catches, because it uses
none of the listed words: "an operator cannot reach the pairing step 5 refuses"
in DEVIATION 1. That is bounded by the CLI surface, which is enumerated in that
command's own `USAGE` string and carries no flag for either field. It is a claim
about `tiphys spawn` only. On the library seam the pairing IS reachable, which
is the whole reason `checkCredentialPolicy` exists.
