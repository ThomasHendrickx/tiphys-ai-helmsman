# Work history: M2-P2 red-witness harness

- phase: M2-P2
- branch: claude/m2-p2-red-witness-harness
- base: origin/main at 4c9bfbc (M2-P1 merged at 8718852, phase declarations at 4c9bfbc)
- started: 2026-08-06
- status: IN PROGRESS (this file is appended incrementally; its mtime is the
  dispatch beacon per T-008)

## Step 0: mechanism lookup (T-005 interim obligation, recorded before code)

MECHANISMS.md read in full at 4c9bfbc. Rows bearing on this phase, and what
each requires here:

1. "Asserting a CI step is wired" (CR-640, CR-661, CR-681): assert BEHAVIOUR,
   not text; a witness for a class must redden under at least two structurally
   different members. This is rule (g) of plan step 4 and criterion 3b. The
   harness must detect text-asserting witnesses by derivation from the test
   sources and require a member that preserves every asserted string while
   inverting the behaviour.
2. "Classifying another program's errors" (V-2, T-003 lesson 4, warning 10):
   derive signatures from REAL captured output. This is rule (f) and criterion
   5a: the capture obligation is derived from a spawn-grep over the changed
   files, and the fixture capture for 5a is the real git contention stderr
   recorded in delivery/review/verification-m1-p3-fix-round.md (evidence line
   68: `error: cannot lock ref 'refs/remotes/origin/main': is at a0e80f0 but
   expected a0d1254`; shape established at lines 59, 64-65 across 312 captured
   contention failures, every one of that shape).
3. "Worktree removal and force branch delete" (V-1, V-3): quoting the
   mechanism row's own rule text, destructive authority is "never inherited
   implicitly" (T-003 structural consequence 4). This is the derived-class
   rule (e):
   a witness whose named tests invoke a command on the manifest's
   `destructiveCommands` list must be class `destructive`, derived from the
   test sources rather than trusted from the declaration.
4. "Reading a path whose type is not established" (CR-520): M2-C-6. Every
   read of a path this harness did not create goes through the DELIVERED
   `classifyEntry` / `refuseOpenForWrite` / `readRegularFileIfPresent` in
   src/task.ts. No second implementation.
5. "Parsing another program's reporter output" (M1-P6 floor defect): the
   harness runs named tests in a child and must parse the result. The
   reporter is PINNED as a controlled input: `--test-reporter tap` is passed
   explicitly to the child, and a stream that does not open with
   `TAP version` is an error naming expected and observed rather than a
   widened parse (witnessed both directions: the named test of the
   `witness-tap-reporter-pin` spec, red 2/2 under the pin-removal mutation
   in the dogfood run below, green at head).
6. "Atomic file replacement" (M1-P5 round 4): no fixed stage names. The
   harness writes records with unique names inside directories it owns; the
   3/5 determinism fixture of criterion 4 is staged as exactly this defect's
   shape (fixed `${path}.stage`, two concurrent passes, loser dies on rename
   ENOENT), per clean-room-m1-p5-round4-criteria.md mutation table row
   "Unique stage name (randomUUID) / two single passes released together
   surface one turn-end / RED 3/5 runs".
7. "Claim file (mutual exclusion by O_EXCL)": read src/lock.ts first if
   adding a claim-file user. This phase adds NO claim-file user: the evidence
   directory claim belongs to the delivered runner (run.ts), and the harness
   only ever writes into per-run scratch directories it created with mkdtemp,
   which no other pass can name.

Rows read and not applicable here: lease compare-and-swap, append-only log
(no log is read; all currency is from files this run wrote), "A guard's own
failure path" (noted for design: the harness's success path is total; refusal
reasons are values, not crashes).

## Step 1: M2-C-1 verification of delivered M2-P1 contracts (before any code)

Verified at 4c9bfbc, files read in full:

1. `src/gates/result.ts`: `GateResult` fields gate, status (green | red |
   not-applicable | error), units, unitLabel, startedAt, endedAt, vacuous?,
   precondition?, pin?, detail, evidence[]. Sole constructor `makeGateResult`
   applies the M2-C-2 rewrite. Exit codes EXIT_GREEN 0, EXIT_RED 1,
   EXIT_NOT_APPLICABLE 20, EXIT_GATE_ERROR 21. Matches the plan.
2. `src/gates/pin.ts`: `takePin(roots)` returns {roots, takenAt, fileCount,
   files: [{path, sha256, size, mtimeMs, ctimeMs}]}. FIVE fields per file,
   not the plan's four: `ctimeMs` was added by CR-809 in M2-P1's fix round,
   documented in the module header as a strict addition, with the explicit
   instruction "M2-P2 criterion 7 and M2-P3 criterion 8 are pin witnesses and
   should be read against five fields, not four". `comparePins` reports any
   difference in any field including added/removed paths. `takePin` THROWS on
   an empty root, a missing root, and any entry that is not a regular file or
   real directory (fail closed, M2-C-6 by reuse).
   CONSEQUENCE RECORDED (divergence handled, see Decisions D-P2-1): ctime is
   not settable from userspace, so a mutate-then-restore cycle inside one
   clone does not produce equal pins across the restore. Executed rather
   than asserted (T-006): a byte-identical mutate-restore of a probe file
   with `utimesSync` restoring the original times still left
   `ctimeMs equal after utimes restore: false` (delta 3.17ms), measured
   2026-08-06 with a 9-line node script; this is exactly the property CR-809
   added ctimeMs to the pin FOR (src/gates/pin.ts header). The plan's step 3
   ordering (start pin, apply member, run red, restore head, run green, end
   pin) is therefore unsatisfiable as literally ordered under the delivered
   five-field pin. Resolution in D-P2-1 below.
3. `src/gates/run.ts`: gate subprocess contract confirmed: invocation is
   `<command...> --result <abs path> --evidence <abs dir>` plus one
   `--<name> <value>` per parameter the gate DECLARES in the manifest
   (`parameters` field, values base | head | phase); a `diff-touches`
   precondition adds `base` to the required set; exit codes as above; a gate
   exiting nonzero without a record is `error`; `counts.verdict` (green+red)
   is the anti-vacuity count; the runner re-applies the M2-C-2 rewrite and
   the pin refusal on ingest; evidence directory is claimed via O_EXCL
   (`.tiphys-gate-run.json`), stdout `gates: run <id>` attribution per
   STATE.md CR-902 carry-forward.
4. `src/gates/manifest.ts` + `gates.manifest.json`: manifest fields version,
   gates[] (id, command argv, unitLabel, applicability required|conditional,
   parameters?, precondition {id, kind in file-exists | file-absent |
   branch-matches | diff-touches | command-exit-zero, kind-specific field},
   modes?), top-level `destructiveCommands`. Delivered list:
   ["pool destroy", "teardown", "src/pool.ts", "src/teardown.ts"] (verified
   verbatim at gates.manifest.json lines 23-28). Zero-gate manifests are
   VALID by schema design (the runner reports no applicable gate instead).
   `loadManifest(path)` returns {ok, manifest, sha256, body} and fails closed
   through `readRegularFileIfPresent`. This phase CONSUMES loadManifest for
   the destructiveCommands list rather than re-reading JSON.
5. `src/gates/validate.ts`: closed keyword set, `loadSchema(document, name)`,
   `validate(schema, instance)` returning Diagnostic {pointer, message},
   `formatDiagnostics` producing `INVALID <json-pointer> <message>` lines.
   Kind-specific required fields that the closed keyword set cannot express
   are enforced in code emitting the same diagnostic contract (the
   manifest.ts pattern, reused here for witness-spec member kinds).
6. `src/task.ts`: `classifyEntry(path)` returns kind absent | dangling |
   regular | irregular {reason} | unexaminable {reason};
   `refuseOpenForWrite(path)` returns reason | undefined;
   `readRegularFileIfPresent(path)` returns read | absent | refused. Exported
   names and signatures confirmed at src/task.ts:118, :158, :179.
7. `test/behaviors.json`: flat JSON object, behavior-name -> description
   string, 207 entries at 4c9bfbc. Resolution "behavior resolves by name"
   means key present; the suite-side name-to-test check is M2-P3's.
8. CI workflow `.github/workflows/gates.yml`: the M2 bundle step passes
   `--base ${{ github.event.pull_request.base.sha }}`
   `--head ${{ github.event.pull_request.head.sha }}` on pull_request (NOT
   github.sha, per CR-830-1). The checkout is SHALLOW (no fetch-depth), and
   the workflow's own comment records that the first diff-touches gate needs
   `fetch-depth: 0` as the coupled other half. `.github/workflows/gates.yml`
   is NOT on this phase's files-to-touch list (it is on M2-P9's declaration),
   so this phase documents the requirement and does not edit the workflow.
   See "CI depth requirement" below and the final report.

No divergence requiring escalation was found EXCEPT the pin-ordering
consequence in item 2, which is resolved by decision D-P2-1 below with the
delivered module's own guidance; recorded here rather than improvised
silently.

## Decisions taken inside this phase

- D-P2-1 (pin bracketing under the five-field pin). The delivered pin
  includes ctimeMs, which userspace cannot restore, so start/end pins taken
  AROUND a mutate-restore cycle are unequal by construction and the plan's
  literal step 3 ordering cannot yield criterion 1's "both pins recorded and
  equal". M2-C-5's stated property is "a run that cannot name what it
  executed is not evidence": the pin must certify the tree a TEST RUN
  actually ran on. Resolution: pins bracket each test execution, not the
  mutate-restore cycle. Per member, the red runs are bracketed by a start and
  end pin over the dangerous tree (which is the tree those runs execute), and
  the green head run is bracketed by its own start and end pin over the head
  tree. Each pair must be equal or the record is `error` naming the paths and
  fields. Criterion 1's "both pins recorded and equal" is discharged by both
  pairs being recorded and each pair being equal; criterion 7's in-harness
  hook rewrites a file between the green run's pins and must produce `error`
  naming the path and mtimeMs. This follows the delivered module's own
  instruction (pin.ts header: read M2-P2 criterion 7 against five fields).
- D-P2-2 (coverage semantics for step 7's "source changed with no witness
  spec covering it"). A changed file under src/ or bin/ is covered when at
  least one witness spec declares a patch or mutation member that touches it
  (mutation: the member's file field; patch: the paths named in the patch's
  diff headers). baseline-ref members cover no specific file: they assert an
  absent-feature baseline, not a file-level dangerous state. Files DELETED by
  the diff are exempt (there is nothing at head for a member to touch;
  rule (d) requires members to touch head-state files). Both choices are
  recorded here because the plan states the rule at file granularity without
  fixing the mapping; each is the deterministic reading that fails closed on
  the files that still exist. Witnessed by the tests
  `witness-coverage-red` (a changed source with no covering member is red
  naming it) and `witness-harness-green-end-to-end` (a covering member
  admits green).
- D-P2-3 (derived-class grep granularity, rule (e)). "A witness whose named
  tests invoke a command on destructiveCommands" is derived by searching the
  FULL SOURCE of every test file containing a named test for any
  destructiveCommands entry as a substring. This over-approximates (a fixture
  string elsewhere in the same file counts), which is the fail-closed
  direction: an over-derived class forces `destructive`, which only tightens
  rule (a) and does not weaken a witness. `grep -n "spec.class"
  src/witness/run.ts` (corrected fix round 1, CR-1261; the earlier "two
  places" was an undercount) returns lines 1011, 1016, 1031, 1035, 1133,
  1138: THREE distinct consultation sites, rule (a) (~1011, which the
  destructive class can only ADD a refusal to), rule (e) (~1031), and rule
  (g)'s `needsClassRules` (~1133, `spec.class === "classification"`), which
  decides whether the two-member/text-asserting rule applies. All six
  occurrences are reads (a `===` comparison or a `${}` interpolation in a
  reason string), not an assignment, so rule (e)'s over-approximation only
  tightens rule (a) and does not alter the value rule (g) reads; the
  fail-closed conclusion holds. The alternative, extracting the named test's
  body from TS source, would be a parser whose failure modes are silent
  under-derivation.
- D-P2-4 (rule (f) scope). The spawn-grep (`child_process`, `execFile`,
  `spawnSync`, `execSync`, four alternatives; corrected fix round 1, CR-1262,
  the earlier prose listed a fifth `exec(` that the shipped pattern
  `/child_process|execFile|spawnSync|execSync/` at src/witness/run.ts does not
  carry) runs over the CHANGED files of the diff; the capture obligation binds
  every witness one of whose patch or mutation members touches a changed file
  that the grep matched. A witness with no member touching a spawning module
  is not "in that state". Fix round 1 (CR-H2) extends this to shell scripts:
  a changed `*.sh` is screened by `shellSpawnsAndParses` instead of the JS
  token grep (see the fix-round-1 section).
- D-P2-5 (baseline-ref member application). Applying a baseline-ref member
  checks out the resolved baseline sha and then restores the named tests'
  FILES from head (`git checkout <head> -- <test files>`), because the
  head-authored test is the thing that must be red against the baseline
  (blueprint section 4: red on baseline, green on head). A named test that
  cannot RUN at the baseline (import crash aborts the file) does not count as
  red: the member requires each named test individually reported failing,
  which is T-003's dangerous-state discipline, not a convenience.
- D-P2-6 (repeats on kernel-corpus witnesses). The kernel's own witness
  specs declare `repeats: 2`. The plan makes repeats a spec field with
  default 5; 2 is chosen for the shipped corpus because every member costs a
  clone plus repeats+1 test runs inside CI's gate bundle, and criterion 9's
  five-minute ceiling governs the whole corpus. The 3/5 determinism fixture
  of criterion 4 uses the default 5, where the measured M1 rate lives.

## CI depth requirement (documented, not assumed; STATE CR-902 carry-forward)

The red-witness gate requires, in the repository it audits:

1. `--base` and `--head` resolvable to commits in the local object store,
   with their merge history present, because the phase diff is
   `git diff base...head` (three-dot: merge-base diff). A shallow clone
   (`fetch-depth: 1`) fails this and the gate reports `error` naming the
   missing revision rather than guessing an empty diff (M2-C-3; witnessed
   by the tests `red-witness-shallow-error` and
   `red-witness-base-absent-error`, plus defang D13 below).
2. The repository to be UNSHALLOW for witness evaluation, because the
   harness creates scratch clones of it and `git clone` from a shallow
   source is refused by git. The gate probes `git rev-parse
   --is-shallow-repository` up front and reports `error` naming
   `fetch-depth: 0` as the requirement rather than failing obscurely later.
3. On pull_request events the checkout HEAD is the synthetic merge commit.
   When `--head` is supplied it is authoritative for the diff, for
   `git show` reads and for the scratch-clone checkout; the CI invocation
   supplies it explicitly (`--head ${{ github.event.pull_request.head.sha
   }}`, .github/workflows/gates.yml), and the runner passes it to this gate
   because the manifest entry declares `parameters: ["base", "head"]`. A
   direct invocation that omits `--head` audits the checkout's HEAD, which
   is the documented local-use default, not the CI shape.

The workflow edit that satisfies 1 and 2 (`fetch-depth: 0` on
actions/checkout) is owned by the gates.yml owner (M2-P9 per the phase
declarations; the workflow's own comment names the coupling). Until it
lands, the M2 gate bundle step on a pull request will report `error` for
`red-witness` (its `diff-touches` precondition needs the base commit, which
the shallow checkout lacks). Flagged in the final report for the
orchestrator's merge sequencing.

## Captures (real program output, warning 10)

1. `witness/captures/node-test-tap-real.txt`: captured 2026-08-06 by running
   `node --test --test-reporter tap sample.test.ts` (Node v22.22.2) against a
   two-test sample, one passing and one failing assertion; stdout redirected
   verbatim; run exit code 1. Referenced by the kernel spec
   `witness-tap-reporter-pin` and asserted by its named test.
2. `witness/captures/git-name-status-real.txt`: captured 2026-08-06 by
   `git diff --name-status --no-renames 8718852...4c9bfbc` in this
   repository (24 lines, exit 0). Stored for future name-status parser
   witnesses; not yet cited by a spec.
3. The criterion 5a fixture capture content is the real git contention
   stderr recorded in `delivery/review/verification-m1-p3-fix-round.md`
   (finding V-2 orchestrator re-performance:
   `error: cannot lock ref 'refs/remotes/origin/main': is at a0e80f0 but
   expected a0d1254`; V-2 evidence rows document 312 captured contention
   failures, every one of that shape). The fixture writes that recorded line
   verbatim as `captures/git-contention-stderr.txt` in the scratch repo.

## Harness environment finding (recorded because it cost a round of red)

A child `node --test` spawned from inside a running `node --test` process
inherits `NODE_TEST_CONTEXT` and refuses to run any file, printing
"run() is being called recursively within a test file. skipping running
files." while exiting 0 with an EMPTY stdout. To the reporter pin that
reads as a non-tap stream. The harness scrubs every `NODE_TEST*` variable
from the child environment (src/witness/run.ts, runNamedTests) and the
parse-failure diagnostic now carries the child's exit code and stderr.

## Dogfood run (the gate against this repository)

Command: `node src/gates/red-witness.ts --result ... --evidence ...
--base 4c9bfbc --head HEAD` at head 79d2049, working tree of this branch.

Output line: `red-witness: green (4 witness(es) evaluated (4 own, 0 stored
re-evaluated in 0ms); every witness red against every declared dangerous
state and green at head)`; exit 0; wall clock 24.8s (real, `time`).

Evidence (witness-records.json): every kernel witness red 2/2 under its
mutation and green at head with equal pins:

- red-witness-base-absent-error: rate 2/2, headGreen true, pins equal/equal
- witness-member-kind-diagnostics: rate 2/2, headGreen true, pins equal/equal
- witness-spec-schema-required: rate 2/2, headGreen true, pins equal/equal
- witness-tap-reporter-pin: rate 2/2, headGreen true, pins equal/equal

This is the mechanical red-witness demonstration for the four spec-guarded
behaviors: the gate itself applied each mutation, saw the named test fail
twice, restored head, and saw it pass, with all pin pairs equal.

**Criterion 9 cost measurement**: stored-witness re-evaluation over the
kernel corpus at this head took 0ms, because every spec in `witness/` is
phase-own in this diff (added by it) and the stored set is empty. The whole
gate run, own witnesses included, was 24.8s, far under the five-minute
ceiling; the number to watch as the corpus grows is the per-member cost,
about 6s (clone, fetch, 2 red runs, 1 green run).

## Cross-phase collisions surfaced by registering a diff-touches gate

Two, one known and one found here. Neither is fixable from this phase's
files-to-touch list; both are reported to the orchestrator.

1. KNOWN (orchestrator notice, 2026-08-06): `test/gates.test.ts`
   ("manifest-self-check reports one unit per schema document") enumerates
   `src/gates/schemas/` by readdir while the delivered
   `schemaDocumentPaths()` is a hardcoded two-entry list; adding
   `witness-spec.schema.json` reddens it (expected 3, gate reports 2). A
   mechanism fix (self-check enumerates the directory) lands on main
   separately; this branch absorbs it on update. Captured in isolation:
   `node --test test/gates.test.ts` fails exactly this test plus item 2's.
2. FOUND HERE: `test/gates.test.ts` ("the workflow's gate bundle step runs
   the gate runner and is able to fail") extracts the PUSH bundle step (the
   one with no `--base`) and executes it against the REAL manifest,
   asserting exit 0. The delivered runner reports `error` for a
   `diff-touches` gate invoked without `--base` (M2-P1 criterion 8, both
   directions, M2-C-3), so the first registered `diff-touches` gate makes
   that arm exit 21. Captured output:
   `gates: declared 2 applicable 1 verdict 1 green 1 red 0 not-applicable 0
   error 1 vacuous 0` / `gates: 1 gate(s) reported error: red-witness` /
   `21 !== 0`. The same shape holds for the REAL push-to-main bundle step
   in CI: after this phase merges, every push to main reports exit 21 at
   the bundle step until the workflow's owner reconciles the plan's "main
   bundle: not run (needs --base)" with the delivered error semantics
   (plan section 1.4 table row `red-witness` versus M2-P1 criterion 8; the
   same wall awaits M2-P5's `citations` gate). This phase registered the
   gate exactly as the plan's table specifies; the reconciliation belongs
   to the workflow owner (gates.yml is on M2-P9's declaration) or to an
   orchestrator arbitration, not to an unauthorized edit from here.

## Pre-submit hazard self-review (M2-D-18; the phase's declared hazard class)

The declared hazard: "a guard that judges guards, so a defect here is
invisible in every other phase's evidence." Each named hazard, walked:

1. "A dangerous state weaker than the state the behaviour actually defends
   against." Mechanized as refusal rules (a) and (d): a bare absent-feature
   baseline is refused for destructive and classification witnesses citing
   T-003 (test `witness-refusal-baseline-destructive`), and a member that
   does not intersect the phase diff is refused naming the member (test
   `witness-diff-intersection`). The V-1 shape specifically: a witness whose
   test destroys a branch carrying nothing is caught not by a rule but by
   execution, 0-of-n red (test `witness-v1-green-against-danger`). Residue,
   stated: a member that intersects the diff and reddens the test can still
   be weaker than the defended state; no script judges semantic strength,
   and section 1.5 of the plan says so (rows the gate does not cover).
2. "A class witness that reddens under one member and is green under the
   rest." The witness is green only when EVERY member was red (evaluateWitness
   loop; tests `witness-text-preserving-member` red direction and
   `witness-text-behavior-green` green direction).
3. "An assertion over a document's TEXT that survives an edit inverting its
   meaning." Rule (g) strong form: derived text-assertion detection, the
   mechanical preservation check, and the CR-661 fixture with the measured
   mutation (exit 1 to exit 0, every asserted string surviving). Tests
   `witness-text-single-member-collapse`, `witness-text-preserving-member`,
   `witness-text-same-line-collapse`. Residue, stated: the detection is
   syntactic (READ-call literals plus four assertion forms); a test that
   reads a document through an aliased callee or asserts through a helper
   escapes it. The self-guard test `witness-test-file-not-text-asserting`
   keeps the kernel's own test file honest against the detection, and the
   limits are recorded here rather than papered over.
4. "A stored witness that has silently gone green since it was written."
   Step 6 re-evaluation, tests `witness-stored-reevaluation-red` and
   `witness-stored-reevaluation-green` (the N-401 shape).
5. "A determinism claim asserted rather than measured." Every member runs
   `repeats` times and the rate is carried on the record; deterministic
   true requires n of n (tests `witness-determinism-rate-red`,
   `witness-determinism-rate-green`, staged as the real 3-of-5 stage-path
   collision).
6. "A re-evaluation pass whose cost makes an implementer narrow it
   quietly." The cost is measured and recorded per run (reEvaluationMs in
   the evidence record and the gate detail line); the dogfood measurement
   is in this file, and the five-minute stop rule of plan step 6 is
   restated here as the operative ceiling rather than silently rescoped.

## Red-witness demonstrations for this phase's own tests (strong form)

Two sources of red evidence, both mechanical:

1. The DOGFOOD GATE RUN above: the four kernel witness specs' named tests
   were each demonstrated red 2/2 under their declared mutation and green
   at head by the gate itself (tests: tap reporter pin, member-kind
   diagnostics, schema required-list, gate-without-base).
2. A 17-defang matrix over `src/witness/run.ts` and
   `src/gates/red-witness.ts` (driver and full JSON report preserved in the
   session scratchpad, `defang/matrix.py` and `defang/report.json`; each
   defang applied by literal substitution, targeted tests run, file
   restored from a byte copy via shutil.copyfile, not `git checkout --`,
   per warning 8; the driver source is the captured evidence of the
   method). Every defang
   reddened every targeted test and the working tree was byte-identical
   afterwards (`git status --porcelain` empty):

| Defang (dangerous state) | Reddened tests |
|---|---|
| D1 rule (a) disabled | baseline-ref destructive refusal |
| D2 rule (e) disabled | derived-class red |
| D3 rule (g) gate off | single-member collapse; same-line collapse |
| D18a text detection forced false | single-member collapse; preserving member; same-line collapse |
| D18b text detection forced true | kernel test file not text-asserting (self-guard) |
| D4 member-red check forced true | V-1 green-against-danger; 3-of-5 rate red; unreached arm; stored re-evaluation red |
| D16 rate zeroed on record | nondeterministic rate recorded |
| D6 rule (f) disabled | derived capture obligation |
| D7 rule (c) missing-capture arm removed | capture missing red |
| D8 rule (d) disabled | diff intersection |
| D9 pin comparison bypassed | pin hook error naming mtimeMs |
| D10 stored set emptied | stored re-evaluation red; stored re-evaluation green (M2-C-2 rewrite fires) |
| D11 baseline resolved from local ref | baseline from fetched remote |
| D12 coverage check disabled | coverage red |
| D13 shallow probe disabled | shallow error naming fetch-depth |
| D19 head-green check forced false | green end-to-end; corrected V-1 green; behavior-test green; reached-arm green |
| D20 capture recording skipped | capture sha and provenance recorded |

"One witness is not a class" applied to this phase's own guards: the
rule (g) class has TWO structurally different reddening members (D3
disables the class-rule gate, D18a disables the detection that feeds it),
and the green path has two (D19 falsifies head-green; D10 empties the
stored set, reddening the green stored test through the M2-C-2 rewrite).

What the matrix did NOT cover, stated per the fix-round contract: the
defangs target the harness and gate modules only; `src/witness/spec.ts`
and the schema document are covered by the dogfood mutations (kernel specs
B and D), not by the matrix; the fixture-side templates inside
test/witness.test.ts are exercised only as fixtures and carry no defang of
their own.

## Full-suite runs (gate 3), with the known cross-phase failures

- Container default toolchain (`node --test` via `npm test`, v22.22.2),
  2026-08-06: 229 tests, 222 pass, 5 fail, 2 skipped (the two standing
  floor-gated doctor skips with recorded reasons). The five failures,
  verbatim names:
  1. `the workflow's gate bundle step runs the gate runner and is able to
     fail` (cross-phase item 2 above, captured output in that section)
  2. `manifest-self-check reports one unit per schema document`
     (cross-phase item 1, the orchestrator-announced known defect)
  3. `doctor and the guard return one verdict about one beacon`
  4. `a resident watcher keeps running and backs off with growing beacon
     gaps`
  5. `the heartbeat schedule is on disk and shared by single passes`
  Failures 3 to 5 are pre-existing real-clock tests; re-run in isolation
  immediately afterwards all three PASS (3 tests, 3 pass, 0 fail,
  captured). During the failing run this shared box was also executing
  sibling phases' suite runs (their floor-toolchain `node --test`
  processes were visible in the process table), which is the load shape
  CLAUDE.md warning 11 and the U-2 record describe. They are not touched
  by this phase's diff and no witness fixture writes outside its scratch
  directories.
- Phase file in isolation, both toolchains: `test/witness.test.ts` 28/28
  pass on v22.22.2 (49.5s) and on the floor toolchain v26.6.0 (23.0s,
  exit 0, 0 skipped), floor run via an explicit PATH prefix and
  `node --version` checked in the invoking shell (warning 1).
- `npm ci` exit 0 and `npm run build` exit 0 with `git status --porcelain`
  empty afterwards, both toolchains' builds not required (build is
  toolchain-pinned tsc).

## Acceptance criteria walk (plan section M2-P2, criteria 1 to 11)

1. Green end to end: test `witness-harness-green-end-to-end` spawns the
   gate CLI on a fixture; exit 0, record green, units 1 = witnesses
   evaluated, red and green pin pairs recorded and equal, applied mutation
   diff verbatim in the record (asserted on the minus and plus lines).
   Both directions of the harness's verdict are covered by the defang D19
   red run.
2. V-1 shape: tests `witness-v1-green-against-danger` (destroy exercised
   against a branch carrying nothing; witness red naming it) and
   `witness-v1-corrected-green` (branch carries a commit; same spec green).
   Both directions.
3. Baseline-ref refusal: test `witness-refusal-baseline-destructive`, red
   citing T-003 before any test runs; mutation member variant evaluable
   (green) in `witness-v1-corrected-green`. Both directions.
   3a. Derived class: test `witness-derived-class`, additive declaration
   red naming the derived class and the matched command; destructive
   declaration green. Both directions.
   3b. Rule (g), the CR-661 shape: tests
   `witness-text-single-member-collapse` (i),
   `witness-text-preserving-member` (ii: the harness mechanically confirms
   the exit-flip member preserves every asserted string,
   preservesAssertedText true, and reports red because the named test
   stayed green, rate 0/1, which is the reproduction's measured
   `exit=0 pass 1 fail 0` shape), `witness-text-behavior-green` (iii), and
   `witness-text-same-line-collapse` (the additional collapse direction).
   The fixture's mutation is exactly the reproduction's: `exit 1` to
   `exit 0` and nothing else.
4. Determinism: tests `witness-determinism-rate-red` (deterministic true,
   red 3/5, red carrying the rate) and `witness-determinism-rate-green`
   (declared false, green, rate 3/5 recorded), staged as the real
   stage-path collision (fixed `${path}.stage`, two passes, loser dies on
   rename ENOENT) at the measured 3-of-5 rate. Both directions.
   4a. Unreached arm: tests `witness-unreached-arm` (record names file,
   lines and the tests that stayed green; asserted on the record fields,
   not only the rate) and `witness-unreached-arm-reached`. Both directions.
5. Captures: tests `witness-capture-recorded` (capture exists, non-empty,
   basename referenced; record carries sha256 and provenance) and
   `witness-capture-missing` (red naming the missing capture). Both
   directions. "No capture cited" is unconstructible past the schema
   (captures minItems 1), which the schema test witnesses.
   5a. Derived obligation: test `witness-derived-capture-obligation` (red
   naming the spawn-grep derivation); the evaluable direction uses the
   REAL captured git contention stderr from
   verification-m1-p3-fix-round.md as the capture
   (`witness-capture-recorded`). Both directions.
6. Diff intersection: test `witness-diff-intersection`, red naming the
   member for a file outside the diff, green with the mutation inside a
   changed hunk. Both directions.
7. Pin witness: test `witness-pin-hook-error` uses the documented
   betweenPins hook for a byte-identical rewrite; record error naming the
   path and mtimeMs (the five-field pin makes a content-only pin unable to
   pass, per the delivered module's design and the ctime probe recorded in
   step 1).
8. M2-C-4: `assertCallerClean` runs after EVERY gate invocation in the
   test file (HEAD unchanged, `git status --porcelain` empty), including
   all criteria 1 to 7 fixtures.
9. Stored re-evaluation: tests `witness-stored-reevaluation-red` (N-401
   shape, red naming the witness and rate 0/1) and
   `witness-stored-reevaluation-green` (revert returns exit 0). Measured
   re-evaluation cost over the kernel corpus recorded in the dogfood
   section (0ms stored; 24.8s whole gate; under the five-minute ceiling).
10. Baseline from fetched remote: test `witness-baseline-fetched` (local
    origin/main stale at c1, upstream advanced to c3; recorded baselineSha
    equals c3, not c1).
11. Suite accounting: full-suite state recorded above; this phase's file
    is 28/28 on both toolchains, every new behavior registered in
    test/behaviors.json (28 additions) and resolving by name (value equals
    the test title, matching the registry convention verified against
    test/cli.test.ts). The full `node --test` exit 0 gate is currently
    blocked by the two KNOWN cross-phase gates.test.ts defects recorded
    above, which the orchestrator is landing a mechanism fix for on main;
    with those two tests excluded the remaining failures are the three
    pre-existing real-clock tests that pass in isolation (captured).

## Progress log

- 2026-08-06: branch created off origin/main at 4c9bfbc. Step 0 and step 1
  recorded above before any code.
- 2026-08-06: schema, spec module, harness engine, gate, manifest
  registration built; test/witness.test.ts 28/28 green in 49.5s on the
  container default toolchain (v22.22.2).
- 2026-08-06: dogfood gate run green (above). Full-suite run and defang
  matrix follow.
- 2026-08-06: full suite measured (222/229 pass; failure set fully
  attributed above); floor-toolchain phase-file run 28/28 exit 0; 17-defang
  matrix complete with zero non-red results; claim grep run with every hit
  carrying adjacent evidence; scope check: changed files are exactly the
  phase declaration's list plus the two standing extras.

## Claim grep output (against the submitted head)

Run: `grep -nEi 'cannot be|impossible|needs a|is covered|catches|would
catch|recovers|anyway|always|never|no way to'
delivery/work-history/m2-p2.md`. Hits at submission, by content rather
than by line number because this section shifts them: (1) the quotation of
the MECHANISMS.md rule text in step 0 item 3, marked as a quotation;
(2) the coverage DEFINITION in D-P2-2, with its witnessing tests named
beside it; (3) the defang restore-method sentence, whose captured evidence
is the preserved driver source; plus this section's own quotation of the
grep pattern. No impossibility or coverage claim stands without a
construction beside it; the one impossibility-shaped claim in this file
(the pin-across-restore consequence) carries its executed probe.

## Fix round 1 (2026-08-06): CR-H1 merge-blocking medium, CR-H2 medium-low, CR-1261/CR-1262 lows

Head before: `7ed8830` (arbitration-m2-p2.md). Branch updated onto `origin/main`
first (merge `8403e9e`), absorbing the schema-count and push-bundle cross-phase
fix; the two shared registries (`test/behaviors.json`, `gates.manifest.json`)
were resolved as a union keeping every key/entry from both sides, JSON-validated.

### CR-H1: the mechanism (not the three idioms)

MECHANISM, named: **rule (g)'s text-assertion detection was NARROWER than the
hazard it guards, so it silently returned `textAsserting=false` for standard
read/assert idioms and let a single-member text-asserting witness ship green.**
Two sub-parts, both mechanism not instance:

1. The document-read regex matched only synchronous `readFileSync`. Async
   `readFile` (node:fs/promises) is a first-class API, not an alias, and was
   invisible.
2. The pattern regex matched only a literal regex/string. A variable regex
   (`assert.match(body, wanted)`) or a variable path (`readFileSync(P)` with
   `const P = "....yml"`) yielded nothing, and detection required
   `patterns.size > 0`, so it fell through to false.

The fix is at the mechanism: `deriveTextAssertions` was rewritten to (a) detect
sync AND async reads; (b) resolve a document path held in a `const`/`let`/`var`
string binding (member F); (c) TIE every assertion to the read RESULT (a bound
variable, or an inline read), so a read consumed by a project function
(`isTransient(body)`) is behaviour and is NOT flagged; (d) recognise the full
assert set the plan names, `assert.match`/`doesNotMatch`, `.includes`/`.indexOf`
(the `assert.ok` wrapper falls out of matching the method call itself), and the
equality family over the whole body; and (e) **fail conservatively**: a read of
a resolvable non-src/test document whose result is asserted, but with no
statically extractable pattern, is text-asserting anyway (documents alone),
subject to rule (g).

Why tied-to-the-read-result and not "any assert form present": the naive
signal would have reddened the retry witness (`assert.equal(isTransient(line),
true)` reads a real capture) and the atomic determinism witness
(`assert.equal(readFileSync(dest), "B")` reads a temp file). Both are genuine
behaviour witnesses and both must stay green; a runtime path (`join(...)`,
`mkdtemp`) resolves to no document on purpose, because it cannot be told apart
from a scratch read.

DERIVATION (full output). A probe drove `deriveTextAssertions` over every named
idiom plus the behaviour/residue cases that must NOT flag (floor node v26.6.0):

```
TEXT-ASSERTING | docs=[".github/workflows/guard.yml"] pats=["falsifiability guard"] | sync + literal regex (already covered)
TEXT-ASSERTING | docs=[".github/workflows/guard.yml"] pats=["falsifiability guard"] | async readFile (DESTRUCTURED named import only; NOT the fs.promises.readFile namespace form, which round 1 left uncovered: see CR-1500 correction in fix round 2)
TEXT-ASSERTING | docs=[".github/workflows/guard.yml"] pats=[] | sync + VARIABLE regex (no extractable pattern)
TEXT-ASSERTING | docs=[".github/workflows/guard.yml"] pats=["falsifiability guard"] | VARIABLE path (const P = doc; readFileSync(P))
TEXT-ASSERTING | docs=[".github/workflows/guard.yml"] pats=["name: guard\\n"] | assert.equal over the whole document
TEXT-ASSERTING | docs=[".github/workflows/guard.yml"] pats=["falsifiability guard"] | assert.ok(body.indexOf(...) >= 0)
TEXT-ASSERTING | docs=[".github/workflows/guard.yml"] pats=["x"] | inline assert.equal(readFileSync(doc), EXPECTED)
not-text       | docs=[] pats=[] | NOT text: read passed to a project function (isTransient)
not-text       | docs=[] pats=[] | NOT text: read of a runtime temp path asserted
not-text       | docs=[] pats=[] | NOT flagged: aliased callee (known residue)
```

WHAT THE DERIVATION DOES NOT COVER (item 3, stated so the search is not mistaken
for absence of defects):

- A document read whose PATH is a runtime value (`join(...)`, a computed path,
  a `mkdtemp` result) is deliberately not flagged; a workflow read through a
  computed path escapes. This is the fail-open cost of not reddening the
  legitimate temp-file behaviour witnesses, and it is a chosen trade.
- A variable pattern (`assert.match(body, wanted)`) is flagged text-asserting
  and so gets the CARDINALITY guard (>=2 structurally different members), but
  the PRESERVATION arm is vacuous for it because the pattern cannot be re-run,
  so a >=2-member variable-pattern witness is not checked for a text-preserving
  meaning-inverting member. Single-member collapse is still caught.
- An aliased callee (`const readDoc = readFileSync; readDoc(path)`) or an
  assertion routed through a helper still escapes; this is the pre-existing
  residue the original hazard self-review named, unchanged by this round.
- The detection scans the resolved head-state source of the NAMED test files
  only; a named test whose body is assembled at runtime is out of scope.

RED WITNESSES (strong form, >=2 structurally different members of the
detection-escape class). Demonstrated red against the DANGEROUS state (the
narrow detection, reached by `git stash` of the two source files with the new
tests present) and green with the fix. The reds are dangerous-state reds: the
gate shipped a false witness GREEN where it must be red (T-003 strong form),
not merely an absent feature.

(the reporter's non-ASCII pass/fail marks are transcribed to FAIL/PASS to keep
this file pure ASCII per convention 3; the test names and assertion text are
verbatim.)

```
FAIL an async-read text-asserting witness with a single member is red naming the collapse       ('green' !== 'red')
FAIL an async-read text-asserting witness is red when its text-preserving member leaves the test green
FAIL a variable-regex text-asserting witness with a single member is red naming the collapse    ('green' !== 'red')
FAIL a variable-path text-asserting witness with a single member is red naming the collapse      ('green' !== 'red')
FAIL a diff touching a spawning shell script without the capture field is red naming the derivation ('green' !== 'red')
PASS a spawning shell script witness citing the real capture is green   (green counterpart; correct under both)
info tests 6  pass 1  fail 5
```

Three structurally different members of the class (async read, variable regex,
variable path) each redden as a single-member collapse; the async idiom also
reddens the preservation arm (a text-preserving `exit 1 -> exit 0` member that
leaves the text test green, `preservesAssertedText=true` with `rate.red 0`).

### CR-H2: rule (f) blind to shell spawn-and-parse

MECHANISM: rule (f)'s spawn derivation was four JS tokens over the changed
files, so a `*.sh` that spawns another program and PARSES its output (M1's V-2
lived in `bin/fm-*.sh` classifying git contention output) contained none of the
tokens and carried no capture obligation. Fix: a changed `*.sh` is screened by
`shellSpawnsAndParses`, the conjunction of a SPAWN signal (a pipeline, a command
substitution, backticks, or stdin consumed by `read`) and a PARSE signal
(`grep`/`awk`/`sed`/`cut`/`tr`, a `case` branch, or a `[[ =~ ]]` test). The
conjunction is narrow on purpose: a script that only runs a command for its exit
status is not burdened.

DERIVATION (full output, floor node v26.6.0):

```
SPAWN+PARSE | pipe into grep (V-2 shape)
SPAWN+PARSE | command substitution + case
SPAWN+PARSE | backtick + awk
SPAWN+PARSE | while read + =~ classify
not         | NOT parse: only exit status
not         | NOT spawn: pure echo
not         | residue: xargs spawn, no pipe/subst/grep
```

WHAT IT DOES NOT COVER: a shell script that spawns via `xargs`/`exec` with no
pipe/substitution/`read` and no in-script classifier (it hands the output to
another program to parse), or classifies via a sourced helper, escapes. This is
a regex over shell text, which MECHANISMS.md row 23 warns is a union of idioms
known on the day; it is scoped to the V-2 shape and its residue is named here
rather than left to look like coverage. Impact on the current corpus is zero
(there are no `*.sh` audited-source files today); it fails closed for future
`bin/*.sh`. RED WITNESS: a `bin/classify.sh` piping git stderr into `grep`,
guarded by a test that hand-writes the git line, is red requiring
`consumesExternalOutput` (green under the old JS-only grep, which is the
dangerous-state red above); citing the real V-2 capture makes it green.

### CR-1261, CR-1262 (lows): work-history accuracy

Corrected in D-P2-3 (spec.class consulted at THREE places, not two; the third
is rule (g)'s `needsClassRules`, `grep -n "spec.class" src/witness/run.ts` =>
lines 1011, 1016, 1031, 1035, 1133, 1138, three distinct sites) and D-P2-4 (the
shipped `SPAWN_GREP` has FOUR alternatives, not five; `exec(` was never in the
pattern).

### Gates (both toolchains)

- Floor node v26.6.0: `npm ci` exit 0 no EBADENGINE; `npm run build` exit 0;
  `git status --porcelain` empty after build; `test/witness.test.ts` 34/34 pass
  in isolation.
- Default node v22.22.2: recorded in the timeline below.
- Full suite failure set is the known cross-phase/real-clock set only (below).

### Claim grep (fix round 1)

Run: `grep -nEi 'cannot be|impossible|needs a|is covered|catches|would
catch|recovers|anyway|always|never|no way to' delivery/work-history/m2-p2.md`.
Every hit added by this section is a residue statement carrying its adjacent
captured derivation (the "does not cover" lists above sit beside the probe
output that bounds them), a quotation of the grep/MECHANISMS text, or a
negated-and-constructed claim (each "escapes" is a NAMED uncovered case, not an
impossibility). No impossibility-shaped claim stands without a construction.

## Fix round 2 (2026-08-06): CR-1500 merge-blocking medium, CR-1501 low (folded)

Head before: `7714805` (arbitration-m2-p2-round2.md, clean-room-m2-p2-round2-hazard.md).
Branch already carried `origin/main` (`8439c88`); merging origin/main was a
no-op ("Already up to date"), the merge base equalling origin/main's head, so
the two shared registries (`test/behaviors.json`, `gates.manifest.json`) needed
no re-union this round. Six new behavior keys were appended to
`test/behaviors.json`.

### CR-1500: the mechanism (namespace member-call recognition), not the idiom

MECHANISM, named: **rule (g)'s read-recognition resolved the read CALLEE only as
a bare identifier (`readFile`/`readFileSync`), so the NAMESPACE-QUALIFIED read,
`fs.readFileSync(...)`, `await fs.promises.readFile(...)`, and an aliased
`fsp.readFile(...)`, was invisible; under it `textAsserting` stayed false, rule
(g)'s cardinality guard was not enforced (`needsClassRules = spec.class ===
"classification" || derivation.textAsserting`, run.ts), and a single deleting
text-asserting member shipped GREEN.** This is the SAME mechanism as CR-H1 (the
detection narrower than the hazard) one level up: round one broadened WHICH
assert forms and WHICH path bindings were seen, but left the callee resolver
matching a bare name. The namespace form is the dominant real-world idiom, more
common than the bare async named-import that round one did close.

The fix is at the mechanism, bounded to the SAME builtin the detector already
targets. A single `READ_CALLEE` fragment,
`(?:[A-Za-z_$][\w$]*\s*\.\s*)*readFile(?:Sync)?`, replaces the two bare
`readFile(?:Sync)?` occurrences (the `READ_BINDING` regex and the
`inlineTextAssertedReads` regex). The prefix is zero or more `<ident>.`
segments, so it resolves the bare form (zero segments), the one-hop namespace
(`fs.readFileSync`), the two-hop chain (`fs.promises.readFile`), and an aliased
namespace (`fsp.readFile`) identically. It resolves the CALLEE only and leaves
`textAssertionsOnVar` (the result-tied assertion detection) unchanged; the
over-reach parity probe below is the evidence that the read's MEANING is
untouched (every real derived-value witness stays not-text under both
detectors).

### CR-1501 (folded): extension-less root documents

MECHANISM: `isDocumentPathLiteral` rejected any path with neither `/` nor `.` as
a bare token, so a read of `Makefile`/`LICENSE` and an assertion on its text
escaped. This is a denylist-shaped gap, so per MECHANISMS.md row 23 it is closed
by a DERIVED closed vocabulary rather than a pattern: `EXTENSIONLESS_ROOT_DOCS`,
the conventional extension-less root files (Makefile, Dockerfile, Containerfile,
Jenkinsfile, Vagrantfile, Rakefile, Gemfile, Procfile, Brewfile, LICENSE,
LICENCE, NOTICE, COPYING, AUTHORS, CONTRIBUTORS, CODEOWNERS, README, CHANGELOG).
An extension-less name OUTSIDE this set stays behaviour and is NAMED residue
below, not chased.

### DERIVATION (full output, floor node v26.6.0)

A probe drove the EXPORTED `deriveTextAssertions` over every read spelling under
BOTH the pre-fix detector (the head-state source at 7714805, obtained with a git
show and swapped in by copy, never a checkout) and the post-fix working tree, so
the flip is visible per spelling. Each recognised case is a single-member
text-asserting witness: under `textAsserting=false` such a witness ships GREEN
(the cardinality guard is not enforced), under `true` it is caught.

```
spelling                                                   PRE-FIX(7714805)  POST-FIX
bare sync readFileSync (control, already caught)           TEXT-ASSERTING  TEXT-ASSERTING
bare async named-import readFile (round-1, already caught) TEXT-ASSERTING  TEXT-ASSERTING
NS sync  fs.readFileSync (CR-1500 member G)                not-text        TEXT-ASSERTING   <== FLIP
NS async fs.promises.readFile (CR-1500 member A)           not-text        TEXT-ASSERTING   <== FLIP
NS alias fsp.readFile (CR-1500 member A2)                  not-text        TEXT-ASSERTING   <== FLIP
NS inline assert.match(fs.readFileSync(doc), /re/)         not-text        TEXT-ASSERTING   <== FLIP
extension-less root doc Makefile, NS read (CR-1501)        not-text        TEXT-ASSERTING   <== FLIP
extension-less root doc LICENSE, bare read (CR-1501)       not-text        TEXT-ASSERTING   <== FLIP
-- RESIDUE (must stay not-text under BOTH) --
aliased callee const rf = fs.readFileSync (residue)        not-text        not-text
callback-style read (residue)                              not-text        not-text
two-hop rebinding const body2 = body (residue)             not-text        not-text
extension-less name NOT in set 'randomtoken' (residue)     not-text        not-text
-- OVER-REACH (must stay not-text: derived-value behaviour) --
NS read passed to project fn isTransient(body)             not-text        not-text
NS read parse-then-assert-derived                          not-text        not-text
```

An over-reach PARITY probe settled the one apparent over-reach: an INLINE
document-looking literal in the read args (`fs.readFileSync(join(tmp,
"state.json"))` + `assert.match`) does flip to TEXT-ASSERTING post-fix, but the
BARE form of the same shape was ALREADY TEXT-ASSERTING pre-fix, so the fix only
gives the namespace form the identical classification the bare form had; it is
not a new over-reach. The REAL derived-value witnesses (a runtime path held in a
VARIABLE bound to `join(...)`, the atomic-fixture shape; a read passed to a
project function; parse-then-assert-on-derived; a computed length) all stay
not-text under BOTH detectors:

```
case                                                           PRE-FIX   POST-FIX
bare  readFileSync(join(tmp,'state.json')) inline literal      TEXT-ASSERTING  TEXT-ASSERTING
NS    fs.readFileSync(join(tmp,'state.json')) inline literal   not-text        TEXT-ASSERTING
-- REAL derived-value witnesses (NS-lifted): stay not-text --
NS var-held runtime path (atomic fixture shape)                not-text        not-text
NS read passed to project fn isTransient(body)                 not-text        not-text
NS parse-then-assert-on-derived                                not-text        not-text
NS computed length                                             not-text        not-text
```

### WHAT THE DERIVATION DOES NOT COVER (item 3, named residue, not chased)

- A read CALLEE bound to another variable (`const rf = fs.readFileSync; rf(p)`,
  or `const readDoc = readFileSync`) still escapes. Resolving it needs callee
  data-flow, one hop beyond a syntactic callee match; it is the same aliased
  residue the round-one hazard self-review named, now shown to hold for the
  namespace alias too.
- A CALLBACK-style read (`readFile(p, (err, data) => assert.match(data, /re/))`)
  escapes: the read result is not bound to a name and not the first argument of
  an assert, so neither `READ_BINDING` nor `inlineTextAssertedReads` sees it.
- TWO-HOP variable rebinding (`const raw = fs.readFileSync(p); const body =
  raw; assert.match(body, /re/)`) escapes: the assertion is on a variable one
  assignment removed from the read binding.
- An extension-less path name OUTSIDE `EXTENSIONLESS_ROOT_DOCS` stays behaviour.
  A read of an unconventional extension-less document escapes; adding the name to
  the set closes it. This is the chosen boundary of the closed vocabulary.
- Unchanged from round one: a read PATH that is a runtime value (`join(...)`, a
  `mkdtemp` result) is deliberately not flagged (the fail-open cost of not
  reddening the legitimate temp-file behaviour witnesses); a variable pattern is
  flagged but its preservation arm is vacuous; detection scans the resolved
  head-state source of the named test files only.

### Correction of the round-one derivation claim (CR-1500 second half)

The round-one derivation table (this file, the CR-H1 block) listed "async
readFile (node:fs/promises) -> TEXT-ASSERTING", which read as node:fs/promises
being covered. That was true ONLY for the DESTRUCTURED named-import form (`import
{ readFile } from "node:fs/promises"; readFile(p)`); the `fs.promises.readFile`
namespace member call from the SAME module was NOT covered. The table row has
been corrected in place to state that scope, and this round closes the namespace
form.

### RED WITNESSES (strong form, >=2 structurally different members)

Members of the "namespace read escape" class: a one-hop namespace sync read
(`fs.readFileSync`), a two-hop chain async read (`fs.promises.readFile`), and an
aliased namespace (`fsp.readFile`) -- three structurally different members. Each
was demonstrated RED against the DANGEROUS state (the pre-fix detector, swapped
in by copying the head-state source at 7714805 into place, never a git checkout,
with the new tests present) and GREEN with the fix. The reds are dangerous-state
reds: the gate shipped a false witness GREEN where it must be red, the exact
CR-1500 property (`'green' !== 'red'`), not an absent feature. One member also
reddens the preservation arm (a text-preserving `exit 1 -> exit 0` member
leaving the namespace text test green, `preservesAssertedText=true` with
`rate.red 0`). Marks transcribed to FAIL/PASS for ASCII (convention 3); names
and assertion text verbatim.

```
FAIL a namespace sync-read text-asserting witness with a single member is red naming the collapse   ('green' !== 'red')
FAIL a namespace async-read text-asserting witness with a single member is red naming the collapse  ('green' !== 'red')
FAIL an aliased-namespace read text-asserting witness with a single member is red naming the collapse ('green' !== 'red')
FAIL a namespace sync-read text-asserting witness is red when its text-preserving member leaves the test green
FAIL deriveTextAssertions recognises the namespace and root-doc reads and leaves the named residue not-text (false !== true, on "namespace sync")
PASS deriveTextAssertions does not falsely redden a namespace derived-value behaviour witness  (over-reach guard, correct under both)
info tests 7  pass 1  fail 6
```

CR-1501's detection delta is witnessed inside the detection matrix test above
(Makefile via a namespace read AND LICENSE via a bare read: two structurally
different recognised members, plus a residue name), red pre-fix and green
post-fix. It is not given a separate gate-level collapse fixture: the gate
machinery downstream of `isDocumentPathLiteral` returning true is already
demonstrated red-green by the namespace collapse fixtures, so the CR-1501 delta
is purely a detection change and is witnessed where the detection lives. An
earlier draft used a gate-level Makefile fixture; it reddened for the WRONG
reason pre-fix (the fixture carried no Makefile, so the head run failed rather
than the witness shipping green), which is not a dangerous-state red, and it was
removed.

### Over-reach not regressed

The round-one property (a legitimate derived-value behaviour witness must not be
falsely reddened) holds for the namespace form: the dedicated over-reach test
("deriveTextAssertions does not falsely redden a namespace derived-value
behaviour witness") is GREEN under both detectors for a namespace read passed to
a project function, a namespace parse-then-assert-on-derived, and a namespace
read of a runtime path held in a variable.

### Gates (both toolchains)

- Floor node v26.6.0: `npm ci` exit 0 no EBADENGINE; `npm run build` exit 0;
  `git status --porcelain` empty after build (only the tracked source/test/doc
  edits pending, `dist/` and `*.tsbuildinfo` gitignored); `test/witness.test.ts`
  40/40 pass in isolation; FULL suite `npm test` 260 tests / 260 pass / 0 fail /
  0 skipped.
- Default node v22.22.2: `npm ci` exit 0; `npm run build` exit 0;
  `test/witness.test.ts` 40/40 pass (TAP `# pass 40 # fail 0 # skipped 0`).

### Claim grep (fix round 2)

Run: `grep -nEi 'cannot be|impossible|needs a|is covered|catches|would
catch|recovers|anyway|always|never|no way to' delivery/work-history/m2-p2.md`.
Every hit added by this section is either a NAMED residue statement sitting
beside the probe output that bounds it (each "escapes" is a constructed uncovered
case with its two-column pre/post row, not an impossibility), a quotation of the
MECHANISMS/contract text, or the corrected coverage claim (the CR-1500 second
half explicitly narrows the earlier "is covered" reading rather than asserting
new coverage). No impossibility-shaped claim stands without a construction; the
verbatim grep output is recorded in the fix-round-2 timeline entry below.
