# M4-P12 work history: the merge preconditions and the branch-protection encoding

- phase: M4-P12
- branch: `claude/m4-p12-merge-preconditions`
- plan section: delivery/plan/kernel-plan-m4.md:1908
- declaration: delivery/plan/phase-declarations/m4-p12.json:1
- toolchain for every command below unless stated otherwise: node v26.6.0 from
  the scratch prefix `/tmp/claude-0/n26/bin/node`, confirmed with
  `node --version` in the shell that ran the command.

## WHAT THIS WORK DID NOT COVER

Read this before any evidence below. The reviewer's first check is this
section (CLAUDE.md:382).

1. **DR-0012 condition 3 is only HALF mechanised, and the half is named.**
   Condition 3 (delivery/decisions/DR-0012-delegated-merge-authority.md:24)
   says both reviewers were given the phase's acceptance criteria as their
   contract and both walked or executed them. What this gate establishes from
   the verdict documents alone is that each one declares a `review-contract`
   and carries a non-empty `criteria[]` whose entries each record `met`.
   Whether that walk COVERS every acceptance criterion the plan declares is a
   comparison against a DIFFERENT document, and it is the shipped Kind B check
   `verdict-criteria-complete` at src/checks.ts:2272, which resolves a
   `plan.yaml` out of its context directory. This repository has no `plan.yaml`,
   so wiring that check in here would have made condition 3 permanently `error`
   about the instrument rather than about the merge. The row's own sentence says
   which half it established. The plan's steps name conditions 1, 2, 4, 5 and 6
   and the ruleset encoding, and give condition 3 no step at all, so this is a
   gap I filled with the weaker readable predicate and declared, not one the
   plan asked me to close.

2. **The gate does not verify that a verdict's `head` names a commit that
   exists**, nor that it is the commit the reviewer actually read. That is
   stated at schemas/verdict.schema.json:34 as what the field does NOT buy, and
   nothing here changes it. `head` makes the CLAIM comparable between documents;
   it does not make it true.

3. **The arbitration document is read as TEXT, not as a typed artifact.**
   Condition 6 requires the document to contain each verdict's FILE NAME and a
   commit-shaped token that is a prefix of the head. It does not parse the
   document, does not check that the ruling is coherent, and does not check that
   the arbitrator had authority. A document containing the two file names and
   the head inside unrelated prose satisfies it. Making arbitration a typed
   artifact with a schema is not on this phase's declaration.

4. **The commit-shaped token rule has a stated false-negative direction.**
   `\b[0-9a-f]{7,40}\b` matches any hex-looking word of seven or more
   characters, so a document containing an unrelated hex token that happens to
   be a prefix of the head would satisfy the head test. I did not find a way to
   distinguish that from a real head citation without a typed field, and I am
   recording it as an open question rather than as something that cannot happen.

5. **No CI arm was observed.** Everything below is a local execution plus live
   reads of the GitHub REST API. I did not watch a workflow run to completion,
   and nothing here discharges T-009's requirement that the post-merge `push`
   run on the new tip be observed; that is the orchestrator's.

6. **`check-dual-review`'s own arms are not re-tested here.** This gate is a
   SECOND CALLER of the same exported primitives from `src/checks.ts`; the
   primitives themselves are guarded by test/dual-review.test.ts and
   test/verdict-head.test.ts, which I did not change and did not re-derive.

7. **The gate has never run non-vacuously on this repository's own head**, and
   it structurally cannot until a phase commits two `kind: verdict` documents
   naming its head. Measured on `main` at bc792b9:
   `grep -rln '^kind: verdict' delivery/` returns two paths, both under
   `delivery/evidence/m3-exit-test/`, and neither is a review of a head. So the
   run recorded in section 7 below is the NOT-APPLICABLE arm with an evaluated
   precondition, not a green one. That is the honest state and it is why the
   exit-test expectation row added in section 5 is `green|not-applicable`.

8. **Only the `rulesets` REST surface was probed for branch protection.** The
   legacy `/branches/main/protection` endpoint was not read, so a repository
   protected by the older mechanism and by no ruleset would read as unprotected
   here. R-064 is delivered as a RULESET
   (delivery/plan/kernel-plan-m4.md:1866), so that is the surface the plan
   names, and reading a second one was not in scope.

## 1. What was built

`src/gates/merge-preconditions.ts`, a new gate id `merge-preconditions`
producing SEVEN rows: one per DR-0012 condition (six) plus the
branch-protection encoding, each row carrying the head sha it was evaluated
against. It is a PRECONDITION READER and not a merge command, which is the
plan's own framing at delivery/plan/kernel-plan-m4.md:1875: M4-D-09 puts the
merge capability in the plugin at cutover and DR-0036 keeps merge authority
with the current process for the whole of M4, so the orchestrator reads this
record before merging, by hand.

The gate word and the row words are not always the same, and the one place
they deliberately differ is written down at src/gates/merge-preconditions.ts:80
rather than left for a reader to trip over. Plan step 4
(delivery/plan/kernel-plan-m4.md:1911) says an ABSENT scope record is `error`.
Criterion 4 (delivery/plan/kernel-plan-m4.md:1938) says condition 5 is RED when
no record exists. Both hold, about different objects: the CONDITION is
unsatisfied either way and its row reads red with a reason naming which arm,
while the GATE could not reach a verdict about an instrument that is not there,
so the gate's status is `error`. The test asserts both.

## 2. The REST reachability probe (plan step 1)

CLAUDE.md standing warning 6 records REST reachability here measured
unreachable on 2026-08-13 and reachable on 2026-08-14, with the cause never
established, and says to PROBE it at the start of a session that depends on it.
Probed 2026-09-17 with plain `curl` from this container:

| probe | result |
|---|---|
| `GET /rate_limit` with `$GH_TOKEN` | **HTTP 200**, `core.limit` 15000 |
| `GET /rate_limit` with a DELIBERATELY INVALID token | **HTTP 200**, same 15000 |
| `GET /repos/ThomasHendrickx/tiphys-ai-helmsman/rulesets` | **HTTP 200** |
| `GET /repos/.../rulesets/20446760` | **HTTP 200** |
| `GET /repos/.../commits/bc792b9.../check-runs` | **HTTP 200**, 2 runs |

The second row is the one that says WHAT is authenticating: an invalid token
receiving an App-installation rate limit means the value in `GH_TOKEN` is not
what the remote sees, and the agent proxy at `$HTTPS_PROXY` substitutes real
credentials on the way out. So the API is reachable today from here, and that
is a fact about today rather than a property, which is exactly why the gate
reports `error` when it cannot reach the API rather than assuming either way.

**THE GATE READS NO CREDENTIAL, AND THE SHIPPED SUITE IS WHAT DECIDED THAT.**
The first draft of this module read `GH_TOKEN` and `GITHUB_TOKEN` for an
Authorization header. The full suite reddened on it:

```
test/m2-exit-test.test.ts:425
x no environment variable changes a production gate's reported status (grep over the gate sources)
  AssertionError: src/gates/merge-preconditions.ts:311 reads the named environment
  variable GH_TOKEN ... A production gate must not read a named environment
  variable that changes its reported status (M2-P9 criterion 3)
```

The line number inside that captured assertion, `merge-preconditions.ts:311`,
is the PRE-FIX file's and is left verbatim rather than corrected: altering
captured output silently is the fabrication the red-witness rule exists to
prevent. It no longer points at an environment read in the shipped file, and a
reader chasing it should read this paragraph instead.

That is the guard working and the rule is right: an ambient variable that
changes a gate's verdict leaves nothing in the record to say the verdict
changed. Two other named reads went with it, `TIPHYS_GITHUB_API_BASE` and
`TIPHYS_GITHUB_REPO`, both now flags only. So the gate now sends NO
Authorization header, and that was measured before it was chosen, with the
header omitted entirely:

| probe, NO Authorization header | result |
|---|---|
| `GET /repos/ThomasHendrickx/tiphys-ai-helmsman` | **HTTP 200** |
| `GET /repos/.../rulesets` | **HTTP 200** |
| `GET /repos/.../commits/713731ff.../check-runs` | **HTTP 200** |

WHAT THAT COSTS, recorded rather than left to be found: in a deployment where
the API genuinely requires a credential, every request answers 401 or 404 and
the gate reports `error`. That is the fail-closed direction and never a silent
pass, and supplying a token would have to be a DECLARED FLAG in the registry
command rather than an ambient environment read.

What the ruleset probe returned, quoted because R-064 and R-065a are both read
off it:

```
name main-protection enforcement active
 rule deletion {}
 rule non_fast_forward {}
 rule pull_request {"required_approving_review_count": 0, ...,
   "allowed_merge_methods": ["merge", "squash", "rebase"]}
 rule required_status_checks {"strict_required_status_checks_policy": true,
   "required_status_checks": [{"context": "gates"}]}
```

R-064 is DONE and now CHECKABLE. R-065a (squash-only) is NOT done:
`allowed_merge_methods` carries all three. The gate reports that as DATA on the
green arm and never turns the row red for it, because it is an owner action the
owner deferred (delivery/plan/kernel-plan-m4.md:1866).

## 3. Conditions 1 and 2 are COMPOSED, not reimplemented (plan step 6)

M4-P10 shipped `dual-review-decorrelation` (src/checks.ts:4589) and
`verdict-pair-approves` (src/checks.ts:4941). This gate does not copy their
rules. It is a second caller of the same exported primitives that
`scripts/check-dual-review.mjs` calls:

- `resolveCorpusSource` (src/checks.ts:3432) to decide which corpus is read,
- `missingRegimeDocument` (src/checks.ts:4123) to refuse a context whose
  delivery regime cannot be determined,
- `readReviewFamilies` (src/checks.ts:4245) for the DR-0038 declaration,
- `loadCommittedVerdicts` (src/checks.ts:3143) for the corpus itself,
- `registeredChecks` (src/checks.ts:5300), then the two checks BY ID.

Every one of those refusals is `error` here rather than red, which is the same
fail-closed placement `scripts/check-dual-review.mjs` makes at the same layer
and for the same reason: a merge gate that cannot establish the regime has not
reached a verdict (M2-C-3).

**A check that is NOT REGISTERED is `error` here, where the sibling script
prints a count and carries on.** That is a deliberate difference and it is
witnessed: `merge-preconditions-composed-check-absence-is-error` reddens the
gate when `dual-review-decorrelation` is filtered out of the selection. On the
merge path a condition with no check behind it is a condition nobody looked at.

## 4. Was `src/cli.ts` edited? NO, and here is the verification

The plan's files-to-touch line reads `src/cli.ts` (edit only if gate
registration requires it; verify first)
(delivery/plan/kernel-plan-m4.md:1926). Verified, and it does not.

src/cli.ts:26 introduces a dispatch table of SUBCOMMANDS, and `gates` is already
in it (src/cli.ts:34), handled by `cmdGates`. A gate is not a subcommand: it is an
argv entry in `gates.manifest.json` that the runner SPAWNS, and
src/gates/run.ts:1524 builds that spawn from the manifest entry's own `command`
array plus `--result`, `--evidence` and one flag per declared parameter.
Nothing in that path reads the CLI dispatch table. The file is therefore
declared and untouched, which costs one printed line in the scope audit and is
the right outcome.

## 5. The drift chain, and the one declaration amendment

Four files travel together and all four moved:

1. `gate-registry.yaml`: one appended entry, the single source.
2. `CLAUDE.md`: the gate block RE-RENDERED with
   `node scripts/render-agent-rules-gates.mjs --write`. Never edited by hand.
3. `roles/implementer.md`: re-rendered with `node scripts/check-brief-drift.mjs
   --write`. The plan's files-to-touch line omits this file and the gate does
   not, which is why the declaration carries it.
4. `gates.manifest.json`: one appended entry, because CI invokes the runner
   with `--manifest` and a registry-only gate is declared and never executed.

**DECLARED AMENDMENT: `scripts/m2-exit-test.sh` was added to the declaration's
`filesToTouch` at the head.** The scope gate allows an addition and prints it
by name (src/gates/scope.ts:110), and this is the reason, stated so a reviewer
can judge it rather than accept it.

Adding a gate to `gates.manifest.json` puts it in the exit harness's DERIVED
expected set (scripts/m2-exit-test.sh:741, whose reason is spelled out at
scripts/m2-exit-test.sh:559), and a manifest gate with no row in
the expectations table is asserted under the STRICT default, required and
green. `merge-preconditions` is a merge precondition reader: its subject is a
head that two committed clean-room verdicts name, and on an ordinary
pull-request run no such verdict exists yet, so its honest answer is
`not-applicable` with an evaluated, unmet precondition. Under the strict
default that is a red exit test on every phase branch, which would be the
harness asserting something false rather than a gate misbehaving. The row added
is the `credential-token` shape, `green|not-applicable` and not required, and
the not-applicable ARM is still checked: leg 4 of the assertion program
(scripts/m2-exit-test.sh:809 onward) requires every not-applicable record to
name an evaluated precondition with an id, `met: false` and a reason, so a
silently skipped or errored gate cannot pass through this row.

The alternative was to leave the gate registry-only. That is worse in two ways
and both are measurable: CI runs `--manifest` on both arms, so a registry-only
gate never executes, which the registry's own header records inside
`gate-registry.yaml` (a root-level YAML path matches no declared citation root,
CLAUDE.md:159, so it is quoted here rather than cited), and that is a gate that
cannot go red; and test/gate-registry.test.ts:1142 reddens for a registry-only
script gate
gate with no declared divergence, which would have meant editing that test file
AND `.github/workflows/gates.yml`, two files instead of one.

## 6. Every acceptance criterion, walked

### Criterion 1: `node --test test/merge-preconditions.test.ts` exits 0, N tests, N > 0

MET. The complete sentence names four things (CLAUDE.md:1029):
interpreter `v26.6.0` at `/tmp/claude-0/n26/bin/node`, `dist/` PRESENT (built
immediately before), invocation `node --test test/merge-preconditions.test.ts`
from the repository root, and **15 tests, 15 pass, 0 fail, 0 SKIPPED**, exit 0.

### Criterion 2: an unreachable API is `error` with units 0

MET, and this is the red witness the repository has paid for twice.

The scratch environment is a REAL CLOSED PORT: a `node:http` server is bound on
loopback, its port read, and the server closed before the gate is pointed at it.
Port 1 was rejected as a fixture because Node answers `bad port` from its
blocked-port list without attempting a connection, which is a different failure.

Shipped gate, closed port: `status` `error`, `units` 0, exit 21, detail naming
the failure. **The assertion is the STATUS WORD, not the detail string**, which
is what the plan demands, because a mutant produces a plausible detail sentence
too.

The mutant is the one the plan names: the request wrapped in a catch that
returns an answer instead of a failure. It is URL-AWARE, because a catch
returning one fixed shape would redden the endpoints it does not fit and report
`red` rather than the green the plan describes; the faithful form of the defect
is a failed request answered with the shape the caller expected. Measured: the
mutant reports **green** on the same closed port. Asserting `notEqual error`
would have been the weaker test and it was rejected in favour of `equal green`.

### Criterion 3: condition 4 green only on the exact head, both directions

MET, ONE FIXTURE PAIR, ONE VARIABLE. Both arms serve the SAME real captured
check-runs response, a genuinely succeeding `gates` run, edited only by
substituting the head sha. Green when `head_sha` equals the head under
evaluation; red when it names an earlier one, with the earlier sha printed.

The dangerous state is the `head_sha` comparison dropped, which is the gate
reading the newest green run on the BRANCH. T-009 one scope down. Measured: the
mutant turns the earlier-head fixture GREEN.

### Criterion 4: condition 5, two arms, two reasons

MET. Scope record reads `red`: row red, reason `reads red, not green`, gate
word `red`. Scope record ABSENT: row red, reason `no scope gate record exists`,
gate word `error`. The test asserts the two row strings are NOT EQUAL, so a
reader can tell a failed audit from an audit that never ran.

Dangerous state: the condition satisfied by the record's EXISTENCE. Measured:
the mutant turns a RED scope record green.

### Criterion 5: existence is not the test, TWO structurally different members

MET. Member one: an arbitration document that EXISTS and names ONE of the two
verdicts, so it has read half the evidence. Member two: one that names BOTH
verdicts and rules about a DIFFERENT head, so it has read the right number of
documents about the wrong commit. Both red, with different reasons.

The presence-only mutant (both the verdict-naming filter and the head-prefix
filter defanged) passes BOTH members, which is what makes them a class rather
than one witness twice.

### Criterion 6: an empty ruleset body is `error`, not a default

MET. The ruleset endpoint answers HTTP 200 with a zero-length body; the row is
`error`, the gate is `error`, and the reason names the emptiness rather than a
parser message.

Dangerous state: the empty body read as a default. The mutant returns the shape
the caller was expecting and the row goes green. That is the silent pass this
whole phase exists against.

### Criterion 7: the ruleset encoding, TWO members

MET. Both members are the REAL captured ruleset detail with one declared
substitution each: `"enforcement": "active"` to `"disabled"`
(present-but-toothless) and `"context": "gates"` to `"build"`
(present-but-wrong). Both red, with different sentences.

The presence-only mutant, which stops comparing the enforcement word and stops
requiring the context, is green on BOTH.

### Criterion 8: the work history quotes the PER-CONDITION rows

MET, in section 7 below. CLAUDE.md:601 is explicit that a green BUNDLE is not
evidence that a PARTICULAR gate asserted anything, and this gate exists to make
the same distinction one scope smaller. No bundle-level line appears anywhere
in this document as evidence about a condition.

## 7. The PER-CONDITION rows, with the head sha

Two runs are quoted. Neither is a bundle line.

### 7a. The gate against THIS repository, head bc792b906c64138bd7a39ae2b8fee993702c27e3

This is the NOT-APPLICABLE arm and it produces no condition rows, which is the
point of quoting it: the repository has no committed verdict naming this head,
so DR-0012's conditions have no subject.

```
merge-preconditions: not-applicable (0 merge preconditions evaluated)
no committed verdict document names head bc792b906c64138bd7a39ae2b8fee993702c27e3,
so no merge is being proposed at this head and DR-0012's conditions have no subject
```

and the record's precondition, verbatim from the written `result.json`:

```
"precondition": {
  "id": "merge-preconditions-verdict-names-this-head",
  "met": false,
  "reason": "no committed verdict document names head bc792b90...",
  "evidence": [
    "0 committed verdict document(s) were read and examined",
    "head under evaluation: bc792b906c64138bd7a39ae2b8fee993702c27e3"
  ]
}
```

### 7b. The gate against a staged context, head dcbe6704813e861736c8d394dca35f7dc31b4f93

Seven rows, each carrying the head, from the green arm of the suite's first
test. This is what the orchestrator reads before a merge.

```
merge-preconditions: green (7 merge preconditions evaluated)
condition-1 (DR-0012:22 two decorrelated clean-room reviews of this head) at dcbe6704813e861736c8d394dca35f7dc31b4f93: green -- dual-review-decorrelation reported no violation over the 2 verdict(s) committed for this head
condition-2 (DR-0012:23 no unresolved finding at medium or above) at dcbe6704813e861736c8d394dca35f7dc31b4f93: green -- verdict-pair-approves reported no violation over the 2 verdict(s) committed for this head
condition-3 (DR-0012:24 the acceptance criteria were the reviewers' contract) at dcbe6704813e861736c8d394dca35f7dc31b4f93: green -- all 2 verdict(s) declare a review-contract (criteria, hazard) and walk at least one acceptance criterion with a recorded met; COMPLETENESS against the plan's acceptance list is the separate check verdict-criteria-complete and is NOT asserted here
condition-4 (DR-0012:25 CI green on the EXACT head) at dcbe6704813e861736c8d394dca35f7dc31b4f93: green -- 1 gates check run(s) concluded success with head_sha equal to the head under evaluation
condition-5 (DR-0012:26 the scope audit passes) at dcbe6704813e861736c8d394dca35f7dc31b4f93: green -- the scope gate record at <staged>/evidence/scope/result.json reads green over 7 unit(s)
condition-6 (DR-0012:27 a recorded arbitration over BOTH verdicts at this head) at dcbe6704813e861736c8d394dca35f7dc31b4f93: green -- the arbitration document <staged>/delivery/review/arbitration-m3-p9.md names all 2 verdict(s) for this head and carries dcbe6704813e861736c8d394dca35f7dc31b4f93, a prefix of the head under evaluation
branch-protection (R-064 the ruleset encoding, R-065a reported as DATA) at dcbe6704813e861736c8d394dca35f7dc31b4f93: green -- main-protection is enforcement active and requires the status check gates (R-064). R-065a DATA, not a verdict: allowed_merge_methods = merge, squash, rebase
```

The only substitution in that block is `<staged>` for the mktemp directory,
which changes on every run and is not a fact about the gate.

## 8. Real captured output, and the transliteration declaration

`witness/captures/m4-p12-github-check-runs.json`,
`witness/captures/m4-p12-github-rulesets-list.json` and
`witness/captures/m4-p12-github-ruleset-detail.json` are verbatim bodies from
the live API, listed in every witness spec's `consumesExternalOutput` block
with their endpoints and date. The test serves those bytes from a loopback
server, so the gate's own fetch, status handling and body parsing run against
what GitHub really sends. A hand-invented body would let the gate pass against
a shape that does not occur, which is what the red-witness rule's stronger form
forbids.

**TRANSLITERATION DECLARATION.** No captured output in this document has been
altered except as follows. The `node --test` reporter glyphs U+2139 and U+2716
do not appear in this document at all, because no verbatim reporter block is
pasted: the suite results are quoted as their numbers and names in prose. The
three API captures are committed as raw bytes and are pure ASCII with no
control characters, measured: zero bytes outside 0x20 to 0x7E plus tab, newline
and carriage return in all three files. Nothing else in any captured output was
changed.

## 9. The witness specs, and why they are specs rather than captures

Nine witness specs ship under `witness/`, each with TWO structurally different
dangerous-state members and each declaring the three API captures in
`consumesExternalOutput`. A capture in a work history is read once by a
reviewer; a spec is re-evaluated by the `red-witness` gate on every later run,
so a future edit that defangs one of these arms reddens rather than passes:

| spec | what it stops being silently removable |
|---|---|
| `merge-preconditions-unreachable-api-is-error` | criterion 2, both the catch-and-continue mutant and the probe skipped entirely |
| `merge-preconditions-ci-green-is-head-scoped` | criterion 3, the head_sha comparison, two spellings of dropping it |
| `merge-preconditions-scope-status-not-existence` | criterion 4, the status read and the instrument-missing distinction |
| `merge-preconditions-arbitration-existence-is-not-the-test` | criterion 5, the verdict-naming half and the head half |
| `merge-preconditions-empty-ruleset-body-is-error` | criterion 6, the empty body defaulted and the empty-body branch never taken |
| `merge-preconditions-ruleset-encoding-compared` | criterion 7, the enforcement word and the required context |
| `merge-preconditions-composed-check-absence-is-error` | a composed check with no registration read as a pass, two spellings |
| `merge-preconditions-composed-check-violations-are-red` | a composed check's violations discarded, and the check never consulted |
| `merge-preconditions-no-verdict-at-head-is-not-applicable` | the one not-applicable arm and the head filter that selects it |
| `merge-preconditions-error-dominates-red` | the gate word, both an ordering swap and a short-circuit |

### The red-witness gate reddened this phase, and what it found is worth stating

The first version of these specs listed TWO named tests on four of them where
only ONE of the two reddens under each member. The gate refused them:

```
gates: red-witness: red: 10 witness(es) evaluated (9 own, 1 stored re-evaluated);
witness merge-preconditions-composed-check-absence-is-error: red:
member 0 (mutation of src/gates/merge-preconditions.ts): no named test reaches this arm
  (line(s) 734, 735, 736; stayed green: a pair of verdicts in which one refuses the
   merge reddens condition 2 while condition 1 stays green)
...
witness merge-preconditions-ruleset-encoding-compared: red:
member 0 ... (line(s) 602; stayed green: R-065a is reported as data on the green arm
  and never turns the branch-protection row red)
```

The mechanism, not the instance: **a witness member is red only when EVERY test
it names fails**, src/witness/run.ts:932 reading
`red: exitCode !== 0 && failed.length === tests.length`. So naming a second test
that the member cannot reach does not strengthen a witness, it DISARMS it.
Naming the extra test felt like more coverage and was the opposite.

The fix is mechanical rather than a judgement: every spec now names only tests
that redden under EVERY one of its members, and the composed-check spec split
into two, one per arm, because its two arms genuinely reach different tests.

**The derivation, and its output, because a fix that addressed only the four
the gate named would be fixing the instance.** Every member of every spec was
re-measured the way the gate measures them, by mutating the shipped file,
running exactly the tests the spec names, and requiring a nonzero exit with
every named test failing:

```
$ node <scratch>/probe.mjs
RED-OK  merge-preconditions-arbitration-existence-is-not-the-test member 0 exit=1 failed=1/1
RED-OK  merge-preconditions-arbitration-existence-is-not-the-test member 1 exit=1 failed=1/1
RED-OK  merge-preconditions-ci-green-is-head-scoped member 0 exit=1 failed=2/2
RED-OK  merge-preconditions-ci-green-is-head-scoped member 1 exit=1 failed=2/2
RED-OK  merge-preconditions-composed-check-absence-is-error member 0 exit=1 failed=1/1
RED-OK  merge-preconditions-composed-check-absence-is-error member 1 exit=1 failed=1/1
RED-OK  merge-preconditions-composed-check-violations-are-red member 0 exit=1 failed=1/1
RED-OK  merge-preconditions-composed-check-violations-are-red member 1 exit=1 failed=1/1
RED-OK  merge-preconditions-empty-ruleset-body-is-error member 0 exit=1 failed=2/2
RED-OK  merge-preconditions-empty-ruleset-body-is-error member 1 exit=1 failed=2/2
RED-OK  merge-preconditions-error-dominates-red member 0 exit=1 failed=1/1
RED-OK  merge-preconditions-error-dominates-red member 1 exit=1 failed=1/1
RED-OK  merge-preconditions-no-verdict-at-head-is-not-applicable member 0 exit=1 failed=1/1
RED-OK  merge-preconditions-no-verdict-at-head-is-not-applicable member 1 exit=1 failed=1/1
RED-OK  merge-preconditions-ruleset-encoding-compared member 0 exit=1 failed=1/1
RED-OK  merge-preconditions-ruleset-encoding-compared member 1 exit=1 failed=1/1
RED-OK  merge-preconditions-scope-status-not-existence member 0 exit=1 failed=1/1
RED-OK  merge-preconditions-scope-status-not-existence member 1 exit=1 failed=1/1
RED-OK  merge-preconditions-unreachable-api-is-error member 0 exit=1 failed=1/1
RED-OK  merge-preconditions-unreachable-api-is-error member 1 exit=1 failed=1/1
problems: 0
```

That is the CAPTURED RED for all twenty members: ten specs, two structurally
different members each, every one demonstrated red against the mutant and
green at the restored head (the suite result in criterion 1 is that green arm).

**WHAT THE DERIVATION DID NOT COVER.** It enumerates every member of every
spec whose id begins `merge-preconditions-`, which is this phase's own set. It
does NOT re-derive the stored witnesses the gate also re-evaluates because
their members touch a file this phase changed; the gate reported one such
(`cutover-behaviors-resolve-by-name`, whose members touch
`test/behaviors.json`) and it was green. It also does not check rules (a) to
(g) of the spec contract, which are the gate's job and not this probe's.

## 10. The claim greps, with every hit settled

Both forms were run and their VERBATIM output is section 12, appended last so
that pasting it shifts no line number in this document. What follows settles
every hit, by content rather than by line number, because a line number here
moves whenever a sentence above it does.

**THIS DOCUMENT IS SELF-REFERENTIAL AND THAT IS STATED RATHER THAN HIDDEN.**
Sections 10 and 12 quote the grep's own alternation and discuss the hits, so
they contain every phrase in the vocabulary by construction. Section 12 records
the fixed-point check that separates those from real claims: after appending
the capture, the grep was run again and EVERY new hit fell inside the appended
block.

The hits outside sections 10 and 12, and what settles each:

1. "The gate has never run non-vacuously on this repository's own head ... and
   it structurally cannot until a phase commits two verdict documents naming
   its head". Two captured commands. The corpus:

   ```
   $ grep -rln '^kind: verdict' delivery/
   delivery/evidence/m3-exit-test/e1/e1-7/verdict-hazard.yaml
   delivery/evidence/m3-exit-test/e1/e1-7/verdict-criteria.yaml
   ```

   and the gate's own declared precondition, run from the repository root:

   ```
   $ node scripts/check-dual-review.mjs --precondition .
   check-dual-review: 0 verdict document(s) (corpus: delivery/review read from
   commit bc792b906c64138bd7a39ae2b8fee993702c27e3, resolved from HEAD)
   ; exit 1
   ```

   The word "structurally" is a claim about the code and not only about today:
   the head filter selects verdicts whose `head` field equals the head under
   evaluation, so a corpus with none reaches the not-applicable arm, and
   section 11 quotes the bundle reporting exactly that arm.

2. "The gate word and the row words are not always the same." Restated as a
   located fact: the divergence is at ONE place only, the absent-scope-record
   arm, and the test "condition 5 is red when the scope record reads red and
   red with a different reason when no record exists, and the gate word is
   error on the absent arm" asserts both words in one run.

3. "with the cause never established" (section 2). A QUOTATION of CLAUDE.md's
   standing warning 6, not my claim. What I measured myself is the probe table
   beside it.

4. "never turns the row red for it" (R-065a). Settled by the test "R-065a is
   reported as data on the green arm and never turns the branch-protection row
   red", which serves the UNEDITED capture, in which `allowed_merge_methods`
   carries all three, and asserts the row is green.

5. "cannot be determined" (section 3). Describes what the shipped primitive
   `missingRegimeDocument` at src/checks.ts:4123 answers. Not a claim of mine.

6. "Never edited by hand" (section 5, the rendered blocks). Settled by the
   `--check` exit 0 in section 11, and by the shipped test "adding a gate to
   the registry without re-rendering makes --check exit nonzero naming the
   added gate, and re-rendering returns exit 0", which is what makes the
   guarantee falsifiable rather than asserted.

7. "a registry-only gate is declared and never executed", twice in section 5.
   Settled by the shipped test at test/gate-registry.test.ts:1142, which
   asserts that `scripts/m2-exit-test.sh` contains no `--registry`. I did not
   re-derive that assertion; I read it and relied on it, and that reliance is
   what this line records.

8. "an audit that never ran" (criterion 4). Settled by the assertion
   `assert.notEqual(redRow, absentRow)` in that test, which is what makes the
   two reasons distinguishable rather than a claim that they are.

9. "the empty-body branch never taken" and "the check never consulted"
   (section 9's table). These NAME the two mutations of those specs, and
   section 9's own derivation output is the capture that settles them: both
   members print `RED-OK ... failed=N/N`.

10. "and never turns the branch-protection row red" inside section 9's quoted
    gate output. That is the gate's own text, quoted verbatim, not a sentence
    of mine.

## 11. The gate bundle, quoted verbatim

Run LAST, after every edit was committed, at head
4931ddfe4558c73df86523c5f4037d0da4b60d13, `--mode direct-pr --base origin/main
--head HEAD --phase m4-p12`. The `--phase` is load-bearing: without it the scope
gate reports `error`, a third state that reads like neither green nor red.

**A CAPTURE OF A BUNDLE CANNOT SIT AT THE HEAD IT DESCRIBES, and pretending
otherwise is how a quoted green stops being about anything.** Committing this
paragraph makes a new head, so the run quoted below is the one at
4931ddfe4558c73df86523c5f4037d0da4b60d13 and the head carrying this document is
its child. The ONLY difference between them is this file:
`git diff --name-only 4931ddf HEAD` prints `delivery/work-history/m4-p12.md`
and nothing else. The bundle was re-run at the child head to confirm the
summary line is identical but for the sha, and that confirmation is in the
implementer's report rather than pasted here, because pasting it would start
the same regress again.

The summary line:

```
gates: declared 17 applicable 13 verdict 13 green 13 red 0 not-applicable 4 error 0 vacuous 0
```

The five lines the brief asks for, verbatim, with the absolute scratch prefix
shortened to `<clone>` where it appears:

```
gates: scope: green: 23 changed path(s) audited against declaration delivery/plan/phase-declarations/m4-p12.json at merge base bc792b906c64138bd7a39ae2b8fee993702c27e3 (sha256 ec1067c6acfd45aa1a431f9c095bb173a083d80196426f7de7b03da2c36a9811) (1 declared path(s) not touched: src/cli.ts) DECLARATION AMENDED AT HEAD: 1 entry/entries ADDED at head 4931ddfe4558c73df86523c5f4037d0da4b60d13 that are absent from the merge-base declaration, allowed and NAMED here for a reviewer to sign off (this gate does not sign them off): filesToTouch scripts/m2-exit-test.sh.
gates: suite: green: suite green via tiphys-suite-events-v1 (child node v26.6.0): reported 1275 test(s) from 64 file(s) (pass 1275, fail 0, skipped 0, todo 0, did-not-run 0); discovered 64 file(s) walking test for .test.ts; 1151 behavior(s) resolve; merge base bc792b906c64
gates: typecheck: green: tsc -b tsconfig.src.json tsconfig.test.json plugin/tsconfig.json --force --listFiles exited 0 and reported 442 distinct file(s); the unit count is those printed paths, not a constant
gates: gate-classes: green: phase M4-P12 declares all 3 required gate class(es) in delivery/plan/phase-declarations/m4-p12.json: correctness: asserted by suite, typecheck, red-witness; scope: asserted by scope; review: asserted by check-dual-review
gates: red-witness: green: 11 witness(es) evaluated (10 own, 1 stored re-evaluated in 7813ms); every witness red against every declared dangerous state and green at head
```

The declaration amendment is PRINTED BY NAME on the green arm, which is what
section 5 asks a reviewer to sign off: `filesToTouch scripts/m2-exit-test.sh`.
The gate says explicitly that it does not sign it off itself.

**THE BUNDLE'S EXIT CODE IS 20, NOT 0, AND THAT IS NOT A RED.** The last line
is:

```
gates: required gate(s) not applicable: citations
```

`citations` is `required` and reported not-applicable with an EVALUATED
precondition, `citations-diff-touches-a-configured-document met: false`, reason
"no changed path under the configured documents globs (23 changed path(s)
total)". 20 is `EXIT_NOT_APPLICABLE` at src/gates/result.ts:67. This is the
same shape CLAUDE.md:601's worked example records, where the not-applicable
line named only `citations`, and `scripts/m2-exit-test.sh` lists `citations` as
`green|not-applicable`, diff-scoped, which is what the CI harness accepts. It
is stated here rather than reported as a green, because reporting exit 20 as
exit 0 would be the same substitution this whole phase exists against.

**THIS GATE'S OWN ROW, and it is the honest one.**

```
gates: merge-preconditions: not-applicable: precondition merge-preconditions-verdicts-present evaluated and unmet: node scripts/check-dual-review.mjs --precondition . exited 1
```

Its record carries `precondition {id: merge-preconditions-verdicts-present,
met: false, reason: ...}`, which is what makes a not-applicable an assertion
that something was evaluated rather than a silence (SC-011), and what the exit
harness's leg 4 requires. The gate has not yet run non-vacuously on this
repository, for the reason in item 7 of the did-not-cover section.

**THE THREE PREFLIGHT STEPS, separately.** `npm ci` exit 0, `npm run build`
exit 0, `git status --short` EMPTY afterwards, and
`node scripts/check-authored-bytes.mjs` exit 0.

Both drift checks, run before pushing:

```
$ node scripts/render-agent-rules-gates.mjs --check
agent-rules-drift: green (24 rendered gate rows compared)
CLAUDE.md's gate block matches gate-registry.yaml row for row (3 preflight step(s), 21 gate(s))
; exit 0

$ node scripts/check-brief-drift.mjs --check
brief-drift: green (21 generated brief gate rows compared)
roles/implementer.md's full gate block matches gate-registry.yaml row for row (21 row(s) compared)
; exit 0
```

## 12. The claim-grep output, verbatim

Appended LAST, so pasting it shifts no line number above it.

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m4-p12.md
64:7. **The gate has never run non-vacuously on this repository's own head**, and
91:The gate word and the row words are not always the same, and the one place
104:unreachable on 2026-08-13 and reachable on 2026-08-14, with the cause never
150:the gate reports `error`. That is the fail-closed direction and never a silent
169:green arm and never turns the row red for it, because it is an owner action the
181:  delivery regime cannot be determined,
218:   `node scripts/render-agent-rules-gates.mjs --write`. Never edited by hand.
223:   with `--manifest` and a registry-only gate is declared and never executed.
248:gate never executes, which the registry's own header records inside
303:reader can tell a failed audit from an audit that never ran.
430:| `merge-preconditions-empty-ruleset-body-is-error` | criterion 6, the empty body defaulted 
433:| `merge-preconditions-composed-check-violations-are-red` | a composed check's violations di
451:  and never turns the branch-protection row red)
512:moves whenever a sentence above it does.
523:1. "The gate has never run non-vacuously on this repository's own head ... and
547:2. "The gate word and the row words are not always the same." Restated as a
553:3. "with the cause never established" (section 2). A QUOTATION of CLAUDE.md's
557:4. "never turns the row red for it" (R-065a). Settled by the test "R-065a is
558:   reported as data on the green arm and never turns the branch-protection row
562:5. "cannot be determined" (section 3). Describes what the shipped primitive
565:6. "Never edited by hand" (section 5, the rendered blocks). Settled by the
571:7. "a registry-only gate is declared and never executed", twice in section 5.
577:8. "an audit that never ran" (criterion 4). Settled by the assertion
581:9. "the empty-body branch never taken" and "the check never consulted"
586:10. "and never turns the branch-protection row red" inside section 9's quoted
670:$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|alw
```

The wrap-insensitive form, run because this prose is hard-wrapped by hand and
the command above is line-based, counting OCCURRENCES so the two numbers are
comparable:

```
$ tr '\n' ' ' < delivery/work-history/m4-p12.md \
  | grep -oEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' \
  | wc -l
80
```

and the line-based form counted the same way, which is the comparison the
rule actually asks for:

```
$ grep -oEi '...' delivery/work-history/m4-p12.md | wc -l
80
```

**THE FIXED-POINT CHECK, which is what makes a self-referential capture
auditable.** The capture above was taken against the document as it stood
BEFORE this section was appended, so it cannot list the hits inside this
section. Re-running both forms on the FINAL document, the one a reviewer holds:

| form | occurrences on the final document |
|---|---|
| wrap-insensitive (`tr` then `grep -oEi`) | 80 |
| line-based, counted as occurrences (`grep -oEi`) | 80 |

The two numbers are EQUAL, so nothing in this document is hidden from the
line-based command by a hard wrap, which is the only property the second form
exists to establish. And every hit the re-run reports that the capture above
does not is inside this section: 25 hits at or below line 680, which is where
this section begins, and 26 inside it, all of them the quoted alternation, the
quoted hit list and this table. No claim of mine lives in this section, and the
capture above is the one taken immediately before it was appended.

