# M4-P3 work history: the request contract widens, and executor.json gets a schema

Branch `claude/m4-p3-request-contract`, cut from `main` at 6980fa2 (M4-P1
remainder, pull request #165). Plan section: delivery/plan/kernel-plan-m4.md:456.

## What this phase changed, and why

`ExecutorRequest` had seven fields and none of them was the brief, although the
brief is an agent payload's entire input and the call site already held its
path. `ExecutorRecord` had three fields and was validated by nothing. This
phase adds the four fields M4-D-05 names, gives the record a schema, and adds
the mechanism that turns a missing field into a REFUSAL rather than an
`undefined` an adapter reads silently.

The mechanism is the part that is not just four fields. Adding optional fields
creates a new dangerous state: an adapter that needs the brief path, gets
`undefined`, and launches an agent with no input. The kernel cannot fix that
per adapter, so the ADAPTER declares what it needs and the kernel refuses
BEFORE it creates anything. The precedent is already in this repository and it
is exact: the `--deadline` parse-time rejection at src/commands/spawn.ts:104
carries the comment "a usage error creates nothing", written after an
unrepresentable value used to raise INSIDE the adapter once a worktree, a
branch and a pool record existed.

### Files touched

| File | What |
|---|---|
| src/spawn.ts | four request fields, `requires` on the adapter, two request-time checks, two record fields |
| src/commands/spawn.ts | `--role`, `--tier`, `--phase`, and the usage string |
| schemas/executor-record.schema.json | created |
| src/commands/validate.ts | the `executor-record` type row |
| test/spawn.test.ts | six new tests, plus `requires: []` on ten existing test adapters |
| test/schemas.test.ts | two new tests |
| test/behaviors.json | seven rows |
| witness/ | six specs and two captures |
| delivery/plan/phase-declarations/m4-p3.json | created (see DEVIATION 1) |
| package.json | **NOT TOUCHED**, and that is the right outcome (criterion, below) |

### The design decisions that are invisible in the diff

**`briefPath` is not optional and the three others are.** src/spawn.ts:103.
`assembleBrief` has already produced the path by the time the request is built,
so an optional `briefPath` would be an optionality the kernel never exercises:
every production path supplies it, and no test could construct the absent case
without inventing one. That is a field that cannot go red. `role`,
`declaredTier` and `phaseId` are `string | undefined` for the opposite reason:
nothing in the kernel produces them.

**One source for the requirable field names.** `requestFieldPresence` in
src/spawn.ts answers, for every field, whether the kernel can fail to supply
it, and `requirableRequestFields` at src/spawn.ts:165 reads the NAME list back
out of the same function. A list of legal names maintained beside a separate
list of presence tests is two things that drift, silently, in the direction
that matters. A registered test then derives the comparison from the request an
adapter is ACTUALLY handed, so a field added to `ExecutorRequest` and not to
the presence map reddens.

**The order of the two checks is load-bearing, and the code says so.**
src/spawn.ts:196 answers "is this a field at all"; src/spawn.ts:242 answers "is
this field absent". An unknown key is also absent, so a single check written as
`request[name] === undefined` would answer an adapter DEFECT with a message
about a missing flag, sending an operator to look for a `--modelName` the
kernel can never accept. The contract check therefore runs first, and one of
the red witnesses is the ORDER SWAP rather than a deletion.

**A non-array `requires` is refused rather than raised.** From M4-P4 the
adapter is resolved from the fleet home, so `requires` arrives from a module
the kernel did not write and the TYPE IS A PROMISE. `adapter.requires.filter`
on `undefined` would be a TypeError raised out of `spawnTask` with no reason
line, which is the same "refusal arriving as a crash" shape the check exists to
prevent, one level lower. This is a content-level addition the plan does not
name; it is declared as DEVIATION 4.

**No resolved model in the launch record, and no `phaseId` either.** The record
is written BEFORE the payload starts, which is the whole basis of the
launch-failed-versus-incomplete distinction, while a harness that requests one
model and is served another resolves mid-turn. A resolved model here would be a
value nobody could have observed at the moment it was written. M4-P7 carries
the resolved half. The record's key set is pinned by a registered test, so a
field arriving here reddens.

**The tier and the role cross verbatim.** The kernel holds no tier vocabulary
and no role vocabulary, so there is nothing here it could legitimately validate
against; a kernel that normalised either value would be holding one. The test
uses `Strongest-Available` and `clean-room-Reviewer`, mixed case and hyphens,
for exactly that reason.

## Acceptance criteria, walked

### Criterion 1: the four request fields

`briefPath: string` at src/spawn.ts:103, then `role`, `declaredTier` and
`phaseId`, each `string | undefined`. Walked by the test
"the executor request carries the assembled brief path, and the adapter reads a
real file at it", which asserts the file AT the path (not the path's shape),
asserts it is not the hook path, and asserts the three optional values cross
verbatim. PASS.

### Criterion 2: a declared requirement that is unmet creates nothing

`readonly requires: readonly string[]` at src/spawn.ts:294. The test
"an adapter requirement the spawn cannot meet refuses before a worktree, a
branch, a task directory or a pool record exists" asserts all four
post-conditions, because exiting nonzero is compatible with having created
three of them, plus a fifth (the adapter was never reached). The other
direction is in the same test: the same declaration with `role` supplied
reaches launch. PASS.

Two of the four post-conditions are not files. They are `git worktree list` and
`git branch --list`, so they are asserted against real captured output at
witness/captures/spawn-requirement-refusal-git-state.txt:1 rather than against
a hand-written string. Two details in that capture would have been got wrong by
hand, and both are recorded there: `git worktree list` RE-PADS its first column
when a longer path joins, and a branch checked out in another worktree is
printed by `git branch --list` with a `+` marker rather than `*` or a bare
indent.

### Criterion 3: an unknown field is refused at load, not at launch

The test "an adapter requiring a field the request contract has no name for is
refused as a contract defect, not as a missing value" asserts that the two
refusals SAY DIFFERENT THINGS, in both directions: the unknown-field reason
must not contain "supplied no value" and the absent-value reason must not
contain "has no field for". A conflated implementation exits nonzero on both
and is red here. The refusal also names the requirable fields, so the adapter
author can fix the defect from the message. PASS.

### Criterion 4: the record validates through the CLI

The test
"the launch record a real spawn wrote validates under --type executor-record,
one missing adapter is refused naming the field, and --type auto cannot resolve
it" runs a real `tiphys spawn` and validates the record THAT SPAWN produced,
never a fixture. Exit 0 on the real record, exit 1 with
`INVALID #/adapter required property adapter is missing` on a copy with the
field deleted. The wording is read out of
witness/captures/executor-record-validate-cli.txt:1 rather than retyped. PASS.

### Criterion 5: Kind A dangerous-instance on additionalProperties

The test "an executor record carrying an undeclared model field is refused, and
is accepted once additionalProperties is removed" lifts the REAL record out of
the capture, asserts `INVALID #/model property model is not permitted here`,
then deletes the keyword from the schema object and asserts the SAME instance
becomes accepted. That is the dangerous state, and it is a future one: nothing
in the document knows the word `model`, so `additionalProperties: false` is the
whole of what stops a later adapter smuggling a vendor model name into the
launch record. PASS.

### Criterion 6: requestedTier and requestedRole, byte for byte

Walked THROUGH THE CLI deliberately, because the value travels from a flag,
through the parser, through `SpawnOptions`, through the request and into the
record, and a test that handed `spawnTask` the values directly would leave the
first two steps unwitnessed. The measured defect shape is a flag that parses
and whose value is dropped, which is red-witness member 0 of
`executor-record-echoes-requested-tier`. The record's key set is asserted, so
a resolved model arriving here reddens. PASS.

### Criterion 7: the vendor-token grep, as a test

Implemented, and DEVIATION 2 below records that the plan's text for this
criterion carries an unfilled placeholder and a number that no token list
reproduces. The test
"no vendor model name appears in the kernel's shipped source, schemas, roles or
configuration" derives the file set at run time from `src/`, `bin/`, `schemas/`,
`roles/` and the root `*.yaml`, matches a SINGLE declared constant of eleven
tokens (`VENDOR_MODEL_TOKENS` in test/schemas.test.ts), and asserts the hit set
as a NAMED list rather than as a count. It carries a non-vacuity control in
both directions, because an empty result and a broken matcher are the same
observation. PASS, with the deviation declared.

### Criterion 8: every pre-existing spawn behavior still resolves by name

The test "every spawn behavior in the registry still resolves by name to a test
title" derives the `spawn-` ids from the registry at run time and searches
EVERY test file. Searching only `test/spawn.test.ts` would have reported three
behaviors missing: `spawn-child-env-scrubbed`, `spawn-child-env-allowlist` and
`spawn-allow-pr-credentials-passthrough` are implemented in
test/credentials-gate.test.ts. No number is pinned. PASS.

### Criterion 9: build, clean status, suite

Recorded in the gate section below.

### The package.json criterion, which is a VERIFICATION and not an edit

The plan says `schemas` is already in `files` at package.json:17 and then says
to VERIFY this and record the check rather than assuming it. Measured:

```
$ node -e 'const p=JSON.parse(require("fs").readFileSync("package.json","utf8"));
  const has=Array.isArray(p.files)&&p.files.includes("schemas");
  console.log(has, p.files.indexOf("schemas")); process.exit(has?0:1)'
true 10
$ echo $?
0
```

The `files` array opens at package.json:17 and `"schemas"` is its eleventh
entry, at package.json:28. The plan's line number names the array, not the
entry; the claim is true and the location is one line off from what a reader
would expect, so it is recorded here rather than silently accepted.

The stronger check, because membership in `files` is a claim about a TARBALL:

```
$ npm pack --dry-run --json | node -e '...filter(p=>p.startsWith("schemas/"))'
schemas/ entries in the tarball: 18
executor-record present: true
```

package.json is byte-identical to `main`. That is the right outcome.

## Red witnesses

Six witness specs, twelve members, every one measured RED against the named
test with the head GREEN. The harness used is the same find-and-replace the
red-witness gate applies, run per member with the file restored afterwards.

```
### spawn-request-carries-brief-path  -> test/spawn.test.ts
  HEAD (unmutated): exit 0 ok=1 notok=0
  member 0 (src/spawn.ts): exit 1 ok=0 notok=1 -> RED
  member 1 (src/spawn.ts): exit 1 ok=0 notok=1 -> RED

### spawn-adapter-requirement-unmet-creates-nothing  -> test/spawn.test.ts
  HEAD (unmutated): exit 0 ok=1 notok=0
  member 0 (src/spawn.ts): exit 1 ok=0 notok=1 -> RED
  member 1 (src/spawn.ts): exit 1 ok=0 notok=1 -> RED

### spawn-adapter-requirement-unknown-field-refused  -> test/spawn.test.ts
  HEAD (unmutated): exit 0 ok=1 notok=0
  member 0 (src/spawn.ts): exit 1 ok=0 notok=1 -> RED
  member 1 (src/spawn.ts): exit 1 ok=0 notok=1 -> RED

### executor-record-echoes-requested-tier  -> test/spawn.test.ts
  HEAD (unmutated): exit 0 ok=1 notok=0
  member 0 (src/commands/spawn.ts): exit 1 ok=0 notok=1 -> RED
  member 1 (src/spawn.ts): exit 1 ok=0 notok=1 -> RED

### executor-record-validates  -> test/spawn.test.ts
  HEAD (unmutated): exit 0 ok=1 notok=0
  member 0 (src/commands/validate.ts): exit 1 ok=0 notok=1 -> RED
  member 1 (src/commands/validate.ts): exit 1 ok=0 notok=1 -> RED

### kernel-carries-no-vendor-model-names  -> test/schemas.test.ts
  HEAD (unmutated): exit 0 ok=1 notok=0
  member 0 (src/commands/spawn.ts): exit 1 ok=0 notok=1 -> RED
  member 1 (schemas/executor-record.schema.json): exit 1 ok=0 notok=1 -> RED
```

### The members are dangerous states, not absent features

Each pair is two structurally different members of one class, which is the
"one witness is not a class" rule:

| witness | member 0 | member 1 |
|---|---|---|
| brief path | the path exists and is the WRONG FILE (the hook path), which a test asserting only `typeof` or `existsSync` would pass | the field is omitted, which is the `undefined` an adapter would launch on |
| unmet requirement | the derivation is emptied, so the check runs and finds nothing | the derivation is intact and the refusal is downgraded to a stderr warning, so the kernel knows and proceeds |
| unknown field | the contract check is deleted, so an unknown name is reported as a missing value | both checks remain and their ORDER IS SWAPPED, which destroys the distinction while both still refuse |
| echoed tier | the flag parses and its value is dropped at the CLI boundary | the request carries the value and the adapter does not copy it into the record |
| record validates | the type row is removed, so the type is unnameable | the row remains and points at another shipped schema, so the type is nameable and means the wrong thing |
| vendor names | a vendor token enters a source comment | a vendor token enters the shipped schema as an enum value |

The seventh behavior, `executor-record-rejects-undeclared-fields`, carries its
red witness INSIDE its own test rather than as a spec: the test removes
`additionalProperties` from the schema object and asserts that the same
smuggled record becomes ACCEPTED, then restores it. That is the Kind A
dangerous-instance form test/schemas.test.ts already uses for the four M3-P1
instances, and a spec-level mutation would be a second copy of it.

### Captured output, and the transliteration question

Both captures are real program output, taken on node v26.6.0 in scratch fleets
built by the same helpers test/spawn.test.ts uses.
witness/captures/spawn-requirement-refusal-git-state.txt:1 carries git's own
two listings; witness/captures/executor-record-validate-cli.txt:1 carries the
record `subprocessAdapter` wrote and four real arms of `tiphys validate`.
NOTHING IN EITHER CAPTURE WAS TRANSLITERATED: both are pure ASCII as produced.
The only substitutions are the mkdtemp scratch prefix, rendered `<tmp>` or
`<path>`, and both captures say so at the point of substitution. No node test
reporter output is pasted anywhere in this document, so the U+2139 and U+2716
collision CLAUDE.md records does not arise here; the TAP counts quoted above
are `ok`/`not ok` lines, which are ASCII.

## The pre-submit hazard self-review

The hazard class: **a widened contract whose new fields are read as
`undefined` and proceed**, **a refusal that arrives after state exists**, and
**a vendor name entering `src/` and closing off every other harness**. What was
attacked, including the attacks that found nothing:

**"Can this pass while the thing it guards is broken?"** The unchanged-state
assertions in criterion 2's test compare two readings of the same two git
listings. A kernel that created nothing, ever, would satisfy them. That is a
guard that cannot go red, so the test carries a control arm: a SUCCESSFUL spawn
in the same test, whose listings must DIFFER from the baseline in the exact
shapes the capture records. This was found by asking the question, not by a
failure.

**A non-regular path at `briefPath`.** The brief path is produced by
`assembleBrief`, which the kernel owns, and the adapter reads it. This phase
does not add a read of an operator-supplied path. The test reads the file with
`readFileSync`, which is a test reading a file the test's own spawn created.
Nothing found.

**The adapter declaration as foreign input.** Found something, and it is
DEVIATION 4: `adapter.requires.filter` on a missing declaration is a TypeError,
not a refusal. Fixed before submitting.

**Requiring a field that is always supplied.** An adapter may declare
`requires: ["taskId"]`, which can never be unmet. That is not a defect: it is a
truthful declaration that happens to be always satisfiable, and the brief-path
witness's adapter declares exactly `requires: ["briefPath"]` for that reason.
What it means is that `requires` on a kernel-supplied field is documentation
rather than a guard, which is stated at the presence map rather than left for a
reader to discover.

**Could the two checks be reordered without any test noticing?** No: that is
witness member 1 of `spawn-adapter-requirement-unknown-field-refused`, measured
red above.

**Could a resolved model be added to the record without a test noticing?** No:
the key set is asserted with `assert.deepEqual` in criterion 6's test.

**Does the vendor-token test cover `bin/`?** Yes, `bin/` is walked. It does NOT
cover `templates/`, `checklists/`, `scripts/`, `test/` or `delivery/`, which is
stated in the non-coverage section rather than implied.

## WHAT THIS WORK DID NOT COVER

A reviewer's first check.

1. **The CLI cannot select an adapter, so criterion 2's literal sentence was
   not executed.** The criterion says an adapter declaring `requires: ["role"]`
   spawned without `--role` makes `tiphys spawn` exit nonzero. `tiphys spawn`
   reaches exactly one adapter until M4-P4 ships `--adapter`, and the shipped
   one declares `requires: []`. Both requirement tests therefore drive
   `spawnTask` through the `ExecutorAdapter` seam, which is the same seam every
   M4-P2 criterion used and for the reason that file's own section comment
   gives. What is NOT covered is the CLI's exit code and stderr on this path.
   DEVIATION 3.
2. **The vendor-token derivation covers five trees and no others.** `src/`,
   `bin/`, `schemas/`, `roles/` and root-level `*.yaml`. It does not read
   `templates/`, `checklists/`, `scripts/`, `tuition/`, `test/` or `delivery/`.
   The intake's property is about the shipped kernel surface, and `test/` in
   particular MUST be excluded: this phase's own witness spec
   witness/kernel-carries-no-vendor-model-names.json contains vendor tokens by
   construction, as the mutation text.
3. **`claude` is not in the token list, deliberately.** The intake states that
   the wider zero-vendor-NAMES property is NOT true and names two shipped
   schemas carrying a vendor-derived branch prefix. A list including `claude`
   would assert a property this repository does not have. The branch prefix has
   its own baseline in the same test, named file by file, whose job is to stop
   the set growing rather than to empty it. What is NOT covered: a vendor name
   that is neither one of the eleven tokens nor the branch prefix, for example
   a new vendor whose model family nobody has heard of yet. The constant is the
   place to add it.
4. **The schema is not exercised against a record any adapter other than
   `subprocessAdapter` wrote,** because no other adapter exists. The first one
   that does is M4-P5's plugin adapter, and nothing here forces it to write a
   record this schema accepts: the kernel does not validate `executor.json`
   after an adapter writes it. That is a real gap and it is not this phase's to
   close; the plan puts validation in the operator's hands (`tiphys validate`),
   not in the launch path.
5. **`--type auto` on an executor record is a usage error, not a resolution.**
   An executor record carries no `kind` field, because criterion 6 fixes the
   record's fields and `kind` is not among them. The type must be NAMED. This
   is asserted rather than left in a comment, so a later phase that gives the
   record a `kind` reddens. See DEVIATION 5 for why the alternative was
   rejected.
6. **No CI arm was observed.** Everything above is local, on the toolchain
   named below. The `push`-event arm of the `gates` workflow runs a different
   bundle from the `pull_request` arm (T-009) and neither was witnessed by this
   implementer.
7. **The scope gate cannot pass on this branch as things stand,** because the
   declaration is not on `main` at the merge base. DEVIATION 1.
8. **Concurrency, races and the watcher are untouched.** This phase adds no
   lease, no retry and no wait. The `requires` checks are pure functions over
   an options object and an adapter object, evaluated once, before any I/O.
9. **The three new flags are not validated against any vocabulary,** by
   design. `--tier bananas` is accepted and recorded. A kernel that refused it
   would be holding the tier vocabulary the plugin owns. What is NOT covered is
   therefore a caller who misspells a tier: nothing in the kernel will tell
   them, and M4-P7 is where a mapping could.

## Deviations, declared

**DEVIATION 1: the phase declaration did not exist, anywhere.** The dispatch
brief said `delivery/plan/phase-declarations/m4-p3.json` was already on `main`
and told me not to create it. It is not on `main` and
`git log --all --oneline -- delivery/plan/phase-declarations/m4-p3.json`
returns nothing, so it has never existed on any branch, including
`origin/claude/m4-declarations-to-main`, which carries eleven M4 declarations
and not this one. The PLAN lists it as files-to-touch (create), so I created it
from the plan's own files-to-touch list. The scope gate reads the declaration
from the MERGE BASE (src/gates/scope.ts:854) and a declaration missing there is
RED with "the declaration must be committed to main before the phase branch is
created", so this branch's `scope` gate is red until the orchestrator lands the
declaration on `main`. Additions at head are allowed and reported as a named
diff since M3-P11, so the two copies need not be identical, but the file must
EXIST at the merge base.

**DEVIATION 2: criterion 7 cites a token list the intake does not contain, and
a count that no list reproduces.** The plan's criterion 7 text is literally
`grep -rniE '<the eleven vendor tokens named in the intake>'` with the angle
brackets, and delivery/plan/m4-intake.md names no list of eleven tokens; the
only vendor names in that document are `Codex` and `Cursor` in one sentence at
delivery/plan/m4-intake.md:377. The criterion also says the grep "returns the
same four hits as before this phase". Measured on `main` at 6980fa2:

| token set | hits in `src/ bin/ schemas/ roles/ *.yaml` |
|---|---|
| eleven vendor MODEL tokens (opus, sonnet, haiku, gpt, codex, gemini, llama, mistral, grok, deepseek, qwen) | **0** |
| `claude`, case-insensitive | 39 (mostly the filename `CLAUDE.md`) |
| `claude`, lowercase only | 7 |
| `claude/m`, the branch prefix, all five trees | 5 |
| `claude/m` excluding root `*.yaml` | 4 |

Only the last row gives four, and it is the vendor-derived BRANCH PREFIX, which
delivery/plan/m4-intake.md:367 explicitly says the zero-vendor-names property
does NOT cover. I implemented the criterion's intent with a declared constant
and asserted the hit set BY NAME, and added the branch-prefix baseline
separately so that "the same hits as before this phase" is enforced for the
thing that actually has hits. The orchestrator should decide whether the plan
text needs a revision; I did not improvise a different criterion, and I did not
pin the number four.

**DEVIATION 3: the requirement tests drive `spawnTask`, not `tiphys spawn`.**
See non-coverage item 1. Criterion 2's literal sentence is not executable in
this phase.

**DEVIATION 4: a content-level addition the plan does not name.**
`checkAdapterContract` refuses an adapter whose `requires` is not an array.
The plan asks for a refusal on an unknown FIELD NAME; a missing declaration is
one step lower and would otherwise be a TypeError raised out of `spawnTask`
with no reason line. Two lines of code, covered by an assertion in criterion
3's test.

**DEVIATION 5: the type row is in `src/commands/validate.ts`, and there is no
second registration site.** The plan's files-to-touch names `src/validate.ts`
for the row, and `src/validate.ts` holds no type list at all: `TYPE_TABLE` and
`resolveAutoType` are both in src/commands/validate.ts, at :129 for the new
row. Every prior phase that registered a type (M3-P1, M3-P2, M3-P3) listed BOTH
files in its declaration; the M4-P3 plan section lists only the first. I have
added `src/commands/validate.ts` to the declaration I wrote.

On registering "in BOTH places": `resolveAutoType` answers `--type auto` by
reading the instance's `kind` and looking it UP IN `TYPE_TABLE`, so the row is
the auto resolver's registration as well; there is no second list to edit. The
residue is that an executor record carries no `kind`, so `--type auto` on one
is a usage error. I did NOT add a `kind` field to `ExecutorRecord`, because
criterion 6 fixes the record's fields and `kind` is not among them, and adding
a field the plan did not name would change an artifact M4-P5 and M4-P7 will
write. The gap is asserted by criterion 4's test rather than left in a comment,
so the decision is cheap for a reviewer to overrule.

**DEVIATION 6: ten pre-existing test adapters gained `requires: []`.**
`readonly requires` is required on `ExecutorAdapter`, so every literal in
test/spawn.test.ts declares it. This is inside a files-to-touch file and is
noted because it is a ten-site edit rather than a local one.

## Environment warnings hit, and one to inherit

Nothing new. Warning 1 was honoured throughout: every command above ran under
`/tmp/claude-0/n26/bin/node`, v26.6.0, checked in the shell that ran it.
Warning 12's three axes are quoted with every suite result below. Warning 7
(`--test-name-pattern` before the positional path) is what the red-witness
harness does and what my own verification harness copied.

One thing a later phase should know: the `requires` field makes EVERY adapter
literal a compile-and-run site. A phase that adds an adapter anywhere, in
`src/` or in a test, must declare it, and the failure mode without the
non-array guard was a TypeError rather than a type error, because the suite
runs TypeScript by stripping rather than by checking.

## The claim grep, both forms, every hit settled

```
$ grep -nEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' delivery/work-history/m4-p3.md
18:per adapter, so the ADAPTER declares what it needs and the kernel refuses
44:so an optional `briefPath` would be an optionality the kernel never exercises:
64:kernel can never accept. The contract check therefore runs first, and one of
106:three of them, plus a fifth (the adapter was never reached). The other
135:never a fixture. Exit 0 on the real record, exit 1 with
314:**Requiring a field that is always supplied.** An adapter may declare
315:`requires: ["taskId"]`, which can never be unmet. That is not a defect: it is a
316:truthful declaration that happens to be always satisfiable, and the brief-path
396:returns nothing, so it has never existed on any branch, including
429:text needs a revision; I did not improvise a different criterion, and I did not
```

The wrap-insensitive form, because this prose is hard-wrapped by hand and the
command above is line-based:

```
$ tr '\n' ' ' < delivery/work-history/m4-p3.md \
    | grep -oEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to' \
    | sort | uniq -c | sort -rn
      6 never
      2 needs a
      2 always
line-visible occurrences: 10
wrap-insensitive occurrences: 10
```

Ten and ten: **no hit straddles a wrap in this document.**

Two hits are grammatical false positives and carry no claim. Line 18 matches
`needs a` inside "what it needs and the kernel"; line 429 is "the plan text
needs a revision", a recommendation to the orchestrator rather than an
assertion about the code.

The remaining eight are settled by these commands, run at this head.

**Lines 44, 314, 315 and 316: the six kernel-supplied fields.** The claim is
that `briefPath` and five siblings are always supplied by the time the request
is built, so requiring one can never be unmet.

```
$ grep -rn "\.launch(" --include=*.ts src/ bin/
src/spawn.ts:833:      adapter.launch({

$ node --input-type=module -e '<probe against the exported checks>'
six kernel-supplied fields required, nothing else supplied:
  {"ok":true}
the four caller-supplied fields required, nothing supplied:
  {"ok":false,"reason":"the probe adapter requires role, declaredTier, phaseId, deadlineSeconds, and this spawn supplied no value for them; nothing was created"}
env under the credential escape hatch:
  {"ok":false,"reason":"the probe adapter requires env, and this spawn supplied no value for it; nothing was created"}
```

There is exactly ONE call site that builds a request, and it supplies all six
unconditionally. The claim is therefore about one site, not about a class of
them, and the drift guard in criterion 1's test compares the field list against
the object that site actually produces.

The third arm is worth reading twice, and it is a fact this grep surfaced
rather than something I set out to show: `env` is reported UNMET under
`allowPrCredentials: true`. That is correct and not a bug. Under the declared
escape hatch the request's `env` IS `undefined`, which is Node's
full-inheritance form, so an adapter that declared it needs a scrubbed
environment is being told the truth. It is recorded here because the sentence
"the kernel always supplies `env`" would have been false.

**Line 64: an adapter cannot require `modelName`.**

```
$ node --input-type=module -e '<probe>'
["taskId","worktree","command","hookPath","recordPath","briefPath","deadlineSeconds","env","role","declaredTier","phaseId"]
subprocessAdapter.requires = []
checkAdapterContract({name:"x",requires:["modelName"]}) =
  {"ok":false,"reason":"the x adapter declares a requirement on modelName, which the executor request contract has no field for; the requirable fields are taskId, worktree, command, hookPath, recordPath, briefPath, deadlineSeconds, env, role, declaredTier, phaseId"}
checkAdapterContract({name:"x",requires:["taskId"]}) = {"ok":true}
```

The list is closed and derived from one function, and the refusal is measured
rather than asserted. What this does NOT show is that a FUTURE phase cannot add
`modelName` to the contract; the claim is about this head.

**Line 106: the adapter was never reached.** Settled inside criterion 2's own
test, which sets a flag in the adapter's `launch` and asserts
`assert.equal(launched, false, "the adapter was reached despite an unmet
requirement")`. It is an assertion, not a narration.

**Line 135: never a fixture.** Settled by reading criterion 4's test: it runs
`spawnCli(scratch, "t-valid", stub, ["--deadline", "300"])`, asserts exit 0,
and then validates `tasks/t-valid/executor.json`, the file that spawn produced.
The only hand-written JSON in that test is the COPY it makes to delete
`adapter` from, which is derived from the real record by
`JSON.parse(readFileSync(recordPath))`.

**Line 396: the declaration has never existed on any branch.**

```
$ git log --all --oneline -- 'delivery/plan/phase-declarations/m4-p3.json'
(no output, exit 0)
$ git ls-tree origin/claude/m4-declarations-to-main delivery/plan/phase-declarations/ --name-only | grep m4
delivery/plan/phase-declarations/m4-p1.json
delivery/plan/phase-declarations/m4-p10.json
delivery/plan/phase-declarations/m4-p13.json
delivery/plan/phase-declarations/m4-p15.json
delivery/plan/phase-declarations/m4-p16.json
delivery/plan/phase-declarations/m4-p19.json
delivery/plan/phase-declarations/m4-p2.json
delivery/plan/phase-declarations/m4-p20.json
delivery/plan/phase-declarations/m4-p23.json
delivery/plan/phase-declarations/m4-p26.json
delivery/plan/phase-declarations/m4-p27.json
```

Eleven M4 declarations on the branch whose commit message is "Put eleven M4
phase declarations on main, because the scope gate reads the merge base", and
`m4-p3` is not among them. The `git log --all` form is the one CLAUDE.md's
identifier-schemes section prescribes, because a deleted file still appears
there. RESIDUE: `--all` covers the refs this clone has fetched. A branch that
exists on the remote and was never fetched here would be invisible, and I did
not re-fetch every ref before running it.

### The grep run against this section, because it is recursive

Quoting the grep and its own output puts every guarded token back into the
document, so re-running the line-based form at this head reports 56 occurrences
rather than 10. Forty-six of those are the quoted command on one line, the
quoted hit list, and the sentences above that name a hit in order to settle it.
Re-run both forms and compare the totals rather than the lines:

```
line-visible: 56
wrap-insensitive: 56
```

Equal, so nothing in this document straddles a wrap. Three occurrences in the
settling prose are claims in their own right and are settled where they stand:
"an adapter that declared it needs a scrubbed environment is being told the
truth" is the probe's third arm, printed above; "the kernel always supplies
`env`" is quoted in order to be called FALSE; and "a branch that exists on the
remote and was never fetched here would be invisible" is a residue statement,
not an assertion that none exists.
