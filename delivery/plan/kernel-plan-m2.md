# Tiphys Kernel Plan M2: Deterministic gates

- Status: DRAFT, revision 2 (**re-grounded 2026-08-05** under DR-0011's recorded consequence, as the explicit step BEFORE adversarial review round 2; revision 1 applied review round 1, M2R-001 to M2R-026, `delivery/review/plan-review-m2-r1.md`, verdict FIX-ROUND-NEEDED, 6 high, 14 medium, 6 low)
- Baseline commit: revision 1 was written against 6ec048284bb2c56a167629141f69f92146dbf4f1 (`origin/main`, M1-P4 merged); revision 0 was drafted at 5b8e8ae and is superseded, the re-baseline being finding M2R-005's first edit. **Revision 2's re-grounding baseline is `a9d4012` on `claude/owner-decisions-round3`, with `origin/main` at `58ac964` (M1-P5 merged, PR #8).**
- Milestone: M2 (deterministic gates), blueprint section 13 row M2
- Binding rule: "If it is not written here, it is not being made. Unanswered questions go to the orchestrator."
- Relation to `delivery/plan/kernel-plan-v1.md`: this document expands section 5 of kernel plan v1 (the M2 outline) into full phases in the format of v1 section 3. Kernel plan v1 remains the governing plan for everything else: its header, section 1, section 2, section 3 (M1 phases, constraints C-1 to C-3, the test accounting rule, the invocation form), sections 6 and 7, section 8 (D-1 to D-19), section 9, section 11 and Appendix A are unchanged by this document except as recorded in M2-D-01. No row of v1's Appendix A moves buckets: all 16 M2 rows stay in M2.
- Process summary: written in parallel with M1 implementation under decided DR-0011 (planning produces documents and touches no source file), from kernel plan v1 section 5, the 16 M2-bucketed rows of `delivery/requirements/migration-table.md`, blueprint sections 1, 4, 10, 11 and 13, the process doc disciplines these gates mechanize, decision records **DR-0002 to DR-0016**, tuition **T-001 to T-007**, the interim mechanism index `MECHANISMS.md`, `delivery/plan/m2-conflict-pre-pass.md`, `delivery/verification/release-verification-interface.md`, and the M1 review and work-history record **through M1-P5's merge (round 4) and M1-P6's round 4** (`delivery/review/`, thirty artifacts; `delivery/verification/cr-520-orchestrator-reproduction.md` and `cr-661-orchestrator-reproduction.md`; `delivery/work-history/`), read for defects that a gate would have caught mechanically. Section 1.5 records that reading as a table and is the instrument by which this plan is judged; section 1.6 records what revision 2's re-grounding took from each input and what it declined.
- Series note: decisions are numbered M2-D-nn and constraints M2-C-n, so they never collide with v1's D-nn and C-n. The M3 plan is being written concurrently; the orchestrator reconciles at the boundary named in section 2.

---

## 1. Standing context

### 1.1 What M1 delivers, and what of it M2 consumes

M2 starts only after the M1 exit test has passed and its evidence is committed on `main` (v1 section 4).

**Revision 1's statement of M1 state is superseded 2026-08-05 by `delivery/STATE.md` at `a9d4012`.** It read: "M1-P4 is merged at 6ec0482, M1-P5 is stopped at the DR-0012 limit awaiting the owner, and M1-P6 is built on its branch and unmerged". The current state is:

- **M1-P5 is MERGED at `58ac964` (PR #8)**, after four fix rounds and six clean-room reviews, with zero high findings from either contract on the merged code. Five of six M1 phases are on `main`.
- **M1-P6 is unblocked, its PR is not opened**, and it is in its fourth hazard review round on branch `claude/m1-p6-toy-sandbox-exit`. Its four deferred criteria are now executable.
- The milestone gate is therefore still unsatisfied and no M2 phase may dispatch, but for a different reason than revision 1 recorded: M1-P6 and the M1 exit test, not an owner stop. DR-0016 has since removed the stop-and-wait escalation that produced revision 1's reading.

One M1 residue is load-bearing for M2 and is named here rather than left in `STATE.md`: **the unprobed-open class is closed for the guard, the watcher and doctor, and is still OPEN in `src/lock.ts`, `src/pool.ts` and `src/brief.ts`**, so `teardown`, four `lock` subcommands and `spawn` still block forever on a named pipe. No M2 phase owns those files and M2 does not fix them. What M2 owes is that it does not ADD an instance, which is constraint M2-C-6.

| M1 deliverable | Where (paths verified at 6ec0482 where merged) | Consumed by |
|---|---|---|
| npm package skeleton, `tsc -b` build, `node --test` suite, `dist/` never committed (D-17, D-18) | package.json, tsconfig.src.json, tsconfig.test.json | every phase |
| CLI dispatch table and the `EX_USAGE` 64 contract | src/cli.ts, bin/tiphys.ts | M2-P1 (the only phase that edits src/cli.ts) |
| CI workflow: matrix job `test`, non-matrixed fan-in job named exactly `gates` (DR-0004's required check context); **no `env:` block anywhere** | .github/workflows/gates.yml | M2-P1 and M2-P9 (steps added inside the `test` job; no new required check name), M2-P8 (the absent `env:` block is why criterion 3 must stage its own dangerous state) |
| behavior registry, appended by every phase (EXT-F-05) | test/behaviors.json | M2-P3 (makes it executable), every phase (registration) |
| fleet layout, `doctor` and its readiness profiles with WARN-to-FAIL promotion (EXT-F-08) | src/fleet.ts, src/commands/doctor.ts | M2-P1 (profile promotion is the model for gate applicability; no doctor edit is planned) |
| executor adapter `subprocessAdapter`, whose `spawnSync(program, args, {cwd, stdio})` passes **no `env` option** so the child inherits everything today, plus the turn-end hook's second `spawnSync` | src/spawn.ts (both call sites), src/hooks.ts, src/task.ts | M2-P8 (both children are scrubbed) |
| exit-test harness pattern: `--mode local\|full`, per-step JSON evidence records, no record for a command not executed, command-scoped git identity, gh-free deterministic PATH | scripts/m1-exit-test.sh, scripts/stub-payload.sh (M1-P6, unmerged) | M2-P9 (same shape), M2-P8 (moves PR creation out of the payload if the delivered shape puts it there) |
| worktree pool, lease lock, watcher, liveness guard | src/pool.ts, src/lock.ts, src/watcher.ts, src/liveness.ts | not consumed by M2. No M2 phase edits them. |
| **`classifyEntry` and `refuseOpenForWrite`: the one answer to "may this path be opened", lstat then stat then open only a regular file** (M1-P5, merged at 58ac964) | src/task.ts | **every M2 gate that reads a path it did not create** (M2-C-6). Added in revision 2. |

M1-P6 is not merged, so its contract above is as-planned, not as-delivered. M1-P5's contracts are now as-delivered at `58ac964`, which narrows but does not remove M2-C-1:

- **M2-C-1 (verify the delivered M1 contract before consuming it).** Any M2 phase consuming an M1 contract performs, as numbered step 1, a verification-first check that the delivered shape matches what this plan assumes, records the finding verbatim in its work history before any code, and stops and escalates if it diverges (process doc section 2b; R-012's verification-first construct). Improvising around a divergence is forbidden (v1 Never list).

### 1.2 What M2 is

M2 mechanizes seven disciplines the current process enforces by prompt and by hand, plus the substrate they need. The placement rule (blueprint section 1) is why each is a script. Where judgment genuinely remains it stays with the L3 reviewer and is named in section 1.5 as NOT COVERED rather than faked in a script.

The blueprint's M2 contents (section 13): red-witness harness (first), full-suite wrapper ported into kernel bin, scope auditor, citation linter, coverage checker, deploy and migration verifiers, credential scoping. All seven are planned below, plus the gate contract and runner they emit into (M2-D-02) and the run-pinning module T-004 requires (M2-D-06).

One property is load-bearing after review round 1: **a gate must not rest on a declaration by the party under suspicion where the same fact can be derived from the diff.** M2R-001 found three such declarations in the red-witness harness, each corresponding to a real M1 defect. The rule now governs M2-P2 step 4 and is stated here because it is general: derive what can be derived, demand evidence for what cannot, and declare the residue in section 1.5.

### 1.3 Conventions and constraints carried forward

All binding conventions of v1 section 1.4 apply unchanged (English only; npm only; no em dashes and pure ASCII; falsifiable criteria only with the register "node --test exits 0 and reports N tests, N > 0"; milestone exit tests are hard gates; commit messages carry no tool names). Parallelism is governed by **M2-D-19** at revision 2, which supersedes M2-D-03 on the strength of the recorded conflict pre-pass rather than on an appeal to DR-0011's authorized-stream list.

Constraints C-1, C-2 and C-3 of v1 section 3 carry forward and each bites in M2:

- **C-1 (one current-state authority, never a log tail).** The suite wrapper derives counts from the runner's machine-readable reporter and the process exit code, never from a summary line (M2-P3 criterion 6 stages a counterfeit summary line). The deploy verifier's authority is the parsed API response.
- **C-2 (no pid, process liveness, signals or /proc for identity or exclusion).** No M2 gate probes a process. Deploy readiness is a polled response with a wall-clock timeout.
- **C-3 (never auto-background).** The runner runs gates sequentially in the foreground; the deploy verifier polls in the foreground; no gate has a daemonize flag (structural greps are criteria in M2-P1 and M2-P7).

New constraints, binding on every M2 implementer:

- **M2-C-2 (never green by omission).** No gate reports green for work it did not do. Every green record carries `units` strictly greater than zero, the unit declared per gate in section 1.4's table. A gate that exits 0 having examined nothing is `error`. **The runner is not exempt at the aggregate level**: zero applicable gates is an error condition (M2R-012).
- **M2-C-3 (fail closed).** A check that cannot reach a verdict reports `error`. It never reports `not-applicable` (which asserts a precondition was evaluated and found unmet) and never green. A precondition whose evaluation fails is `error`. A gate whose required invocation parameter (`--base`, `--head`, `--phase`) is absent is `error`, not `not-applicable` (M2R-003). Adopted from the fail-closed rule already in v1 M1-P4 step 5 (FM-035, FM-038).
- **M2-C-4 (a gate never mutates the caller's working tree).** Gates that must run tests against another state do so in a scratch clone they create and own. After any gate run the caller's repository has an unchanged HEAD and a byte-identical working tree. Grounded in T-004 lesson 1.
- **M2-C-5 (a run that cannot name what it executed is not evidence).** Any gate that executes a test suite records a pin manifest (file set, sha256, size, mtime) over the source and test roots **of the tree it actually ran**, taken at the start and end of that run, and any difference in any of the four fields makes the record `error`. This discharges T-004 lesson 3. It does **not** reproduce T-004's incident, which was a shared worktree that M2-C-4 abolishes (M2R-009). The mtime field is retained because T-004's forensics describe a byte-identical rewrite, which a content-only pin would pass.
- **M2-C-6 (a gate establishes a path's type before opening it), NEW in revision 2.** Every M2 gate reads paths supplied by configuration, by a diff, or by a manifest, and therefore reads paths it did not create. Each such read and each such write goes through the delivered `classifyEntry` and `refuseOpenForWrite` in `src/task.ts`, never a bare `readFileSync`, `openSync`, `appendFileSync` or `renameSync`. A path that is not a regular file makes the gate report `error` naming the path and the type observed; a gate never blocks indefinitely on a path it did not create. Grounded in M1's most expensive defect class (CR-520 and CR-560, `delivery/verification/cr-520-orchestrator-reproduction.md`), in the mechanism index row "Reading a path whose type is not established" (`MECHANISMS.md`), and in T-005's finding that the cost of this class was paid twice because the second component reimplemented the mechanism instead of reusing it. **M2 uses the delivered implementation rather than writing a second one**, which is the concrete form of T-005's lesson inside this milestone. The residue is explicit: this constraint stops M2 adding a thirteenth instance, and does not close the twelve M1 instances, which have no M2 owner.

### 1.4 The gate set, stated once

Ten manifest entries. Each phase's registration step is bound to this table; a registration that differs from it is a review finding (M2R-007). "PR bundle" is the exit run on the M2-P9 pull-request head with `--base main --head HEAD --phase M2-P9`; "main bundle" is the weaker second run described in section 4.

| id | Built by | unitLabel | applicability | precondition kind | PR bundle | main bundle |
|---|---|---|---|---|---|---|
| `manifest-self-check` | M2-P1 | schema documents validated | required | none | green | green |
| `red-witness` | M2-P2 | witnesses evaluated | required | `diff-touches` src/, bin/ | green | not run (needs `--base`) |
| `suite` | M2-P3 | tests reported | required | none | green | green |
| `scope` | M2-P4 | changed paths audited | required | `branch-matches` phase pattern, plus `--phase` | green | not run (needs `--phase`) |
| `citations` | M2-P5 | citations resolved | required | `diff-touches` configured documents | green | not run (needs `--base`) |
| `coverage` | M2-P6 | finding ids checked | required | `file-exists` inventory config | green | green |
| `deploy` | M2-P7 | release verifications satisfied | conditional | `file-exists` release-verification declaration | not-applicable (structural) | not-applicable (structural) |
| `migrations` | M2-P7 | migrations compared | conditional | `file-exists` release-verification declaration | not-applicable (structural) | not-applicable (structural) |
| `credential-scrub` | M2-P8 | credential sources probed | required | none | green | green |
| `credential-token` | M2-P8 | tokens probed | conditional | env `TIPHYS_IMPLEMENTER_TOKEN` present | green with owner action A-3, else not-applicable | same |

**The two `not-applicable` rows are STRUCTURAL, not local (revision 2, `delivery/verification/release-verification-interface.md` observation O-3).** Revision 1 left a reader free to conclude that `deploy` and `migrations` report not-applicable because THIS repository has no deploy target, and that some other repository's PR bundle would exercise them. It would not. Release verification runs after a merge, against a commit that exists only once the merge has happened, so its appearance in any pre-merge bundle can only ever be `not-applicable`, on every repository, forever. The entries stay in the manifest for uniform reporting; the real call site is the orchestrator's post-merge step with the merged sha as an argument, which M2-D-11 defers to M4. **No M2 bundle exercises these two gates in anger, and section 4's not-proven list says so.**

Two file-declaration rules follow from building a scope auditor in this milestone:

- **Literal paths only (M2R-016).** Every files-to-touch entry in this plan is a literal path or literal directory, because the phase declaration projection the scope auditor reads is generated from it. A description is not a path.
- **Shared files (M2R-020).** `gates.manifest.json` and `test/behaviors.json` are appended by nearly every phase and are named in every phase's conflicts-with by construction rather than repeated per phase. `test/behaviors.json` and the phase work history remain the **two** standing pre-authorized extras of CLAUDE.md, unchanged by M2: `gates.manifest.json` and each witness spec appear on the declaring phase's own files-to-touch list, so DR-0012 clause 5 needs no amendment and no M2 phase edits CLAUDE.md (M2-D-12, disposing of M2R-006).

Other shared fields, stated once:

- **migrations: none** for every M2 phase (library).
- **substrate (DR-0007): substrate-neutral** for every phase. Two gates reach outside the filesystem (`deploy`, `credential-token`); neither is substrate-specific and both are precondition-gated.
- **Invocation form** (v1 section 3, PR-102): `tiphys <cmd>` means `node bin/tiphys.ts <cmd>`; the exit harness invokes `dist/bin/tiphys.js` after `npm run build`.
- **Test accounting rule** (EXT-F-05) applies unchanged and becomes executable from M2-P3 onward, which is the gap `clean-room-m1-p1.md` CR-002 recorded as open until M2.
- **Standing environment warnings** of CLAUDE.md are forwarded in every brief. **Six bite M2** (revision 2 adds two): warning 1 (three Node versions, the container default 22 versus the >=26 floor, and the floor-satisfying toolchain that CAN be fetched, which removes "witnessed in CI" as the only discharge for a floor-dependent assertion), warning 4 (computed-URL dynamic import from `test/` into `src/`), warning 6 (`gh` absent locally, present in CI), warning 7 (`--test-name-pattern` precedes the positional path, which M2-P2 depends on), **warning 10** (never derive a retry or error signature from hand-written examples; capture real stderr, which is M2-P2 rule (f) and criterion 5a), and **warning 11** (suite wall time grows with real-clock waits, which now applies to M2-P7's poll deadlines as well as to lease waits).
- **Three CLAUDE.md rules added after revision 1 are binding on every M2 phase** and are forwarded in every brief: the **fix-round contract** (name the mechanism not the finding, publish the derivation command and its full output, state what the derivation did not cover, and the reviewer's first check is the third item), the **claim grep** run before submitting any work history, and **"one witness is not a class"** (a witness for a class must redden under at least two structurally different members), which revision 2 turns into a gate rule at M2-P2 rule (g) and M2-D-17.

### 1.5 What M1's real defects would have hit (the instrument)

One row per significant defect the project has actually paid for, with the gate that catches it and the criterion demonstrating that capability, or an explicit NOT COVERED naming where the requirement lands. A blank cell is not permitted. This is the cheapest available check on the rest of the plan, and revision 0 not having it is what let M2R-001, M2R-002 and M2R-005 escape its author.

**Revision 2 brings the table current.** Revision 1's thirteen rows were built when M1 had thirteen recorded defects: the record then stopped at `clean-room-m1-p5-second.md`. M1 has since produced M1-P5's third and fourth rounds and M1-P6's four rounds. Rows 1 to 13 are unchanged in id and in text. Rows 14 to 22 are new and cover the classes revision 1 could not have seen: the unprobed-open class at its full extent, the `atomicWrite` wake loss, the guard-that-asserts-text class, the reporter-format class, and the three tuition classes T-005 to T-007.

| # | Defect, as recorded | Source | Caught by | Demonstrated by |
|---|---|---|---|---|
| 1 | **V-1**: `pool destroy` force-deleted a task branch carrying committed unpushed work; the guarding test destroyed a branch still at its base commit, so it could never observe the loss | `verification-m1-p3-fix-round.md` V-1; T-003 lesson 2 | `red-witness` (M2-P2), dangerous-state rule plus derived destructive class | M2-P2 criteria 2 and 3a |
| 2 | **V-2**: a retry matcher was narrowed using hand-authored example strings while 312 real captured contention failures existed; the real transient stopped being retried | `verification-m1-p3-fix-round.md` V-2; T-003 lesson 4 | `red-witness` (M2-P2), `consumesExternalOutput` **required by derivation from the diff**, not by opt-in | M2-P2 criteria 5 and 5a |
| 3 | **U-10**: a work history claimed two deterministic witnesses where only one was; measured 11/20, 8/20, 6/20 | `verification-m1-p3-fix-round-2.md` U-10 | `red-witness` (M2-P2), determinism rule with measured rate | M2-P2 criterion 4 |
| 4 | **N-401**: witness W9 went silently GREEN 3/3 after a later change in the same component, while the work history still presented it as a live guard | `clean-room-m1-p4.md` N-401 | `red-witness` (M2-P2), durable witness corpus with re-evaluation of stored witnesses whose dangerous state intersects the diff | M2-P2 criterion 9 |
| 5 | **CR-002**: the behavior-to-test mapping could rot silently; no executable guard existed | `clean-room-m1-p1.md` CR-002 | `suite` (M2-P3), registry resolution by name | M2-P3 criteria 3 and 4 |
| 6 | **PR-106 class**: a test-selection pattern that silently drops files once subdirectories exist | v1 M1-P1 step 2; `package.json` scripts.test today | `suite` (M2-P3), discovery parity enumerated by walking declared roots, never by expanding the runner's own pattern | M2-P3 criterion 2 |
| 7 | **F-3**: deleting two tests removed the only coverage of the worktree-add retry, leaving unwitnessed retry logic | `final-review-m1-p3.md` F-3 | `suite` (M2-P3) **in part**: a registered behavior whose test disappears is caught by name; logic that was never registered is not | M2-P3 criterion 4; residue in section 4 not-proven item 8 |
| 8 | **F-1 / F-2**: a thrown error bypassed rollback, producing an 18-line stack trace and orphaned state, and the printed recovery route was wrong until driven by hand | `clean-room-m1-p4.md` F-1, F-2 | **NOT COVERED by any M2 gate.** Error-path correctness, found by an L3 reviewer driving the CLI with forced failures | Lands at M3: clean-room probe list (R-055, R-059) and the checklist extension mechanism (R-054). Section 4 not-proven item 9 |
| 9 | **M1-P5 finding 1 (CRITICAL)**: a stranded claim file made every future watcher pass silently and permanently report nothing for a genuinely pending signal, while the beacon stayed fresh so the guard reported the fleet healthy | `clean-room-m1-p5-second.md` finding 1; `STATE.md` | **NOT COVERED by any M2 gate.** A silent-permanent-failure design defect with a green suite throughout | Lands at M3: clean-room probe list, and the mechanism index (section 2 item 10). Section 4 not-proven item 10 |
| 10 | **M1-P5 duplicate-mechanism divergence**: the claim-file pattern was implemented twice and diverged, because the project has no index from mechanism to rule | T-005 | **NOT COVERED by any M2 gate.** M2 claims only the machine-readable destructive-authority half (section 2 item 11), which is a different list | **Owned by M3**: the mechanism index, delivered by M3-P6 (brief clauses plus stub index) and M3-P8 (generated projection of the tuition feed's `mechanisms[]`), under M3's D-M3-23, seeded with the `claim-file` rule this very defect established. Section 2 item 10 |
| 11 | **N-402**: a work history declared a path untestable when it was one CLI flag away, telling the next reader not to try | `clean-room-m1-p4.md` N-402 | **NOT COVERED by any M2 gate.** An honesty defect in prose | Lands at M3's reporting and work-history contracts (R-052a, R-057a, T-003's universal-quantifier consequence). Section 4 not-proven item 11 |
| 12 | **CR-104**: a deviation declaration undercounted its own extras (four declared, five present) | `clean-room-m1-p2.md` CR-104 | **NOT COVERED by any M2 gate.** The scope auditor audits files, not the accuracy of a prose declaration | Lands at M3's report contract (R-057a deviations section). Section 4 not-proven item 11 |
| 13 | **M1-P5 NEW-2**: a named pipe at a metadata path hangs the guard and the watcher forever, live-locking doctor, spawn and teardown | `STATE.md` in flight; M1-P5 final confirmation | **NOT COVERED by any M2 gate.** Adversarial input probing | Lands at M3's clean-room probe list (R-055 names the state that can never exit). Section 4 not-proven item 10 |
| 14 | **CR-520 (HIGH)**: `readBeacon` and its siblings opened paths without establishing their type; the class ran to twelve paths, was closed for the guard, the watcher and doctor over four rounds, and is still open in `src/lock.ts`, `src/pool.ts` and `src/brief.ts` | `clean-room-m1-p5-third-hazard.md` CR-520; `delivery/verification/cr-520-orchestrator-reproduction.md`; `STATE.md` carried-forward | **NOT COVERED as a detection gate.** M2's answer is preventive and self-directed rather than detective: **M2-C-6** binds every M2 gate to the delivered `classifyEntry` and `refuseOpenForWrite`, so M2 does not add a thirteenth instance and does not reimplement the mechanism (T-005's lesson applied to this milestone) | M2-P1 criterion 16 and M2-P5 criterion 9, both staged with a real `mkfifo` against the dangerous state. The twelve M1 instances have no M2 owner and stay a carried-forward item. Section 4 not-proven item 12 |
| 15 | **CR-560 (MEDIUM)**: a twelfth path in the same class, unlisted and unescalated after a round convened to enumerate the class; a named pipe at `<fleet>/warnings.md` hangs `tiphys spawn` forever and strands a worktree, a branch and a pool record | `clean-room-m1-p5-round4-hazard.md` CR-560, CR-562 | **NOT COVERED by any M2 gate.** The defect is an incomplete derivation, and no script can tell a complete enumeration from an incomplete one without already knowing the answer | The mechanical counterfactual now exists and is prompt-level, not a gate: CLAUDE.md's fix-round contract items 2 and 3 (publish the derivation command and its full output, state what it did not cover) with the reviewer's first check being item 3. Section 4 not-proven item 13 |
| 16 | **`atomicWrite` fixed-stage-name wake loss** (pre-existing, found in M1-P5 round 4): two concurrent passes shared `${path}.stage`; the loser died on ENOENT after advancing its seen state, dropping a signal in a protocol whose rule is duplicate-rather-than-drop | `clean-room-m1-p5-round4-criteria.md` mutation rows; `clean-room-m1-p5-round4-hazard.md`; `MECHANISMS.md` "Atomic file replacement" | `red-witness` (M2-P2) **in part**: once a witness exists, the determinism rule records the measured rate (this defect reproduced 3 of 5) instead of accepting a "deterministic" claim. Finding the defect took a hazard reviewer constructing real concurrency, which no gate does | M2-P2 criterion 4. Residue in section 4 not-proven item 14 |
| 17 | **The guard-that-asserts-text class**: a registered witness reddens when the asserted TEXT is deleted and stays GREEN against a one-line edit that preserves the text and inverts the behaviour. Six confirmed instances across four M1-P6 rounds: `exit 1` to `exit 0`, two placements of `\|\| true`, a step-level `if: false`, a quoted YAML key that the whitelist regex cannot see, and the guarded step moved into a job the fan-in does not need | `clean-room-m1-p6-round2-hazard.md` CR-640; `clean-room-m1-p6-round2-criteria.md` CR-661; `delivery/verification/cr-661-orchestrator-reproduction.md`; `clean-room-m1-p6-round3-hazard.md` CR-681; `clean-room-m1-p6-round4-hazard.md` CR-720, CR-721, CR-722; `MECHANISMS.md` "Asserting a CI step is wired" | `red-witness` (M2-P2), **rule (g) added in revision 2** (M2-D-17): a class witness must declare at least two structurally different dangerous-state members and be red against each, and where the named tests assert over a document's TEXT, one member must preserve the asserted text while inverting the behaviour | M2-P2 criterion 3b. **Revision 1's rules (a) to (f) admit this class**, derived by reading each rule against each instance rather than by execution; M2-D-17 carries the derivation and names the falsifier that would overturn it |
| 18 | **CR-604 plus the reporter-pin defect**: a seeded project's `npm test` checked by exit code only passes on a suite with zero tests; the count parse that fixed it then failed on the floor toolchain while passing on the container default, because the default reporter format differs between the two toolchains this project runs | `clean-room-m1-p6-hazard.md` CR-604; `clean-room-m1-p6-criteria.md` mutation table (reporter-pin, red at the parent, green at head on both toolchains); `MECHANISMS.md` "Parsing another program's reporter output" | `suite` (M2-P3) **in part**: for the kernel's own suite, M2-C-2 makes a zero-unit green an error and the pinned reporter makes the format a controlled input rather than a widened parse. No M2 gate examines a seeded project's suite, which is where CR-604 actually lived | M2-P3 criteria 1 and 11 (criterion 11 is new in revision 2). Residue in section 4 not-proven item 15 |
| 19 | **CR-680 (MEDIUM)**: the evidence bundle recorded a claim that was false, a lease report presented as non-fatal while the run died sixteen records later | `clean-room-m1-p6-round3-hazard.md` CR-680 | **NOT COVERED by any M2 gate.** The record was internally consistent and machine-valid; the falsity was in what it asserted about a run that then did not continue that way. Schema validity and count arithmetic cannot see it | Lands at M3's report contract (R-084 to R-089a). Section 4 not-proven item 16 |
| 20 | **CR-580 and CR-565 (LOW, found twice in one round)**: the work history's self-reported registry count was wrong by exactly one throughout, while being described as "computed independently" | `clean-room-m1-p5-round4-criteria.md` CR-580; `clean-room-m1-p5-round4-hazard.md` CR-565 | `suite` (M2-P3) **in part**: the gate computes the authoritative registry and test counts and writes them into a record, so a prose figure has a machine number to be checked against. No M2 gate reads a work history or compares a prose count to the computed one | M2-P3 criteria 1, 3 and 4 produce the number. The comparison lands at M3's report contract. Section 4 not-proven item 16 |
| 21 | **T-006 class, seven instances across M1, one of them the orchestrator's own**: impossibility, coverage and remedy claims written without the construction that would settle them, including a claimed impossibility a reviewer disproved in minutes with `symlinkSync(p, p)` and a claimed coverage whose uncovered case was a permanent hang of every supervision command | `delivery/tuition/T-006`; `delivery/verification/cr-661-orchestrator-reproduction.md` (the seventh instance) | **NOT COVERED by any M2 gate, decided rather than assumed (M2-D-16).** The claim grep is already binding in CLAUDE.md and is mechanical; making it a gate is not justified by the measurement recorded in M2-D-16 | Lands at M3's work-history and report contract, where a declared claims section gives the check a schema instead of a regex over free prose. T-006's one M2 consequence is taken: M2-P2 step 5 makes an unreached classifier arm a reported state. Section 4 not-proven item 17 |
| 22 | **T-007 class**: a phase met 15 of 15 executed acceptance criteria, walked by two reviewers on different model families, and still live-locked every supervision command, because no criterion described the failure surface | `delivery/tuition/T-007`; `clean-room-m1-p5-third-criteria.md` versus `clean-room-m1-p5-third-hazard.md`; `arbitration-m1-p5-third-round.md` | **NOT COVERED, and not coverable by a gate at all.** T-007's own finding about M2 is that gates inherit the criteria's blind spots, so more gates cannot close it | M2's answer is not a gate: every phase section below declares a **hazard class** (M2-D-18, section 1.6), which is the second review contract's input and a pre-submit self-review for the implementer. Section 4 not-proven item 18 |

**Verdict of the table, revision 2: of 22 recorded defects and defect classes, 11 are caught by an M2 gate with a named criterion (7 outright, 4 in part) and 11 are caught by no M2 gate.** Revision 1's figure was 7 of 13 on the same accounting convention, in which "caught" includes partial coverage. Two things in that comparison are worth stating rather than leaving to a reader:

- **The proportion did not improve.** 7 of 13 is 54 per cent; 11 of 22 is 50 per cent. M1's later defects are, on balance, of the kind M2 does not mechanize: incomplete derivations, false records, hostile filesystem input and design rules that leave a green suite. This is evidence for T-007's thesis, not against it.
- **One row moved only because this re-grounding moved it.** Without rule (g) and M2-D-17 the count would be 10 of 22, and the guard-that-asserts-text class, six confirmed instances produced by a milestone whose first gate is the red-witness harness, would have been uncaught by that harness. That is the single largest capability revision 2 adds.

Every uncovered row names its owner and appears in section 4's not-proven list, so the milestone's evidence cannot be read as covering it. Rows 9, 10, 13, 14 and 15 are the most severe defects M1 produced, and none of them is caught by a gate. That is the honest headline of this milestone: M2 mechanizes test honesty, scope, citations, coverage and credentials; it does not mechanize design-rule violations that leave a green suite, hostile input, or an incomplete enumeration, and no script placement under blueprint section 1 makes it able to.

### 1.6 Re-grounding record (revision 2, 2026-08-05)

DR-0011's recorded consequence makes this an explicit step before adversarial review, not a review finding. One row per input, with what the plan took from it or the reason it changed nothing. An input silently ignored would be worse than one rejected with a reason.

| Input | Disposition |
|---|---|
| `delivery/tuition/T-005` (lessons do not propagate) plus the interim `MECHANISMS.md`, eleven mechanisms | **Changed the plan in three places.** M2-P2's grounding now names `MECHANISMS.md` and its brief requires a mechanism lookup before writing (step 0). Three of its rows become gate inputs: "Asserting a CI step is wired" becomes M2-P2 rule (g); "Parsing another program's reporter output" becomes M2-P3's reporter pin; "Reading a path whose type is not established" becomes **M2-C-6**, which is the largest single addition, because every M2 gate reads paths it did not create. Section 2 item 10's claim that "the index does not exist for a gate to check during M2" is corrected: it exists, and M2 still builds no checker for it, for the reason stated there. |
| `delivery/tuition/T-006` (unexecuted claims about the world) plus CLAUDE.md's binding claim grep | **Changed the plan in two places, and declined a third with a measurement.** Taken: T-006's own M2 consequence, that an arm of a classifier no test reaches is a reportable state, becomes M2-P2 step 5's unreached-arm report. Taken: section 1.5 row 21 and section 4 not-proven item 17. **Declined with evidence: no M2 gate mechanizes the claim grep** (M2-D-16, which carries the measurement). T-006 itself routes the record contract to M3. |
| `delivery/tuition/T-007` (criteria cannot contain the defect) | **Changed every phase section.** Each of M2-P1 to M2-P9 now declares a `hazard class` beside its acceptance criteria, per M2-D-18. Taken also: T-007's explicit M2 consequence that "all acceptance criteria met" must not be expressible as a terminal green, recorded in M2-D-18 and in section 4. |
| `delivery/decisions/DR-0013` (Ajv 8.20.0, `yaml` 2.9.0) | **Changed one passage, and confirmed two.** Section 2 item 5 and M2-D-04 already read correctly against the decision and are confirmed, not rewritten: M2 ships its validator, M3-P1 retires it as an ENGINE, M2's scope is unchanged and nothing in M2 assumes extension. What was missing is the coherence obligation the decision creates in the other direction: DR-0013 clause 6 promises that "all existing M2 validation tests are re-run against it", which only holds if M2's validator emits the diagnostic contract M3 declares and M2's tests assert no engine-specific wording. That is now written into M2-P1 step 4 and criterion 10. |
| `delivery/decisions/DR-0014` and `delivery/verification/release-verification-interface.md` | **Rewrote M2-P7 substantially**, which is the largest change in this revision. See M2-P7 and M2-D-20. Also changed: section 1.4's gate table and its new structural-not-applicable note, section 4's not-proven item 1, risk 1, and Appendix A's residue rows for R-068 and R-069. |
| `delivery/decisions/DR-0015` (owner is not an approval step) and `DR-0016` (escalation threshold) | **Changed three passages.** Section 4's preamble no longer says the exit test runs "with the owner present"; the A-3 step is a credential the orchestrator cannot hold, which DR-0016 keeps owner-reserved as elevated access, not an approval. Section 3's preamble drops DR-0012's stop-and-wait as a stop and records DR-0016's replacement (a fresh implementer plus a third review contract, dispatched immediately, owner notified asynchronously). Section 6 item 4's proposed owner decision on concurrency is withdrawn under DR-0016's test and taken as M2-D-19. |
| `delivery/plan/m2-conflict-pre-pass.md` | **Changed the plan's parallel structure, and one thing downstream of it that is easy to miss.** M2-D-03's conclusion is superseded (its cost analysis is kept and answered), M2-D-19 records the new structure and its authority, section 3's preamble states it, and every phase's `parallelizable` field is rewritten. The downstream catch: **M2-D-08's justification for shipping no merge-time witness gate was "M2 runs no parallel phases", which this input falsifies.** The conclusion survives on a different and better reason (DR-0004's current-branch ruleset already delivers the property, and has been witnessed doing so), with the genuinely uncovered residue named in risk 9 and section 4 item 3 rather than absorbed. |
| M1's actual defect record (`delivery/review/`, thirty artifacts; work histories m1-p5 and m1-p6) | **Rebuilt section 1.5** from 13 rows to 22, added M2-P2 rule (g), M2-P3 criterion 11, and M2-C-6. Updated section 1.1's statement of M1 state, which was materially stale. |

**Two inputs were read and changed nothing, recorded here so the omission is deliberate.** DR-0015's amendment of plan v1 section 4 stage B touches the M1 exit test's approval artifact, not M2's exit test, which has no stage B and no approval artifact of its own. And `MECHANISMS.md`'s rows for the lease compare-and-swap, the append-only log and a guard's own failure path bear on M1 and M3 components that no M2 phase builds or edits; they are forwarded in briefs under the mechanism-lookup obligation rather than turned into gate rules, because a rule with no M2 component to bind is machinery for a state this milestone does not reach.

---

## 2. Boundary with M3

The M3 plan is being detail-planned concurrently. These items sit on the boundary and are claimed or disclaimed explicitly. **M3 phase ids move as that plan is revised, so items below name phases by NAME; the orchestrator reconciles ids before M3 dispatch** (M2R-019).

1. **Gate registry versus gate manifest (R-094).** M2 builds a manifest: one file for one repository, with per-gate preconditions and applicability. M3's canonical gate registry phase builds the single source consumed by CI and briefs, keyed per assurance mode. M2 deliberately does not implement assurance modes; the manifest schema reserves a `modes` field, validated if present and ignored by the M2 runner, so the promotion is additive.
2. **Report contract and status line (R-084 to R-089a).** M3's reporting and work-history contracts phase. M2 claims only R-089b, the machine half. Because the report contract does not exist yet, M2-P6 defines its own input contract and the M3 report schema must emit that shape or supersede it (risk 4).
3. **Plan schema (R-011, R-018, R-019, R-021).** M3's schema foundation phase. M2's scope auditor consumes a minimal phase declaration projection; the recommendation that the plan schema's phase object be a superset of those fields has been adopted in the M3 draft.
4. **Schemas directory.** Root `schemas/` is reserved for M3 by CLAUDE.md, so M2's schema documents live under `src/gates/schemas/` (M2-D-05). Relocation is M3's call.
5. **JSON Schema validation technology.** M2 ships a minimal in-repo validator with a closed keyword set that errors loudly on any keyword it does not implement (M2-D-04). The library question is DR-0013, DECIDED 2026-08-05 for Ajv 8.20.0 exact at Draft 2020-12; M2's module boundary is the seam, and M3-P1 retires this validator as an ENGINE while preserving that boundary. M2 ships and uses it unchanged. **Checked against DR-0013 at revision 2 and confirmed**: M2's scope does not change, no M2 phase assumes the validator will be extended, and the closed keyword set of M2-P1 step 4 is deliberately not grown to meet M3's five artifact schemas. **One obligation runs the other way and is new in revision 2.** DR-0013 clause 6 promises that M3-P1 will re-run "all existing M2 validation tests" against Ajv. That promise is only keepable if M2's validator emits the diagnostic contract M3 declares, `INVALID <json-pointer> <message>` with deterministic ordering (`delivery/plan/kernel-plan-m3.md` step 11 of M3-P1), and if M2's own tests assert that contract rather than any engine-specific wording. Both are now required by M2-P1 step 4 and criterion 10. Without them the retirement is a rewrite of M2's tests rather than an engine swap, and the "boundary preserved" language in DR-0013 would be false at the moment it was acted on.
6. **Fix-round verification as a pipeline requirement** (T-003 structural consequence 1). A pipeline-shape rule, not a gate: no script can tell an honest fix round from a dishonest one. M3's assurance-mode phase. Disclaimed.
7. **Universal-quantifier linting in work histories** (T-003 structural consequence 3). M3's reporting and work-history contracts phase. Disclaimed.
8. **Tuition flow (R-070, R-091) and role briefs (R-004 to R-009b).** M3. Disclaimed.
9. **Credential scoping's owner half.** Branch protection is M4 (R-064); DR-0004 items 2 and 3 are approved owner actions; item 4 is owner action A-3 in section 6.
10. **Mechanism index (T-005): M2 disclaims the ARTIFACT in full, and M3 has claimed it. The orphan M2R-018 raised is closed from both sides.** M3 plan revision 1 owns it in two adjacent phases under its decision D-M3-23: **M3-P6** (delivery-role briefs) ships the implementer brief's `mechanism-lookup` and `mechanism-sibling` clauses plus a committed stub index carrying the rule T-005 itself establishes, and **M3-P8** (tuition flow and the mechanism index) replaces the stub with the index generated as a **projection of the tuition schema's `mechanisms[]` field**, seeded with mechanisms this project has already paid for, each carrying resolving evidence, because a rule without a citation is not a rule. That is the shape T-005 asks for (one artifact, not two to maintain), and the split exists because a brief cannot require reading a file that does not exist.

    **Corrected at revision 2, because revision 1's premise is now false.** Revision 1 wrote that "the index does not exist for a gate to check during M2". It exists: `MECHANISMS.md` was committed at the repository root on 2026-08-05 with eleven mechanisms, under T-005's own "cheap interim measure, available now", and it says of itself that it is the interim and is intended to be SUPERSEDED by M3-P8's generated projection. So the reason M2 builds no checker changes, and the conclusion does not: **building a gate against an artifact whose stated purpose is to be replaced, in the milestone before its replacement ships, is machinery for a state this milestone does not reach.** M2 still claims none of the index, including the checkable half.

    **What DID change is that M2 now READS it.** Three of its eleven rows are inputs to this plan rather than to a gate over it: "Reading a path whose type is not established" becomes constraint M2-C-6, "Asserting a CI step is wired" becomes M2-P2 rule (g) and M2-D-17, and "Parsing another program's reporter output" becomes M2-P3's reporter pin. Every M2 phase brief carries the mechanism-lookup obligation in the interim form, which is a dispatch duty, not a gate.

    **What M2 needs from the index at gate time: still nothing.** No M2 gate reads it and no M2 phase is blocked by it. Two couplings are worth naming so the artifacts do not drift: M3-P8's seeded `destructive-git-operation` entry and M2's `destructiveCommands` manifest list are two views of one rule, so the index entry should cite the manifest list as its machine-readable form; and M3-P8's generated index must not silently drop an interim row, because eleven rows exist today and the seeded set the M3 plan names is smaller. **The dependency direction is M3 reads M2**, never the reverse, which is the correct order because M2 ships first and the manifest exists before the index does.
11. **Destructive-authority declaration** (T-003 structural consequence 4: "any kernel command that can destroy work must state its destructive authority explicitly in its contract, and force semantics must never be inherited implicitly"). This splits into three halves, and the split is stated here so none of them falls between the plans:
    - **The machine-readable list: M2 claims it.** A `destructiveCommands` list in `gates.manifest.json`, delivered by M2-P1 step 6 and consumed by M2-P2 step 4 rule (e), which derives a witness's class rather than trusting the implementer's declaration. It is M2's because it is a gate input: without it the derived-class rule that covers section 1.5 row 1 cannot function. It is a list of commands, not a statement of authority, and it claims nothing about prose.
    - **The rule text with its evidence: already claimed by M3**, as M3-P8's seeded `destructive-git-operation` mechanism-index entry citing V-1. M2 disclaims it.
    - **The enforcement that a new destructive command states its authority in its own contract: unclaimed by either plan, and M2 concludes it is M3's, not a gate concern.** No script can judge whether a contract states an authority; the checkable part (does this command appear on the `destructiveCommands` list) is already M2's list, and the judgeable part is a brief clause plus a review probe. **Ask to the M3 planner: place it in M3-P6's implementer brief, adjacent to the `mechanism-lookup` clause, as the obligation that an implementer adding or extending a command that can destroy work states its destructive authority in the command's own contract, never inherits force semantics from a caller, and adds the command to the manifest's `destructiveCommands` list.** That last conjunct is what keeps M2's machine half and M3's prose half from diverging, and it is the clause that would have caught V-1 at authoring time rather than at verification time.
12. **Release verification and the charter (DR-0014), NEW at revision 2.** M2 builds one post-merge verification contract with two static manifest entries and two reference adapters (M2-D-20, M2-P7). **M2 does not build the charter field that selects and configures a verification**, does not build a registry of N charter-declared verifications, and does not generalize the two entries. The charter's verification field is M3's, and the investigation's instruction to M3 is to RESERVE the space rather than design it (`delivery/verification/release-verification-interface.md` section 8 item 4), because the predicate that makes it checkable needs a real charter, which arrives at M4's pilot. Three specific hand-offs, stated so neither plan assumes the other did them: **(a)** M2's outcome enum and record schema are the shape M3's charter field must configure or supersede, in the same way section 2 item 2 treats the report contract; **(b)** the charter coherence check the investigation recommends (a charter declaring a non-local deployment topology and declaring verification `none` is internally contradictory) is a Kind B derived check in M3's own vocabulary and is recommended to M3, not built here; **(c)** the investigation recommends that a change FROM a declared verification TO `none` belongs on the charter's `escalation-contract` stop-for list, which is a one-line addition to an M3 default list and is the difference between "the owner chose to run without verification" and "verification stopped happening". M2 disclaims all three and names them so they are not orphaned.

---

## 3. Phases

Nine phases, branches `claude/m2-pN-<slug>`. One phase, one branch, one PR.

**Merge authority**, restated at revision 2 because two decisions have changed it since revision 1. DR-0012 delegates merging to the orchestrator conditional on two independent clean-room reviews on different model families, no unresolved high or medium finding, CI green on the exact head, and a passing scope audit. **DR-0015 removes the milestone-boundary carve-out**: the owner is not an approval step anywhere in execution, exit tests remain hard gates, and their evidence still goes to the owner unasked, which is a reporting obligation and not a click. **DR-0016 replaces DR-0012's stop-and-wait limit**: when a phase needs more than two fix rounds or a high recurs in one component, the response is a **fresh implementer plus a third review contract, dispatched immediately**, with the owner notified asynchronously rather than waited on; only if that round also fails does the phase go to the owner. Revision 1's parenthetical "which is where M1-P5 currently sits" is superseded: M1-P5 merged at `58ac964`, and the measured evidence behind DR-0016 is that the fresh implementer, not the escalation, was what broke that phase's spiral.

**Parallel structure (revision 2, superseding M2-D-03's sequential default).** M2-P1 is the only serialising phase. **M2-P2 through M2-P8 are mutually disjoint and run concurrently once M2-P1 has merged; M2-P9 runs last.** The recorded pairwise disjointness check DR-0011 condition 1 requires is `delivery/plan/m2-conflict-pre-pass.md`, and the authority for taking it without a new owner decision is M2-D-19. Three consequences bind every dispatch:

1. **Merge order stays dependency order even when work order is concurrent** (DR-0011 condition 3). A concurrent phase's pull request never merges before the phases its grounding names.
2. **`gates.manifest.json` and `test/behaviors.json` are never re-serialised over.** Both are append-only registries keyed by name; conflicts are resolved as a union against the true merge base, asserting no overlapping key and nothing removed or retitled, with the registry test as the backstop. This was performed successfully once already during M1-P6's branch update (145 keys plus 7 added, zero overlap, nothing removed).
3. **The measured serial resource is orchestrator arbitration, one per phase per round**, not implementer or reviewer capacity. Seven phases landing together is seven arbitrations plus seven merge sequences, and DR-0004's ruleset requires each branch to be current with `main` before merging, which serialises the merges themselves. Dispatches are staggered so the reviews do not all land in one window, and the last merges should each expect a branch update.

**No M2 phase edits CLAUDE.md.** Revision 0 had M2-P1 and M2-P3 amending the record of the binding conventions while merging under an authority whose clause 5 they redefined, with orchestrator self-authorization as the fallback; M2R-006 is right that this is circular and that the beneficiary cannot rule on its own authority. The circularity is removed at the source rather than blessed: `gates.manifest.json` and witness specs are declared on each phase's own files-to-touch list instead of being added to CLAUDE.md's standing extras, so DR-0012 clause 5 remains true as written. The one remaining CLAUDE.md change, updating the human-facing gate list once M2's gates exist, is a documentation pull request listed in section 6 as owner item O-2 and blocks no phase.

Phase declarations for the scope auditor are authored by the orchestrator from this document before dispatch and committed to `main`, never authored on the phase branch (M2-P4 step 4).

### M2-P1: Gate contract, manifest, runner, and run pinning

**Amended 2026-08-06 after the first dual review** (arbitration-m2-p1.md): three ripples marked inline above with their dates, adopting deviations D1 and D2 as plan text and restating the aggregate applicability rule at the mechanism CR-800 named. Applied by the orchestrator after the fix round settled the semantics, so the plan is amended once to match reality.

- id: M2-P1
- branch: claude/m2-p1-gate-contract-and-runner
- intent: Establish how a gate is declared, invoked and reported, so the gates that follow emit one comparable record type and the exit test can count them. Deliver the runner with its diff and phase parameters, the manifest including the destructive-command list, the result record and schema, the pin module, precondition semantics, and CI wiring that measures something real from its first run.
- grounding: M1 merged with exit evidence on `main`. DR-0006 (lintable-schema-first governs gate output formats). SC-011 (unmet precondition reports not-applicable, never green). Blueprint section 4 and FM-060 as adopted in v1 M1-P4 (every toolbelt boundary is a subprocess with an exit code). T-004 lesson 3 (a run that cannot name what it executed is not evidence). DR-0013 (the diagnostic contract M3-P1 will keep when it swaps the engine). `MECHANISMS.md` row "Reading a path whose type is not established", which is M2-C-6. Verify absent first: `src/gates/`, `gates.manifest.json`.
- hazard class (T-007, M2-D-18): **the spine every later record rests on, so a defect here is invisible in every gate downstream.** What can produce a record that is wrong in the same direction as the thing it measures: a runner that writes a record for a gate it did not execute, a counter whose buckets double-count or hide one another, a pin that passes a tree that changed, a status that can be constructed as green with nothing examined, and a configured path that is not a regular file and blocks the runner instead of reporting.
- steps:
  1. M2-C-1 verification: confirm the delivered `src/cli.ts` dispatch shape and `EX_USAGE`, and the delivered workflow job layout. Confirm that a step added inside the `test` job introduces no new check-run name, because DR-0004's ruleset names the context `gates` verbatim.
  2. Create `src/gates/result.ts`: the `GateResult` record. Fields `gate`, `status` (exactly `green`, `red`, `not-applicable`, `error`), `units`, `unitLabel`, `startedAt`, `endedAt`, `precondition` ({id, met, reason, evidence}) when declared, `pin` (start and end manifests, for gates bound by M2-C-5), `detail`, `evidence` (relative paths), and `vacuous` (boolean; amendment 2026-08-06, deviation D2 adopted: the constructor's green-zero-units rewrite is recorded on the record it produced rather than inferred later from counts). Constructor rule: a record with status `green` and `units` 0 is rewritten to `error` naming M2-C-2 and cannot be constructed otherwise.
  3. Create `src/gates/pin.ts` (moved here from M2-P2 per M2R-010, because two gates consume it and a phase cannot depend on a module another phase owns): `takePin(roots)` returns `{roots, takenAt, files: [{path, sha256, size, mtimeMs}]}`; `comparePins(a, b)` returns differences including added and removed paths. Any difference of any kind, including a byte-identical rewrite that changes only mtime, is a difference.
  4. Create `src/gates/schemas/gate-result.schema.json` and `gate-manifest.schema.json`, and `src/gates/validate.ts`: a validator over a closed keyword set (`type`, `required`, `properties`, `additionalProperties`, `enum`, `items`, `minimum`, `minItems`, `pattern`, `const`, local `$ref`). Any keyword outside the set encountered while loading a schema is a load error naming the keyword, never a silent pass. **Diagnostic contract (revision 2, DR-0013 clause 6).** Every validation failure is reported as `INVALID <json-pointer> <message>`, one line per failure, in a deterministic order, and that string is the module's public contract. The validator's own internal wording is never asserted by a test: `test/gates.test.ts` asserts the pointer, the ordering and the message text this plan fixes, so M3-P1 can retire the engine and re-run these tests unchanged, which is what DR-0013 promises and what "preserving the module boundary" has to mean to be true. Do not grow the keyword set to anticipate M3's schemas; DR-0013 decided that question against extension.
  5. Verification-first sub-step, then implement: determine empirically whether `tsc -b` at the pinned compiler emits `src/gates/schemas/*.json` into `dist/`, or whether an explicit copy step is required; record which, implement that one, extend the package `files` allowlist if needed. Criterion 10 witnesses the outcome from `npm pack`.
  6. Create `src/gates/manifest.ts` and `gates.manifest.json`. Fields: `version`; `gates[]` with `id`, `command` (argv array), `parameters` (amendment 2026-08-06, deviation D1 adopted: the gate declares which runner parameters it consumes, so the runner passes what is declared rather than deciding what another program needs by inspecting it), `unitLabel`, `applicability` (`required` or `conditional`), `precondition` ({id, kind, ...}) with `kind` in the closed set `file-exists`, `file-absent`, `branch-matches`, `diff-touches`, `command-exit-zero`; reserved and validated but unused in M2, `modes`; and top-level `destructiveCommands`, a list of CLI commands and module paths whose operations can destroy work (section 2 item 11), seeded with `pool destroy`, `teardown`, and any command accepting `--discard`, `--force` or `--delete-branch-force`. The initial manifest carries one gate, `manifest-self-check`, which validates every schema document and the manifest against its own schema, so the first CI run measures something real (M2R-012).
  7. Create `src/gates/run.ts` and `src/commands/gates.ts`; register `gates` in `src/cli.ts` (the only M2 edit to that file). Signature: `tiphys gates run --manifest <file> --evidence <dir> [--base <ref>] [--head <ref>] [--phase <id>] [--only <id>...]`. Gates run sequentially in the foreground as subprocesses (C-3), stdout and stderr captured into the evidence directory. Rules: a gate whose precondition kind is `diff-touches` or `branch-matches`, or whose command requires `--phase`, reports `error` when that parameter is absent, never `not-applicable` (M2-C-3, M2R-003); a gate subprocess exiting nonzero **without writing a result record** is `error`, not `red`, because Node exits 1 on an uncaught exception and that collides with the red code; zero applicable gates is `error` with reason `no applicable gate` (M2-C-2 at the aggregate level), where APPLICABLE means the gate REACHED A GREEN OR RED VERDICT (amendment 2026-08-06, CR-800: a gate-authored not-applicable record and a runner-evaluated unmet precondition are the same thing to the counters, `counts.applicable` is derived from the result rather than from whether a child was spawned, `counts.verdict` is the green-plus-red total the anti-vacuity rule consults, and the success reason is structurally unreachable over zero green verdicts). Exit codes: `0` green, `1` red, `20` not-applicable, `21` gate error, `64` usage.
     **M2-C-6 wiring (revision 2).** Every path the runner opens comes from outside it: the manifest path, the evidence directory, each precondition's `file-exists` target, and each gate's record file. Each of those reads and writes goes through the delivered `classifyEntry` and `refuseOpenForWrite` in `src/task.ts` (verify their exported names and signatures at step 1, M2-C-1), never a bare `readFileSync` or `openSync`. A path that is not a regular file makes that gate `error` naming the path and the observed type; the runner returns rather than blocking. Do not write a second implementation of this check: T-005's recorded cost is exactly what happens when a project reimplements a mechanism instead of reusing the one that already carries the rule.
  8. Write `summary.json`: manifest path and sha256, per-gate status, and counts `declared`, `applicable`, `green`, `red`, `not-applicable`, `error`, `vacuous`, and `verdict` (amendment 2026-08-06, CR-866: `verdict` is green plus red, the total the anti-vacuity rule consults, added when the aggregate rule was restated), where **`vacuous` is a strict subset of `error` and `error` is the total** (M2R-021). No record is written for a gate that was not executed.
  9. Wire `node dist/bin/tiphys.js gates run` into the existing `test` job of `.github/workflows/gates.yml`, passing `--base ${{ github.event.pull_request.base.sha }} --head ${{ github.sha }}` on pull requests. No CLAUDE.md edit (section 3 preamble). M2-P9 later replaces this step with the exit harness so the set runs once (M2R-026).
  10. Tests in `test/gates.test.ts` and `test/pin.test.ts`; register behaviors.
- files-to-touch: src/gates/result.ts, src/gates/pin.ts, src/gates/validate.ts, src/gates/manifest.ts, src/gates/run.ts, src/gates/schemas/gate-result.schema.json, src/gates/schemas/gate-manifest.schema.json, src/commands/gates.ts, gates.manifest.json, test/gates.test.ts, test/pin.test.ts (create); src/cli.ts (edit), .github/workflows/gates.yml (edit), package.json (edit only if step 5 requires a build or files change).
- acceptance criteria:
  1. `npm ci`, `npm run build`, `npm test` each exit 0; after the build `git status --porcelain` is empty.
  2. Against a fixture manifest of four gates (one exiting 0 with units 3, one exiting 1 with a red record, one whose `file-exists` precondition names an absent file, one exiting 21 with an error record), the evidence contains exactly four records with statuses `green`, `red`, `not-applicable`, `error` in that mapping; `summary.json` reports declared 4, applicable 3, green 1, red 1, not-applicable 1, error 1, vacuous 0, with `error` equal to the number of error-status records; the runner exits nonzero.
  3. Against a manifest containing only the green gate, the runner exits 0 with applicable 1, green 1, vacuous 0.
  4. A fixture gate exiting 0 with `units` 0 is recorded `error`, counted in both `vacuous` (1) and `error` (1), and the runner exits nonzero; with `units` 1 it is `green` and the runner exits 0 (both directions).
  5. A `required` gate whose precondition is unmet is `not-applicable` and the runner exits nonzero naming it; declared `conditional`, the runner exits 0 (both directions).
  6. A `command-exit-zero` precondition whose command does not exist yields `error`, never `not-applicable` and never `green` (M2-C-3).
  7. A fixture gate that throws an uncaught exception (exit 1, no record) is `error`, not `red`.
  8. A manifest whose only gate declares precondition kind `diff-touches`, invoked **without** `--base`, yields `error` for that gate and a nonzero runner exit; the same invocation with `--base` yields the gate's real verdict (both directions, M2R-003).
  9. A manifest with zero gate entries, and separately a manifest whose every gate is not-applicable, both make the runner exit nonzero with reason `no applicable gate` (M2R-012).
  10. Schema validation: a manifest missing `id` is rejected naming the field; a record with a status outside the enum is rejected; loading a schema containing a keyword outside the closed set fails naming the keyword rather than validating anything. **Each rejection is emitted as `INVALID <json-pointer> <message>`, and a fixture with three simultaneous violations produces the three lines in a fixed order across ten consecutive runs (deterministic ordering, DR-0013 clause 6); the tests assert the pointer, the order and the message text, and `grep` over `test/gates.test.ts` shows no assertion on any string produced by the validator's internals rather than by this contract.** `npm pack` output contains both schema documents, and criterion 3's fixture re-run through `dist/` behaves identically (the compiled entry resolves its schemas).
  11. `takePin` over a fixture root, then a byte-identical rewrite of one file (content hash unchanged, mtime changed), then a second pin: `comparePins` reports exactly one difference naming the path and the `mtimeMs` field; with no rewrite it reports none (both directions).
  12. `tiphys gates run` with an unknown flag exits 64 with usage on stderr.
  13. Structural: grep over `src/gates/` shows no `detached: true`, no `unref`, no `process.kill`, no `/proc`, no pid usage (C-2, C-3).
  14. Split observation (M2R-023): (a) the check-run list on the phase PR contains exactly the contexts it contained before this phase, `test (26)` and `gates`, evidenced from the check-runs API; (b) the ruleset's required contexts are unchanged, evidenced by `gh api repos/.../rulesets` recorded in the work history, or marked CI-deferred with the reason that `gh` is absent locally (warning 6).
  15. `node --test` exits 0 with 0 failing and zero unaccounted tests; the behavior registry criterion of section 1.4 holds.
  16. **M2-C-6, staged against the dangerous state (new in revision 2).** With `mkfifo` used to place a named pipe at the manifest path, and separately at a `file-exists` precondition target, and separately at the path a fixture gate writes its record to, each run reports `error` naming the path and the observed type and **returns within the gate's bound** (asserted by the command having returned before the assertion executes, and by a harness timeout that would report a code rather than an unexplained wait). With the same paths as regular files, all three are the gate's real verdict (both directions, three placements). A `grep` over `src/gates/` shows no `readFileSync`, `openSync`, `appendFileSync` or `renameSync` on an externally supplied path that does not route through `classifyEntry` or `refuseOpenForWrite`, and the grep and its full output are recorded in the work history per CLAUDE.md's fix-round contract item 2.
- suggested model tier: strongest (the record shape, status vocabulary, exit codes and pin are consumed by every later phase and promoted by M3).
- citations: SC-011, DR-0006, DR-0004 (required check context), DR-0013 (diagnostic contract), blueprint section 4 and section 1, v1 M1-P4 (FM-060), T-004 lesson 3, T-003 structural consequence 4 (`destructiveCommands`), T-005 and `MECHANISMS.md` (M2-C-6), CR-520. Rows discharged: none (Appendix A note).
- conflicts-with: M2-P3 and M2-P9 (`package.json`, `.github/workflows/gates.yml`), plus the shared files of section 1.4.
- blocked-by: M1 exit test passed and its evidence on `main`.
- parallelizable: **no, and it is the only phase for which that is true.** M2-P1 is the serialising phase: every other phase grounds on it (M2-D-19, `delivery/plan/m2-conflict-pre-pass.md`).

### M2-P2: Red-witness harness

- id: M2-P2
- branch: claude/m2-p2-red-witness-harness
- intent: Make red-witness discipline mechanical and not self-certified: a test guards a behavior only when demonstrated red against a dangerous state that intersects the phase diff, green at head, in a pinned isolated run, with the witness class and the captured-output obligation derived from the diff rather than declared by the implementer, and with stored witnesses re-evaluated so one cannot silently go green later.
- grounding: M2-P1 merged (record, manifest, pin, `destructiveCommands`). Blueprint section 4 (input test IDs plus baseline SHA; red on baseline, green on head; emits an evidence file) and section 10 point 4 (target latest `main`, re-verified at merge; the merge-time gate is M5). CLAUDE.md's strengthened red-witness rule and its **"one witness is not a class"** rule; T-003 (dangerous state, hand-authored fixtures); T-006 (an unreached classifier arm is a reportable state); `clean-room-m1-p4.md` N-401 (a registered witness that went green); `verification-m1-p3-fix-round-2.md` U-10 (overstated determinism); **`delivery/verification/cr-661-orchestrator-reproduction.md` and `clean-room-m1-p6-round4-hazard.md` CR-720 to CR-722 (the guard-that-asserts-text class, six confirmed instances)**; **`MECHANISMS.md`**, in particular the rows "Asserting a CI step is wired", "Classifying another program's errors" and "Worktree removal and force branch delete". Section 1.5 rows 1 to 4, **and now 17**, are this phase's contract.
- hazard class (T-007, M2-D-18): **a guard that judges guards, so a defect here is invisible in every other phase's evidence.** What can make a witness pass while guarding nothing: a dangerous state weaker than the state the behaviour actually defends against; a class witness that reddens under one member and is green under the rest of the class; an assertion over a document's TEXT that survives an edit inverting its meaning; a stored witness that has silently gone green since it was written; a determinism claim asserted rather than measured; and a re-evaluation pass whose cost makes an implementer narrow it quietly.
- steps:
  0. **Mechanism lookup before writing (T-005, interim form).** Read `MECHANISMS.md` in full and record in the work history which rows bear on this phase and what each requires. Three do: "Asserting a CI step is wired" is rule (g) below, "Classifying another program's errors" is rule (f), and "Worktree removal and force branch delete" is the derived-class rule (e). Recording that lookup is the phase's discharge of the interim measure T-005 prescribes; it is not a substitute for the index M3-P8 builds.
  1. M2-C-1 verification of M2-P1's record, manifest, pin and `destructiveCommands` contracts.
  2. Create `src/gates/schemas/witness-spec.schema.json` and `src/witness/spec.ts`. A witness spec is one JSON document per behavior at `witness/<behavior-id>.json`, **durable and repository-level, not phase-scoped** (M2R-002, M2-D-14). Fields: `id`; `behavior` (must resolve in `test/behaviors.json`); `tests` (names, run with `--test-name-pattern` before the positional path); `class` (`additive`, `destructive`, `classification`); **`dangerousStates`, an ARRAY of one or more members** (each {kind: `baseline-ref` | `patch` | `mutation`, plus ref, patch path, or file and substitution}), which replaces revision 1's single `dangerousState` object so that rule (g) has something to require rather than something to bolt on; `deterministic` and `repeats` (default 5); optional `consumesExternalOutput` ({program, captures[], provenance}). Every member is applied, run and recorded separately, and the witness is green only when every member was red.
  3. Create `src/witness/run.ts`. Per witness, and **per declared dangerous-state member**: create a scratch clone the harness owns, never writing to the caller's tree (M2-C-4); fetch `origin` and resolve the baseline from the fetched remote, never a stale local ref; take the start pin over the clone's source and test roots; apply the member; run the named tests and require a nonzero exit with each named test reported failing; repeat `repeats` times when `deterministic` is declared, recording each run; restore head; run green; take the end pin; write the record including the applied mutation or patch diff verbatim, per member.
  4. Refusal rules, evaluated before any test runs, each producing `red` with a reason naming its source. Rules (a) to (c) are as reviewed; (d) to (f) are the M2R-001 additions that stop the gate resting on the implementer's own declaration (M2-D-15):
     - (a) `class` `destructive` or `classification` with a member of `kind` `baseline-ref`: a bare absent-feature baseline is not the dangerous state (T-003).
     - (b) `behavior` does not resolve in `test/behaviors.json`.
     - (c) `consumesExternalOutput` declared but no capture cited, or a cited capture missing or empty, or no cited capture's basename referenced from the named tests' sources.
     - (d) **Diff intersection**: each `patch` or `mutation` member must touch at least one file changed in the phase diff, and for `mutation` at least one line inside a changed hunk; otherwise `red` with reason "declared dangerous state does not intersect the phase diff", naming the member. The applied mutation diff is recorded verbatim per member so a reviewer can judge it.
     - (e) **Derived class**: a witness whose named tests invoke a command on the manifest's `destructiveCommands` list, or whose behavior name resolves to a module on that list, must be `destructive`; a declaration weaker than the derived class is `red`. The class is derived, not only declared.
     - (f) **Derived capture obligation**: `consumesExternalOutput` is required, not optional, when the phase diff touches a module that spawns a subprocess or parses another program's output, decided by a deterministic `child_process`, `execFile`, `spawnSync`, `execSync` grep over the changed files; a witness in that state without the field is `red`.
     - (g) **One witness is not a class (new in revision 2, M2-D-17).** A witness declaring `class: classification`, and any witness whose named tests assert over the TEXT of a document rather than over behaviour, must declare **at least two structurally different members** in `dangerousStates`, and is `red` when it declares one. "Structurally different" is derived, not judged: two `mutation` members touching the same line, or two `patch` members whose diffs are byte-identical after path normalisation, count as one member and the witness is `red` naming the collapse. **Text-assertion detection is derived from the test sources**, not declared: a deterministic grep over the named tests' sources for `assert.match`, `assert.doesNotMatch`, `.includes(` and `assert.ok(` applied to the contents of a file read from a path outside `src/` and `test/` (a workflow, a manifest, a configuration document) marks the witness text-asserting. For a text-asserting witness the rule is stronger, and it is the one that catches the six recorded instances: **at least one member must PRESERVE every string the named tests assert on while inverting the behaviour**, and the harness verifies that preservation mechanically by re-running the assertions' own greps against the mutated document, reporting `red` with reason "every declared member removes the asserted text; none inverts the behaviour" when no member does. Grounded in six confirmed instances (`clean-room-m1-p6-round2-hazard.md` CR-640, `clean-room-m1-p6-round2-criteria.md` CR-661, `delivery/verification/cr-661-orchestrator-reproduction.md`, `clean-room-m1-p6-round3-hazard.md` CR-681, `clean-room-m1-p6-round4-hazard.md` CR-720 to CR-722) and in CLAUDE.md's binding rule of the same name.
  5. Determinism rule: when `deterministic` is true every repetition of every member must be red; k of n red with k < n is `red` and the record carries the measured rate per member. When false, the rate is recorded and 0/n is still `red`. **Unreached-arm report (T-006's M2 consequence, new in revision 2)**: where a member is a `mutation` inside a changed hunk and the named tests do not change verdict at all, the record does not merely carry the rate 0/n. It names the mutated file, the mutated line and every named test that stayed green, under the reason "no named test reaches this arm". T-006's finding is that an arm no test reaches is a reportable state rather than an acceptable one, and the instance that produced it was a sabotage run coming back 0/3 whose measurement was right and whose adjacent prose was wrong; naming the arm is what makes the prose harder to write wrongly.
  6. **Re-evaluation of stored witnesses** (M2R-002): after evaluating the phase's own witnesses, the gate re-runs every stored witness ANY of whose declared dangerous-state members touches a file in the current diff. A stored witness now GREEN against any of its own members is `red` with reason "witness no longer guards its behavior", naming the witness and the measured rate. The phase **measures and records the wall-clock cost** of this re-evaluation over the kernel's witness corpus and states it in the work history; if it exceeds five minutes on the corpus at that time, the implementer stops and the orchestrator decides the scoping rather than the implementer narrowing it silently (the review's honest failure 6 declines to guess the ceiling, and so does this plan).
  7. Register `red-witness` per section 1.4. Applicability `required`, precondition `diff-touches` on `src/` or `bin/`; source changed with no witness spec covering it is `red`, never `not-applicable`; `--base` absent is `error` (M2-C-3). Merge-time re-verification (blueprint section 10 point 4) is a parameter (`--baseline <ref>`), not an enforcement (M2-D-08). **Revision 2 rewrites that decision's justification without changing its conclusion, because M2-D-19 falsified the old reason.** Revision 1 justified it with "M2 runs no parallel phases"; M2 now runs seven, so the semantic-overlap hazard DR-0011 names as the reason parallelism waits for M5 is real in this milestone. The property is nonetheless obtained, by a mechanism that already exists and has already fired: **DR-0004's ruleset requires a branch to be current with `main` before it can merge, and CI must be green on that exact head**, which was witnessed refusing a merge on a stale branch on 2026-08-05. So every phase's `red-witness` run necessarily executes against a head containing every previously merged phase, which is merge-time re-verification obtained from the ruleset rather than from a hook. What is genuinely NOT obtained, and stays M5's: automated detection of semantic overlap between two phases that are both still in flight, before either merges. That residue is stated in risk 9 and in section 4.
  8. Tests in `test/witness.test.ts` against scratch git repositories with command-scoped git identity (warning 5).
- files-to-touch: src/witness/spec.ts, src/witness/run.ts, src/gates/red-witness.ts, src/gates/schemas/witness-spec.schema.json, test/witness.test.ts, witness/ (create; each spec this phase adds is listed as a literal path in the phase declaration); gates.manifest.json, test/behaviors.json.
- acceptance criteria:
  1. On a scratch repository where the named test is red at the declared dangerous state and green at head, the harness exits 0, status `green`, `units` equals witnesses evaluated, both pins recorded and equal, and the applied dangerous-state diff appears verbatim in the record.
  2. A witness whose test is **green against its declared dangerous state** (staged as the V-1 shape: a destroy exercised against a branch carrying no commits) is red naming the witness; with the fixture corrected so the branch carries a commit, the same spec exits 0 (section 1.5 row 1, both directions).
  3. A `destructive` witness whose only `dangerousStates` member has `kind` `baseline-ref` is red before any test runs, citing T-003; with `kind` `mutation` and a real substitution it is evaluable (both directions).
     3a. **Derived class**: a witness whose named tests invoke a command on `destructiveCommands` but declares `class: additive` is red naming the derived class; declaring `destructive` makes the same witness evaluable (both directions; this is the escape M2R-001 found, and section 1.5 row 1 depends on it).
     3b. **One witness is not a class (new in revision 2, rule (g), section 1.5 row 17).** Staged as the CR-661 shape with the real captured artifact rather than a fixture: a fixture workflow carrying a guard step whose failure path is `exit 1`, and a witness whose named test asserts on the step's text. (i) With one member that DELETES the step, the witness is red naming the single-member collapse, not green. (ii) With a second member that changes `exit 1` to `exit 0` and nothing else, the harness confirms mechanically that every asserted string survives that member, runs it, and reports `red` for the witness because the named test stays green under it. (iii) With the test rewritten to extract and execute the step against a stubbed harness, both members are red and the witness is green. **Three directions, and (ii) is the one that matters**: the reproduction in `delivery/verification/cr-661-orchestrator-reproduction.md` is the source of the mutation and its measured outcome (`exit=0 pass 1 fail 0` with only `exit 1` changed to `exit 0`), so the fixture is derived from real captured output rather than from a string chosen to match this implementation (CLAUDE.md warning 10). Additionally: a witness declaring two members that touch the same line is red naming the collapse.
  4. A witness declaring `deterministic` true whose test is red in 3 of 5 repetitions is red carrying the rate 3/5; declared false, it is green with the rate recorded (section 1.5 row 3, both directions). The 3/5 fixture is staged as the section 1.5 row 16 shape, a stage-path collision between two concurrent passes, because that is the real M1 defect whose measured rate was 3 of 5 (`clean-room-m1-p5-round4-criteria.md`), not a rate invented for the test.
     4a. **Unreached arm (T-006, new in revision 2).** A witness whose `mutation` member sits inside a changed hunk on a branch of a classifier that none of the named tests reaches is red with reason "no named test reaches this arm", and the record names the file, the line and every named test that stayed green; adding a test that reaches the arm makes the same witness evaluable (both directions). A record carrying only the rate 0/n, without naming the arm, fails this criterion.
  5. `consumesExternalOutput` declared with no capture cited is red; with a capture that exists, is non-empty and is referenced by the named tests' sources, it is evaluable, and the record carries the capture's sha256 and provenance (both directions).
     5a. **Derived capture obligation**: a phase diff touching a module that calls `spawnSync`, with a witness that omits `consumesExternalOutput`, is red naming the derivation; supplying the field with the **real captured git contention stderr recorded in `delivery/review/verification-m1-p3-fix-round.md`** as the capture makes it evaluable (section 1.5 row 2, both directions, using real captured output rather than a fixture string).
  6. A `mutation` member naming a file outside the phase diff is red naming the member; moving the same mutation into a changed hunk makes it evaluable (both directions, M2R-001 edit d).
  7. **Pin witness** (M2R-009, a mechanism witness, not a reproduction of T-004's incident): with a documented in-harness hook that rewrites one file in the scratch clone with byte-identical content between the start and end pins, the record is `error` and the reason names the path and the `mtimeMs` field; a content-hash-only pin cannot pass this criterion.
  8. After every run in criteria 1 to 7, the caller's repository HEAD is unchanged and `git status --porcelain` is empty (M2-C-4).
  9. **Stored-witness re-evaluation** (section 1.5 row 4, the N-401 shape): a stored witness from an earlier fixture phase, made green by a later change in the same run, is red naming the witness and its measured rate; reverting the later change returns exit 0 (both directions). The work history records the measured wall-clock cost of re-evaluation over the kernel's corpus.
  10. With the scratch repository's local `main` behind its remote, the recorded baseline SHA equals the fetched remote head, not the local ref.
  11. `node --test` exits 0 with 0 failing and zero unaccounted tests; the behavior registry criterion of section 1.4 holds.
- suggested model tier: strongest (this gate judges every other test; a false witness here is invisible everywhere else).
- citations: R-015b, R-028b, R-036, R-037b, R-056b; blueprint section 4 and section 10 point 4; CLAUDE.md red-witness rule, "one witness is not a class", warning 10; T-003; T-005 and `MECHANISMS.md`; T-006; `verification-m1-p3-fix-round.md` V-1 and V-2; `verification-m1-p3-fix-round-2.md` U-10; `clean-room-m1-p4.md` N-401; `clean-room-m1-p6-round2-criteria.md` CR-661 and `delivery/verification/cr-661-orchestrator-reproduction.md`; `clean-room-m1-p6-round4-hazard.md` CR-720 to CR-722.
- conflicts-with: the shared files of section 1.4.
- blocked-by: M2-P1 merged.
- parallelizable: **yes, concurrently with M2-P3 to M2-P8 once M2-P1 has merged** (M2-D-19; created files are disjoint per `delivery/plan/m2-conflict-pre-pass.md`). Merge order stays dependency order.

### M2-P3: Full-suite wrapper with parity counting

- id: M2-P3
- branch: claude/m2-p3-suite-wrapper
- intent: Replace "the suite is green" with a machine-countable claim: discovery parity measured independently of the runner's own selection pattern, registry resolution, skip accounting, pinning, and the wrapper's exit code as the only truth.
- grounding: M2-P1 merged (result contract and `src/gates/pin.ts`). Process doc section 2e item 8. Blueprint section 4. v1 section 5's note that the EXISTS wrapper lives in a project not present here, so it is BUILT from the contract under D-1's degradation rule. `clean-room-m1-p1.md` CR-002. Constraint C-1 and M2-C-5. **`MECHANISMS.md` row "Parsing another program's reporter output" and the M1-P6 defect that established it** (`clean-room-m1-p6-criteria.md` mutation table: the count parse was red at the parent on the floor toolchain and green at head on both), plus `clean-room-m1-p6-hazard.md` CR-604. Section 1.5 rows 5, 6, 7, **18 and 20** are this phase's contract.
- hazard class (T-007, M2-D-18): **a measurement that can shrink silently, which the process doc calls the most dangerous output a suite can produce.** What can make the suite smaller or the count wrong without anyone noticing: a checker whose enumeration shares the runner's own selection pattern, so the defect is invisible to the thing looking for it; a reporter whose default format differs between the two toolchains this project runs, so the parse works where it is tested and fails where it is authoritative; a skip with no reason; a count taken from prose; a behaviour deleted from the registry rather than from the code; and a wrapper defect that makes the suite itself unrunnable.
- steps:
  1. M2-C-1 verification: confirm the delivered `test/behaviors.json` shape and `npm test` script, and record which machine-readable reporters the pinned Node emits and which is chosen. Parsing a human-facing summary line is forbidden (C-1).
  2. Create `src/gates/suite.ts`: run the suite with the chosen machine-readable reporter into the evidence directory; parse counts from the structured stream and the process exit code only. **Reporter pin (revision 2, `MECHANISMS.md`).** The reporter is a controlled input, not an observation: the gate REQUESTS one named reporter explicitly for the child (never inheriting an ambient default), records the name it requested in the result record, and validates that the stream it received is that format before parsing a single count. A stream that is not the pinned format is `error` naming the expected and observed formats. **Widening the parse to accept whichever format arrived is forbidden**, because a format-agnostic matcher is a union of the formats known on the day, and the M1-P6 defect that established this rule was exactly a default that differs between the container toolchain and the floor toolchain, so the parse passed where it was written and failed where it counted.
  3. **Discovery parity by independent enumeration** (M2R-008): enumerate candidate tests by walking the declared test **roots** (`test/`) for the declared filename suffix (`.test.ts`), never by expanding the runner's own selection pattern, and compare that set with the reporter's file set. A file found by the walk but absent from the reporter is a parity failure. This is what makes the PR-106 hazard visible, because the pattern is the thing that can be wrong and an enumeration sharing it cannot see that.
  4. Registry resolution: every behavior in `test/behaviors.json` resolves by name to a test in the run, and every behavior registered at the merge base is still present (name comparison against the merge-base copy; `--base` absent is `error`).
  5. Skip accounting: a skipped test carries a non-empty reason; a skip without one is a parity failure (the executable form of EXT-F-05).
  6. Parity term mapping (M2R-022): R-048's identity is `passed + failed + skipped + did-not-run == discovered`. Record the mapping explicitly: `pass`, `fail` and `skipped` map directly; `cancelled` carries `did-not-run`; `todo` is recorded and counted; `discovered` is step 3's independent walk, not the reporter's `total`. A test that never registers appears in neither side of the reporter's arithmetic, and the compensating control for that bucket is step 4's registry resolution, stated as such.
  7. Pin (M2-C-5): the record carries start and end pins over the declared source and test roots.
  8. Exit-code truth: the wrapper's own exit code is the gate; if the runner exits 0 while any parity check fails, the wrapper exits nonzero. `units` equals tests reported.
  9. Register `suite` per section 1.4. Add an `npm run gate:suite` script; `npm test` stays a bare `node --test` so the suite is always runnable without the gate and a wrapper defect cannot make it unrunnable (M2-D-09). No CLAUDE.md edit; the human-facing gate list is owner item O-2.
  10. Tests in `test/suite-gate.test.ts` against fixture suites in scratch directories.
- files-to-touch: src/gates/suite.ts, test/suite-gate.test.ts (create); package.json (edit, add the gate script); gates.manifest.json, test/behaviors.json.
- acceptance criteria:
  1. On this repository the wrapper exits 0, status `green`, `units` equals tests reported, and the recorded arithmetic satisfies step 6's mapping with `discovered` taken from the independent walk.
  2. A test file placed at `test/sub/x.test.ts` that the configured runner pattern fails to select leaves the bare runner at exit 0 and makes the wrapper exit nonzero naming the file; correcting the pattern returns exit 0 (section 1.5 row 6, both directions; the wrapper's enumeration cannot share the defect because it walks roots rather than expanding the pattern).
  3. With a registered behavior's test renamed in a fixture suite, the bare runner exits 0 and the wrapper exits nonzero naming the behavior; restoring the name returns exit 0 (section 1.5 row 5, both directions).
  4. With a behavior present in the merge-base registry deleted from the head registry, the wrapper exits nonzero naming it (section 1.5 row 7).
  5. A fixture test skipped without a reason makes the wrapper exit nonzero; with a reason it stays at exit 0 (both directions).
  6. A fixture test whose body prints a counterfeit summary line (for example "pass 999") changes no recorded count and does not affect the verdict; the line is captured verbatim in the evidence (C-1).
  7. A fixture suite where the runner exits 0 but the reporter stream is truncated mid-run is `error`, not `green` (M2-C-3).
  8. The record carries equal start and end pins over the declared roots; a byte-identical rewrite of one source file between them makes the record `error` naming the path and `mtimeMs` (M2-C-5, both directions).
  9. Invoked without `--base`, the gate is `error`, not `not-applicable`, because the merge-base registry comparison cannot be performed.
  10. `node --test` exits 0 with 0 failing and zero unaccounted tests; the behavior registry criterion of section 1.4 holds.
  11. **Reporter pin (new in revision 2, section 1.5 row 18, both directions).** The record names the reporter the gate requested, and a fixture stream in a different but perfectly valid reporter format makes the gate `error` naming the expected and observed formats rather than parsing anything; the pinned format parses to the correct counts. Additionally, and this is the direction that reproduces the M1-P6 defect rather than merely describing it: the gate is run **on both installed toolchains, the container default and the floor toolchain**, and produces byte-identical counts and the same requested-reporter name in both records, with both runs and their `node --version` output recorded in the work history (CLAUDE.md warning 1: three Node versions are installed and which one you get depends on how the shell was started, so this is measured, not assumed).
- suggested model tier: strongest (the failure mode is a silently smaller suite, which the process doc calls the most dangerous output a suite can produce).
- citations: R-048; process doc section 2e item 8; blueprint section 4; C-1; M2-C-5; `clean-room-m1-p1.md` CR-002; v1 M1-P1 step 2 (PR-106); `MECHANISMS.md` (reporter output); `clean-room-m1-p6-hazard.md` CR-604; CLAUDE.md warning 1.
- conflicts-with: M2-P1 (`package.json`), plus the shared files of section 1.4.
- blocked-by: M2-P1 merged (both the result contract and `src/gates/pin.ts`). `package.json` is contended with M2-P1, which merges first by the dependency graph, so the contention is resolved by ordering and needs no special handling (`delivery/plan/m2-conflict-pre-pass.md` contention 2).
- parallelizable: **yes, concurrently with M2-P2 and M2-P4 to M2-P8 once M2-P1 has merged** (M2-D-19).

### M2-P4: Scope auditor

- id: M2-P4
- branch: claude/m2-p4-scope-auditor
- intent: Make "every changed file is on the phase's list or a declared extra" a check with an exit code, read from the merge-base copy of the declaration so it cannot be widened from inside the phase.
- grounding: M2-P1 merged. Blueprint section 4 and section 11. Process doc sections 1c and 3. CLAUDE.md's two standing pre-authorized extras. `clean-room-m1-p2.md` deviation 1, a real case: `test/behaviors.json` was edited while absent from the phase list and was correctly judged a clerical gap in the plan rather than scope creep, which is the distinction the standing-extras rule encodes.
- hazard class (T-007, M2-D-18): **a checker of a declaration that the party under audit can write.** What can defeat it: a declaration widened on the branch being audited; a branch name that does not match the phase pattern, which is the one path by which source can reach a pull request unaudited; a rename or a deletion counted on one side only; a merge base resolved against a stale local ref; and a diff computed against the head of `main` rather than against the merge base, which silently absolves everything another phase landed in between.
- steps:
  1. M2-C-1 verification of the M2-P1 contracts.
  2. Create `src/gates/schemas/phase-declaration.schema.json` and the loader: `{id, branch, filesToTouch: [literal paths], declaredExtras: [literal paths], citations: [ids]}`.
  3. Create `src/gates/scope.ts`: resolve the merge base of `--head` against `--base`; compute changed paths with `git diff --name-status`, counting renames as both paths and counting deletions; compare against the declaration plus the two standing extras, with the work-history extra scoped to `delivery/work-history/<phase-id>.md`.
  4. Read the declaration from the **merge base**, not the head, so an implementer editing it on their own branch cannot widen the audited scope. Record the declaration path and its merge-base blob sha256.
  5. `units` equals changed paths audited. A listed file not touched is not a violation and is recorded as an under-touch count in the detail line.
  6. Register `scope` per section 1.4: `required`, precondition `branch-matches` the phase pattern `claude/m[0-9]+-p[0-9]+-.*` plus a supplied `--phase`. Branch matches with no declaration at the merge base: `red`. Branch does not match (paperwork branches): `not-applicable` with the reason recorded. `--phase` or `--base` absent: `error` (M2-C-3).
  7. Tests in `test/scope-gate.test.ts` against scratch repositories.
- files-to-touch: src/gates/scope.ts, src/gates/schemas/phase-declaration.schema.json, test/scope-gate.test.ts (create); gates.manifest.json, test/behaviors.json.
- acceptance criteria:
  1. A declaration listing A and B with a diff touching A and B exits 0 with `units` 2; adding undeclared C exits nonzero naming C (both directions).
  2. A diff touching `test/behaviors.json` and `delivery/work-history/<this-phase>.md` without declaring them is green; touching `delivery/work-history/<another-phase>.md` is red naming it.
  3. Renaming a declared file to an undeclared path is red naming the new path; renaming to another declared path is green (both directions).
  4. Deleting a declared file is green; deleting an undeclared file is red naming it.
  5. A declaration modified on the head branch to add C does not change the verdict for a diff touching C: the gate stays red and the record's declaration sha256 equals the merge-base blob (the anti-widening property, staged against the dangerous state).
  6. A branch matching the phase pattern with no merge-base declaration is red naming the branch; a non-matching branch is `not-applicable` with the reason recorded; `--phase` omitted on a matching branch is `error` (three directions).
  7. `node --test` exits 0 with 0 failing and zero unaccounted tests; the behavior registry criterion of section 1.4 holds.
- suggested model tier: cheaper tier acceptable (diff arithmetic against a specified declaration).
- citations: R-020 (enforcement half; the verify-before-editing half is an M3 implementer-brief duty, Appendix A residue), R-058; blueprint section 4 and section 11; process doc sections 1c and 3; CLAUDE.md standing extras; `clean-room-m1-p2.md` deviation 1.
- conflicts-with: the shared files of section 1.4.
- blocked-by: M2-P1 merged.
- parallelizable: **yes, concurrently with M2-P2, M2-P3 and M2-P5 to M2-P8 once M2-P1 has merged** (M2-D-19). Note the standing duty this phase inherits from concurrency: with seven phases in flight, the phase declarations the auditor reads must all be on `main` before any of them dispatches (section 3 preamble).

### M2-P5: Citation linter

- id: M2-P5
- branch: claude/m2-p5-citation-linter
- intent: Make every file:line citation checkable, with an explicit root-matching rule so a citation into a repository this checkout does not contain is classified deterministically rather than guessed.
- grounding: M2-P1 merged. Blueprint section 4 and section 11. Process doc sections 1a and 1d. The real case in this repository: v1 M1-P3 step 1 and M1-P4 step 5 cite `bin/fm-lock.sh:47-85` and `bin/fm-teardown.sh:678-712` as **bare relative paths**, and this repository has its own `bin/` directory, so without a stated matching rule the same string resolves two ways (M2R-014). Constraint M2-C-6: this gate walks a whole tree of documents it did not create, which is the widest untrusted-path surface any M2 gate has.
- hazard class (T-007, M2-D-18): **a linter over documents it does not own, walking a tree supplied by configuration.** What can defeat it: a path that resolves under two declared roots, guessed rather than refused; a document with zero citations reading as clean; an external root whose match list is implied rather than stated; a whole-corpus scope that reddens merged history and tempts an implementer to edit documents outside the phase; and an entry in the walked tree that is not a regular file, which is the M1-P5 class (CR-520) applied to a gate rather than to the kernel.
- steps:
  1. M2-C-1 verification of the M2-P1 contracts.
  2. Create `src/gates/citations.ts` and its config schema: `roots` and `externalRoots`, **each carrying an explicit `match` list of path globs**; `documents` (the configured set); `citationRequired` (the subset in which a document with zero citations is red); and the recognized grammars (`path:line`, `path:start-end`, and the optional content-hash suffix, documented in the module). External roots match first and their globs are stated in the config (`bin/fm-*.sh`, `bin/fm-session-lock-lib.sh`); a path matching more than one root is a **config error reported as `error`, never guessed**.
  3. Resolution: a citation under a local root must name an existing file and a line or range within its line count, else `red` naming the citation and the file's actual line count. A citation under a declared external root is recorded `unverifiable-external` with provenance, counted separately, and never counted as a resolved unit. A citation matching no declared root is `red`.
  4. Vacuous guard, narrowed (M2R-014): a document in the `citationRequired` subset (plans, reviews, verifications) with zero recognized citations is `red`; other configured documents with zero citations contribute zero units and are recorded, not red, because `delivery/STATE.md`, tuition entries and work histories legitimately carry none. The `documents` and `citationRequired` globs are committed configuration and narrowing them is a scope-audited change.
  5. Scope (M2-D-10): the kernel entry lints configured documents **changed in the diff**; `--base` absent is `error`. The phase additionally performs a one-shot inventory over every existing `delivery/**` document, records the full result in its work history, and **reports** stale citations without editing any other document.
  6. Register `citations` per section 1.4; tests in `test/citation-gate.test.ts` against fixtures and real files.
  7. Second call site (R-025): the same executable is what a plan review runs; documented in the module. No second implementation.
- files-to-touch: src/gates/citations.ts, src/gates/schemas/citation-config.schema.json, test/citation-gate.test.ts (create); gates.manifest.json, test/behaviors.json.
- acceptance criteria:
  1. A fixture citing `src/cli.ts:1` is green with `units` equal to citations resolved; `src/cli.ts:<lineCount+1>` is red naming the citation and the line count; `src/nope.ts:1` is red naming the missing file (three directions).
  2. A range `path:12-40` inside a file of at least 40 lines is green; a range whose end exceeds the file is red naming the end line (both directions).
  3. A citation carrying the content-hash suffix is green when the cited lines still hash to the recorded value and red when one character changes (both directions).
  4. Against the real text of `delivery/plan/kernel-plan-v1.md`, with the firstmate external root declared as `match: ["bin/fm-*.sh", "bin/fm-session-lock-lib.sh"]`, the firstmate citations are counted `unverifiable-external` with provenance and the gate does not red on them; removing that root from the config makes the same citations red as unmatched (both directions, on a real document, with the matching mechanism stated rather than assumed).
  5. A path glob listed under two roots makes the gate `error` naming the ambiguous glob, never a guessed resolution.
  6. A `citationRequired` document with zero citations is red; the same document with one valid citation is green; a configured but not `citationRequired` document with zero citations contributes zero units and is not red (three directions).
  7. The one-shot inventory over `delivery/**` is recorded in the work history with per-document counts (resolved, unresolved, unverifiable-external), and the diff shows no document modified other than this phase's own files.
  8. `node --test` exits 0 with 0 failing and zero unaccounted tests; the behavior registry criterion of section 1.4 holds.
  9. **M2-C-6 over the walked corpus (new in revision 2, both directions).** With `mkfifo` used to place a named pipe at a path inside a configured `documents` root, the gate reports `error` naming the path and the observed type and returns, rather than blocking; with the same path a regular document it is linted normally. The run is bounded by a harness timeout so a block reports as a code rather than as an unexplained wait, and the reads route through the delivered `classifyEntry` rather than a second implementation (T-005).
- suggested model tier: cheaper tier acceptable.
- citations: R-010b, R-025; blueprint section 4 and section 11; process doc sections 1a and 1d; M2-C-6, `MECHANISMS.md`, CR-520.
- conflicts-with: the shared files of section 1.4.
- blocked-by: M2-P1 merged.
- parallelizable: **yes, concurrently with M2-P2 to M2-P4 and M2-P6 to M2-P8 once M2-P1 has merged** (M2-D-19).

### M2-P6: Coverage checker

- id: M2-P6
- branch: claude/m2-p6-coverage-checker
- intent: Make "no orphans" a check with an exit code: every finding lands in exactly one accepted bucket, parked buckets carry a reason, phantom coverage fails, milestone totals are computed rather than assumed, and every finding in a final report has exactly one non-empty outcome.
- grounding: M2-P1 merged. Blueprint section 4 and section 11. Process doc sections 1c and 7. SC-009 and D-7 (accepted reference types are exactly phase, decision, parked; an open question is a decision record with status open).
- hazard class (T-007, M2-D-18): **arithmetic over two documents that can drift, where a total can add up while a row is lost.** What can defeat it: a renumbering that leaves a bucket row pointing at an id that no longer exists; a bucket value matching no declared kind, silently uncounted; an empty inventory producing a green with nothing examined; a milestone extraction that cannot produce the totals the plan states, so the two views agree only because one is never computed; and a finding whose outcome cell is present but empty.
- steps:
  1. M2-C-1 verification of the M2-P1 contracts.
  2. Create `src/gates/coverage.ts` and a config schema declaring the `inventory` source, the `coverage` table source, and `bucketKinds`: a named set of regexes, each with a `requiresNote` flag **and a `milestone` extraction (a capture group)**, so the record reports counts per milestone as well as per kind (M2R-015: v1 Appendix A writes M1 rows as `M1-P1` to `M1-P5` and M2 to M5 rows as bare milestones, so kind counts alone cannot produce the stated totals). The kernel config declares phase (`M([0-9]+)-P[0-9]+`), milestone (`M([0-9]+)`), decision (`DR-[0-9]{4}`, `D-[0-9]+`, `M2-D-[0-9]+`), and `parked` with `requiresNote` true.
  3. Checks: an inventory id with no bucket row (orphan); with more than one bucket row (double-bucketed); a bucket row whose id is absent from the inventory (phantom coverage, which is how a renumbering is caught); a bucket value matching no declared kind; a `parked` row with an empty note.
  4. Finding-to-outcome parity (R-089b): given a report's findings table and the same inventory, every finding has exactly one non-empty outcome; missing, duplicated or empty fails naming the id. The input shape is declared here and is the contract M3's report schema must satisfy or supersede (section 2 item 2).
  5. `units` equals inventory ids checked; an empty inventory is `error`, never green (M2-C-2).
  6. Register `coverage` per section 1.4; tests against fixtures and this repository's real artifacts.
- files-to-touch: src/gates/coverage.ts, src/gates/schemas/coverage-config.schema.json, test/coverage-gate.test.ts (create); gates.manifest.json, test/behaviors.json.
- acceptance criteria:
  1. Against the real pair (`delivery/requirements/migration-table.md` as inventory, `delivery/plan/kernel-plan-v1.md` Appendix A as coverage table), the gate exits 0 with `units` 115 and reports per-milestone counts M1 11, M2 16, M3 74, M4 13, M5 1, parked 0, alongside per-kind counts phase 11 and milestone 104 (two different views, both recorded; this is the arithmetic M2R-015 found unproducible without the extraction rule).
  2. A bucket value `M1-P3` is counted under milestone M1 and kind phase; a row whose bucket matches no declared kind is red naming the row (both directions).
  3. With one appendix row deleted in a scratch copy, the gate exits nonzero naming that row id as an orphan; restoring it returns exit 0 (both directions).
  4. A row duplicated into two buckets is red naming the id as double-bucketed.
  5. A bucket row whose id is absent from the inventory is red naming it as phantom coverage.
  6. A `parked` row with an empty note is red; with a non-empty note it is green (both directions).
  7. An empty inventory is `error` with `units` 0, counted vacuous rather than green.
  8. Finding-to-outcome parity: a fixture report covering 5 of 6 inventory ids is red naming the sixth; an empty outcome cell is red naming that id; all six covered and non-empty is green with `units` 6 (three directions).
  9. `node --test` exits 0 with 0 failing and zero unaccounted tests; the behavior registry criterion of section 1.4 holds.
- suggested model tier: cheaper tier acceptable.
- citations: R-023, R-089b; blueprint section 4 and section 11; process doc sections 1c and 7; SC-009, D-7.
- conflicts-with: the shared files of section 1.4.
- blocked-by: M2-P1 merged.
- parallelizable: **yes, concurrently with M2-P2 to M2-P5, M2-P7 and M2-P8 once M2-P1 has merged** (M2-D-19).

### M2-P7: Release verification contract, with deploy and migrations as its two registry entries

- id: M2-P7
- branch: claude/m2-p7-deploy-and-migration-verifiers
**Rewritten at revision 2 (2026-08-05) under DR-0014 and `delivery/verification/release-verification-interface.md`.** Revision 1's version of this phase built two independent verifiers around a `{endpoint, statusPath, readyValue, failureValues}` configuration, with the poller's response semantics conditional on a platform being reachable. DR-0014 rejects the framing that release verification is one thing with a platform selector, and the investigation that DR-0014 commissioned establishes that the revision 1 configuration shape cannot express two things real platforms do, one of which is precisely the incident this gate exists to prevent. The centre of gravity moves from "poll a platform" to "define the contract and prove it with adapters". Revision 1's steps 3 and 4 and its criteria 1 to 4 are superseded; what survives is named below rather than silently dropped.

- intent: Build ONE post-merge release-verification contract with two registry entries, in which the kernel owns the subject, the clock and the loop, and an adapter owns the platform mapping. Ship the record and its schema, the kernel-owned poll loop, the subprocess adapter boundary with its fail-closed rules, and two reference adapters whose fixtures are byte-derived from real captured platform responses. Prove that on this repository both entries report not-applicable with a named reason, and prove the contract's anti-fooling rules against the dangerous state rather than against the absent feature.
- grounding: M2-P1 merged. **DR-0014** (release verification is a pluggable interface with kernel-shipped reference adapters, decided in principle 2026-08-05) and **`delivery/verification/release-verification-interface.md`**, the investigation it commissioned, whose appendix A holds real captured Vercel deployment records, real GitHub Actions workflow-run fields and a real Supabase applied-migration response. Blueprint section 4 and section 11. Process doc section 4 (both recorded incidents: deploys silently not spawning, migrations skipped by a flake while the code deployed anyway). SC-011. C-2 and C-3. M2-C-3. T-003 lesson 4 and CLAUDE.md warning 10 (derive from real captured output, never from hand-written strings chosen to match the implementation). M2-D-07 (every toolbelt boundary is a subprocess with an exit code), which the investigation adopts as the adapter boundary.
- hazard class (T-007, M2-D-18): **a verifier that can be fooled while reporting an observation, which is worse than no verifier because it converts an unchecked assumption into a recorded, cited, evidenced one.** What can produce a green that means something else: an adapter that verified a different subject and was believed; a commit sha matched without a target, so a preview build answers for production; a release object created before the merge, so the previous release answers for this one; a `pending` or `absent` state read as a pass; an expired or unresolvable credential producing a permanent soft state, which is the M1-P5 stranded-claim shape exactly; an adapter that hangs and takes the kernel with it, or that owns its own timeout and decides what it means; an adapter whose only observation is that the deploying command exited 0, which is incident one restated; and a verification switched to `none` inside the branch that needed it on.
- steps:
  1. Verification-first: confirm and record that this repository declares no release verification, no migrations and no charter, and record explicitly that **release verification is structurally not a pull-request gate** (investigation section 1.3 and observation O-3): it runs after a merge, against a commit that exists only once the merge has happened, so a pre-merge bundle can only ever report not-applicable, on every repository. That record is part of the phase's evidence, and it is the sentence that stops a later reader concluding the exit test exercised these gates.
  2. **Captured-response rule, now largely dischargeable rather than conditional (supersedes revision 1 step 2).** The captures the rule demanded exist: appendix A of the investigation holds a Vercel deployment object with its verbatim field names and observed values, GitHub Actions runs in which `status` and `conclusion` are separate fields and an in-progress run carries no `conclusion` key at all, and a Supabase `list_migrations` response of `{"migrations":[]}` from two `ACTIVE_HEALTHY` production projects. Commit these under `test/fixtures/release/` as captures with provenance, and derive every parser fixture from them byte for byte. **What remains uncaptured is named and is not invented**: no non-success deployment state from any platform was observed, so **no failure vocabulary ships**. The safe rule that needs no such list, and which is what the phase implements: one satisfying value per configured adapter, every other observed value recorded verbatim and treated as `pending` until the deadline, at which point it becomes `red` naming the last observed value. Unknown never becomes green. The phase records which values it observed and which it did not.
  3. **Create `src/gates/release.ts`, the one contract module.** It owns:
     - **The subject**, which the kernel knows for certain at merge: `{repository, integrationRef, mergedSha, mergedAt, phaseId}`. Platform vocabulary (deployment id, workflow run id, build id, migration set) is the adapter's and the kernel never learns it.
     - **The outcome vocabulary**, six values: `satisfied`, `failed`, `pending`, `absent`, `not-applicable`, `error`. There is deliberately no `unknown`, no `skipped` and no `warn`, because a soft state is where a false green hides (M2-C-3).
     - **A total mapping to `GateResult` status, implemented in exactly one place**: `satisfied` to `green` with `units` 1 per verification satisfied, `failed` to `red`, `not-applicable` to `not-applicable`, `error` to `error`. **`pending` and `absent` are never terminal**; they are loop states, and at the deadline the kernel converts both to `red` with distinct reasons, `deadline reached, last observed <value>` and `deadline reached, no release object for subject`. An adapter cannot make pending mean pass.
     - **The clock.** Interval, deadline, maximum attempts and the verdict at the deadline are the kernel's, from configuration the kernel validated. The adapter never sleeps, never loops, never schedules and never decides what a timeout means. Every attempt writes a record carrying the attempt number, the wall-clock instant, the outcome, the resolved identity and the transport-level detail. Polling is foreground (C-3).
     - **The subprocess boundary.** An adapter is an executable named by committed configuration as an argv, spawned with a request file path in argv (so nothing sensitive lands in a process listing), writing its response to a kernel-supplied `recordPath`. Because it is a subprocess, the per-attempt timeout is enforceable rather than promised: the kernel kills an attempt that overruns and records that attempt as `error`, and an adapter that hangs cannot hang the kernel. This is the second reason the boundary is a process, the first being that it works for any language.
     - **Seven fail-closed rules on the response**, each corresponding to a way a verifier can be fooled: exit 0 with no response file written is `error`, not success; a response failing schema validation is `error` naming the field; a response whose echoed subject differs field by field from the passed subject is `error`, checked BEFORE the outcome is read; `satisfied` with no `resolved` identity is `error`; `satisfied` with no `observation` is `error`; an unrecognized `contractVersion` is `error` naming the versions the kernel accepts; an `outcome` outside the enum is `error`, never coerced.
     - **The release-object-older-than-the-merge check**, recorded as an observation rather than promoted to an error: the adapter reports `observedAt` and the kernel knows `mergedAt`, so a release object whose creation instant precedes the merge is flagged in the record. The investigation declines to make it a hard error without one real counter-example, and so does this plan (section 8 item 8 of the investigation).
  4. **`src/gates/deploy.ts` and `src/gates/migrations.ts` become thin entry points** that name a declared verification and call `src/gates/release.ts`. The manifest keeps its two static entries exactly as section 1.4 declares them, so no gate-runner or exit-test arithmetic changes. **M2 does not generalize to N charter-declared verifications**; that is M3's, per section 2 item 12.
  5. **Two reference adapters, which use the same boundary as any third party's.** `src/gates/adapters/migrations-command.ts` (repository inventory from a declared directory and pattern; applied inventory from a project-declared command; id comparison always, plus content-drift comparison where the applied inventory exposes a checksum) and `src/gates/adapters/http-json.ts` (the generic transport adapter, and **the only place a JSON pointer exists**). Each ships as a module with a main guard, invoked as a subprocess by argv, per M2-D-07. **They are not privileged**: no in-process path exists for a kernel-shipped adapter, because a defect in the subprocess boundary that the shipped adapters bypass would be invisible until the first third party hit it. Promoting them to a `tiphys verify-adapter <id>` subcommand is deliberately NOT done here, because M2-D-07 reserves `src/cli.ts` to M2-P1; the promotion is M3's or M4's call and costs nothing later.
  6. **Extraction leaves the interface (the single largest design change).** An adapter returns an OUTCOME; it never returns a `statusPath`. JSON-pointer extraction is a feature of `http-json` configured per project, and the kernel never learns what a `statusPath` is. This is what makes third-party adapters possible at all, since a store or service-manager adapter has no JSON pointer to offer, and it is what dissolves revision 1's criterion 3 rather than patching it.
  7. **Not-applicable becomes three distinguishable states** (DR-0014, investigation section 4): a **declared none** reports `not-applicable` carrying `declared: true`, a non-empty reason, and the declaration's source path and merge-base blob sha256; a **precondition unmet** reports `not-applicable` carrying `declared: false`, the precondition id and the evidence of its evaluation; a **misconfiguration** (adapter not found, credential variable unset, endpoint missing) reports `error`, never not-applicable. Four defences make a silent disabling cost something: an absent configuration field is `error`, not `none`, because silence is never permission; `none` without a non-empty reason is `error`; **the declaration is read from the MERGE BASE, not the head**, exactly the anti-widening rule M2-P4 step 4 uses and for exactly the same reason; and the reason is surfaced in the record and the bundle.
  8. **Credentials: the contract only, not the wiring.** The configuration declares credential NAMES, never values, and a named credential that is not resolvable is `error`, never a silent unauthenticated request that a platform answers with a 401 some parser then reads as pending. The investigation's design is that the kernel injects exactly those named variables as a per-invocation extension of M2-P8's allowlist. **M2-P7 does not build that binding**, because M2-P7 and M2-P8 run concurrently under M2-D-19 and a phase may not depend on a module a sibling owns. The wiring lands at M4 with the pilot, where a real credential first exists, and this step records the obligation so it is not lost: the extension is per-invocation and per-adapter, the allowlist is never replaced by a denylist and never disabled, and the extension may never include a pull-request-capable or push-capable credential, which is checkable by running M2-P8's own `credential-scrub` probe from inside the adapter's child environment rather than by building a second mechanism.
  9. R-032's blocking half, unchanged from revision 1: the contract module writes a verdict record keyed to the verified commit sha and documents the consumption contract ("the next dispatch requires a green verdict record for the merged sha"). The enforcement is wired at M4 with the pilot (M2-D-11); M2 adds no dispatch block to `spawn`.
  10. Register both entries per section 1.4 as `conditional` with `file-exists` preconditions on the release-verification declaration.
  11. Tests in `test/deploy-gate.test.ts` (in-process `node:http` stub on loopback, ephemeral port, no external network, replaying the appendix A captures), `test/migration-gate.test.ts` (fixture inventory commands) and `test/release-contract.test.ts` (the loop, the boundary and the seven fail-closed rules, each against a purpose-built misbehaving adapter). Deadlines in tests are short and explicit; the production default is never used in a test (CLAUDE.md warning 11, suite wall time grows with real-clock waits).
  12. **Nine things this phase must NOT decide**, carried from the investigation's section 8 so an implementer does not fill them by inventing: any platform's non-success status vocabulary; whether `state` or `readyState` is authoritative where they differ; whether the interface must distinguish "built" from "serving"; which applied-migration inventory is authoritative per stack and whether checksums exist; the generalization to N charter-declared verifications; long-running release milestones such as a store review, which is a different mechanism with a different lifetime and not an extension of this interface; the kernel's maximum deadline and default poll interval, which are numbers and are settled by measuring real releases on the pilot, the one captured data point being a 318-second Vercel production build; whether the older-than-the-merge check is a hard error; and whether in-process npm adapters are ever admitted. Each is settled by a named capture or by M4's pilot, not by this phase.
- files-to-touch: src/gates/release.ts, src/gates/deploy.ts, src/gates/migrations.ts, src/gates/adapters/http-json.ts, src/gates/adapters/migrations-command.ts, src/gates/schemas/release-record.schema.json, src/gates/schemas/verifier-config.schema.json, test/deploy-gate.test.ts, test/migration-gate.test.ts, test/release-contract.test.ts, test/fixtures/release/ (create; each committed capture is listed as a literal path in the phase declaration); gates.manifest.json, test/behaviors.json. Every created path is still disjoint from every other M2 phase, so `delivery/plan/m2-conflict-pre-pass.md`'s conclusion survives this rewrite.
- acceptance criteria:
  1. **Loop arithmetic.** Against a stub adapter returning `pending` twice then `satisfied`, the gate exits 0 with `units` 1 and exactly three attempt records, each carrying its attempt number, instant, outcome, resolved identity and transport detail; against a stub that returns `pending` forever it exits nonzero at the deadline with elapsed at least the configured deadline and the last observed outcome named (both directions). Supersedes revision 1 criterion 1, which counted polls of an endpoint rather than attempts of an adapter.
  2. **`absent` is not `pending`, which is the incident-one witness.** Against a stub returning `absent` twice then `satisfied`, the gate exits 0; against a stub returning `absent` forever it exits nonzero at the deadline with the reason `deadline reached, no release object for subject`, textually distinct from the pending deadline reason (both directions). A gate that cannot distinguish these two cannot detect "deploys silently not spawning", which is the incident this gate is named after.
  3. **Superseded, and replaced (revision 1 criterion 3 made an absent status pointer `error`).** The investigation's observation O-2 shows that criterion misclassifies a healthy in-flight run: a real in-progress GitHub Actions run carries `"status":"in_progress"` and **no `conclusion` key at all**, and a pointer at `conclusion` would therefore make a perfectly healthy deploy `error`. Fail-closed is right; classifying "not finished yet" as "cannot reach a verdict" is not. The replacement, staged against the real captures in `test/fixtures/release/`: **(a)** replaying the captured in-progress run, `http-json` reports `pending` and the gate loops; **(b)** replaying the captured `"status":"completed","conclusion":"success"` run it reports `satisfied`; **(c)** replaying the captured `"status":"completed","conclusion":"cancelled"` run it reports a non-satisfying terminal value that becomes `red`; **(d)** against a body the adapter genuinely cannot parse it reports `error` (four directions, all four derived from captured output rather than from strings written to match this implementation).
  4. **The seven fail-closed rules, each red against the dangerous state rather than against the absent feature.** Seven purpose-built misbehaving adapters, one per rule: exits 0 writing nothing; echoes a subject differing in one field; returns `satisfied` with no `resolved`; returns `satisfied` with no `observation`; returns an unrecognized `contractVersion`; returns an out-of-enum outcome; and hangs past its per-attempt timeout. Each produces `error` naming the rule and the field, and **the hanging adapter's attempt is `error` while the kernel returns**, asserted by the command having returned before the assertion executes and bounded by a harness timeout so a block reports as a code rather than as an unexplained wait. With each adapter corrected, the same run reaches its real verdict (seven directions plus their inverses).
  5. **Subject echo and identity, staged against the real Vercel capture.** Against the captured deployment list, an adapter filtering on `meta.githubCommitSha` AND `target` reports `satisfied` for the production record; with the same sha present only on a `target: null` preview record it reports `absent`, not `satisfied` (both directions). This is the fooling the capture proves is possible: sha `929d387be1fc2d1c9464d172b9610947076ccf9e` appears on two distinct deployment ids in the capture, one with `"target": null` and one with `"target": "production"`, so a verifier keyed on sha alone would report satisfied from a preview build while production never deployed.
  6. Structural: grep over `src/gates/release.ts`, both entry points and both adapters shows no `detached`, no `unref`, no `process.kill` used for identity or exclusion, no `/proc` and no pid usage; the run is synchronous, asserted by the command having returned before the assertion executes (C-2, C-3). The per-attempt kill is a timeout enforcement on a process the kernel itself spawned and is exempted explicitly in the module documentation, which is the one place a `kill` may appear, and the exemption names C-2's actual prohibition (pid as identity or exclusion) to show it is not being widened.
  7. Migration verifier: repository [001, 002] with applied [001] is red naming 002; applied [001, 002, 003] is red naming 003 as drift; equal lists are green with `units` 2; equal ids with a differing checksum, where the inventory exposes checksums, is red naming the migration (four directions).
  8. **The two inventories are not symmetric, and the capture is why.** A declared REPOSITORY migrations location containing zero migrations is `not-applicable` with that reason. **A non-empty repository inventory with an EMPTY applied inventory is `red`, never not-applicable**, because that is the second recorded incident exactly ("migrations skipped by a flake while the code deployed anyway"). The fixture for the empty applied inventory is the captured `{"migrations":[]}` response, which the investigation observed from two `ACTIVE_HEALTHY` production projects and which therefore is a real response a real healthy project produces, not a synthetic edge case (both directions).
  9. **Not-applicable as a declared configuration, four directions.** A merge-base declaration of `none` with a non-empty reason reports `not-applicable` carrying `declared: true`, the reason and the declaration blob sha256; the same declaration absent entirely is `error`, not a quiet skip; `none` with an empty reason is `error`; and a declaration flipped to `none` on the head branch does not change the verdict, with the record's sha256 equal to the merge-base blob (the anti-widening property, staged against the dangerous state exactly as M2-P4 criterion 5 does).
  10. **On this repository both gates are `not-applicable`**, each naming its reason and the evidence of evaluation, and the record states that this is structural rather than local. With a fabricated release-verification declaration in a scratch copy naming an adapter that reports `absent` forever, the deploy entry becomes applicable and reports `red` at the deadline; with a fabricated migrations declaration whose repository inventory holds two migrations and whose applied inventory returns one, the migration entry becomes applicable and reports `red` (both verifiers carry the not-hardcoded witness, in both directions).
  11. No secret value appears in any evidence record or attempt record: configuration and records name environment variable names only, and a test setting a token-shaped value in a configured credential variable asserts the value appears nowhere under the evidence directory, including in captured adapter stdout and stderr.
  12. `node --test` exits 0 with 0 failing and zero unaccounted tests; the behavior registry criterion of section 1.4 holds.
- suggested model tier: strongest (fail-closed semantics, a process boundary, an anti-fooling surface, and the highest risk of building for a state that does not exist; risk 1).
- citations: DR-0014; `delivery/verification/release-verification-interface.md` sections 1 to 8 and appendix A; R-032 (residue at M4 per M2-D-11), R-068, R-069; blueprint section 4 and section 11; process doc section 4; SC-011; C-2, C-3; M2-C-3; M2-D-07; T-003 lesson 4; CLAUDE.md warnings 10 and 11.
- conflicts-with: the shared files of section 1.4.
- blocked-by: M2-P1 merged.
- parallelizable: **yes, concurrently with M2-P2 to M2-P6 and M2-P8 once M2-P1 has merged** (M2-D-19). It is the largest of the seven and, on the pre-pass's reasoning, the likely determinant of the concurrent window's wall time.

### M2-P8: Credential scoping

- id: M2-P8
- branch: claude/m2-p8-credential-scoping
- intent: Make "implementers never create PRs" structural rather than instructional: the executor hands each child a redirected, allowlisted environment in which **no pull-request-capable credential can be resolved from any source**, and the gate proves that by probing the sources rather than by counting variable names.
- grounding: M2-P1 merged; M1-P4's `subprocessAdapter` merged at 6ec0482 with `spawnSync(program, args, {cwd, stdio})` and no `env` option, plus a second `spawnSync` for the turn-end hook, so both children inherit everything today. Blueprint section 4 and section 11. Process doc sections 0 and 2a. DR-0004 item 4, whose exact setup this plan proposes as owner action A-3; **DR-0004's ruleset is now LIVE and was witnessed refusing a merge on a stale branch (2026-08-05), so only item 4 remains queued**. M2R-004 is the security-relevant finding this phase answers: an environment-only scrub leaves the credential stores in place, and dropping the store pointers restores the default paths.
- hazard class (T-007, M2-D-18): **a scrub that can be believed rather than probed, in the one phase where being wrong is a security defect rather than a process defect.** What survives an environment scrub: a credential store still reachable by a default path once its pointer is dropped rather than redirected; a second child launch that nobody wired; a metric that counts variable names, which grows by adding names and measures a tautology; an allowlist widened by an implementer to turn a red gate green; a probe whose assertion was reshaped to fit an unexpected status code instead of escalating; and a redirected `HOME` silently breaking the M1 exit harness's commit and push path.
- steps:
  1. M2-C-1 verification: read the delivered adapter and both `spawnSync` call sites and record how each child environment is constructed; read the delivered `scripts/stub-payload.sh` and `scripts/m1-exit-test.sh` and record whether the stub payload opens the pull request itself.
  2. Create `src/exec/env.ts`. The child environment is built from an **allowlist** (M2-D-13): PATH, LANG and locale variables, TMPDIR, the `TIPHYS_*` variables spawn needs, the command-scoped git identity variables (`GIT_AUTHOR_*`, `GIT_COMMITTER_*`, preserving the EXT-F-02 discipline), and nothing else by default.
  3. **Redirect, never drop, the credential-store pointers** (M2R-004 edit 1). `HOME`, `XDG_CONFIG_HOME`, `GH_CONFIG_DIR`, `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` are set to harness-owned empty paths inside the task directory. Dropping them is forbidden and the module records why: dropping a redirection returns the child to the default credential path (`$HOME/.config/gh/hosts.yml`, `~/.gitconfig` credential helpers, `~/.netrc`, `~/.git-credentials`), and dropping `GIT_CONFIG_GLOBAL` would additionally undo v1 M1-P2's EXT-F-02 discipline by handing the child the user's real global git configuration.
  4. Wire the allowlist into **both** child launches: the payload adapter and the turn-end hook invocation (M2R-004 edit 4).
  5. Consequence for the M1 exit-test harness: if the delivered stub payload opens the pull request itself, move pull-request creation out of the payload and into the harness, so the M1 exit test passes under the scrub and the exercised shape matches R-008 instead of contradicting it. If the harness already creates it, record that and change nothing.
  6. Create `src/gates/credentials.ts` with two registered entries. **`credential-scrub` (required, offline, capability-based rather than name-based)**: from inside the constructed child environment, assert that no pull-request-capable credential can be resolved from any probed source: environment, gh configuration (`gh auth status` reports no authenticated host), git global and system configuration (`git config --get-all credential.helper` is empty), `~/.netrc`, `~/.git-credentials`. `units` equals the number of credential **sources probed**, not names checked, because under an allowlist no excluded name can survive by construction and a name count measures a tautology that grows by adding names (M2R-004). **`credential-token` (conditional)**: when `TIPHYS_IMPLEMENTER_TOKEN` is present, run the safe negative probe; when absent, `not-applicable` naming owner action A-3, never green.
  7. Verification-first for the token probe, before writing any assertion: capture the real API responses for the scoped implementer token and for an orchestrator token, record both verbatim in the work history, and derive the assertion from them. If the observed status codes differ from expectation, stop and escalate rather than reshaping the assertion to fit (T-003 lesson 4).
  8. Tests in `test/credentials-gate.test.ts`; the token probe is skipped locally with a recorded reason (owner-held) and witnessed once during the M2 exit test.
  9. **Forward-looking note, added at revision 2, which changes nothing this phase builds.** `delivery/verification/release-verification-interface.md` section 6.1 designs release-verification credentials as a per-invocation, per-adapter, named extension of the allowlist this phase creates, and proposes reusing `credential-scrub` from inside a verifier's child environment rather than building a second mechanism. **M2-P7 and M2-P8 run concurrently under M2-D-19, so neither may consume the other's module**, and the binding therefore lands at M4 with the pilot. What this phase owes that future is only that `src/exec/env.ts` exposes the allowlist as data that can be extended per invocation rather than as a hard-coded literal inside the spawn call, which costs nothing now and is a rewrite later. Do not build the extension, the verifier hook, or any release-verification awareness in this phase.
- files-to-touch: src/exec/env.ts, src/gates/credentials.ts, test/credentials-gate.test.ts (create); src/spawn.ts (edit: the subprocess adapter's child environment and the turn-end hook invocation; verify the seam first), src/hooks.ts (edit only if the generated hook script needs the same redirection), scripts/stub-payload.sh (edit only if step 1 finds the payload creates the PR), scripts/m1-exit-test.sh (edit only in that same case); gates.manifest.json, test/behaviors.json.
- acceptance criteria:
  1. A spawn whose stub exec dumps its environment shows none of the credential variables the parent had set, and shows `HOME`, `GH_CONFIG_DIR`, `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` pointing at harness-owned paths inside the task directory; with `--allow-pr-credentials` the parent's values are present (both directions).
  2. The scrub is an allowlist: `TIPHYS_UNRELATED_SECRET` set in the parent does not reach the child; an allowlisted variable does (both directions; a denylist implementation cannot satisfy this).
  3. **Capability check with its own dangerous state staged** (M2R-004 edit 3, replacing revision 0's criterion that relied on an ambient `GITHUB_TOKEN` the CI workflow does not export): the test writes a token-shaped `hosts.yml` into a fake `HOME` and a credential helper into a fake global git config, then asserts from inside the constructed child environment that neither is resolvable and that `credential-scrub` is green with `units` equal to the sources probed; removing the redirection so the child sees the fake `HOME` makes the gate red naming the resolvable source (both directions, and this criterion cannot pass while the thing it guards is broken).
  4. The same scrub and the same assertions hold for the turn-end hook child (the second `spawnSync`), witnessed separately.
  5. No regression: the M1 exit-test harness in local mode exits 0 after this phase, with the stub payload's commit and push succeeding under the redirected environment through the command-scoped git identity (witnessed end to end, not asserted).
  6. `credential-token` is `not-applicable` naming owner action A-3 when no implementer token is present, counted not-applicable rather than green.
  7. `credential-token`, witnessed once with the owner-provisioned token: the probe records the captured HTTP status and body verbatim and is green; with an unscoped token it is red (both directions). Pull-request list counts are identical before and after every probe run, so the probe creates nothing.
  8. `node --test` exits 0 with 0 failing and zero unaccounted tests; the behavior registry criterion of section 1.4 holds.
- suggested model tier: strongest (credentials, an external API contract, and an edit to a merged M1 seam).
- citations: R-008; blueprint section 4 and section 11; process doc sections 0 and 2a; DR-0004 item 4; v1 M1-P2 criterion 7 (EXT-F-02, the discipline the redirection preserves); T-003 lesson 4; `delivery/verification/release-verification-interface.md` section 6.1 (forward note only).
- conflicts-with: M2-P9 (`scripts/`), plus the shared files of section 1.4.
- blocked-by: M2-P1 merged; M1-P6 merged (the harness scripts step 5 may edit). The `credential-token` witness is blocked by owner action A-3; nothing else in the phase is.
- parallelizable: **yes, concurrently with M2-P2 to M2-P7 once M2-P1 has merged** (M2-D-19). Its only overlap with M2-P9 is `scripts/stub-payload.sh`, and M2-P9 grounds on M2-P8 merging, so the contention is resolved by ordering (`delivery/plan/m2-conflict-pre-pass.md` contention 3).

### M2-P9: M2 exit-test harness and evidence

- id: M2-P9
- branch: claude/m2-p9-exit-test
- intent: Turn "all gates run green in CI on the kernel repo itself" into a scripted, falsifiable procedure producing a committed evidence bundle, run in the state where every gate can actually evaluate, and prove the assertion code fails without shipping any switch that makes a production gate lie.
- grounding: M2-P1 to M2-P8 merged. Blueprint section 13 row M2 as amended by SC-011. The M1-P6 harness pattern, **including the four rounds of findings it produced**: CR-604 (an exit-code-only suite check passes on a suite with zero tests), CR-640, CR-661, CR-681 and CR-720 to CR-722 (a guard asserting text survives a defang that preserves the text), CR-680 (a bundle recording a claim the run then contradicted) and CR-602 (a bundle record carrying a hand-written value that cannot be re-derived from the bundle). M2R-003 (the exit run needs a base, a head and a phase) and M2R-011 (falsifiability must not ship an override).
- hazard class (T-007, M2-D-18): **an exit test that can pass vacuously, which would be the most ironic failure available to this milestone.** What can make the bundle green while proving nothing: a gate silently not run whose absence reads as success; a record present for a gate that did not execute; an assertion over a count rather than over the expected-status table, so a missing gate is invisible; a self-test that exercises a shipped production override rather than the assertion code; the two bundles conflated so the weaker one's silence is read as the stronger one's evidence; a bundle value hand-written rather than derived, which cannot be re-checked from the bundle; and a wiring assertion over the workflow's TEXT that survives the workflow being defanged, which M1-P6 produced six times.
- steps:
  1. Create `scripts/m2-exit-test.sh <evidence-dir>`: run `npm ci`, `npm run build`, then `node dist/bin/tiphys.js gates run --manifest gates.manifest.json --evidence <dir> --base main --head HEAD --phase M2-P9`; then evaluate the assertions of step 2. Every step appends a JSON evidence record; no record is written for a command not executed.
  2. Assertions over the bundle, made against **section 1.4's expected-status table for the PR bundle** rather than against a bare count (M2R-003 edit 3): every gate in the table has exactly one record with the expected status; every `required` gate is green; every `not-applicable` record names its reason and the evidence of evaluation, which for a precondition-unmet record is the precondition id and its evaluation and for a declared-none record is the declaration path and its merge-base blob sha256 (the three states of M2-P7 step 7 are distinguishable in the bundle, not collapsed); zero `error`; zero vacuous; recomputed counts equal `summary.json`.
  3. **Falsifiability without a production override** (M2R-011): `scripts/m2-exit-test.sh --self-test <dir>` runs the same assertion code over a **fixture** manifest containing (a) a fixture gate that writes its own record with status `green` and `units` 0, and (b) a `required` fixture gate whose `file-exists` precondition names an absent file. Under each the harness exits nonzero naming the gate. No production gate carries an override flag, and because M2-D-07 makes gates subprocesses that author their own records, a hand-written record file is the realistic dangerous state rather than a synthetic one.
  4. Add the second, explicitly weaker **main bundle**: `gates run --manifest gates.manifest.json --evidence <dir-main> --only manifest-self-check,suite,coverage,credential-scrub,deploy,migrations`, whose expected statuses are section 1.4's main-bundle column. The three diff-scoped gates are **not run** in this bundle and therefore have no record, which the assertions state explicitly rather than reading their absence as success.
  5. **CI wiring, single caller** (M2R-026): the workflow's `test` job invokes the exit harness only, and the harness is the single caller of `gates run`; M2-P1's direct `gates run` step is replaced, not duplicated, so the gate set runs once per pull request. No new required check name.
  6. Commit the milestone bundle at `delivery/evidence/m2-exit-test/` through a paperwork pull request.
- files-to-touch: scripts/m2-exit-test.sh, test/m2-exit-test.test.ts (create); .github/workflows/gates.yml (edit, replacing M2-P1's step with the harness invocation); gates.manifest.json, test/behaviors.json.
- acceptance criteria:
  1. `scripts/m2-exit-test.sh <dir>` on the phase pull-request head exits 0 on a machine with only git, Node at the floor, and npm, and the evidence contains one record per PR-bundle gate plus `summary.json`, each validating against the gate-result schema.
  2. The bundle matches section 1.4's PR-bundle column exactly: seven required gates green, `deploy` and `migrations` not-applicable with their reason named and recorded as structural rather than local, `credential-token` green or not-applicable per A-3, zero `error`, zero vacuous.
  3. `--self-test` exits nonzero naming the vacuous fixture gate, and in its second fixture exits nonzero naming the required not-applicable fixture gate; no environment variable exists anywhere in the package whose effect is to change a production gate's reported status (grep evidence recorded).
  4. The main bundle exits 0 with exactly the six gates of step 4 recorded and no record for the three diff-scoped gates; the harness's assertion states that their absence is expected in this bundle and would be a failure in the PR bundle (the two bundles are distinguishable, not conflatable).
  5. The CI `gates` check completes successfully on the phase PR, the check-run list contains exactly the contexts it contained before this phase, and the gate set is invoked exactly once per run (evidenced by one `summary.json` per job). **The test that guards this wiring asserts BEHAVIOUR, not text (revision 2, section 1.5 row 17 and CLAUDE.md's "one witness is not a class").** The workflow step is a shell script: extract it and execute it against stubs, asserting it exits nonzero when the harness exits 0 and zero when the harness fails at the expected point. Its witness declares at least two structurally different members, at least one of which preserves every string the test asserts on while inverting the behaviour (`exit 1` to `exit 0`, or a step-level `if: false`). A witness that reddens only when the step is deleted does not satisfy this criterion, which is precisely the finding M1-P6 produced six times across four rounds.
     5a. **Containment and scope of the wiring assertion** (CR-720, CR-721, CR-722, all reproduced with captured output). The test asserts that the guarded step is INSIDE the job the required check consumes, not merely present somewhere in the file; it applies the same key whitelist to the fan-in job's own step and to the fan-in job's own keys, since a step-level `if: false` there turns the required check green unconditionally; and it collects keys by parsed structure or by exact allowed line shapes rather than by a name regex, since a quoted YAML key is invisible to `[\w-]+` while being the same key to any parser. Each of the three is exercised as its own member, red before and green after (three directions).
  6. The committed bundle contains the counts and the manifest sha256, and that sha256 matches `gates.manifest.json` at the evidenced commit. **Every value in the bundle is derivable from the bundle** (CR-602): a grep-and-recompute pass over the committed records reproduces every reported figure, and no record carries a hand-written `observed` value that the bundle cannot re-derive.
  7. `node --test` exits 0 with 0 failing and zero unaccounted tests; the behavior registry criterion of section 1.4 holds.
- suggested model tier: cheaper tier acceptable (scripting against contracts fixed by M2-P1 to M2-P8).
- citations: blueprint section 13 as amended by SC-011; v1 section 5; v1 M1-P6 (harness pattern and falsifiability criterion); `clean-room-m1-p6-hazard.md` CR-602 and CR-604; `clean-room-m1-p6-round4-hazard.md` CR-720 to CR-722; `delivery/verification/cr-661-orchestrator-reproduction.md`.
- conflicts-with: M2-P1 and M2-P8 (`.github/workflows/gates.yml`, `scripts/`), plus the shared files of section 1.4.
- blocked-by: M2-P1 to M2-P8 merged.
- parallelizable: **no.** It runs last by grounding, after all seven concurrent phases have merged (M2-D-19).

---

## 4. M2 exit test

Run by the orchestrator under the current process after M2-P8 has merged, **on the M2-P9 pull-request head with `--base main --head HEAD --phase M2-P9`**. Revision 0 ran it on `main`, where the runner has no base, no head and no phase, so the three diff-scoped gates could not evaluate at all and section 4's own claim about how many gates this repository cannot satisfy was wrong (M2R-003). The blueprint's exit condition, "all gates run green in CI on the kernel repo itself", is satisfied by the CI run of a phase pull request; `main` is not that state.

**Revision 1's "with the owner present for the one owner-held step" is superseded 2026-08-05 by DR-0015 and DR-0016.** The owner is not an approval step anywhere in execution, milestone boundaries included, and being asked a question whose answer was already obvious is a failure of the system rather than a courtesy. Two things survive that change and one does not:

- **The exit test remains a HARD GATE**, and M3 may not start before its evidence is committed on `main`. DR-0015 removes a click, not a gate.
- **Its evidence still goes to the owner in full, unasked.** That is a reporting obligation, not an approval.
- **E5 is not an approval and never was.** It needs a credential the orchestrator does not hold, which DR-0016 keeps expressly owner-reserved as elevated access. If A-3 has not been provisioned, the milestone exits with that clause outstanding rather than waiting on it.

Procedure:

- E1. Preconditions: the M2-P9 head with M2-P1 to M2-P8 merged; `npm ci` and `npm run build` exit 0. Recorded with exit codes.
- E2. `scripts/m2-exit-test.sh <dir>` runs the PR bundle and exits 0.
- E3. Bundle assertions against section 1.4's PR-bundle column: every listed gate has exactly one record with the expected status; every required gate is green; every not-applicable record names its reason and its evaluation evidence, with the three not-applicable states of M2-P7 step 7 distinguishable rather than collapsed; zero `error`; zero vacuous; recomputed counts equal `summary.json`.
- E4. The weaker main bundle is run and recorded, with the three diff-scoped gates explicitly not run rather than reported not-applicable.
- E5. Owner-held CREDENTIAL step (not an approval, DR-0016): with the A-3 implementer token supplied for the duration, `credential-token` executes and is recorded green, with the captured API response verbatim and evidence that no pull request was created. If A-3 has not been provisioned, it is recorded `not-applicable` naming A-3, the milestone exits **with one owner-side clause outstanding** carried in `delivery/STATE.md`, and it is never recorded green. The exit test does not wait on it.
- E6. `--self-test` is exercised and exits nonzero on both fixtures. Recorded.
- E7. The evidence bundle is committed at `delivery/evidence/m2-exit-test/` and reaches `main` through a pull request. M3 may not start before that commit is on `main` (hard milestone gate).

What this exit test proves: that on the M2-P9 head, ten manifest entries are declared, invoked as subprocesses and reported in one validated record shape; that every required gate examined at least one unit and returned green; that the two release-verification entries said so explicitly with their reason named and were counted separately; that no gate reported green having examined nothing; that the assertion code fails on both injected failure shapes; and that **eleven of the twenty-two defects and defect classes in section 1.5 now have a mechanical guard with a named criterion** (seven outright, four in part).

**What a green bundle is NOT, stated here because T-007 requires it (M2-D-18).** "All acceptance criteria met" must not be expressible as a terminal green, and neither may "all gates green". A green bundle is ONE input to a phase's assurance, alongside two review contracts of which one walks the criteria and one is given the phase's declared hazard class. T-007's evidence for this is not theoretical: two reviewers on different model families walked all fifteen of a phase's acceptance criteria by direct execution, agreed on every mechanical fact, and one of them found a high-severity defect that live-locked every supervision command, because the criteria did not describe it. **Gates inherit the criteria's blind spots.** A reader who treats section 4's green bundle as a statement that M2's phases are safe has made exactly the error T-007 records, and nothing in this milestone can stop them except this paragraph and section 1.5's eleven uncovered rows.

What it does not prove, recorded rather than assumed away:

1. Nothing about a project with a live release to verify. Both entries are exercised against stub adapters and against real captured platform responses replayed from `test/fixtures/release/`, and against this repository's not-applicable path; **no attempt of any adapter reaches a real platform during M2**, and first contact is M4's pilot wiring. Specifically not proven: any platform's non-success status vocabulary, since no failed deployment record was captured and none is invented; whether `state` or `readyState` is authoritative where they differ; whether a deployment can be READY without being the thing users reach (the capture shows an `aliasError` field, healthy and null, which suggests the distinction exists and does not establish it); the kernel's maximum deadline and default poll interval, which are numbers settled by measuring real releases; and whether the release-object-older-than-the-merge check should be an error rather than a recorded observation. Where the applied-migration inventory exposes no checksum, content drift is undetectable and the record says so per run.
   1a. **The two release-verification entries are not-applicable in every bundle for a STRUCTURAL reason, not a local one.** They are post-merge checks appearing in pre-merge bundles, so no bundle any repository could produce would exercise them (section 1.4's note, investigation observation O-3). Their exercise happens in tests and at M4, never in the milestone's own evidence.
2. Nothing about assurance modes; mode selection and the registry that carries it are M3's registry phase.
3. Nothing about merge-time red-witness re-verification as an ENFORCEMENT; the harness takes a baseline parameter and the merge-time gate is M5. What the milestone does obtain, from DR-0004's ruleset rather than from anything M2 builds, is that every phase's gates ran on a head current with `main` (M2-D-08's replacement justification). **What it does not obtain, and this matters more now that seven phases run concurrently: nothing detects semantic overlap between two phases that are BOTH still in flight**, since the conflict pre-pass sees files and not behaviour. That is exactly the property DR-0011 says parallelism waits for M5 to get, and M2 runs without it deliberately (risk 9).
4. The credential-scoping proof is two-part: the capability check runs on every pull request; the token's own incapacity is witnessed once, by the owner. Branch protection is DR-0004 items 2 and 3 plus M4's R-064.
5. A scope audit is only as good as the declaration it reads. The auditor refuses a phase branch with no merge-base declaration, but a branch not matching the phase pattern is not-applicable, which paperwork branches need and which is the one path by which source could reach a pull request unaudited.
6. Citation linting is diff-scoped (M2-D-10); a stale citation in an untouched document is found by M2-P5's one-shot inventory, not by the standing gate.
7. Gate behavior on a codebase unlike this one is unwitnessed until M4.
8. **Section 1.5 row 7 residue**: the suite wrapper catches a *registered* behavior losing its test, not logic that was never witnessed in the first place. F-3's class is only partly mechanized.
9. **Section 1.5 row 8**: error-path correctness (a thrown error bypassing rollback, an inaccurate printed recovery route) is caught by no M2 gate and stays with the L3 clean-room reviewer until M3's probe lists.
10. **Section 1.5 rows 9 and 13**: silent permanent failure with a green suite and a fresh beacon, and hostile-input hangs, are caught by no M2 gate. These are the two most severe defects M1 produced, and no script placement under blueprint section 1 makes M2 able to catch them. Their nearest structural answer, the mechanism index that would have carried M1-P3's loud-failure rule to M1-P5, is M3's and is delivered by M3-P6 and M3-P8 (section 2 item 10); the M2 evidence bundle says nothing about it either way.
11. **Section 1.5 rows 11 and 12**: honesty defects in prose (a false untestability claim, a deviation undercount) are caught by no M2 gate and land with M3's report and work-history contracts.
12. **Section 1.5 row 14**: M2-C-6 proves that M2's own gates do not block on a path they did not create. It proves nothing about the twelve M1 instances of that class, of which `teardown`, four `lock` subcommands and `spawn` remain open in `src/lock.ts`, `src/pool.ts` and `src/brief.ts`. Those are M1-P3 and M1-P4 files, no M2 phase owns them, and the carried-forward item in `delivery/STATE.md` is their only owner. A reader must not take M2's green bundle as evidence that the class is closed.
13. **Section 1.5 row 15**: nothing here catches an incomplete derivation. CR-560 was a twelfth path found after a round convened to enumerate the class, and the counterfactual is CLAUDE.md's fix-round contract (publish the derivation command and its output, state what it did not cover), which is a prompt-level rule checked by a reviewer.
14. **Section 1.5 row 16**: the suite and witness gates record a measured red rate honestly; neither constructs the concurrency that produced the `atomicWrite` wake loss. Finding a defect of that shape still requires a hazard reviewer building real contention.
15. **Section 1.5 row 18**: `suite` pins the reporter format and refuses a zero-unit green for THIS repository's suite. No M2 gate examines a seeded or downstream project's suite, which is where CR-604 actually lived, and that gap closes at M4.
16. **Section 1.5 rows 19 and 20**: a bundle record can be schema-valid, internally consistent and false about what the run then did (CR-680), and a prose count can contradict the machine count sitting beside it (CR-580, CR-565). Both land at M3's report contract. M2 supplies the authoritative numbers and reads no prose.
17. **Section 1.5 row 21**: no M2 gate checks an impossibility, coverage or remedy claim. M2-D-16 records the measurement behind that decision rather than asserting it, and the seventh recorded instance of that class was produced by the orchestrator after the norm was documented, which is the evidence that a reminder does not fix it and that M3's record contract must.
18. **Section 1.5 row 22, and the largest of these**: acceptance criteria are a specification of intended behaviour and not of the failure surface, so gates built to make criteria checkable inherit their blind spots. The declared hazard class per phase is M2's answer and it is a review input, not a gate. It is unwitnessed by this exit test by construction.

---

## 5. Decisions taken in this plan (flag if you disagree)

- **M2-D-01 (phase decomposition).** v1 section 5 outlines seven M2 items; this plan delivers nine phases: the gate contract and runner are separated (M2-D-02), the scope auditor and citation linter are separated (distinct inputs and failure modes), and the exit harness is its own phase as in M1-P6. No requirement row moves and no outline item is dropped. **v1 Appendix A's parenthetical M2-Pn notes refer to v1 section 5's outline numbering and are superseded by this plan's Appendix A (R-015b's "(M2-P1)" is now M2-P2, R-048's "(M2-P2)" is now M2-P3, R-020's "(M2-P3)" is now M2-P4, R-023's "(M2-P4)" is now M2-P6, R-008's "(M2-P7)" is now M2-P8, and R-094's "manifest seed M2-P5" is now M2-P1); the orchestrator updates those parentheticals in the same revision that approves this plan** (M2R-019).
- **M2-D-02 (a spine phase before the gates).** The gates must emit comparable records for the exit test to count them, and the runner and pin must exist before the first gate is wired. M2-P1 delivers the contract, manifest, runner, pin, exit codes and CI wiring. The blueprint's "red-witness harness (first)" is honoured where it matters: red-witness is the first gate.
- **M2-D-03 (parallelism: sequential by default). SUPERSEDED 2026-08-05 by M2-D-19 and `delivery/plan/m2-conflict-pre-pass.md`.** Kept in full because its reasoning is the reasoning M2-D-19 had to answer. As written: revision 0 claimed DR-0011 authorizes seven parallel M2 phases. It does not: DR-0011's decision section authorizes M1-P4 implementation, M1-P6 in parallel on disjoint files, and detailed planning of M2 and M3, with the recommendation "scoped to M1-P6 only"; and its condition 1 cancels a parallel start on **any** files-to-touch overlap, which `gates.manifest.json` and `test/behaviors.json` create on every pair (M2R-013). **M2 therefore runs sequentially.** Two costs make that the right default anyway: DR-0012 requires two cross-model reviews per pull request, so N concurrent phases is 2N concurrent reviews, and DR-0011's own evidence says review throughput is this project's constraint. What further authorization would be needed for concurrency, stated plainly: a new owner decision extending DR-0011 to M2 implementation phases with an explicit append-only carve-out for the two shared files, bounded at two phases in flight, with the pairwise check recorded per pair as condition 1 requires. This plan does not assume that decision (section 6 item 4).
- **M2-D-04 (schema validation technology).** A minimal in-repo validator over a closed keyword set that fails loudly on any keyword it does not implement. M1 shipped zero runtime dependencies (D-3); the library question is DR-0013, DECIDED 2026-08-05 for Ajv at M3's schema work, with M2's module boundary as the seam; M2's own scope is unchanged.
- **M2-D-05 (where M2 schemas live).** Under `src/gates/schemas/`, because CLAUDE.md reserves root `schemas/` for M3.
- **M2-D-06 (run pinning is Layer 1, delivered in M2-P1).** T-004's structural consequence is deterministic and scriptable. Revision 0 placed it in M2-P2; M2R-010 is right that two gates consume it and that a phase cannot depend on a module another phase owns, so it is spine. It discharges T-004 lesson 3 and does not reproduce T-004's incident, which M2-C-4 abolishes.
- **M2-D-07 (gates are subprocesses named by the manifest).** Only M2-P1 edits `src/cli.ts`; each later phase adds its own module with a main guard and one manifest entry. This follows the blueprint's framing that every toolbelt boundary is a subprocess with an exit code, and it is what makes a hand-written record file the realistic dangerous state for M2-P9's self-test.
- **M2-D-08 (no merge-time witness gate in M2). Conclusion unchanged at revision 2; its justification is REPLACED, because M2-D-19 falsified the old one.** The latest-main target stays a harness parameter rather than an enforcement, and the enforcement stays M5's. Revision 1's reason was "M2 runs no parallel phases and has no merge-time hook"; M2 now runs seven concurrent phases, and semantic overlap between concurrent phases is precisely what DR-0011 says the M5 merge-time gate exists to catch. **The replacement reason is that the property is already obtained from a mechanism that exists and has already fired**: DR-0004's ruleset requires each branch to be current with `main` before merging and requires CI green on that exact head, witnessed refusing a stale-branch merge on 2026-08-05, so every `red-witness` run happens against a head carrying every previously merged phase. Building a second mechanism for a property the ruleset already enforces would be machinery for a state this milestone does not reach. **The residue is named rather than absorbed**: detecting semantic overlap between two phases BOTH still in flight, before either merges, is not obtained by the ruleset and is not built here (risk 9).
- **M2-D-09 (`npm test` stays a bare runner).** The wrapper becomes the gate and gets `npm run gate:suite`; `npm test` stays `node --test` so a wrapper defect cannot make the suite unrunnable.
- **M2-D-10 (citation linting is diff-scoped).** A whole-corpus gate would red the merged history on day one and tempt an implementer to edit documents outside its phase. The corpus is inventoried once, in M2-P5's work history, and findings go to the orchestrator.
- **M2-D-11 (R-032's enforcement half lands at M4).** M2 builds the verifier and its verdict record; the dispatch block is wired where a dispatch loop and a real deployment exist. Recorded as residue in Appendix A, following v1's PR-201 precedent.
- **M2-D-12 (no M2 phase edits CLAUDE.md).** Revision 0 had M2-P1 and M2-P3 amending the binding-conventions record while merging under DR-0012, whose clause 5 they redefined, with orchestrator self-authorization as the fallback. M2R-006 is correct that this is circular: the beneficiary cannot rule on its own authority, and CLAUDE.md's Never list forbids reopening a decided record. The circularity is removed at the source: `gates.manifest.json` and witness specs are declared per phase rather than added to CLAUDE.md's standing extras, so DR-0012 clause 5 stays true as written and the standing extras remain two. The human-facing gate list update is owner item O-2 and blocks no phase.
- **M2-D-13 (credential scoping is an allowlist, and pointers are redirected, not dropped).** A denylist fails the first time a credential arrives under an unenumerated name. Dropping `HOME`, `GH_CONFIG_DIR` or `GIT_CONFIG_GLOBAL` is worse than useless because it returns the child to the default credential stores and undoes v1 M1-P2's EXT-F-02 discipline; they are redirected to harness-owned empty paths instead (M2R-004).
- **M2-D-14 (witness specs are durable, repository-level, and declared per phase).** They live at `witness/<behavior-id>.json` so a witness can be re-evaluated after the phase that wrote it (M2R-002), and each phase lists the specs it adds as literal paths in its own files-to-touch rather than growing the standing-extras list, which keeps the scope audit tight and DR-0012 clause 5 intact.
- **M2-D-15 (a gate does not rest on a declaration where the fact can be derived).** Witness class and the captured-output obligation are derived from the diff and from the manifest's `destructiveCommands`, and the dangerous state must intersect the diff. Where a fact genuinely cannot be derived (the `deterministic` claim), the gate demands measurement rather than accepting the assertion. This is the general form of M2R-001 and it is why section 1.5 rows 1 and 2 are covered rather than opt-in.

Decisions M2-D-16 to M2-D-20 are new at revision 2 and are the substance of the re-grounding.

- **M2-D-16 (the claim grep is NOT mechanized as an M2 gate, and the decision is measured rather than asserted).** T-006 records seven instances across M1 of impossibility, coverage and remedy claims written without the construction that would settle them, one of them the orchestrator's own and produced after the norm was documented. CLAUDE.md now carries a binding pre-submit claim grep. The question this re-grounding was asked is whether any M2 gate can check it mechanically. **A gate can enforce a proximity rule** (each grep hit carries an adjacent captured command, or is restated as an open question) and cannot judge whether the cited command settles the claim. So the question is the cost of the proximity rule, and the plan measures it rather than guessing, which is the same discipline M2-P2 step 6 applies to re-evaluation cost. **Measured 2026-08-05 over the six M1 work histories** (`grep -cEi 'cannot be|impossible|needs a|is covered|catches|would catch|recovers|anyway|always|never|no way to'`): m1-p1 2 hits in 74 lines, m1-p2 4 in 88, m1-p3 35 in 375, m1-p4 27 in 459, m1-p5 41 in 1225, m1-p6 6 in 391. **Inspecting the FIRST TWENTY of m1-p5's 41 hits** (the sample actually examined, stated as a sample because the remaining 21 were not read, and because writing "the 41 hits show" would be the eighth instance of the very class this decision is about): seventeen of the twenty are incidental prose ("never block", "guard() never raises", "always transient", a witness described as "the seen record never advanced"), not claims about the world. **That is a sample, not a census**, and the honest statement of what it supports is that the false-positive rate is high enough to decide on and has not been measured exactly. **A gate would therefore demand evidence or annotation on roughly forty lines per phase of which a handful are real, and a gate that fires mostly on innocent lines trains its operator to ignore it**, which is the same failure the release-verification investigation names for a verification that is structurally red on the happy path. **Decision: M2 builds no claims gate.** T-006 itself routes the structural answer to M3's work-history and record contract, which is the right home, because there the check attaches to a DECLARED claims section with a schema rather than to a regex over free prose, and the false-positive surface disappears. What M2 does take from T-006 is its one named M2 consequence, at M2-P2 step 5. Recorded as section 1.5 row 21 and section 4 not-proven item 17.
- **M2-D-17 (one witness is not a class, so a class witness declares at least two structurally different dangerous-state members).** CLAUDE.md carries this as a binding rule; revision 2 makes it a gate rule at M2-P2 rule (g), because it is derivable rather than judgeable. The evidence is six confirmed instances across four M1-P6 rounds in which a registered witness reddened against deletion of the asserted text and stayed green against a one-line edit preserving the text and inverting the behaviour: `exit 1` to `exit 0` (reproduced independently by the orchestrator with captured output), two placements of `|| true`, a step-level `if: false`, a quoted YAML key invisible to a `[\w-]+` whitelist, and the guarded step moved into a job the fan-in does not consume. **Revision 1's rules (a) to (f) admit all six, derived by reading each rule against each recorded instance and not by execution**, which is stated as the reasoning it is because neither the rules nor the witnesses are code yet. The derivation: each recorded instance's witness declares a dangerous state that DOES intersect the phase diff (rule d passes), is not a destructive-command witness (rule e is silent), touches no subprocess-spawning module in a way that triggers the capture obligation (rule f is silent), and reddens honestly against the member it declares (rules a to c pass). If a reviewer can construct one of the six that a revision 1 rule already refuses, rule (g) is narrower than claimed and this decision should be re-argued. This is the single largest capability revision 2 adds, and it converts section 1.5 row 17 from uncaught to caught. The `dangerousState` field becomes `dangerousStates`, an array, so the rule has something to require rather than something to bolt on.
- **M2-D-18 (every phase declares a hazard class, and no aggregate of gates is a completeness claim).** T-007 records a phase meeting fifteen of fifteen executed acceptance criteria, walked by two reviewers on different model families who agreed on every mechanical fact, while live-locking `doctor`, `spawn`, `teardown` and both watcher modes; the approving review does not contain the word `readBeacon`, because no criterion covers that path. **Criteria specify intended behaviour and not the failure surface**, and the reviewer who found it differed in its BRIEF, not only its model. Two consequences are taken here. First, **every M2 phase section carries a `hazard class` field** beside its acceptance criteria, so the second review contract is derivable from the plan rather than improvised per dispatch, and so it is also a pre-submit self-review for the implementer. Second, T-007's explicit instruction to M2, that "all acceptance criteria met" must not be expressible as a terminal green: no M2 gate takes criteria as an input, none ever will, and section 4 states in the plan's own voice that a green bundle is one input rather than a verdict. The hazard classes below are the plan's, not the implementer's, and an implementer who believes one is wrong escalates rather than rewriting it.
- **M2-D-19 (M2-P2 through M2-P8 run concurrently after M2-P1; supersedes M2-D-03).** The recorded pairwise files-to-touch disjointness check DR-0011 condition 1 demands now exists as `delivery/plan/m2-conflict-pre-pass.md`, and it establishes that nothing between P2 and P8 grounds on anything between P2 and P8, and that the six created-file sets are pairwise disjoint. M2-D-03's blocker was never the graph; it was that `gates.manifest.json` and `test/behaviors.json` create an overlap on every pair, and condition 1 cancels a parallel start on any overlap. **The resolution is that both are append-only registries keyed by name and are resolved as a union against the true merge base**, which was performed successfully once already during M1-P6's branch update (145 keys plus 7 added, zero overlap, nothing removed), not that the overlap is waived. **Authority, stated rather than assumed, because M2-D-03 said this needed an owner decision.** Under DR-0016 an agent escalates only when two or more options are genuinely comparable AND the consequence is high impact and costly to reverse. The second condition fails on DR-0011's own reversibility field for exactly this question: "a parallel phase can be stopped and re-sequenced at any time; the cost of being wrong is merge conflict and rework, not lost work". The owner's recorded instruction on the matter is "go as much parallel as possible", applied at its widest. So this is the orchestrator's decision to take and record, and M2-D-03's proposed owner decision is withdrawn rather than raised (section 6 item 4). The measured prize is roughly 14 hours against 36 on M1's measured per-phase cost. The cost is real and is stated in risk 9: seven phases landing together is seven arbitrations and seven merge sequences, and DR-0004's ruleset requires each branch current with `main`, which serialises the merges themselves.
- **M2-D-20 (release verification is one contract with two registry entries; the kernel owns the subject and the clock; adapters are subprocesses; extraction leaves the interface).** Taken from DR-0014 and the investigation it commissioned. The four load-bearing parts: **one contract**, because everything contract-level is shared between deploy and migrations and the only differences are inside the adapter, while **two registry entries** stay because a project must be able to declare one and not the other and because collapsing them would hide "migrations were not checked" inside a green aggregate, which is the M2-C-2 vacuous-green hazard in a new hat; **the kernel owns the subject, the clock, the loop and the outcome-to-status mapping**, and `pending` and `absent` are loop states that go red at the deadline and never green; **adapters are executables spawned as subprocesses returning validated JSON**, including the kernel's own reference adapters, so no privileged path exists and the per-attempt timeout is enforceable rather than promised under C-3; and **extraction moves out of the interface**, so an adapter returns an outcome and never a `statusPath`, which is what makes a third-party adapter possible at all and what dissolves revision 1's criterion 3 rather than patching it. **Why this is the agent's decision to take under DR-0016**: DR-0014 decided the direction and commissioned the investigation, the investigation produced a defended recommendation on every point above, and a question with a recommendation the agent would defend is not a genuine tie. The two genuinely open questions it surfaces are recorded as owner items in section 6 rather than decided here.

---

## 6. Open questions and owner decisions

Per D-7 an open question is a decision record with status open. None of these blocks M2-P1's dispatch; the milestone gate (M1 exit evidence on `main`) does. **Under DR-0016 this list is deliberately shorter than revision 1's**: a question carrying a recommendation the agent would defend is the agent's to take and record, and two of revision 1's items are withdrawn on that basis rather than left standing as owner work.

1. **Owner action A-3 (DR-0004 item 4): provision the scoped implementer token.** This is elevated access the orchestrator does not hold, which DR-0016 keeps expressly owner-reserved, and it is not an approval step. Proposed setup: a fine-grained personal access token, repository-scoped to `ThomasHendrickx/tiphys-ai-helmsman` only; repository permissions Contents read and write, Metadata read, **Pull requests: no access**, Actions, Workflows, Administration and Secrets: no access; shortest expiry covering M2; supplied to the exit run as `TIPHYS_IMPLEMENTER_TOKEN` and **not** placed in CI secrets, because CI witnesses the capability check rather than the token. **Update at revision 2: the DR-0004 ruleset is now LIVE and was witnessed refusing a merge on a stale branch (2026-08-05, `delivery/STATE.md`), so only item 4 remains.** One thing still needs confirming alongside the token and is not established by that witnessing: that the ruleset has **no bypass actors**, since a token that cannot open a pull request but can push to `main` defeats the point. Blocks the E5 clause of the exit test and M2-P8 criterion 7; nothing else, and the exit test does not wait on it.
2. **Owner item O-2 (documentation, non-blocking): CLAUDE.md's human-facing gate list.** Once M2's gates exist, CLAUDE.md's three-gate list is stale. No M2 phase edits it (M2-D-12). The orchestrator raises it as a documentation pull request for owner sanction, because CLAUDE.md is the record of the binding conventions and DR-0012's limits reserve that class. If the owner is unreachable, the manifest remains the machine truth and the stale prose is recorded in `delivery/STATE.md` rather than edited under delegated authority.
3. **CLOSED at revision 2. Was: DR-0014, decided 2026-08-05, release verification is a PLUGGABLE INTERFACE, not a platform selector.** This item originally asked which platform the deploy verifier's first adapter targets. The owner rejected the framing: one instance may need a Vercel check, another Hetzner, another a local project with no remote release, another a mobile app. DR-0014 decided the direction in principle and commissioned an interface investigation, which has now reported (`delivery/verification/release-verification-interface.md`) and is absorbed as M2-D-20 and the rewritten M2-P7. Nothing here is left open for the owner: the investigation produced a defended recommendation on every design point, and under DR-0016 that makes it the agent's to take and record. Revision 1's conditional branch ("with a reachable platform the poller ships with captured-response semantics; without one it does not ship at all") is superseded: real captures exist, and what is genuinely uncaptured is enumerated in M2-P7 step 12 rather than deferred wholesale.
4. **WITHDRAWN at revision 2, not raised. Was: a decision record extending DR-0011 to M2 implementation phases.** M2-D-03 proposed asking the owner for concurrency authority. Under DR-0016 that question fails the escalation test on its second limb: DR-0011's own reversibility field for this exact question reads "a parallel phase can be stopped and re-sequenced at any time; the cost of being wrong is merge conflict and rework, not lost work", and the owner's recorded instruction is "go as much parallel as possible". A question with a recommendation the agent would defend, over a reversible consequence, is a failure of the system to ask. Taken as M2-D-19 and recorded there with its authority and its costs.
5. **DR-0010** (harness orchestration primitive, due at M4) is open and blocks nothing in M2. **DR-0008 is no longer open**: it was decided 2026-08-05 for public npmjs under `@tiphys`, with `@tiphys/kernel` and `@tiphys/claude-code-plugin` (`delivery/STATE.md`), and revision 1's listing of it as deferred is superseded. It still blocks nothing in M2.
6. **NEW at revision 2, and the one genuine owner decision this re-grounding surfaced: release-verification credentials where a platform offers no read-only scope.** A verifier reads, so the principle is obvious and needs no owner. What is not obvious, and what the investigation flags explicitly as a genuine owner decision rather than a design detail (section 6.2 item 1), is what happens when a target platform issues only account-wide or team-wide tokens with no read-only scope: **accept a write-capable credential in the orchestrator's environment, or declare that project's verification `none` with a reason.** Both are legitimate, they are genuinely comparable, and the consequence is a standing credential exposure, so this meets both limbs of DR-0016's test. It is raised here and **due at M4's pilot, not now**, because no platform is targeted during M2 and the orchestrator holds no such credential. Two supporting rules the investigation recommends and this plan adopts without owner input, because each has one defensible answer: a named credential that will not resolve is `error` rather than a silent unauthenticated request, and release-verification credentials never reach a spawned implementer, by the same reasoning that keeps pull-request credentials away from them (R-008).
7. **Vetoable plan decisions**: none remaining. Revision 0's M2-D-12 was vetoable because it edited CLAUDE.md; it no longer does. M2-D-16, M2-D-19 and M2-D-20 are decisions this re-grounding took under DR-0016 rather than raising, each recorded with its reasoning and its evidence so the owner can reverse any of them by reading rather than by being asked.

---

## 7. Risks

Ordered by the question the M1-P3 lesson forces: does M2 reach the state this component guards, and can its tests be red against the dangerous state?

1. **M2-P7 remains the component most likely to repeat M1-P3, and revision 2 changes the shape of the risk rather than removing it.** The milestone still never reaches the state it guards: no release to verify, no charter, and no project will have one until M4. What improved: real captured platform responses now exist (`delivery/verification/release-verification-interface.md` appendix A), so the fixtures are byte-derived from observations rather than from a self-written stub, which is what T-003 lesson 4 demands; and the contract, the loop and the seven fail-closed rules are testable end to end with no external network, so most of the phase is genuinely witnessable. What did not improve, and is now the concentrated form of the risk: **no attempt of any adapter reaches a real platform during M2**, no non-success state of any platform was ever observed, and the phase deliberately ships no failure vocabulary. The residual hazard is therefore no longer "the semantics are invented" but "the conservative rule is untested against a real failure", and the mitigation is that unknown never becomes green: every unrecognized value is recorded verbatim and treated as pending until the deadline, at which point it is red. That is slower to fail than it could be, and it is the correct trade until a failure record is captured. The migration adapter stays in M2 because its inputs are local and its drift semantics are checkable, now including the applied-versus-repository asymmetry that a real capture established.
2. **M2-P2 is the gate that judges every other test.** Revision 0's version could be defeated by omitting a field, which is the same shape as the defects it is named after; rules (d) to (f) close that. Residual: the derived-capture rule uses a grep over changed files, which is deterministic but coarse and will demand a capture in cases where a module spawns a subprocess unrelated to the behavior under test. That cost is accepted deliberately, because the alternative is the opt-in that let V-2 through, and the implementer's recourse is a real capture or an escalation, never a quiet omission. The stored-witness re-evaluation has a cost this plan refuses to guess: step 6 measures it and escalates rather than narrowing it silently. **Rule (g) adds a second residual of the same kind, stated rather than discovered**: it doubles the run cost of every class witness and of every text-asserting witness, since each member is applied, run and recorded separately, and its text-preservation check is a re-run of the named tests' own greps against a mutated document, which is mechanical but not free. The plan does not guess that cost either: **M2-P2 step 6's measurement and escalation rule covers the whole harness, rule (g) included**, and an implementer who finds the corpus too slow escalates rather than weakening the rule. Weakening rule (g) specifically is what would restore the six-instance class it exists to catch, so it is named here as the wrong lever to reach for.
3. **M2-P8 is the security-relevant phase and it edits a merged M1 seam.** Its token assertion depends on external API status codes captured at build time, which can rot; step 7 requires capture and escalation rather than reshaping. Redirecting `HOME` for spawned children is a real behavior change for the M1 exit-test payload, which is why criterion 5 witnesses the harness end to end rather than asserting no regression. Note also that the phase is where an implementer is most tempted to satisfy a red gate by widening the allowlist; the allowlist is enumerated in the module and is on the scope-audited file list.
4. **Input-shape duplication with M3** (phase declaration, coverage inventory, findings table). If M3 invents different shapes, M2's gates need rework in M3. Mitigation is procedural: the orchestrator reconciles before M3 dispatch, with section 2 as the flag. The M3 draft has already adopted the plan-schema superset recommendation, which is the largest of the three.
5. **Every pull request now runs ten manifest entries**, two of which execute suites, with M2-P9 making the harness the single caller so the set runs once rather than twice (M2R-026). CI time and review time both grow, and a flaky gate taxes every future run.
6. **The exit test could pass vacuously**, which would be the most ironic failure available. Mitigations are structural: M2-C-2 makes a units-0 green an error, `required` applicability makes a silently not-applicable gate a failure, zero applicable gates is an error, and `--self-test` exercises the assertion code against fixtures rather than against a shipped override.
7. **Planning against an unfinished M1, narrowed at revision 2.** M1-P5 is now MERGED at `58ac964`, so M2-P2's and M2-P8's consumption of the delivered watcher-era contracts, and M2-C-6's consumption of `classifyEntry`, are against real code rather than against a plan. **M1-P6 is still unmerged and is in its fourth hazard review round**, so M2-P8 step 5 and M2-P9's harness pattern remain as-planned, and M1-P6's shape has changed materially across those rounds. M2-C-1 converts that into a verification-first step in every affected phase, but a large divergence in the delivered exit harness forces a plan revision before M2-P8 and M2-P9 dispatch. Revision 0 was written before M1-P4 merged and before T-005 existed, which is exactly how the section 1.5 gaps got in; **this revision is the discharge of that exposure for M1-P5, and the exposure to M1-P6 remains and must be re-checked at dispatch.**
8. **Eleven of twenty-two recorded defects and defect classes are not mechanized by this milestone** (section 1.5), including the five most severe. The risk is not that M2 fails to catch them; it is that a later reader treats a green gate bundle as coverage, which is exactly what T-007 records happening to a criteria-walking review. Section 4's not-proven items 8 to 18 exist to make that misreading cost something, and the M3 plan must claim each named owner or they become orphans. One of the two M2R-018 raised is now closed from both sides: M3 revision 1 claims the mechanism index at M3-P6 and M3-P8 under D-M3-23 (section 2 item 10). The residue still open is the third half of the destructive-authority declaration, the implementer-brief clause, which section 2 item 11 hands to M3-P6 explicitly rather than assuming. **Note also that the proportion did not improve between revisions**: 7 of 13 became 11 of 22, and the defects M1 produced after revision 1 was written are disproportionately of the kind no gate catches.
9. **Concurrency's cost lands on the orchestrator, not on the implementers (new at revision 2, M2-D-19).** Seven concurrent phases is fourteen concurrent clean-room reviews under DR-0012, seven arbitrations and seven merge sequences, and DR-0004's ruleset requires each branch to be current with `main` before merging, which serialises the merges themselves and means the last few will each need a branch update as `main` moves under them. The measured serial resource is orchestrator arbitration, one per phase per round. The mitigations are in dispatch order rather than as an afterthought: stagger the seven dispatches so their reviews do not all land in one window, merge in grounding order, and expect the branch updates. The failure mode to watch for is not a merge conflict, which the union rule handles; it is an orchestrator arbitrating seven phases at once and doing each one less carefully, which is exactly how M1's fix rounds produced twelve of thirteen re-reviewed rounds carrying a new finding attributable to the round itself.
   **The second failure mode is semantic overlap, and it is the reason DR-0011 says parallelism waits for M5.** Two phases in flight can change related behaviour without touching a shared file, and the conflict pre-pass cannot see that, by construction. M2 gets most of the protection for free: DR-0004's ruleset forces each branch current with `main` and CI green on that exact head, so every phase's `red-witness` and `suite` runs execute against a head carrying every previously merged sibling (M2-D-08's replacement justification). What that does NOT cover is two phases both still in flight, where neither has yet seen the other. The mitigation available today is procedural and costs nothing: merge in grounding order, and treat the branch update each later phase needs as a re-verification rather than as a formality, re-reading its review's mechanical findings against the updated head rather than assuming they carry.
10. **Revision 2 is a re-grounding, not a review, and it added scope (new at revision 2).** M2-C-6, M2-P2 rule (g), M2-P3 criterion 11, M2-P9 criterion 5a and the M2-P7 rewrite all grew this milestone. Each is grounded in a recorded M1 defect or in a decided record, and each is named in section 1.6 so a reviewer can strike it cleanly rather than having to find it. **The honest risk is that a re-grounding agent is the wrong party to judge whether its own additions are worth their cost**, and adversarial review round 2 is where that judgement belongs. The specific additions most worth attacking are rule (g)'s run cost (risk 2), M2-P7's growth from two modules to five plus a fixtures directory, and whether M2-C-6 belongs in every gate or only in the two that walk trees they did not create.

---

## Appendix A: Requirements coverage

Every row v1's Appendix A places in the M2 bucket, mapped to exactly one M2 phase. Sixteen rows, sixteen mappings, zero orphans, zero parked. Counts by phase: **M2-P1 = 0, M2-P2 = 5, M2-P3 = 1, M2-P4 = 2, M2-P5 = 2, M2-P6 = 2, M2-P7 = 3, M2-P8 = 1, M2-P9 = 0. Total = 16.** Unchanged by revision 1.

| Row | Rule (abbreviated from the migration table) | Phase |
|---|---|---|
| R-008 | Implementer never creates PRs; enforced by token scope or branch protection | M2-P8 |
| R-010b | Citations in verification output must be real (file exists, line in range) | M2-P5 |
| R-015b | Repro redness is verified, not asserted | M2-P2 |
| R-020 | Files-to-touch is verified before editing and enforced after | M2-P4 |
| R-023 | Every input finding lands in a phase, decision, open question or parked-with-reason | M2-P6 |
| R-025 | Plan review re-verifies every file:line citation (same artifact, second call site) | M2-P5 |
| R-028b | Red-on-baseline proven mechanically at implementation | M2-P2 |
| R-032 | Next phase starts only after the previous merged and its deploy verified | M2-P7 |
| R-036 | Red-witness: red on baseline, green after, evidence emitted | M2-P2 |
| R-037b | The fake-repair red/green demonstration is mechanical | M2-P2 |
| R-048 | Full-suite wrapper with discovery-count parity; exit code is the truth | M2-P3 |
| R-056b | The revert check is computable | M2-P2 |
| R-058 | Scope audit: every changed file on the list or a declared extra | M2-P4 |
| R-068 | Verify migrations actually applied after every merge | M2-P7 |
| R-069 | Verify the production deploy reached READY before the next phase starts | M2-P7 |
| R-089b | Every-finding-has-an-outcome is checkable | M2-P6 |

Phases discharging no row: M2-P1 delivers the contract, manifest, runner and pin that all sixteen rows' components emit into and are invoked by; M2-P9 delivers the milestone's exit procedure. Neither corresponds to a rule extracted from the process doc, because the process doc has no rule about how its own gates are wired together; both are implied by blueprint section 13's M2 exit condition and justified in M2-D-02.

Residue notes (partial discharge, recorded rather than hidden, following v1's PR-201 precedent):

| Row | Discharged in M2 | Residue, and where it lands |
|---|---|---|
| R-020 | the enforcement half ("enforced after"), by the scope auditor | the "verified before editing" half is an implementer-brief duty at M3's delivery-role briefs phase (M2R-025) |
| R-032 | the deploy verifier, its verdict record keyed to the verified sha, and the documented consumption contract | the enforcement that blocks the next dispatch, wired at M4 with the pilot (M2-D-11) |
| R-068 | id-level comparison always, content-drift comparison where the applied inventory exposes checksums, and the applied-versus-repository asymmetry established by a real capture (a non-empty repository inventory with an empty applied inventory is red, never not-applicable) | content drift where the inventory command exposes no checksum, recorded per run and in section 4 not-proven item 1 (M2R-017); and which applied-migration inventory is authoritative per stack, which needs the pilot |
| R-069 | the verification contract, the kernel-owned loop and clock, the subprocess adapter boundary with its seven fail-closed rules, the record and its schema, the three not-applicable states, and two reference adapters with fixtures byte-derived from real captured responses (revision 2, DR-0014 and M2-D-20) | any platform's non-success vocabulary, which is not observed and is deliberately not invented; the maximum deadline and default poll interval, which are numbers settled by measuring real releases; and first contact with a live platform, all at M4 with the pilot (M2-P7 step 12) |
| R-089b | the parity checker plus its declared input contract | binding that input contract to the report schema at M3's reporting phase (section 2 item 2) |
| R-008 | the executor environment redirection and the capability check, plus the token probe | branch protection (DR-0004 items 2 and 3; R-064 at M4) and the token itself (owner action A-3) |

Cross-check against v1 Appendix A: the sixteen rows above are exactly the rows v1 marks M2. No row is added, removed, or moved to another milestone by this document, so v1's bucket totals (M1 11, M2 16, M3 74, M4 13, M5 1, parked 0) are unchanged. The one change this plan asks of v1 is textual and is stated in M2-D-01: the parenthetical phase ids in v1 Appendix A's M2 notes and its R-094 note refer to superseded outline numbering.
